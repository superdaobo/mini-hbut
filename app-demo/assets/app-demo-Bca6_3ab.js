const __vite__mapDeps=(i,m=__vite__mapDeps,d=(m.f||(m.f=["./more-modules-CfNfahoc.js","./runtime-bridge-BU9jeOXP.js","./Dashboard-UrJPaK7f.js","./vue-core-DWoFi2CM.js","./layout_collision_fx-7Q3nAaTI.js","./layout_collision_fx-B3wx6YZO.css","./app_icon-BoqTJkLh.js","./debug-tools-BgYILSb8.js","./capture-D-zd0oUS.js","./Dashboard-CT8zQsnF.css","./GradeView-N9gbvgbt.js","./time-DFqn0g8e.js","./TEmptyState-DID0vJFI.js","./GradeView-B8vksDuw.css","./ElectricityView-CLm3pg_r.js","./qrcode-BmGSs2uE.js","./static_resource_cache-BcKPBR_J.js","./one_code_open-Dn8ZBP9Y.js","./TPageHeader.vue_vue_type_script_setup_true_lang-EUTLkGHa.js","./ElectricityView-DQagoHjW.css","./ClassroomView-cWUS1oSK.js","./ClassroomView-C9qDaRAD.css","./ScheduleView-C4bOhmhT.js","./ScheduleView-CVDBoRBe.css","./GlobalScheduleView-D84YvdEU.js","./GlobalScheduleView-CzUDbCgG.css","./CourseSelectionView-BMRpc8XA.js","./CourseSelectionView-CCSwXVJY.css","./StudentInfoView-DV69XQmO.js","./StudentInfoView-_Mg1xMZF.css","./ExamView-BmaHbvFj.js","./ExamView-1r5FIS7X.css","./RankingView-Bs4h5R4V.js","./RankingView-DMtsSc11.css","./CalendarView-DH21TJJn.js","./CalendarView-BuykqbVb.css","./SchoolInboxView-MbHvX6_1.js","./school_inbox_content-CS2pL4iH.js","./SchoolInboxView-CM7vaOZp.css","./AcademicProgressView-DMsnKGje.js","./AcademicProgressView-6zVOUPB5.css","./TrainingPlanView-CFTILl1Z.js","./TrainingPlanView-DffaNhuW.css","./ForumView-9b3udUVk.js","./ForumView-X4dCE9r2.css","./MeView-CVCZvOoF.js","./MeView-B6x7Fl1Z.css","./OfficialView-BcgMJjXp.js","./OfficialView-2v_9pDeI.css","./FeedbackView-BV_i_ej-.js","./FeedbackView-BvjU0U9F.css","./NotificationView-7-zUa1H-.js","./NotificationView-D2cpcbPc.css","./ConfigEditor-DcA0XKzI.js","./markdown-BYynpwuf.js","./markdown-C5EaCmSh.css","./ConfigEditor-DNuwj4jl.css","./SettingsView-DVlvyPWH.js","./SettingsView-DR5pq2rF.css","./PrivacyDataView-DyYcj3qh.js","./PrivacyDataView-Du4-rzwG.css","./ExportCenterView-C0qwtQ_s.js","./ExportCenterView-DdewyUDE.css","./ServiceStatsView-sE71R7Th.js","./ServiceStatsView-DgkFbmGH.css","./SchoolWebsiteView-1SJXwiwj.js","./school_website_embed-CYHxPvBP.js","./SchoolWebsiteView-e2R0Z7NR.css","./QuickLinksView-_vWGY7Id.js","./QuickLinksView-CdShEdjx.css","./CampusNetworkView-2ujEcVie.js","./CampusNetworkView-CmRQZtFW.css","./MoreView-DF8C1Y3V.js","./TStatusBadge-eTRu_ujN.js","./MoreView-Cv_a1Lr-.css","./MoreModuleHostView-DWmzk142.js","./MoreModuleHostView-BDMqeuN3.css","./MoreChaoxingCheckinView-B7WqCfcF.js","./useGeolocation-Bwa1bREk.js","./MoreChaoxingCheckinView-kA3dXZZB.css","./TransactionHistory-D0jvFeZr.js","./TransactionHistory-BMprHFVv.css","./CampusCodeView-BG0C3oxN.js","./CampusCodeView-Bd1qW0fp.css","./AiChatView-Gv9wZdbP.js","./online-learning-D2Kpu1dg.js","./AiChatView-CQsooMgH.css","./config-Cs6HnoHt.js","./CampusGuideShell-Di1XqMxg.js","./towergo_map-DWW3wH7A.js","./CampusGuideShell-BqoCZy-m.css","./CampusMapView-CGTJE6ya.js","./walking_route_service-Dgwa-Ehl.js","./CampusMapView-E5WyI2Zl.css","./LibraryView-D5DIxlga.js","./LibraryView-DKNIqGjf.css","./ResourceShareView-Cji6UT-x.js","./ResourceShareView-C-aJauIc.css","./ChaoxingClassView-KMl9RxsT.js","./ChaoxingClassView-D02zFAzX.css","./ChaoxingHubView-5Vb_Jemu.js","./ChaoxingHubView-BMPJ7pbD.css","./ChaoxingInboxView-DpqaBG-F.js","./ChaoxingInboxView-C6qDx5xC.css","./TeachingEvalView-D5aVbcwj.js","./TeachingEvalView-BUFlKShU.css","./BroadbandView-BgTF-IOD.js","./BroadbandView-DUlZ4SwC.css","./SportsVenueView-CB9g1DLj.js","./SportsVenueView-_pGL1Zx6.css","./TowerGoView-D9BUe_PI.js","./TowerGoView-CF9YbzmZ.css","./SmartOrientationView-DeqBG5RH.js","./SmartOrientationView-CMlq_Myi.css","./IdentityAuthHistoryView-Yiwq0-3C.js","./IdentityAuthHistoryView-iMtUVCM_.css","./fontawesome-B9llV4TX.css","./solid-Bbzz84sQ.css"])))=>i.map(i=>d[i]);
var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
import { e as platformBridge, g as getNativeAppVersion, _ as __vitePreload, a as isTauriRuntime, d as invokeNative, i as isTestAccountSession, r as resolveTestAccountCachePayload, p as pushDebugLog, f as resolveTestAccountHttpResponse, w as writeSnapshotWithRetry, h as requestRefresh, j as clearSnapshot, k as writeElectricitySnapshot, l as writeExamSnapshot, m as writeWidgetThemeColor$1, n as normalizeBackgroundDetectedEvent, c as isCapacitorRuntime, o as getRuntime, q as detectRuntime, s as readNativeBinaryFile, t as toNativeFileSrc, u as isIOSLike, v as isDesktopLike, x as saveRememberedUsername, y as exitNativeApp, z as getCurrentNativeWindow, T as TEST_ACCOUNT, A as getTestAccountGrades, B as seedTestAccountCaches, C as clearRememberedUsername, D as clearTestAccountSession, E as isAndroidLike, F as markTestAccountSession, G as installMiniHbutDeepLinkListeners, H as resolveNotificationActionTarget, I as identityFetchAuthHistory, J as getIdentityDeviceDisplayName, K as getDebugLogs, L as formatDebugTime, M as initDebugLogger } from "./runtime-bridge-BU9jeOXP.js";
import { r as ref, o as onMounted, a as openBlock, c as createElementBlock, b as createBaseVNode, t as toDisplayString, F as Fragment, d as createCommentVNode, e as createTextVNode, n as normalizeClass, f as normalizeStyle, g as renderList, w as withModifiers, h as computed, i as reactive, j as createBlock, k as withCtx, u as unref, T as Transition, l as onBeforeUnmount, m as withKeys, p as createVNode, q as renderSlot, s as Teleport, v as watch, x as effectScope, y as defineComponent, z as provide, A as readonly, B as toRef, C as nextTick, D as defineAsyncComponent, E as defineStore, G as storeToRefs, H as createApp, I as createPinia } from "./vue-core-DWoFi2CM.js";
import { a as allowsInAppGithubUpdater, g as getUpdateCheckMode, D as DEMO_MODE_BANNER_ZH, b as DEMO_MODE_BANNER_EN, c as isCustomJavaScriptAllowed, i as isViewAllowed, s as shouldApplyAppStoreRestrictions, n as normalizeModuleHostSessionPayload, r as resolveModuleHostPreviewSource, d as canUseLocalModuleBridgePreview, e as isLocalModuleBridgePreviewUrl, f as isAppStoreBuild, h as getFeaturePolicy } from "./more-modules-CfNfahoc.js";
import { i as isNightModeEnabled, N as NIGHT_MODE_CHANGED_EVENT, h as hasBootMetric, m as markBootMetric, r as resetBootMetrics, a as initNightModeClass } from "./debug-tools-BgYILSb8.js";
import "./capture-D-zd0oUS.js";
(function polyfill() {
  const relList = document.createElement("link").relList;
  if (relList && relList.supports && relList.supports("modulepreload")) {
    return;
  }
  for (const link of document.querySelectorAll('link[rel="modulepreload"]')) {
    processPreload(link);
  }
  new MutationObserver((mutations) => {
    for (const mutation of mutations) {
      if (mutation.type !== "childList") {
        continue;
      }
      for (const node of mutation.addedNodes) {
        if (node.tagName === "LINK" && node.rel === "modulepreload")
          processPreload(node);
      }
    }
  }).observe(document, { childList: true, subtree: true });
  function getFetchOpts(link) {
    const fetchOpts = {};
    if (link.integrity) fetchOpts.integrity = link.integrity;
    if (link.referrerPolicy) fetchOpts.referrerPolicy = link.referrerPolicy;
    if (link.crossOrigin === "use-credentials")
      fetchOpts.credentials = "include";
    else if (link.crossOrigin === "anonymous") fetchOpts.credentials = "omit";
    else fetchOpts.credentials = "same-origin";
    return fetchOpts;
  }
  function processPreload(link) {
    if (link.ep)
      return;
    link.ep = true;
    const fetchOpts = getFetchOpts(link);
    fetch(link.href, fetchOpts);
  }
})();
const normalizeHttpUrl = (href) => {
  try {
    const url = new URL(href, window.location.href);
    if (!/^https?:/i.test(url.href)) return null;
    return url.href;
  } catch {
    return null;
  }
};
const isLikelyMiniProgramCode = (value) => {
  const trimmed = (value || "").trim();
  return trimmed.startsWith("#小程序://") || trimmed.startsWith("小程序://");
};
const extractMiniProgramToken = (value) => {
  const trimmed = (value || "").trim();
  if (!trimmed) return "";
  const clean = trimmed.replace(/^#/, "");
  const tokenMatch = clean.match(/\/([A-Za-z0-9_-]{6,})$/);
  return tokenMatch?.[1] || "";
};
const miniProgramCandidates = (value) => {
  const trimmed = (value || "").trim();
  if (!trimmed) return [];
  const clean = trimmed.replace(/^#/, "");
  const candidates = /* @__PURE__ */ new Set();
  const token = extractMiniProgramToken(clean);
  if (token) {
    candidates.add(`weixin://dl/business/?t=${token}`);
    candidates.add(`wechat://dl/business/?t=${token}`);
    candidates.add("weixin://");
  }
  candidates.add(trimmed);
  candidates.add(clean);
  return [...candidates].filter(Boolean);
};
const openExternal = async (href) => {
  const url = normalizeHttpUrl(href);
  if (!url) return false;
  return platformBridge.openHttp(url);
};
const openExternalRaw = async (target) => {
  const raw = (target || "").trim();
  if (!raw) return false;
  const tryTargets = isLikelyMiniProgramCode(raw) ? miniProgramCandidates(raw) : [raw, encodeURI(raw)];
  for (const item of tryTargets) {
    const ok = await platformBridge.openUri(item);
    if (ok) return true;
  }
  return false;
};
const isHttpLink = (href) => !!normalizeHttpUrl(href);
const GITHUB_REPO = "superdaobo/mini-hbut";
const GITHUB_RELEASES_URL = `https://github.com/${GITHUB_REPO}/releases`;
const GH_API_PROXY_PREFIX = "https://gh-proxy.com/";
const GH_DOWNLOAD_PROXY_PREFIX = "https://gh-proxy.org/";
const EDGEONE_CDN_BASE = "https://hbut.6661111.xyz";
const GITHUB_PAGES_CDN_BASE = "https://superdaobo.github.io/mini-hbut";
const CDN_BASES = Object.freeze(
  [EDGEONE_CDN_BASE, GITHUB_PAGES_CDN_BASE].filter((base) => String(base || "").trim())
);
const STABLE_MANIFEST_URLS = CDN_BASES.flatMap((base) => [
  `${base}/releases/latest.json`,
  `${base}/releases/stable-latest.json`
]);
const DEV_MANIFEST_URLS = CDN_BASES.map((base) => `${base}/releases/dev-latest.json`);
STABLE_MANIFEST_URLS[0] || "";
DEV_MANIFEST_URLS[0] || "";
const DEV_RELEASE_TAG = "dev-latest";
const API_PROXIES = [
  `${GH_API_PROXY_PREFIX}https://api.github.com/repos/${GITHUB_REPO}/releases/latest`,
  `https://api.github.com/repos/${GITHUB_REPO}/releases/latest`,
  `https://cdn.jsdelivr.net/gh/${GITHUB_REPO}@latest/package.json`
];
const DEV_API_PROXIES = [
  `${GH_API_PROXY_PREFIX}https://api.github.com/repos/${GITHUB_REPO}/releases/tags/${DEV_RELEASE_TAG}`,
  `https://api.github.com/repos/${GITHUB_REPO}/releases/tags/${DEV_RELEASE_TAG}`
];
const DOWNLOAD_PROXIES = [
  (tag, filename) => `https://ghfast.top/https://github.com/${GITHUB_REPO}/releases/download/${tag}/${filename}`,
  (tag, filename) => `https://v4.gh-proxy.org/https://github.com/${GITHUB_REPO}/releases/download/${tag}/${filename}`,
  (tag, filename) => `${GH_DOWNLOAD_PROXY_PREFIX}https://github.com/${GITHUB_REPO}/releases/download/${tag}/${filename}`,
  (tag, filename) => `https://cdn.gh-proxy.org/https://github.com/${GITHUB_REPO}/releases/download/${tag}/${filename}`,
  (tag, filename) => `https://github.com/${GITHUB_REPO}/releases/download/${tag}/${filename}`
];
const hostnameOf = (value) => {
  try {
    return new URL(String(value || "").trim()).hostname.toLowerCase();
  } catch {
    return "";
  }
};
const isOfficialDownloadUrl = (url) => {
  const value = String(url || "").trim();
  if (!value) return false;
  return CDN_BASES.some((base) => value.startsWith(`${base}/releases/`));
};
const describeUpdateDownloadSource = (url, index = 0) => {
  const value = String(url || "").trim();
  if (value.startsWith(`${EDGEONE_CDN_BASE}/`)) {
    return { label: "EdgeOne 主站", tag: "edgeone" };
  }
  if (value.startsWith(`${GITHUB_PAGES_CDN_BASE}/`)) {
    return { label: "GitHub Pages 备用", tag: "ghpages" };
  }
  const host = hostnameOf(value);
  if (host === "ghfast.top") {
    return { label: "代理下载 1", tag: "proxy1" };
  }
  if (host === "v4.gh-proxy.org") {
    return { label: "代理下载 2", tag: "proxy2" };
  }
  if (host === "gh-proxy.org") {
    return { label: "代理下载 3", tag: "proxy3" };
  }
  if (host === "cdn.gh-proxy.org") {
    return { label: "代理下载 4", tag: "proxy4" };
  }
  if (host === "ghproxy.net" || host === "mirror.ghproxy.com") {
    return { label: `代理下载 ${index + 1}`, tag: `proxy${index + 1}` };
  }
  if (host === "github.com" || host.endsWith(".github.com")) {
    return { label: "GitHub 源站", tag: "github" };
  }
  return { label: `线路 ${index + 1}`, tag: `line${index}` };
};
const buildCdnReleaseAssetUrls = (downloadDir, filename) => {
  const dir = String(downloadDir || "").trim().replace(/^\/+|\/+$/g, "");
  const name = String(filename || "").trim().replace(/^\/+/, "");
  if (!dir || !name) return [];
  return CDN_BASES.map((base) => `${base}/releases/${dir}/${name}`);
};
const buildUpdateDownloadUrls = (tag, filename, primaryUrl = "") => {
  const candidates = [
    ...DOWNLOAD_PROXIES.map((fn) => fn(tag, filename)),
    primaryUrl,
    ...buildCdnReleaseAssetUrls(tag, filename)
  ];
  return uniqueUrls(candidates).filter((url) => !isOfficialDownloadUrl(url));
};
const buildDownloadOpenUrls = (downloadUrls) => uniqueUrls(downloadUrls).filter((url) => !isOfficialDownloadUrl(url));
function toGhProxyUrl(url) {
  const value = String(url || "").trim();
  if (!value) return "";
  if (value.startsWith("https://gh-proxy.org/")) return value;
  if (value.startsWith("https://gh-proxy.com/")) return value;
  if (value.startsWith("https://mirror.ghproxy.com/")) return value;
  if (value.startsWith("https://github.com/")) return `${GH_DOWNLOAD_PROXY_PREFIX}${value}`;
  return value;
}
const uniqueUrls = (list) => {
  const seen = /* @__PURE__ */ new Set();
  const out = [];
  for (const item of list || []) {
    const url = String(item || "").trim();
    if (!url || seen.has(url)) continue;
    seen.add(url);
    out.push(url);
  }
  return out;
};
const withCacheBust$1 = (url) => {
  const text = String(url || "").trim();
  if (!text) return "";
  const joiner = text.includes("?") ? "&" : "?";
  return `${text}${joiner}_t=${Date.now()}`;
};
const UPDATE_CHANNEL_KEY = "hbu_update_channel";
const SKIPPED_VERSION_STABLE_KEY = "hbu_skipped_version";
const SKIPPED_VERSION_DEV_KEY = "hbu_skipped_version_dev";
const memoryPrefs = {
  channel: "stable",
  skippedStable: "",
  skippedDev: ""
};
const storageGet = (key) => {
  try {
    if (typeof localStorage !== "undefined" && localStorage) {
      return localStorage.getItem(key);
    }
  } catch {
  }
  if (key === UPDATE_CHANNEL_KEY) return memoryPrefs.channel;
  if (key === SKIPPED_VERSION_STABLE_KEY) return memoryPrefs.skippedStable;
  if (key === SKIPPED_VERSION_DEV_KEY) return memoryPrefs.skippedDev;
  return null;
};
const storageSet = (key, value) => {
  try {
    if (typeof localStorage !== "undefined" && localStorage) {
      localStorage.setItem(key, value);
      return;
    }
  } catch {
  }
  if (key === UPDATE_CHANNEL_KEY) memoryPrefs.channel = value;
  if (key === SKIPPED_VERSION_STABLE_KEY) memoryPrefs.skippedStable = value;
  if (key === SKIPPED_VERSION_DEV_KEY) memoryPrefs.skippedDev = value;
};
const normalizeUpdateChannel = (value) => {
  const text = String(value || "").trim().toLowerCase();
  if (text === "dev" || text === "beta" || text === "development" || text === "nightly") return "dev";
  return "stable";
};
const getUpdateChannel = () => normalizeUpdateChannel(storageGet(UPDATE_CHANNEL_KEY));
const setUpdateChannel = (channel) => {
  const next = normalizeUpdateChannel(channel);
  storageSet(UPDATE_CHANNEL_KEY, next);
  return next;
};
const getSkippedVersionKey = (channel = getUpdateChannel()) => normalizeUpdateChannel(channel) === "dev" ? SKIPPED_VERSION_DEV_KEY : SKIPPED_VERSION_STABLE_KEY;
const getSkippedVersion = (channel = getUpdateChannel()) => String(storageGet(getSkippedVersionKey(channel)) || "").trim();
const setSkippedVersion = (version, channel = getUpdateChannel()) => {
  const text = String(version || "").trim();
  if (!text) return;
  storageSet(getSkippedVersionKey(channel), text);
};
function parseVersion(version) {
  const raw = String(version || "").trim().replace(/^v/i, "");
  const [corePart, prereleasePart = ""] = raw.split("-", 2);
  const core = corePart.split(".").map((segment) => {
    const match = String(segment || "").match(/^(\d+)/);
    return Number(match?.[1] || 0);
  });
  const prerelease = prereleasePart ? prereleasePart.split(".").map((segment) => /^\d+$/.test(segment) ? Number(segment) : String(segment || "").toLowerCase()) : [];
  return {
    raw,
    core,
    prerelease,
    isPrerelease: prerelease.length > 0
  };
}
function compareVersions$1(v1, v2) {
  const left = parseVersion(v1);
  const right = parseVersion(v2);
  const coreCmp = compareVersionCore(left, right);
  if (coreCmp !== 0) return coreCmp;
  if (left.isPrerelease && !right.isPrerelease) return -1;
  if (!left.isPrerelease && right.isPrerelease) return 1;
  const preLen = Math.max(left.prerelease.length, right.prerelease.length);
  for (let i = 0; i < preLen; i += 1) {
    const lv = left.prerelease[i];
    const rv = right.prerelease[i];
    if (lv === void 0) return -1;
    if (rv === void 0) return 1;
    if (lv === rv) continue;
    if (typeof lv === "number" && typeof rv === "number") return lv > rv ? 1 : -1;
    if (typeof lv === "number") return -1;
    if (typeof rv === "number") return 1;
    return String(lv).localeCompare(String(rv));
  }
  return 0;
}
function compareVersionCore(left, right) {
  const len = Math.max(left.core.length, right.core.length);
  for (let i = 0; i < len; i += 1) {
    const lv = left.core[i] || 0;
    const rv = right.core[i] || 0;
    if (lv > rv) return 1;
    if (lv < rv) return -1;
  }
  return 0;
}
function isPrereleaseVersion(version) {
  return parseVersion(version).isPrerelease;
}
function isCurrentInstallDev(version) {
  const text = String(version || "").replace(/^v/i, "").trim();
  if (!text) return false;
  if (isPrereleaseVersion(text)) return true;
  return /(^|[-._])(alpha|beta|rc|dev|nightly)([-._]|$)/i.test(text);
}
function isStableRelease(release) {
  const latestVersion = String(release?.version || release?.tag_name || "").replace(/^v/i, "");
  if (!latestVersion) return false;
  if (release?.prerelease) return false;
  if (isPrereleaseVersion(latestVersion)) return false;
  const channel = String(release?.channel || "").trim().toLowerCase();
  if (channel && channel !== "main" && channel !== "stable" && channel !== "release") {
    return false;
  }
  const tag = String(release?.tag_name || "").trim().toLowerCase();
  if (/(^|[-._])(alpha|beta|rc|dev)([-._]|$)/.test(tag) || tag === "dev-latest") {
    return false;
  }
  return true;
}
function isDevRelease(release) {
  if (!release) return false;
  const channel = String(release?.channel || "").trim().toLowerCase();
  if (channel === "dev" || channel === "beta") return true;
  const tag = String(release?.tag_name || "").trim().toLowerCase();
  if (tag === DEV_RELEASE_TAG || tag === "beta-latest") return true;
  if (release?.prerelease) return true;
  const version = String(release?.version || tag).replace(/^v/i, "");
  return isPrereleaseVersion(version);
}
function shouldOfferRelease(release, currentVersion, channel = "stable") {
  const preferred = normalizeUpdateChannel(channel);
  const latestVersion = String(release?.version || release?.tag_name || "").replace(/^v/i, "");
  const currentText = String(currentVersion || "").replace(/^v/i, "");
  if (!latestVersion) return false;
  if (preferred === "stable") {
    if (!isStableRelease(release)) return false;
    if (!currentText) return true;
    const latest2 = parseVersion(latestVersion);
    const current2 = parseVersion(currentText);
    if (current2.isPrerelease) return compareVersionCore(latest2, current2) > 0;
    return compareVersions$1(latestVersion, currentText) > 0;
  }
  if (!isDevRelease(release)) return false;
  if (!currentText) return true;
  const latest = parseVersion(latestVersion);
  const current = parseVersion(currentText);
  const coreCmp = compareVersionCore(latest, current);
  if (coreCmp < 0) return false;
  if (coreCmp === 0 && !current.isPrerelease && latest.isPrerelease) return true;
  return compareVersions$1(latestVersion, currentText) > 0;
}
function getPlatform$1() {
  const ua = navigator.userAgent.toLowerCase();
  if (ua.includes("android")) return "android";
  if (ua.includes("iphone") || ua.includes("ipad") || ua.includes("ios")) return "ios";
  if (ua.includes("win")) return "windows";
  if (ua.includes("mac")) return "macos";
  if (ua.includes("linux")) return "linux";
  return "unknown";
}
function getAssetPatterns(platform) {
  switch (platform) {
    case "android":
      return [/\.apk$/i];
    case "ios":
      return [/\.ipa$/i];
    case "windows":
      return [/x64-setup\.exe$/i, /\.msi$/i, /\.exe$/i];
    case "macos":
      return [/\.dmg$/i, /\.app\.zip$/i, /universal\.app\.zip$/i, /\.zip$/i];
    case "linux":
      return [/\.AppImage$/i, /\.deb$/i];
    default:
      return [];
  }
}
const normalizePackageJsonAsRelease = (data) => {
  const obj = data && typeof data === "object" ? data : {};
  const version = obj.version;
  if (!version) return null;
  const tagName = `v${String(version).replace(/^v/, "")}`;
  return {
    tag_name: tagName,
    name: tagName,
    body: obj.description ? String(obj.description) : "版本更新",
    html_url: GITHUB_RELEASES_URL,
    assets: [],
    published_at: (/* @__PURE__ */ new Date()).toISOString()
  };
};
function buildExpectedAssetName(platform, version, { preferDevZip = false } = {}) {
  const v = String(version).replace(/^v/, "");
  switch (platform) {
    case "windows":
      return `Mini-HBUT_${v}_x64-setup.exe`;
    case "macos":
      return preferDevZip ? `Mini-HBUT_${v}_universal.app.zip` : `Mini-HBUT_${v}_universal.dmg`;
    case "linux":
      return `Mini-HBUT_${v}_amd64.AppImage`;
    case "android":
      return `Mini-HBUT_${v}_arm64.apk`;
    case "ios":
      return `Mini-HBUT_${v}_iOS.ipa`;
    default:
      return "";
  }
}
const extractReleaseNoteVersion = (notes) => {
  const text = String(notes || "").trim();
  if (!text) return "";
  const headingMatch = text.match(/Mini-HBUT\s+v?(\d+\.\d+\.\d+(?:[-.\w]+)?)\s+更新说明/i);
  if (headingMatch?.[1]) return headingMatch[1].replace(/^v/i, "");
  const genericMatch = text.slice(0, 160).match(/\bv?(\d+\.\d+\.\d+(?:[-.\w]+)?)\b/i);
  return genericMatch?.[1] ? genericMatch[1].replace(/^v/i, "") : "";
};
const normalizeReleaseNotesForVersion = (notes, version) => {
  const body = String(notes || "").trim();
  if (!body) return "";
  const expectedVersion = String(version || "").trim().replace(/^v/i, "");
  const noteVersion = extractReleaseNoteVersion(body);
  if (expectedVersion && noteVersion && compareVersions$1(noteVersion, expectedVersion) !== 0) {
    return "";
  }
  return body;
};
const normalizeCdnManifestAsRelease = (manifest, cdnBase = EDGEONE_CDN_BASE) => {
  const raw = manifest && typeof manifest === "object" ? manifest : null;
  if (!raw?.tag || !raw?.assets) return null;
  const tag = String(raw.tag || "").trim();
  const downloadDir = String(raw.downloadDir || tag).trim() || tag;
  const version = String(raw.version || tag).replace(/^v/, "");
  const rawBody = String(raw.release_notes || raw.body || "").trim();
  const body = normalizeReleaseNotesForVersion(rawBody, version || tag);
  const assetBase = String(cdnBase || EDGEONE_CDN_BASE).replace(/\/+$/, "");
  const assets = Object.values(raw.assets || {}).filter(Boolean).map((filename) => ({
    name: filename,
    // 资产 URL 绑定实际命中的 CDN；下载时还会追加 GitHub 代理与另一 CDN 候选
    browser_download_url: assetBase ? `${assetBase}/releases/${downloadDir}/${filename}` : ""
  }));
  return {
    tag_name: tag,
    name: `v${version}`,
    body,
    html_url: `${GITHUB_RELEASES_URL}/tag/${tag}`,
    assets,
    published_at: raw.generatedAt ? String(raw.generatedAt) : void 0,
    version,
    prerelease: !!raw.prerelease,
    channel: String(raw.channel || "").trim(),
    downloadDir,
    __fromCdnManifest: true,
    __cdnBase: assetBase,
    __staleReleaseNotes: Boolean(rawBody && !body)
  };
};
const mergeCdnReleaseWithApiNotes = (cdnRelease, apiRelease) => {
  if (!cdnRelease || !apiRelease) return apiRelease || cdnRelease;
  const cdnVersion = String(cdnRelease.version || cdnRelease.tag_name || "").replace(/^v/i, "");
  const apiVersion = String(apiRelease.version || apiRelease.tag_name || "").replace(/^v/i, "");
  if (!cdnVersion || !apiVersion) {
    return apiRelease || cdnRelease;
  }
  const cmp = compareVersions$1(cdnVersion, apiVersion);
  if (cmp !== 0) {
    return cmp > 0 ? cdnRelease : apiRelease;
  }
  return {
    ...cdnRelease,
    assets: cdnRelease.assets || [],
    body: apiRelease.body || cdnRelease.body || "",
    html_url: apiRelease.html_url || cdnRelease.html_url,
    published_at: apiRelease.published_at || cdnRelease.published_at,
    name: apiRelease.name || cdnRelease.name,
    __releaseNotesFromApi: Boolean(apiRelease.body)
  };
};
const describeError = (error) => {
  if (!error) return "";
  if (error instanceof Error) return `${error.message}
${error.stack || ""}`.trim();
  if (typeof error === "string") return error;
  try {
    return JSON.stringify(error);
  } catch {
    return String(error);
  }
};
const isRecoverableNativeFetchError = (error) => {
  const text = describeError(error).toLowerCase();
  if (!text) return true;
  return text.includes("当前运行时不支持 invoke") || text.includes("window.__tauri_internal") || text.includes("__tauri_internal") || text.includes("__tauri_ipc__") || text.includes("tauri is not defined") || text.includes("ipc channel not found") || text.includes("could not find the webview window") || text.includes("this command is not allowed") || text.includes("not running in tauri");
};
const withTimeout$2 = async (promise, ms = 9e3) => {
  let timer;
  try {
    return await Promise.race([
      promise,
      new Promise((_, reject) => {
        timer = setTimeout(() => reject(new Error("timeout")), ms);
      })
    ]);
  } finally {
    if (timer) clearTimeout(timer);
  }
};
const fetchJson = async (url, timeoutMs = 6e3) => {
  const requestUrl = withCacheBust$1(url);
  if (isTauriRuntime()) {
    try {
      return await withTimeout$2(
        invokeNative("fetch_remote_json", { url: requestUrl }),
        timeoutMs
      );
    } catch (error) {
      if (!isRecoverableNativeFetchError(error)) {
        throw error;
      }
    }
  }
  const resp = await withTimeout$2(
    fetch(requestUrl, {
      headers: { Accept: "application/json" },
      cache: "no-store"
    }),
    timeoutMs
  );
  if (!resp.ok) {
    throw new Error(`请求远程 JSON 失败: HTTP ${resp.status}`);
  }
  return await resp.json();
};
const withManifestCacheBust = (url) => {
  const text = String(url || "").trim();
  if (!text) return text;
  try {
    const parsed = new URL(text);
    parsed.searchParams.set("_cb", String(Date.now()));
    return parsed.toString();
  } catch {
    const join = text.includes("?") ? "&" : "?";
    return `${text}${join}_cb=${Date.now()}`;
  }
};
const fetchFirstCdnManifest = async (urls, timeoutMs = 6e3) => {
  for (const url of urls || []) {
    const text = String(url || "").trim();
    if (!text) continue;
    try {
      const manifest = await fetchJson(withManifestCacheBust(text), timeoutMs);
      if (!manifest?.tag || !manifest?.assets) continue;
      const base = CDN_BASES.find((b) => text.startsWith(`${b}/`)) || EDGEONE_CDN_BASE;
      return { manifest, base };
    } catch {
    }
  }
  return null;
};
async function fetchStableReleaseInfo(currentVersion) {
  let cdnCandidate = null;
  const hit = await fetchFirstCdnManifest(STABLE_MANIFEST_URLS, 6e3);
  if (hit) {
    const release = normalizeCdnManifestAsRelease(hit.manifest, hit.base);
    if (release && shouldOfferRelease(release, currentVersion, "stable")) {
      if (release.__staleReleaseNotes) {
        cdnCandidate = release;
      } else {
        return release;
      }
    } else if (release) {
      cdnCandidate = release;
    }
  }
  let fallback = null;
  for (const url of API_PROXIES) {
    try {
      const data = await fetchJson(url, 9e3);
      const release = data?.tag_name ? data : normalizePackageJsonAsRelease(data);
      if (!release) continue;
      if (!fallback || (release.assets?.length || 0) > (fallback.assets?.length || 0)) {
        fallback = release;
      }
      if (shouldOfferRelease(release, currentVersion, "stable")) {
        return mergeCdnReleaseWithApiNotes(cdnCandidate, release);
      }
    } catch {
    }
  }
  return cdnCandidate || fallback;
}
async function fetchDevReleaseInfo(currentVersion) {
  let cdnCandidate = null;
  const hit = await fetchFirstCdnManifest(DEV_MANIFEST_URLS, 6e3);
  if (hit) {
    const release = normalizeCdnManifestAsRelease(hit.manifest, hit.base);
    if (release) {
      release.channel = release.channel || "dev";
      release.prerelease = true;
      if (!release.tag_name) release.tag_name = DEV_RELEASE_TAG;
      cdnCandidate = release;
      if (shouldOfferRelease(release, currentVersion, "dev") && !release.__staleReleaseNotes) {
        return release;
      }
    }
  }
  let fallback = null;
  for (const url of DEV_API_PROXIES) {
    try {
      const data = await fetchJson(url, 9e3);
      if (!data?.tag_name && !data?.assets) continue;
      const release = {
        ...data,
        prerelease: true,
        channel: "dev",
        version: String(data.tag_name || DEV_RELEASE_TAG).replace(/^v/i, "")
      };
      if (Array.isArray(data.assets) && data.assets.length) {
        const sample = String(data.assets[0]?.name || "");
        const m = sample.match(/Mini-HBUT_([^_]+)_/i);
        if (m?.[1]) release.version = m[1];
      }
      if (!fallback || (release.assets?.length || 0) > (fallback.assets?.length || 0)) {
        fallback = release;
      }
      if (shouldOfferRelease(release, currentVersion, "dev")) {
        return mergeCdnReleaseWithApiNotes(cdnCandidate, release);
      }
    } catch {
    }
  }
  return cdnCandidate || fallback;
}
async function fetchReleaseInfo(currentVersion, channel = "stable") {
  const preferred = normalizeUpdateChannel(channel);
  if (preferred === "dev") return fetchDevReleaseInfo(currentVersion);
  return fetchStableReleaseInfo(currentVersion);
}
const buildUpdateResultFromRelease = (release, currentVersion, channel) => {
  const preferred = normalizeUpdateChannel(channel);
  const tagName = release.tag_name || release.name || "";
  const latestVersion = String(release.version || tagName).replace(/^v/, "");
  const currentText = String(currentVersion || "").replace(/^v/, "");
  const platform = getPlatform$1();
  const patterns = getAssetPatterns(platform);
  const isPrerelease = preferred === "dev" || isDevRelease(release) || isPrereleaseVersion(latestVersion);
  if (!shouldOfferRelease(release, currentText, preferred)) {
    return {
      hasUpdate: false,
      currentVersion: currentText,
      latestVersion,
      tagName,
      channel: preferred,
      isPrerelease,
      platform
    };
  }
  let asset = null;
  if (Array.isArray(release.assets) && release.assets.length > 0) {
    for (const pattern of patterns) {
      asset = release.assets.find((item) => pattern.test(item.name || "")) || null;
      if (asset) break;
    }
  }
  const downloadTag = preferred === "dev" ? String(release.downloadDir || tagName || DEV_RELEASE_TAG).trim() || DEV_RELEASE_TAG : tagName;
  const releaseUrl = toGhProxyUrl(release.html_url) || release.html_url || `${GH_DOWNLOAD_PROXY_PREFIX}${GITHUB_RELEASES_URL}`;
  if (!asset) {
    const versionForName = preferred === "dev" ? latestVersion : tagName;
    const expectedName = buildExpectedAssetName(platform, versionForName, {
      preferDevZip: preferred === "dev"
    });
    if (expectedName) {
      const downloadUrls2 = buildUpdateDownloadUrls(downloadTag, expectedName);
      return {
        hasUpdate: true,
        currentVersion: currentText,
        latestVersion,
        tagName,
        channel: preferred,
        isPrerelease,
        releaseNotes: release.body || "暂无更新说明",
        releaseUrl,
        downloadUrls: downloadUrls2,
        preferredDownloadUrl: downloadUrls2[0] || "",
        assetName: expectedName,
        platform,
        publishedAt: release.published_at
      };
    }
    return {
      hasUpdate: false,
      pending: true,
      currentVersion: currentText,
      latestVersion,
      tagName,
      channel: preferred,
      isPrerelease,
      releaseNotes: release.body || "暂无更新说明",
      releaseUrl,
      platform,
      publishedAt: release.published_at
    };
  }
  const downloadUrls = buildUpdateDownloadUrls(downloadTag, asset.name, asset.browser_download_url);
  return {
    hasUpdate: true,
    currentVersion: currentText,
    latestVersion,
    tagName,
    channel: preferred,
    isPrerelease,
    releaseNotes: release.body || "暂无更新说明",
    releaseUrl,
    downloadUrls,
    preferredDownloadUrl: downloadUrls[0] || "",
    assetName: asset.name,
    platform,
    publishedAt: release.published_at
  };
};
async function checkForUpdates(currentVersion, options = {}) {
  try {
    const { allowsInAppGithubUpdater: allowsInAppGithubUpdater2 } = await __vitePreload(async () => {
      const { allowsInAppGithubUpdater: allowsInAppGithubUpdater3 } = await import("./more-modules-CfNfahoc.js").then((n) => n.z);
      return { allowsInAppGithubUpdater: allowsInAppGithubUpdater3 };
    }, true ? __vite__mapDeps([0,1]) : void 0, import.meta.url);
    if (!allowsInAppGithubUpdater2()) {
      return {
        mode: "apple_storefront",
        hasUpdate: false,
        error: false,
        message: "本安装通过 App Store / TestFlight 分发，请使用苹果更新。",
        currentVersion,
        channel: normalizeUpdateChannel(options.channel ?? getUpdateChannel())
      };
    }
  } catch {
  }
  const channel = normalizeUpdateChannel(options.channel ?? getUpdateChannel());
  try {
    const release = await fetchReleaseInfo(currentVersion, channel);
    if (!release) {
      return {
        error: true,
        message: "无法连接更新服务",
        currentVersion,
        channel
      };
    }
    if (channel === "stable" && !isStableRelease(release) && !shouldOfferRelease(release, currentVersion, "stable")) {
      return {
        hasUpdate: false,
        currentVersion,
        latestVersion: String(release.version || release.tag_name || "").replace(/^v/, ""),
        channel
      };
    }
    return buildUpdateResultFromRelease(release, currentVersion, channel);
  } catch (error) {
    return {
      error: true,
      message: error?.message || "检查更新失败",
      currentVersion,
      channel
    };
  }
}
const openFirstUrl = async (downloadUrls) => {
  for (const url of downloadUrls || []) {
    const ok = await openExternal(url);
    if (ok) return { success: true, url };
  }
  return { success: false };
};
async function downloadUpdate(downloadUrls, filename, onProgress) {
  if (!Array.isArray(downloadUrls) || downloadUrls.length === 0) {
    throw new Error("没有可用的下载链接");
  }
  const preferred = buildDownloadOpenUrls(downloadUrls);
  if (typeof onProgress === "function") onProgress(20);
  const opened = await openFirstUrl(preferred);
  if (opened.success) {
    if (typeof onProgress === "function") onProgress(100);
    return {
      success: true,
      method: "external-open",
      url: opened.url || "",
      filename: filename || ""
    };
  }
  throw new Error("无法打开浏览器下载链接");
}
async function getCurrentVersion() {
  try {
    const native = await getNativeAppVersion();
    const nativeText = String(native || "").trim();
    if (nativeText) return nativeText.replace(/^v/i, "");
  } catch {
  }
  const viteVersion = String("1.4.6").trim();
  if (viteVersion) return viteVersion.replace(/^v/i, "");
  return "1.0.0";
}
const updater = {
  checkForUpdates,
  downloadUpdate,
  getCurrentVersion,
  compareVersions: compareVersions$1,
  toGhProxyUrl,
  getUpdateChannel,
  setUpdateChannel,
  normalizeUpdateChannel,
  getSkippedVersion,
  setSkippedVersion,
  shouldOfferRelease,
  isDevRelease,
  isCurrentInstallDev
};
const updater$1 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  buildCdnReleaseAssetUrls,
  buildDownloadOpenUrls,
  buildUpdateDownloadUrls,
  checkForUpdates,
  compareVersions: compareVersions$1,
  default: updater,
  describeUpdateDownloadSource,
  downloadUpdate,
  getCurrentVersion,
  getSkippedVersion,
  getSkippedVersionKey,
  getUpdateChannel,
  isCurrentInstallDev,
  isDevRelease,
  isOfficialDownloadUrl,
  mergeCdnReleaseWithApiNotes,
  normalizeCdnManifestAsRelease,
  normalizeUpdateChannel,
  setSkippedVersion,
  setUpdateChannel,
  shouldOfferRelease,
  toGhProxyUrl
}, Symbol.toStringTag, { value: "Module" }));
const APPLE_BUNDLE_ID = "com.hbut.mini";
const DEFAULT_APPLE_APP_ID = "6787857278";
const SKIPPED_APPLE_VERSION_KEY = "hbu_skipped_apple_store_version";
const DEFAULT_LOOKUP_URLS = [
  `https://itunes.apple.com/lookup?bundleId=${APPLE_BUNDLE_ID}&country=cn`,
  `https://itunes.apple.com/lookup?bundleId=${APPLE_BUNDLE_ID}`
];
const stripVersion = (value) => String(value || "").trim().replace(/^v/i, "");
function getConfiguredAppleAppId() {
  try {
    const fromEnv = String(
      "6787857278"
    ).trim();
    if (fromEnv) return fromEnv.replace(/^id/i, "");
  } catch {
  }
  return DEFAULT_APPLE_APP_ID;
}
function getSkippedAppleStoreVersion() {
  try {
    return String(globalThis.localStorage?.getItem(SKIPPED_APPLE_VERSION_KEY) || "").trim();
  } catch {
    return "";
  }
}
function setSkippedAppleStoreVersion(version) {
  const text = stripVersion(version);
  if (!text) return;
  try {
    globalThis.localStorage?.setItem(SKIPPED_APPLE_VERSION_KEY, text);
  } catch {
  }
}
function buildAppStoreOpenUrls(options = {}) {
  const trackViewUrl = String(options.trackViewUrl || "").trim();
  const id = String(options.trackId || options.appId || getConfiguredAppleAppId() || "").trim().replace(/^id/i, "");
  const urls = [];
  if (trackViewUrl) urls.push(trackViewUrl);
  if (id) {
    urls.push(`itms-apps://apps.apple.com/app/id${id}`);
    urls.push(`https://apps.apple.com/app/id${id}`);
  }
  return [...new Set(urls.filter(Boolean))];
}
function buildTestFlightOpenUrls(appId) {
  const id = String(appId || getConfiguredAppleAppId() || "").trim().replace(/^id/i, "");
  const urls = [];
  if (id) {
    urls.push(`itms-beta://beta.itunes.apple.com/v1/app/${id}`);
    urls.push(`https://beta.itunes.apple.com/v1/app/${id}`);
  }
  urls.push("itms-beta://");
  return [...new Set(urls.filter(Boolean))];
}
async function openAppStoreUpdatePage(options = {}) {
  for (const url of buildAppStoreOpenUrls(options)) {
    if (/^https?:\/\//i.test(url)) {
      if (await openExternal(url)) return true;
    } else if (await openExternalRaw(url)) {
      return true;
    }
  }
  return false;
}
async function openTestFlightApp(appId) {
  for (const url of buildTestFlightOpenUrls(appId)) {
    if (/^https?:\/\//i.test(url)) {
      if (await openExternal(url)) return true;
    } else if (await openExternalRaw(url)) {
      return true;
    }
  }
  return false;
}
async function fetchLookupJson(url, timeoutMs = 8e3) {
  const controller = new AbortController();
  const timer = setTimeout(() => controller.abort(), timeoutMs);
  try {
    const res = await fetch(url, {
      method: "GET",
      signal: controller.signal,
      headers: { Accept: "application/json" }
    });
    if (!res.ok) return null;
    return await res.json();
  } catch {
    return null;
  } finally {
    clearTimeout(timer);
  }
}
async function checkAppleStoreUpdate(currentVersion, options = {}) {
  const current = stripVersion(currentVersion) || "0.0.0";
  const base = {
    mode: "apple_storefront",
    hasUpdate: false,
    currentVersion: current,
    storeVersion: "",
    trackId: getConfiguredAppleAppId(),
    trackViewUrl: ""
  };
  if (allowsInAppGithubUpdater()) {
    return {
      ...base,
      error: true,
      message: "当前构建应使用 GitHub/CDN 内部更新"
    };
  }
  const urls = (options.lookupUrls || DEFAULT_LOOKUP_URLS).filter(Boolean);
  let lastError = "无法连接 App Store 查询服务";
  for (const url of urls) {
    const payload = await fetchLookupJson(url);
    if (!payload) {
      lastError = "无法连接 App Store 查询服务";
      continue;
    }
    const count = Number(payload.resultCount || 0);
    const row = Array.isArray(payload.results) ? payload.results[0] : null;
    if (!count || !row) {
      return {
        ...base,
        notOnStore: true,
        hasUpdate: false,
        message: "App Store 暂无此应用的正式版记录。若使用 TestFlight，请打开 TestFlight 查看测试更新。"
      };
    }
    const storeVersion = stripVersion(row.version);
    const trackId = String(row.trackId || base.trackId || "").trim();
    const trackViewUrl = String(row.trackViewUrl || "").trim();
    const hasUpdate = Boolean(storeVersion && compareVersions$1(storeVersion, current) > 0);
    return {
      mode: "apple_storefront",
      hasUpdate,
      currentVersion: current,
      storeVersion,
      trackId,
      trackViewUrl,
      message: hasUpdate ? `App Store 最新 v${storeVersion}，当前 v${current}。请通过 App Store 更新。` : storeVersion ? `已是 App Store 最新正式版（v${storeVersion}）。TestFlight 测试包请在 TestFlight 中查看。` : "已是最新正式版。TestFlight 测试包请在 TestFlight 中查看。"
    };
  }
  return {
    ...base,
    error: true,
    message: lastError
  };
}
const _export_sfc = (sfc, props) => {
  const target = sfc.__vccOpts || sfc;
  for (const [key, val] of props) {
    target[key] = val;
  }
  return target;
};
const _hoisted_1$b = { class: "update-dialog" };
const _hoisted_2$a = { class: "dialog-header" };
const _hoisted_3$a = { class: "material-symbols-outlined dialog-header-icon" };
const _hoisted_4$9 = { class: "dialog-content" };
const _hoisted_5$7 = { class: "channel-badge-row" };
const _hoisted_6$7 = {
  class: "channel-pill",
  "data-channel": "stable"
};
const _hoisted_7$6 = {
  key: 0,
  class: "channel-pill",
  "data-channel": "stable"
};
const _hoisted_8$4 = {
  key: 0,
  class: "checking"
};
const _hoisted_9$4 = { class: "version-info" };
const _hoisted_10$4 = { class: "version-badge current" };
const _hoisted_11$3 = { class: "version-badge new" };
const _hoisted_12$3 = { class: "apple-message" };
const _hoisted_13$3 = {
  key: 2,
  class: "up-to-date"
};
const _hoisted_14$3 = { class: "version" };
const _hoisted_15$3 = {
  key: 0,
  class: "version muted"
};
const _hoisted_16$3 = {
  key: 3,
  class: "error"
};
const _hoisted_17$2 = { class: "channel-row" };
const _hoisted_18$1 = ["aria-pressed"];
const _hoisted_19$1 = { class: "channel-badge-row" };
const _hoisted_20$1 = ["data-channel"];
const _hoisted_21$1 = ["data-channel"];
const _hoisted_22$1 = {
  key: 0,
  class: "install-hint"
};
const _hoisted_23$1 = {
  key: 1,
  class: "checking"
};
const _hoisted_24$1 = { class: "version-info" };
const _hoisted_25$1 = ["data-install"];
const _hoisted_26$1 = ["data-channel"];
const _hoisted_27$1 = { class: "release-notes" };
const _hoisted_28$1 = ["innerHTML"];
const _hoisted_29$1 = {
  key: 0,
  class: "download-progress"
};
const _hoisted_30$1 = { class: "progress-bar" };
const _hoisted_31$1 = { class: "progress-text" };
const _hoisted_32$1 = {
  key: 1,
  class: "source-table"
};
const _hoisted_33$1 = { class: "source-list" };
const _hoisted_34$1 = ["onClick"];
const _hoisted_35$1 = { class: "source-label" };
const _hoisted_36$1 = { class: "material-symbols-outlined source-icon" };
const _hoisted_37$1 = { class: "source-speed" };
const _hoisted_38 = { class: "platform-info" };
const _hoisted_39 = { class: "platform-line" };
const _hoisted_40 = {
  key: 0,
  class: "platform-line"
};
const _hoisted_41 = {
  key: 3,
  class: "up-to-date"
};
const _hoisted_42 = { class: "version" };
const _hoisted_43 = {
  key: 4,
  class: "up-to-date"
};
const _hoisted_44 = { class: "version" };
const _hoisted_45 = {
  key: 0,
  class: "version muted"
};
const _hoisted_46 = {
  key: 5,
  class: "error"
};
const _hoisted_47 = { class: "dialog-actions" };
const _hoisted_48 = ["disabled"];
const _hoisted_49 = ["disabled"];
const _hoisted_50 = ["disabled"];
const _sfc_main$c = {
  __name: "UpdateDialog",
  emits: ["close"],
  setup(__props, { emit: __emit }) {
    const emit = __emit;
    const checking = ref(true);
    const updateInfo = ref(null);
    const appleInfo = ref(null);
    const downloading = ref(false);
    const downloadProgress = ref(0);
    const error = ref("");
    const currentVersion = ref("");
    const updateChannel = ref(getUpdateChannel());
    const updateMode = ref(getUpdateCheckMode());
    const openingStore = ref(false);
    const sourceSpeedResults = ref({});
    const showSourceTable = ref(false);
    const isAppleMode = computed(() => updateMode.value === "apple_storefront");
    const isDevChannel = computed(() => updateChannel.value === "dev");
    const channelBadge = computed(() => isDevChannel.value ? "开发版" : "正式版");
    const isInstallDev = computed(() => isCurrentInstallDev(currentVersion.value));
    const installBadge = computed(() => isInstallDev.value ? "开发版" : "正式版");
    const currentVersionLabel = computed(
      () => `当前 · ${installBadge.value} v${currentVersion.value || "--"}`
    );
    const checkUpdate = async () => {
      checking.value = true;
      error.value = "";
      showSourceTable.value = false;
      sourceSpeedResults.value = {};
      updateInfo.value = null;
      appleInfo.value = null;
      updateMode.value = getUpdateCheckMode();
      try {
        const version = currentVersion.value || await getCurrentVersion();
        currentVersion.value = version;
        if (isAppleMode.value) {
          const result2 = await checkAppleStoreUpdate(version);
          appleInfo.value = result2;
          if (result2.error) {
            error.value = result2.message || "检查更新失败，请稍后重试";
          }
          return;
        }
        const result = await checkForUpdates(version, { channel: updateChannel.value });
        updateInfo.value = result;
      } catch (e) {
        error.value = "检查更新失败，请稍后重试";
        console.error(e);
      } finally {
        checking.value = false;
      }
    };
    const handleOpenAppStore = async () => {
      openingStore.value = true;
      try {
        const ok = await openAppStoreUpdatePage({
          trackId: appleInfo.value?.trackId,
          trackViewUrl: appleInfo.value?.trackViewUrl
        });
        if (!ok) {
          error.value = "无法打开 App Store，请手动在商店中搜索 Mini-HBUT";
        }
      } finally {
        openingStore.value = false;
      }
    };
    const handleOpenTestFlight = async () => {
      openingStore.value = true;
      try {
        const ok = await openTestFlightApp(appleInfo.value?.trackId);
        if (!ok) {
          error.value = "无法打开 TestFlight，请从主屏幕打开 TestFlight App";
        }
      } finally {
        openingStore.value = false;
      }
    };
    const handleSkipApple = () => {
      if (appleInfo.value?.storeVersion) {
        setSkippedAppleStoreVersion(appleInfo.value.storeVersion);
      }
      emit("close");
    };
    const handleChannelToggle = () => {
      const next = isDevChannel.value ? "stable" : "dev";
      updateChannel.value = setUpdateChannel(next);
      void checkUpdate();
    };
    const downloadSources = computed(() => {
      if (!updateInfo.value?.downloadUrls) return [];
      return updateInfo.value.downloadUrls.filter((url) => !isOfficialDownloadUrl(url)).map((url, idx) => ({
        url,
        ...describeUpdateDownloadSource(url, idx)
      }));
    });
    const testSourceSpeed = async (url) => {
      sourceSpeedResults.value[url] = { ms: 0, status: "testing" };
      const start = performance.now();
      try {
        const controller = new AbortController();
        const timer = setTimeout(() => controller.abort(), 8e3);
        const response = await fetch(url, {
          method: "HEAD",
          mode: "no-cors",
          signal: controller.signal
        });
        clearTimeout(timer);
        const ms = Math.round(performance.now() - start);
        sourceSpeedResults.value[url] = {
          ms,
          status: ms < 2e3 ? "ok" : ms < 5e3 ? "slow" : "fail"
        };
      } catch {
        const ms = Math.round(performance.now() - start);
        if (ms < 5e3) {
          sourceSpeedResults.value[url] = { ms, status: ms < 2e3 ? "ok" : "slow" };
        } else {
          sourceSpeedResults.value[url] = { ms, status: "fail" };
        }
      }
    };
    const testAllSources = () => {
      downloadSources.value.forEach(({ url }) => {
        testSourceSpeed(url);
      });
    };
    const getSpeedColor = (status) => {
      switch (status) {
        case "ok":
          return "speed-ok";
        case "slow":
          return "speed-slow";
        case "fail":
          return "speed-fail";
        case "testing":
          return "speed-testing";
        default:
          return "";
      }
    };
    const getSpeedText = (result) => {
      if (!result) return "—";
      if (result.status === "testing") return "...";
      if (result.status === "fail") return "超时";
      return `${result.ms}ms`;
    };
    const handleDownload = () => {
      showSourceTable.value = true;
      testAllSources();
    };
    const handleDownloadFromSource = async (url) => {
      downloading.value = true;
      downloadProgress.value = 0;
      error.value = "";
      try {
        const result = await downloadUpdate(
          [url],
          updateInfo.value.assetName,
          (progress) => {
            downloadProgress.value = progress;
          }
        );
        if (result.success) {
          downloadProgress.value = 100;
        }
      } catch (e) {
        error.value = "下载失败，请尝试其他线路";
        console.error(e);
      } finally {
        downloading.value = false;
      }
    };
    const handleSkip = () => {
      if (updateInfo.value?.latestVersion) {
        setSkippedVersion(updateInfo.value.latestVersion, updateChannel.value);
      }
      emit("close");
    };
    onMounted(() => {
      checkUpdate();
    });
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("div", {
        class: "update-dialog-overlay",
        onClick: _cache[2] || (_cache[2] = withModifiers(($event) => emit("close"), ["self"]))
      }, [
        createBaseVNode("div", _hoisted_1$b, [
          createBaseVNode("div", _hoisted_2$a, [
            createBaseVNode("span", _hoisted_3$a, toDisplayString(isAppleMode.value ? "shop" : "sync"), 1),
            createBaseVNode("h3", null, toDisplayString(isAppleMode.value ? "App 更新" : "版本更新"), 1)
          ]),
          createBaseVNode("div", _hoisted_4$9, [
            isAppleMode.value ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
              _cache[8] || (_cache[8] = createBaseVNode("p", { class: "apple-lead" }, " 本安装通过 App Store / TestFlight 分发。版本检查仅对照 App Store 正式版，不会下载 GitHub 安装包。 ", -1)),
              createBaseVNode("div", _hoisted_5$7, [
                createBaseVNode("span", _hoisted_6$7, "当前 v" + toDisplayString(currentVersion.value || "--"), 1),
                appleInfo.value?.storeVersion ? (openBlock(), createElementBlock("span", _hoisted_7$6, " 商店 v" + toDisplayString(appleInfo.value.storeVersion), 1)) : createCommentVNode("", true)
              ]),
              checking.value ? (openBlock(), createElementBlock("div", _hoisted_8$4, [..._cache[3] || (_cache[3] = [
                createBaseVNode("div", { class: "spinner" }, null, -1),
                createBaseVNode("p", null, "正在查询 App Store…", -1)
              ])])) : appleInfo.value?.hasUpdate ? (openBlock(), createElementBlock(Fragment, { key: 1 }, [
                createBaseVNode("div", _hoisted_9$4, [
                  createBaseVNode("div", _hoisted_10$4, "当前 v" + toDisplayString(appleInfo.value.currentVersion), 1),
                  _cache[4] || (_cache[4] = createBaseVNode("span", { class: "arrow" }, "→", -1)),
                  createBaseVNode("div", _hoisted_11$3, "商店 v" + toDisplayString(appleInfo.value.storeVersion), 1)
                ]),
                createBaseVNode("p", _hoisted_12$3, toDisplayString(appleInfo.value.message), 1)
              ], 64)) : appleInfo.value && !appleInfo.value.error ? (openBlock(), createElementBlock("div", _hoisted_13$3, [
                _cache[5] || (_cache[5] = createBaseVNode("span", { class: "material-symbols-outlined status-icon status-icon--success" }, "check_circle", -1)),
                createBaseVNode("p", null, toDisplayString(appleInfo.value.notOnStore ? "商店暂无正式版记录" : "已是 App Store 最新正式版"), 1),
                createBaseVNode("span", _hoisted_14$3, "当前 v" + toDisplayString(currentVersion.value), 1),
                appleInfo.value.storeVersion ? (openBlock(), createElementBlock("span", _hoisted_15$3, "商店 v" + toDisplayString(appleInfo.value.storeVersion), 1)) : createCommentVNode("", true),
                _cache[6] || (_cache[6] = createBaseVNode("p", { class: "apple-tf-hint" }, " 若你安装的是 TestFlight 测试版，请打开 TestFlight 查看是否有新的测试构建。 ", -1))
              ])) : error.value || appleInfo.value?.error ? (openBlock(), createElementBlock("div", _hoisted_16$3, [
                _cache[7] || (_cache[7] = createBaseVNode("span", { class: "material-symbols-outlined status-icon status-icon--warn" }, "error", -1)),
                createBaseVNode("p", null, toDisplayString(error.value || appleInfo.value?.message || "无法查询 App Store"), 1),
                createBaseVNode("button", {
                  class: "retry-btn",
                  onClick: checkUpdate
                }, "重试")
              ])) : createCommentVNode("", true)
            ], 64)) : (openBlock(), createElementBlock(Fragment, { key: 1 }, [
              createBaseVNode("div", _hoisted_17$2, [
                _cache[10] || (_cache[10] = createBaseVNode("div", { class: "channel-copy" }, [
                  createBaseVNode("strong", null, "接收开发版更新（Beta）"),
                  createBaseVNode("p", null, [
                    createTextVNode("开启后从 CDN / GitHub "),
                    createBaseVNode("code", null, "dev-latest"),
                    createTextVNode(" 检查预发布构建，可能不稳定。")
                  ])
                ], -1)),
                createBaseVNode("button", {
                  type: "button",
                  class: normalizeClass(["channel-switch", { on: isDevChannel.value }]),
                  "aria-pressed": isDevChannel.value,
                  onClick: handleChannelToggle
                }, [..._cache[9] || (_cache[9] = [
                  createBaseVNode("span", { class: "channel-knob" }, null, -1)
                ])], 10, _hoisted_18$1)
              ]),
              createBaseVNode("div", _hoisted_19$1, [
                createBaseVNode("span", {
                  class: "channel-pill",
                  "data-channel": isInstallDev.value ? "dev" : "stable"
                }, " 当前安装：" + toDisplayString(installBadge.value), 9, _hoisted_20$1),
                createBaseVNode("span", {
                  class: "channel-pill",
                  "data-channel": updateChannel.value
                }, "接收频道：" + toDisplayString(channelBadge.value), 9, _hoisted_21$1)
              ]),
              isInstallDev.value && !isDevChannel.value ? (openBlock(), createElementBlock("p", _hoisted_22$1, " 当前安装为开发版。若要继续接收 Beta 推送，请打开上方开关。 ")) : createCommentVNode("", true),
              checking.value ? (openBlock(), createElementBlock("div", _hoisted_23$1, [
                _cache[11] || (_cache[11] = createBaseVNode("div", { class: "spinner" }, null, -1)),
                createBaseVNode("p", null, "正在检查" + toDisplayString(isDevChannel.value ? "开发版" : "正式版") + "更新...", 1)
              ])) : updateInfo.value?.hasUpdate ? (openBlock(), createElementBlock(Fragment, { key: 2 }, [
                createBaseVNode("div", _hoisted_24$1, [
                  createBaseVNode("div", {
                    class: "version-badge current",
                    "data-install": isInstallDev.value ? "dev" : "stable"
                  }, toDisplayString(currentVersionLabel.value), 9, _hoisted_25$1),
                  _cache[12] || (_cache[12] = createBaseVNode("span", { class: "arrow" }, "→", -1)),
                  createBaseVNode("div", {
                    class: "version-badge new",
                    "data-channel": updateInfo.value.channel || updateChannel.value
                  }, toDisplayString(updateInfo.value.isPrerelease || isDevChannel.value ? "开发版" : "新版本") + " v" + toDisplayString(updateInfo.value.latestVersion), 9, _hoisted_26$1)
                ]),
                createBaseVNode("div", _hoisted_27$1, [
                  _cache[13] || (_cache[13] = createBaseVNode("h4", null, "更新内容:", -1)),
                  createBaseVNode("div", {
                    class: "notes-content",
                    innerHTML: updateInfo.value.releaseNotes.replace(/\n/g, "<br>")
                  }, null, 8, _hoisted_28$1)
                ]),
                downloading.value ? (openBlock(), createElementBlock("div", _hoisted_29$1, [
                  createBaseVNode("div", _hoisted_30$1, [
                    createBaseVNode("div", {
                      class: "progress-fill",
                      style: normalizeStyle({ width: downloadProgress.value + "%" })
                    }, null, 4)
                  ]),
                  createBaseVNode("span", _hoisted_31$1, toDisplayString(downloadProgress.value) + "%", 1)
                ])) : createCommentVNode("", true),
                showSourceTable.value && !downloading.value ? (openBlock(), createElementBlock("div", _hoisted_32$1, [
                  createBaseVNode("div", { class: "source-table-header" }, [
                    _cache[14] || (_cache[14] = createBaseVNode("span", null, "选择下载线路", -1)),
                    createBaseVNode("button", {
                      class: "retest-btn",
                      onClick: testAllSources
                    }, "重新测速")
                  ]),
                  createBaseVNode("div", _hoisted_33$1, [
                    (openBlock(true), createElementBlock(Fragment, null, renderList(downloadSources.value, (source) => {
                      return openBlock(), createElementBlock("div", {
                        key: source.url,
                        class: "source-row",
                        onClick: ($event) => handleDownloadFromSource(source.url)
                      }, [
                        createBaseVNode("div", _hoisted_35$1, [
                          createBaseVNode("span", _hoisted_36$1, toDisplayString(source.tag === "github" ? "code" : "link"), 1),
                          createBaseVNode("span", null, toDisplayString(source.label), 1)
                        ]),
                        createBaseVNode("div", _hoisted_37$1, [
                          createBaseVNode("span", {
                            class: normalizeClass(["speed-badge", getSpeedColor(sourceSpeedResults.value[source.url]?.status)])
                          }, toDisplayString(getSpeedText(sourceSpeedResults.value[source.url])), 3)
                        ])
                      ], 8, _hoisted_34$1);
                    }), 128))
                  ])
                ])) : createCommentVNode("", true),
                createBaseVNode("div", _hoisted_38, [
                  createBaseVNode("span", _hoisted_39, [
                    _cache[15] || (_cache[15] = createBaseVNode("span", { class: "material-symbols-outlined platform-icon" }, "smartphone", -1)),
                    createTextVNode(" 检测到平台: " + toDisplayString(updateInfo.value.platform), 1)
                  ]),
                  updateInfo.value.assetName ? (openBlock(), createElementBlock("span", _hoisted_40, [
                    _cache[16] || (_cache[16] = createBaseVNode("span", { class: "material-symbols-outlined platform-icon" }, "inventory_2", -1)),
                    createTextVNode(" " + toDisplayString(updateInfo.value.assetName), 1)
                  ])) : createCommentVNode("", true)
                ])
              ], 64)) : updateInfo.value?.pending ? (openBlock(), createElementBlock("div", _hoisted_41, [
                _cache[17] || (_cache[17] = createBaseVNode("span", { class: "material-symbols-outlined status-icon" }, "hourglass_top", -1)),
                _cache[18] || (_cache[18] = createBaseVNode("p", null, "新版本正在构建中，请稍后再试", -1)),
                createBaseVNode("span", _hoisted_42, "v" + toDisplayString(updateInfo.value.latestVersion), 1)
              ])) : updateInfo.value && !updateInfo.value.hasUpdate && !updateInfo.value.error ? (openBlock(), createElementBlock("div", _hoisted_43, [
                _cache[19] || (_cache[19] = createBaseVNode("span", { class: "material-symbols-outlined status-icon status-icon--success" }, "check_circle", -1)),
                createBaseVNode("p", null, toDisplayString(isDevChannel.value ? "已是最新开发版" : "已是最新正式版"), 1),
                createBaseVNode("span", _hoisted_44, toDisplayString(currentVersionLabel.value), 1),
                updateInfo.value.latestVersion ? (openBlock(), createElementBlock("span", _hoisted_45, "远端 " + toDisplayString(updateInfo.value.latestVersion), 1)) : createCommentVNode("", true)
              ])) : error.value || updateInfo.value?.error ? (openBlock(), createElementBlock("div", _hoisted_46, [
                _cache[20] || (_cache[20] = createBaseVNode("span", { class: "material-symbols-outlined status-icon status-icon--warn" }, "error", -1)),
                createBaseVNode("p", null, toDisplayString(error.value || updateInfo.value?.message || "无法获取更新信息，请检查网络连接"), 1),
                createBaseVNode("button", {
                  class: "retry-btn",
                  onClick: checkUpdate
                }, "重试")
              ])) : createCommentVNode("", true)
            ], 64))
          ]),
          createBaseVNode("div", _hoisted_47, [
            isAppleMode.value && appleInfo.value?.hasUpdate ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
              createBaseVNode("button", {
                class: "btn-secondary update-action-secondary",
                onClick: handleSkipApple
              }, "稍后"),
              createBaseVNode("button", {
                class: "btn-primary update-action-primary",
                disabled: openingStore.value,
                onClick: handleOpenAppStore
              }, toDisplayString(openingStore.value ? "打开中…" : "前往 App Store 更新"), 9, _hoisted_48)
            ], 64)) : isAppleMode.value ? (openBlock(), createElementBlock(Fragment, { key: 1 }, [
              !checking.value ? (openBlock(), createElementBlock("button", {
                key: 0,
                class: "btn-secondary update-action-secondary",
                disabled: openingStore.value,
                onClick: handleOpenTestFlight
              }, " 打开 TestFlight ", 8, _hoisted_49)) : createCommentVNode("", true),
              createBaseVNode("button", {
                class: "btn-primary",
                onClick: _cache[0] || (_cache[0] = ($event) => emit("close"))
              }, "关闭")
            ], 64)) : updateInfo.value?.hasUpdate ? (openBlock(), createElementBlock(Fragment, { key: 2 }, [
              createBaseVNode("button", {
                class: "btn-secondary update-action-secondary",
                onClick: handleSkip
              }, "跳过此版本"),
              createBaseVNode("button", {
                class: "btn-primary update-action-primary",
                onClick: handleDownload,
                disabled: downloading.value || showSourceTable.value
              }, toDisplayString(downloading.value ? "下载中..." : showSourceTable.value ? "请选择线路" : "立即更新"), 9, _hoisted_50)
            ], 64)) : (openBlock(), createElementBlock("button", {
              key: 3,
              class: "btn-primary",
              onClick: _cache[1] || (_cache[1] = ($event) => emit("close"))
            }, "关闭"))
          ])
        ])
      ]);
    };
  }
};
const UpdateDialog = /* @__PURE__ */ _export_sfc(_sfc_main$c, [["__scopeId", "data-v-099f3369"]]);
const toastState = reactive({
  show: false,
  message: "",
  type: "info",
  // info, success, warning, error
  timer: null
});
const showToast = (message, type = "info", duration = 2e3) => {
  if (toastState.timer) {
    clearTimeout(toastState.timer);
  }
  toastState.message = message;
  toastState.type = type;
  toastState.show = true;
  toastState.timer = setTimeout(() => {
    toastState.show = false;
  }, duration);
};
const _hoisted_1$a = { class: "toast-content" };
const _sfc_main$b = {
  __name: "Toast",
  setup(__props) {
    return (_ctx, _cache) => {
      return openBlock(), createBlock(Transition, { name: "toast-fade" }, {
        default: withCtx(() => [
          unref(toastState).show ? (openBlock(), createElementBlock("div", {
            key: 0,
            class: normalizeClass(["toast-container", unref(toastState).type])
          }, [
            createBaseVNode("div", _hoisted_1$a, toDisplayString(unref(toastState).message), 1)
          ], 2)) : createCommentVNode("", true)
        ]),
        _: 1
      });
    };
  }
};
const Toast = /* @__PURE__ */ _export_sfc(_sfc_main$b, [["__scopeId", "data-v-004e62ac"]]);
const _imports_0 = "" + new URL("../splash/cas_building.png", import.meta.url).href;
const _hoisted_1$9 = ["onKeydown"];
const _hoisted_2$9 = { class: "splash-bg" };
const _hoisted_3$9 = { class: "splash-particles" };
const _hoisted_4$8 = { class: "splash-content" };
const _hoisted_5$6 = { class: "splash-logo-ring" };
const _hoisted_6$6 = ["src"];
const _hoisted_7$5 = { class: "loader-text" };
const _sfc_main$a = {
  __name: "SplashScreen",
  props: {
    status: { type: String, default: "connecting" },
    statusText: { type: String, default: "正在连接教务系统…" }
  },
  emits: ["dismiss"],
  setup(__props, { expose: __expose, emit: __emit }) {
    const emit = __emit;
    const show = ref(true);
    const fadeOut = ref(false);
    const logoReady = ref(false);
    const buildingReady = ref(false);
    const contentReady = ref(false);
    const dotsCount = ref(0);
    const splashLogoSrc = ref("/splash/app_icon.png");
    let dotsTimer = null;
    let staggerTimer = null;
    const statusDots = computed(() => ".".repeat(dotsCount.value % 4));
    const dismiss = () => {
      if (fadeOut.value) return;
      fadeOut.value = true;
      if (dotsTimer) {
        clearInterval(dotsTimer);
        dotsTimer = null;
      }
      requestAnimationFrame(() => {
        setTimeout(() => {
          show.value = false;
          emit("dismiss");
        }, 300);
      });
    };
    const handleSplashLogoError = () => {
      if (splashLogoSrc.value !== "/splash/app_icon.png") {
        splashLogoSrc.value = "/splash/app_icon.png";
      }
    };
    __expose({ dismiss });
    onMounted(() => {
      dotsTimer = setInterval(() => {
        dotsCount.value++;
      }, 500);
      requestAnimationFrame(() => {
        logoReady.value = true;
        staggerTimer = setTimeout(() => {
          buildingReady.value = true;
          setTimeout(() => {
            contentReady.value = true;
          }, 100);
        }, 150);
      });
    });
    onBeforeUnmount(() => {
      if (dotsTimer) clearInterval(dotsTimer);
      if (staggerTimer) clearTimeout(staggerTimer);
    });
    return (_ctx, _cache) => {
      return openBlock(), createBlock(Transition, { name: "splash-leave" }, {
        default: withCtx(() => [
          show.value ? (openBlock(), createElementBlock("div", {
            key: 0,
            class: normalizeClass(["splash-root", { "splash-fade-out": fadeOut.value }]),
            role: "button",
            tabindex: "0",
            title: "点击跳过",
            onClick: dismiss,
            onKeydown: [
              withKeys(withModifiers(dismiss, ["prevent"]), ["enter"]),
              withKeys(withModifiers(dismiss, ["prevent"]), ["space"])
            ]
          }, [
            createBaseVNode("div", _hoisted_2$9, [
              _cache[0] || (_cache[0] = createBaseVNode("div", { class: "splash-bg-image" }, null, -1)),
              _cache[1] || (_cache[1] = createBaseVNode("div", { class: "splash-bg-overlay" }, null, -1)),
              createBaseVNode("div", _hoisted_3$9, [
                (openBlock(), createElementBlock(Fragment, null, renderList(20, (i) => {
                  return createBaseVNode("span", {
                    key: i,
                    class: "particle",
                    style: normalizeStyle({ "--i": i })
                  }, null, 4);
                }), 64))
              ])
            ]),
            createBaseVNode("div", _hoisted_4$8, [
              createBaseVNode("div", {
                class: normalizeClass(["splash-brand", { "brand-enter": logoReady.value }])
              }, [
                createBaseVNode("div", _hoisted_5$6, [
                  createBaseVNode("img", {
                    src: splashLogoSrc.value,
                    alt: "Mini-HBUT",
                    class: "splash-logo",
                    onError: handleSplashLogoError
                  }, null, 40, _hoisted_6$6)
                ]),
                _cache[2] || (_cache[2] = createBaseVNode("h1", { class: "splash-title" }, "Mini-HBUT", -1)),
                _cache[3] || (_cache[3] = createBaseVNode("p", { class: "splash-subtitle" }, "湖北工业大学智慧校园助手", -1))
              ], 2),
              createBaseVNode("div", {
                class: normalizeClass(["splash-illustration", { "illustration-enter": buildingReady.value }])
              }, [..._cache[4] || (_cache[4] = [
                createBaseVNode("img", {
                  src: _imports_0,
                  alt: "",
                  class: "splash-building"
                }, null, -1)
              ])], 2),
              createBaseVNode("div", {
                class: normalizeClass(["splash-loader", { "loader-enter": contentReady.value }])
              }, [
                _cache[5] || (_cache[5] = createBaseVNode("div", { class: "loader-spinner" }, [
                  createBaseVNode("div", { class: "spinner-ring" }),
                  createBaseVNode("div", { class: "spinner-ring spinner-ring-2" }),
                  createBaseVNode("div", { class: "spinner-ring spinner-ring-3" }),
                  createBaseVNode("div", { class: "spinner-dot" })
                ], -1)),
                createBaseVNode("p", _hoisted_7$5, toDisplayString(__props.statusText) + toDisplayString(statusDots.value), 1)
              ], 2)
            ])
          ], 42, _hoisted_1$9)) : createCommentVNode("", true)
        ]),
        _: 1
      });
    };
  }
};
const SplashScreen = /* @__PURE__ */ _export_sfc(_sfc_main$a, [["__scopeId", "data-v-4fde983f"]]);
const _hoisted_1$8 = {
  key: 0,
  class: "demo-mode-banner",
  role: "status"
};
const _hoisted_2$8 = { class: "demo-mode-banner__text" };
const _hoisted_3$8 = { class: "demo-mode-banner__en" };
const DISMISS_KEY = "hbu_demo_banner_dismissed";
const _sfc_main$9 = {
  __name: "DemoModeBanner",
  emits: ["dismiss"],
  setup(__props, { expose: __expose, emit: __emit }) {
    const visible = ref(false);
    const emit = __emit;
    onMounted(() => {
      if (!isTestAccountSession()) {
        visible.value = false;
        return;
      }
      try {
        visible.value = localStorage.getItem(DISMISS_KEY) !== "1";
      } catch {
        visible.value = true;
      }
    });
    const dismiss = () => {
      try {
        localStorage.setItem(DISMISS_KEY, "1");
      } catch {
      }
      visible.value = false;
      emit("dismiss");
    };
    __expose({
      showAgain() {
        try {
          localStorage.removeItem(DISMISS_KEY);
        } catch {
        }
        if (isTestAccountSession()) visible.value = true;
      }
    });
    return (_ctx, _cache) => {
      return visible.value ? (openBlock(), createElementBlock("div", _hoisted_1$8, [
        createBaseVNode("div", _hoisted_2$8, [
          _cache[0] || (_cache[0] = createBaseVNode("strong", null, "演示模式", -1)),
          createBaseVNode("span", null, toDisplayString(unref(DEMO_MODE_BANNER_ZH)), 1),
          createBaseVNode("span", _hoisted_3$8, toDisplayString(unref(DEMO_MODE_BANNER_EN)), 1)
        ]),
        createBaseVNode("button", {
          type: "button",
          class: "demo-mode-banner__close",
          "aria-label": "关闭",
          onClick: dismiss
        }, " × ")
      ])) : createCommentVNode("", true);
    };
  }
};
const DemoModeBanner = /* @__PURE__ */ _export_sfc(_sfc_main$9, [["__scopeId", "data-v-028123ed"]]);
const _hoisted_1$7 = {
  key: 0,
  class: "t-modal__header"
};
const _hoisted_2$7 = {
  key: 0,
  class: "t-modal__title"
};
const _hoisted_3$7 = { class: "t-modal__body" };
const _hoisted_4$7 = {
  key: 1,
  class: "t-modal__footer"
};
const _sfc_main$8 = {
  __name: "TModal",
  props: {
    visible: { type: Boolean, default: false },
    title: { type: String, default: "" },
    width: { type: String, default: "360px" },
    closable: { type: Boolean, default: true },
    closeOnOverlay: { type: Boolean, default: true }
  },
  emits: ["close"],
  setup(__props, { emit: __emit }) {
    const props = __props;
    const emit = __emit;
    const containerStyle = computed(() => ({ maxWidth: props.width }));
    const onOverlayClick = () => {
      if (props.closeOnOverlay) emit("close");
    };
    return (_ctx, _cache) => {
      return openBlock(), createBlock(Teleport, { to: "body" }, [
        createVNode(Transition, { name: "t-modal" }, {
          default: withCtx(() => [
            __props.visible ? (openBlock(), createElementBlock("div", {
              key: 0,
              class: "t-modal__overlay",
              onClick: withModifiers(onOverlayClick, ["self"])
            }, [
              createBaseVNode("div", {
                class: "t-modal__container glass-card",
                style: normalizeStyle(containerStyle.value)
              }, [
                __props.title || __props.closable ? (openBlock(), createElementBlock("div", _hoisted_1$7, [
                  __props.title ? (openBlock(), createElementBlock("h3", _hoisted_2$7, toDisplayString(__props.title), 1)) : createCommentVNode("", true),
                  __props.closable ? (openBlock(), createElementBlock("button", {
                    key: 1,
                    class: "t-modal__close",
                    onClick: _cache[0] || (_cache[0] = ($event) => _ctx.$emit("close")),
                    "aria-label": "关闭"
                  }, "✕")) : createCommentVNode("", true)
                ])) : createCommentVNode("", true),
                createBaseVNode("div", _hoisted_3$7, [
                  renderSlot(_ctx.$slots, "default", {}, void 0, true)
                ]),
                _ctx.$slots.footer ? (openBlock(), createElementBlock("div", _hoisted_4$7, [
                  renderSlot(_ctx.$slots, "footer", {}, void 0, true)
                ])) : createCommentVNode("", true)
              ], 4)
            ])) : createCommentVNode("", true)
          ]),
          _: 3
        })
      ]);
    };
  }
};
const TModal = /* @__PURE__ */ _export_sfc(_sfc_main$8, [["__scopeId", "data-v-d7b13343"]]);
const HOME_WIDGET_ORDER_DEFAULT = ["module_grid", "today_panel"];
const HOME_MODULE_ORDER_DEFAULT = [
  "grades",
  "classroom",
  "electricity",
  "transactions",
  "exams",
  "ranking",
  "campus_code",
  "calendar",
  "school_inbox",
  "academic",
  "qxzkb",
  "course_selection",
  "training",
  "teaching_eval",
  "chaoxing_hub",
  "chaoxing_inbox",
  "chaoxing_class",
  "broadband",
  "sports_venue",
  "library",
  "campus_map",
  "resource_share",
  "towergo",
  "smart_orientation",
  "ai"
];
const NOTIFICATION_CARD_ORDER_DEFAULT = [
  "class_reminder",
  "electricity",
  "grades",
  "exams",
  "school_inbox"
];
const buildDefaultWorkspaceLayout = () => ({
  version: 1,
  home: {
    widgetsOrder: [...HOME_WIDGET_ORDER_DEFAULT],
    moduleOrder: [...HOME_MODULE_ORDER_DEFAULT]
  },
  notifications: {
    cardsOrder: [...NOTIFICATION_CARD_ORDER_DEFAULT]
  }
});
const UI_PRESETS = {
  campus_blue: {
    label: "校园晴空",
    tagline: "蓝青渐变，清爽稳定，适合日常学习与导航。",
    category: "light",
    primary: "#2563eb",
    secondary: "#06b6d4",
    background: "radial-gradient(980px at 12% 10%, rgba(37, 99, 235, 0.25), transparent 58%), radial-gradient(900px at 88% 84%, rgba(6, 182, 212, 0.22), transparent 56%), linear-gradient(146deg, #e8f0ff 0%, #e7f7ff 52%, #f2fbff 100%)",
    text: "#0f172a",
    muted: "#475569",
    surfaceOpacity: 0.88,
    borderOpacity: 0.26,
    radiusScale: 1.04,
    fontScale: 1,
    spaceScale: 1,
    motionScale: 1
  },
  graphite_night: {
    label: "深海石墨",
    tagline: "低亮度高对比，适合夜间长时间使用。",
    category: "dark",
    primary: "#60a5fa",
    secondary: "#22d3ee",
    background: "radial-gradient(980px at 10% 10%, rgba(96, 165, 250, 0.2), transparent 56%), radial-gradient(900px at 88% 82%, rgba(34, 211, 238, 0.18), transparent 58%), linear-gradient(150deg, #0a1120 0%, #0f172a 52%, #1e293b 100%)",
    text: "#e2e8f0",
    muted: "#cbd5e1",
    surfaceOpacity: 0.76,
    borderOpacity: 0.24,
    radiusScale: 1.06,
    fontScale: 1,
    spaceScale: 1,
    motionScale: 0.95
  },
  forest_mint: {
    label: "薄荷森林",
    tagline: "柔和绿调，信息层次清晰，护眼耐看。",
    category: "vivid",
    primary: "#059669",
    secondary: "#0ea5e9",
    background: "radial-gradient(980px at 12% 10%, rgba(5, 150, 105, 0.24), transparent 58%), radial-gradient(920px at 88% 84%, rgba(14, 165, 233, 0.18), transparent 58%), linear-gradient(148deg, #ecfdf5 0%, #def7f1 52%, #eef9ff 100%)",
    text: "#052e2b",
    muted: "#0f766e",
    surfaceOpacity: 0.9,
    borderOpacity: 0.24,
    radiusScale: 1.08,
    fontScale: 1,
    spaceScale: 1.02,
    motionScale: 1
  },
  sunset_orange: {
    label: "夕照暖橙",
    tagline: "高识别暖色风格，适合功能入口与通知场景。",
    category: "vivid",
    primary: "#f97316",
    secondary: "#ec4899",
    background: "radial-gradient(1000px at 10% 12%, rgba(249, 115, 22, 0.24), transparent 56%), radial-gradient(920px at 86% 82%, rgba(236, 72, 153, 0.2), transparent 56%), linear-gradient(145deg, #fff7ed 0%, #ffe4e6 52%, #fff1f2 100%)",
    text: "#431407",
    muted: "#9d174d",
    surfaceOpacity: 0.9,
    borderOpacity: 0.22,
    radiusScale: 1.08,
    fontScale: 1,
    spaceScale: 1.02,
    motionScale: 1.08
  },
  minimal_slate: {
    label: "极简岩灰",
    tagline: "弱装饰高信息密度，适合高频数据操作。",
    category: "neutral",
    primary: "#334155",
    secondary: "#0ea5e9",
    background: "radial-gradient(900px at 12% 10%, rgba(100, 116, 139, 0.18), transparent 58%), linear-gradient(150deg, #f8fafc 0%, #f1f5f9 52%, #e2e8f0 100%)",
    text: "#0f172a",
    muted: "#475569",
    surfaceOpacity: 0.94,
    borderOpacity: 0.22,
    radiusScale: 0.94,
    fontScale: 0.98,
    spaceScale: 0.94,
    motionScale: 0.8
  }
};
const SYSTEM_UI_SETTINGS = {
  preset: "campus_blue",
  primary: UI_PRESETS.campus_blue.primary,
  secondary: UI_PRESETS.campus_blue.secondary,
  background: UI_PRESETS.campus_blue.background,
  text: UI_PRESETS.campus_blue.text,
  muted: UI_PRESETS.campus_blue.muted,
  customCss: "",
  customJs: "",
  surfaceOpacity: UI_PRESETS.campus_blue.surfaceOpacity ?? 0.88,
  borderOpacity: UI_PRESETS.campus_blue.borderOpacity ?? 0.26,
  radiusScale: UI_PRESETS.campus_blue.radiusScale ?? 1,
  fontScale: UI_PRESETS.campus_blue.fontScale ?? 1,
  spaceScale: UI_PRESETS.campus_blue.spaceScale ?? 1,
  motionScale: UI_PRESETS.campus_blue.motionScale ?? 1,
  danger: "#ef4444",
  success: "#10b981",
  scheduleCourseCardStyle: "modern",
  startupPage: "home",
  splashEnabled: true,
  workspaceLayout: buildDefaultWorkspaceLayout(),
  profile: {
    cardStyle: "glass",
    navStyle: "floating",
    density: "balanced",
    iconStyle: "duotone",
    decor: "mesh"
  }
};
const DARK_UI_TEXT = "#e2e8f0";
const DARK_UI_MUTED = "#94a3b8";
const DARK_UI_BG_GRADIENT = "linear-gradient(180deg, #0f172a 0%, #1e293b 100%)";
const STORAGE_KEY$3 = "hbu_ui_settings_v2";
const CARD_STYLES = ["glass", "solid", "outline"];
const NAV_STYLES = ["floating", "pill", "compact"];
const DENSITY_STYLES = ["comfortable", "balanced", "compact"];
const ICON_STYLES = ["duotone", "line", "mono"];
const DECOR_STYLES = ["mesh", "grain", "none"];
const SCHEDULE_COURSE_CARD_STYLES = ["modern", "traditional", "class"];
const STARTUP_PAGES = ["home", "schedule"];
const isRecord$2 = (value) => !!value && typeof value === "object";
const clamp = (value, min, max) => Math.min(max, Math.max(min, value));
const cloneWorkspaceLayout = (layout = buildDefaultWorkspaceLayout()) => JSON.parse(JSON.stringify(layout));
const hexToRgb = (hex) => {
  if (!hex || typeof hex !== "string") return null;
  const normalized = hex.replace("#", "").trim();
  if (normalized.length !== 6) return null;
  const r = Number.parseInt(normalized.slice(0, 2), 16);
  const g = Number.parseInt(normalized.slice(2, 4), 16);
  const b = Number.parseInt(normalized.slice(4, 6), 16);
  if (Number.isNaN(r) || Number.isNaN(g) || Number.isNaN(b)) return null;
  return { r, g, b };
};
const makeRgba = (hex, alpha) => {
  const rgb = hexToRgb(hex);
  if (!rgb) return `rgba(59, 130, 246, ${alpha})`;
  return `rgba(${rgb.r}, ${rgb.g}, ${rgb.b}, ${alpha})`;
};
const normalizeProfile = (profile = {}) => {
  const base = { ...SYSTEM_UI_SETTINGS.profile };
  const next = { ...base, ...profile || {} };
  if (!CARD_STYLES.includes(next.cardStyle)) next.cardStyle = base.cardStyle;
  if (!NAV_STYLES.includes(next.navStyle)) next.navStyle = base.navStyle;
  if (!DENSITY_STYLES.includes(next.density)) next.density = base.density;
  if (!ICON_STYLES.includes(next.iconStyle)) next.iconStyle = base.iconStyle;
  if (!DECOR_STYLES.includes(next.decor)) next.decor = base.decor;
  return next;
};
const isProfileEqual = (a, b) => CARD_STYLES.includes(a.cardStyle) && NAV_STYLES.includes(a.navStyle) && DENSITY_STYLES.includes(a.density) && ICON_STYLES.includes(a.iconStyle) && DECOR_STYLES.includes(a.decor) && a.cardStyle === b.cardStyle && a.navStyle === b.navStyle && a.density === b.density && a.iconStyle === b.iconStyle && a.decor === b.decor;
const normalizeOrderedKeys = (rawList, defaults) => {
  const seen = /* @__PURE__ */ new Set();
  const output = [];
  (Array.isArray(rawList) ? rawList : []).forEach((item) => {
    const key = String(item || "").trim();
    if (!key || !defaults.includes(key) || seen.has(key)) return;
    seen.add(key);
    output.push(key);
  });
  defaults.forEach((key) => {
    if (seen.has(key)) return;
    seen.add(key);
    output.push(key);
  });
  return output;
};
const normalizeWorkspaceLayout = (raw) => {
  const base = buildDefaultWorkspaceLayout();
  if (!isRecord$2(raw)) return base;
  const homeRaw = isRecord$2(raw.home) ? raw.home : {};
  const notificationsRaw = isRecord$2(raw.notifications) ? raw.notifications : {};
  return {
    version: 1,
    home: {
      widgetsOrder: normalizeOrderedKeys(
        homeRaw.widgetsOrder,
        [...HOME_WIDGET_ORDER_DEFAULT]
      ),
      moduleOrder: normalizeOrderedKeys(
        homeRaw.moduleOrder,
        [...HOME_MODULE_ORDER_DEFAULT]
      )
    },
    notifications: {
      cardsOrder: normalizeOrderedKeys(
        notificationsRaw.cardsOrder,
        [...NOTIFICATION_CARD_ORDER_DEFAULT]
      )
    }
  };
};
const normalizeSettings$3 = (raw) => {
  const base = {
    ...SYSTEM_UI_SETTINGS,
    profile: { ...SYSTEM_UI_SETTINGS.profile },
    workspaceLayout: cloneWorkspaceLayout(SYSTEM_UI_SETTINGS.workspaceLayout)
  };
  if (!isRecord$2(raw)) return base;
  const merged = {
    ...base,
    ...raw,
    profile: normalizeProfile(raw.profile),
    workspaceLayout: normalizeWorkspaceLayout(raw.workspaceLayout)
  };
  if (!UI_PRESETS[merged.preset]) {
    merged.preset = base.preset;
  }
  merged.customCss = typeof merged.customCss === "string" ? merged.customCss : "";
  merged.customJs = typeof merged.customJs === "string" ? merged.customJs : "";
  merged.surfaceOpacity = clamp(Number(merged.surfaceOpacity) || base.surfaceOpacity, 0.5, 1);
  merged.borderOpacity = clamp(Number(merged.borderOpacity) || base.borderOpacity, 0.05, 0.8);
  merged.radiusScale = clamp(Number(merged.radiusScale) || base.radiusScale, 0.7, 1.6);
  merged.fontScale = clamp(Number(merged.fontScale) || base.fontScale, 0.82, 1.2);
  merged.spaceScale = clamp(Number(merged.spaceScale) || base.spaceScale, 0.82, 1.2);
  merged.motionScale = clamp(Number(merged.motionScale) || base.motionScale, 0.2, 1.3);
  merged.scheduleCourseCardStyle = SCHEDULE_COURSE_CARD_STYLES.includes(
    merged.scheduleCourseCardStyle
  ) ? merged.scheduleCourseCardStyle : base.scheduleCourseCardStyle;
  merged.startupPage = STARTUP_PAGES.includes(merged.startupPage) ? merged.startupPage : base.startupPage;
  merged.splashEnabled = typeof merged.splashEnabled === "boolean" ? merged.splashEnabled : base.splashEnabled;
  merged.profile = normalizeProfile(merged.profile);
  merged.workspaceLayout = normalizeWorkspaceLayout(merged.workspaceLayout);
  return merged;
};
const loadStoredSettings$2 = () => {
  try {
    const raw = localStorage.getItem(STORAGE_KEY$3);
    if (!raw) return null;
    return JSON.parse(raw);
  } catch {
    return null;
  }
};
const storeSettings = (settings) => {
  try {
    localStorage.setItem(STORAGE_KEY$3, JSON.stringify(settings));
  } catch {
  }
};
const applyCustomCode = (settings) => {
  if (typeof document === "undefined") return;
  const cssId = "custom-theme-css";
  const jsId = "custom-theme-js";
  const cssEl = document.getElementById(cssId);
  const jsEl = document.getElementById(jsId);
  if (settings.customCss) {
    const styleEl = cssEl || document.createElement("style");
    styleEl.id = cssId;
    styleEl.textContent = settings.customCss;
    if (!cssEl) document.head.appendChild(styleEl);
  } else {
    cssEl?.remove();
  }
  if (settings.customJs && isCustomJavaScriptAllowed()) {
    jsEl?.remove();
    const scriptEl = document.createElement("script");
    scriptEl.id = jsId;
    scriptEl.type = "text/javascript";
    scriptEl.textContent = settings.customJs;
    document.body.appendChild(scriptEl);
  } else {
    jsEl?.remove();
  }
};
const applyUiSettings = (settings) => {
  if (typeof document === "undefined") return;
  const root = document.documentElement;
  const currentPreset = UI_PRESETS[settings.preset] || UI_PRESETS[SYSTEM_UI_SETTINGS.preset];
  const isDark = currentPreset?.category === "dark" || isNightModeEnabled();
  const textColor = isDark ? DARK_UI_TEXT : settings.text;
  const mutedColor = isDark ? DARK_UI_MUTED : settings.muted;
  const bgGradient = isDark ? DARK_UI_BG_GRADIENT : settings.background;
  const surface = isDark ? `rgba(15, 23, 42, ${settings.surfaceOpacity})` : `rgba(255, 255, 255, ${settings.surfaceOpacity})`;
  const border = isDark ? `rgba(148, 163, 184, ${Math.max(0.14, settings.borderOpacity)})` : `rgba(148, 163, 184, ${settings.borderOpacity})`;
  const backBtnBg = isDark ? "rgba(15, 23, 42, 0.82)" : "rgba(255, 255, 255, 0.95)";
  const backBtnText = isDark ? "#e2e8f0" : "#0f172a";
  const backBtnBorder = isDark ? "rgba(148, 163, 184, 0.34)" : "rgba(15, 23, 42, 0.16)";
  const backBtnShadow = isDark ? "0 8px 18px rgba(2, 6, 23, 0.42)" : "0 8px 18px rgba(15, 23, 42, 0.18)";
  const backBtnHoverBg = isDark ? "rgba(30, 41, 59, 0.9)" : "#ffffff";
  root.style.setProperty("--ui-primary", settings.primary);
  root.style.setProperty("--ui-secondary", settings.secondary);
  root.style.setProperty("--ui-primary-soft", makeRgba(settings.primary, 0.12));
  root.style.setProperty("--ui-primary-soft-strong", makeRgba(settings.primary, 0.22));
  root.style.setProperty("--ui-bg-gradient", bgGradient);
  root.style.setProperty("--ui-text", textColor);
  root.style.setProperty("--ui-muted", mutedColor);
  root.style.setProperty("--ui-surface", surface);
  root.style.setProperty("--ui-surface-border", border);
  root.style.setProperty("--ui-radius-scale", String(settings.radiusScale));
  root.style.setProperty("--ui-font-scale", String(settings.fontScale));
  root.style.setProperty("--ui-space-scale", String(settings.spaceScale));
  __vitePreload(() => Promise.resolve().then(() => widget_bridge), true ? void 0 : void 0, import.meta.url).then((m) => m.writeWidgetThemeColor(settings.primary)).catch(() => {
  });
  root.style.setProperty("--ui-motion-scale", String(settings.motionScale));
  root.style.setProperty("--ui-danger", settings.danger);
  root.style.setProperty("--ui-success", settings.success);
  root.style.setProperty("--ui-back-btn-bg", backBtnBg);
  root.style.setProperty("--ui-back-btn-text", backBtnText);
  root.style.setProperty("--ui-back-btn-border", backBtnBorder);
  root.style.setProperty("--ui-back-btn-shadow", backBtnShadow);
  root.style.setProperty("--ui-back-btn-hover-bg", backBtnHoverBg);
  root.style.setProperty("--primary-color", settings.primary);
  root.style.setProperty("--secondary-color", settings.secondary);
  root.style.setProperty("--bg-gradient", bgGradient);
  root.style.setProperty("--glass-bg", surface);
  root.style.setProperty("--glass-border", border);
  root.style.setProperty("--text-color", textColor);
  root.setAttribute("data-theme", settings.preset);
  root.setAttribute("data-ui-card", settings.profile.cardStyle);
  root.setAttribute("data-ui-nav", settings.profile.navStyle);
  root.setAttribute("data-ui-density", settings.profile.density);
  root.setAttribute("data-ui-icon", settings.profile.iconStyle);
  root.setAttribute("data-ui-decor", settings.profile.decor);
  applyCustomCode(settings);
};
const flushUiSettings = () => {
  const normalized = normalizeSettings$3(state$3);
  let needsPatch = false;
  const stateRecord = state$3;
  for (const key of Object.keys(normalized)) {
    if (key === "profile" || key === "workspaceLayout") continue;
    if (stateRecord[key] !== normalized[key]) {
      needsPatch = true;
      stateRecord[key] = normalized[key];
    }
  }
  if (!isProfileEqual(normalized.profile, state$3.profile)) {
    needsPatch = true;
    state$3.profile = { ...normalized.profile };
  }
  const nextLayoutText = JSON.stringify(normalized.workspaceLayout);
  const prevLayoutText = JSON.stringify(state$3.workspaceLayout);
  if (nextLayoutText !== prevLayoutText) {
    needsPatch = true;
    state$3.workspaceLayout = cloneWorkspaceLayout(normalized.workspaceLayout);
  }
  if (!needsPatch) {
    storeSettings(normalized);
    applyUiSettings(normalized);
    return;
  }
  storeSettings(normalized);
  applyUiSettings(normalized);
};
const state$3 = reactive(normalizeSettings$3(loadStoredSettings$2()));
let initialized$1 = false;
let scope = null;
const initUiSettings = () => {
  if (initialized$1) return;
  initialized$1 = true;
  if (typeof window !== "undefined") {
    window.addEventListener(NIGHT_MODE_CHANGED_EVENT, () => {
      applyUiSettings(normalizeSettings$3(state$3));
    });
  }
  scope = effectScope();
  scope.run(() => {
    const sync = () => {
      const normalized = normalizeSettings$3(state$3);
      const stateRecord = state$3;
      for (const key of Object.keys(normalized)) {
        if (key === "profile" || key === "workspaceLayout") continue;
        if (stateRecord[key] !== normalized[key]) {
          stateRecord[key] = normalized[key];
        }
      }
      if (!isProfileEqual(normalized.profile, state$3.profile)) {
        state$3.profile = { ...normalized.profile };
      }
      if (JSON.stringify(normalized.workspaceLayout) !== JSON.stringify(state$3.workspaceLayout)) {
        state$3.workspaceLayout = cloneWorkspaceLayout(normalized.workspaceLayout);
      }
      storeSettings(normalized);
      applyUiSettings(normalized);
    };
    sync();
    watch(state$3, sync, { deep: true });
  });
};
const useUiSettings = () => state$3;
const applyPreset = (presetKey) => {
  const preset = UI_PRESETS[presetKey];
  if (!preset) return;
  state$3.preset = presetKey;
  state$3.primary = preset.primary;
  state$3.secondary = preset.secondary;
  state$3.background = preset.background;
  state$3.text = preset.text;
  state$3.muted = preset.muted;
  if (typeof preset.surfaceOpacity === "number") state$3.surfaceOpacity = preset.surfaceOpacity;
  if (typeof preset.borderOpacity === "number") state$3.borderOpacity = preset.borderOpacity;
  if (typeof preset.radiusScale === "number") state$3.radiusScale = preset.radiusScale;
  if (typeof preset.fontScale === "number") state$3.fontScale = preset.fontScale;
  if (typeof preset.spaceScale === "number") state$3.spaceScale = preset.spaceScale;
  if (typeof preset.motionScale === "number") state$3.motionScale = preset.motionScale;
};
const resetUiSettings = () => {
  Object.assign(state$3, normalizeSettings$3(SYSTEM_UI_SETTINGS));
  flushUiSettings();
};
const applyUiSettingsSnapshot = (raw) => {
  const normalized = normalizeSettings$3(raw);
  Object.assign(state$3, normalized);
  state$3.profile = { ...normalized.profile };
  state$3.workspaceLayout = cloneWorkspaceLayout(normalized.workspaceLayout);
  flushUiSettings();
};
const _hoisted_1$6 = { class: "workspace-editor__hero" };
const _hoisted_2$6 = { class: "workspace-editor__tabs" };
const _hoisted_3$6 = { class: "workspace-section" };
const _hoisted_4$6 = { class: "workspace-list" };
const _hoisted_5$5 = ["data-layout-key"];
const _hoisted_6$5 = ["onPointerdown"];
const _hoisted_7$4 = { class: "workspace-card__body" };
const _hoisted_8$3 = { class: "workspace-section" };
const _hoisted_9$3 = { class: "workspace-grid" };
const _hoisted_10$3 = ["data-layout-key"];
const _hoisted_11$2 = ["onPointerdown"];
const _hoisted_12$2 = { class: "workspace-card__body" };
const _hoisted_13$2 = {
  key: 1,
  class: "workspace-section"
};
const _hoisted_14$2 = { class: "workspace-list" };
const _hoisted_15$2 = ["data-layout-key"];
const _hoisted_16$2 = ["onPointerdown"];
const _hoisted_17$1 = { class: "workspace-card__body" };
const _sfc_main$7 = {
  __name: "WorkspaceLayoutEditor",
  props: {
    visible: { type: Boolean, default: false },
    initialTab: { type: String, default: "home" }
  },
  emits: ["close"],
  setup(__props, { emit: __emit }) {
    const props = __props;
    const emit = __emit;
    const uiSettings = useUiSettings();
    const rootRef = ref(null);
    const activeTab = ref("home");
    const draftLayout = ref(cloneWorkspaceLayout(buildDefaultWorkspaceLayout()));
    const draggingKey = ref("");
    const draggingSection = ref("");
    const dragEnabled = ref(false);
    const homeWidgetMeta = {
      module_grid: {
        title: "功能宫格",
        desc: "首页 16 个主功能入口"
      },
      today_panel: {
        title: "今日上课提醒",
        desc: "当前/即将上课课程卡片"
      }
    };
    const homeModuleMeta = {
      grades: "成绩查询",
      classroom: "空教室",
      electricity: "电费查询",
      transactions: "交易记录",
      exams: "考试安排",
      ranking: "绩点排名",
      campus_code: "校园码",
      calendar: "校历",
      academic: "学业情况",
      qxzkb: "全校课表",
      course_selection: "选课中心",
      training: "培养方案",
      library: "图书查询",
      campus_map: "校园地图",
      resource_share: "资料分享",
      chaoxing_class: "学习通",
      ai: "校园助手"
    };
    const notificationCardMeta = {
      class_reminder: {
        title: "上课提醒",
        desc: "下一门课与提醒提前时间"
      },
      electricity: {
        title: "电费监控",
        desc: "宿舍电量与低电状态"
      },
      grades: {
        title: "成绩动态",
        desc: "最近成绩变化摘要"
      },
      exams: {
        title: "考试列表",
        desc: "近期与明日考试安排"
      }
    };
    const homeWidgetItems = computed(
      () => draftLayout.value.home.widgetsOrder.map((key, index) => ({
        key,
        index,
        ...homeWidgetMeta[key]
      }))
    );
    const homeModuleItems = computed(
      () => draftLayout.value.home.moduleOrder.map((key, index) => ({
        key,
        index,
        title: homeModuleMeta[key] || key
      }))
    );
    const notificationCardItems = computed(
      () => draftLayout.value.notifications.cardsOrder.map((key, index) => ({
        key,
        index,
        ...notificationCardMeta[key]
      }))
    );
    const setDraftFromState = () => {
      draftLayout.value = cloneWorkspaceLayout(uiSettings.workspaceLayout);
    };
    const getSectionList = (section) => {
      if (section === "home-widgets") return draftLayout.value.home.widgetsOrder;
      if (section === "home-modules") return draftLayout.value.home.moduleOrder;
      if (section === "notifications-cards") return draftLayout.value.notifications.cardsOrder;
      return [];
    };
    const setSectionList = (section, nextList) => {
      if (section === "home-widgets") {
        draftLayout.value.home.widgetsOrder = [...nextList];
        return;
      }
      if (section === "home-modules") {
        draftLayout.value.home.moduleOrder = [...nextList];
        return;
      }
      if (section === "notifications-cards") {
        draftLayout.value.notifications.cardsOrder = [...nextList];
      }
    };
    const moveKeyBefore = (section, dragKey, targetKey) => {
      const list = [...getSectionList(section)];
      const fromIndex = list.indexOf(dragKey);
      const targetIndex = list.indexOf(targetKey);
      if (fromIndex < 0 || targetIndex < 0 || fromIndex === targetIndex) return;
      list.splice(fromIndex, 1);
      list.splice(targetIndex, 0, dragKey);
      setSectionList(section, list);
    };
    const resolveClosestKey = (section, clientX, clientY) => {
      const root = rootRef.value;
      if (!root) return "";
      const selector = `[data-layout-section="${section}"][data-layout-key]`;
      const cards = [...root.querySelectorAll(selector)];
      if (!cards.length) return "";
      const directNode = document.elementFromPoint(clientX, clientY)?.closest?.(selector);
      if (directNode) {
        return String(directNode.getAttribute("data-layout-key") || "").trim();
      }
      let bestKey = "";
      let bestDistance = Number.POSITIVE_INFINITY;
      cards.forEach((card) => {
        const rect = card.getBoundingClientRect();
        const centerX = rect.left + rect.width / 2;
        const centerY = rect.top + rect.height / 2;
        const dx = centerX - clientX;
        const dy = centerY - clientY;
        const distance = dx * dx + dy * dy;
        if (distance < bestDistance) {
          bestDistance = distance;
          bestKey = String(card.getAttribute("data-layout-key") || "").trim();
        }
      });
      return bestKey;
    };
    const stopDragging = () => {
      draggingKey.value = "";
      draggingSection.value = "";
      dragEnabled.value = false;
      window.removeEventListener("pointermove", handlePointerMove);
      window.removeEventListener("pointerup", handlePointerUp);
      window.removeEventListener("pointercancel", handlePointerUp);
    };
    const handlePointerMove = (event) => {
      if (!dragEnabled.value || !draggingSection.value || !draggingKey.value) return;
      const targetKey = resolveClosestKey(draggingSection.value, event.clientX, event.clientY);
      if (!targetKey || targetKey === draggingKey.value) return;
      moveKeyBefore(draggingSection.value, draggingKey.value, targetKey);
    };
    const handlePointerUp = () => {
      stopDragging();
    };
    const handleDragStart = (section, key, event) => {
      if (!props.visible) return;
      event.preventDefault();
      draggingSection.value = section;
      draggingKey.value = key;
      dragEnabled.value = true;
      window.addEventListener("pointermove", handlePointerMove);
      window.addEventListener("pointerup", handlePointerUp);
      window.addEventListener("pointercancel", handlePointerUp);
    };
    const handleRestoreDefault = () => {
      const defaults = buildDefaultWorkspaceLayout();
      if (activeTab.value === "home") {
        draftLayout.value.home = cloneWorkspaceLayout(defaults).home;
        showToast("首页布局已恢复默认", "success");
        return;
      }
      draftLayout.value.notifications = cloneWorkspaceLayout(defaults).notifications;
      showToast("通知布局已恢复默认", "success");
    };
    const handleCancel = () => {
      setDraftFromState();
      stopDragging();
      emit("close");
    };
    const handleSave = () => {
      uiSettings.workspaceLayout = cloneWorkspaceLayout(draftLayout.value);
      flushUiSettings();
      stopDragging();
      showToast("工作台布局已保存", "success");
      emit("close");
    };
    watch(
      () => props.visible,
      (visible) => {
        if (!visible) {
          stopDragging();
          return;
        }
        activeTab.value = props.initialTab === "notifications" ? "notifications" : "home";
        setDraftFromState();
      }
    );
    watch(
      () => props.initialTab,
      (tab) => {
        if (!props.visible) return;
        activeTab.value = tab === "notifications" ? "notifications" : "home";
      }
    );
    onBeforeUnmount(() => {
      stopDragging();
    });
    return (_ctx, _cache) => {
      return openBlock(), createBlock(unref(TModal), {
        visible: __props.visible,
        title: "工作台布局",
        width: "960px",
        "close-on-overlay": false,
        onClose: handleCancel
      }, {
        default: withCtx(() => [
          createBaseVNode("div", {
            ref_key: "rootRef",
            ref: rootRef,
            class: "workspace-editor"
          }, [
            createBaseVNode("div", _hoisted_1$6, [
              _cache[2] || (_cache[2] = createBaseVNode("div", null, [
                createBaseVNode("strong", null, "网格吸附布局"),
                createBaseVNode("p", null, "拖动带虚线握把的卡片完成排序。只有点击“完成”后才会写入当前方案。")
              ], -1)),
              createBaseVNode("div", _hoisted_2$6, [
                createBaseVNode("button", {
                  type: "button",
                  class: normalizeClass(["workspace-editor__tab", { active: activeTab.value === "home" }]),
                  onClick: _cache[0] || (_cache[0] = ($event) => activeTab.value = "home")
                }, " 首页布局 ", 2),
                createBaseVNode("button", {
                  type: "button",
                  class: normalizeClass(["workspace-editor__tab", { active: activeTab.value === "notifications" }]),
                  onClick: _cache[1] || (_cache[1] = ($event) => activeTab.value = "notifications")
                }, " 通知布局 ", 2)
              ])
            ]),
            activeTab.value === "home" ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
              createBaseVNode("section", _hoisted_3$6, [
                _cache[3] || (_cache[3] = createBaseVNode("div", { class: "workspace-section__head" }, [
                  createBaseVNode("h4", null, "首页工作台组件"),
                  createBaseVNode("span", null, "固定横幅与公告不参与排序")
                ], -1)),
                createBaseVNode("div", _hoisted_4$6, [
                  (openBlock(true), createElementBlock(Fragment, null, renderList(homeWidgetItems.value, (item) => {
                    return openBlock(), createElementBlock("article", {
                      key: item.key,
                      class: normalizeClass(["workspace-card workspace-card--wide", { dragging: draggingSection.value === "home-widgets" && draggingKey.value === item.key }]),
                      "data-layout-section": "home-widgets",
                      "data-layout-key": item.key
                    }, [
                      createBaseVNode("button", {
                        type: "button",
                        class: "workspace-card__handle",
                        onPointerdown: ($event) => handleDragStart("home-widgets", item.key, $event)
                      }, " ⋮⋮ ", 40, _hoisted_6$5),
                      createBaseVNode("div", _hoisted_7$4, [
                        createBaseVNode("strong", null, toDisplayString(item.index + 1) + ". " + toDisplayString(item.title), 1),
                        createBaseVNode("span", null, toDisplayString(item.desc), 1)
                      ])
                    ], 10, _hoisted_5$5);
                  }), 128))
                ])
              ]),
              createBaseVNode("section", _hoisted_8$3, [
                _cache[4] || (_cache[4] = createBaseVNode("div", { class: "workspace-section__head" }, [
                  createBaseVNode("h4", null, "首页功能宫格"),
                  createBaseVNode("span", null, "4 列吸附排序，保存后立即应用到首页")
                ], -1)),
                createBaseVNode("div", _hoisted_9$3, [
                  (openBlock(true), createElementBlock(Fragment, null, renderList(homeModuleItems.value, (item) => {
                    return openBlock(), createElementBlock("article", {
                      key: item.key,
                      class: normalizeClass(["workspace-card workspace-card--module", { dragging: draggingSection.value === "home-modules" && draggingKey.value === item.key }]),
                      "data-layout-section": "home-modules",
                      "data-layout-key": item.key
                    }, [
                      createBaseVNode("button", {
                        type: "button",
                        class: "workspace-card__handle",
                        onPointerdown: ($event) => handleDragStart("home-modules", item.key, $event)
                      }, " ⋮⋮ ", 40, _hoisted_11$2),
                      createBaseVNode("div", _hoisted_12$2, [
                        createBaseVNode("strong", null, toDisplayString(item.index + 1), 1),
                        createBaseVNode("span", null, toDisplayString(item.title), 1)
                      ])
                    ], 10, _hoisted_10$3);
                  }), 128))
                ])
              ])
            ], 64)) : (openBlock(), createElementBlock("section", _hoisted_13$2, [
              _cache[5] || (_cache[5] = createBaseVNode("div", { class: "workspace-section__head" }, [
                createBaseVNode("h4", null, "通知信息卡"),
                createBaseVNode("span", null, "移动端长按可直接打开本编辑器")
              ], -1)),
              createBaseVNode("div", _hoisted_14$2, [
                (openBlock(true), createElementBlock(Fragment, null, renderList(notificationCardItems.value, (item) => {
                  return openBlock(), createElementBlock("article", {
                    key: item.key,
                    class: normalizeClass(["workspace-card workspace-card--wide", { dragging: draggingSection.value === "notifications-cards" && draggingKey.value === item.key }]),
                    "data-layout-section": "notifications-cards",
                    "data-layout-key": item.key
                  }, [
                    createBaseVNode("button", {
                      type: "button",
                      class: "workspace-card__handle",
                      onPointerdown: ($event) => handleDragStart("notifications-cards", item.key, $event)
                    }, " ⋮⋮ ", 40, _hoisted_16$2),
                    createBaseVNode("div", _hoisted_17$1, [
                      createBaseVNode("strong", null, toDisplayString(item.index + 1) + ". " + toDisplayString(item.title), 1),
                      createBaseVNode("span", null, toDisplayString(item.desc), 1)
                    ])
                  ], 10, _hoisted_15$2);
                }), 128))
              ])
            ])),
            createBaseVNode("div", { class: "workspace-editor__footer" }, [
              createBaseVNode("button", {
                type: "button",
                class: "ghost-btn",
                onClick: handleCancel
              }, "取消"),
              createBaseVNode("button", {
                type: "button",
                class: "ghost-btn",
                onClick: handleRestoreDefault
              }, "恢复默认"),
              createBaseVNode("button", {
                type: "button",
                class: "primary-btn",
                onClick: handleSave
              }, "完成")
            ])
          ], 512)
        ]),
        _: 1
      }, 8, ["visible"]);
    };
  }
};
const WorkspaceLayoutEditor = /* @__PURE__ */ _export_sfc(_sfc_main$7, [["__scopeId", "data-v-6a63e9f5"]]);
const _sfc_main$6 = /* @__PURE__ */ defineComponent({
  __name: "AppShell",
  props: {
    view: {},
    studentId: {},
    loggedIn: { type: Boolean }
  },
  setup(__props) {
    const props = __props;
    const shellContext = {
      view: readonly(toRef(props, "view")),
      studentId: readonly(toRef(props, "studentId")),
      loggedIn: computed(() => props.loggedIn)
    };
    provide("mini-hbut-app-shell", shellContext);
    return (_ctx, _cache) => {
      return renderSlot(_ctx.$slots, "default");
    };
  }
});
const SENSITIVE_SCOPE_ID = "student.identity";
const IDENTITY_SCOPE_META = {
  openid: { label: "确认你的 Mini-HBUT 身份", risk: "basic" },
  profile: { label: "查看你的基础资料", risk: "basic" },
  [SENSITIVE_SCOPE_ID]: {
    label: "获取你的学校身份（如学号、姓名）",
    risk: "sensitive"
  },
  offline_access: { label: "长期保持登录状态", risk: "basic" }
};
const enrichScopes = (scopes) => {
  if (!Array.isArray(scopes)) return [];
  return scopes.map((scope2) => {
    const meta = IDENTITY_SCOPE_META[scope2.id];
    return {
      id: scope2.id,
      label: scope2.label && scope2.label.trim() ? scope2.label : meta?.label || scope2.id,
      risk: scope2.risk === "sensitive" ? "sensitive" : "basic"
    };
  });
};
const groupScopesByRisk = (scopes) => {
  const grouped = { basic: [], sensitive: [] };
  for (const scope2 of enrichScopes(scopes)) {
    grouped[scope2.risk].push(scope2);
  }
  return grouped;
};
const hasSensitiveScope = (scopes) => Array.isArray(scopes) && scopes.some((scope2) => scope2.id === SENSITIVE_SCOPE_ID);
const SENSITIVE_SCOPE_NOTICE = "该应用将获取你的学校身份信息（学号、姓名）。允许前 App 会先在线验证你的学校登录状态。";
const NON_OFFICIAL_NOTICE = "学校身份由 Mini-HBUT 本地验证提供，不代表湖北工业大学官方认证。";
const DEFAULT_FETCH_TIMEOUT_MS = 1e4;
const makeTimeoutError = (label, timeoutMs) => {
  const error = new Error(`${label || "operation"} timeout after ${timeoutMs}ms`);
  error.name = "TimeoutError";
  return error;
};
const withTimeout$1 = (task, timeoutMs = DEFAULT_FETCH_TIMEOUT_MS, label = "operation") => {
  const safeTimeout = Number.isFinite(timeoutMs) && timeoutMs > 0 ? timeoutMs : DEFAULT_FETCH_TIMEOUT_MS;
  return (...args) => {
    let timer = null;
    const timeout = new Promise((_resolve, reject) => {
      timer = setTimeout(() => reject(makeTimeoutError(label, safeTimeout)), safeTimeout);
    });
    return Promise.race([Promise.resolve().then(() => task(...args)), timeout]).finally(() => {
      if (timer) clearTimeout(timer);
    });
  };
};
const isRecord$1 = (value) => value !== null && typeof value === "object" && !Array.isArray(value);
const errorMessage$1 = (error) => {
  if (error instanceof Error) return error.message;
  if (isRecord$1(error) && typeof error.message === "string") return error.message;
  return String(error ?? "");
};
const asApiPayload = (value) => isRecord$1(value) ? value : {};
const DEFAULT_TTL = 5 * 60 * 1e3;
const LONG_TTL = 3 * 24 * 60 * 60 * 1e3;
const EXTRA_LONG_TTL = 7 * 24 * 60 * 60 * 1e3;
const SHORT_TTL = 30 * 1e3;
const MAX_LOCAL_CACHE_ENTRIES = 220;
const MAX_LOCAL_CACHE_VALUE_BYTES = 180 * 1024;
const JWXT_MAINTENANCE_KEY$1 = "hbu_jwxt_maintenance";
const JWXT_MAINTENANCE_TIME_KEY$1 = "hbu_jwxt_maintenance_time";
const JWXT_MAINTENANCE_HINT_KEY$1 = "hbu_jwxt_maintenance_hint";
const JWXT_MAINTENANCE_EVENT$1 = "hbu-jwxt-maintenance";
const MAINTENANCE_FAILURE_THRESHOLD = 2;
const MAINTENANCE_FAILURE_WINDOW_MS = 60 * 1e3;
const MAINTENANCE_BACKOFF_MS = 60 * 1e3;
const JWXT_MAINTENANCE_FAIL_COUNT_KEY = "hbu_jwxt_maintenance_fail_count";
const JWXT_MAINTENANCE_FAIL_TIME_KEY = "hbu_jwxt_maintenance_fail_time";
const CACHE_INVALIDATION_EVENT = "hbu-cache-invalidation";
const CACHE_INVALIDATION_STORAGE_KEY = "hbu_cache_invalidation_broadcast";
const JWXT_KEY_PREFIXES = [
  "schedule:",
  "grades:",
  "semesters",
  "classroom:",
  "exams:",
  "ranking:",
  "calendar:",
  "academic:",
  "training:",
  "studentinfo:",
  "student_info:",
  "student_login_access:"
];
const memoryCache = /* @__PURE__ */ new Map();
const isQuotaExceededError = (error) => {
  const message = errorMessage$1(error).toLowerCase();
  return message.includes("quota") || message.includes("exceeded the quota") || message.includes("domexception 22") || message.includes("ns_error_dom_quota_reached");
};
const shouldPersistToLocalStorage = (key, payloadText) => {
  const text = String(key || "");
  if (text.startsWith("classroom:")) return false;
  if (text.startsWith("static_resource:")) {
    return payloadText.length <= 2.5 * 1024 * 1024;
  }
  if (!payloadText) return true;
  return payloadText.length <= MAX_LOCAL_CACHE_VALUE_BYTES;
};
const shouldPersistTestAccountPayload = (key) => {
  const text = String(key || "").trim();
  if (text === "semesters") return false;
  if (text.startsWith("classroom:")) return false;
  return true;
};
const collectCacheEntries = () => {
  const entries = [];
  for (let i = 0; i < localStorage.length; i += 1) {
    const storageKey = localStorage.key(i);
    if (!storageKey || !storageKey.startsWith("cache:")) continue;
    const raw = localStorage.getItem(storageKey);
    if (!raw) continue;
    let timestamp = 0;
    try {
      const parsed = JSON.parse(raw);
      timestamp = Number(parsed?.timestamp) || 0;
    } catch {
      timestamp = 0;
    }
    entries.push({ storageKey, timestamp });
  }
  entries.sort((a, b) => a.timestamp - b.timestamp);
  return entries;
};
const trimLocalCacheStorage = (count = 24) => {
  const entries = collectCacheEntries();
  if (!entries.length) return;
  const removeCount = Math.min(count, entries.length);
  for (let i = 0; i < removeCount; i += 1) {
    const storageKey = entries[i].storageKey;
    if (!storageKey.startsWith("cache:")) continue;
    localStorage.removeItem(storageKey);
  }
};
const enforceLocalCacheCountLimit = () => {
  const entries = collectCacheEntries();
  const overflow = entries.length - MAX_LOCAL_CACHE_ENTRIES;
  if (overflow <= 0) return;
  trimLocalCacheStorage(overflow);
};
function getCacheKey(key) {
  return `cache:${key}`;
}
const clearLocalCacheByPrefix = (prefix) => {
  const pref = String(prefix || "");
  if (!pref) return;
  for (const key of memoryCache.keys()) {
    if (key.startsWith(pref)) {
      memoryCache.delete(key);
    }
  }
  const keysToRemove = [];
  for (let i = 0; i < localStorage.length; i += 1) {
    const key = localStorage.key(i);
    if (key && key.startsWith(`cache:${pref}`)) {
      keysToRemove.push(key);
    }
  }
  keysToRemove.forEach((k) => localStorage.removeItem(k));
};
let invalidationListenerInstalled = false;
const recentInvalidationIds = /* @__PURE__ */ new Set();
const MAX_RECENT_INVALIDATION_IDS = 64;
const createInvalidationId = () => {
  if (typeof crypto !== "undefined" && typeof crypto.randomUUID === "function") {
    return crypto.randomUUID();
  }
  return `inv-${Date.now()}-${Math.random().toString(36).slice(2, 10)}`;
};
const pruneRecentInvalidationIds = () => {
  while (recentInvalidationIds.size > MAX_RECENT_INVALIDATION_IDS) {
    const oldest = recentInvalidationIds.values().next().value;
    if (oldest) recentInvalidationIds.delete(oldest);
  }
};
const applyInvalidationPayload = (payload) => {
  if (!isRecord$1(payload)) return;
  if (!payload || !Array.isArray(payload?.prefixes)) return;
  const id = String(payload?.id || "");
  if (id) {
    if (recentInvalidationIds.has(id)) return;
    recentInvalidationIds.add(id);
    pruneRecentInvalidationIds();
  }
  for (const prefix of payload.prefixes) {
    if (typeof prefix !== "string" || !prefix) continue;
    clearLocalCacheByPrefix(prefix);
  }
};
const handleInvalidationStorageEvent = (event) => {
  if (!event || event.key !== CACHE_INVALIDATION_STORAGE_KEY) return;
  try {
    applyInvalidationPayload(JSON.parse(event.newValue || "null"));
  } catch {
  }
};
const ensureInvalidationListener = () => {
  if (invalidationListenerInstalled || typeof window === "undefined") return;
  invalidationListenerInstalled = true;
  window.addEventListener("storage", handleInvalidationStorageEvent);
  window.addEventListener(CACHE_INVALIDATION_EVENT, (event) => {
    applyInvalidationPayload(event.detail);
  });
};
ensureInvalidationListener();
const broadcastCacheInvalidation = (prefixes) => {
  ensureInvalidationListener();
  const uniquePrefixes = [...new Set(prefixes.map(String).filter(Boolean))];
  if (!uniquePrefixes.length) return;
  const payload = { id: createInvalidationId(), prefixes: uniquePrefixes, at: Date.now() };
  try {
    localStorage.setItem(CACHE_INVALIDATION_STORAGE_KEY, JSON.stringify(payload));
  } catch {
  }
  if (typeof window !== "undefined") {
    try {
      window.dispatchEvent(new CustomEvent(CACHE_INVALIDATION_EVENT, { detail: payload }));
    } catch {
    }
  }
};
function clearCacheByPrefix(prefix) {
  const pref = String(prefix || "");
  if (!pref) return;
  clearLocalCacheByPrefix(pref);
  broadcastCacheInvalidation([pref]);
}
function clearUserScopedCaches(studentId) {
  const sid = String(studentId || "").trim();
  if (!sid) return;
  const prefixes = [];
  for (const prefix of JWXT_KEY_PREFIXES) {
    if (prefix === "semesters") continue;
    prefixes.push(`${prefix}${sid}`);
  }
  prefixes.push(`grade_teachers:${sid}`);
  prefixes.push(`training:options:${sid}`);
  prefixes.push(`training:jys:${sid}`);
  for (const prefix of prefixes) {
    clearLocalCacheByPrefix(prefix);
  }
  broadcastCacheInvalidation(prefixes);
}
function getCachedData(key, ttl = DEFAULT_TTL) {
  const now = Date.now();
  const inMemory = memoryCache.get(key);
  if (inMemory && now - inMemory.timestamp < ttl) {
    if (asApiPayload(inMemory.data).offline) {
      return null;
    }
    return { data: inMemory.data, fromCache: true, timestamp: inMemory.timestamp };
  }
  const raw = localStorage.getItem(getCacheKey(key));
  if (!raw) return null;
  try {
    const parsed = JSON.parse(raw);
    if (parsed && now - parsed.timestamp < ttl) {
      if (asApiPayload(parsed.data).offline) {
        return null;
      }
      memoryCache.set(key, parsed);
      return { data: parsed.data, fromCache: true, timestamp: parsed.timestamp };
    }
  } catch {
    return null;
  }
  return null;
}
const getAnyCachedEntry = (key) => {
  const inMemory = memoryCache.get(key);
  if (inMemory?.data) {
    return { data: inMemory.data, timestamp: Number(inMemory.timestamp) || Date.now() };
  }
  const raw = localStorage.getItem(getCacheKey(key));
  if (!raw) return null;
  try {
    const parsed = JSON.parse(raw);
    if (!parsed?.data) return null;
    return { data: parsed.data, timestamp: Number(parsed.timestamp) || Date.now() };
  } catch {
    return null;
  }
};
const isScheduleSemesterScopedKey = (key) => {
  const text = String(key || "").trim();
  if (!text) return false;
  return /^schedule:[^:]+:[^:]+$/.test(text);
};
const deriveFallbackPrefixes = (key) => {
  const text = String(key || "").trim();
  if (!text) return [];
  if (isScheduleSemesterScopedKey(text)) {
    return [text];
  }
  const prefixes = /* @__PURE__ */ new Set([text]);
  const jsonIndex = text.indexOf(":{");
  if (jsonIndex > 0) {
    prefixes.add(text.slice(0, jsonIndex));
  }
  const parts = text.split(":");
  while (parts.length > 2) {
    parts.pop();
    prefixes.add(parts.join(":"));
  }
  return [...prefixes];
};
const getAnyCachedEntryByPrefix = (prefix) => {
  const pref = String(prefix || "").trim();
  if (!pref) return null;
  let latest = null;
  for (const [key, value] of memoryCache.entries()) {
    if (key === pref || key.startsWith(`${pref}:`)) {
      const timestamp = Number(value?.timestamp) || 0;
      if (!latest || timestamp > latest.timestamp) {
        latest = {
          data: value?.data,
          timestamp: timestamp || Date.now()
        };
      }
    }
  }
  for (let i = 0; i < localStorage.length; i += 1) {
    const storageKey = localStorage.key(i);
    if (!storageKey || !storageKey.startsWith("cache:")) continue;
    const rawKey = storageKey.slice(6);
    if (!(rawKey === pref || rawKey.startsWith(`${pref}:`))) continue;
    const raw = localStorage.getItem(storageKey);
    if (!raw) continue;
    try {
      const parsed = JSON.parse(raw);
      if (!parsed?.data) continue;
      const timestamp = Number(parsed.timestamp) || 0;
      if (!latest || timestamp > latest.timestamp) {
        latest = {
          data: parsed.data,
          timestamp: timestamp || Date.now()
        };
      }
    } catch {
    }
  }
  return latest;
};
const getBestCachedEntry = (key) => {
  const exact = getAnyCachedEntry(key);
  if (exact) return exact;
  const prefixes = deriveFallbackPrefixes(key);
  for (const prefix of prefixes) {
    const hit = getAnyCachedEntryByPrefix(prefix);
    if (hit) return hit;
  }
  return null;
};
function getStaleCachedData(key) {
  const stale = getBestCachedEntry(key);
  if (!stale) return null;
  return {
    data: withOfflineMeta(stale.data, stale.timestamp),
    fromCache: true,
    timestamp: stale.timestamp,
    stale: true
  };
}
const withOfflineMeta = (data, timestamp) => {
  if (!data || typeof data !== "object") {
    return {
      success: true,
      data,
      offline: true,
      sync_time: new Date(timestamp).toISOString()
    };
  }
  const record = data;
  return {
    ...record,
    offline: true,
    sync_time: record.sync_time || new Date(timestamp).toISOString()
  };
};
function setCachedData(key, data) {
  const payload = { data, timestamp: Date.now() };
  memoryCache.set(key, payload);
  let payloadText = "";
  try {
    payloadText = JSON.stringify(payload);
  } catch {
    return;
  }
  if (!shouldPersistToLocalStorage(key, payloadText)) {
    return;
  }
  const storageKey = getCacheKey(key);
  try {
    localStorage.setItem(storageKey, payloadText);
    enforceLocalCacheCountLimit();
    return;
  } catch (error) {
    if (!isQuotaExceededError(error)) {
      console.warn("[Cache] localStorage set failed:", error);
      return;
    }
  }
  try {
    trimLocalCacheStorage(40);
    localStorage.setItem(storageKey, payloadText);
    enforceLocalCacheCountLimit();
  } catch (error) {
    if (!isQuotaExceededError(error)) {
      console.warn("[Cache] localStorage retry set failed:", error);
    }
  }
}
const isJwxtCacheKey = (key) => {
  const text = String(key || "");
  return JWXT_KEY_PREFIXES.some((prefix) => text.startsWith(prefix));
};
const looksLikeNetworkIssue = (message) => {
  const text = String(message || "").toLowerCase();
  if (!text) return false;
  return text.includes("error sending request for url") || text.includes("connection refused") || text.includes("timed out") || text.includes("timeout") || text.includes("failed to fetch") || text.includes("networkerror") || text.includes("network error") || text.includes("dns") || text.includes("econn") || text.includes("network") || text.includes("socket") || text.includes("eof") || text.includes("broken pipe");
};
const looksLikeMaintenanceIssue = (message) => {
  const text = String(message || "").toLowerCase();
  if (!text) return false;
  const noScheduleHints = ["暂无可用课表", "暂无课表", "无课表", "假期", "vacation", "no schedule"];
  if (noScheduleHints.some((hint) => text.includes(hint))) {
    return false;
  }
  return text.includes("维护") || text.includes("暂不可用") || text.includes("无法连接") || text.includes("连接失败");
};
const isSessionInvalidError = (error) => {
  const record = isRecord$1(error) ? error : {};
  const response = isRecord$1(record.response) ? record.response : {};
  const status = Number(response.status ?? record.status ?? record.statusCode ?? 0);
  if (status === 401 || status === 403) return true;
  const text = errorMessage$1(error).toLowerCase();
  if (!text) return false;
  if (/(^|\D)(401|403)(\D|$)/.test(text)) return true;
  return /(未登录|登录((已)?过期|超时)|会话((已)?过期|超时|失效)|登录失效|凭证(失效|过期)|token.*(失效|过期|invalid)|session\s+(timed out|timeout|expired|invalid)|unauthorized|forbidden)/.test(
    text
  );
};
const emitMaintenanceEvent = (active, hint = "", extra = {}) => {
  if (typeof window === "undefined") return;
  window.dispatchEvent(
    new CustomEvent(JWXT_MAINTENANCE_EVENT$1, {
      detail: {
        active: !!active,
        hint: hint || "",
        detail: extra.detail || "",
        phase: extra.phase || (active ? "maintenance" : "idle"),
        error: extra.error || extra.detail || "",
        at: Date.now()
      }
    })
  );
};
const readFailureState = () => {
  let count = 0;
  let lastFailAt = 0;
  try {
    count = Number(localStorage.getItem(JWXT_MAINTENANCE_FAIL_COUNT_KEY)) || 0;
    lastFailAt = Number(localStorage.getItem(JWXT_MAINTENANCE_FAIL_TIME_KEY)) || 0;
  } catch {
  }
  if (Date.now() - lastFailAt > MAINTENANCE_FAILURE_WINDOW_MS) count = 0;
  return count;
};
const persistFailureState = (count) => {
  try {
    localStorage.setItem(JWXT_MAINTENANCE_FAIL_COUNT_KEY, String(count));
    localStorage.setItem(JWXT_MAINTENANCE_FAIL_TIME_KEY, String(Date.now()));
  } catch {
  }
};
const recordJwxtFailure = (error, hint = "") => {
  if (looksLikeNetworkIssue(errorMessage$1(error))) return false;
  const next = readFailureState() + 1;
  persistFailureState(next);
  if (next >= MAINTENANCE_FAILURE_THRESHOLD) {
    setMaintenanceFlag(hint || String(errorMessage$1(error) || ""));
    return true;
  }
  return false;
};
const isMaintenanceBackoffActive = () => {
  try {
    if (localStorage.getItem(JWXT_MAINTENANCE_KEY$1) !== "1") return false;
    const lastFailAt = Number(localStorage.getItem(JWXT_MAINTENANCE_FAIL_TIME_KEY)) || 0;
    return Date.now() - lastFailAt < MAINTENANCE_BACKOFF_MS;
  } catch {
    return false;
  }
};
const setMaintenanceFlag = (hint = "", extra = {}) => {
  try {
    localStorage.setItem(JWXT_MAINTENANCE_KEY$1, "1");
    localStorage.setItem(JWXT_MAINTENANCE_TIME_KEY$1, String(Date.now()));
    if (hint) {
      localStorage.setItem(JWXT_MAINTENANCE_HINT_KEY$1, hint);
    }
  } catch {
  }
  emitMaintenanceEvent(
    true,
    hint || "教务系统正在维护或暂不可用，当前展示缓存数据。",
    { phase: extra.phase || "maintenance", detail: extra.detail || extra.error || "" }
  );
};
const clearMaintenanceFlag = () => {
  try {
    localStorage.removeItem(JWXT_MAINTENANCE_KEY$1);
    localStorage.removeItem(JWXT_MAINTENANCE_TIME_KEY$1);
    localStorage.removeItem(JWXT_MAINTENANCE_HINT_KEY$1);
    localStorage.removeItem(JWXT_MAINTENANCE_FAIL_COUNT_KEY);
    localStorage.removeItem(JWXT_MAINTENANCE_FAIL_TIME_KEY);
  } catch {
  }
  emitMaintenanceEvent(false);
};
const DEFAULT_SWR_OPTIONS = {
  staleWhileRevalidate: true,
  priority: "foreground"
};
const backgroundRefreshInflight = /* @__PURE__ */ new Map();
const recordRequestMetric = (key, { source = "unknown", start = Date.now(), stale = false, priority = "foreground", error = "" } = {}) => {
  try {
    pushDebugLog("Cache", `请求缓存指标 key=${key} source=${source}`, "debug", {
      key: String(key || ""),
      source,
      duration_ms: Math.max(0, Date.now() - (Number(start) || Date.now())),
      stale: !!stale,
      priority: String(priority || "foreground"),
      error: error ? String(error).slice(0, 160) : ""
    });
  } catch {
  }
};
const refreshCacheInBackground = async (key, fetcher, priority) => {
  if (backgroundRefreshInflight.has(key)) {
    return backgroundRefreshInflight.get(key);
  }
  const task = (async () => {
    const start = Date.now();
    try {
      const data = await fetcher();
      if (data && data.success && !data.offline) {
        setCachedData(key, data);
        if (isJwxtCacheKey(key)) {
          clearMaintenanceFlag();
        }
        recordRequestMetric(key, { source: "remote", start, priority });
      } else {
        recordRequestMetric(key, {
          source: "remote",
          start,
          priority,
          error: asApiPayload(data).error || asApiPayload(data).msg || asApiPayload(data).message || "unsuccessful-response"
        });
      }
    } catch (error) {
      recordRequestMetric(key, {
        source: "remote",
        start,
        priority,
        error: errorMessage$1(error)
      });
    }
  })().finally(() => {
    backgroundRefreshInflight.delete(key);
  });
  backgroundRefreshInflight.set(key, task);
  return task;
};
const cacheDebug = (message, detail) => {
};
async function fetchWithCache(key, fetcher, ttl = DEFAULT_TTL, options = {}) {
  if (ttl && typeof ttl === "object") {
    options = ttl;
    ttl = DEFAULT_TTL;
  }
  const requestOptions = options || {};
  const priority = requestOptions.priority || "foreground";
  const staleWhileRevalidate = !!requestOptions.staleWhileRevalidate;
  const forceRemote = !!requestOptions.forceRemote;
  const timeoutMs = Number(requestOptions.timeoutMs);
  const remoteFetcher = Number.isFinite(timeoutMs) && timeoutMs > 0 ? withTimeout$1(fetcher, timeoutMs, `fetchWithCache:${key}`) : fetcher;
  const testAccountPayload = isTestAccountSession() ? resolveTestAccountCachePayload(key) : null;
  if (testAccountPayload) {
    if (shouldPersistTestAccountPayload(key)) {
      setCachedData(key, testAccountPayload);
    }
    recordRequestMetric(key, { source: "test-account-cache", start: Date.now(), priority });
    return {
      data: testAccountPayload,
      fromCache: true,
      timestamp: Date.now(),
      demo: true
    };
  }
  const maintenanceMode = localStorage.getItem(JWXT_MAINTENANCE_KEY$1) === "1";
  const cached = forceRemote ? null : getCachedData(key, Number(ttl));
  if (cached) {
    const data = maintenanceMode ? withOfflineMeta(cached.data, cached.timestamp) : cached.data;
    recordRequestMetric(key, { source: "memory-cache", start: Date.now(), priority });
    if (maintenanceMode && !forceRemote && isJwxtCacheKey(key) && !isMaintenanceBackoffActive()) {
      refreshCacheInBackground(key, remoteFetcher, "background").catch(() => {
      });
    }
    return { ...cached, data, fromCache: true };
  }
  if (!forceRemote && maintenanceMode) {
    const stale = getBestCachedEntry(key);
    if (stale) {
      recordRequestMetric(key, { source: "stale-cache", start: Date.now(), stale: true, priority });
      return {
        data: withOfflineMeta(stale.data, stale.timestamp),
        fromCache: true,
        timestamp: stale.timestamp,
        stale: true
      };
    }
  }
  if (staleWhileRevalidate) {
    const stale = getStaleCachedData(key);
    if (stale) {
      recordRequestMetric(key, { source: "stale-cache", start: Date.now(), stale: true, priority });
      if (!isJwxtCacheKey(key) || !isMaintenanceBackoffActive()) {
        refreshCacheInBackground(key, remoteFetcher, "background").catch(() => {
        });
      }
      return stale;
    }
  }
  const remoteStart = Date.now();
  try {
    const data = await remoteFetcher();
    cacheDebug("[Cache] Fetched data for key:", `${key} success=${data?.success}`);
    if (data && data.success && !data.offline) {
      setCachedData(key, data);
      if (isJwxtCacheKey(key)) {
        clearMaintenanceFlag();
      }
      recordRequestMetric(key, { source: "remote", start: remoteStart, priority });
      return { data, fromCache: false, timestamp: Date.now() };
    }
    if (data && data.success && data.offline && requestOptions.cacheOfflinePayload) {
      const { offline: _drop, ...cacheable } = data;
      setCachedData(key, cacheable);
      recordRequestMetric(key, {
        source: "remote-offline",
        start: remoteStart,
        priority,
        error: asApiPayload(data).error || asApiPayload(data).msg || asApiPayload(data).message || "offline-payload"
      });
      return { data, fromCache: false, timestamp: Date.now() };
    }
    const stale = getBestCachedEntry(key);
    const message = String(asApiPayload(data).error || asApiPayload(data).msg || asApiPayload(data).message || "");
    const sessionInvalid = isSessionInvalidError({ message });
    const shouldFallback = !!stale && !sessionInvalid && (maintenanceMode || isJwxtCacheKey(key) && // 学校维护或网络异常均回退缓存展示（#587：网络错误不置维护横幅，但保留离线兜底）
    (looksLikeMaintenanceIssue(message) || looksLikeNetworkIssue(message)));
    if (shouldFallback) {
      if (isJwxtCacheKey(key)) {
        recordJwxtFailure(message, message);
      }
      recordRequestMetric(key, {
        source: "stale-cache",
        start: remoteStart,
        stale: true,
        priority,
        error: message
      });
      return {
        data: withOfflineMeta(stale.data, stale.timestamp),
        fromCache: true,
        timestamp: stale.timestamp,
        stale: true
      };
    }
    recordRequestMetric(key, { source: "remote", start: remoteStart, priority, error: message });
    return { data, fromCache: false, timestamp: Date.now() };
  } catch (error) {
    const stale = getBestCachedEntry(key);
    const sessionInvalid = isSessionInvalidError(error);
    if (stale && !sessionInvalid) {
      console.warn("[Cache] Fetch failed, fallback to stale cache:", key, error);
      if (isJwxtCacheKey(key)) {
        recordJwxtFailure(error, String(errorMessage$1(error) || ""));
      }
      recordRequestMetric(key, {
        source: "stale-cache",
        start: remoteStart,
        stale: true,
        priority,
        error: errorMessage$1(error)
      });
      return {
        data: withOfflineMeta(stale.data, stale.timestamp),
        fromCache: true,
        timestamp: stale.timestamp,
        stale: true
      };
    }
    if (!sessionInvalid && isJwxtCacheKey(key) && looksLikeMaintenanceIssue(errorMessage$1(error))) {
      recordJwxtFailure(error, String(errorMessage$1(error) || ""));
    }
    recordRequestMetric(key, {
      source: "remote",
      start: remoteStart,
      priority,
      error: errorMessage$1(error)
    });
    throw error;
  }
}
const api = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  DEFAULT_SWR_OPTIONS,
  DEFAULT_TTL,
  EXTRA_LONG_TTL,
  LONG_TTL,
  SHORT_TTL,
  clearCacheByPrefix,
  clearUserScopedCaches,
  fetchWithCache,
  getCacheKey,
  getCachedData,
  getStaleCachedData,
  setCachedData
}, Symbol.toStringTag, { value: "Module" }));
const IDENTITY_DEVICE_ID_KEY$1 = "hbu_identity_device_id";
const IDENTITY_USER_ID_KEY = "hbu_identity_user_id";
const IDENTITY_VERIFIED_AT_KEY = "hbu_identity_verified_at";
const safeStorage = () => {
  try {
    return globalThis.localStorage || null;
  } catch {
    return null;
  }
};
const readStorageString = (key) => {
  const storage = safeStorage();
  if (!storage) return null;
  try {
    return storage.getItem(key);
  } catch {
    return null;
  }
};
const writeStorageString = (key, value) => {
  const storage = safeStorage();
  if (!storage) return;
  try {
    storage.setItem(key, value);
  } catch {
  }
};
const removeStorageKey = (key) => {
  const storage = safeStorage();
  if (!storage) return;
  try {
    storage.removeItem(key);
  } catch {
  }
};
const state$2 = reactive({
  approvalPhase: "idle",
  activeRequestId: null,
  requestDetail: null,
  errorCode: null,
  userSafeMessage: "",
  queuedCount: 0,
  needsLogin: false,
  sessionValidation: "idle",
  lastResult: null,
  suppressedForLogin: false,
  deviceStatus: null,
  deviceId: readStorageString(IDENTITY_DEVICE_ID_KEY$1),
  userId: readStorageString(IDENTITY_USER_ID_KEY),
  verifiedAt: (() => {
    const raw = readStorageString(IDENTITY_VERIFIED_AT_KEY);
    const ts = Number(raw || 0);
    return Number.isFinite(ts) && ts > 0 ? ts : null;
  })(),
  revoking: false,
  deviceError: "",
  deviceRefreshing: false
});
const overlayVisible = computed(
  () => state$2.lastResult !== null || state$2.approvalPhase !== "idle" && state$2.activeRequestId !== null && !state$2.suppressedForLogin
);
const maskStudentId = (studentId) => {
  const sid = String(studentId || "").trim();
  if (!/^\d{10}$/.test(sid)) return sid;
  return `${sid.slice(0, 2)}******${sid.slice(-2)}`;
};
const getCachedStudentName = (studentId) => {
  const sid = String(studentId || "").trim();
  if (!sid) return "";
  try {
    const cached = getCachedData(`studentinfo:${sid}`);
    const payload = cached?.data;
    return String(payload?.data?.name || "").trim();
  } catch {
    return "";
  }
};
const getRemainingSeconds = (detail) => {
  if (!detail?.expires_at) return 0;
  const expires = new Date(detail.expires_at).getTime();
  if (!Number.isFinite(expires)) return 0;
  return Math.max(0, Math.ceil((expires - Date.now()) / 1e3));
};
const diagReport = (event, details) => {
  try {
    void fetch("http://127.0.0.1:4399/debug/logs/push", {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({ scope: "identity-store", level: "info", message: event, details }),
      signal: AbortSignal.timeout(1500)
    }).catch(() => void 0);
  } catch {
  }
};
const setIdentityApprovalPhase = (phase2, options = {}) => {
  state$2.approvalPhase = phase2;
  if (options.requestId !== void 0) state$2.activeRequestId = options.requestId;
  diagReport("phase_set", { phase: phase2, active: state$2.activeRequestId });
  if (options.detail !== void 0) state$2.requestDetail = options.detail;
  if (options.errorCode !== void 0) state$2.errorCode = options.errorCode;
  if (options.message !== void 0) state$2.userSafeMessage = options.message;
  if (options.needsLogin !== void 0) state$2.needsLogin = options.needsLogin;
  if (phase2 === "ready") {
    state$2.errorCode = null;
    state$2.userSafeMessage = "";
    state$2.sessionValidation = "idle";
  }
};
const setIdentityQueuedCount = (count) => {
  state$2.queuedCount = Math.max(0, count);
};
const setIdentitySessionValidation = (status) => {
  state$2.sessionValidation = status;
};
const setIdentityLastResult = (result) => {
  state$2.lastResult = result;
};
const setIdentitySuppressedForLogin = (suppressed) => {
  state$2.suppressedForLogin = suppressed;
};
const setIdentityDeviceStatus = (status) => {
  state$2.deviceStatus = status;
};
const setIdentityDeviceMeta = (meta) => {
  if (meta.deviceId !== void 0) {
    state$2.deviceId = meta.deviceId;
    if (meta.deviceId) {
      writeStorageString(IDENTITY_DEVICE_ID_KEY$1, meta.deviceId);
    } else {
      removeStorageKey(IDENTITY_DEVICE_ID_KEY$1);
    }
  }
  if (meta.userId !== void 0) {
    state$2.userId = meta.userId;
    if (meta.userId) {
      writeStorageString(IDENTITY_USER_ID_KEY, meta.userId);
    } else {
      removeStorageKey(IDENTITY_USER_ID_KEY);
    }
  }
};
const markIdentityVerifiedNow = () => {
  const now = Date.now();
  state$2.verifiedAt = now;
  writeStorageString(IDENTITY_VERIFIED_AT_KEY, String(now));
};
const setIdentityRevoking = (revoking) => {
  state$2.revoking = revoking;
};
const setIdentityDeviceError = (message) => {
  state$2.deviceError = message;
};
const setIdentityDeviceRefreshing = (refreshing) => {
  state$2.deviceRefreshing = refreshing;
};
const clearIdentityDeviceMeta = () => {
  state$2.deviceId = null;
  state$2.userId = null;
  removeStorageKey(IDENTITY_DEVICE_ID_KEY$1);
  removeStorageKey(IDENTITY_USER_ID_KEY);
};
const finishIdentityRequest = (requestId, outcome, options = {}) => {
  state$2.lastResult = {
    requestId,
    outcome,
    message: options.message || "",
    errorCode: options.errorCode,
    retryable: !!options.retryable
  };
  state$2.approvalPhase = outcome === "approved" ? "approved" : outcome === "denied" ? "denied" : outcome === "cancelled" ? "denied" : outcome === "expired" ? "expired" : "error";
  state$2.activeRequestId = null;
  state$2.requestDetail = null;
  state$2.errorCode = options.errorCode ?? null;
  state$2.userSafeMessage = options.message || "";
  state$2.needsLogin = false;
  state$2.sessionValidation = "idle";
  state$2.suppressedForLogin = false;
};
const clearIdentityLastResult = () => {
  state$2.lastResult = null;
  if (state$2.approvalPhase !== "idle") {
    state$2.approvalPhase = "idle";
  }
};
const resetIdentityUiState = () => {
  state$2.approvalPhase = "idle";
  state$2.activeRequestId = null;
  state$2.requestDetail = null;
  state$2.errorCode = null;
  state$2.userSafeMessage = "";
  state$2.queuedCount = 0;
  state$2.needsLogin = false;
  state$2.sessionValidation = "idle";
  state$2.lastResult = null;
  state$2.suppressedForLogin = false;
  state$2.deviceStatus = null;
  state$2.deviceId = readStorageString(IDENTITY_DEVICE_ID_KEY$1);
  state$2.userId = readStorageString(IDENTITY_USER_ID_KEY);
  const raw = readStorageString(IDENTITY_VERIFIED_AT_KEY);
  const ts = Number(raw || 0);
  state$2.verifiedAt = Number.isFinite(ts) && ts > 0 ? ts : null;
  state$2.revoking = false;
  state$2.deviceError = "";
  state$2.deviceRefreshing = false;
};
const identityUiState = state$2;
const FOCUSABLE_SELECTOR = 'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])';
const getFocusableElements = (card) => {
  if (!card) return [];
  return Array.from(card.querySelectorAll(FOCUSABLE_SELECTOR)).filter(
    (el) => !el.hasAttribute("disabled") && el.tabIndex >= 0
  );
};
const trapTabFocus = (card, event, activeElement) => {
  if (!card || event.key !== "Tab") return;
  const focusables = getFocusableElements(card);
  if (focusables.length === 0) {
    event.preventDefault();
    return;
  }
  const first = focusables[0];
  const last = focusables[focusables.length - 1];
  const inside = activeElement !== null && card.contains(activeElement);
  if (!inside) {
    event.preventDefault();
    first.focus();
    return;
  }
  if (event.shiftKey && activeElement === first) {
    event.preventDefault();
    last.focus();
    return;
  }
  if (!event.shiftKey && activeElement === last) {
    event.preventDefault();
    first.focus();
  }
};
const focusCard = (card) => {
  if (!card) return;
  try {
    ;
    card.focus?.();
  } catch {
  }
};
const _hoisted_1$5 = { class: "identity-client-card" };
const _hoisted_2$5 = { class: "identity-client-head" };
const _hoisted_3$5 = { class: "identity-client-main" };
const _hoisted_4$5 = { class: "identity-client-name" };
const _hoisted_5$4 = { class: "identity-client-host" };
const _hoisted_6$4 = { class: "identity-client-developer" };
const _sfc_main$5 = /* @__PURE__ */ defineComponent({
  __name: "IdentityClientCard",
  props: {
    client: {}
  },
  setup(__props) {
    const props = __props;
    const reviewLabel = computed(() => {
      const status = String(props.client?.review_status || "").trim().toLowerCase();
      if (status === "active" || status === "verified") return { text: "已审核", ok: true };
      if (status === "suspended" || status === "revoked") return { text: "已暂停", ok: false };
      return { text: "未审核", ok: false };
    });
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("div", _hoisted_1$5, [
        createBaseVNode("div", _hoisted_2$5, [
          _cache[0] || (_cache[0] = createBaseVNode("div", {
            class: "identity-client-icon",
            "aria-hidden": "true"
          }, [
            createBaseVNode("span", { class: "material-symbols-outlined" }, "apps")
          ], -1)),
          createBaseVNode("div", _hoisted_3$5, [
            createBaseVNode("h3", _hoisted_4$5, toDisplayString(__props.client.name || "未知应用"), 1),
            createBaseVNode("p", _hoisted_5$4, toDisplayString(__props.client.homepage_host || "—"), 1)
          ]),
          createBaseVNode("span", {
            class: normalizeClass(["identity-review-badge", { ok: reviewLabel.value.ok }])
          }, toDisplayString(reviewLabel.value.text), 3)
        ]),
        createBaseVNode("p", _hoisted_6$4, [
          _cache[1] || (_cache[1] = createBaseVNode("span", {
            class: "material-symbols-outlined identity-client-developer-icon",
            "aria-hidden": "true"
          }, "badge", -1)),
          createTextVNode(" 开发者：" + toDisplayString(__props.client.developer_display_name || "—"), 1)
        ])
      ]);
    };
  }
});
const IdentityClientCard = /* @__PURE__ */ _export_sfc(_sfc_main$5, [["__scopeId", "data-v-91e6e18f"]]);
const _hoisted_1$4 = { class: "identity-scope-list" };
const _hoisted_2$4 = {
  key: 0,
  class: "identity-scope-group"
};
const _hoisted_3$4 = { class: "identity-scope-items" };
const _hoisted_4$4 = { class: "identity-scope-text" };
const _hoisted_5$3 = {
  key: 1,
  class: "identity-scope-group identity-scope-group--sensitive",
  "aria-label": "敏感权限"
};
const _hoisted_6$3 = { class: "identity-scope-items" };
const _hoisted_7$3 = { class: "identity-scope-text" };
const _hoisted_8$2 = {
  class: "identity-scope-notice",
  role: "note"
};
const _hoisted_9$2 = {
  key: 1,
  class: "identity-scope-empty"
};
const _hoisted_10$2 = {
  class: "identity-nonofficial",
  role: "note"
};
const _sfc_main$4 = /* @__PURE__ */ defineComponent({
  __name: "IdentityScopeList",
  props: {
    scopes: {}
  },
  setup(__props) {
    const props = __props;
    const grouped = computed(() => groupScopesByRisk(props.scopes || []));
    const hasAny = computed(
      () => (props.scopes || []).length > 0
    );
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("div", _hoisted_1$4, [
        hasAny.value ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
          grouped.value.basic.length ? (openBlock(), createElementBlock("section", _hoisted_2$4, [
            _cache[1] || (_cache[1] = createBaseVNode("h4", { class: "identity-scope-group-title" }, "基础权限", -1)),
            createBaseVNode("ul", _hoisted_3$4, [
              (openBlock(true), createElementBlock(Fragment, null, renderList(grouped.value.basic, (scope2) => {
                return openBlock(), createElementBlock("li", {
                  key: scope2.id,
                  class: "identity-scope-item"
                }, [
                  _cache[0] || (_cache[0] = createBaseVNode("span", {
                    class: "material-symbols-outlined identity-scope-icon",
                    "aria-hidden": "true"
                  }, "info", -1)),
                  createBaseVNode("div", _hoisted_4$4, [
                    createBaseVNode("strong", null, toDisplayString(scope2.id), 1),
                    createBaseVNode("p", null, toDisplayString(scope2.label), 1)
                  ])
                ]);
              }), 128))
            ])
          ])) : createCommentVNode("", true),
          grouped.value.sensitive.length ? (openBlock(), createElementBlock("section", _hoisted_5$3, [
            _cache[4] || (_cache[4] = createBaseVNode("h4", { class: "identity-scope-group-title" }, [
              createBaseVNode("span", {
                class: "material-symbols-outlined identity-scope-group-title-icon",
                "aria-hidden": "true"
              }, "warning"),
              createTextVNode(" 敏感权限 ")
            ], -1)),
            createBaseVNode("ul", _hoisted_6$3, [
              (openBlock(true), createElementBlock(Fragment, null, renderList(grouped.value.sensitive, (scope2) => {
                return openBlock(), createElementBlock("li", {
                  key: scope2.id,
                  class: "identity-scope-item identity-scope-item--sensitive"
                }, [
                  _cache[2] || (_cache[2] = createBaseVNode("span", {
                    class: "material-symbols-outlined identity-scope-icon",
                    "aria-hidden": "true"
                  }, "shield_person", -1)),
                  createBaseVNode("div", _hoisted_7$3, [
                    createBaseVNode("strong", null, toDisplayString(scope2.id), 1),
                    createBaseVNode("p", null, toDisplayString(scope2.label), 1)
                  ])
                ]);
              }), 128))
            ]),
            createBaseVNode("p", _hoisted_8$2, [
              _cache[3] || (_cache[3] = createBaseVNode("span", {
                class: "material-symbols-outlined identity-scope-notice-icon",
                "aria-hidden": "true"
              }, "gpp_maybe", -1)),
              createTextVNode(" " + toDisplayString(unref(SENSITIVE_SCOPE_NOTICE)), 1)
            ])
          ])) : createCommentVNode("", true)
        ], 64)) : (openBlock(), createElementBlock("p", _hoisted_9$2, "该应用未请求任何权限")),
        createBaseVNode("p", _hoisted_10$2, [
          _cache[5] || (_cache[5] = createBaseVNode("span", {
            class: "material-symbols-outlined identity-nonofficial-icon",
            "aria-hidden": "true"
          }, "verified_user", -1)),
          createTextVNode(" " + toDisplayString(unref(NON_OFFICIAL_NOTICE)), 1)
        ])
      ]);
    };
  }
});
const IdentityScopeList = /* @__PURE__ */ _export_sfc(_sfc_main$4, [["__scopeId", "data-v-75640810"]]);
const _hoisted_1$3 = {
  key: 0,
  class: "identity-result",
  role: "status",
  "aria-live": "polite"
};
const _hoisted_2$3 = { class: "identity-result-title" };
const _hoisted_3$3 = { class: "identity-result-desc" };
const _hoisted_4$3 = { class: "identity-result-actions" };
const _sfc_main$3 = /* @__PURE__ */ defineComponent({
  __name: "IdentityResultState",
  props: {
    result: {}
  },
  emits: ["close"],
  setup(__props, { emit: __emit }) {
    const props = __props;
    const emit = __emit;
    const view = computed(() => {
      const result = props.result;
      if (!result) return null;
      switch (result.outcome) {
        case "approved":
          return {
            icon: "check_circle",
            tone: "ok",
            title: "已允许登录",
            desc: result.message || "网页将自动完成登录，你可以返回浏览器"
          };
        case "denied":
          return { icon: "cancel", tone: "warn", title: "已拒绝授权", desc: result.message || "已拒绝此次授权" };
        case "cancelled":
          return { icon: "close", tone: "warn", title: "已取消授权", desc: result.message || "已取消此次授权" };
        case "expired":
          return {
            icon: "schedule",
            tone: "warn",
            title: "请求已过期",
            desc: result.message || "应用请求已过期，请从网页重新发起"
          };
        case "error":
          return { icon: "error", tone: "err", title: "授权失败", desc: result.message || "授权处理失败，请稍后重试" };
      }
    });
    return (_ctx, _cache) => {
      return view.value ? (openBlock(), createElementBlock("div", _hoisted_1$3, [
        createBaseVNode("span", {
          class: normalizeClass(["material-symbols-outlined identity-result-icon", `tone-${view.value.tone}`]),
          "aria-hidden": "true"
        }, toDisplayString(view.value.icon), 3),
        createBaseVNode("h3", _hoisted_2$3, toDisplayString(view.value.title), 1),
        createBaseVNode("p", _hoisted_3$3, toDisplayString(view.value.desc), 1),
        createBaseVNode("div", _hoisted_4$3, [
          createBaseVNode("button", {
            class: "btn-primary btn-ripple",
            onClick: _cache[0] || (_cache[0] = ($event) => emit("close"))
          }, "完成")
        ])
      ])) : createCommentVNode("", true);
    };
  }
});
const IdentityResultState = /* @__PURE__ */ _export_sfc(_sfc_main$3, [["__scopeId", "data-v-b07a03da"]]);
const _hoisted_1$2 = { class: "identity-overlay-header" };
const _hoisted_2$2 = ["disabled"];
const _hoisted_3$2 = {
  key: 0,
  class: "identity-loading",
  role: "status"
};
const _hoisted_4$2 = {
  key: 1,
  class: "identity-loading",
  role: "status"
};
const _hoisted_5$2 = {
  key: 2,
  class: "identity-needs-login"
};
const _hoisted_6$2 = { class: "identity-actions" };
const _hoisted_7$2 = {
  key: 3,
  class: "identity-loading",
  role: "status"
};
const _hoisted_8$1 = {
  key: 4,
  class: "identity-approval-body"
};
const _hoisted_9$1 = {
  key: 0,
  class: "identity-test-banner",
  role: "note"
};
const _hoisted_10$1 = {
  class: "identity-current",
  "aria-label": "当前 Mini-HBUT 身份"
};
const _hoisted_11$1 = { class: "identity-current-line" };
const _hoisted_12$1 = {
  key: 0,
  class: "identity-current-sid"
};
const _hoisted_13$1 = { class: "identity-current-meta" };
const _hoisted_14$1 = { class: "identity-actions" };
const _hoisted_15$1 = ["disabled"];
const _hoisted_16$1 = ["disabled"];
const _sfc_main$2 = /* @__PURE__ */ defineComponent({
  __name: "IdentityApprovalOverlay",
  props: {
    identity: {},
    studentId: {},
    isLoggedIn: { type: Boolean },
    forceUpdateVisible: { type: Boolean },
    blockingAnnouncementVisible: { type: Boolean }
  },
  setup(__props) {
    const props = __props;
    const ui = identityUiState;
    const cardRef = ref(null);
    const phase2 = computed(() => ui.approvalPhase);
    const shouldShow = computed(
      () => overlayVisible.value && !props.forceUpdateVisible && !props.blockingAnnouncementVisible
    );
    let ovDiagTimer = null;
    const ovDiagReport = () => {
      try {
        void fetch("http://127.0.0.1:4399/debug/logs/push", {
          method: "POST",
          headers: { "content-type": "application/json" },
          body: JSON.stringify({
            scope: "identity-overlay",
            level: "info",
            message: `shouldShow=${String(shouldShow.value)} visible=${overlayVisible.value} force=${props.forceUpdateVisible} block=${props.blockingAnnouncementVisible} phase=${ui.approvalPhase} active=${ui.activeRequestId}`
          })
        }).catch(() => void 0);
      } catch {
      }
    };
    ovDiagReport();
    ovDiagTimer = setInterval(ovDiagReport, 3e3);
    onBeforeUnmount(() => {
      if (ovDiagTimer) clearInterval(ovDiagTimer);
    });
    const busy = computed(() => phase2.value === "approving" || phase2.value === "denying");
    const closeEnabled = computed(() => !busy.value);
    const cachedName = computed(() => getCachedStudentName(props.studentId || ""));
    const verifiedAtText = computed(() => {
      if (!ui.verifiedAt) return "—";
      try {
        return new Date(ui.verifiedAt).toLocaleString();
      } catch {
        return "—";
      }
    });
    const remainingSeconds = ref(0);
    let countdownTimer = null;
    const syncCountdown = () => {
      remainingSeconds.value = getRemainingSeconds(ui.requestDetail);
    };
    const startCountdown = () => {
      stopCountdown();
      syncCountdown();
      countdownTimer = setInterval(syncCountdown, 1e3);
    };
    const stopCountdown = () => {
      if (countdownTimer !== null) {
        clearInterval(countdownTimer);
        countdownTimer = null;
      }
    };
    const remainingText = computed(() => {
      const sec = remainingSeconds.value;
      if (sec <= 0) return "即将过期";
      return `${sec} 秒`;
    });
    const expiryUrgent = computed(() => remainingSeconds.value > 0 && remainingSeconds.value <= 30);
    const handleKeydown = (event) => {
      if (event.key === "Escape") {
        event.preventDefault();
        if (ui.lastResult) {
          props.identity.confirmResult();
        } else {
          void props.identity.cancelActive();
        }
        return;
      }
      trapTabFocus(cardRef.value, event, document.activeElement);
    };
    const handleClose = () => {
      if (!closeEnabled.value) return;
      if (ui.lastResult) {
        props.identity.confirmResult();
      } else {
        void props.identity.cancelActive();
      }
    };
    const handleGoLogin = () => {
      props.identity.goLogin();
    };
    watch(
      shouldShow,
      (visible) => {
        if (visible) {
          void nextTick(() => focusCard(cardRef.value));
          startCountdown();
        } else {
          stopCountdown();
        }
      },
      { immediate: true }
    );
    onBeforeUnmount(() => {
      stopCountdown();
    });
    return (_ctx, _cache) => {
      return openBlock(), createBlock(Teleport, { to: "body" }, [
        createVNode(Transition, { name: "modal-pop" }, {
          default: withCtx(() => [
            shouldShow.value ? (openBlock(), createElementBlock("div", {
              key: 0,
              class: "identity-overlay",
              role: "dialog",
              "aria-modal": "true",
              "aria-label": "Mini-HBUT 登录授权",
              onKeydown: handleKeydown
            }, [
              createBaseVNode("div", {
                ref_key: "cardRef",
                ref: cardRef,
                class: "identity-overlay-card modal-pop-card",
                tabindex: "-1"
              }, [
                createBaseVNode("header", _hoisted_1$2, [
                  _cache[5] || (_cache[5] = createBaseVNode("div", { class: "identity-overlay-title" }, [
                    createBaseVNode("span", {
                      class: "material-symbols-outlined identity-overlay-title-icon",
                      "aria-hidden": "true"
                    }, "verified_user"),
                    createBaseVNode("h2", null, "Mini-HBUT 登录授权")
                  ], -1)),
                  createBaseVNode("button", {
                    class: "identity-overlay-close",
                    type: "button",
                    "aria-label": "取消此次授权",
                    disabled: !closeEnabled.value,
                    onClick: handleClose
                  }, [..._cache[4] || (_cache[4] = [
                    createBaseVNode("span", {
                      class: "material-symbols-outlined",
                      "aria-hidden": "true"
                    }, "close", -1)
                  ])], 8, _hoisted_2$2)
                ]),
                phase2.value === "loading_request" || phase2.value === "received" ? (openBlock(), createElementBlock("div", _hoisted_3$2, [..._cache[6] || (_cache[6] = [
                  createBaseVNode("span", {
                    class: "material-symbols-outlined identity-loading-icon identity-spin",
                    "aria-hidden": "true"
                  }, "sync", -1),
                  createBaseVNode("h3", null, "正在获取授权信息…", -1)
                ])])) : phase2.value === "validating_session" ? (openBlock(), createElementBlock("div", _hoisted_4$2, [
                  _cache[7] || (_cache[7] = createBaseVNode("span", {
                    class: "material-symbols-outlined identity-loading-icon identity-spin",
                    "aria-hidden": "true"
                  }, "manage_accounts", -1)),
                  _cache[8] || (_cache[8] = createBaseVNode("h3", null, "正在验证学校登录状态…", -1)),
                  _cache[9] || (_cache[9] = createBaseVNode("p", { class: "identity-loading-hint" }, "验证通过后即可继续授权", -1)),
                  createBaseVNode("button", {
                    class: "btn-secondary btn-ripple identity-loading-cancel",
                    type: "button",
                    onClick: _cache[0] || (_cache[0] = ($event) => __props.identity.cancelActive())
                  }, " 取消 ")
                ])) : phase2.value === "needs_login" ? (openBlock(), createElementBlock("div", _hoisted_5$2, [
                  _cache[10] || (_cache[10] = createBaseVNode("span", {
                    class: "material-symbols-outlined identity-needs-login-icon",
                    "aria-hidden": "true"
                  }, "login", -1)),
                  _cache[11] || (_cache[11] = createBaseVNode("h3", null, "需要先登录 Mini-HBUT", -1)),
                  _cache[12] || (_cache[12] = createBaseVNode("p", null, "完成学校登录后，授权请求会自动继续，无需重新点击网页按钮。", -1)),
                  createBaseVNode("div", _hoisted_6$2, [
                    createBaseVNode("button", {
                      class: "btn-secondary btn-ripple",
                      type: "button",
                      onClick: _cache[1] || (_cache[1] = ($event) => __props.identity.denyActive())
                    }, "取消"),
                    createBaseVNode("button", {
                      class: "btn-primary btn-ripple",
                      type: "button",
                      onClick: handleGoLogin
                    }, "去登录")
                  ])
                ])) : busy.value ? (openBlock(), createElementBlock("div", _hoisted_7$2, [
                  _cache[13] || (_cache[13] = createBaseVNode("span", {
                    class: "material-symbols-outlined identity-loading-icon identity-spin",
                    "aria-hidden": "true"
                  }, "lock", -1)),
                  createBaseVNode("h3", null, toDisplayString(phase2.value === "approving" ? "正在提交授权…" : "正在处理…"), 1),
                  _cache[14] || (_cache[14] = createBaseVNode("p", { class: "identity-loading-hint" }, "请稍候，不要重复点击", -1))
                ])) : phase2.value === "ready" && unref(ui).requestDetail ? (openBlock(), createElementBlock("div", _hoisted_8$1, [
                  unref(ui).requestDetail.client.is_test ? (openBlock(), createElementBlock("div", _hoisted_9$1, [..._cache[15] || (_cache[15] = [
                    createTextVNode(" 🧪 ", -1),
                    createBaseVNode("strong", null, "测试应用", -1),
                    createTextVNode("：本授权仅用于链路测试， 不会获取、保存或使用你的任何真实数据。 ", -1)
                  ])])) : createCommentVNode("", true),
                  createVNode(IdentityClientCard, {
                    client: unref(ui).requestDetail.client
                  }, null, 8, ["client"]),
                  createVNode(IdentityScopeList, {
                    scopes: unref(ui).requestDetail.scopes
                  }, null, 8, ["scopes"]),
                  createBaseVNode("section", _hoisted_10$1, [
                    _cache[16] || (_cache[16] = createBaseVNode("h4", null, "当前 Mini-HBUT 身份", -1)),
                    createBaseVNode("p", _hoisted_11$1, [
                      createBaseVNode("strong", null, toDisplayString(cachedName.value || unref(maskStudentId)(__props.studentId)), 1),
                      cachedName.value ? (openBlock(), createElementBlock("span", _hoisted_12$1, toDisplayString(unref(maskStudentId)(__props.studentId)), 1)) : createCommentVNode("", true)
                    ]),
                    _cache[17] || (_cache[17] = createBaseVNode("p", { class: "identity-current-meta" }, "学校身份验证方式：Mini-HBUT 本地验证", -1)),
                    createBaseVNode("p", _hoisted_13$1, "最近验证：" + toDisplayString(verifiedAtText.value), 1)
                  ]),
                  createBaseVNode("p", {
                    class: normalizeClass(["identity-expiry", { urgent: expiryUrgent.value }])
                  }, [
                    _cache[18] || (_cache[18] = createBaseVNode("span", {
                      class: "material-symbols-outlined identity-expiry-icon",
                      "aria-hidden": "true"
                    }, "schedule", -1)),
                    createTextVNode(" 请求剩余时间：" + toDisplayString(remainingText.value), 1)
                  ], 2),
                  createBaseVNode("div", _hoisted_14$1, [
                    createBaseVNode("button", {
                      class: "btn-secondary btn-ripple identity-deny-btn",
                      type: "button",
                      disabled: busy.value,
                      onClick: _cache[2] || (_cache[2] = ($event) => __props.identity.denyActive())
                    }, " 拒绝 ", 8, _hoisted_15$1),
                    createBaseVNode("button", {
                      class: "btn-primary btn-ripple identity-allow-btn",
                      type: "button",
                      disabled: busy.value,
                      onClick: _cache[3] || (_cache[3] = ($event) => __props.identity.approveActive())
                    }, " 允许 ", 8, _hoisted_16$1)
                  ])
                ])) : unref(ui).lastResult ? (openBlock(), createBlock(IdentityResultState, {
                  key: 5,
                  result: unref(ui).lastResult,
                  onClose: handleClose
                }, null, 8, ["result"])) : createCommentVNode("", true)
              ], 512)
            ], 32)) : createCommentVNode("", true)
          ]),
          _: 1
        })
      ]);
    };
  }
});
const IdentityApprovalOverlay = /* @__PURE__ */ _export_sfc(_sfc_main$2, [["__scopeId", "data-v-0b857b5e"]]);
const createAsyncPage = (loader) => defineAsyncComponent({ loader, delay: 0, suspensible: false });
const loadDashboardView = () => __vitePreload(() => import("./Dashboard-UrJPaK7f.js"), true ? __vite__mapDeps([2,1,0,3,4,5,6,7,8,9]) : void 0, import.meta.url);
const loadGradeView = () => __vitePreload(() => import("./GradeView-N9gbvgbt.js"), true ? __vite__mapDeps([10,3,11,12,1,0,7,8,13]) : void 0, import.meta.url);
const loadElectricityView = () => __vitePreload(() => import("./ElectricityView-CLm3pg_r.js"), true ? __vite__mapDeps([14,15,1,0,11,16,17,18,3,7,8,19]) : void 0, import.meta.url);
const loadClassroomView = () => __vitePreload(() => import("./ClassroomView-cWUS1oSK.js"), true ? __vite__mapDeps([20,11,18,3,12,1,0,7,8,21]) : void 0, import.meta.url);
const loadScheduleView = () => __vitePreload(() => import("./ScheduleView-C4bOhmhT.js"), true ? __vite__mapDeps([22,1,0,11,3,7,8,23]) : void 0, import.meta.url);
const loadGlobalScheduleView = () => __vitePreload(() => import("./GlobalScheduleView-D84YvdEU.js"), true ? __vite__mapDeps([24,3,11,18,12,1,0,7,8,25]) : void 0, import.meta.url);
const loadCourseSelectionView = () => __vitePreload(() => import("./CourseSelectionView-BMRpc8XA.js"), true ? __vite__mapDeps([26,12,3,1,0,7,8,27]) : void 0, import.meta.url);
const loadStudentInfoView = () => __vitePreload(() => import("./StudentInfoView-DV69XQmO.js"), true ? __vite__mapDeps([28,3,11,1,0,12,7,8,29]) : void 0, import.meta.url);
const loadExamView = () => __vitePreload(() => import("./ExamView-BmaHbvFj.js"), true ? __vite__mapDeps([30,11,18,3,12,1,0,7,8,31]) : void 0, import.meta.url);
const loadRankingView = () => __vitePreload(() => import("./RankingView-Bs4h5R4V.js"), true ? __vite__mapDeps([32,11,18,3,12,1,0,7,8,33]) : void 0, import.meta.url);
const loadCalendarView = () => __vitePreload(() => import("./CalendarView-DH21TJJn.js"), true ? __vite__mapDeps([34,11,18,3,12,1,0,7,8,35]) : void 0, import.meta.url);
const loadSchoolInboxView = () => __vitePreload(() => import("./SchoolInboxView-MbHvX6_1.js"), true ? __vite__mapDeps([36,1,0,11,37,18,3,12,7,8,38]) : void 0, import.meta.url);
const loadAcademicProgressView = () => __vitePreload(() => import("./AcademicProgressView-DMsnKGje.js"), true ? __vite__mapDeps([39,3,11,18,12,1,0,7,8,40]) : void 0, import.meta.url);
const loadTrainingPlanView = () => __vitePreload(() => import("./TrainingPlanView-CFTILl1Z.js"), true ? __vite__mapDeps([41,3,11,18,12,1,0,7,8,42]) : void 0, import.meta.url);
const loadForumView = () => __vitePreload(() => import("./ForumView-9b3udUVk.js"), true ? __vite__mapDeps([43,1,0,3,7,8,44]) : void 0, import.meta.url);
const loadMeView = () => __vitePreload(() => import("./MeView-CVCZvOoF.js"), true ? __vite__mapDeps([45,3,6,1,0,7,8,46]) : void 0, import.meta.url);
const loadOfficialView = () => __vitePreload(() => import("./OfficialView-BcgMJjXp.js"), true ? __vite__mapDeps([47,1,0,18,3,12,7,8,48]) : void 0, import.meta.url);
const loadFeedbackView = () => __vitePreload(() => import("./FeedbackView-BV_i_ej-.js"), true ? __vite__mapDeps([49,1,0,18,3,12,7,8,50]) : void 0, import.meta.url);
const loadNotificationView = () => __vitePreload(() => import("./NotificationView-7-zUa1H-.js"), true ? __vite__mapDeps([51,3,6,1,0,16,4,5,11,7,8,52]) : void 0, import.meta.url);
const loadConfigEditorView = () => __vitePreload(() => import("./ConfigEditor-DcA0XKzI.js"), true ? __vite__mapDeps([53,3,54,55,1,0,7,8,56]) : void 0, import.meta.url);
const loadSettingsView = () => __vitePreload(() => import("./SettingsView-DVlvyPWH.js"), true ? __vite__mapDeps([57,1,0,3,7,8,58]) : void 0, import.meta.url);
const loadPrivacyDataView = () => __vitePreload(() => import("./PrivacyDataView-DyYcj3qh.js"), true ? __vite__mapDeps([59,0,1,18,3,7,8,60]) : void 0, import.meta.url);
const loadExportCenterView = () => __vitePreload(() => import("./ExportCenterView-C0qwtQ_s.js"), true ? __vite__mapDeps([61,1,0,3,11,8,7,62]) : void 0, import.meta.url);
const loadServiceStatsView = () => __vitePreload(() => import("./ServiceStatsView-sE71R7Th.js"), true ? __vite__mapDeps([63,3,1,0,7,8,64]) : void 0, import.meta.url);
const loadSchoolWebsiteView = () => __vitePreload(() => import("./SchoolWebsiteView-1SJXwiwj.js"), true ? __vite__mapDeps([65,1,0,66,3,7,8,67]) : void 0, import.meta.url);
const loadQuickLinksView = () => __vitePreload(() => import("./QuickLinksView-_vWGY7Id.js"), true ? __vite__mapDeps([68,3,1,0,7,8,69]) : void 0, import.meta.url);
const loadCampusNetworkView = () => __vitePreload(() => import("./CampusNetworkView-2ujEcVie.js"), true ? __vite__mapDeps([70,3,18,1,0,7,8,71]) : void 0, import.meta.url);
const loadMoreView = () => __vitePreload(() => import("./MoreView-DF8C1Y3V.js"), true ? __vite__mapDeps([72,73,3,0,1,7,8,74]) : void 0, import.meta.url);
const loadMoreModuleHostView = () => __vitePreload(() => import("./MoreModuleHostView-DWmzk142.js"), true ? __vite__mapDeps([75,1,0,18,3,12,7,8,76]) : void 0, import.meta.url);
const loadMoreChaoxingCheckinView = () => __vitePreload(() => import("./MoreChaoxingCheckinView-B7WqCfcF.js"), true ? __vite__mapDeps([77,3,18,12,73,78,1,0,7,8,79]) : void 0, import.meta.url);
const loadTransactionHistoryView = () => __vitePreload(() => import("./TransactionHistory-D0jvFeZr.js"), true ? __vite__mapDeps([80,11,1,0,12,3,7,8,81]) : void 0, import.meta.url);
const loadCampusCodeView = () => __vitePreload(() => import("./CampusCodeView-BG0C3oxN.js"), true ? __vite__mapDeps([82,15,1,0,18,3,7,8,83]) : void 0, import.meta.url);
const loadAiChatView = () => __vitePreload(() => import("./AiChatView-Gv9wZdbP.js"), true ? __vite__mapDeps([84,54,55,1,0,3,85,7,8,86]) : void 0, import.meta.url);
const loadCampusMapView = async () => {
  const { readCampusGuideMode } = await __vitePreload(async () => {
    const { readCampusGuideMode: readCampusGuideMode2 } = await import("./config-Cs6HnoHt.js");
    return { readCampusGuideMode: readCampusGuideMode2 };
  }, true ? __vite__mapDeps([87,1,0]) : void 0, import.meta.url);
  return readCampusGuideMode() === "tencent" ? __vitePreload(() => import("./CampusGuideShell-Di1XqMxg.js"), true ? __vite__mapDeps([88,89,1,0,87,78,3,18,7,8,90]) : void 0, import.meta.url) : __vitePreload(() => import("./CampusMapView-CGTJE6ya.js"), true ? __vite__mapDeps([91,3,89,1,0,92,18,7,8,93]) : void 0, import.meta.url);
};
const loadLibraryView = () => __vitePreload(() => import("./LibraryView-D5DIxlga.js"), true ? __vite__mapDeps([94,3,18,1,0,7,8,95]) : void 0, import.meta.url);
const loadResourceShareView = () => __vitePreload(() => import("./ResourceShareView-Cji6UT-x.js"), true ? __vite__mapDeps([96,1,0,18,3,7,8,97]) : void 0, import.meta.url);
const loadChaoxingClassView = () => __vitePreload(() => import("./ChaoxingClassView-KMl9RxsT.js"), true ? __vite__mapDeps([98,1,0,18,3,7,8,99]) : void 0, import.meta.url);
const loadChaoxingHubView = () => __vitePreload(() => import("./ChaoxingHubView-5Vb_Jemu.js"), true ? __vite__mapDeps([100,3,18,12,73,1,0,7,8,101]) : void 0, import.meta.url);
const loadChaoxingInboxView = () => __vitePreload(() => import("./ChaoxingInboxView-DpqaBG-F.js"), true ? __vite__mapDeps([102,1,0,11,37,18,3,12,7,8,103]) : void 0, import.meta.url);
const loadTeachingEvalView = () => __vitePreload(() => import("./TeachingEvalView-D5aVbcwj.js"), true ? __vite__mapDeps([104,3,1,0,18,12,7,8,105]) : void 0, import.meta.url);
const loadBroadbandView = () => __vitePreload(() => import("./BroadbandView-BgTF-IOD.js"), true ? __vite__mapDeps([106,17,1,0,15,18,3,7,8,107]) : void 0, import.meta.url);
const loadSportsVenueView = () => __vitePreload(() => import("./SportsVenueView-CB9g1DLj.js"), true ? __vite__mapDeps([108,3,1,0,18,12,7,8,109]) : void 0, import.meta.url);
const loadTowerGoView = () => __vitePreload(() => import("./TowerGoView-D9BUe_PI.js"), true ? __vite__mapDeps([110,3,92,1,0,89,7,8,111]) : void 0, import.meta.url);
const loadSmartOrientationView = () => __vitePreload(() => import("./SmartOrientationView-DeqBG5RH.js"), true ? __vite__mapDeps([112,1,0,11,18,3,12,7,8,113]) : void 0, import.meta.url);
const loadIdentityAuthHistoryView = () => __vitePreload(() => import("./IdentityAuthHistoryView-Yiwq0-3C.js"), true ? __vite__mapDeps([114,18,3,12,1,0,7,8,115]) : void 0, import.meta.url);
const VIEW_COMPONENTS = {
  home: createAsyncPage(loadDashboardView),
  grades: createAsyncPage(loadGradeView),
  electricity: createAsyncPage(loadElectricityView),
  classroom: createAsyncPage(loadClassroomView),
  schedule: createAsyncPage(loadScheduleView),
  qxzkb: createAsyncPage(loadGlobalScheduleView),
  course_selection: createAsyncPage(loadCourseSelectionView),
  studentinfo: createAsyncPage(loadStudentInfoView),
  exams: createAsyncPage(loadExamView),
  ranking: createAsyncPage(loadRankingView),
  calendar: createAsyncPage(loadCalendarView),
  school_inbox: createAsyncPage(loadSchoolInboxView),
  academic: createAsyncPage(loadAcademicProgressView),
  training: createAsyncPage(loadTrainingPlanView),
  forum: createAsyncPage(loadForumView),
  me: createAsyncPage(loadMeView),
  official: createAsyncPage(loadOfficialView),
  feedback: createAsyncPage(loadFeedbackView),
  notifications: createAsyncPage(loadNotificationView),
  config: createAsyncPage(loadConfigEditorView),
  settings: createAsyncPage(loadSettingsView),
  privacy_data: createAsyncPage(loadPrivacyDataView),
  export_center: createAsyncPage(loadExportCenterView),
  service_stats: createAsyncPage(loadServiceStatsView),
  school_website: createAsyncPage(loadSchoolWebsiteView),
  quick_links: createAsyncPage(loadQuickLinksView),
  campus_network: createAsyncPage(loadCampusNetworkView),
  more: createAsyncPage(loadMoreView),
  more_module_host: createAsyncPage(loadMoreModuleHostView),
  more_chaoxing_checkin: createAsyncPage(loadMoreChaoxingCheckinView),
  transactions: createAsyncPage(loadTransactionHistoryView),
  campus_code: createAsyncPage(loadCampusCodeView),
  ai: createAsyncPage(loadAiChatView),
  campus_map: createAsyncPage(loadCampusMapView),
  library: createAsyncPage(loadLibraryView),
  resource_share: createAsyncPage(loadResourceShareView),
  chaoxing_class: createAsyncPage(loadChaoxingClassView),
  chaoxing_hub: createAsyncPage(loadChaoxingHubView),
  chaoxing_inbox: createAsyncPage(loadChaoxingInboxView),
  teaching_eval: createAsyncPage(loadTeachingEvalView),
  broadband: createAsyncPage(loadBroadbandView),
  sports_venue: createAsyncPage(loadSportsVenueView),
  towergo: createAsyncPage(loadTowerGoView),
  smart_orientation: createAsyncPage(loadSmartOrientationView),
  identity_auth_history: createAsyncPage(loadIdentityAuthHistoryView)
};
const VIEW_PREFETCHERS = {
  home: loadDashboardView,
  schedule: loadScheduleView,
  qxzkb: loadGlobalScheduleView,
  course_selection: loadCourseSelectionView,
  me: loadMeView,
  official: loadOfficialView,
  feedback: loadFeedbackView,
  config: loadConfigEditorView,
  settings: loadSettingsView,
  privacy_data: loadPrivacyDataView,
  export_center: loadExportCenterView,
  service_stats: loadServiceStatsView,
  school_website: loadSchoolWebsiteView,
  quick_links: loadQuickLinksView,
  campus_network: loadCampusNetworkView,
  more: loadMoreView,
  more_module_host: loadMoreModuleHostView,
  more_chaoxing_checkin: loadMoreChaoxingCheckinView,
  grades: loadGradeView,
  electricity: loadElectricityView,
  transactions: loadTransactionHistoryView,
  campus_code: loadCampusCodeView,
  notifications: loadNotificationView,
  classroom: loadClassroomView,
  studentinfo: loadStudentInfoView,
  exams: loadExamView,
  ranking: loadRankingView,
  calendar: loadCalendarView,
  school_inbox: loadSchoolInboxView,
  academic: loadAcademicProgressView,
  training: loadTrainingPlanView,
  forum: loadForumView,
  ai: loadAiChatView,
  campus_map: loadCampusMapView,
  library: loadLibraryView,
  resource_share: loadResourceShareView,
  chaoxing_class: loadChaoxingClassView,
  chaoxing_hub: loadChaoxingHubView,
  chaoxing_inbox: loadChaoxingInboxView,
  teaching_eval: loadTeachingEvalView,
  broadband: loadBroadbandView,
  sports_venue: loadSportsVenueView,
  towergo: loadTowerGoView,
  smart_orientation: loadSmartOrientationView,
  identity_auth_history: loadIdentityAuthHistoryView
};
const normalizeIdentifier = (value) => String(value ?? "").trim();
const useAuthStore = defineStore("auth", () => {
  const studentId = ref("");
  const userUuid = ref("");
  const hydrated = ref(false);
  const onlineSessionState = ref("unknown");
  const isLoggedIn = computed(() => studentId.value.length > 0);
  const hydrate = (snapshot = {}) => {
    studentId.value = normalizeIdentifier(snapshot.studentId);
    userUuid.value = normalizeIdentifier(snapshot.userUuid);
    hydrated.value = true;
  };
  const establishSession = (snapshot) => {
    const nextStudentId = normalizeIdentifier(snapshot.studentId);
    if (!nextStudentId) throw new Error("studentId is required to establish a session");
    studentId.value = nextStudentId;
    userUuid.value = normalizeIdentifier(snapshot.userUuid);
    hydrated.value = true;
  };
  const clearSession = () => {
    studentId.value = "";
    userUuid.value = "";
    onlineSessionState.value = "unknown";
    hydrated.value = true;
  };
  return {
    studentId,
    userUuid,
    hydrated,
    onlineSessionState,
    isLoggedIn,
    hydrate,
    establishSession,
    clearSession
  };
});
const useGradeStore = defineStore("grade", () => {
  const grades = ref([]);
  const refreshing = ref(false);
  const offline = ref(false);
  const syncTime = ref("");
  const lastError = ref("");
  const hasGrades = computed(() => grades.value.length > 0);
  const hydrate = (snapshot = {}) => {
    grades.value = Array.isArray(snapshot.grades) ? [...snapshot.grades] : [];
    offline.value = Boolean(snapshot.offline);
    syncTime.value = String(snapshot.syncTime ?? "").trim();
    lastError.value = "";
  };
  const beginRefresh = () => {
    refreshing.value = true;
    lastError.value = "";
  };
  const finishRefresh = (snapshot) => {
    hydrate(snapshot);
    refreshing.value = false;
  };
  const failRefresh = (error, preserveExisting = true) => {
    refreshing.value = false;
    offline.value = preserveExisting && grades.value.length > 0;
    lastError.value = error instanceof Error ? error.message : String(error ?? "unknown grade error");
  };
  const clear = () => {
    grades.value = [];
    refreshing.value = false;
    offline.value = false;
    syncTime.value = "";
    lastError.value = "";
  };
  return {
    grades,
    refreshing,
    offline,
    syncTime,
    lastError,
    hasGrades,
    hydrate,
    beginRefresh,
    finishRefresh,
    failRefresh,
    clear
  };
});
const DEFAULT_RESUME_POLICY = {
  softRemountAfterMs: 10 * 60 * 1e3,
  hardReloadAfterMs: 15 * 60 * 1e3,
  hardReloadBudget: 1
};
const useLifecycleStore = defineStore("lifecycle", () => {
  const hiddenAt = ref(0);
  const lastResumeAt = ref(0);
  const hardReloadCount = ref(0);
  const phase2 = ref("active");
  const isHidden = computed(() => phase2.value === "hidden");
  const markHidden = (at = Date.now()) => {
    hiddenAt.value = Math.max(0, at);
    phase2.value = "hidden";
  };
  const consumeHiddenDuration = (at = Date.now()) => {
    const duration = hiddenAt.value > 0 ? Math.max(0, at - hiddenAt.value) : 0;
    hiddenAt.value = 0;
    lastResumeAt.value = at;
    phase2.value = "resuming";
    return duration;
  };
  const markActive = () => {
    phase2.value = "active";
  };
  const evaluateResume = (idleMs, policy = DEFAULT_RESUME_POLICY) => {
    if (idleMs >= policy.hardReloadAfterMs && hardReloadCount.value < policy.hardReloadBudget) return "hard-reload";
    if (idleMs >= policy.softRemountAfterMs) return "soft-remount";
    return "none";
  };
  const recordHardReload = () => {
    hardReloadCount.value += 1;
  };
  return {
    hiddenAt,
    lastResumeAt,
    hardReloadCount,
    phase: phase2,
    isHidden,
    markHidden,
    consumeHiddenDuration,
    markActive,
    evaluateResume,
    recordHardReload
  };
});
const MAIN_TABS = ["home", "schedule", "notifications", "me"];
const ME_SUB_VIEWS = [
  "official",
  "feedback",
  "config",
  "settings",
  "privacy_data",
  "export_center",
  "service_stats",
  "school_website",
  "quick_links",
  "campus_network",
  "identity_auth_history",
  "more",
  "more_module_host",
  "more_chaoxing_checkin"
];
const LOGIN_REQUIRED_ME_VIEWS = ["school_website", "quick_links", "campus_network"];
const isLoginRequiredView = (view) => {
  const normalized = String(view).trim();
  return LOGIN_REQUIRED_ME_VIEWS.includes(normalized);
};
const HIERARCHICAL_PARENT_VIEW_MAP = Object.freeze({
  schedule: "home",
  forum: "home",
  notifications: "home",
  me: "home",
  official: "me",
  feedback: "me",
  config: "me",
  settings: "me",
  privacy_data: "me",
  export_center: "me",
  service_stats: "me",
  school_website: "me",
  quick_links: "me",
  campus_network: "me",
  identity_auth_history: "me",
  more: "me",
  more_module_host: "more",
  more_chaoxing_checkin: "more",
  smart_orientation: "home"
});
const normalizeViewName = (view) => {
  const normalized = String(view || "").trim();
  return normalized || "home";
};
const MAIN_TAB_SET = new Set(MAIN_TABS);
const ME_SUB_VIEW_SET = new Set(ME_SUB_VIEWS);
const resolveTab = (view, requestedTab) => {
  const requestedRaw = String(requestedTab ?? "").trim();
  if (requestedRaw) {
    const requested = normalizeViewName(requestedRaw);
    if (MAIN_TAB_SET.has(requested)) return requested;
  }
  if (MAIN_TAB_SET.has(view)) return view;
  if (ME_SUB_VIEW_SET.has(view)) return "me";
  return "home";
};
const resolveModule = (view, requestedModule) => {
  const explicit = String(requestedModule ?? "").trim();
  if (explicit) return explicit;
  return MAIN_TAB_SET.has(view) || view === "home" ? "" : view;
};
const useNavigationStore = defineStore("navigation", () => {
  const currentView = ref("home");
  const activeTab = ref("home");
  const currentModule = ref("");
  const navDirection = ref("replace");
  const initialized2 = ref(false);
  const isRootView = computed(() => MAIN_TAB_SET.has(currentView.value));
  const applySnapshot = (snapshot = {}) => {
    const view = normalizeViewName(snapshot.view || "home");
    currentView.value = view;
    activeTab.value = resolveTab(view, snapshot.tab);
    currentModule.value = resolveModule(view, snapshot.module);
    navDirection.value = snapshot.direction || "replace";
    initialized2.value = true;
  };
  const navigate = (view, options = {}) => {
    applySnapshot({ view, ...options, direction: options.direction || "forward" });
  };
  const replace = (snapshot) => applySnapshot({ ...snapshot, direction: "replace" });
  const backTo = (view, tab) => applySnapshot({ view, tab, direction: "back" });
  return {
    currentView,
    activeTab,
    currentModule,
    navDirection,
    initialized: initialized2,
    isRootView,
    applySnapshot,
    navigate,
    replace,
    backTo
  };
});
const useUpdateStore = defineStore("update", () => {
  const dialogVisible = ref(false);
  const checking = ref(false);
  const currentVersion = ref("");
  const availableVersion = ref("");
  const forceUpdateInfo = ref(null);
  const lastError = ref("");
  const forceRequired = computed(() => forceUpdateInfo.value !== null);
  const beginCheck = () => {
    checking.value = true;
    lastError.value = "";
  };
  const finishCheck = (version) => {
    checking.value = false;
    availableVersion.value = String(version ?? "").trim();
  };
  const failCheck = (error) => {
    checking.value = false;
    lastError.value = error instanceof Error ? error.message : String(error ?? "unknown update error");
  };
  const requireForceUpdate = (info) => {
    forceUpdateInfo.value = { ...info };
  };
  const clearForceUpdate = () => {
    forceUpdateInfo.value = null;
  };
  return {
    dialogVisible,
    checking,
    currentVersion,
    availableVersion,
    forceUpdateInfo,
    lastError,
    forceRequired,
    beginCheck,
    finishCheck,
    failCheck,
    requireForceUpdate,
    clearForceUpdate
  };
});
const SESSION_COOKIE_KEY = "hbu_session_cookies";
const SESSION_COOKIE_TIME_KEY = "hbu_session_cookie_time";
const COOKIE_SNAPSHOT_KEY = "hbu_cookie_snapshot";
const LOGIN_SESSION_TOKEN_KEY$1 = "hbu_login_session_token";
const LOGIN_METHOD_KEY = "hbu_login_method";
const LOGIN_METHOD_VIEW_KEY = "hbu_login_entry_mode";
const LOGIN_TEMP_FLAG_KEY = "hbu_login_temporary";
const LOGOUT_REASON_KEY = "hbu_logout_reason";
const TEMP_SESSION_EXPIRED_REASON = "temp_session_expired";
const JWXT_MAINTENANCE_KEY = "hbu_jwxt_maintenance";
const JWXT_MAINTENANCE_TIME_KEY = "hbu_jwxt_maintenance_time";
const JWXT_MAINTENANCE_HINT_KEY = "hbu_jwxt_maintenance_hint";
const JWXT_MAINTENANCE_DETAIL_KEY = "hbu_jwxt_maintenance_detail";
const JWXT_MAINTENANCE_PHASE_KEY = "hbu_jwxt_maintenance_phase";
const JWXT_MAINTENANCE_EVENT = "hbu-jwxt-maintenance";
const REMOTE_CONFIG_MODE_EVENT = "hbu-remote-config-mode-changed";
const ANNOUNCEMENT_CONFIRM_KEY = "hbu_announcement_confirmed";
const ANNOUNCEMENT_SNAPSHOT_KEY = "hbu_announcement_snapshot";
const API_BASE$2 = "/api";
const MODULE_HOST_SESSION_KEY = "hbu_more_module_host_session";
const HOME_LAYOUT_DEBUG_HIDDEN_KEY = "hbu_home_layout_debug_hidden";
const HOME_LAYOUT_DEBUG_FORCE_KEY = "hbu_home_layout_debug_enabled";
const HOME_SCROLL_STORAGE_KEY = "hbu_home_scroll_top";
const SESSION_REFRESH_INTERVAL = 20 * 60 * 1e3;
const ELECTRICITY_REFRESH_INTERVAL = 10 * 60 * 1e3;
const JWXT_RECOVERY_INTERVAL = 65 * 1e3;
const REMOTE_CONFIG_REFRESH_INTERVAL = 60 * 1e3;
const GRADE_CACHE_REFRESH_RETRY_MS = 8e3;
const IOS_RESUME_SOFT_REMOUNT_MS = 10 * 60 * 1e3;
const IOS_RESUME_HARD_RELOAD_MS = 15 * 60 * 1e3;
const IOS_RELOAD_MIN_INTERVAL_MS = 60 * 1e3;
const IOS_HARD_RELOAD_MAX_PER_SESSION = 1;
const DAILY_ACCESS_STORAGE_KEY = "hbu_daily_access_grant";
const DAILY_ACCESS_SALT = "MiniHBUT::DailyAccess::2025";
const KEY_ALPHABET = "23456789ABCDEFGHJKLMNPQRSTUVWXYZ";
const getAccessStorage = () => {
  if (typeof window === "undefined") return null;
  return window.sessionStorage || window.localStorage;
};
const PROTECTED_VIEWS = Object.freeze([
  // 视图级门禁已迁移到模块级（更多页仅刷课模块单点校验）
]);
const PROTECTED_VIEW_LABELS = Object.freeze({
  more: "更多中心"
});
const normalizeDate = (value = /* @__PURE__ */ new Date()) => {
  if (value instanceof Date && !Number.isNaN(value.getTime())) return value;
  const parsed = new Date(value);
  if (!Number.isNaN(parsed.getTime())) return parsed;
  return /* @__PURE__ */ new Date();
};
const getDailyDateStamp = (value = /* @__PURE__ */ new Date()) => {
  const date = normalizeDate(value);
  const year = date.getFullYear();
  const month = `${date.getMonth() + 1}`.padStart(2, "0");
  const day = `${date.getDate()}`.padStart(2, "0");
  return `${year}${month}${day}`;
};
const fnv1a = (input) => {
  let hash = 2166136261;
  for (const char of String(input || "")) {
    hash ^= char.codePointAt(0) || 0;
    hash = Math.imul(hash, 16777619);
  }
  return hash >>> 0;
};
const encodeChunk = (value, length = 5) => {
  let current = value >>> 0;
  let output = "";
  for (let i = 0; i < length; i += 1) {
    output = KEY_ALPHABET[current & 31] + output;
    current = current >>> 5;
  }
  return output;
};
const generateDailyAccessKey = (value = /* @__PURE__ */ new Date()) => {
  const stamp = getDailyDateStamp(value);
  const left = encodeChunk((fnv1a(`${DAILY_ACCESS_SALT}:${stamp}:L`) ^ 324508639) >>> 0);
  const right = encodeChunk((fnv1a(`${DAILY_ACCESS_SALT}:${stamp}:R`) ^ 610839776) >>> 0);
  return `${left}-${right}`;
};
const sanitizeDailyAccessInput = (value = "") => {
  const cleaned = String(value || "").toUpperCase().replace(/[^A-Z0-9]/g, "").replace(/[IO]/g, "").slice(0, 10);
  return cleaned.length > 5 ? `${cleaned.slice(0, 5)}-${cleaned.slice(5)}` : cleaned;
};
const verifyDailyAccessKey = (value, date = /* @__PURE__ */ new Date()) => {
  return sanitizeDailyAccessInput(value) === generateDailyAccessKey(date);
};
const isProtectedView = (view) => {
  const normalized = String(view || "").trim();
  return PROTECTED_VIEWS.includes(normalized);
};
const getProtectedViewLabel = (view) => {
  const normalized = String(view || "").trim();
  return PROTECTED_VIEW_LABELS[normalized] || "该页面";
};
const hasDailyAccessGrant = (date = /* @__PURE__ */ new Date()) => {
  const storage = getAccessStorage();
  if (!storage) return false;
  const expected = getDailyDateStamp(date);
  const stored = String(storage.getItem(DAILY_ACCESS_STORAGE_KEY) || "").trim();
  if (!stored) return false;
  if (stored === expected) return true;
  storage.removeItem(DAILY_ACCESS_STORAGE_KEY);
  return false;
};
const markDailyAccessGranted = (date = /* @__PURE__ */ new Date()) => {
  const storage = getAccessStorage();
  if (!storage) return;
  storage.setItem(DAILY_ACCESS_STORAGE_KEY, getDailyDateStamp(date));
};
const clearDailyAccessGrant = () => {
  const storage = getAccessStorage();
  if (!storage) return;
  storage.removeItem(DAILY_ACCESS_STORAGE_KEY);
};
const createAppState = (stores, options = {}) => {
  const navigationRefs = storeToRefs(stores.navigation);
  const authRefs = storeToRefs(stores.auth);
  const gradeRefs = storeToRefs(stores.grade);
  const updateRefs = storeToRefs(stores.update);
  const viewTransitionEnterActive = computed(
    () => navigationRefs.navDirection.value === "back" ? "module-fade-back-enter-active" : navigationRefs.navDirection.value === "forward" ? "module-fade-fwd-enter-active" : void 0
  );
  const viewTransitionLeaveActive = computed(
    () => navigationRefs.navDirection.value === "back" ? "module-fade-back-leave-active" : navigationRefs.navDirection.value === "forward" ? "module-fade-fwd-leave-active" : void 0
  );
  const viewTransitionEnterFrom = computed(
    () => navigationRefs.navDirection.value === "back" ? "module-fade-back-enter-from" : navigationRefs.navDirection.value === "forward" ? "module-fade-fwd-enter-from" : void 0
  );
  const viewTransitionLeaveTo = computed(
    () => navigationRefs.navDirection.value === "back" ? "module-fade-back-leave-to" : navigationRefs.navDirection.value === "forward" ? "module-fade-fwd-leave-to" : void 0
  );
  const savedLoginMode = String(localStorage.getItem(LOGIN_METHOD_VIEW_KEY) || "").trim();
  const mutable = {
    unlistenCloseRequested: null,
    isClosingByUser: false,
    viewportResizeRaf: 0,
    desktopResizePerfTimer: null,
    pendingScrollToTopOnViewChange: false,
    lastResumeHandledAt: 0,
    resumePendingSnapshot: null,
    iosReloadFallbackAt: 0,
    iosHardReloadCount: 0,
    lastSoftRemountAt: 0,
    appBootstrapped: false,
    capacitorAppStateListener: null,
    widgetCrossDayTimer: null,
    removeNotificationActionListener: null,
    removeHomeLayoutDiagnosticsErrorCapture: null,
    gradeTeacherRefreshTimer: null,
    gradeRealtimeRetryTimer: null,
    gradeNavigationToken: 0,
    sessionKeepAliveTimer: null,
    electricityKeepAliveTimer: null,
    jwxtRecoveryTimer: null,
    jwxtRecoveryInFlight: false,
    remoteConfigRefreshTimer: null,
    markdownModulePromise: null,
    activeAnnouncementRenderToken: 0,
    blockingAnnouncementRenderToken: 0
  };
  const remoteConfig = ref(null);
  const announcementData = ref({ pinned: [], ticker: [], list: [], confirm: [] });
  const activeAnnouncement = ref(null);
  const showAnnouncementModal = ref(false);
  const blockingAnnouncement = ref(null);
  const showBlockingAnnouncement = ref(false);
  const activeAnnouncementHtml = ref("");
  const blockingAnnouncementHtml = ref("");
  const configAdminIds = computed(() => {
    const ids = remoteConfig.value?.config_admin_ids;
    const merged = new Set(Array.isArray(ids) ? ids : []);
    merged.add("2510231106");
    return [...merged];
  });
  const isConfigAdmin = computed(() => configAdminIds.value.includes(authRefs.studentId.value));
  const aiModelOptions = computed(() => {
    const models = remoteConfig.value?.ai_models;
    return Array.isArray(models) ? models : [];
  });
  const forceUpdateResolvedUrl = computed(() => {
    if (!allowsInAppGithubUpdater()) {
      const store = String(updateRefs.forceUpdateInfo.value?.store_url || "").trim();
      return store;
    }
    const raw = String(updateRefs.forceUpdateInfo.value?.download_url || "").trim();
    return raw.startsWith("https://") ? toGhProxyUrl(raw) : raw;
  });
  const forceUpdateDisplayUrl = computed(() => {
    const url = forceUpdateResolvedUrl.value;
    if (!url) return "";
    try {
      const parsed = new URL(url);
      return `${parsed.hostname}${parsed.pathname}`;
    } catch {
      return url;
    }
  });
  const pendingProtectedView = ref(null);
  const homeLayoutDebugHidden = ref(
    typeof window !== "undefined" && localStorage.getItem(HOME_LAYOUT_DEBUG_HIDDEN_KEY) === "1"
  );
  const showTabBar = computed(
    () => MAIN_TABS.includes(navigationRefs.currentView.value)
  );
  const homeLayoutDebugForced = (() => {
    if (typeof window === "undefined") return false;
    const search = window.location.search || "";
    const hash = window.location.hash || "";
    return localStorage.getItem(HOME_LAYOUT_DEBUG_FORCE_KEY) === "1" || /(?:[?&])debugLayout=1(?:&|$)/.test(search) || /(?:[?&])debugLayout=1(?:&|$)/.test(hash);
  })();
  return {
    currentView: navigationRefs.currentView,
    activeTab: navigationRefs.activeTab,
    currentModule: navigationRefs.currentModule,
    navDirection: navigationRefs.navDirection,
    studentId: authRefs.studentId,
    userUuid: authRefs.userUuid,
    isLoggedIn: authRefs.isLoggedIn,
    onlineSessionState: authRefs.onlineSessionState,
    gradeData: gradeRefs.grades,
    gradesOffline: gradeRefs.offline,
    gradesSyncTime: gradeRefs.syncTime,
    isLoading: gradeRefs.refreshing,
    showUpdateDialog: updateRefs.dialogVisible,
    showForceUpdate: ref(false),
    forceUpdateInfo: updateRefs.forceUpdateInfo,
    forceUpdateResolvedUrl,
    forceUpdateDisplayUrl,
    showSplash: ref(
      useUiSettings().splashEnabled !== false && !options.skipSplashForFastScheduleBoot
    ),
    splashStatus: ref("connecting"),
    splashStatusText: ref("正在启动…"),
    splashRef: ref(null),
    viewRenderNonce: ref(0),
    widgetDeeplinkDate: ref(""),
    widgetDeeplinkPeriod: ref(0),
    moduleHostSession: ref({}),
    appShellRef: ref(null),
    homeScrollSnapshot: ref(0),
    homeScrollRestoring: ref(false),
    loginMode: ref(savedLoginMode && savedLoginMode !== "auto" ? savedLoginMode : "portal_password"),
    showLoginPrompt: ref(false),
    showExitDialog: ref(false),
    exitingApp: ref(false),
    showDailyAccessDialog: ref(false),
    dailyAccessInput: ref(""),
    dailyAccessError: ref(""),
    pendingProtectedView,
    lastGradeRefreshUsedOffline: ref(false),
    gradeTeacherCache: ref(null),
    gradeTeacherCacheSid: ref(""),
    jwxtMaintenanceMode: ref(false),
    jwxtMaintenanceHint: ref(""),
    jwxtMaintenanceDetail: ref(""),
    jwxtRecoveryPhase: ref("idle"),
    jwxtLastCheckTime: ref(""),
    jwxtSessionLastError: ref(""),
    remoteConfig,
    announcementData,
    activeAnnouncement,
    showAnnouncementModal,
    blockingAnnouncement,
    showBlockingAnnouncement,
    activeAnnouncementHtml,
    blockingAnnouncementHtml,
    showWorkspaceLayoutEditor: ref(false),
    workspaceLayoutEditorTab: ref("home"),
    homeLayoutDebugHidden,
    homeLayoutDebugExpanded: ref(false),
    homeLayoutDebugReport: ref(""),
    homeLayoutDebugForced,
    viewTransitionEnterActive,
    viewTransitionLeaveActive,
    viewTransitionEnterFrom,
    viewTransitionLeaveTo,
    protectedViewPromptTitle: computed(
      () => getProtectedViewLabel(pendingProtectedView.value?.view)
    ),
    configAdminIds,
    isConfigAdmin,
    aiModelOptions,
    showTabBar,
    showHomeLayoutDebug: computed(
      () => navigationRefs.currentView.value === "home" && !homeLayoutDebugHidden.value && homeLayoutDebugForced
    ),
    mutable
  };
};
function resolvePolicySafeView(view, fallback = "home") {
  const normalized = normalizeViewName(view);
  if (isViewAllowed(normalized)) return normalized;
  const safeFallback = normalizeViewName(fallback);
  return isViewAllowed(safeFallback) ? safeFallback : "home";
}
function resolvePolicySafeSnapshotView(snapshot, currentView = "home", fallback = "home") {
  const raw = snapshot?.view || snapshot?.module || snapshot?.tab || currentView;
  return resolvePolicySafeView(raw, fallback);
}
const DISABLED_HOME_MODULE_REASONS = Object.freeze({
  // 场馆 H5 依赖 172.16.54.20 校园网 + accessToken；外网 third/open 无法落地
  sports_venue: "运动场馆需校园内网，当前环境暂不可用"
});
function isHomeModuleHardDisabled(moduleId) {
  return Object.prototype.hasOwnProperty.call(
    DISABLED_HOME_MODULE_REASONS,
    String(moduleId || "").trim()
  );
}
function canOpenModule(module, session = {}) {
  const id = typeof module === "string" ? String(module || "").trim() : String(module?.id || "").trim();
  if (!id) {
    return { ok: false, reason: "未知模块" };
  }
  if (isHomeModuleHardDisabled(id)) {
    return {
      ok: false,
      reason: DISABLED_HOME_MODULE_REASONS[id] || "暂不可用"
    };
  }
  if (typeof module === "object" && module) {
    if (module.available === false) {
      return {
        ok: false,
        reason: String(module.unavailableReason || module.desc || "").trim() || "暂不可用"
      };
    }
    if (module.requiresLogin && !session.isLoggedIn) {
      return { ok: false, reason: "请先登录", needLogin: true };
    }
  }
  return { ok: true };
}
function decideHomeNavigate(moduleId, modules, session = {}) {
  const id = String(moduleId || "").trim();
  if (!id) {
    return { ok: false, reason: "未知模块" };
  }
  const found = (modules || []).find((m) => String(m?.id || "").trim() === id);
  return canOpenModule(found || { id }, session);
}
const asRecord$1 = (value) => value !== null && typeof value === "object" && !Array.isArray(value) ? value : {};
const errorMessage = (error) => {
  if (error instanceof Error) return error.message;
  if (error !== null && typeof error === "object") {
    const record = error;
    const candidate = [record.message, record.error, record.kind].filter((item) => typeof item === "string" && item.trim().length > 0).map((item) => item.trim()).find((item) => item.length > 0);
    if (candidate) return candidate;
  }
  return String(error ?? "");
};
const invoke = (command, args) => invokeNative(command, args);
const hasTauri = isTauriRuntime();
const LOCAL_BRIDGE = "http://127.0.0.1:4399";
const BRIDGE_BASE = hasTauri ? LOCAL_BRIDGE : "/bridge";
const looksLikeJson = (contentType, text) => {
  if (contentType.includes("application/json")) return true;
  const trimmed = (text || "").trim();
  return trimmed.startsWith("{") || trimmed.startsWith("[");
};
const looksLikeHtml = (contentType, text) => {
  if (contentType.includes("text/html")) return true;
  const trimmed = (text || "").trim().toLowerCase();
  return trimmed.startsWith("<!doctype") || trimmed.startsWith("<html");
};
const parseJsonSafely = (text) => {
  try {
    return JSON.parse(text);
  } catch (e) {
    return null;
  }
};
const DEFAULT_BRIDGE_TIMEOUT_MS = 12e3;
const fetchBridgeJson = async (url, options = {}, fallbackUrl = null, timeoutMs = DEFAULT_BRIDGE_TIMEOUT_MS) => {
  const controller = typeof AbortController !== "undefined" ? new AbortController() : null;
  const timer = controller ? setTimeout(() => controller.abort(), timeoutMs) : null;
  try {
    const res = await fetch(url, controller ? { ...options, signal: controller.signal } : options);
    const contentType = res.headers.get("content-type") || "";
    const text = await res.text();
    if (looksLikeJson(contentType, text)) {
      const parsed = parseJsonSafely(text);
      if (parsed !== null) return parsed;
    }
    if (fallbackUrl && looksLikeHtml(contentType, text)) {
      return fetchBridgeJson(fallbackUrl, options, null, timeoutMs);
    }
    return { success: false, error: `非JSON响应: ${text.slice(0, 200)}` };
  } catch (err) {
    if (controller?.signal.aborted) {
      return { success: false, error: "请求超时，请检查网络后重试" };
    }
    if (fallbackUrl) {
      return fetchBridgeJson(fallbackUrl, options, null, timeoutMs);
    }
    return { success: false, error: `请求失败: ${errorMessage(err)}` };
  } finally {
    if (timer !== null) clearTimeout(timer);
  }
};
const bridgePost = async (path, payload = {}) => {
  const url = `${BRIDGE_BASE}${path}`;
  const fallbackUrl = hasTauri ? null : `${LOCAL_BRIDGE}${path}`;
  return asRecord$1(await fetchBridgeJson(url, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(payload || {})
  }, fallbackUrl));
};
const bridgeGet = async (path) => {
  const url = `${BRIDGE_BASE}${path}`;
  const fallbackUrl = hasTauri ? null : `${LOCAL_BRIDGE}${path}`;
  return asRecord$1(await fetchBridgeJson(url, { method: "GET" }, fallbackUrl));
};
const unwrapBridge = (payload) => {
  if (payload && typeof payload === "object" && "data" in payload) {
    return payload.data;
  }
  return payload;
};
const mockResponse = (data) => ({
  data,
  status: 200,
  statusText: "OK",
  headers: {},
  config: {}
});
const mockError = (message) => ({
  response: {
    data: { error: String(message), success: false },
    status: 500,
    statusText: "Internal Server Error"
  },
  message: String(message)
});
const get = async (url, config = {}) => {
  console.log("[Axios Adapter] GET request received:", url);
  console.log("[Axios Adapter] Full URL:", url);
  try {
    if (isTestAccountSession()) {
      const testAccountResponse = resolveTestAccountHttpResponse("get", url, config);
      if (testAccountResponse) return mockResponse(testAccountResponse);
      return mockResponse({
        success: false,
        demo_disabled: true,
        error: "未知测试账号 HTTP 请求已拦截"
      });
    }
    if (url.includes("/v3/login_params")) {
      const data = asRecord$1(await invoke("get_login_page"));
      return mockResponse({
        success: true,
        lt: data.lt,
        execution: data.execution,
        salt: data.salt,
        captcha_required: data.captcha_required,
        // 必须包含 inputs 字段，即使为空对象，避免前端空判断报错
        inputs: {}
      });
    }
    if (url.includes("/v2/semesters")) {
      try {
        if (!hasTauri) {
          const res = await bridgePost("/fetch_semesters");
          return mockResponse(unwrapBridge(res));
        }
        const semesters = await invoke("fetch_semesters");
        return mockResponse(semesters);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/qxzkb/options")) {
      try {
        if (!hasTauri) {
          const payload = await bridgeGet("/qxzkb/options");
          return mockResponse(unwrapBridge(payload));
        }
        const options = await invoke("fetch_qxzkb_options");
        return mockResponse(options);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/classroom/buildings")) {
      try {
        if (!hasTauri) {
          const res = await bridgePost("/fetch_classroom_buildings");
          return mockResponse(unwrapBridge(res));
        }
        const buildings = await invoke("fetch_classroom_buildings");
        console.log("[Axios Adapter] Buildings response:", JSON.stringify(buildings));
        return mockResponse(buildings);
      } catch (err) {
        console.error("[Axios Adapter] Buildings error:", err);
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    return mockResponse({ success: false, error: "Unknown GET endpoint: " + url });
  } catch (e) {
    console.error("[Axios Adapter] GET Error:", e);
    throw mockError(errorMessage(e));
  }
};
let inFlightPromise = null;
const isLoginInFlight = () => inFlightPromise !== null;
const runExclusiveLogin = (fn) => {
  const existing = inFlightPromise;
  if (existing) return existing;
  let task;
  task = (async () => {
    try {
      return await fn();
    } finally {
      if (inFlightPromise === task) inFlightPromise = null;
    }
  })();
  inFlightPromise = task;
  return task;
};
const handleAuthPost = async (url, data) => {
  if (url.includes("/v2/start_login")) {
    const username = String(data.username ?? "");
    const password = String(data.password ?? "");
    const captcha = String(data.captcha ?? "");
    const lt = String(data.lt ?? "");
    const execution = String(data.execution ?? "");
    try {
      if (!hasTauri) {
        const response2 = await bridgePost("/login", {
          username,
          password,
          captcha,
          lt,
          execution
        });
        if (response2?.success) {
          return mockResponse({ success: true, data: response2.data });
        }
        return mockResponse({
          success: false,
          error: errorMessage(response2.error) || "登录失败"
        });
      }
      const response = await runExclusiveLogin(
        () => invoke("login", {
          username,
          password,
          captcha,
          lt,
          execution
        })
      );
      return mockResponse({ success: true, data: response });
    } catch (error) {
      return mockResponse({ success: false, error: errorMessage(error) });
    }
  }
  if (url.includes("/v3/refresh_captcha")) {
    if (!hasTauri) {
      return mockResponse({ success: false, error: "浏览器模式不支持验证码接口" });
    }
    const imageData = await invoke("get_captcha");
    const parts = imageData.split(",");
    return mockResponse({
      success: true,
      captcha_base64: parts.length > 1 ? parts[1] : parts[0],
      jsessionid: "ignored"
    });
  }
  return null;
};
const SEMESTER_RE = /^(\d{4})-(\d{4})-([12])$/;
const parseSemester = (semester) => {
  const text = String(semester || "").trim();
  const m = text.match(SEMESTER_RE);
  if (!m) return null;
  const startYear = Number(m[1]);
  const endYear = Number(m[2]);
  const term = Number(m[3]);
  if (!Number.isFinite(startYear) || !Number.isFinite(endYear) || !Number.isFinite(term)) {
    return null;
  }
  return { text, startYear, endYear, term };
};
const weight = (parsed) => parsed.startYear * 10 + parsed.term;
const compareSemesterDesc = (a, b) => {
  const pa = parseSemester(a);
  const pb = parseSemester(b);
  if (pa && pb) {
    if (pa.startYear !== pb.startYear) return pb.startYear - pa.startYear;
    if (pa.term !== pb.term) return pb.term - pa.term;
    return String(b).localeCompare(String(a));
  }
  if (pa && !pb) return -1;
  if (!pa && pb) return 1;
  return String(b).localeCompare(String(a));
};
const normalizeSemesterList = (list) => {
  const seen = /* @__PURE__ */ new Set();
  const out = [];
  for (const item of Array.isArray(list) ? list : []) {
    const sem = String(item || "").trim();
    if (!sem || seen.has(sem)) continue;
    seen.add(sem);
    out.push(sem);
  }
  out.sort(compareSemesterDesc);
  return out;
};
const readStoredScheduleSemester = () => {
  try {
    const raw = localStorage.getItem("hbu_schedule_meta");
    if (!raw) return "";
    const parsed = JSON.parse(raw);
    return String(parsed?.semester || "").trim();
  } catch {
    return "";
  }
};
const deriveSemesterByDate = (date = /* @__PURE__ */ new Date()) => {
  const today = date instanceof Date ? date : new Date(date);
  if (Number.isNaN(today.getTime())) return "";
  const year = today.getFullYear();
  const month = today.getMonth() + 1;
  const day = today.getDate();
  let academicYearStart = year - 1;
  let term = 1;
  if (month >= 9) {
    academicYearStart = year;
    term = 1;
  } else if (month >= 3 || month === 2 && day >= 15) {
    academicYearStart = year - 1;
    term = 2;
  }
  return `${academicYearStart}-${academicYearStart + 1}-${term}`;
};
const getPreferredSemesterFast = (date = /* @__PURE__ */ new Date()) => {
  const stored = readStoredScheduleSemester();
  if (parseSemester(stored)) return stored;
  return deriveSemesterByDate(date);
};
const mergeSemesterOptions = (list, selectedSemester = "") => {
  const merged = normalizeSemesterList(list);
  const selected = String(selectedSemester || "").trim();
  if (selected && !merged.includes(selected)) {
    merged.push(selected);
  }
  return normalizeSemesterList(merged);
};
const resolveCurrentSemester = (semesterList, hintedCurrent = "") => {
  const list = normalizeSemesterList(semesterList);
  if (!list.length) return "";
  const hinted = String(hintedCurrent || "").trim();
  if (hinted && list.includes(hinted)) return hinted;
  const stored = readStoredScheduleSemester();
  if (stored && list.includes(stored)) return stored;
  return list[0];
};
const semesterIsNewer = (a, b) => {
  const pa = parseSemester(a);
  const pb = parseSemester(b);
  if (!pa && !pb) return false;
  if (pa && !pb) return true;
  if (!pa && pb) return false;
  return weight(pa) > weight(pb);
};
const STORAGE_KEY$2 = "hbu_app_settings_v1";
const LOCAL_HOST_PATTERN = /^(localhost|127\.|0\.0\.0\.0|10\.|192\.168\.|172\.(1[6-9]|2\d|3[0-1])\.)/i;
const DEFAULT_CLOUD_SYNC_ENDPOINT = "https://mini-hbut-testocr1.hf.space/api/cloud-sync";
const DEFAULT_BACKEND_TARGETS = {
  portal: "https://e.hbut.edu.cn/stu/index.html#/",
  jwxt: "https://jwxt.hbut.edu.cn/admin",
  chaoxing: "https://hbut.jw.chaoxing.com/admin",
  oneCode: "https://code.hbut.edu.cn",
  library: "https://lib.hbut.edu.cn"
};
const DEFAULT_SETTINGS$1 = {
  retry: {
    electricity: 2,
    classroom: 2
  },
  retryDelayMs: 2e3,
  resourceShare: {
    previewThreadsMobile: 3,
    previewThreadsDesktop: 4,
    downloadThreadsMobile: 4,
    downloadThreadsDesktop: 6
  },
  backend: {
    useRemoteConfig: true,
    ocrEndpoint: "",
    tempUploadEndpoint: "",
    cloudSyncEndpoint: "",
    cloudSyncSecretRef: "",
    moduleTargets: { ...DEFAULT_BACKEND_TARGETS },
    moduleParams: {
      requestTimeoutMs: 15e3,
      probeTimeoutMs: 8e3,
      cloudSyncCooldownSec: 180,
      cloudSyncUploadCooldownSec: 120,
      cloudSyncDownloadCooldownSec: 10
    }
  }
};
const isRecord = (value) => !!value && typeof value === "object";
const clampNumber$1 = (value, min, max, fallback) => {
  const num = Number(value);
  if (Number.isNaN(num)) return fallback;
  return Math.min(max, Math.max(min, num));
};
const normalizeUrl = (value) => {
  const text = String(value || "").trim();
  if (!text) return "";
  if (/^https?:\/\//i.test(text)) return text.replace(/\s+/g, "");
  const prefix = LOCAL_HOST_PATTERN.test(text) ? "http://" : "https://";
  return `${prefix}${text}`.replace(/\s+/g, "");
};
const normalizeSettings$2 = (raw) => {
  const base = JSON.parse(JSON.stringify(DEFAULT_SETTINGS$1));
  if (!raw || typeof raw !== "object") return base;
  const source = raw;
  const next = { ...base, ...source };
  next.retry = { ...base.retry, ...isRecord(source.retry) ? source.retry : {} };
  next.resourceShare = { ...base.resourceShare, ...isRecord(source.resourceShare) ? source.resourceShare : {} };
  next.backend = { ...base.backend, ...isRecord(source.backend) ? source.backend : {} };
  next.backend.moduleTargets = { ...base.backend.moduleTargets };
  next.backend.moduleParams = {
    ...base.backend.moduleParams,
    ...isRecord(source.backend) && isRecord(source.backend.moduleParams) ? source.backend.moduleParams : {}
  };
  next.retry.electricity = clampNumber$1(next.retry.electricity, 0, 5, base.retry.electricity);
  next.retry.classroom = clampNumber$1(next.retry.classroom, 0, 5, base.retry.classroom);
  next.retryDelayMs = clampNumber$1(next.retryDelayMs, 500, 1e4, base.retryDelayMs);
  next.resourceShare.previewThreadsMobile = clampNumber$1(
    next.resourceShare.previewThreadsMobile,
    1,
    8,
    base.resourceShare.previewThreadsMobile
  );
  next.resourceShare.previewThreadsDesktop = clampNumber$1(
    next.resourceShare.previewThreadsDesktop,
    1,
    12,
    base.resourceShare.previewThreadsDesktop
  );
  next.resourceShare.downloadThreadsMobile = clampNumber$1(
    next.resourceShare.downloadThreadsMobile,
    1,
    8,
    base.resourceShare.downloadThreadsMobile
  );
  next.resourceShare.downloadThreadsDesktop = clampNumber$1(
    next.resourceShare.downloadThreadsDesktop,
    1,
    12,
    base.resourceShare.downloadThreadsDesktop
  );
  next.backend.useRemoteConfig = next.backend.useRemoteConfig !== false;
  next.backend.ocrEndpoint = normalizeUrl(next.backend.ocrEndpoint);
  next.backend.tempUploadEndpoint = normalizeUrl(next.backend.tempUploadEndpoint);
  next.backend.cloudSyncEndpoint = normalizeUrl(next.backend.cloudSyncEndpoint);
  next.backend.cloudSyncSecretRef = String(next.backend.cloudSyncSecretRef || "").trim();
  next.backend.moduleTargets = { ...base.backend.moduleTargets };
  next.backend.moduleParams.requestTimeoutMs = clampNumber$1(
    next.backend.moduleParams.requestTimeoutMs,
    5e3,
    6e4,
    base.backend.moduleParams.requestTimeoutMs
  );
  next.backend.moduleParams.probeTimeoutMs = clampNumber$1(
    next.backend.moduleParams.probeTimeoutMs,
    3e3,
    3e4,
    base.backend.moduleParams.probeTimeoutMs
  );
  next.backend.moduleParams.cloudSyncCooldownSec = clampNumber$1(
    next.backend.moduleParams.cloudSyncCooldownSec,
    10,
    3600,
    base.backend.moduleParams.cloudSyncCooldownSec
  );
  next.backend.moduleParams.cloudSyncUploadCooldownSec = clampNumber$1(
    next.backend.moduleParams.cloudSyncUploadCooldownSec,
    120,
    3600,
    base.backend.moduleParams.cloudSyncUploadCooldownSec
  );
  next.backend.moduleParams.cloudSyncDownloadCooldownSec = clampNumber$1(
    next.backend.moduleParams.cloudSyncDownloadCooldownSec,
    10,
    3600,
    base.backend.moduleParams.cloudSyncDownloadCooldownSec
  );
  return next;
};
const loadStoredSettings$1 = () => {
  try {
    const raw = localStorage.getItem(STORAGE_KEY$2);
    if (!raw) return null;
    return JSON.parse(raw);
  } catch {
    return null;
  }
};
const state$1 = reactive(normalizeSettings$2(loadStoredSettings$1()));
const initAppSettings = () => {
  watch(
    state$1,
    () => {
      const normalized = normalizeSettings$2(state$1);
      const normalizedText = JSON.stringify(normalized);
      const stateText = JSON.stringify(state$1);
      if (stateText !== normalizedText) {
        state$1.retry = { ...normalized.retry };
        state$1.retryDelayMs = normalized.retryDelayMs;
        state$1.resourceShare = { ...normalized.resourceShare };
        state$1.backend = {
          ...normalized.backend,
          moduleTargets: { ...normalized.backend.moduleTargets },
          moduleParams: { ...normalized.backend.moduleParams }
        };
      }
      try {
        localStorage.setItem(STORAGE_KEY$2, normalizedText);
      } catch {
      }
    },
    { deep: true, immediate: true }
  );
};
const useAppSettings = () => state$1;
const resetAppSettings = () => {
  Object.assign(state$1, normalizeSettings$2(DEFAULT_SETTINGS$1));
};
const applyAppSettingsSnapshot = (raw) => {
  const normalized = normalizeSettings$2(raw);
  state$1.retry = { ...normalized.retry };
  state$1.retryDelayMs = normalized.retryDelayMs;
  state$1.resourceShare = { ...normalized.resourceShare };
  state$1.backend = {
    ...normalized.backend,
    moduleTargets: { ...normalized.backend.moduleTargets },
    moduleParams: { ...normalized.backend.moduleParams }
  };
};
const RAW_API_BASE = "/api";
const DEFAULT_CHANNEL_ID = "hbut-default";
const DEFAULT_INTERVAL_MINUTES = 30;
const MIN_INTERVAL_MINUTES = 15;
const POWER_ALERT_THRESHOLD = 10;
const APP_BOOT_ID = `${Date.now()}-${Math.random().toString(36).slice(2, 10)}`;
const STORAGE_KEYS = {
  bg: "hbu_notify_bg",
  exam: "hbu_notify_exam",
  grade: "hbu_notify_grade",
  power: "hbu_notify_power",
  class: "hbu_notify_class",
  classLeadMinutes: "hbu_notify_class_lead_min",
  interval: "hbu_notify_interval",
  dormSelection: "last_dorm_selection",
  schoolInbox: "hbu_notify_school_inbox"
};
const CLASS_PERIOD_TIME_MAP = {
  1: { start: "08:20", end: "09:05" },
  2: { start: "09:10", end: "09:55" },
  3: { start: "10:15", end: "11:00" },
  4: { start: "11:05", end: "11:50" },
  5: { start: "14:00", end: "14:45" },
  6: { start: "14:50", end: "15:35" },
  7: { start: "15:55", end: "16:40" },
  8: { start: "16:45", end: "17:30" },
  9: { start: "18:30", end: "19:15" },
  10: { start: "19:20", end: "20:05" },
  11: { start: "20:10", end: "20:55" }
};
const readBool = (key, fallback) => {
  const raw = localStorage.getItem(key);
  if (raw === null) return fallback;
  return raw === "true";
};
const readInt = (key, fallback) => {
  const raw = Number(localStorage.getItem(key) || fallback);
  if (!Number.isFinite(raw)) return fallback;
  return raw;
};
const readJSON$1 = (key, fallback = null) => {
  try {
    const raw = localStorage.getItem(key);
    if (!raw) return fallback;
    return JSON.parse(raw);
  } catch {
    return fallback;
  }
};
const writeJSON$1 = (key, value) => {
  try {
    localStorage.setItem(key, JSON.stringify(value));
  } catch {
  }
};
const nowIso = () => (/* @__PURE__ */ new Date()).toISOString();
const toSafeText$6 = (value) => String(value ?? "").trim();
const normalizeDormPathValue = (value) => {
  if (value && typeof value === "object") {
    const item = value;
    return String(item.value ?? item.id ?? item.label ?? item.name ?? "").trim();
  }
  return String(value ?? "").trim();
};
const toSafeNumber = (value) => {
  const num = Number.parseFloat(String(value ?? "").trim());
  return Number.isFinite(num) ? num : NaN;
};
const hashText$1 = (input) => {
  const text = String(input || "");
  let hash = 0;
  for (let i = 0; i < text.length; i += 1) {
    hash = hash * 31 + text.charCodeAt(i) >>> 0;
  }
  return hash.toString(16);
};
const parseDay = (value) => {
  const text = toSafeText$6(value);
  if (!text) return null;
  const normalized = text.replace(/\./g, "-").replace(/\//g, "-");
  const dateOnly = normalized.includes("T") ? normalized.split("T")[0] : normalized;
  const date = new Date(dateOnly);
  if (Number.isNaN(date.getTime())) return null;
  date.setHours(0, 0, 0, 0);
  return date;
};
const toDayKey = (date) => {
  if (!(date instanceof Date) || Number.isNaN(date.getTime())) return "";
  const y = date.getFullYear();
  const m = String(date.getMonth() + 1).padStart(2, "0");
  const d = String(date.getDate()).padStart(2, "0");
  return `${y}-${m}-${d}`;
};
const getTomorrowKey = () => {
  const date = /* @__PURE__ */ new Date();
  date.setHours(0, 0, 0, 0);
  date.setDate(date.getDate() + 1);
  return toDayKey(date);
};
const getDormSelection = () => {
  const parsed = readJSON$1(STORAGE_KEYS.dormSelection, []);
  if (!Array.isArray(parsed) || parsed.length !== 4) return [];
  return parsed.map((item) => normalizeDormPathValue(item)).filter((item) => item !== "");
};
const getNotifySettings = () => {
  const intervalRaw = readInt(STORAGE_KEYS.interval, DEFAULT_INTERVAL_MINUTES);
  const interval = [15, 30, 60].includes(intervalRaw) ? intervalRaw : DEFAULT_INTERVAL_MINUTES;
  const classLeadRaw = readInt(STORAGE_KEYS.classLeadMinutes, 30);
  const classLeadMinutes = Math.min(120, Math.max(5, classLeadRaw));
  return {
    enableBackground: readBool(STORAGE_KEYS.bg, false),
    enableExamReminder: readBool(STORAGE_KEYS.exam, true),
    enableGradeNotice: readBool(STORAGE_KEYS.grade, true),
    enablePowerNotice: readBool(STORAGE_KEYS.power, true),
    enableClassReminder: readBool(STORAGE_KEYS.class, true),
    enableSchoolInbox: readBool(STORAGE_KEYS.schoolInbox, true),
    classLeadMinutes,
    intervalMinutes: interval
  };
};
const getApiBase = () => {
  const base = String(RAW_API_BASE).trim();
  if (/^https?:\/\//i.test(base)) return base;
  return base || "/api";
};
const toApiUrl = (path) => `${getApiBase().replace(/\/+$/, "")}${path}`;
const toMinutes = (timeText) => {
  const text = toSafeText$6(timeText);
  if (!text || !text.includes(":")) return NaN;
  const [h, m] = text.split(":").map((item) => Number(item));
  if (!Number.isFinite(h) || !Number.isFinite(m)) return NaN;
  return h * 60 + m;
};
const getCurrentMinutePrecise = () => {
  const now = /* @__PURE__ */ new Date();
  return now.getHours() * 60 + now.getMinutes() + now.getSeconds() / 60;
};
const getTodayWeekday = () => {
  const weekday = (/* @__PURE__ */ new Date()).getDay();
  return weekday === 0 ? 7 : weekday;
};
const toPositiveInt$2 = (value, fallback = 0) => {
  const num = Number(value);
  if (!Number.isFinite(num) || num <= 0) return fallback;
  return Math.floor(num);
};
const normalizeWeeks = (weeks) => {
  if (!Array.isArray(weeks)) return [];
  return weeks.map((item) => Number(item)).filter((item) => Number.isFinite(item) && item > 0);
};
const buildGradesSignature = (grades) => {
  const list = Array.isArray(grades) ? grades : [];
  const rows = list.map((item) => {
    const grade = item && typeof item === "object" ? item : {};
    const term = toSafeText$6(grade.term);
    const name = toSafeText$6(grade.course_name);
    const score = toSafeText$6(grade.final_score);
    const credit = toSafeText$6(grade.course_credit);
    const teacher = toSafeText$6(grade.teacher);
    return `${term}|${name}|${score}|${credit}|${teacher}`;
  }).sort();
  return `${rows.length}:${hashText$1(rows.join("||"))}`;
};
const pickGradePreview = (grades, limit = 6) => {
  const list = Array.isArray(grades) ? [...grades] : [];
  list.sort((a, b) => compareSemesterDesc(toSafeText$6(a?.term), toSafeText$6(b?.term)));
  return list.slice(0, limit).map((item) => ({
    term: toSafeText$6(item?.term),
    course_name: toSafeText$6(item?.course_name),
    final_score: toSafeText$6(item?.final_score),
    teacher: toSafeText$6(item?.teacher)
  }));
};
const normalizeExam = (item) => {
  const raw = item && typeof item === "object" ? item : {};
  const dateText = toSafeText$6(raw.exam_date || raw.date);
  const timeText = toSafeText$6(raw.exam_time || raw.start_time);
  return {
    course_name: toSafeText$6(raw.course_name),
    exam_type: toSafeText$6(raw.exam_type),
    exam_date: dateText,
    exam_time: timeText,
    location: toSafeText$6(raw.location),
    seat_no: toSafeText$6(raw.seat_no || raw.seat_number)
  };
};
const pickUpcomingExams = (exams, limit = 8) => {
  const today = /* @__PURE__ */ new Date();
  today.setHours(0, 0, 0, 0);
  const normalized = (Array.isArray(exams) ? exams : []).map(normalizeExam);
  const upcoming = normalized.filter((item) => {
    const day = parseDay(item.exam_date);
    if (!day) return true;
    return day >= today;
  }).sort((a, b) => {
    const da = parseDay(a.exam_date);
    const db = parseDay(b.exam_date);
    const ta = da?.getTime() || Number.MAX_SAFE_INTEGER;
    const tb = db?.getTime() || Number.MAX_SAFE_INTEGER;
    return ta - tb;
  });
  const tomorrowKey = getTomorrowKey();
  return upcoming.slice(0, limit).map((item) => ({
    ...item,
    is_tomorrow: toDayKey(parseDay(item.exam_date)) === tomorrowKey
  }));
};
const buildTomorrowExamSignature = (exams) => {
  const rows = (Array.isArray(exams) ? exams : []).map((item) => {
    const raw = item && typeof item === "object" ? item : {};
    const course = toSafeText$6(raw.course_name);
    const date = toSafeText$6(raw.exam_date);
    const time = toSafeText$6(raw.exam_time);
    const location2 = toSafeText$6(raw.location);
    return `${course}|${date}|${time}|${location2}`;
  }).sort();
  return `${rows.length}:${hashText$1(rows.join("||"))}`;
};
const snapshotKeyFor = (studentId) => `hbu_notify_snapshot:${studentId}`;
const gradeSigKeyFor = (studentId) => `hbu_notify_grade_signature:${studentId}`;
const examSigKeyFor = (studentId) => `hbu_notify_exam_tomorrow:${studentId}`;
const powerStateKeyFor = (studentId, roomKey) => `hbu_notify_power_state:${studentId}:${roomKey}`;
const classReminderStateKeyFor = (studentId) => `hbu_notify_class_state:${studentId}`;
const schoolInboxStateKeyFor = (studentId) => `hbu_notify_school_inbox_state:${studentId}`;
const resolveLoginMode = () => toSafeText$6(localStorage.getItem("hbu_login_method"));
const getRequestTimeoutMs = () => {
  try {
    const settings = useAppSettings();
    const value = Number(settings?.backend?.moduleParams?.requestTimeoutMs || 15e3);
    if (!Number.isFinite(value)) return 15e3;
    return Math.min(6e4, Math.max(5e3, value));
  } catch {
    return 15e3;
  }
};
const getCoursePeriodRange = (course) => {
  const startPeriod = toPositiveInt$2(course?.period ?? course?.start_period, 0);
  if (startPeriod < 1 || startPeriod > 11) return null;
  const endByField = toPositiveInt$2(course?.end_period, 0);
  const span = Math.max(1, toPositiveInt$2(course?.djs ?? course?.duration, 1));
  const computedEnd = endByField > 0 ? endByField : startPeriod + span - 1;
  const endPeriod = Math.min(11, Math.max(startPeriod, computedEnd));
  return { startPeriod, endPeriod };
};
const getCourseMergeSignature = (course) => {
  const id = toSafeText$6(course?.id || "");
  const name = toSafeText$6(course?.name || "");
  const teacher = toSafeText$6(course?.teacher || "");
  const room = toSafeText$6(course?.room_code || course?.room || "");
  const className = toSafeText$6(course?.class_name || "");
  const building = toSafeText$6(course?.building || "");
  const custom = course?.is_custom ? "1" : "0";
  return `${id}|${name}|${teacher}|${room}|${className}|${building}|${custom}`;
};
const getMergedTodayClasses = (courses, currentWeek, weekday) => {
  const normalized = (Array.isArray(courses) ? courses : []).filter((course) => {
    const raw = course && typeof course === "object" ? course : {};
    return toPositiveInt$2(raw.weekday, 0) === weekday;
  }).filter((course) => {
    const raw = course && typeof course === "object" ? course : {};
    const weeks = normalizeWeeks(raw.weeks);
    return weeks.length === 0 || weeks.includes(currentWeek);
  }).map((course) => {
    const raw = course && typeof course === "object" ? course : {};
    const range = getCoursePeriodRange(raw);
    if (!range) return null;
    return {
      ...raw,
      startPeriod: range.startPeriod,
      endPeriod: range.endPeriod,
      signature: getCourseMergeSignature(raw),
      room: toSafeText$6(raw?.room_code || raw?.room || "待定教室"),
      teacher: toSafeText$6(raw?.teacher || ""),
      name: toSafeText$6(raw?.name || "未命名课程")
    };
  }).filter((course) => course !== null);
  const signatureCount = /* @__PURE__ */ new Map();
  normalized.forEach((course) => {
    signatureCount.set(course.signature, (signatureCount.get(course.signature) || 0) + 1);
  });
  const sorted = normalized.map((course) => {
    const rawSpan = Math.max(1, course.endPeriod - course.startPeriod + 1);
    const duplicateCount = Number(signatureCount.get(course.signature) || 0);
    const unitSpan = course.is_custom ? rawSpan : duplicateCount > 1 ? 1 : rawSpan;
    const endPeriod = Math.min(11, course.startPeriod + unitSpan - 1);
    return {
      ...course,
      rawSpan,
      unitSpan,
      endPeriod
    };
  }).sort((a, b) => a.startPeriod - b.startPeriod || a.endPeriod - b.endPeriod);
  const merged = [];
  let i = 0;
  while (i < sorted.length) {
    const current = sorted[i];
    const mergedItem = { ...current };
    let j = i + 1;
    while (j < sorted.length) {
      const next = sorted[j];
      if (mergedItem.unitSpan === 1 && next.unitSpan === 1 && next.signature === mergedItem.signature && next.startPeriod === mergedItem.endPeriod + 1) {
        mergedItem.endPeriod = next.endPeriod;
        j += 1;
      } else {
        break;
      }
    }
    const slot = CLASS_PERIOD_TIME_MAP[mergedItem.startPeriod];
    const startClock = slot?.start || "--:--";
    mergedItem.startClock = startClock;
    mergedItem.startMinutes = toMinutes(startClock);
    merged.push(mergedItem);
    i = j;
  }
  return merged;
};
const toCourseReminderItems = (mergedClasses, leadMinutes) => {
  const nowMinute = getCurrentMinutePrecise();
  return (Array.isArray(mergedClasses) ? mergedClasses : []).map((course) => {
    const raw = course && typeof course === "object" ? course : {};
    const startMinutes = Number(raw?.startMinutes);
    if (!Number.isFinite(startMinutes)) return null;
    const minsUntilStart = startMinutes - nowMinute;
    if (minsUntilStart < 0 || minsUntilStart > leadMinutes) return null;
    return {
      id: `${toSafeText$6(raw?.signature || "")}|${raw.startPeriod}|${raw.endPeriod}`,
      name: toSafeText$6(raw?.name || "未命名课程"),
      teacher: toSafeText$6(raw?.teacher || ""),
      room: toSafeText$6(raw?.room_code || raw?.room || "待定教室"),
      weekday: toPositiveInt$2(raw?.weekday, 0),
      startPeriod: toPositiveInt$2(raw?.startPeriod, 0),
      endPeriod: toPositiveInt$2(raw?.endPeriod, 0),
      startClock: toSafeText$6(raw?.startClock || "--:--"),
      minsUntilStart: Math.max(0, Math.floor(minsUntilStart))
    };
  }).filter((item) => item !== null).sort((a, b) => a.startPeriod - b.startPeriod);
};
const getSchedulePayloadForReminder = (studentId, semesterHint = "") => {
  const sid = toSafeText$6(studentId);
  if (!sid) return null;
  const sem = toSafeText$6(semesterHint);
  if (sem) {
    const scoped = getCachedData(`schedule:${sid}:${sem}`, LONG_TTL);
    if (scoped?.data) return scoped.data;
  }
  const global = getCachedData(`schedule:${sid}`, LONG_TTL);
  if (global?.data) return global.data;
  return null;
};
const REMINDER_WINDOW_DAYS = 14;
const MIN_REMINDER_WINDOW_DAYS = 7;
const MAX_REMINDER_WINDOW_DAYS = 14;
const EXAM_REMINDER_LEAD_DAYS = 1;
const MAX_PENDING_REMINDERS = 50;
const stableHash31 = (text) => {
  let hash = 0;
  for (let i = 0; i < text.length; i += 1) {
    hash = hash * 31 + text.charCodeAt(i) >>> 0;
  }
  return hash & 2147483647 || 1;
};
const buildReminderKey = (key) => {
  return [
    "mini-hbut",
    "r1",
    toSafeText$6(key.studentId),
    key.type,
    toSafeText$6(key.semester),
    toSafeText$6(key.courseIdentity),
    toSafeText$6(key.occurrence),
    String(Math.max(0, Math.floor(Number(key.leadMinutes) || 0)))
  ].join("|");
};
const deriveReminderId = (key) => {
  return stableHash31(buildReminderKey(key));
};
const clampWindowDays = (days) => {
  if (!Number.isFinite(days)) return REMINDER_WINDOW_DAYS;
  return Math.min(MAX_REMINDER_WINDOW_DAYS, Math.max(MIN_REMINDER_WINDOW_DAYS, Math.floor(days)));
};
const computeReminderWindow = (now, windowDays = REMINDER_WINDOW_DAYS) => {
  const days = clampWindowDays(windowDays);
  const start = now instanceof Date && !Number.isNaN(now.getTime()) ? now : /* @__PURE__ */ new Date();
  const end = new Date(start.getTime() + days * 24 * 60 * 60 * 1e3);
  return {
    startEpochMs: start.getTime(),
    endEpochMs: end.getTime(),
    startKey: toDayKey(start),
    endKey: toDayKey(end)
  };
};
const computeFingerprint = (spec) => `${spec.title}|${spec.body}|${spec.atEpochMs}|${spec.targetView}`;
const mergeCoursesForDay = (courses, week, weekday) => {
  const normalized = (Array.isArray(courses) ? courses : []).filter((course) => {
    const raw = course && typeof course === "object" ? course : {};
    return toPositiveInt$2(raw.weekday, 0) === weekday;
  }).filter((course) => {
    const raw = course && typeof course === "object" ? course : {};
    const weeks = normalizeWeeks(raw.weeks);
    return weeks.length === 0 || weeks.includes(week);
  }).map((course) => {
    const raw = course && typeof course === "object" ? course : {};
    const range = getCoursePeriodRange(raw);
    if (!range) return null;
    return {
      ...raw,
      startPeriod: range.startPeriod,
      endPeriod: range.endPeriod,
      signature: getCourseMergeSignature(raw),
      room: toSafeText$6(raw?.room_code || raw?.room || "待定教室"),
      teacher: toSafeText$6(raw?.teacher || ""),
      name: toSafeText$6(raw?.name || "未命名课程")
    };
  }).filter((course) => course !== null);
  const signatureCount = /* @__PURE__ */ new Map();
  normalized.forEach((course) => {
    signatureCount.set(course.signature, (signatureCount.get(course.signature) || 0) + 1);
  });
  const sorted = normalized.map((course) => {
    const rawSpan = Math.max(1, course.endPeriod - course.startPeriod + 1);
    const duplicateCount = Number(signatureCount.get(course.signature) || 0);
    const unitSpan = course.is_custom ? rawSpan : duplicateCount > 1 ? 1 : rawSpan;
    const endPeriod = Math.min(11, course.startPeriod + unitSpan - 1);
    return { ...course, rawSpan, unitSpan, endPeriod };
  }).sort((a, b) => a.startPeriod - b.startPeriod || a.endPeriod - b.endPeriod);
  const merged = [];
  let i = 0;
  while (i < sorted.length) {
    const current = sorted[i];
    const mergedItem = { ...current };
    let j = i + 1;
    while (j < sorted.length) {
      const next = sorted[j];
      if (mergedItem.unitSpan === 1 && next.unitSpan === 1 && next.signature === mergedItem.signature && next.startPeriod === mergedItem.endPeriod + 1) {
        mergedItem.endPeriod = next.endPeriod;
        j += 1;
      } else {
        break;
      }
    }
    const slot = CLASS_PERIOD_TIME_MAP[mergedItem.startPeriod];
    const startClock = slot?.start || "--:--";
    mergedItem.startClock = startClock;
    mergedItem.startMinutes = Number.isFinite(toMinutes(startClock)) ? toMinutes(startClock) : 0;
    merged.push(mergedItem);
    i = j;
  }
  return merged;
};
const buildClassReminderPlan = (input) => {
  const now = input.now instanceof Date && !Number.isNaN(input.now.getTime()) ? input.now : /* @__PURE__ */ new Date();
  const window2 = input.window || computeReminderWindow(now);
  const leadMinutes = Math.min(120, Math.max(5, Number(input.leadMinutes ?? 30) || 30));
  const courses = Array.isArray(input.courses) ? input.courses : [];
  if (courses.length === 0) return [];
  const currentWeek = Math.max(1, toPositiveInt$2(input.currentWeek, 0) || 1);
  const specs = [];
  const dayCount = Math.ceil((window2.endEpochMs - window2.startEpochMs) / 864e5);
  const todayWeekday = now.getDay() === 0 ? 7 : now.getDay();
  for (let dayIndex = 0; dayIndex < dayCount; dayIndex += 1) {
    const dayStart = new Date(window2.startEpochMs + dayIndex * 864e5);
    dayStart.setHours(0, 0, 0, 0);
    let week = currentWeek;
    if (input.startDate) {
      const base = parseDay(input.startDate);
      if (base) {
        const diffDays = Math.floor((dayStart.getTime() - base.getTime()) / 864e5);
        week = Math.max(1, Math.floor(diffDays / 7) + 1);
      }
    } else {
      const anchor = new Date(now.getTime() - (todayWeekday - 1) * 864e5);
      anchor.setHours(0, 0, 0, 0);
      week = Math.max(1, currentWeek + Math.floor((dayStart.getTime() - anchor.getTime()) / (7 * 864e5)));
    }
    const weekday = dayStart.getDay() === 0 ? 7 : dayStart.getDay();
    const merged = mergeCoursesForDay(courses, week, weekday);
    for (const course of merged) {
      const dateKey = toDayKey(dayStart);
      const at = new Date(dayStart.getTime() + course.startMinutes * 6e4 - leadMinutes * 6e4);
      if (at.getTime() <= now.getTime() || at.getTime() > window2.endEpochMs) continue;
      const identity = `${course.signature}|${course.startPeriod}|${course.endPeriod}`;
      const id = deriveReminderId({
        studentId: input.studentId,
        type: "class",
        semester: input.semester,
        courseIdentity: identity,
        occurrence: dateKey,
        leadMinutes
      });
      const teacherText = course.teacher ? `，授课教师 ${course.teacher}` : "";
      const title = "上课提醒";
      const body = `将于 ${course.startClock} 开始：${course.name}（${course.room}${teacherText}）`;
      specs.push({
        id,
        type: "class",
        studentId: input.studentId,
        semester: input.semester,
        title,
        body,
        atEpochMs: at.getTime(),
        atEpochSecs: Math.floor(at.getTime() / 1e3),
        targetView: "schedule",
        fingerprint: computeFingerprint({ title, body, atEpochMs: at.getTime(), targetView: "schedule" })
      });
    }
  }
  return specs.sort((a, b) => a.atEpochMs - b.atEpochMs);
};
const buildExamReminderPlan = (input) => {
  const now = input.now instanceof Date && !Number.isNaN(input.now.getTime()) ? input.now : /* @__PURE__ */ new Date();
  const window2 = input.window || computeReminderWindow(now);
  const leadDays = Math.max(0, Math.floor(Number(input.leadDays ?? EXAM_REMINDER_LEAD_DAYS) || EXAM_REMINDER_LEAD_DAYS));
  const exams = Array.isArray(input.exams) ? input.exams : [];
  if (exams.length === 0) return [];
  const specs = [];
  for (const raw of exams) {
    const exam = raw && typeof raw === "object" ? raw : {};
    const date = parseDay(exam.exam_date || exam.date);
    if (!date) continue;
    const courseName = toSafeText$6(exam.course_name) || "未命名考试";
    const timeText = toSafeText$6(exam.exam_time || exam.start_time);
    const clock = timeText.split("-")[0].trim() || "09:00";
    const [h, m] = clock.split(":").map((item) => Number(item));
    const startAt = new Date(date.getTime());
    startAt.setHours(Number.isFinite(h) ? h : 9, Number.isFinite(m) ? m : 0, 0, 0);
    const at = new Date(startAt.getTime() - leadDays * 864e5);
    if (at.getTime() <= now.getTime() || at.getTime() > window2.endEpochMs) continue;
    const dateKey = toDayKey(date);
    const identity = `${courseName}|${dateKey}|${timeText}`;
    const id = deriveReminderId({
      studentId: input.studentId,
      type: "exam",
      semester: input.semester,
      courseIdentity: identity,
      occurrence: dateKey,
      leadMinutes: leadDays * 24 * 60
    });
    const title = "考试提醒";
    const body = leadDays > 0 ? `${courseName} 将于 ${leadDays} 天后（${dateKey}${timeText ? ` ${timeText}` : ""}）进行，请提前做好准备。` : `${courseName} 将于 ${dateKey}${timeText ? ` ${timeText}` : ""} 进行，请提前做好准备。`;
    specs.push({
      id,
      type: "exam",
      studentId: input.studentId,
      semester: input.semester,
      title,
      body,
      atEpochMs: at.getTime(),
      atEpochSecs: Math.floor(at.getTime() / 1e3),
      targetView: "exams",
      fingerprint: computeFingerprint({ title, body, atEpochMs: at.getTime(), targetView: "exams" })
    });
  }
  return specs.sort((a, b) => a.atEpochMs - b.atEpochMs);
};
const ledgerKeyFor = (studentId) => `hbu_local_reminder_ledger:${toSafeText$6(studentId)}`;
const ACTIVE_SCOPE_KEY = "hbu_local_reminder_active_scope";
const readLedger = (studentId) => {
  const sid = toSafeText$6(studentId);
  const state2 = readJSON$1(ledgerKeyFor(sid), null);
  return {
    scope: state2?.scope || sid,
    updatedAt: state2?.updatedAt || "",
    entries: Array.isArray(state2?.entries) ? state2.entries : []
  };
};
const writeLedger = (studentId, state2) => {
  writeJSON$1(ledgerKeyFor(studentId), state2);
};
const clearLedger = (studentId) => {
  try {
    localStorage.removeItem(ledgerKeyFor(toSafeText$6(studentId)));
  } catch {
  }
};
const readActiveScope = () => {
  try {
    return toSafeText$6(localStorage.getItem(ACTIVE_SCOPE_KEY));
  } catch {
    return "";
  }
};
const writeActiveScope = (studentId) => {
  try {
    localStorage.setItem(ACTIVE_SCOPE_KEY, toSafeText$6(studentId));
  } catch {
  }
};
const clearActiveScope = () => {
  try {
    localStorage.removeItem(ACTIVE_SCOPE_KEY);
  } catch {
  }
};
const diffReminders = (expected, ledger) => {
  const ledgerById = /* @__PURE__ */ new Map();
  ledger.entries.forEach((entry) => ledgerById.set(entry.id, entry));
  const expectedIds = /* @__PURE__ */ new Set();
  const toSchedule = [];
  const toKeep = [];
  for (const spec of expected) {
    expectedIds.add(spec.id);
    const old = ledgerById.get(spec.id);
    if (!old || old.fingerprint !== spec.fingerprint) {
      toSchedule.push(spec);
    } else {
      toKeep.push(spec.id);
    }
  }
  const toCancel = ledger.entries.filter((entry) => !expectedIds.has(entry.id)).map((entry) => entry.id);
  return { toSchedule, toCancel, toKeep };
};
const applyReminderCap = (specs, cap = MAX_PENDING_REMINDERS) => {
  if (specs.length <= cap) return specs.slice();
  return specs.slice().sort((a, b) => a.atEpochMs - b.atEpochMs).slice(0, cap);
};
let injectedPlatform$1 = null;
const setLocalReminderPlatform = (platform) => {
  injectedPlatform$1 = platform;
};
const getPlatform = () => {
  if (injectedPlatform$1) return injectedPlatform$1;
  return {
    async schedule(input) {
      const mod = await __vitePreload(() => import("./runtime-bridge-BU9jeOXP.js").then((n) => n.a3), true ? __vite__mapDeps([1,0]) : void 0, import.meta.url);
      return mod.scheduleLocalNotification(input);
    },
    async pending() {
      const mod = await __vitePreload(() => import("./runtime-bridge-BU9jeOXP.js").then((n) => n.a3), true ? __vite__mapDeps([1,0]) : void 0, import.meta.url);
      return mod.getPendingLocalNotifications();
    },
    async cancel(ids) {
      const mod = await __vitePreload(() => import("./runtime-bridge-BU9jeOXP.js").then((n) => n.a3), true ? __vite__mapDeps([1,0]) : void 0, import.meta.url);
      return mod.cancelLocalNotifications(ids);
    },
    async permission() {
      const mod = await __vitePreload(() => import("./runtime-bridge-BU9jeOXP.js").then((n) => n.a4), true ? __vite__mapDeps([1,0]) : void 0, import.meta.url);
      return mod.platformBridge.getNotificationPermission();
    }
  };
};
const isSupportedRuntime = async () => {
  try {
    const native = await __vitePreload(() => import("./runtime-bridge-BU9jeOXP.js").then((n) => n.a6), true ? __vite__mapDeps([1,0]) : void 0, import.meta.url);
    return native.isTauriRuntime();
  } catch {
    return false;
  }
};
const inflight$1 = /* @__PURE__ */ new Map();
const reconcileLocalReminders = async (input) => {
  const sid = toSafeText$6(input.studentId);
  if (!sid) {
    return { success: false, reason: input.reason, skipped: "missing-student-id", expected: 0, scheduled: 0, canceled: 0, kept: 0, failed: 0, errors: [], windowDays: REMINDER_WINDOW_DAYS, ledgerCount: 0, pendingQueried: false };
  }
  const running = inflight$1.get(sid);
  if (running) return running;
  const task = (async () => {
    const errors = [];
    const fail = (skipped) => ({
      success: false,
      reason: input.reason,
      skipped,
      expected: 0,
      scheduled: 0,
      canceled: 0,
      kept: 0,
      failed: 0,
      errors,
      windowDays: REMINDER_WINDOW_DAYS,
      ledgerCount: 0,
      pendingQueried: false
    });
    if (!await isSupportedRuntime()) {
      return fail("unsupported-runtime");
    }
    const platform = input.platform || getPlatform();
    if (!input.skipPermissionCheck) {
      let permission = "denied";
      try {
        permission = await platform.permission();
      } catch {
        permission = "denied";
      }
      if (permission !== "granted") return fail("permission-denied");
    }
    const activeScope = readActiveScope();
    if (activeScope && activeScope !== sid) {
      const cancelResult = await cancelLocalRemindersForScope(activeScope, platform);
      if (!cancelResult.success) {
        errors.push(`取消旧账号(${activeScope})提醒失败`);
      }
    }
    writeActiveScope(sid);
    const now = input.now instanceof Date && !Number.isNaN(input.now.getTime()) ? input.now : /* @__PURE__ */ new Date();
    const window2 = computeReminderWindow(now, input.windowDays);
    const settings = input.settings || getNotifySettings();
    let courses = [];
    let semester = toSafeText$6(input.semesterHint);
    let startDate = "";
    let currentWeek = 1;
    if (Array.isArray(input.courses)) {
      courses = input.courses;
      semester = toSafeText$6(input.scheduleMeta?.semester || input.semesterHint || semester);
      startDate = toSafeText$6(input.scheduleMeta?.start_date || input.scheduleMeta?.startDate);
      currentWeek = toPositiveInt$2(input.scheduleMeta?.current_week, 0) || 1;
    } else {
      const payload = getSchedulePayloadForReminder(sid, input.semesterHint);
      if (payload?.data && Array.isArray(payload.data)) {
        courses = payload.data;
      }
      const meta = payload?.meta || input.scheduleMeta;
      semester = toSafeText$6(meta?.semester || input.semesterHint || semester);
      startDate = toSafeText$6(meta?.start_date || meta?.startDate);
      currentWeek = toPositiveInt$2(meta?.current_week, 0) || 1;
    }
    try {
      const native = await __vitePreload(() => import("./runtime-bridge-BU9jeOXP.js").then((n) => n.a6), true ? __vite__mapDeps([1,0]) : void 0, import.meta.url);
      if (native.isTauriRuntime()) {
        const customRes = await native.invokeNative("list_custom_schedule_courses", {
          studentId: sid,
          semester
        });
        const customData = customRes && typeof customRes === "object" ? customRes : {};
        if (customData.success !== false && Array.isArray(customData.data)) {
          courses = [
            ...courses,
            ...customData.data.map((item) => ({
              ...item,
              is_custom: true
            }))
          ];
        }
      }
    } catch {
    }
    let exams = [];
    if (Array.isArray(input.exams)) {
      exams = input.exams;
    } else {
      try {
        const cachedRaw = await __vitePreload(() => Promise.resolve().then(() => api), true ? void 0 : void 0, import.meta.url).then(
          (api2) => api2.getCachedData(`exams:${sid}:current`, 3 * 24 * 60 * 60 * 1e3)
        );
        const cached = cachedRaw;
        if (cached?.data && Array.isArray(cached.data.data)) {
          exams = cached.data.data;
        }
      } catch {
      }
    }
    let expected = [];
    if (settings.enableClassReminder) {
      expected = expected.concat(
        buildClassReminderPlan({
          studentId: sid,
          semester,
          courses,
          startDate,
          currentWeek,
          leadMinutes: settings.classLeadMinutes,
          now,
          window: window2
        })
      );
    }
    if (settings.enableExamReminder) {
      expected = expected.concat(
        buildExamReminderPlan({
          studentId: sid,
          semester,
          exams,
          now,
          window: window2
        })
      );
    }
    expected = applyReminderCap(expected);
    const ledger = readLedger(sid);
    const diff = diffReminders(expected, ledger);
    const scheduledIds = [];
    const failedIds = [];
    for (const spec of diff.toSchedule) {
      try {
        const ok = await platform.schedule({
          id: spec.id,
          title: spec.title,
          body: spec.body,
          atEpochSecs: spec.atEpochSecs,
          channelId: "hbut-default",
          targetView: spec.targetView
        });
        if (ok) {
          scheduledIds.push(spec.id);
        } else {
          failedIds.push(spec.id);
          errors.push(`登记提醒失败 id=${spec.id}`);
        }
      } catch {
        failedIds.push(spec.id);
        errors.push(`登记提醒异常 id=${spec.id}`);
      }
    }
    let canceledIds = [];
    if (diff.toCancel.length > 0) {
      try {
        const ok = await platform.cancel(diff.toCancel);
        if (ok) canceledIds = diff.toCancel;
        else errors.push(`取消提醒失败 ids=${diff.toCancel.join(",")}`);
      } catch {
        errors.push(`取消提醒异常 ids=${diff.toCancel.join(",")}`);
      }
    }
    const expectedById = /* @__PURE__ */ new Map();
    expected.forEach((spec) => expectedById.set(spec.id, spec));
    const cancelSet = new Set(canceledIds);
    const keptSet = new Set(diff.toKeep);
    const failedSet = new Set(failedIds);
    const nextEntries = [];
    for (const entry of ledger.entries) {
      if (cancelSet.has(entry.id)) continue;
      if (keptSet.has(entry.id) || scheduledIds.includes(entry.id) || failedSet.has(entry.id)) {
        const spec = expectedById.get(entry.id);
        nextEntries.push(
          spec ? { id: spec.id, fingerprint: spec.fingerprint, type: spec.type, atEpochMs: spec.atEpochMs } : entry
        );
      }
    }
    for (const spec of expected) {
      if (scheduledIds.includes(spec.id) && !nextEntries.some((entry) => entry.id === spec.id)) {
        nextEntries.push({ id: spec.id, fingerprint: spec.fingerprint, type: spec.type, atEpochMs: spec.atEpochMs });
      }
    }
    writeLedger(sid, { scope: sid, updatedAt: (/* @__PURE__ */ new Date()).toISOString(), entries: nextEntries });
    let pendingQueried = false;
    try {
      await platform.pending();
      pendingQueried = true;
    } catch {
    }
    return {
      success: errors.length === 0,
      reason: input.reason,
      expected: expected.length,
      scheduled: scheduledIds.length,
      canceled: canceledIds.length,
      kept: diff.toKeep.length,
      failed: failedIds.length,
      errors,
      windowDays: Math.round((window2.endEpochMs - window2.startEpochMs) / 864e5),
      windowStart: new Date(window2.startEpochMs).toISOString(),
      windowEnd: new Date(window2.endEpochMs).toISOString(),
      ledgerCount: nextEntries.length,
      pendingQueried
    };
  })();
  inflight$1.set(sid, task);
  try {
    return await task;
  } finally {
    inflight$1.delete(sid);
  }
};
const queryPendingLocalReminders = async (platform) => {
  try {
    const active = platform || getPlatform();
    const items = await active.pending();
    return Array.isArray(items) ? items : [];
  } catch {
    return [];
  }
};
const cancelLocalRemindersForScope = async (studentId, platform) => {
  const sid = toSafeText$6(studentId);
  if (!sid) return { success: true, canceled: 0, studentId: "" };
  const ledger = readLedger(sid);
  const ids = ledger.entries.map((entry) => entry.id);
  if (ids.length === 0) {
    clearLedger(sid);
    return { success: true, canceled: 0, studentId: sid };
  }
  try {
    const active = platform || getPlatform();
    const ok = await active.cancel(ids);
    if (ok) {
      clearLedger(sid);
      return { success: true, canceled: ids.length, studentId: sid };
    }
    return { success: false, canceled: 0, studentId: sid };
  } catch {
    return { success: false, canceled: 0, studentId: sid };
  }
};
const clearRemindersForLogout = async (studentId) => {
  const result = await cancelLocalRemindersForScope(studentId);
  const sid = toSafeText$6(studentId);
  if (readActiveScope() === sid) clearActiveScope();
  return result;
};
const local_reminder_scheduler = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  EXAM_REMINDER_LEAD_DAYS,
  MAX_PENDING_REMINDERS,
  MAX_REMINDER_WINDOW_DAYS,
  MIN_REMINDER_WINDOW_DAYS,
  REMINDER_WINDOW_DAYS,
  applyReminderCap,
  buildClassReminderPlan,
  buildExamReminderPlan,
  buildReminderKey,
  cancelLocalRemindersForScope,
  clearActiveScope,
  clearLedger,
  clearRemindersForLogout,
  computeFingerprint,
  computeReminderWindow,
  deriveReminderId,
  diffReminders,
  ledgerKeyFor,
  mergeCoursesForDay,
  queryPendingLocalReminders,
  readActiveScope,
  readLedger,
  reconcileLocalReminders,
  setLocalReminderPlatform,
  writeActiveScope,
  writeLedger
}, Symbol.toStringTag, { value: "Module" }));
const post = async (url, data = {}, _config = {}) => {
  console.log("[Axios Adapter] POST request received:", url);
  console.log("[Axios Adapter] POST data:", JSON.stringify(data));
  const fireCustomCourseReconcile = () => {
    const sid = String(data?.student_id || data?.studentId || "").trim();
    if (!sid) return;
    void reconcileLocalReminders({ studentId: sid, reason: "custom-course-crud" }).catch(() => {
    });
  };
  try {
    const testAccountResponse = resolveTestAccountHttpResponse("post", url, data);
    if (testAccountResponse && (isTestAccountSession() || url.includes("/v2/start_login"))) {
      return mockResponse(testAccountResponse);
    }
    if (isTestAccountSession()) {
      return mockResponse({
        success: false,
        demo_disabled: true,
        error: "未知测试账号 HTTP 请求已拦截"
      });
    }
    const authResponse = await handleAuthPost(url, data);
    if (authResponse) return authResponse;
    if (url.includes("/v2/quick_fetch")) {
      if (!hasTauri) {
        const res = await bridgePost("/sync_grades", {
          current_only: !!data?.teacher_current_only
        });
        if (res?.success) {
          return mockResponse(unwrapBridge(res));
        }
        return mockResponse({ success: false, error: errorMessage(res.error) || "获取成绩失败" });
      }
      const grades = await invoke("sync_grades", {
        currentOnly: !!data?.teacher_current_only
      });
      return mockResponse(grades);
    }
    if (url.includes("/v2/grade_teacher_cache")) {
      try {
        if (hasTauri) {
          const payload = await invoke("get_grade_teacher_cache", {
            studentId: data?.student_id || data?.studentId || null
          });
          return mockResponse(payload);
        }
        return mockResponse({ success: true, by_kcbh: {}, semesters: {} });
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/grade_teachers/current")) {
      try {
        if (hasTauri) {
          const payload = await invoke("sync_grade_teachers_current_semester");
          return mockResponse(payload);
        }
        return mockResponse({ success: false, error: "浏览器模式不支持任课教师补齐" });
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/schedule/query")) {
      let schedule = {};
      if (!hasTauri) {
        const res = await bridgePost("/sync_schedule", data || {});
        if (res?.success && res?.data) {
          schedule = { success: true, ...asRecord$1(res.data) };
        } else {
          return mockResponse({ success: false, error: errorMessage(res.error) || "获取课表失败" });
        }
      } else {
        const raw = await invoke("sync_schedule", { semester: data?.semester || null });
        schedule = asRecord$1(raw);
      }
      const schedulePayload = asRecord$1(schedule);
      const scheduleSid = String(data?.student_id || "");
      if (scheduleSid) {
        void reconcileLocalReminders({
          studentId: scheduleSid,
          courses: Array.isArray(schedulePayload.data) ? schedulePayload.data : null,
          scheduleMeta: schedulePayload.meta || null,
          reason: "schedule-sync"
        }).catch(() => {
        });
      }
      return mockResponse({ success: true, ...schedulePayload });
    }
    if (url.includes("/v2/schedule/custom/list_all")) {
      try {
        if (hasTauri) {
          const payload = await invoke("list_all_custom_schedule_courses", {
            studentId: data?.student_id || ""
          });
          return mockResponse(payload);
        }
        const res = await bridgePost("/schedule/custom/list_all", data || {});
        if (res?.success && res?.data) {
          return mockResponse({ success: true, ...res.data });
        }
        return mockResponse({ success: false, error: errorMessage(res.error) || "获取全部自定义课程失败" });
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/schedule/custom/list")) {
      try {
        if (hasTauri) {
          const payload = await invoke("list_custom_schedule_courses", {
            studentId: data?.student_id || "",
            semester: data?.semester || ""
          });
          return mockResponse(payload);
        }
        const res = await bridgePost("/schedule/custom/list", data || {});
        if (res?.success && res?.data) {
          return mockResponse({ success: true, ...res.data });
        }
        return mockResponse({ success: false, error: errorMessage(res.error) || "获取自定义课程失败" });
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/schedule/custom/add")) {
      try {
        if (hasTauri) {
          const payload = await invoke("add_custom_schedule_course", { req: data || {} });
          fireCustomCourseReconcile();
          return mockResponse(payload);
        }
        const res = await bridgePost("/schedule/custom/add", data || {});
        if (res?.success && res?.data) {
          fireCustomCourseReconcile();
          return mockResponse({ success: true, ...res.data });
        }
        return mockResponse({ success: false, error: errorMessage(res.error) || "添加自定义课程失败" });
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/schedule/custom/update")) {
      try {
        if (hasTauri) {
          const payload = await invoke("update_custom_schedule_course", { req: data || {} });
          fireCustomCourseReconcile();
          return mockResponse(payload);
        }
        const res = await bridgePost("/schedule/custom/update", data || {});
        if (res?.success && res?.data) {
          fireCustomCourseReconcile();
          return mockResponse({ success: true, ...res.data });
        }
        return mockResponse({ success: false, error: errorMessage(res.error) || "修改自定义课程失败" });
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/schedule/custom/delete")) {
      try {
        if (hasTauri) {
          const payload = await invoke("delete_custom_schedule_course", { req: data || {} });
          fireCustomCourseReconcile();
          return mockResponse(payload);
        }
        const res = await bridgePost("/schedule/custom/delete", data || {});
        if (res?.success && res?.data) {
          fireCustomCourseReconcile();
          return mockResponse({ success: true, ...res.data });
        }
        return mockResponse({ success: false, error: errorMessage(res.error) || "删除自定义课程失败" });
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/schedule/export_calendar")) {
      try {
        if (hasTauri) {
          const payload = await invoke("export_schedule_calendar", { req: data || {} });
          return mockResponse({ success: true, ...payload });
        }
        const res = await bridgePost("/export_schedule_calendar", data || {});
        if (res?.success && res?.data) {
          return mockResponse({ success: true, ...res.data });
        }
        return mockResponse({ success: false, error: errorMessage(res.error) || "导出日历失败" });
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/exams")) {
      try {
        const { semester } = data;
        let payload = null;
        if (!hasTauri) {
          const res = await bridgePost("/fetch_exams", { semester: semester || null });
          if (!res?.success) {
            return mockResponse({ success: false, error: errorMessage(res.error) || "获取考试失败" });
          }
          payload = unwrapBridge(res);
        } else {
          payload = await invoke("fetch_exams", { semester: semester || null });
        }
        const base = asRecord$1(payload);
        const isSuccess = payload !== null && base.success !== false;
        if (isSuccess) {
          const rawList = Array.isArray(payload) ? payload : Array.isArray(base.data) ? base.data : [];
          const transformedExams = rawList.map((rawExam) => {
            const exam = asRecord$1(rawExam);
            return {
              ...exam,
              exam_date: exam.date || exam.exam_date || "",
              exam_time: exam.start_time && exam.end_time ? `${String(exam.start_time)}-${String(exam.end_time)}` : exam.start_time || exam.exam_time || "",
              seat_no: exam.seat_number || exam.seat_no || ""
            };
          });
          const examSid = String(data?.student_id || "");
          if (examSid) {
            void reconcileLocalReminders({
              studentId: examSid,
              exams: transformedExams,
              reason: "exams-sync"
            }).catch(() => {
            });
          }
          return mockResponse({ ...base, success: true, data: transformedExams });
        }
        return mockResponse(Object.keys(base).length ? base : { success: false, error: "获取考试失败" });
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/ranking")) {
      try {
        const { student_id, semester } = data;
        if (!hasTauri) {
          const res = await bridgePost("/fetch_ranking", {
            student_id: student_id || "",
            semester: semester || ""
          });
          return mockResponse(unwrapBridge(res));
        }
        const ranking = await invoke("fetch_ranking", {
          studentId: student_id || "",
          semester: semester || ""
        });
        return mockResponse(ranking);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/student_info")) {
      try {
        if (!hasTauri) {
          const res = await bridgePost("/fetch_student_info");
          return mockResponse(unwrapBridge(res));
        }
        const info = await invoke("fetch_student_info");
        return mockResponse(info);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/student_login_access")) {
      try {
        const page = Number(data?.page) > 0 ? Number(data.page) : 1;
        const pageSizeRaw = Number(data?.page_size ?? data?.pageSize);
        const pageSize = pageSizeRaw > 0 ? pageSizeRaw : 10;
        if (!hasTauri) {
          const res = await bridgePost("/fetch_personal_login_access_info", {
            page,
            page_size: pageSize
          });
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("fetch_personal_login_access_info", {
          page,
          pageSize
        });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/classroom/query")) {
      try {
        console.log("[Axios Adapter] Classroom query with data:", data);
        if (!hasTauri) {
          const res = await bridgePost("/fetch_classrooms", {
            week: data.week || null,
            weekday: data.weekday || null,
            periods: data.periods || null,
            building: data.building || null
          });
          return mockResponse(unwrapBridge(res));
        }
        const classrooms = await invoke("fetch_classrooms", {
          week: data.week || null,
          weekday: data.weekday || null,
          periods: data.periods || null,
          building: data.building || null
        });
        console.log("[Axios Adapter] Classroom response:", classrooms);
        return mockResponse(classrooms);
      } catch (err) {
        console.error("[Axios Adapter] Classroom error:", err);
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/training_plan/options")) {
      try {
        console.log("[Axios Adapter] Training plan options request");
        if (!hasTauri) {
          const res = await bridgePost("/fetch_training_plan_options");
          return mockResponse(unwrapBridge(res));
        }
        const options = await invoke("fetch_training_plan_options");
        console.log("[Axios Adapter] Training plan options response:", options);
        return mockResponse(options);
      } catch (err) {
        console.error("[Axios Adapter] Training plan options error:", err);
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/training_plan/jys")) {
      try {
        const yxid = data.yxid || data.kkyx || "";
        console.log("[Axios Adapter] Training plan JYS request for yxid:", yxid);
        if (!hasTauri) {
          const res = await bridgePost("/fetch_training_plan_jys", { yxid });
          return mockResponse(unwrapBridge(res));
        }
        const jys = await invoke("fetch_training_plan_jys", { yxid });
        return mockResponse(jys);
      } catch (err) {
        console.error("[Axios Adapter] Training plan JYS error:", err);
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/training_plan") && !url.includes("/options") && !url.includes("/jys")) {
      try {
        console.log("[Axios Adapter] Training plan courses request:", JSON.stringify(data));
        if (!hasTauri) {
          const res = await bridgePost("/fetch_training_plan_courses", {
            grade: data.grade || null,
            kkxq: data.kkxq || null,
            kkyx: data.kkyx || null,
            kkjys: data.kkjys || null,
            kcxz: data.kcxz || null,
            kcgs: data.kcgs || null,
            kcbh: data.kcbh || null,
            kcmc: data.kcmc || null,
            page: data.page ? Number.parseInt(String(data.page), 10) : 1,
            page_size: data.page_size ? Number.parseInt(String(data.page_size), 10) : 50
          });
          return mockResponse(unwrapBridge(res));
        }
        const courses = await invoke("fetch_training_plan_courses", {
          grade: data.grade || null,
          kkxq: data.kkxq || null,
          kkyx: data.kkyx || null,
          kkjys: data.kkjys || null,
          kcxz: data.kcxz || null,
          kcgs: data.kcgs || null,
          kcbh: data.kcbh || null,
          kcmc: data.kcmc || null,
          page: data.page ? Number.parseInt(String(data.page), 10) : 1,
          page_size: data.page_size ? Number.parseInt(String(data.page_size), 10) : 50
        });
        console.log("[Axios Adapter] Training plan courses response:", JSON.stringify(courses));
        return mockResponse(courses);
      } catch (err) {
        console.error("[Axios Adapter] Training plan courses error:", err);
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/library/dict")) {
      try {
        if (!hasTauri) {
          const res = await bridgePost("/library/dict", {});
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("fetch_library_dict");
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/library/search")) {
      try {
        if (!hasTauri) {
          const res = await bridgePost("/library/search", { params: data || {} });
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("search_library_books", { params: data || {} });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/library/detail")) {
      try {
        const payloadData = data || {};
        if (!hasTauri) {
          const res = await bridgePost("/library/detail", {
            title: payloadData.title || "",
            isbn: payloadData.isbn || "",
            record_id: payloadData.record_id ?? payloadData.recordId ?? null
          });
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("fetch_library_book_detail", {
          title: payloadData.title || "",
          isbn: payloadData.isbn || "",
          recordId: payloadData.record_id ?? payloadData.recordId ?? null
        });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/qxzkb/jcinfo")) {
      try {
        const { xnxq } = data;
        if (!hasTauri) {
          const res = await bridgePost("/qxzkb/jcinfo", { xnxq });
          return mockResponse(unwrapBridge(res));
        }
        const info = await invoke("fetch_qxzkb_jcinfo", { xnxq });
        return mockResponse(info);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/qxzkb/zyxx")) {
      try {
        const { yxid, nj } = data;
        if (!hasTauri) {
          const res = await bridgePost("/qxzkb/zyxx", { yxid, nj });
          return mockResponse(unwrapBridge(res));
        }
        const info = await invoke("fetch_qxzkb_zyxx", { yxid, nj });
        return mockResponse(info);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/qxzkb/kkjys")) {
      try {
        const { kkyxid } = data;
        if (!hasTauri) {
          const res = await bridgePost("/qxzkb/kkjys", { kkyxid });
          return mockResponse(unwrapBridge(res));
        }
        const info = await invoke("fetch_qxzkb_kkjys", { kkyxid });
        return mockResponse(info);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/qxzkb/query")) {
      try {
        const queryPayload = { ...data };
        if (!hasTauri) {
          const res = await bridgePost("/qxzkb/query", queryPayload);
          return mockResponse(unwrapBridge(res));
        }
        const result = await invoke("fetch_qxzkb_list", { query: queryPayload });
        return mockResponse(result);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/course_selection/overview")) {
      try {
        if (!hasTauri) {
          const res = await bridgePost("/course_selection/overview", {});
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("fetch_course_selection_overview");
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/course_selection/list")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/course_selection/list", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("fetch_course_selection_list", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/course_selection/end_time")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/course_selection/end_time", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("fetch_course_selection_end_time", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/course_selection/child_classes")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/course_selection/child_classes", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("fetch_course_selection_child_classes", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/course_selection/selected_courses")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/course_selection/selected_courses", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("fetch_course_selection_selected_courses", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/course_selection/select")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/course_selection/select", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("select_course_selection_course", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/course_selection/withdraw")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/course_selection/withdraw", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("withdraw_course_selection_course", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/course_selection/detail_intro")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/course_selection/detail_intro", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("fetch_course_selection_detail_intro", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/course_selection/detail_teacher")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/course_selection/detail_teacher", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("fetch_course_selection_detail_teacher", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/online_learning/overview")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/overview", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("online_learning_overview", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/online_learning/sync_now")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/sync_now", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("online_learning_sync_now", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/online_learning/sync_runs")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/sync_runs", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("online_learning_list_sync_runs", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/online_learning/clear_cache")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/clear_cache", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("online_learning_clear_cache", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/chaoxing/session_status")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/chaoxing/session_status", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("chaoxing_get_session_status", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/chaoxing/courses")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/chaoxing/courses", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("chaoxing_fetch_courses", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/chaoxing/course_outline")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/chaoxing/course_outline", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("chaoxing_fetch_course_outline", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/chaoxing/course_score") || url.includes("chaoxing/course_score")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/chaoxing/course_score", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("chaoxing_fetch_course_score", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/chaoxing/course_progress")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/chaoxing/course_progress", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("chaoxing_fetch_course_progress", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/chaoxing/launch_url")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/chaoxing/launch_url", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("chaoxing_get_launch_url", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/yuketang/qr_login/create")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/yuketang/qr_login/create", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("yuketang_create_qr_login", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/yuketang/qr_login/poll")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/yuketang/qr_login/poll", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("yuketang_poll_qr_login", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/yuketang/courses")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/yuketang/courses", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("yuketang_fetch_courses", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/yuketang/course_outline")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/yuketang/course_outline", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("yuketang_fetch_course_outline", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/yuketang/course_progress")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/yuketang/course_progress", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("yuketang_fetch_course_progress", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/chaoxing/knowledge_cards")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/chaoxing/knowledge_cards", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("chaoxing_get_knowledge_cards", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/chaoxing/video_status")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/chaoxing/video_status", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("chaoxing_get_video_status", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/chaoxing/course_score")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/chaoxing/course_score", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("chaoxing_fetch_course_score", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/chaoxing/report_progress")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/chaoxing/report_progress", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("chaoxing_report_progress", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/yuketang/course_chapters")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/yuketang/course_chapters", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("yuketang_get_course_chapters", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/yuketang/leaf_info")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/yuketang/leaf_info", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("yuketang_get_leaf_info", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/yuketang/heartbeat")) {
      try {
        const payloadData = { ...data || {} };
        if (!hasTauri) {
          const res = await bridgePost("/online_learning/yuketang/heartbeat", payloadData);
          return mockResponse(unwrapBridge(res));
        }
        const payload = await invoke("yuketang_send_heartbeat", { req: payloadData });
        return mockResponse(payload);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/electricity/balance")) {
      const { area_id, building_id, layer_id, room_id } = data;
      const accountPayload = {
        "utilityType": "electric",
        "bigArea": "",
        "area": area_id,
        "building": building_id,
        "unit": "",
        "level": layer_id,
        "room": room_id,
        "subArea": ""
      };
      try {
        let res = {};
        if (!hasTauri) {
          const bridge = await bridgePost("/electricity_query_account", { payload: accountPayload });
          res = asRecord$1(unwrapBridge(bridge));
        } else {
          res = await invoke("electricity_query_account", { payload: accountPayload });
        }
        if (!res.success) {
          return mockResponse({ success: false, error: res.message || res.error || "电费查询失败" });
        }
        const resultData = asRecord$1(res.resultData);
        const templateList = Array.isArray(resultData.templateList) ? resultData.templateList : [];
        let balance = "0.00";
        let quantity = "0.00";
        templateList.forEach((rawItem) => {
          const item = asRecord$1(rawItem);
          if (item.code === "balance") balance = String(item.value ?? "0.00");
          if (item.code === "quantity") quantity = String(item.value ?? "0.00");
        });
        const offline = !!res.offline;
        const syncTime = res.sync_time || resultData.sync_time;
        return mockResponse({
          success: true,
          balance,
          quantity,
          status: resultData.utilityStatusName || "未知",
          offline,
          sync_time: syncTime || (offline ? "" : (/* @__PURE__ */ new Date()).toISOString())
        });
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/campus_code/config")) {
      const payload = {
        devCode: data?.dev_code || data?.devCode || ""
      };
      try {
        if (!hasTauri) {
          const res2 = await bridgePost("/campus_code/config", { payload });
          return mockResponse(unwrapBridge(res2));
        }
        const res = await invoke("campus_code_fetch_config", { payload });
        return mockResponse(res);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/campus_code/qrcode")) {
      const payload = {
        mode: data?.mode || "online",
        devCode: data?.dev_code || data?.devCode || "",
        qrcodeType: data?.qrcode_type || data?.qrcodeType || ""
      };
      try {
        if (!hasTauri) {
          const res2 = await bridgePost("/campus_code/qrcode", { payload });
          return mockResponse(unwrapBridge(res2));
        }
        const res = await invoke("campus_code_fetch_qrcode", { payload });
        return mockResponse(res);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/campus_code/order_status")) {
      const payload = {
        qrcode: data?.qrcode || "",
        offline: !!data?.offline
      };
      try {
        if (!hasTauri) {
          const res2 = await bridgePost("/campus_code/order_status", { payload });
          return mockResponse(unwrapBridge(res2));
        }
        const res = await invoke("campus_code_fetch_order_status", { payload });
        return mockResponse(res);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/calendar")) {
      try {
        const { semester } = data;
        if (!hasTauri) {
          const res = await bridgePost("/fetch_calendar_data", { semester: semester || null });
          return mockResponse(unwrapBridge(res));
        }
        const calendar = await invoke("fetch_calendar_data", {
          semester: semester || null
        });
        return mockResponse(calendar);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    if (url.includes("/v2/academic_progress")) {
      try {
        const { fasz } = data;
        if (!hasTauri) {
          const res = await bridgePost("/fetch_academic_progress", { fasz: fasz || 1 });
          return mockResponse(unwrapBridge(res));
        }
        const progress = await invoke("fetch_academic_progress", {
          fasz: fasz || 1
        });
        return mockResponse(progress);
      } catch (err) {
        return mockResponse({ success: false, error: errorMessage(err) });
      }
    }
    console.warn("[Axios Adapter] Unknown POST endpoint:", url);
    return mockResponse({ success: false, error: "Unknown POST endpoint: " + url });
  } catch (e) {
    console.error("[Axios Adapter] POST Error:", e);
    throw mockError(errorMessage(e));
  }
};
const axiosGet = (url, config = {}) => get(url, config);
const axiosPost = (url, data = {}, config = {}) => post(url, data, config);
const axiosInstance = {
  get: axiosGet,
  post: axiosPost,
  create: () => axiosInstance,
  interceptors: {
    request: { use: () => {
    }, eject: () => {
    } },
    response: { use: () => {
    }, eject: () => {
    } }
  },
  defaults: { headers: { common: {}, get: {}, post: {} } }
};
const PERIOD_TIME_MAP = {
  1: { start: "08:20", end: "09:05" },
  2: { start: "09:10", end: "09:55" },
  3: { start: "10:15", end: "11:00" },
  4: { start: "11:05", end: "11:50" },
  5: { start: "14:00", end: "14:45" },
  6: { start: "14:50", end: "15:35" },
  7: { start: "15:55", end: "16:40" },
  8: { start: "16:45", end: "17:30" },
  9: { start: "18:30", end: "19:15" },
  10: { start: "19:20", end: "20:05" },
  11: { start: "20:10", end: "20:55" },
  12: { start: "21:00", end: "21:45" },
  13: { start: "21:50", end: "22:35" },
  14: { start: "22:40", end: "23:25" }
};
const toSafeString = (v) => String(v ?? "").trim();
const toPositiveInt$1 = (v, fallback) => {
  const n = Number(v);
  if (!Number.isFinite(n) || n <= 0) return fallback;
  return Math.floor(n);
};
const isValidTimeStr = (s) => typeof s === "string" && /^\d{2}:\d{2}$/.test(s);
function formatLocalDate(now) {
  const formatter = new Intl.DateTimeFormat("zh-CN", {
    timeZone: "Asia/Shanghai",
    year: "numeric",
    month: "2-digit",
    day: "2-digit"
  });
  const parts = formatter.formatToParts(now);
  const year = parts.find((p) => p.type === "year")?.value ?? "1970";
  const month = parts.find((p) => p.type === "month")?.value ?? "01";
  const day = parts.find((p) => p.type === "day")?.value ?? "01";
  return `${year}-${month}-${day}`;
}
function getIsoWeekday(now) {
  const formatter = new Intl.DateTimeFormat("en-US", {
    timeZone: "Asia/Shanghai",
    weekday: "short"
  });
  const weekdayStr = formatter.format(now);
  const map = {
    Mon: 1,
    Tue: 2,
    Wed: 3,
    Thu: 4,
    Fri: 5,
    Sat: 6,
    Sun: 7
  };
  return map[weekdayStr] ?? 1;
}
function extractCoursesOfDay(cache, weekIndex, weekday) {
  if (!Array.isArray(cache) || cache.length === 0) return [];
  const results = [];
  for (const item of cache) {
    if (!item || typeof item !== "object") continue;
    const entry = item;
    const itemWeekday = toPositiveInt$1(entry.weekday ?? entry.day, 0);
    if (itemWeekday !== weekday) continue;
    const hasWeekIndex = "week_index" in entry;
    const hasWeeks = "weeks" in entry && Array.isArray(entry.weeks);
    if (hasWeekIndex) {
      if (toPositiveInt$1(entry.week_index, 0) !== weekIndex) continue;
    } else if (hasWeeks) {
      const weeks = entry.weeks.map((w) => Number(w)).filter((w) => Number.isFinite(w) && w > 0);
      if (!weeks.includes(weekIndex)) continue;
    }
    const name = toSafeString(entry.name);
    if (!name) continue;
    const periodStart = toPositiveInt$1(
      entry.period_start ?? entry.period ?? entry.start_period,
      0
    );
    if (periodStart < 1 || periodStart > 14) continue;
    const periodEnd = resolvePeriodEnd(entry, periodStart);
    const timeStart = resolveTimeStart(entry, periodStart);
    const timeEnd = resolveTimeEnd(entry, periodEnd);
    const location2 = toSafeString(entry.location ?? entry.room_code ?? entry.room);
    const teacher = toSafeString(entry.teacher);
    const color = typeof entry.color === "string" && /^#[0-9A-Fa-f]{6}$/.test(entry.color) ? entry.color : void 0;
    const trimmedName = name.slice(0, 80);
    const trimmedLocation = location2.slice(0, 80);
    const existingIdx = results.findIndex(
      (r) => r.name === trimmedName && r.location === trimmedLocation && // 节次有重叠或相邻（periodStart <= existing.period_end + 1）
      periodStart <= r.period_end + 1 && periodEnd >= r.period_start - 1
    );
    if (existingIdx >= 0) {
      const existing = results[existingIdx];
      const newStart = Math.min(existing.period_start, periodStart);
      const newEnd = Math.max(existing.period_end, periodEnd);
      results[existingIdx] = {
        ...existing,
        period_start: newStart,
        period_end: newEnd,
        time_start: newStart < existing.period_start ? timeStart : existing.time_start,
        time_end: newEnd > existing.period_end ? timeEnd : existing.time_end
      };
      continue;
    }
    const course = {
      period_start: periodStart,
      period_end: periodEnd,
      time_start: timeStart,
      time_end: timeEnd,
      name: name.slice(0, 80),
      location: location2.slice(0, 80),
      teacher: teacher.slice(0, 80)
    };
    if (color) course.color = color;
    results.push(course);
  }
  results.sort(
    (a, b) => a.time_start.localeCompare(b.time_start) || a.period_start - b.period_start
  );
  return results;
}
function buildTodayCourseSnapshot(params) {
  const now = params.now ?? /* @__PURE__ */ new Date();
  const cache = Array.isArray(params.cache) ? params.cache : [];
  const date = formatLocalDate(now);
  const weekday = getIsoWeekday(now);
  const courses = extractCoursesOfDay(cache, params.weekIndex, weekday);
  return {
    version: 1,
    generated_at: now.toISOString(),
    date,
    student_id: params.studentId,
    week_index: params.weekIndex,
    weekday,
    courses
  };
}
function resolvePeriodEnd(entry, periodStart) {
  const explicit = toPositiveInt$1(entry.period_end ?? entry.end_period, 0);
  if (explicit >= periodStart && explicit <= 14) return explicit;
  return periodStart;
}
function resolveTimeStart(entry, periodStart) {
  if (isValidTimeStr(entry.time_start)) return entry.time_start;
  return PERIOD_TIME_MAP[periodStart]?.start ?? "00:00";
}
function resolveTimeEnd(entry, periodEnd) {
  if (isValidTimeStr(entry.time_end)) return entry.time_end;
  return PERIOD_TIME_MAP[periodEnd]?.end ?? "23:59";
}
const SCHEDULE_LOCK_KEY$1 = "hbu_schedule_lock";
function readJson(key) {
  try {
    const raw = localStorage.getItem(key);
    if (!raw) return null;
    return JSON.parse(raw);
  } catch {
    return null;
  }
}
function toSafeText$5(value) {
  return String(value ?? "").trim();
}
function readScheduleLockInline(studentId) {
  const record = readJson(SCHEDULE_LOCK_KEY$1);
  if (!record) return "";
  const sid = toSafeText$5(studentId);
  const recordSid = toSafeText$5(record.student_id);
  if (sid && recordSid && sid !== recordSid) return "";
  return toSafeText$5(record.semester);
}
function getCachedScheduleSnapshotInline(studentId, semester) {
  const sid = toSafeText$5(studentId);
  if (!sid) return null;
  const buildKey = (s, sem2) => sem2 ? `schedule:${s}:${sem2}` : `schedule:${s}`;
  const tryParse = (cacheKey) => {
    try {
      const raw = localStorage.getItem(getCacheKey(cacheKey));
      if (!raw) return null;
      const parsed = JSON.parse(raw);
      if (!parsed?.data) return null;
      return { data: parsed.data };
    } catch {
      return null;
    }
  };
  const sem = toSafeText$5(semester);
  if (sem) {
    const scoped = tryParse(buildKey(sid, sem));
    if (scoped) return scoped;
  }
  return tryParse(buildKey(sid));
}
function readCustomCoursesInline(studentId, _semester) {
  try {
    const sid = toSafeText$5(studentId);
    if (!sid) return [];
    const key = `hbu_schedule_render_snapshot_v1:${sid}`;
    const raw = localStorage.getItem(key);
    if (!raw) return [];
    const parsed = JSON.parse(raw);
    if (parsed?.custom_schedule_data && Array.isArray(parsed.custom_schedule_data)) {
      return parsed.custom_schedule_data;
    }
    return [];
  } catch {
    return [];
  }
}
function readCurrentWeekFromMeta() {
  try {
    const raw = localStorage.getItem("hbu_schedule_meta");
    if (!raw) return 1;
    const parsed = JSON.parse(raw);
    const week = Number(parsed?.current_week);
    return Number.isFinite(week) && week >= 1 ? Math.floor(week) : 1;
  } catch {
    return 1;
  }
}
async function afterScheduleRefresh(sid, payload, opts) {
  try {
    const remoteCourses = payload?.data;
    if (!Array.isArray(remoteCourses)) return;
    const lockedSemester = readScheduleLockInline(sid);
    const customCourses = readCustomCoursesInline(sid, lockedSemester);
    const allCourses = [...remoteCourses, ...customCourses];
    const snapshot = buildTodayCourseSnapshot({
      cache: allCourses,
      studentId: sid,
      weekIndex: opts.selectedWeek
    });
    await writeSnapshotWithRetry(snapshot);
    await requestRefresh();
  } catch (err) {
    const code = err?.code ?? "UNKNOWN";
    const message = err instanceof Error ? err.message : String(err);
    console.warn(`[widget] afterScheduleRefresh failed: ${message}`);
    pushDebugLog("widget", "widget_write_failed", "warn", { code, source: "afterScheduleRefresh" });
  }
}
async function tryWriteSnapshotFromCache(sid) {
  try {
    if (!sid) return;
    const lockedSemester = readScheduleLockInline(sid);
    const cached = getCachedScheduleSnapshotInline(sid, lockedSemester);
    const remoteCourses = cached?.data?.data;
    if (!Array.isArray(remoteCourses)) return;
    const customCourses = readCustomCoursesInline(sid, lockedSemester);
    const allCourses = [...remoteCourses, ...customCourses];
    const weekIndex = readCurrentWeekFromMeta();
    const snapshot = buildTodayCourseSnapshot({
      cache: allCourses,
      studentId: sid,
      weekIndex
    });
    await writeSnapshotWithRetry(snapshot);
    await requestRefresh();
  } catch (err) {
    const code = err?.code ?? "UNKNOWN";
    const message = err instanceof Error ? err.message : String(err);
    console.warn(`[widget] tryWriteSnapshotFromCache failed: ${message}`);
    pushDebugLog("widget", "widget_write_failed", "warn", { code, source: "tryWriteSnapshotFromCache" });
  }
}
async function clearWidgetForLogout() {
  try {
    await clearSnapshot();
  } catch (err) {
    const code = err?.code ?? "UNKNOWN";
    const message = err instanceof Error ? err.message : String(err);
    console.warn(`[widget] clearWidgetForLogout failed: ${message}`);
    pushDebugLog("widget", "widget_write_failed", "warn", { code, source: "clearWidgetForLogout" });
  }
}
async function debugWidgetPaths() {
  try {
    if (!isTauriRuntime()) return { error: "not tauri runtime" };
    return await invokeNative("debug_widget_paths");
  } catch (err) {
    return { error: String(err) };
  }
}
async function writeElectricityToWidget(data) {
  try {
    await writeElectricitySnapshot(data);
  } catch (err) {
    console.warn("[widget] writeElectricityToWidget failed:", err);
    pushDebugLog("widget", "widget_write_failed", "warn", { source: "writeElectricityToWidget" });
  }
}
async function writeExamToWidget(data) {
  try {
    await writeExamSnapshot(data);
  } catch (err) {
    console.warn("[widget] writeExamToWidget failed:", err);
    pushDebugLog("widget", "widget_write_failed", "warn", { source: "writeExamToWidget" });
  }
}
async function writeWidgetThemeColor(color) {
  try {
    await writeWidgetThemeColor$1(color);
    await requestRefresh();
  } catch (err) {
    console.warn("[widget] writeWidgetThemeColor failed:", err);
    pushDebugLog("widget", "widget_write_failed", "warn", { source: "writeWidgetThemeColor" });
  }
}
const widget_bridge = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  afterScheduleRefresh,
  clearWidgetForLogout,
  debugWidgetPaths,
  tryWriteSnapshotFromCache,
  writeElectricityToWidget,
  writeExamToWidget,
  writeWidgetThemeColor
}, Symbol.toStringTag, { value: "Module" }));
const API_BASE$1 = "/api";
const SCHEDULE_META_KEY = "hbu_schedule_meta";
const SCHEDULE_LOCK_KEY = "hbu_schedule_lock";
const SCHEDULE_RENDER_SNAPSHOT_SCHEMA = 1;
const SCHEDULE_RENDER_SNAPSHOT_PREFIX = "hbu_schedule_render_snapshot_v1";
const SCHEDULE_POPUP_PENDING_KEY = "hbu_schedule_popup_pending";
const SCHEDULE_SWITCH_PENDING_KEY = "hbu_schedule_switch_pending";
const MAX_SEMESTER_PROBE = 8;
const MANUAL_SCHEDULE_LOCK_REASON = "manual-select";
const AUTO_SCHEDULE_LOCK_REASONS = /* @__PURE__ */ new Set([
  "",
  "warmup",
  "first-enter",
  "schedule-fetch",
  "pending-switch",
  "notify-background",
  "fallback-semester",
  "locked-cache"
]);
const toSafeText$4 = (value) => String(value ?? "").trim();
const toPositiveInt = (value, fallback = 1) => {
  const num = Number(value || 0);
  if (!Number.isFinite(num) || num <= 0) return fallback;
  return Math.floor(num);
};
const readJSON = (key, fallback = null) => {
  try {
    const raw = localStorage.getItem(key);
    if (!raw) return fallback;
    const parsed = JSON.parse(raw);
    return parsed && typeof parsed === "object" ? parsed : fallback;
  } catch {
    return fallback;
  }
};
const writeJSON = (key, value) => {
  try {
    localStorage.setItem(key, JSON.stringify(value));
  } catch {
  }
};
const readScheduleMeta = () => {
  return readJSON(SCHEDULE_META_KEY, {});
};
const updateStoredScheduleMeta = (meta, fallbackSemester = "") => {
  const cached = readScheduleMeta();
  const semester = toSafeText$4(meta?.semester || fallbackSemester || cached?.semester);
  const startDate = toSafeText$4(meta?.start_date || cached?.start_date);
  const week = toPositiveInt(meta?.current_week || cached?.current_week, 1);
  const totalWeeks = toPositiveInt(meta?.total_weeks || cached?.total_weeks, 25);
  const vacationNotice = toSafeText$4(meta?.vacation_notice || cached?.vacation_notice);
  localStorage.setItem(
    SCHEDULE_META_KEY,
    JSON.stringify({
      semester,
      start_date: startDate,
      current_week: week,
      total_weeks: totalWeeks,
      vacation_notice: vacationNotice
    })
  );
  return semester;
};
const buildScheduleCacheKey = (studentId, semester = "") => {
  const sid = toSafeText$4(studentId);
  const sem = toSafeText$4(semester);
  if (!sid) return "";
  return sem ? `schedule:${sid}:${sem}` : `schedule:${sid}`;
};
const buildScheduleRenderSnapshotKey = (studentId) => {
  const sid = toSafeText$4(studentId);
  if (!sid) return "";
  return `${SCHEDULE_RENDER_SNAPSHOT_PREFIX}:${sid}`;
};
const normalizeScheduleRenderSnapshot = (raw, studentId = "", semester = "") => {
  if (!raw || typeof raw !== "object") return null;
  const sid = toSafeText$4(raw.student_id || studentId);
  const sem = toSafeText$4(raw.semester);
  if (!sid || !sem) return null;
  if (studentId && sid !== toSafeText$4(studentId)) return null;
  if (semester && sem !== toSafeText$4(semester)) return null;
  const schemaVersion = Number(raw.schema_version || 0);
  if (schemaVersion !== SCHEDULE_RENDER_SNAPSHOT_SCHEMA) return null;
  return {
    schema_version: SCHEDULE_RENDER_SNAPSHOT_SCHEMA,
    student_id: sid,
    semester: sem,
    meta: raw.meta && typeof raw.meta === "object" ? {
      semester: toSafeText$4(raw.meta.semester || sem),
      start_date: toSafeText$4(raw.meta.start_date),
      current_week: toPositiveInt(raw.meta.current_week, 1),
      total_weeks: toPositiveInt(raw.meta.total_weeks, 25),
      vacation_notice: toSafeText$4(raw.meta.vacation_notice)
    } : {
      semester: sem,
      start_date: "",
      current_week: 1,
      total_weeks: 25,
      vacation_notice: ""
    },
    selected_week: toPositiveInt(raw.selected_week, 1),
    sync_time: toSafeText$4(raw.sync_time),
    offline: !!raw.offline,
    remote_schedule_data: Array.isArray(raw.remote_schedule_data) ? raw.remote_schedule_data : [],
    custom_schedule_data: Array.isArray(raw.custom_schedule_data) ? raw.custom_schedule_data : [],
    merged_schedule_data: Array.isArray(raw.merged_schedule_data) ? raw.merged_schedule_data : [],
    updated_at: toSafeText$4(raw.updated_at)
  };
};
const readScheduleRenderSnapshot = (studentId, semester = "") => {
  const key = buildScheduleRenderSnapshotKey(studentId);
  if (!key) return null;
  const raw = readJSON(key, null);
  return normalizeScheduleRenderSnapshot(raw, studentId, semester);
};
const writeScheduleRenderSnapshot = (studentId, snapshot) => {
  const normalized = normalizeScheduleRenderSnapshot(
    {
      schema_version: SCHEDULE_RENDER_SNAPSHOT_SCHEMA,
      ...snapshot,
      student_id: toSafeText$4(snapshot?.student_id || studentId)
    },
    studentId
  );
  if (!normalized) return null;
  const key = buildScheduleRenderSnapshotKey(normalized.student_id);
  if (!key) return null;
  writeJSON(key, normalized);
  return normalized;
};
const clearScheduleRenderSnapshot = (studentId = "") => {
  const key = buildScheduleRenderSnapshotKey(studentId);
  if (!key) return false;
  try {
    localStorage.removeItem(key);
    return true;
  } catch {
    return false;
  }
};
const courseCount = (payload) => Array.isArray(payload?.data) ? payload.data.length : 0;
const readScheduleLockRecord = () => readJSON(SCHEDULE_LOCK_KEY, null);
const normalizeScheduleLockRecord = (record) => {
  if (!record || typeof record !== "object") return null;
  const semester = toSafeText$4(record.semester);
  if (!semester) return null;
  const student_id = toSafeText$4(record.student_id);
  const reason = toSafeText$4(record.reason);
  const at = Number(record.at || 0);
  return {
    student_id,
    semester,
    reason,
    at: Number.isFinite(at) && at > 0 ? at : 0
  };
};
const isAutoScheduleLockReason = (reason = "") => {
  const text = toSafeText$4(reason);
  if (text === MANUAL_SCHEDULE_LOCK_REASON) return false;
  return AUTO_SCHEDULE_LOCK_REASONS.has(text);
};
const readScheduleLockDetail = (studentId = "") => {
  const sid = toSafeText$4(studentId);
  const record = normalizeScheduleLockRecord(readScheduleLockRecord());
  if (!record) return null;
  if (sid && record.student_id && sid !== record.student_id) return null;
  return record;
};
const readScheduleLock = (studentId = "") => {
  const record = readScheduleLockDetail(studentId);
  return record?.semester || "";
};
const clearScheduleLock = (studentId = "") => {
  const sid = toSafeText$4(studentId);
  const record = normalizeScheduleLockRecord(readScheduleLockRecord());
  if (!record) return false;
  if (sid && record.student_id && sid !== record.student_id) return false;
  try {
    localStorage.removeItem(SCHEDULE_LOCK_KEY);
    return true;
  } catch {
    return false;
  }
};
const writeScheduleLock = (studentId, semester, reason = "manual") => {
  const sid = toSafeText$4(studentId);
  const sem = toSafeText$4(semester);
  if (!sid || !sem) return "";
  writeJSON(SCHEDULE_LOCK_KEY, {
    student_id: sid,
    semester: sem,
    reason: toSafeText$4(reason),
    at: Date.now()
  });
  return sem;
};
const markScheduleSwitchPending = (studentId, semester, reason = "background") => {
  const sid = toSafeText$4(studentId);
  const sem = toSafeText$4(semester);
  if (!sid || !sem) return;
  writeJSON(SCHEDULE_SWITCH_PENDING_KEY, {
    student_id: sid,
    semester: sem,
    reason: toSafeText$4(reason),
    at: Date.now()
  });
};
const consumeScheduleSwitchPending = (studentId = "") => {
  const sid = toSafeText$4(studentId);
  const record = readJSON(SCHEDULE_SWITCH_PENDING_KEY, null);
  if (!record) return "";
  const targetSid = toSafeText$4(record.student_id);
  if (sid && targetSid && sid !== targetSid) return "";
  localStorage.removeItem(SCHEDULE_SWITCH_PENDING_KEY);
  return toSafeText$4(record.semester);
};
const queueScheduleSemesterPopup = (studentId, semester, reason = "warmup") => {
  const sid = toSafeText$4(studentId);
  const sem = toSafeText$4(semester);
  if (!sid || !sem) return;
  writeJSON(SCHEDULE_POPUP_PENDING_KEY, {
    student_id: sid,
    semester: sem,
    reason: toSafeText$4(reason),
    at: Date.now()
  });
};
const parseCacheEntry = (cacheKey) => {
  const raw = localStorage.getItem(getCacheKey(cacheKey));
  if (!raw) return null;
  try {
    const parsed = JSON.parse(raw);
    if (!parsed?.data) return null;
    return {
      data: parsed.data,
      timestamp: Number(parsed.timestamp) || Date.now()
    };
  } catch {
    return null;
  }
};
const getCachedScheduleSnapshot = (studentId, semester = "") => {
  const sid = toSafeText$4(studentId);
  const sem = toSafeText$4(semester);
  if (!sid) return null;
  const scopedKey = sem ? buildScheduleCacheKey(sid, sem) : "";
  if (scopedKey) {
    const scoped = parseCacheEntry(scopedKey);
    if (scoped) return { ...scoped, key: scopedKey, semester: sem };
  }
  const fallbackKey = buildScheduleCacheKey(sid);
  const fallback = parseCacheEntry(fallbackKey);
  if (fallback) return { ...fallback, key: fallbackKey, semester: sem };
  return null;
};
const buildNearestSemesterOrder = (semesterList, anchorSemester = "") => {
  const list = normalizeSemesterList(semesterList);
  if (!list.length) return [];
  const anchor = toSafeText$4(anchorSemester);
  const anchorIndex = anchor ? list.indexOf(anchor) : -1;
  if (anchorIndex < 0) return list;
  const order = [];
  const seen = /* @__PURE__ */ new Set();
  for (let offset = 0; order.length < list.length; offset += 1) {
    if (offset === 0) {
      const sem = list[anchorIndex];
      if (!seen.has(sem)) {
        seen.add(sem);
        order.push(sem);
      }
      continue;
    }
    const newerIndex = anchorIndex - offset;
    if (newerIndex >= 0) {
      const sem = list[newerIndex];
      if (!seen.has(sem)) {
        seen.add(sem);
        order.push(sem);
      }
    }
    const olderIndex = anchorIndex + offset;
    if (olderIndex < list.length) {
      const sem = list[olderIndex];
      if (!seen.has(sem)) {
        seen.add(sem);
        order.push(sem);
      }
    }
  }
  return order;
};
const querySchedule = async (studentId, semester = "") => {
  const sid = toSafeText$4(studentId);
  const sem = toSafeText$4(semester);
  const key = buildScheduleCacheKey(sid, sem);
  if (!key) return null;
  const { data, fromCache, stale } = await fetchWithCache(key, async () => {
    const res = await axiosInstance.post(`${API_BASE$1}/v2/schedule/query`, {
      student_id: sid,
      semester: sem || void 0
    });
    return res.data;
  });
  return { key, data, fromCache: !!fromCache, stale: !!stale, semester: sem };
};
const normalizeSemesterPayload = (payload) => {
  if (!payload?.success) return null;
  return {
    success: true,
    semester: toSafeText$4(payload?.meta?.semester),
    count: courseCount(payload),
    payload
  };
};
const isAuthoritativeSchedulePayload = (payload, queryResult) => {
  if (!payload?.success) return false;
  if (payload?.offline) return false;
  if (queryResult?.fromCache || queryResult?.stale) return false;
  return true;
};
const warmupScheduleForStudent = async (studentId, options = {}) => {
  const sid = toSafeText$4(studentId);
  if (!sid) {
    return { success: false, error: "missing-student-id" };
  }
  const reasonText = toSafeText$4(options?.reason || "warmup");
  const existingLock = readScheduleLock(sid);
  if (existingLock && !options?.forceProbe) {
    const snapshot = getCachedScheduleSnapshot(sid, existingLock);
    if (snapshot?.data?.success && !snapshot?.data?.offline) {
      return {
        success: true,
        semester: existingLock,
        count: courseCount(snapshot.data),
        fromCache: true,
        source: "locked-cache",
        payload: snapshot.data
      };
    }
  }
  const preferredSemester = toSafeText$4(options?.preferredSemester);
  const cachedMeta = readScheduleMeta();
  const cachedSemester = toSafeText$4(cachedMeta?.semester);
  let semesterList = [];
  let currentSemester = "";
  try {
    const semesterRes = await fetchWithCache("semesters", async () => {
      const res = await axiosInstance.get(`${API_BASE$1}/v2/semesters`);
      return res.data;
    });
    const semesterData = semesterRes?.data;
    if (semesterData?.success) {
      semesterList = normalizeSemesterList(semesterData?.semesters || []);
      currentSemester = toSafeText$4(semesterData?.current);
    }
  } catch {
    semesterList = [];
  }
  const anchorSemester = resolveCurrentSemester(
    semesterList,
    preferredSemester || currentSemester || cachedSemester
  );
  const orderedCandidates = buildNearestSemesterOrder(semesterList, anchorSemester);
  const limitedCandidates = orderedCandidates.slice(0, MAX_SEMESTER_PROBE);
  let firstSuccess = null;
  let picked = null;
  let extraNewerChecks = 0;
  const MAX_EXTRA_NEWER = 2;
  for (const semester of limitedCandidates) {
    if (picked && picked.semester === anchorSemester) break;
    if (picked) {
      if (!semesterIsNewer(semester, picked.semester)) continue;
      if (extraNewerChecks >= MAX_EXTRA_NEWER) continue;
      extraNewerChecks++;
    }
    let queryResult = null;
    try {
      queryResult = await querySchedule(sid, semester);
    } catch {
      continue;
    }
    const payload = queryResult?.data;
    if (payload?.need_login) {
      return { success: false, need_login: true, error: "need-login" };
    }
    const normalized = normalizeSemesterPayload(payload);
    if (!normalized) continue;
    if (!firstSuccess) {
      firstSuccess = {
        semester: semester || normalized.semester,
        payload: normalized.payload,
        fromCache: queryResult?.fromCache,
        stale: queryResult?.stale
      };
    }
    if (normalized.count > 0) {
      picked = {
        semester: semester || normalized.semester,
        payload: normalized.payload,
        fromCache: queryResult?.fromCache,
        stale: queryResult?.stale,
        count: normalized.count
      };
    }
  }
  if (!picked && firstSuccess) {
    picked = {
      semester: toSafeText$4(firstSuccess.semester || firstSuccess.payload?.meta?.semester),
      payload: firstSuccess.payload,
      fromCache: !!firstSuccess.fromCache,
      stale: !!firstSuccess.stale,
      count: courseCount(firstSuccess.payload)
    };
  }
  if (!picked) {
    try {
      const fallback = await querySchedule(sid, "");
      const normalized = normalizeSemesterPayload(fallback?.data);
      if (normalized) {
        picked = {
          semester: normalized.semester || anchorSemester || cachedSemester,
          payload: normalized.payload,
          fromCache: fallback?.fromCache,
          stale: fallback?.stale,
          count: normalized.count
        };
      }
    } catch {
      picked = null;
    }
  }
  if (!picked?.payload?.success) {
    return {
      success: false,
      semester: anchorSemester || cachedSemester,
      error: "schedule-warmup-failed"
    };
  }
  const authoritative = isAuthoritativeSchedulePayload(picked.payload, picked);
  const previousStoredSemester = cachedSemester;
  const payloadSemester = toSafeText$4(picked.payload?.meta?.semester || picked.semester);
  let selectedSemester = payloadSemester || previousStoredSemester || anchorSemester;
  if (authoritative || !previousStoredSemester) {
    selectedSemester = updateStoredScheduleMeta(picked.payload?.meta, selectedSemester);
  }
  const scopedKey = buildScheduleCacheKey(sid, payloadSemester || selectedSemester);
  if (scopedKey) {
    setCachedData(scopedKey, picked.payload);
  }
  setCachedData(buildScheduleCacheKey(sid), picked.payload);
  if (authoritative || options?.forceLock) {
    writeScheduleLock(sid, selectedSemester, reasonText);
  } else {
    const lockDetail = readScheduleLockDetail(sid);
    if (lockDetail && isAutoScheduleLockReason(lockDetail.reason)) {
      clearScheduleLock(sid);
    }
  }
  if (!options?.skipPopup) {
    queueScheduleSemesterPopup(sid, selectedSemester, reasonText);
  }
  const metaWeek = Number(picked.payload?.meta?.current_week) || 1;
  afterScheduleRefresh(sid, picked.payload, { selectedWeek: metaWeek }).catch(() => {
  });
  return {
    success: true,
    semester: selectedSemester,
    count: courseCount(picked.payload),
    fromCache: !!picked.fromCache,
    stale: !!picked.stale,
    authoritative,
    source: picked.count > 0 ? "nearest-with-data" : "fallback-semester",
    payload: picked.payload
  };
};
const LEDGER_SCHEMA_VERSION = 1;
const LEDGER_MAX_ENTRIES = 100;
const LEDGER_ENTRY_TTL_MS = 7 * 24 * 60 * 60 * 1e3;
const ledgerStorageKeyFor = (studentId) => `hbu_notification_ledger:${toSafeText$6(studentId)}`;
const buildLedgerEventKey = (domain, signature) => {
  const d = toSafeText$6(domain) || "unknown";
  const sig = toSafeText$6(signature);
  return sig ? `${d}:${sig}` : "";
};
const emptyLedger = (studentId) => ({
  schema: LEDGER_SCHEMA_VERSION,
  scope: toSafeText$6(studentId),
  updatedAt: "",
  entries: []
});
const readLedgerState = (studentId) => {
  const sid = toSafeText$6(studentId);
  const state2 = readJSON$1(ledgerStorageKeyFor(sid), null);
  if (!state2 || typeof state2 !== "object" || state2.schema !== LEDGER_SCHEMA_VERSION) {
    return emptyLedger(sid);
  }
  return {
    schema: LEDGER_SCHEMA_VERSION,
    scope: toSafeText$6(state2.scope) || sid,
    updatedAt: toSafeText$6(state2.updatedAt),
    entries: Array.isArray(state2.entries) ? state2.entries.filter(
      (entry) => !!entry && typeof entry === "object" && !!toSafeText$6(entry.eventKey) && typeof entry.notifiedAt === "string"
    ).map((entry) => ({
      eventKey: toSafeText$6(entry.eventKey),
      domain: toSafeText$6(entry.domain) || "unknown",
      notifiedAt: toSafeText$6(entry.notifiedAt)
    })) : []
  };
};
const writeLedgerState = (studentId, state2) => {
  const sid = toSafeText$6(studentId);
  if (!sid) return;
  const pruned = pruneLedgerState(
    {
      scope: sid,
      updatedAt: (/* @__PURE__ */ new Date()).toISOString(),
      entries: Array.isArray(state2?.entries) ? state2.entries : []
    },
    /* @__PURE__ */ new Date()
  );
  writeJSON$1(ledgerStorageKeyFor(sid), pruned);
};
const pruneLedgerState = (state2, now) => {
  const timestamp = now instanceof Date && !Number.isNaN(now.getTime()) ? now.getTime() : Date.now();
  const cutoff = timestamp - LEDGER_ENTRY_TTL_MS;
  const byKey = /* @__PURE__ */ new Map();
  for (const entry of Array.isArray(state2?.entries) ? state2.entries : []) {
    const key = toSafeText$6(entry.eventKey);
    if (!key) continue;
    const at = Date.parse(toSafeText$6(entry.notifiedAt));
    if (Number.isFinite(at) && at < cutoff) continue;
    const current = byKey.get(key);
    if (!current || !Number.isFinite(at) || at >= Date.parse(current.notifiedAt)) {
      byKey.set(key, {
        eventKey: key,
        domain: toSafeText$6(entry.domain) || "unknown",
        notifiedAt: toSafeText$6(entry.notifiedAt)
      });
    }
  }
  const entries = Array.from(byKey.values());
  if (entries.length > LEDGER_MAX_ENTRIES) {
    entries.sort(
      (a, b) => Date.parse(a.notifiedAt) - Date.parse(b.notifiedAt) || a.eventKey.localeCompare(b.eventKey)
    );
    entries.splice(0, entries.length - LEDGER_MAX_ENTRIES);
  }
  return {
    schema: LEDGER_SCHEMA_VERSION,
    scope: toSafeText$6(state2.scope),
    updatedAt: toSafeText$6(state2.updatedAt) || new Date(timestamp).toISOString(),
    entries
  };
};
const hasLedgerEntry = (studentId, eventKey) => {
  const key = toSafeText$6(eventKey);
  if (!key) return false;
  return readLedgerState(studentId).entries.some((entry) => entry.eventKey === key);
};
const recordLedgerEntry = (studentId, eventKey, domain, notifiedAt) => {
  const sid = toSafeText$6(studentId);
  const key = toSafeText$6(eventKey);
  if (!sid || !key) return false;
  const now = toSafeText$6(notifiedAt) || (/* @__PURE__ */ new Date()).toISOString();
  const state2 = readLedgerState(sid);
  const existing = state2.entries.find((entry) => entry.eventKey === key);
  if (existing) {
    existing.notifiedAt = now;
    existing.domain = toSafeText$6(domain) || existing.domain;
    writeLedgerState(sid, state2);
    return false;
  }
  state2.entries.push({ eventKey: key, domain: toSafeText$6(domain) || "unknown", notifiedAt: now });
  writeLedgerState(sid, state2);
  return true;
};
const encodeUtf8 = (text) => new TextEncoder().encode(text);
const compareUtf8Bytes$1 = (a, b) => {
  const common = Math.min(a.length, b.length);
  for (let i = 0; i < common; i += 1) {
    if (a[i] !== b[i]) return a[i] - b[i];
  }
  return a.length - b.length;
};
const formatCreditV1 = (value) => {
  const number = typeof value === "number" ? value : Number(value);
  if (!Number.isFinite(number)) return "";
  return number.toFixed(6);
};
const scoreToTextV1 = (value) => {
  if (typeof value === "number") return Number.isFinite(value) ? String(value) : "";
  return toSafeText$6(value);
};
const toContractGradeRecord = (item) => {
  const raw = item && typeof item === "object" ? item : {};
  return {
    courseName: toSafeText$6(raw.course_name ?? raw.courseName),
    courseType: toSafeText$6(raw.course_type ?? raw.courseType),
    credit: formatCreditV1(raw.course_credit ?? raw.courseCredit ?? raw.credit),
    score: scoreToTextV1(raw.final_score ?? raw.score)
  };
};
const buildCrossEndGradeSignature = async (grades) => {
  const list = Array.isArray(grades) ? grades : [];
  const lines = [];
  for (const item of list) {
    const record = toContractGradeRecord(item);
    if (!record.courseName) continue;
    lines.push(`${record.courseName}|${record.courseType}|${record.credit}|${record.score}`);
  }
  if (lines.length === 0) return "";
  const encoded = lines.map((line) => encodeUtf8(line));
  encoded.sort((a, b) => compareUtf8Bytes$1(a, b));
  const payload = encoded.map((bytes) => new TextDecoder().decode(bytes)).join("\n");
  const digest = await crypto.subtle.digest("SHA-256", encodeUtf8(payload).slice());
  return Array.from(new Uint8Array(digest)).map((byte) => byte.toString(16).padStart(2, "0")).join("");
};
const INBOX_PEEK_LIMIT = 100;
let injectedPlatform = null;
const setBackgroundInboxPlatform = (platform) => {
  injectedPlatform = platform;
};
const getInboxPlatform = () => {
  if (injectedPlatform) return injectedPlatform;
  return {
    async peek(limit) {
      const mod = await __vitePreload(() => import("./runtime-bridge-BU9jeOXP.js").then((n) => n.a3), true ? __vite__mapDeps([1,0]) : void 0, import.meta.url);
      return mod.peekBackgroundEvents(limit);
    },
    async ack(ids) {
      const mod = await __vitePreload(() => import("./runtime-bridge-BU9jeOXP.js").then((n) => n.a3), true ? __vite__mapDeps([1,0]) : void 0, import.meta.url);
      return mod.ackBackgroundEvents(ids);
    }
  };
};
const isPlainObject = (value) => !!value && typeof value === "object" && !Array.isArray(value);
const mapEventSource = (value) => {
  const text = toSafeText$6(value);
  if (text === "android") return "android-workmanager";
  if (text === "ios") return "ios-bgapprefresh";
  return value;
};
const toInboxEvent = (raw) => {
  if (!isPlainObject(raw)) return null;
  const payload = isPlainObject(raw.payload) ? raw.payload : {};
  const merged = {
    id: toSafeText$6(raw.id),
    detectedAt: toSafeText$6(payload.detectedAt) || toSafeText$6(raw.occurredAt),
    signature: toSafeText$6(payload.signature),
    type: payload.type,
    source: mapEventSource(toSafeText$6(payload.source) || raw.source),
    targetView: payload.targetView,
    presented: payload.presented,
    meta: payload.meta
  };
  const normalized = normalizeBackgroundDetectedEvent(merged);
  if (!normalized) return null;
  return {
    ...normalized,
    inboxId: toSafeText$6(raw.id),
    kind: toSafeText$6(raw.kind),
    scope: raw.scope == null ? "" : toSafeText$6(raw.scope)
  };
};
const eventTypeToDomain = (type) => {
  if (type === "grades-changed") return "grades";
  if (type === "exams-changed") return "exams";
  if (type === "school-message") return "school-message";
  return "";
};
const defaultSyncDomain = async (domain, studentId) => {
  const sid = toSafeText$6(studentId);
  if (domain === "exams") {
    try {
      const native = await __vitePreload(() => import("./runtime-bridge-BU9jeOXP.js").then((n) => n.a6), true ? __vite__mapDeps([1,0]) : void 0, import.meta.url);
      if (!native.isTauriRuntime()) return { ok: false, error: "unsupported-runtime" };
      const res = await native.invokeNative("fetch_exams", { semester: "" });
      if (!res?.success) {
        const message = toSafeText$6(res?.error || "fetch_exams 失败");
        return { ok: false, error: message ? message.slice(0, 120) : "fetch_exams 失败" };
      }
      const exams = Array.isArray(res.data) ? res.data : [];
      if (sid) {
        const api$1 = await __vitePreload(() => Promise.resolve().then(() => api), true ? void 0 : void 0, import.meta.url);
        api$1.setCachedData(`exams:${sid}:current`, { success: true, data: exams });
      }
      if (sid) {
        void __vitePreload(() => Promise.resolve().then(() => local_reminder_scheduler), true ? void 0 : void 0, import.meta.url).then(
          (mod) => mod.reconcileLocalReminders({ studentId: sid, exams, reason: "bg-inbox-exams" }).catch(() => {
          })
        ).catch(() => {
        });
      }
      return { ok: true };
    } catch (error) {
      const message = toSafeText$6(error?.message || error);
      return { ok: false, error: message ? message.slice(0, 120) : "fetch_exams 调用失败" };
    }
  }
  if (domain !== "grades") return { ok: true };
  try {
    const native = await __vitePreload(() => import("./runtime-bridge-BU9jeOXP.js").then((n) => n.a6), true ? __vite__mapDeps([1,0]) : void 0, import.meta.url);
    if (!native.isTauriRuntime()) return { ok: false, error: "unsupported-runtime" };
    await native.invokeNative("sync_grades", { currentOnly: false });
    return { ok: true };
  } catch (error) {
    const message = toSafeText$6(error?.message || error);
    return { ok: false, error: message ? message.slice(0, 120) : "sync_grades 调用失败" };
  }
};
const inflight = /* @__PURE__ */ new Map();
const consumeBackgroundEventsOnce = async (input) => {
  const sid = toSafeText$6(input.studentId);
  if (!sid) {
    return {
      success: true,
      reason: input.reason,
      skipped: "missing-student-id",
      peeked: 0,
      matched: 0,
      domains: [],
      synced: 0,
      acked: 0,
      suppressed: 0,
      errors: []
    };
  }
  const running = inflight.get(sid);
  if (running) return running;
  const task = (async () => {
    const errors = [];
    const platform = getInboxPlatform();
    let raw = [];
    try {
      raw = await platform.peek(INBOX_PEEK_LIMIT);
    } catch (error) {
      const message = toSafeText$6(error?.message || error);
      errors.push(`读取后台事件失败: ${message ? message.slice(0, 120) : "unknown"}`);
      return {
        success: false,
        reason: input.reason,
        peeked: 0,
        matched: 0,
        domains: [],
        synced: 0,
        acked: 0,
        suppressed: 0,
        errors
      };
    }
    const allEvents = raw.map(toInboxEvent).filter((evt) => !!evt);
    if (allEvents.length === 0) {
      return {
        success: true,
        reason: input.reason,
        skipped: "no-events",
        peeked: raw.length,
        matched: 0,
        domains: [],
        synced: 0,
        acked: 0,
        suppressed: 0,
        errors
      };
    }
    const matched = allEvents.filter((evt) => !evt.scope || evt.scope === sid);
    const byDomain = /* @__PURE__ */ new Map();
    const nonBusiness = [];
    for (const evt of matched) {
      const domain = eventTypeToDomain(evt.type);
      if (!domain) {
        nonBusiness.push(evt);
        continue;
      }
      const list = byDomain.get(domain) || [];
      list.push(evt);
      byDomain.set(domain, list);
    }
    const domains = Array.from(byDomain.keys());
    const syncDomain = input.syncDomain || defaultSyncDomain;
    const ackIds = [];
    let synced = 0;
    let suppressed = 0;
    for (const domain of domains) {
      const list = byDomain.get(domain) || [];
      let result;
      try {
        result = await syncDomain(domain, sid);
      } catch (error) {
        const message = toSafeText$6(error?.message || error);
        result = { ok: false, error: message ? message.slice(0, 120) : "sync 调用异常" };
      }
      if (!result.ok) {
        errors.push(`${domain} 完整同步失败（事件已保留待重试）: ${result.error || "unknown"}`);
        continue;
      }
      synced += 1;
      for (const evt of list) {
        if (evt.presented && evt.signature) {
          recordLedgerEntry(sid, buildLedgerEventKey(domain, evt.signature), domain, evt.detectedAt);
          suppressed += 1;
        }
        ackIds.push(evt.inboxId);
      }
    }
    for (const evt of nonBusiness) {
      ackIds.push(evt.inboxId);
    }
    if (ackIds.length > 0) {
      try {
        const ok = await platform.ack(ackIds);
        if (!ok) {
          errors.push("确认后台事件失败（事件保留，重复处理幂等安全）");
        }
      } catch {
        errors.push("确认后台事件异常（事件保留，重复处理幂等安全）");
      }
    }
    pushDebugLog(
      "Notify",
      `后台事件消费完成 reason=${input.reason || "unknown"} peek=${allEvents.length} matched=${matched.length} domains=${domains.join(",") || "-"} synced=${synced} acked=${ackIds.length} suppressed=${suppressed}`,
      "debug"
    );
    return {
      success: errors.length === 0,
      reason: input.reason,
      peeked: allEvents.length,
      matched: matched.length,
      domains,
      synced,
      acked: ackIds.length,
      suppressed,
      errors
    };
  })();
  inflight.set(sid, task);
  try {
    return await task;
  } finally {
    inflight.delete(sid);
  }
};
const hasUnconsumedPresentedEvent = async (studentId, domain) => {
  const sid = toSafeText$6(studentId);
  if (!sid) return false;
  try {
    const platform = getInboxPlatform();
    const raw = await platform.peek(INBOX_PEEK_LIMIT);
    for (const item of raw) {
      const evt = toInboxEvent(item);
      if (!evt) continue;
      if (evt.scope && evt.scope !== sid) continue;
      if (eventTypeToDomain(evt.type) !== domain) continue;
      if (evt.presented && evt.signature) {
        recordLedgerEntry(sid, buildLedgerEventKey(domain, evt.signature), domain, evt.detectedAt);
        return true;
      }
    }
  } catch {
  }
  return false;
};
const background_notification = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  INBOX_PEEK_LIMIT,
  consumeBackgroundEventsOnce,
  eventTypeToDomain,
  hasUnconsumedPresentedEvent,
  setBackgroundInboxPlatform,
  toInboxEvent
}, Symbol.toStringTag, { value: "Module" }));
const BG_FEATURE_KEY_EXAMS = "hbu_bg_feature_exams";
const readBgFeatureEnabled = (key, fallback = true) => {
  try {
    const raw = localStorage.getItem(key);
    if (raw === null) return fallback;
    return raw === "true";
  } catch {
    return fallback;
  }
};
const toSafeText$3 = (value) => String(value ?? "").trim();
const encodeUtf8Bytes = (text) => new TextEncoder().encode(text);
const compareUtf8Bytes = (a, b) => {
  const common = Math.min(a.length, b.length);
  for (let i = 0; i < common; i += 1) {
    if (a[i] !== b[i]) return a[i] - b[i];
  }
  return a.length - b.length;
};
const buildCrossEndExamSignature = async (exams) => {
  const list = Array.isArray(exams) ? exams : [];
  const lines = [];
  for (const item of list) {
    const raw = item && typeof item === "object" ? item : {};
    const courseName = toSafeText$3(raw.course_name ?? raw.courseName);
    if (!courseName) continue;
    const examDate = toSafeText$3(raw.exam_date ?? raw.examDate);
    const examTime = toSafeText$3(raw.exam_time ?? raw.examTime);
    const location2 = toSafeText$3(raw.location);
    const seatNo = toSafeText$3(raw.seat_no ?? raw.seatNo ?? raw.seat_number);
    const examType = toSafeText$3(raw.exam_type ?? raw.examType);
    lines.push(`${courseName}|${examDate}|${examTime}|${location2}|${seatNo}|${examType}`);
  }
  if (lines.length === 0) return "";
  const encoded = lines.map((line) => encodeUtf8Bytes(line));
  encoded.sort((a, b) => compareUtf8Bytes(a, b));
  const payload = encoded.map((bytes) => new TextDecoder().decode(bytes)).join("\n");
  const digest = await crypto.subtle.digest("SHA-256", encodeUtf8Bytes(payload).slice());
  return Array.from(new Uint8Array(digest)).map((byte) => byte.toString(16).padStart(2, "0")).join("");
};
const examsChangeBaselineKeyFor = (studentId) => `hbu_notify_exams_change_baseline:${toSafeText$3(studentId)}`;
const buildExamLedgerEventKey = async (exams) => {
  const signature = await buildCrossEndExamSignature(exams);
  return signature ? buildLedgerEventKey("exams", signature) : "";
};
const isSchoolInboxItemRead = (item) => {
  const raw = item && typeof item === "object" ? item : {};
  return !!(raw?.is_read ?? raw?.isRead);
};
const readSchoolInboxState = (studentId) => {
  const state2 = readJSON$1(
    schoolInboxStateKeyFor(studentId),
    null
  );
  const ids = Array.isArray(state2?.ids) ? state2.ids.map((item) => toSafeText$6(item)).filter(Boolean) : [];
  return {
    initialized: state2?.initialized === true,
    ids
  };
};
const snapshotChaoxingNoticeCookie = async (loginMode) => {
  const mode = toSafeText$6(loginMode).toLowerCase();
  if (!mode.startsWith("chaoxing") || !isTauriRuntime()) return;
  try {
    const cookies = await invokeNative("get_cookies");
    if (cookies) {
      localStorage.setItem("hbu_chaoxing_notice_cookie", String(cookies));
    }
  } catch {
  }
};
const emitSnapshotUpdate = (snapshot) => {
  if (typeof window === "undefined") return;
  window.dispatchEvent(new CustomEvent("hbu-notify-snapshot-updated", { detail: snapshot }));
};
const resolveRoomLabel = () => {
  const savedLabel = localStorage.getItem("last_dorm_selection_label");
  if (savedLabel && savedLabel.trim()) return savedLabel.trim();
  try {
    const selection = readJSON$1("last_dorm_selection", []);
    if (!Array.isArray(selection) || selection.length !== 4) return "";
    const cacheRaw = localStorage.getItem("cache:static_resource:dormitory_data");
    if (!cacheRaw) return selection.join(" / ");
    const cached = JSON.parse(cacheRaw);
    const dormData = cached?.data?.data || cached?.data;
    if (!Array.isArray(dormData)) return selection.join(" / ");
    const [areaId, buildingId, layerId] = selection;
    const labels = [];
    const area = dormData.find((a) => String(a?.value) === String(areaId));
    labels.push(area?.label || String(areaId));
    const building = area?.children?.find(
      (b) => String(b?.value) === String(buildingId)
    );
    labels.push(building?.label || String(buildingId));
    let layer = building?.children?.find(
      (l) => String(l?.value) === String(layerId)
    );
    if (!layer && String(layerId).startsWith("merged_")) {
      const parts = String(layerId).split("_");
      const lightId = parts[2];
      layer = building?.children?.find(
        (l) => String(l?.value) === lightId
      );
    }
    const layerLabel = layer?.label || "";
    const floorMatch = String(layerLabel).match(/(\d+)/);
    labels.push(floorMatch ? `${floorMatch[0]}层` : String(layerLabel || "?"));
    return labels.join(" ");
  } catch {
    const selection = readJSON$1("last_dorm_selection", []);
    return Array.isArray(selection) ? selection.join("/") : "";
  }
};
const syncWidgetData = async (snapshot) => {
  const elec = snapshot?.electricity;
  if (elec?.success && elec?.configured) {
    const roomLabel = resolveRoomLabel();
    await writeElectricityToWidget({
      quantity: Number(elec.quantity) || 0,
      room: roomLabel,
      acQuantity: Number(elec.acQuantity) || 0,
      isLow: !!elec.isLow
    });
  }
  const exams = snapshot?.exams;
  if (exams?.upcoming && Array.isArray(exams.upcoming) && exams.upcoming.length > 0) {
    const today = /* @__PURE__ */ new Date();
    today.setHours(0, 0, 0, 0);
    const futureExams = exams.upcoming.filter((e) => {
      if (!e.exam_date) return true;
      return new Date(String(e.exam_date)) >= today;
    });
    if (futureExams.length > 0) {
      const first = futureExams[0];
      const examDate = first.exam_date ? new Date(String(first.exam_date)) : null;
      const daysLeft = examDate ? Math.ceil((examDate.getTime() - today.getTime()) / (1e3 * 60 * 60 * 24)) : -1;
      await writeExamToWidget({
        exams: futureExams.slice(0, 3).map((e) => ({
          course_name: String(e.course_name || ""),
          exam_date: String(e.exam_date || ""),
          exam_time: String(e.exam_time || ""),
          location: String(e.location || ""),
          seat_no: String(e.seat_no || "")
        })),
        days_left: daysLeft
      });
    }
  }
};
const ensureNotifyReady = async (allowPrompt = false) => {
  let permission = "prompt";
  try {
    permission = await platformBridge.getNotificationPermission();
  } catch {
    return false;
  }
  if (permission !== "granted" && allowPrompt) {
    try {
      permission = await platformBridge.requestNotificationPermission();
    } catch {
      permission = "denied";
    }
  }
  if (permission !== "granted") return false;
  try {
    await platformBridge.ensureNotificationChannel(DEFAULT_CHANNEL_ID);
  } catch {
  }
  return true;
};
const buildGradeLedgerEventKey = async (grades) => {
  const signature = await buildCrossEndGradeSignature(grades);
  return signature ? buildLedgerEventKey("grades", signature) : "";
};
const sendQueuedNotifications = async (queue2, allowPrompt = false, studentId = "") => {
  if (!Array.isArray(queue2) || queue2.length === 0) return [];
  const canNotify = await ensureNotifyReady(allowPrompt);
  if (!canNotify) return [];
  const sent = [];
  for (let i = 0; i < queue2.length; i += 1) {
    const notice = queue2[i];
    if (!notice?.title) continue;
    try {
      const ok = await platformBridge.sendLocalNotification({
        channelId: DEFAULT_CHANNEL_ID,
        title: String(notice.title),
        body: String(notice.body || ""),
        id: Math.floor(Date.now() / 1e3) + i,
        targetView: String(notice.targetView || "notifications")
      });
      if (ok) {
        sent.push(notice);
        if (studentId && notice.eventKey) {
          recordLedgerEntry(
            toSafeText$6(studentId),
            toSafeText$6(notice.eventKey),
            toSafeText$6(notice.domain) || "unknown"
          );
        }
      }
    } catch {
    }
  }
  return sent;
};
const refreshScheduleSilently = async (studentId) => {
  const timeoutMs = getRequestTimeoutMs();
  try {
    const res = await axiosInstance.post(
      toApiUrl("/v2/schedule/query"),
      { student_id: studentId },
      { timeout: timeoutMs }
    );
    const data = res?.data;
    if (!data?.success) {
      return {
        success: false,
        error: toSafeText$6(data?.error || "课表刷新失败")
      };
    }
    const incomingSemester = toSafeText$6(data?.meta?.semester);
    const total = Array.isArray(data.data) ? data.data.length : 0;
    const lockedSemester = readScheduleLock(studentId);
    setCachedData(`schedule:${studentId}`, data);
    if (incomingSemester) {
      setCachedData(`schedule:${studentId}:${incomingSemester}`, data);
    }
    const hasSwitchableNewSemester = !!lockedSemester && !!incomingSemester && incomingSemester !== lockedSemester && total > 0;
    if (hasSwitchableNewSemester) {
      markScheduleSwitchPending(studentId, incomingSemester, "notify-background");
      queueScheduleSemesterPopup(studentId, incomingSemester, "new-semester");
    } else if (data?.meta) {
      const semesterForMeta = lockedSemester || incomingSemester;
      localStorage.setItem(
        "hbu_schedule_meta",
        JSON.stringify({
          semester: semesterForMeta,
          start_date: toSafeText$6(data.meta.start_date),
          current_week: Number(data.meta.current_week || 0)
        })
      );
    }
    return {
      success: true,
      total,
      semester: incomingSemester,
      current_week: Number(data?.meta?.current_week || 0),
      locked_semester: lockedSemester,
      pending_switch: hasSwitchableNewSemester
    };
  } catch (error) {
    return {
      success: false,
      error: toSafeText$6(error?.message || error || "课表刷新失败")
    };
  }
};
const checkGrades = async (studentId, settings, queue2) => {
  const sid = toSafeText$6(studentId);
  const timeoutMs = getRequestTimeoutMs();
  try {
    const res = await axiosInstance.post(
      toApiUrl("/v2/quick_fetch"),
      { student_id: studentId },
      { timeout: timeoutMs }
    );
    const data = res?.data;
    if (!data?.success) {
      return {
        success: false,
        total: 0,
        changed: false,
        latestItems: [],
        error: toSafeText$6(data?.error || "成绩检查失败")
      };
    }
    setCachedData(`grades:${studentId}`, data);
    const grades = Array.isArray(data.data) ? data.data : [];
    const signature = buildGradesSignature(grades);
    const sigKey = gradeSigKeyFor(studentId);
    const prevSignature = toSafeText$6(localStorage.getItem(sigKey));
    const changed = !!prevSignature && prevSignature !== signature;
    localStorage.setItem(sigKey, signature);
    if (changed && settings.enableGradeNotice) {
      const ledgerKey = await buildGradeLedgerEventKey(grades);
      const alreadyNotified = !!ledgerKey && hasLedgerEntry(sid, ledgerKey);
      const nativePending = !alreadyNotified && await hasUnconsumedPresentedEvent(sid, "grades");
      if (!alreadyNotified && !nativePending) {
        queue2.push({
          title: "成绩有更新",
          body: `检测到新的成绩变动，共 ${grades.length} 条成绩记录，请进入应用查看详情。`,
          targetView: "grades",
          eventKey: ledgerKey || void 0,
          domain: "grades"
        });
      }
    }
    return {
      success: true,
      total: grades.length,
      changed,
      latestItems: pickGradePreview(grades, 6),
      signature
    };
  } catch (error) {
    return {
      success: false,
      total: 0,
      changed: false,
      latestItems: [],
      error: toSafeText$6(error?.message || error || "成绩检查失败")
    };
  }
};
const checkExams = async (studentId, settings, queue2) => {
  const timeoutMs = getRequestTimeoutMs();
  const sid = toSafeText$6(studentId);
  try {
    const res = await axiosInstance.post(
      toApiUrl("/v2/exams"),
      { student_id: studentId, semester: "" },
      { timeout: timeoutMs }
    );
    const data = res?.data;
    if (!data?.success) {
      return {
        success: false,
        total: 0,
        upcoming: [],
        tomorrowCount: 0,
        error: toSafeText$6(data?.error || "考试检查失败")
      };
    }
    setCachedData(`exams:${studentId}:current`, data);
    const exams = Array.isArray(data.data) ? data.data : [];
    const upcoming = pickUpcomingExams(exams, 8);
    const tomorrow = upcoming.filter((item) => item.is_tomorrow);
    const tomorrowKey = getTomorrowKeyForState();
    const tomorrowSignature = buildTomorrowExamSignature(tomorrow);
    const persisted = readJSON$1(examSigKeyFor(studentId), {});
    const shouldNotify = settings.enableExamReminder && tomorrow.length > 0 && (toSafeText$6(persisted?.day) !== tomorrowKey || toSafeText$6(persisted?.signature) !== tomorrowSignature);
    if (shouldNotify) {
      queue2.push({
        title: "考试提醒",
        body: tomorrow.length === 1 ? `明天有考试：${toSafeText$6(tomorrow[0].course_name)}` : `明天共有 ${tomorrow.length} 门考试，请提前做好准备。`,
        targetView: "exams"
      });
    }
    writeJSON$1(examSigKeyFor(studentId), {
      day: tomorrowKey,
      signature: tomorrowSignature,
      updated_at: nowIso()
    });
    if (readBgFeatureEnabled(BG_FEATURE_KEY_EXAMS)) {
      const changeSig = await buildCrossEndExamSignature(exams);
      const baselineKey = examsChangeBaselineKeyFor(sid);
      const prevBaseline = toSafeText$6(localStorage.getItem(baselineKey));
      if (changeSig && prevBaseline && prevBaseline !== changeSig) {
        const ledgerKey = await buildExamLedgerEventKey(exams);
        const alreadyNotified = !!ledgerKey && hasLedgerEntry(sid, ledgerKey);
        const nativePending = !alreadyNotified && await hasUnconsumedPresentedEvent(sid, "exams");
        if (!alreadyNotified && !nativePending) {
          queue2.push({
            title: "考试安排有更新",
            body: "检测到考试安排变化，请打开 Mini-HBUT 查看详情。",
            targetView: "exams",
            eventKey: ledgerKey || void 0,
            domain: "exams"
          });
        }
      }
      if (changeSig) {
        localStorage.setItem(baselineKey, changeSig);
      }
    }
    if (Array.isArray(exams)) {
      void __vitePreload(() => Promise.resolve().then(() => local_reminder_scheduler), true ? void 0 : void 0, import.meta.url).then(
        (mod) => mod.reconcileLocalReminders({
          studentId: sid,
          exams,
          reason: "notify-exams-refresh"
        }).catch(() => {
        })
      ).catch(() => {
      });
    }
    return {
      success: true,
      total: exams.length,
      upcoming,
      tomorrowCount: tomorrow.length
    };
  } catch (error) {
    return {
      success: false,
      total: 0,
      upcoming: [],
      tomorrowCount: 0,
      error: toSafeText$6(error?.message || error || "考试检查失败")
    };
  }
};
const getTomorrowKeyForState = () => {
  const date = /* @__PURE__ */ new Date();
  date.setHours(0, 0, 0, 0);
  date.setDate(date.getDate() + 1);
  return toDayKey(date);
};
const checkElectricity = async (studentId, settings, queue2, launchCheck = false) => {
  const timeoutMs = getRequestTimeoutMs();
  const selectedPath = getDormSelection();
  if (selectedPath.length !== 4) {
    return {
      success: false,
      configured: false,
      selectedPath: [],
      error: "未设置宿舍房间，请先在电费模块选择房间。"
    };
  }
  const [area_id, building_id, layer_id, room_id] = selectedPath;
  const roomKey = selectedPath.join("-");
  const powerStateKey = powerStateKeyFor(studentId, roomKey);
  const layerStr = String(layer_id);
  const isDual = layerStr.startsWith("merged_");
  let lightLayerId = layer_id;
  let acLayerId = null;
  if (isDual) {
    const parts = layerStr.split("_");
    lightLayerId = parts[2];
    acLayerId = parts[3];
  }
  let acRoomValue = isDual ? toSafeText$6(readJSON$1("last_dorm_ac_room", "")) : null;
  if (isDual && !acRoomValue && acLayerId) {
    const m = String(room_id).match(/^(.+?)--(\d+)-(\d+)$/);
    if (m) {
      acRoomValue = `${m[1]}--${acLayerId}-1${m[3]}`;
    }
  }
  try {
    const lightRes = await axiosInstance.post(
      toApiUrl("/v2/electricity/balance"),
      {
        area_id,
        building_id,
        layer_id: isDual ? lightLayerId : layer_id,
        room_id,
        student_id: studentId
      },
      { timeout: timeoutMs }
    );
    const lightData = lightRes?.data;
    if (!lightData?.success) {
      return {
        success: false,
        configured: true,
        selectedPath,
        error: toSafeText$6(lightData?.error || "电费检查失败（照明）")
      };
    }
    const quantity = toSafeNumber(lightData.quantity);
    const balance = toSafeNumber(lightData.balance);
    let acQuantity = null;
    let acBalance = null;
    if (isDual && acLayerId && acRoomValue) {
      try {
        const acRes = await axiosInstance.post(
          toApiUrl("/v2/electricity/balance"),
          {
            area_id,
            building_id,
            layer_id: acLayerId,
            room_id: acRoomValue,
            student_id: studentId
          },
          { timeout: timeoutMs }
        );
        const acData = acRes?.data;
        if (acData?.success) {
          acQuantity = toSafeNumber(acData.quantity);
          acBalance = toSafeNumber(acData.balance);
        }
      } catch {
      }
    }
    const isLightLow = Number.isFinite(quantity) && quantity < POWER_ALERT_THRESHOLD;
    const isAcLow = acQuantity !== null && Number.isFinite(acQuantity) && acQuantity < POWER_ALERT_THRESHOLD;
    const isLow = isLightLow || isAcLow;
    const state2 = readJSON$1(powerStateKey, {});
    let shouldNotify = false;
    if (settings.enablePowerNotice && isLow) {
      if (launchCheck) {
        shouldNotify = toSafeText$6(state2?.last_launch_boot) !== APP_BOOT_ID;
      } else {
        shouldNotify = !state2?.was_low;
      }
    }
    if (shouldNotify) {
      let bodyText;
      if (isDual) {
        const parts = [];
        if (isLightLow) parts.push(`照明 ${Number.isFinite(quantity) ? quantity.toFixed(2) : lightData.quantity} 度`);
        if (isAcLow) parts.push(`空调 ${acQuantity !== null && Number.isFinite(acQuantity) ? acQuantity.toFixed(2) : "?"} 度`);
        bodyText = `当前宿舍 ${parts.join("、")} 不足 ${POWER_ALERT_THRESHOLD} 度，请及时充值。`;
      } else {
        bodyText = `当前宿舍剩余电量 ${Number.isFinite(quantity) ? quantity.toFixed(2) : lightData.quantity} 度，已低于 ${POWER_ALERT_THRESHOLD} 度，请及时充值。`;
      }
      queue2.push({
        title: "电费不足提醒",
        body: bodyText,
        targetView: "electricity"
      });
    }
    writeJSON$1(powerStateKey, {
      was_low: isLow,
      last_quantity: Number.isFinite(quantity) ? quantity : null,
      last_balance: Number.isFinite(balance) ? balance : null,
      ac_quantity: Number.isFinite(acQuantity) ? acQuantity : null,
      ac_balance: Number.isFinite(acBalance) ? acBalance : null,
      is_dual: isDual,
      last_launch_boot: launchCheck && isLow && settings.enablePowerNotice ? APP_BOOT_ID : toSafeText$6(state2?.last_launch_boot),
      last_notified_at: shouldNotify ? nowIso() : toSafeText$6(state2?.last_notified_at),
      updated_at: nowIso()
    });
    return {
      success: true,
      configured: true,
      selectedPath,
      quantity: Number.isFinite(quantity) ? quantity : null,
      balance: Number.isFinite(balance) ? balance : null,
      acQuantity: Number.isFinite(acQuantity) ? acQuantity : null,
      acBalance: Number.isFinite(acBalance) ? acBalance : null,
      isDual,
      status: toSafeText$6(lightData.status),
      isLow,
      sync_time: toSafeText$6(lightData.sync_time)
    };
  } catch (error) {
    return {
      success: false,
      configured: true,
      selectedPath,
      error: toSafeText$6(error?.message || error || "电费检查失败")
    };
  }
};
const checkClassReminder = async (studentId, settings, queue2, scheduleResult) => {
  const sid = toSafeText$6(studentId);
  if (!sid) {
    return {
      success: false,
      enabled: false,
      totalToday: 0,
      triggered: 0,
      reason: "missing-student-id"
    };
  }
  if (!settings.enableClassReminder) {
    return {
      success: true,
      enabled: false,
      totalToday: 0,
      triggered: 0
    };
  }
  const semesterHint = toSafeText$6(scheduleResult?.semester);
  const payload = getSchedulePayloadForReminder(sid, semesterHint);
  const courses = Array.isArray(payload?.data) ? payload.data.slice() : [];
  const semesterForCustom = toSafeText$6(payload?.meta?.semester || semesterHint);
  if (semesterForCustom) {
    try {
      const customRes = await axiosInstance.post(
        toApiUrl("/v2/schedule/custom/list"),
        { student_id: sid, semester: semesterForCustom },
        { timeout: getRequestTimeoutMs() }
      );
      const customData = customRes?.data ?? null;
      if (customData?.success && Array.isArray(customData?.data)) {
        courses.push(
          ...customData.data.map((item) => ({
            ...item,
            is_custom: true
          }))
        );
      }
    } catch {
    }
  }
  const currentWeek = toPositiveInt$2(payload?.meta?.current_week, 0) || toPositiveInt$2(readJSON$1("hbu_schedule_meta", {})?.current_week, 1) || 1;
  const weekday = getTodayWeekday();
  const leadMinutes = Math.min(120, Math.max(5, Number(settings.classLeadMinutes || 30)));
  const todayKey2 = toDayKey(/* @__PURE__ */ new Date());
  const todayClasses = getMergedTodayClasses(courses, currentWeek, weekday);
  const candidates = toCourseReminderItems(todayClasses, leadMinutes);
  const nowMinute = getCurrentMinutePrecise();
  const nextUpcomingCourse = todayClasses.filter((course) => Number.isFinite(course?.startMinutes) && (course?.startMinutes || 0) >= nowMinute).sort((a, b) => a.startPeriod - b.startPeriod)[0] || null;
  const stateKey = classReminderStateKeyFor(sid);
  const state2 = readJSON$1(stateKey, {});
  const sentIds = state2?.day === todayKey2 && Array.isArray(state2?.sent_ids) ? state2.sent_ids.map((item) => toSafeText$6(item)).filter(Boolean) : [];
  const sentSet = new Set(sentIds);
  const toNotify = candidates.filter((item) => !sentSet.has(item.id));
  toNotify.forEach((item) => {
    const suffix = item.teacher ? `，授课教师 ${item.teacher}` : "";
    const leadText = item.minsUntilStart > 0 ? `${item.minsUntilStart} 分钟后` : "即将";
    queue2.push({
      title: "上课提醒",
      body: `${leadText}开始：${item.name}（${item.startClock}，${item.room}${suffix}）`,
      targetView: "schedule"
    });
  });
  const nextCourse = candidates[0] || null;
  const nextIds = [...sentSet, ...toNotify.map((item) => item.id)];
  writeJSON$1(stateKey, {
    day: todayKey2,
    sent_ids: nextIds.slice(-120),
    updated_at: nowIso()
  });
  pushDebugLog(
    "Notify",
    `上课提醒检查完成 total=${todayClasses.length} trigger=${toNotify.length} lead=${leadMinutes}min`,
    "debug",
    {
      semester: semesterHint,
      currentWeek,
      weekday
    }
  );
  const nextCourseItem = nextUpcomingCourse || nextCourse;
  return {
    success: true,
    enabled: true,
    totalToday: todayClasses.length,
    triggered: toNotify.length,
    leadMinutes,
    currentWeek,
    nextCourse: nextCourseItem ? {
      name: toSafeText$6(nextCourseItem?.name || ""),
      room: toSafeText$6(nextCourseItem?.room || ""),
      teacher: toSafeText$6(nextCourseItem?.teacher || ""),
      startClock: toSafeText$6(nextCourseItem?.startClock || ""),
      minsUntilStart: Number.isFinite(Number(nextCourseItem?.minsUntilStart)) ? Number(nextCourseItem?.minsUntilStart) : Math.max(0, Math.floor(Number(nextCourseItem?.startMinutes || nowMinute) - nowMinute))
    } : null
  };
};
const checkSchoolInbox = async (studentId, settings, queue2) => {
  const sid = toSafeText$6(studentId);
  if (!sid) {
    return { success: false, enabled: false, total: 0, triggered: 0, reason: "missing-student-id" };
  }
  if (!settings.enableSchoolInbox) {
    return { success: true, enabled: false, total: 0, triggered: 0 };
  }
  if (!isTauriRuntime()) {
    return {
      success: false,
      enabled: true,
      total: 0,
      triggered: 0,
      error: "学校消息抓取需在 Tauri 桌面端前台运行"
    };
  }
  try {
    const loginMode = resolveLoginMode();
    const response = await invokeNative("school_inbox_fetch", { loginMode });
    const items = Array.isArray(response?.items) ? response.items : [];
    const stateKey = schoolInboxStateKeyFor(sid);
    const state2 = readSchoolInboxState(sid);
    const isFirstSync = !state2.initialized;
    const knownSet = new Set(state2.ids);
    const allIds = items.map((item) => {
      const raw = item && typeof item === "object" ? item : {};
      return toSafeText$6(raw?.id);
    }).filter(Boolean);
    const toNotify = isFirstSync ? [] : items.filter((item) => {
      const raw = item && typeof item === "object" ? item : {};
      const id = toSafeText$6(raw?.id);
      if (!id || knownSet.has(id)) return false;
      return !isSchoolInboxItemRead(item);
    });
    const notYetNotified = [];
    for (const item of toNotify) {
      const raw = item && typeof item === "object" ? item : {};
      const id = toSafeText$6(raw?.id);
      const eventKey = id ? buildLedgerEventKey("school-message", id) : "";
      if (eventKey && hasLedgerEntry(sid, eventKey)) continue;
      notYetNotified.push({ item, eventKey });
    }
    for (const { item, eventKey } of notYetNotified) {
      const raw = item && typeof item === "object" ? item : {};
      queue2.push({
        title: toSafeText$6(raw?.title) || "学校通知",
        body: toSafeText$6(raw?.summary) || "你有新的学校消息",
        targetView: "notifications",
        eventKey: eventKey || void 0,
        domain: "school-message"
      });
    }
    writeJSON$1(stateKey, {
      initialized: true,
      ids: allIds.slice(0, 500),
      updated_at: nowIso()
    });
    await snapshotChaoxingNoticeCookie(loginMode);
    pushDebugLog(
      "Notify",
      `学校消息检查完成 total=${items.length} trigger=${notYetNotified.length} first=${isFirstSync ? "1" : "0"}`,
      "info",
      { source: toSafeText$6(response?.source), loginMode }
    );
    return {
      success: true,
      enabled: true,
      total: items.length,
      triggered: notYetNotified.length,
      source: toSafeText$6(response?.source),
      checkedAt: toSafeText$6(response?.fetchedAt),
      baseline: isFirstSync
    };
  } catch (error) {
    const message = toSafeText$6(error?.message || error);
    pushDebugLog("Notify", `学校消息检查失败: ${message}`, "warn");
    return {
      success: false,
      enabled: true,
      total: 0,
      triggered: 0,
      error: message || "学校消息检查失败"
    };
  }
};
const NOTIFY_SNAPSHOT_EVENT = "hbu-notify-snapshot-updated";
const markSchoolInboxNotified = (studentId, itemId) => {
  const sid = toSafeText$6(studentId);
  const id = toSafeText$6(itemId);
  if (!sid || !id) return false;
  const state2 = readSchoolInboxState(sid);
  if (state2.ids.includes(id)) return true;
  const nextIds = [id, ...state2.ids].slice(0, 500);
  writeJSON$1(schoolInboxStateKeyFor(sid), {
    initialized: true,
    ids: nextIds,
    updated_at: nowIso()
  });
  return true;
};
const getStoredSnapshot = (studentId) => {
  const sid = toSafeText$6(studentId);
  if (!sid) return null;
  return readJSON$1(snapshotKeyFor(sid), null);
};
const setStoredSnapshot = (studentId, snapshot) => {
  const sid = toSafeText$6(studentId);
  if (!sid || !snapshot) return;
  writeJSON$1(snapshotKeyFor(sid), snapshot);
};
const runNotificationCheck = async (input = {}) => {
  const {
    studentId,
    launchCheck = false,
    reason = "manual",
    priority = "foreground",
    allowPermissionPrompt = false
  } = input || {};
  const sid = toSafeText$6(studentId);
  if (!sid) return null;
  const settings = getNotifySettings();
  const dormSelection = getDormSelection();
  const shouldRun = launchCheck || reason === "manual" || reason === "resume" || settings.enableBackground;
  const checkedAt = nowIso();
  if (!shouldRun) {
    const fallbackSnapshot = getStoredSnapshot(sid);
    if (fallbackSnapshot) return fallbackSnapshot;
    return {
      studentId: sid,
      checkedAt,
      runtime: "idle",
      skipped: true,
      settings,
      schedule: { success: false, error: "后台检查未启用" },
      grades: { success: false, total: 0, changed: false, latestItems: [] },
      exams: { success: false, total: 0, upcoming: [], tomorrowCount: 0 },
      classReminder: {
        success: false,
        enabled: !!settings.enableClassReminder,
        totalToday: 0,
        triggered: 0
      },
      electricity: {
        success: false,
        configured: false,
        selectedPath: dormSelection,
        error: "后台检查未启用"
      },
      schoolInbox: {
        success: false,
        enabled: !!settings.enableSchoolInbox,
        total: 0,
        triggered: 0
      },
      notifications: { queued: 0, sent: 0, items: [] }
    };
  }
  const queue2 = [];
  pushDebugLog("Notify", `开始通知检查 reason=${reason} priority=${priority} launch=${launchCheck ? "1" : "0"}`, "info", {
    studentId: sid,
    settings
  });
  const [schedule, grades, exams, electricity] = await Promise.all([
    refreshScheduleSilently(sid),
    checkGrades(sid, settings, queue2),
    checkExams(sid, settings, queue2),
    checkElectricity(sid, settings, queue2, launchCheck)
  ]);
  const [classReminder, schoolInbox] = await Promise.all([
    checkClassReminder(sid, settings, queue2, schedule),
    checkSchoolInbox(sid, settings, queue2)
  ]);
  if (schedule?.success) {
    void reconcileLocalReminders({
      studentId: sid,
      semesterHint: String(schedule?.semester || ""),
      reason: "notify-schedule-refresh"
    }).catch(() => {
    });
  }
  const sent = await sendQueuedNotifications(queue2, allowPermissionPrompt, sid);
  pushDebugLog(
    "Notify",
    `通知检查完成 queue=${queue2.length} sent=${sent.length}`,
    "info",
    {
      studentId: sid,
      reason,
      classTriggered: classReminder?.triggered || 0
    }
  );
  const snapshot = {
    studentId: sid,
    checkedAt,
    runtime: getRuntime(),
    reason,
    priority,
    launchCheck,
    settings,
    schedule,
    grades,
    exams,
    classReminder,
    schoolInbox,
    electricity,
    notifications: {
      queued: queue2.length,
      sent: sent.length,
      items: sent
    }
  };
  setStoredSnapshot(sid, snapshot);
  emitSnapshotUpdate(snapshot);
  syncWidgetData(snapshot).catch(() => {
  });
  return snapshot;
};
let monitorTimer = null;
let monitorLaunchTimer = null;
let monitorStudentId = "";
let monitorChecking = false;
let monitorResumeListener = null;
let monitorOnUpdate = null;
const clearResumeListener = async () => {
  if (!monitorResumeListener) return;
  try {
    await monitorResumeListener.remove();
  } catch {
  }
  monitorResumeListener = null;
};
const monitorCheck = async ({
  launchCheck = false,
  reason = "interval",
  priority = "foreground"
} = {}) => {
  if (!monitorStudentId || monitorChecking) return null;
  monitorChecking = true;
  try {
    const snapshot = await runNotificationCheck({
      studentId: monitorStudentId,
      launchCheck,
      reason,
      priority,
      allowPermissionPrompt: false
    });
    if (typeof monitorOnUpdate === "function" && snapshot) {
      monitorOnUpdate(snapshot);
    }
    return snapshot;
  } finally {
    monitorChecking = false;
  }
};
const startNotificationMonitor = async (input = {}) => {
  const { studentId, onUpdate } = input || {};
  const sid = toSafeText$6(studentId);
  await stopNotificationMonitor();
  if (!sid) return false;
  monitorStudentId = sid;
  monitorOnUpdate = typeof onUpdate === "function" ? onUpdate : null;
  const settings = getNotifySettings();
  const intervalMinutes = Math.max(
    MIN_INTERVAL_MINUTES,
    Number(settings.intervalMinutes || DEFAULT_INTERVAL_MINUTES)
  );
  monitorTimer = window.setInterval(() => {
    monitorCheck({ launchCheck: false, reason: "interval", priority: "background" }).catch(() => {
    });
  }, intervalMinutes * 60 * 1e3);
  pushDebugLog("Notify", `通知轮询已启动 interval=${intervalMinutes}min`, "info", { studentId: sid });
  monitorLaunchTimer = window.setTimeout(() => {
    monitorLaunchTimer = null;
    monitorCheck({ launchCheck: true, reason: "app-launch", priority: "background" }).catch(() => {
    });
  }, 7e3);
  if (isCapacitorRuntime()) {
    try {
      const mod = await __vitePreload(() => import("./runtime-bridge-BU9jeOXP.js").then((n) => n.a7), true ? __vite__mapDeps([1,0]) : void 0, import.meta.url);
      monitorResumeListener = await mod.App.addListener("appStateChange", (state2) => {
        if (state2?.isActive) {
          monitorCheck({ launchCheck: false, reason: "resume", priority: "background" }).catch(() => {
          });
        }
      });
    } catch {
    }
  }
  return true;
};
const stopNotificationMonitor = async () => {
  if (monitorTimer) {
    window.clearInterval(monitorTimer);
    monitorTimer = null;
  }
  if (monitorLaunchTimer) {
    window.clearTimeout(monitorLaunchTimer);
    monitorLaunchTimer = null;
  }
  await clearResumeListener();
  monitorStudentId = "";
  monitorChecking = false;
  monitorOnUpdate = null;
  pushDebugLog("Notify", "通知轮询已停止", "debug");
};
const getLastNotifySnapshot = (studentId) => getStoredSnapshot(studentId);
const getNotificationMonitorSettings = () => getNotifySettings();
const API_BASE = "/api";
const REMOTE_CONFIG_SNAPSHOT_KEY$1 = "hbu_remote_config_snapshot";
const CLOUD_SYNC_DEVICE_ID_KEY$2 = "hbu_cloud_sync_device_id";
const CLOUD_SYNC_LAST_SUCCESS_PREFIX = "hbu_cloud_sync_last_success:";
const CLOUD_SYNC_LAST_UPLOAD_SUCCESS_PREFIX = "hbu_cloud_sync_last_upload_success:";
const CLOUD_SYNC_LAST_DOWNLOAD_SUCCESS_PREFIX = "hbu_cloud_sync_last_download_success:";
const CLOUD_SYNC_BOOTSTRAP_PREFIX = "hbu_cloud_sync_bootstrap_done:";
const CLOUD_SYNC_STATUS_PREFIX = "hbu_cloud_sync_status:";
const CLOUD_SYNC_AUTO_UPLOAD_META_PREFIX = "hbu_cloud_sync_auto_upload_meta:";
const DEFAULT_TIMEOUT_MS$1 = 12e3;
const DEFAULT_COOLDOWN_SEC = 180;
const DEFAULT_UPLOAD_COOLDOWN_SEC = 120;
const DEFAULT_DOWNLOAD_COOLDOWN_SEC = 10;
const DEFAULT_SECRET_REF = "kv1-main";
const SYNC_SCHEMA_VERSION = 4;
const STUDENT_ID_RE$2 = /^\d{10}$/;
const CHALLENGE_SKEW_MS$1 = 3e3;
const CHALLENGE_FALLBACK_TTL_MS$1 = 60 * 1e3;
const safeParseJson$1 = (raw, fallback) => {
  if (!raw) return fallback;
  try {
    return JSON.parse(raw);
  } catch {
    return fallback;
  }
};
const toSafeText$2 = (value) => String(value || "").trim();
const isValidStudentId$2 = (value) => STUDENT_ID_RE$2.test(toSafeText$2(value));
const normalizeProxyEndpoint = (value) => {
  const text = toSafeText$2(value);
  if (!text) return "";
  const withProtocol = /^https?:\/\//i.test(text) ? text : `https://${text}`;
  const normalized = withProtocol.replace(/\/+$/, "");
  if (/\/api\/cloud-sync$/i.test(normalized)) return normalized;
  return `${normalized}/api/cloud-sync`;
};
const clampNumber = (value, min, max, fallback) => {
  const num = Number(value);
  if (!Number.isFinite(num)) return fallback;
  return Math.min(max, Math.max(min, Math.round(num)));
};
const makeStudentKey = (prefix, studentId) => {
  const sid = toSafeText$2(studentId);
  if (!sid) return "";
  return `${prefix}${sid}`;
};
const readCloudSyncStatusInternal = (studentId) => {
  const key = makeStudentKey(CLOUD_SYNC_STATUS_PREFIX, studentId);
  if (!key) return null;
  const parsed = safeParseJson$1(localStorage.getItem(key), null);
  return parsed && typeof parsed === "object" ? parsed : null;
};
const readAutoUploadMeta = (studentId) => {
  const key = makeStudentKey(CLOUD_SYNC_AUTO_UPLOAD_META_PREFIX, studentId);
  if (!key) return {};
  const parsed = safeParseJson$1(localStorage.getItem(key), {});
  return parsed && typeof parsed === "object" ? parsed : {};
};
const writeAutoUploadMeta = (studentId, patch = {}) => {
  const sid = toSafeText$2(studentId);
  const key = makeStudentKey(CLOUD_SYNC_AUTO_UPLOAD_META_PREFIX, sid);
  if (!key) return {};
  const prev = readAutoUploadMeta(sid);
  const next = {
    studentId: sid,
    updatedAt: Date.now(),
    ...prev,
    ...patch && typeof patch === "object" ? patch : {}
  };
  try {
    localStorage.setItem(key, JSON.stringify(next));
  } catch {
  }
  return next;
};
const writeCloudSyncStatus = (studentId, patch = {}) => {
  const sid = toSafeText$2(studentId);
  if (!sid) return null;
  const key = makeStudentKey(CLOUD_SYNC_STATUS_PREFIX, sid);
  if (!key) return null;
  const prev = readCloudSyncStatusInternal(sid) || {};
  const next = {
    studentId: sid,
    updatedAt: Date.now(),
    ...prev,
    ...patch && typeof patch === "object" ? patch : {}
  };
  try {
    localStorage.setItem(key, JSON.stringify(next));
  } catch {
  }
  return next;
};
const pruneValue = (value) => {
  if (value == null) return void 0;
  if (typeof value === "string") {
    const text = value.trim();
    return text ? text : void 0;
  }
  if (Array.isArray(value)) {
    const arr = value.map(pruneValue).filter((item) => item !== void 0);
    return arr.length ? arr : void 0;
  }
  if (typeof value === "object") {
    const next = {};
    Object.entries(value).forEach(([k, v]) => {
      const normalized = pruneValue(v);
      if (normalized !== void 0) {
        next[k] = normalized;
      }
    });
    return Object.keys(next).length ? next : void 0;
  }
  return value;
};
const stableStringify = (value) => {
  const normalized = pruneValue(value);
  if (normalized === void 0) return "";
  const seen = /* @__PURE__ */ new WeakSet();
  const sortValue = (input) => {
    if (!input || typeof input !== "object") return input;
    if (seen.has(input)) return "[Circular]";
    seen.add(input);
    if (Array.isArray(input)) {
      return input.map(sortValue);
    }
    const output = {};
    Object.keys(input).sort().forEach((key) => {
      const item = sortValue(input[key]);
      if (item !== void 0) output[key] = item;
    });
    return output;
  };
  return JSON.stringify(sortValue(normalized));
};
const hashText = (value) => {
  const text = String(value || "");
  let hash = 2166136261;
  for (let i = 0; i < text.length; i += 1) {
    hash ^= text.charCodeAt(i);
    hash = Math.imul(hash, 16777619);
  }
  return (hash >>> 0).toString(36);
};
const buildClientSnapshot = async () => {
  const runtime = detectRuntime();
  let currentVersion = "";
  try {
    currentVersion = toSafeText$2(await getCurrentVersion());
  } catch {
    currentVersion = "";
  }
  const clientPlatform = typeof navigator !== "undefined" ? toSafeText$2(navigator.platform || navigator.userAgent || "") : "";
  return pruneValue({
    version: currentVersion,
    platform: clientPlatform || runtime,
    runtime
  }) || {};
};
const buildNotifySnapshot = (studentId) => {
  const sid = toSafeText$2(studentId);
  const snapshot = sid ? getLastNotifySnapshot(sid) || {} : {};
  const settings = getNotificationMonitorSettings() || {};
  return pruneValue({
    snapshot,
    settings
  }) || {};
};
const buildSettingsSnapshot = () => pruneValue({
  app: safeParseJson$1(localStorage.getItem("hbu_app_settings_v1"), {}),
  ui: safeParseJson$1(localStorage.getItem("hbu_ui_settings_v2"), {}),
  font: safeParseJson$1(localStorage.getItem("hbu_font_settings_v1"), {}),
  login: {
    mode: toSafeText$2(localStorage.getItem("hbu_login_entry_mode")),
    method: toSafeText$2(localStorage.getItem("hbu_login_method")),
    remember: toSafeText$2(localStorage.getItem("hbu_remember"))
  }
}) || {};
const ensureDeviceId$2 = () => {
  let id = toSafeText$2(localStorage.getItem(CLOUD_SYNC_DEVICE_ID_KEY$2));
  if (id) return id;
  try {
    id = (crypto?.randomUUID?.() || "").trim();
  } catch {
    id = "";
  }
  if (!id) {
    id = `device-${Date.now()}-${Math.random().toString(36).slice(2, 10)}`;
  }
  localStorage.setItem(CLOUD_SYNC_DEVICE_ID_KEY$2, id);
  return id;
};
const getCooldownPrefixByAction = (action = "manual") => {
  const normalized = toSafeText$2(action).toLowerCase();
  if (normalized === "upload") return CLOUD_SYNC_LAST_UPLOAD_SUCCESS_PREFIX;
  if (normalized === "download") return CLOUD_SYNC_LAST_DOWNLOAD_SUCCESS_PREFIX;
  return CLOUD_SYNC_LAST_SUCCESS_PREFIX;
};
const getLastSuccessTs = (studentId, action = "") => {
  const key = makeStudentKey(getCooldownPrefixByAction(action), studentId);
  if (!key) return 0;
  return Number(localStorage.getItem(key) || 0) || 0;
};
const setLastSuccessTs = (studentId, action = "", ts = Date.now()) => {
  const key = makeStudentKey(getCooldownPrefixByAction(action), studentId);
  if (!key) return;
  localStorage.setItem(key, String(ts));
};
const clearLastSuccessTs = (studentId, action = "") => {
  const key = makeStudentKey(getCooldownPrefixByAction(action), studentId);
  if (!key) return;
  localStorage.removeItem(key);
};
const discoverSemestersFromCache = (studentId) => {
  const sid = toSafeText$2(studentId);
  const semesters = /* @__PURE__ */ new Set();
  if (!sid) return [];
  const pattern = new RegExp(`^cache:schedule:${sid}:(.+)$`);
  for (let i = 0; i < localStorage.length; i += 1) {
    const key = localStorage.key(i);
    if (!key) continue;
    const match = key.match(pattern);
    if (!match) continue;
    const semester = toSafeText$2(match[1]);
    if (semester) semesters.add(semester);
  }
  const scheduleMeta = safeParseJson$1(localStorage.getItem("hbu_schedule_meta"), {});
  const metaSemester = toSafeText$2(scheduleMeta?.semester);
  if (metaSemester) semesters.add(metaSemester);
  return normalizeSemesterList([...semesters]).map(
    (item) => toSafeText$2(typeof item === "string" ? item : item?.value)
  );
};
const fetchSemestersForSync = async (studentId) => {
  const local = discoverSemestersFromCache(studentId);
  try {
    const res = await axiosInstance.get(`${API_BASE}/v2/semesters`);
    const resData = res?.data && typeof res.data === "object" ? res.data : {};
    const remote = normalizeSemesterList(
      resData.semesters || []
    ).map((item) => toSafeText$2(typeof item === "string" ? item : item?.value));
    if (!remote.length) return local;
    const merged = /* @__PURE__ */ new Set([...local, ...remote]);
    return normalizeSemesterList([...merged]).map(
      (item) => toSafeText$2(typeof item === "string" ? item : item?.value)
    );
  } catch {
    return local;
  }
};
const fetchAllCustomCourses = async (studentId) => {
  const sid = toSafeText$2(studentId);
  if (!sid) return {};
  const semesters = await fetchSemestersForSync(sid);
  const output = {};
  for (const semester of semesters) {
    try {
      const res = await axiosInstance.post(`${API_BASE}/v2/schedule/custom/list`, {
        student_id: sid,
        semester
      });
      const resData = res?.data && typeof res.data === "object" ? res.data : {};
      if (!resData.success) continue;
      const list = Array.isArray(resData.data) ? resData.data : [];
      const normalized = list.map(normalizeCloudCourse).filter((item) => item !== null);
      if (normalized.length > 0) {
        output[semester] = normalized;
      }
    } catch {
    }
  }
  return output;
};
const extractDataArray = (value) => {
  if (Array.isArray(value)) return value;
  const obj = value && typeof value === "object" ? value : {};
  if (Array.isArray(obj.data)) return obj.data;
  return [];
};
const isNonEmptyObject = (value) => !!value && typeof value === "object" && !Array.isArray(value) && Object.keys(value).length > 0;
const toArrayOfObjects = (value) => {
  if (!Array.isArray(value)) return [];
  return value.filter(
    (item) => !!item && typeof item === "object"
  );
};
const readCacheEntry = (key) => {
  const cacheKey = toSafeText$2(key);
  if (!cacheKey) return null;
  const raw = localStorage.getItem(`cache:${cacheKey}`);
  if (!raw) return null;
  const parsed = safeParseJson$1(raw, null);
  if (!parsed || typeof parsed !== "object") return null;
  const record = parsed;
  return {
    data: record?.data || null,
    timestamp: Number(record?.timestamp || 0) || 0
  };
};
const readCachedEntriesByPrefix = (prefix) => {
  const pref = toSafeText$2(prefix);
  if (!pref) return [];
  const entries = [];
  for (let i = 0; i < localStorage.length; i += 1) {
    const storageKey = localStorage.key(i);
    if (!storageKey || !storageKey.startsWith("cache:")) continue;
    const cacheKey = storageKey.slice("cache:".length);
    if (!(cacheKey === pref || cacheKey.startsWith(`${pref}:`))) continue;
    const parsed = safeParseJson$1(localStorage.getItem(storageKey), null);
    if (!parsed || typeof parsed !== "object") continue;
    const record = parsed;
    entries.push({
      key: cacheKey,
      data: record?.data || null,
      timestamp: Number(record?.timestamp || 0) || 0
    });
  }
  entries.sort((a, b) => b.timestamp - a.timestamp);
  return entries;
};
const readLatestCacheObject = (prefix) => {
  const pref = toSafeText$2(prefix);
  if (!pref) return null;
  let latest = null;
  let latestTs = 0;
  for (let i = 0; i < localStorage.length; i += 1) {
    const storageKey = localStorage.key(i);
    if (!storageKey || !storageKey.startsWith("cache:")) continue;
    const cacheKey = storageKey.slice("cache:".length);
    if (!(cacheKey === pref || cacheKey.startsWith(`${pref}:`))) continue;
    const parsed = safeParseJson$1(localStorage.getItem(storageKey), null);
    if (!parsed || typeof parsed !== "object") continue;
    const record = parsed;
    const ts = Number(record?.timestamp || 0);
    if (ts >= latestTs) {
      latestTs = ts;
      latest = record?.data || null;
    }
  }
  return latest;
};
const extractSuffixFromKey = (key, prefix) => {
  const full = toSafeText$2(key);
  const pref = toSafeText$2(prefix);
  if (!full || !pref) return "";
  if (full === pref) return "";
  if (!full.startsWith(`${pref}:`)) return "";
  return toSafeText$2(full.slice(pref.length + 1));
};
const normalizeSemesterFromText = (value) => {
  const text = toSafeText$2(value);
  if (!text) return "";
  const direct = text.match(/(\d{4}-\d{4}-[12])/);
  if (direct) return direct[1];
  const loose = text.match(/(\d{4})\D+(\d{4})\D*([12一二])/);
  if (!loose) return "";
  const termRaw = loose[3];
  const term = termRaw === "二" ? "2" : termRaw === "一" ? "1" : termRaw;
  return `${loose[1]}-${loose[2]}-${term}`;
};
const normalizeCloudCourse = (raw) => {
  const item = raw && typeof raw === "object" ? raw : {};
  const weeks = Array.isArray(item?.weeks) ? item.weeks.map((w) => Number(w)).filter((w) => Number.isFinite(w) && w > 0) : [];
  const weekday = Number(item?.weekday || 0);
  const period = Number(item?.period || 0);
  const djs = Number(item?.djs || 0);
  const name = toSafeText$2(item?.name);
  if (!name || !weekday || !period || !djs || !weeks.length) return null;
  return {
    course_id: toSafeText$2(item?.course_id || item?.courseId || item?.id || item?.source_id || item?.sourceId),
    source_id: toSafeText$2(item?.source_id || item?.sourceId || item?.id || item?.course_id || item?.courseId),
    name,
    teacher: toSafeText$2(item?.teacher),
    room: toSafeText$2(item?.room || item?.room_code),
    weekday,
    period,
    djs,
    weeks
  };
};
const pickCourseId = (item) => toSafeText$2(
  item?.course_id || item?.courseId || item?.kcid || item?.kch_id || item?.kch || item?.course_code || item?.courseCode || item?.source_id || item?.sourceId || item?.id
);
const pickCourseCode = (item) => toSafeText$2(
  item?.course_code || item?.courseCode || item?.kcbh || item?.kch || item?.course_no || item?.courseNo
);
const deriveGradeSemester = (item, fallback = "") => {
  if (!item || typeof item !== "object") return toSafeText$2(fallback);
  const direct = normalizeSemesterFromText(item?.semester || item?.xnxq || item?.term || item?.xq);
  if (direct) return direct;
  const xn = toSafeText$2(item?.xnmmc || item?.school_year);
  const xq = toSafeText$2(item?.xqmmc || item?.term_name || item?.semester_name);
  const merged = normalizeSemesterFromText(`${xn}-${xq}`);
  return merged || toSafeText$2(fallback);
};
const normalizeGradeItem = (item, fallbackSemester = "") => {
  const raw = item && typeof item === "object" ? item : null;
  if (!raw) return null;
  const term = deriveGradeSemester(raw, fallbackSemester);
  const courseName = toSafeText$2(raw?.course_name || raw?.name || raw?.kcmc);
  if (!courseName) return null;
  return pruneValue({
    ...raw,
    term,
    course_name: courseName,
    grade_id: toSafeText$2(raw?.grade_id || raw?.gradeId || raw?.id),
    course_id: toSafeText$2(raw?.course_id || raw?.courseId || raw?.kcid || raw?.kch_id || raw?.kch),
    course_code: toSafeText$2(raw?.course_code || raw?.courseCode || raw?.kcbh || raw?.kch),
    course_nature: toSafeText$2(raw?.course_nature || raw?.courseNature || raw?.kcxzmc || raw?.kcxz),
    course_nature_code: toSafeText$2(raw?.course_nature_code || raw?.courseNatureCode || raw?.kcxz),
    course_credit: toSafeText$2(raw?.course_credit || raw?.courseCredit || raw?.xf),
    final_score: toSafeText$2(raw?.final_score || raw?.finalScore || raw?.score || raw?.zhcj || raw?.cj),
    earned_credit: toSafeText$2(raw?.earned_credit || raw?.earnedCredit || raw?.hdxf || raw?.jd),
    xfjd: toSafeText$2(raw?.xfjd || raw?.creditGradePoint || raw?.gpa || raw?.fxcj),
    sfbk: toSafeText$2(raw?.sfbk),
    sfsq: toSafeText$2(raw?.sfsq),
    cjbj: toSafeText$2(raw?.cjbj),
    course_teacher: toSafeText$2(raw?.course_teacher || raw?.courseTeacher || raw?.rkjs),
    teacher: toSafeText$2(raw?.teacher || raw?.teacher_name || raw?.jsxm || raw?.cjlrjsxm)
  });
};
const makeGradeFingerprint = (item, semester = "") => {
  const sem = toSafeText$2(semester) || deriveGradeSemester(item);
  const name = toSafeText$2(item?.course_name || item?.name || item?.kcmc);
  const score = toSafeText$2(item?.score || item?.final_score || item?.zcj || item?.cj);
  const credit = toSafeText$2(item?.credit || item?.xf || item?.course_credit);
  const gradeId = toSafeText$2(item?.grade_id || item?.gradeId || item?.id);
  const code = toSafeText$2(item?.course_code || item?.courseCode || item?.kch);
  return `${sem}|${gradeId}|${code}|${name}|${score}|${credit}`;
};
const accumulateGradeSnapshot = (sourceEntries) => {
  const all = [];
  const bySemester = {};
  const seen = /* @__PURE__ */ new Set();
  sourceEntries.forEach(({ semester, list }) => {
    list.forEach((item) => {
      const normalized = normalizeGradeItem(item, semester);
      if (!normalized) return;
      const sem = deriveGradeSemester(normalized, semester);
      const fp = makeGradeFingerprint(normalized, sem);
      if (seen.has(fp)) return;
      seen.add(fp);
      all.push(normalized);
      if (sem) {
        if (!Array.isArray(bySemester[sem])) {
          bySemester[sem] = [];
        }
        bySemester[sem].push(normalized);
      }
    });
  });
  return {
    all,
    bySemester
  };
};
const buildGradeSnapshot = (studentId, latestGrades = void 0) => {
  const sid = toSafeText$2(studentId);
  const prefix = `grades:${sid}`;
  const hasAuthoritativeList = Array.isArray(latestGrades);
  const sourceEntries = [];
  if (hasAuthoritativeList) {
    sourceEntries.push({
      semester: "",
      list: toArrayOfObjects(latestGrades)
    });
    return accumulateGradeSnapshot(sourceEntries);
  }
  readCachedEntriesByPrefix(prefix).forEach((entry) => {
    sourceEntries.push({
      semester: normalizeSemesterFromText(extractSuffixFromKey(entry.key, prefix)),
      list: toArrayOfObjects(extractDataArray(entry.data))
    });
  });
  return accumulateGradeSnapshot(sourceEntries);
};
const replaceAuthoritativeGradeCaches = (studentId, grades = []) => {
  const sid = toSafeText$2(studentId);
  if (!sid) return { all: [], bySemester: {} };
  const list = toArrayOfObjects(grades);
  const gradesSnapshot = accumulateGradeSnapshot([{ semester: "", list }]);
  clearCacheByPrefix(`grades:${sid}`);
  setCachedData(`grades:${sid}`, { success: true, data: gradesSnapshot.all });
  Object.entries(gradesSnapshot.bySemester).forEach(([semester, semesterList]) => {
    const sem = normalizeSemesterFromText(semester);
    const gradeList = toArrayOfObjects(semesterList);
    if (!sem || !gradeList.length) return;
    setCachedData(`grades:${sid}:${sem}`, { success: true, data: gradeList });
  });
  return gradesSnapshot;
};
const normalizePersonalInfoPayload = (payload, fallbackStudentId = "") => {
  const raw = payload && typeof payload === "object" ? payload : null;
  const source = raw?.data && typeof raw.data === "object" ? raw.data : raw;
  if (!source || typeof source !== "object") return null;
  const normalized = pruneValue({
    student_id: toSafeText$2(source?.student_id || source?.studentId || fallbackStudentId),
    name: toSafeText$2(source?.name || source?.student_name || source?.studentName),
    gender: toSafeText$2(source?.gender),
    birth_date: toSafeText$2(source?.birth_date || source?.birthDate),
    ethnicity: toSafeText$2(source?.ethnicity),
    college: toSafeText$2(source?.college),
    major: toSafeText$2(source?.major),
    class_name: toSafeText$2(source?.class_name || source?.className),
    grade: toSafeText$2(source?.grade || source?.grade_year || source?.gradeYear),
    duration: toSafeText$2(source?.duration),
    enrollment_date: toSafeText$2(source?.enrollment_date || source?.enrollmentDate)
  });
  if (!normalized || typeof normalized !== "object") return null;
  const record = normalized;
  if (!record.student_id && !record.name) return null;
  return record;
};
const buildPersonalInfoSnapshot = (studentId) => {
  const sid = toSafeText$2(studentId);
  const direct = readCacheEntry(`studentinfo:${sid}`)?.data;
  const legacy = readCacheEntry(`student_info:${sid}`)?.data;
  return normalizePersonalInfoPayload(direct, sid) || normalizePersonalInfoPayload(legacy, sid) || {};
};
const extractRankingObject = (value) => {
  if (!value || typeof value !== "object") return null;
  const raw = value;
  if (isNonEmptyObject(raw?.data)) return raw.data;
  if (isNonEmptyObject(raw)) return raw;
  return null;
};
const buildRankingSnapshot = (studentId) => {
  const sid = toSafeText$2(studentId);
  const prefix = `ranking:${sid}`;
  const bySemester = {};
  readCachedEntriesByPrefix(prefix).forEach((entry) => {
    const fromKey = normalizeSemesterFromText(extractSuffixFromKey(entry.key, prefix));
    const rankingObj = extractRankingObject(entry.data);
    if (!rankingObj) return;
    const sem = normalizeSemesterFromText(rankingObj?.semester) || (fromKey || "all");
    if (!isNonEmptyObject(bySemester[sem])) {
      bySemester[sem] = rankingObj;
    }
  });
  const latest = extractRankingObject(readLatestCacheObject(prefix));
  const current = isNonEmptyObject(bySemester.all) ? bySemester.all : latest || Object.values(bySemester)[0] || null;
  return {
    current,
    bySemester
  };
};
const normalizeScheduleCourseItem = (item) => {
  if (!item || typeof item !== "object") return null;
  const raw = item;
  return pruneValue({
    ...raw,
    course_id: toSafeText$2(raw?.course_id || raw?.courseId || raw?.id || raw?.source_id || raw?.sourceId),
    source_id: toSafeText$2(raw?.source_id || raw?.sourceId || raw?.id || raw?.course_id || raw?.courseId),
    raw_course_id: toSafeText$2(
      raw?.raw_course_id || raw?.rawCourseId || raw?.course_id || raw?.courseId || raw?.source_id || raw?.sourceId || raw?.id
    )
  });
};
const normalizeSchedulePayload = (rawPayload, semester = "") => {
  if (!rawPayload || typeof rawPayload !== "object") return null;
  const raw = rawPayload;
  const data = Array.isArray(raw?.data) ? raw.data.map((item) => normalizeScheduleCourseItem({
    ...item,
    course_id: item?.course_id || item?.courseId || item?.id,
    source_id: item?.source_id || item?.sourceId || item?.id,
    raw_course_id: item?.raw_course_id || item?.rawCourseId || item?.course_id || item?.source_id || item?.id
  })).filter((item) => item !== null) : [];
  const rawMeta = raw?.meta && typeof raw.meta === "object" ? raw.meta : {};
  const sem = toSafeText$2(rawMeta?.semester || semester);
  const meta = pruneValue({
    ...rawMeta,
    semester: sem || toSafeText$2(rawMeta?.semester)
  }) || {};
  return {
    success: true,
    data,
    meta,
    offline: !!raw?.offline,
    sync_time: toSafeText$2(raw?.sync_time) || (/* @__PURE__ */ new Date()).toISOString()
  };
};
const buildScheduleSnapshot = (studentId) => {
  const sid = toSafeText$2(studentId);
  if (!sid) return { by_semester: {} };
  const semesters = discoverSemestersFromCache(sid);
  const bySemester = {};
  for (const semester of semesters) {
    const entry = readCacheEntry(`schedule:${sid}:${semester}`);
    const payload = normalizeSchedulePayload(entry?.data, semester);
    if (!payload) continue;
    bySemester[semester] = payload;
  }
  return {
    by_semester: bySemester
  };
};
const normalizeExamItem = (item, fallbackSemester = "") => {
  const raw = item && typeof item === "object" ? item : null;
  if (!raw) return null;
  const courseName = toSafeText$2(raw?.course_name || raw?.courseName || raw?.name || raw?.kcmc);
  const examDate = toSafeText$2(raw?.exam_date || raw?.examDate || raw?.date || raw?.kssj);
  const examTime = toSafeText$2(raw?.exam_time || raw?.examTime || raw?.time || raw?.kssjd);
  const location2 = toSafeText$2(raw?.location || raw?.room || raw?.exam_room || raw?.ksdd);
  const courseId = pickCourseId(raw);
  const courseCode = pickCourseCode(raw);
  if (!courseName && !examDate && !location2) return null;
  return pruneValue({
    ...raw,
    semester: normalizeSemesterFromText(raw?.semester || raw?.xnxq || fallbackSemester) || toSafeText$2(fallbackSemester),
    course_id: courseId,
    course_code: courseCode,
    course_name: courseName,
    exam_type: toSafeText$2(raw?.exam_type || raw?.examType || raw?.ksxz),
    exam_date: examDate,
    exam_time: examTime,
    location: location2,
    seat_no: toSafeText$2(raw?.seat_no || raw?.seatNo || raw?.zwh),
    missing_course_id: !courseId
  });
};
const buildExamSnapshot = (studentId) => {
  const sid = toSafeText$2(studentId);
  const prefix = `exams:${sid}`;
  const entries = [];
  const current = readCacheEntry(`exams:${sid}:current`);
  if (current) {
    entries.push({
      key: `exams:${sid}:current`,
      semester: "current",
      data: current.data,
      timestamp: current.timestamp
    });
  }
  readCachedEntriesByPrefix(prefix).forEach((entry) => {
    if (entry.key === `exams:${sid}:current`) return;
    entries.push({
      ...entry,
      semester: normalizeSemesterFromText(extractSuffixFromKey(entry.key, prefix)) || extractSuffixFromKey(entry.key, prefix)
    });
  });
  const bySemester = {};
  const all = [];
  const seen = /* @__PURE__ */ new Set();
  entries.forEach((entry) => {
    const sem = toSafeText$2(entry.semester) || "current";
    const list = toArrayOfObjects(extractDataArray(entry.data));
    const normalized = list.map((item) => normalizeExamItem(item, sem)).filter((item) => item !== null);
    if (!normalized.length) return;
    bySemester[sem] = normalized;
    normalized.forEach((item) => {
      const fp = stableStringify({
        semester: sem,
        course_id: item.course_id,
        course_code: item.course_code,
        course_name: item.course_name,
        exam_date: item.exam_date,
        exam_time: item.exam_time,
        location: item.location
      });
      if (seen.has(fp)) return;
      seen.add(fp);
      all.push(item);
    });
  });
  return pruneValue({
    all,
    by_semester: bySemester
  }) || { all: [], by_semester: {} };
};
const buildAcademicSnapshot = (studentId, latestGrades = []) => {
  const sid = toSafeText$2(studentId);
  const gradesSnapshot = buildGradeSnapshot(sid, latestGrades);
  const rankingSnapshot = buildRankingSnapshot(sid);
  const examSnapshot = buildExamSnapshot(sid);
  const scheduleMeta = safeParseJson$1(localStorage.getItem("hbu_schedule_meta"), {});
  return {
    grades: gradesSnapshot.all,
    grades_by_semester: gradesSnapshot.bySemester,
    ranking: rankingSnapshot.current || {},
    ranking_by_semester: rankingSnapshot.bySemester,
    exams: examSnapshot,
    personal_info: buildPersonalInfoSnapshot(sid),
    schedule_meta: scheduleMeta && typeof scheduleMeta === "object" ? scheduleMeta : {},
    schedule: buildScheduleSnapshot(sid)
  };
};
const hasNonEmptyCourseMap = (value) => {
  if (!value || typeof value !== "object" || Array.isArray(value)) return false;
  return Object.values(value).some((list) => Array.isArray(list) && list.length > 0);
};
const mergeCustomCourseSemesters = (customCourses) => {
  if (!customCourses || typeof customCourses !== "object" || Array.isArray(customCourses)) {
    return {};
  }
  const output = {};
  for (const [semester, list] of Object.entries(customCourses)) {
    const sem = toSafeText$2(semester);
    if (!sem || !Array.isArray(list)) continue;
    const normalized = normalizeCloudCourseList(list);
    if (normalized.length > 0) {
      output[sem] = normalized;
    }
  }
  return output;
};
const normalizeCloudCourseList = (list) => {
  return list.map((item) => normalizeCloudCourseItem(item)).filter((item) => item !== null);
};
const normalizeCloudCourseItem = (raw) => {
  const item = raw && typeof raw === "object" ? raw : null;
  if (!item) return null;
  const weeks = Array.isArray(item?.weeks) ? item.weeks.map((w) => Number(w)).filter((w) => Number.isFinite(w) && w > 0) : [];
  const weekday = Number(item?.weekday || 0);
  const period = Number(item?.period || 0);
  const djs = Number(item?.djs || 0);
  const name = toSafeText$2(item?.name);
  if (!name || !weekday || !period || !djs || !weeks.length) return null;
  return {
    course_id: toSafeText$2(item?.course_id || item?.courseId || item?.id || item?.source_id || item?.sourceId),
    source_id: toSafeText$2(item?.source_id || item?.sourceId || item?.id || item?.course_id || item?.courseId),
    name,
    teacher: toSafeText$2(item?.teacher),
    room: toSafeText$2(item?.room || item?.room_code),
    weekday,
    period,
    djs,
    weeks
  };
};
const getIdentityAccessToken = async (forceRefresh = false) => {
  return null;
};
const asRecord = (value) => value && typeof value === "object" ? value : {};
const challengeState$1 = {
  token: "",
  expiresAt: 0,
  endpoint: ""
};
const clearCloudSyncChallengeState = () => {
  challengeState$1.token = "";
  challengeState$1.expiresAt = 0;
};
const shouldAttachChallenge = (path) => {
  const normalized = toSafeText$2(path).toLowerCase();
  return normalized.startsWith("/upload") || normalized.startsWith("/download");
};
const canReuseChallenge$1 = (config) => {
  const endpoint = toSafeText$2(config?.proxyEndpoint || config?.endpoint);
  if (!endpoint) return false;
  if (challengeState$1.endpoint !== endpoint) return false;
  if (!challengeState$1.token) return false;
  return challengeState$1.expiresAt > Date.now() + CHALLENGE_SKEW_MS$1;
};
const loadCloudSyncChallenge = async (config, force = false) => {
  if (!force && canReuseChallenge$1(config)) {
    return challengeState$1.token;
  }
  const secretRef = toSafeText$2(config?.secretRef) || DEFAULT_SECRET_REF;
  const query = new URLSearchParams({ secret_ref: secretRef }).toString();
  const res = await requestCloudSync(`/ping?${query}`, {
    method: "GET",
    config,
    skipChallenge: true,
    allowRetry: false
  });
  const token = toSafeText$2(res?.challenge);
  if (!token) {
    throw new Error("云同步鉴权挑战获取失败");
  }
  const ttlSec = Number(res?.challenge_expires_in || 0);
  const ttlMs = Number.isFinite(ttlSec) && ttlSec > 0 ? Math.max(1e4, Math.round(ttlSec * 1e3)) : CHALLENGE_FALLBACK_TTL_MS$1;
  challengeState$1.token = token;
  challengeState$1.endpoint = toSafeText$2(config?.proxyEndpoint || config?.endpoint);
  challengeState$1.expiresAt = Date.now() + ttlMs;
  return token;
};
const requestCloudSync = async (path, { method = "GET", body, config, skipChallenge = false, allowRetry = true }) => {
  const endpoint = toSafeText$2(config?.proxyEndpoint || config?.endpoint);
  if (!endpoint) {
    throw new Error("云同步中转地址未配置");
  }
  const url = `${endpoint.replace(/\/+$/, "")}${path}`;
  const timeoutMs = Math.max(3e3, Number(config?.timeoutMs || DEFAULT_TIMEOUT_MS$1));
  const makeController = () => typeof AbortController !== "undefined" ? new AbortController() : null;
  const sendOnce = async (challengeToken2 = "", identityToken2 = null) => {
    const controller = makeController();
    const timer = window.setTimeout(() => {
      controller?.abort?.();
    }, timeoutMs);
    try {
      const headers = {
        Accept: "application/json"
      };
      if (body !== void 0) {
        headers["Content-Type"] = "application/json";
      }
      if (identityToken2) {
        headers.Authorization = `Bearer ${identityToken2}`;
      }
      if (challengeToken2) {
        headers["x-cloud-sync-challenge"] = challengeToken2;
      }
      const response2 = await fetch(url, {
        method,
        headers,
        body: body === void 0 ? void 0 : JSON.stringify(body),
        signal: controller?.signal
      });
      const text2 = await response2.text();
      const parsed2 = safeParseJson$1(text2, null);
      return { response: response2, parsed: parsed2, text: text2 };
    } finally {
      window.clearTimeout(timer);
    }
  };
  let challengeToken = "";
  if (!skipChallenge && shouldAttachChallenge(path)) {
    challengeToken = await loadCloudSyncChallenge(config, false);
  }
  let identityToken = await getIdentityAccessToken();
  let { response, parsed, text } = await sendOnce(challengeToken, identityToken);
  if (challengeToken) {
    clearCloudSyncChallengeState();
  }
  if (!response.ok && allowRetry && !skipChallenge && shouldAttachChallenge(path) && response.status === 401) {
    clearCloudSyncChallengeState();
    identityToken = await getIdentityAccessToken(true);
    challengeToken = await loadCloudSyncChallenge(config, true);
    ({ response, parsed, text } = await sendOnce(challengeToken, identityToken));
    if (challengeToken) {
      clearCloudSyncChallengeState();
    }
  }
  if (!response.ok) {
    const errText = toSafeText$2(
      (parsed && typeof parsed === "object" ? parsed?.error || parsed?.message : "") || text || `HTTP ${response.status}`
    );
    const error = new Error(errText || `HTTP ${response.status}`);
    error.status = response.status;
    throw error;
  }
  if (!parsed || typeof parsed !== "object") {
    throw new Error("云同步服务返回了无效响应");
  }
  const parsedObj = parsed;
  if (parsedObj.success === false) {
    throw new Error(toSafeText$2(parsedObj.error || parsedObj.message) || "云同步服务返回失败");
  }
  return parsedObj;
};
const STORAGE_KEY$1 = "hbu_font_settings_v1";
const FONT_NAME = "DeyiHei";
const FONT_DB_NAME = "hbu_font_binary_cache_v1";
const FONT_DB_STORE = "fonts";
const FONT_SOURCES = [
  "https://raw.gitcode.com/superdaobo/mini-hbut-config/blobs/c297dc6928402fc0c73cec17ea7518d3731f7022/SmileySans-Oblique.ttf"
];
const REMOTE_FONT_SOURCES = {
  heiti: {
    jsdelivr: [
      "https://cdn.jsdelivr.net/npm/@fontsource/noto-sans-sc/files/noto-sans-sc-chinese-simplified-400-normal.woff2",
      "https://cdn.jsdelivr.net/npm/@fontsource/noto-sans-sc/files/noto-sans-sc-chinese-simplified-400-normal.woff"
    ],
    unpkg: [
      "https://unpkg.com/@fontsource/noto-sans-sc/files/noto-sans-sc-chinese-simplified-400-normal.woff2",
      "https://unpkg.com/@fontsource/noto-sans-sc/files/noto-sans-sc-chinese-simplified-400-normal.woff"
    ]
  },
  songti: {
    jsdelivr: [
      "https://cdn.jsdelivr.net/npm/@fontsource/noto-serif-sc/files/noto-serif-sc-chinese-simplified-400-normal.woff2",
      "https://cdn.jsdelivr.net/npm/@fontsource/noto-serif-sc/files/noto-serif-sc-chinese-simplified-400-normal.woff"
    ],
    unpkg: [
      "https://unpkg.com/@fontsource/noto-serif-sc/files/noto-serif-sc-chinese-simplified-400-normal.woff2",
      "https://unpkg.com/@fontsource/noto-serif-sc/files/noto-serif-sc-chinese-simplified-400-normal.woff"
    ]
  },
  kaiti: {
    jsdelivr: [
      "https://cdn.jsdelivr.net/npm/@fontsource/ma-shan-zheng/files/ma-shan-zheng-chinese-simplified-400-normal.woff2",
      "https://cdn.jsdelivr.net/npm/@fontsource/ma-shan-zheng/files/ma-shan-zheng-chinese-simplified-400-normal.woff"
    ],
    unpkg: [
      "https://unpkg.com/@fontsource/ma-shan-zheng/files/ma-shan-zheng-chinese-simplified-400-normal.woff2",
      "https://unpkg.com/@fontsource/ma-shan-zheng/files/ma-shan-zheng-chinese-simplified-400-normal.woff"
    ]
  },
  fangsong: {
    jsdelivr: [
      "https://cdn.jsdelivr.net/npm/@fontsource/zcool-xiaowei/files/zcool-xiaowei-chinese-simplified-400-normal.woff2",
      "https://cdn.jsdelivr.net/npm/@fontsource/zcool-xiaowei/files/zcool-xiaowei-chinese-simplified-400-normal.woff"
    ],
    unpkg: [
      "https://unpkg.com/@fontsource/zcool-xiaowei/files/zcool-xiaowei-chinese-simplified-400-normal.woff2",
      "https://unpkg.com/@fontsource/zcool-xiaowei/files/zcool-xiaowei-chinese-simplified-400-normal.woff"
    ]
  }
};
const ANDROID_SANS = "'Noto Sans SC', 'Noto Sans CJK SC', 'Roboto', sans-serif";
const ANDROID_SERIF = "'Noto Serif SC', 'Noto Serif CJK SC', serif";
const FONT_FALLBACK = `'Noto Sans SC', 'Microsoft YaHei', 'PingFang SC', 'Hiragino Sans GB', 'Source Han Sans SC', 'WenQuanYi Micro Hei', ${ANDROID_SANS}`;
const FONT_STACKS = {
  default: FONT_FALLBACK,
  deyihei: `'DeyiHei', ${FONT_FALLBACK}`,
  heiti: `'HBUT-Heiti', 'SimHei', 'Heiti SC', 'Microsoft YaHei', ${ANDROID_SANS}, ${FONT_FALLBACK}`,
  songti: `'HBUT-Songti', 'SimSun', 'Songti SC', 'STSong', ${ANDROID_SERIF}, ${FONT_FALLBACK}`,
  kaiti: `'HBUT-KaiTi', 'KaiTi', 'STKaiti', 'KaiTi_GB2312', 'Kaiti SC', ${ANDROID_SERIF}, ${FONT_FALLBACK}`,
  fangsong: `'HBUT-FangSong', 'FangSong', 'STFangsong', ${ANDROID_SERIF}, ${FONT_FALLBACK}`
};
const SUPPORTED_FONTS = new Set(Object.keys(FONT_STACKS));
const FONT_CDN_PROVIDERS = ["auto", "jsdelivr", "unpkg"];
const openFontDb = () => new Promise((resolve) => {
  if (typeof indexedDB === "undefined") {
    resolve(null);
    return;
  }
  try {
    const request = indexedDB.open(FONT_DB_NAME, 1);
    request.onupgradeneeded = () => {
      const db = request.result;
      if (!db.objectStoreNames.contains(FONT_DB_STORE)) {
        db.createObjectStore(FONT_DB_STORE, { keyPath: "id" });
      }
    };
    request.onsuccess = () => resolve(request.result);
    request.onerror = () => resolve(null);
  } catch {
    resolve(null);
  }
});
const getFontRecordId = (cacheName) => `${cacheName}:latest`;
const readFontCacheRecord = async (cacheName) => {
  const db = await openFontDb();
  if (!db) return null;
  return new Promise((resolve) => {
    try {
      const tx = db.transaction(FONT_DB_STORE, "readonly");
      const store = tx.objectStore(FONT_DB_STORE);
      const request = store.get(getFontRecordId(cacheName));
      request.onsuccess = () => {
        const record = request.result;
        if (!record?.bytes) {
          resolve(null);
          return;
        }
        resolve(record);
      };
      request.onerror = () => resolve(null);
    } catch {
      resolve(null);
    }
  });
};
const writeFontCacheRecord = async (cacheName, sourceUrl, format, bytes) => {
  const db = await openFontDb();
  if (!db || !bytes?.byteLength) return false;
  return new Promise((resolve) => {
    try {
      const tx = db.transaction(FONT_DB_STORE, "readwrite");
      const store = tx.objectStore(FONT_DB_STORE);
      const payload = {
        id: getFontRecordId(cacheName),
        sourceUrl,
        format,
        bytes: toArrayBuffer(bytes),
        updatedAt: Date.now()
      };
      const request = store.put(payload);
      request.onsuccess = () => resolve(true);
      request.onerror = () => resolve(false);
    } catch {
      resolve(false);
    }
  });
};
const FONT_CDN_OPTIONS = [
  { key: "auto", label: "自动（推荐）", desc: "优先 jsDelivr，失败自动回退 unpkg" },
  { key: "jsdelivr", label: "jsDelivr", desc: "优先使用 jsDelivr CDN" },
  { key: "unpkg", label: "unpkg", desc: "优先使用 unpkg CDN" }
];
const CDN_PROVIDER_ORDER = {
  auto: ["jsdelivr", "unpkg"],
  jsdelivr: ["jsdelivr", "unpkg"],
  unpkg: ["unpkg", "jsdelivr"]
};
const DEFAULT_SETTINGS = {
  font: "default",
  loaded: false,
  cdnProvider: "auto"
};
const loadStoredSettings = () => {
  try {
    const raw = localStorage.getItem(STORAGE_KEY$1);
    if (!raw) return null;
    return JSON.parse(raw);
  } catch {
    return null;
  }
};
const normalizeSettings$1 = (raw) => {
  if (!raw || typeof raw !== "object") return { ...DEFAULT_SETTINGS };
  const obj = raw;
  const requested = typeof obj.font === "string" ? obj.font : "default";
  const requestedProvider = typeof obj.cdnProvider === "string" ? obj.cdnProvider : "auto";
  const provider = FONT_CDN_PROVIDERS.includes(requestedProvider) ? requestedProvider : "auto";
  return {
    font: SUPPORTED_FONTS.has(requested) ? requested : "default",
    loaded: !!obj.loaded,
    cdnProvider: provider
  };
};
const state = reactive(normalizeSettings$1(loadStoredSettings()));
const isTauri = () => isTauriRuntime();
const fontLoadingTasks = /* @__PURE__ */ new Map();
const resolveWebFontUrl = () => {
  return FONT_SOURCES[0];
};
const REMOTE_FONT_PROFILE_MAP = {
  heiti: {
    family: "HBUT-Heiti",
    cacheName: "hbu-font-cache-heiti"
  },
  songti: {
    family: "HBUT-Songti",
    cacheName: "hbu-font-cache-songti"
  },
  kaiti: {
    family: "HBUT-KaiTi",
    cacheName: "hbu-font-cache-kaiti"
  },
  fangsong: {
    family: "HBUT-FangSong",
    cacheName: "hbu-font-cache-fangsong"
  }
};
const REMOTE_FONT_FAMILY_MAP = {
  deyihei: FONT_NAME,
  heiti: REMOTE_FONT_PROFILE_MAP.heiti.family,
  songti: REMOTE_FONT_PROFILE_MAP.songti.family,
  kaiti: REMOTE_FONT_PROFILE_MAP.kaiti.family,
  fangsong: REMOTE_FONT_PROFILE_MAP.fangsong.family
};
const inferFontFormat = (url) => {
  const lower = String(url || "").toLowerCase();
  if (lower.includes(".woff2")) return "woff2";
  if (lower.includes(".woff")) return "woff";
  return "truetype";
};
const resolveRemoteFontSources = (fontKey, provider) => {
  const map = REMOTE_FONT_SOURCES[fontKey];
  if (!map) return [];
  const order = CDN_PROVIDER_ORDER[provider] || CDN_PROVIDER_ORDER.auto;
  const seen = /* @__PURE__ */ new Set();
  const list = [];
  for (const vendor of order) {
    const sources = map[vendor] || [];
    for (const url of sources) {
      const text = String(url || "").trim();
      if (!text || seen.has(text)) continue;
      seen.add(text);
      list.push(text);
    }
  }
  return list;
};
const applyFont = (fontKey) => {
  const root = document.documentElement;
  if (!root) return;
  const stack = FONT_STACKS[fontKey] || FONT_STACKS.default;
  root.style.setProperty("--ui-font-family", stack);
};
const ensureFontFaceStyle = (fontFamily, src, format) => {
  if (typeof document === "undefined") return;
  const id = `font-face-${fontFamily.replace(/[^\w-]/g, "_")}`;
  const existing = document.getElementById(id);
  const css = `@font-face { font-family: '${fontFamily}'; src: url('${src}') format('${format}'); font-display: swap; }`;
  if (existing) {
    existing.textContent = css;
    return;
  }
  const style = document.createElement("style");
  style.id = id;
  style.textContent = css;
  document.head.appendChild(style);
};
const toArrayBuffer = (bytes) => {
  return bytes.buffer.slice(bytes.byteOffset, bytes.byteOffset + bytes.byteLength);
};
const decodeBase64 = (base64Text) => {
  const normalized = base64Text.replace(/\s+/g, "");
  const binary = atob(normalized);
  const bytes = new Uint8Array(binary.length);
  for (let i = 0; i < binary.length; i += 1) {
    bytes[i] = binary.charCodeAt(i);
  }
  return bytes;
};
const loadFontFromBytes = async (fontFamily, bytes) => {
  if (typeof document === "undefined") return false;
  if (typeof FontFace === "undefined") return false;
  const font = new FontFace(fontFamily, toArrayBuffer(bytes));
  await font.load();
  document.fonts.add(font);
  await document.fonts.load(`12px ${fontFamily}`);
  return true;
};
const loadFontFromUrl = async (fontFamily, src, format) => {
  if (typeof document === "undefined") return false;
  try {
    if (typeof FontFace !== "undefined") {
      const font = new FontFace(fontFamily, `url(${src})`);
      await font.load();
      document.fonts.add(font);
      await document.fonts.load(`12px ${fontFamily}`);
      return true;
    }
  } catch {
  }
  ensureFontFaceStyle(fontFamily, src, format);
  if (document.fonts?.load) {
    await document.fonts.load(`12px ${fontFamily}`);
  }
  return true;
};
const loadDeyiHeiFontInTauri = async (force = false) => {
  pushDebugLog("Font", `加载得意黑（Tauri）force=${force ? "1" : "0"}`, "debug");
  const payload = await invokeNative("download_deyihei_font_payload", {
    url: FONT_SOURCES[0],
    urls: FONT_SOURCES,
    force: !!force
  });
  if (payload.base64) {
    const bytes = decodeBase64(payload.base64);
    const loaded = await loadFontFromBytes(FONT_NAME, bytes);
    if (loaded) return true;
  }
  if (payload.path) {
    try {
      const bytes = await readNativeBinaryFile(payload.path);
      const loaded = await loadFontFromBytes(FONT_NAME, bytes);
      if (loaded) return true;
    } catch {
    }
    const fileSrc2 = await toNativeFileSrc(payload.path);
    return loadFontFromUrl(FONT_NAME, fileSrc2, "truetype");
  }
  const path = await invokeNative("download_deyihei_font", {
    url: FONT_SOURCES[0],
    urls: FONT_SOURCES,
    force: !!force
  });
  const fileSrc = await toNativeFileSrc(path);
  return loadFontFromUrl(FONT_NAME, fileSrc, "truetype");
};
const loadDeyiHeiFontInWeb = async (force = false) => {
  const resolvedUrl = resolveWebFontUrl();
  pushDebugLog("Font", `加载得意黑（Web）force=${force ? "1" : "0"}`, "debug");
  return loadRemoteFontWithCache(FONT_NAME, [resolvedUrl], "hbu-font-cache-deyihei", force);
};
const fetchRemoteFontResponse = async (sourceUrl, cacheName, force = false, localOnly = false) => {
  let response;
  try {
    if (!force && typeof caches !== "undefined") {
      const cache = await caches.open(cacheName);
      response = await cache.match(sourceUrl);
    }
    if (!response && !localOnly) {
      response = await fetch(sourceUrl, { mode: "cors" });
      if (!response.ok) throw new Error(`font download failed: ${response.status}`);
      if (typeof caches !== "undefined") {
        const cache = await caches.open(cacheName);
        await cache.put(sourceUrl, response.clone());
      }
    }
  } catch {
    response = void 0;
  }
  return response ?? null;
};
const loadRemoteFontWithCache = async (fontFamily, sources, cacheName, force = false, localOnly = false) => {
  if (!force) {
    try {
      const localRecord = await readFontCacheRecord(cacheName);
      if (localRecord?.bytes) {
        const bytes = new Uint8Array(localRecord.bytes);
        const loaded = await loadFontFromBytes(fontFamily, bytes);
        if (loaded) {
          pushDebugLog("Font", `命中本地字体缓存：${cacheName}`);
          return true;
        }
      }
    } catch {
    }
  }
  for (const sourceUrl of sources) {
    const response = await fetchRemoteFontResponse(sourceUrl, cacheName, force, localOnly);
    if (response) {
      try {
        const buffer = await response.arrayBuffer();
        const bytes = new Uint8Array(buffer);
        const loaded = await loadFontFromBytes(fontFamily, bytes);
        if (loaded) {
          await writeFontCacheRecord(cacheName, sourceUrl, inferFontFormat(sourceUrl), bytes);
          pushDebugLog("Font", `字体已写入本地缓存：${cacheName} (${sourceUrl})`, "debug");
          return true;
        }
      } catch {
      }
    }
    if (localOnly) {
      continue;
    }
    try {
      const loaded = await loadFontFromUrl(fontFamily, sourceUrl, inferFontFormat(sourceUrl));
      if (loaded) {
        try {
          const persistResponse = await fetchRemoteFontResponse(sourceUrl, cacheName, false, false);
          if (persistResponse) {
            const persistBuffer = await persistResponse.arrayBuffer();
            const persistBytes = new Uint8Array(persistBuffer);
            await writeFontCacheRecord(cacheName, sourceUrl, inferFontFormat(sourceUrl), persistBytes);
          }
        } catch {
        }
        return true;
      }
    } catch {
    }
  }
  pushDebugLog("Font", `字体加载失败：${fontFamily}`, "warn");
  return false;
};
const loadRemoteFontInWeb = async (fontKey, force = false, localOnly = false) => {
  const profile = REMOTE_FONT_PROFILE_MAP[fontKey];
  if (!profile) return false;
  const sources = resolveRemoteFontSources(fontKey, state.cdnProvider);
  if (sources.length === 0) return false;
  pushDebugLog(
    "Font",
    `加载云端字体：${fontKey}，provider=${state.cdnProvider}，force=${force ? "1" : "0"}，localOnly=${localOnly ? "1" : "0"}`,
    "debug"
  );
  return loadRemoteFontWithCache(profile.family, sources, profile.cacheName, force, localOnly);
};
const loadRemoteFontInTauri = async (fontKey, force = false) => {
  const profile = REMOTE_FONT_PROFILE_MAP[fontKey];
  if (!profile) return false;
  const sources = resolveRemoteFontSources(fontKey, state.cdnProvider);
  if (sources.length === 0) return false;
  pushDebugLog("Font", `加载云端字体（Tauri）：${fontKey}，urls=${sources.length}`, "debug");
  try {
    const payload = await invokeNative("download_remote_font_payload", {
      urls: sources,
      cacheName: `${fontKey}.font`,
      force: !!force
    });
    if (payload.base64) {
      const bytes = decodeBase64(payload.base64);
      const loaded = await loadFontFromBytes(profile.family, bytes);
      if (loaded) return true;
    }
    if (payload.path) {
      const fileSrc = await toNativeFileSrc(payload.path);
      const format = inferFontFormat(sources[0] || "");
      return loadFontFromUrl(profile.family, fileSrc, format);
    }
  } catch (e) {
    pushDebugLog("Font", `Tauri 字体下载失败：${fontKey} — ${e}`, "warn");
  }
  return false;
};
const loadFontByKey = async (fontKey, force = false, localOnly = false) => {
  if (fontKey === "default") return true;
  const remoteFamily = REMOTE_FONT_FAMILY_MAP[fontKey];
  if (!force && remoteFamily && document.fonts?.check(`12px ${remoteFamily}`)) {
    if (fontKey === "deyihei") state.loaded = true;
    return true;
  }
  let ok = false;
  if (fontKey === "deyihei") {
    if (localOnly && !state.loaded) return false;
    ok = isTauri() ? await loadDeyiHeiFontInTauri(force) : await loadDeyiHeiFontInWeb(force);
    state.loaded = !!ok;
    return ok;
  }
  if (isTauri()) {
    ok = await loadRemoteFontInTauri(fontKey, force);
    if (ok) return true;
  }
  ok = await loadRemoteFontInWeb(fontKey, force, localOnly);
  return ok;
};
const ensureFontLoaded = async (fontKey, force = false, localOnly = false) => {
  if (fontKey === "default") return true;
  const existingTask = fontLoadingTasks.get(fontKey);
  if (existingTask) return existingTask;
  const task = (async () => {
    try {
      return await loadFontByKey(fontKey, force, localOnly);
    } catch {
      return false;
    } finally {
      fontLoadingTasks.delete(fontKey);
    }
  })();
  fontLoadingTasks.set(fontKey, task);
  return task;
};
const loadDeyiHeiFont = async (force = false) => {
  const ok = await ensureFontLoaded("deyihei", force);
  state.loaded = !!ok;
  return ok;
};
const setFontCdnProvider = (provider) => {
  if (!FONT_CDN_PROVIDERS.includes(provider)) return;
  state.cdnProvider = provider;
};
const prefetchCdnFonts = async (force = false, onProgress, targetKeys) => {
  const allTargets = ["heiti", "songti", "kaiti", "fangsong", "deyihei"];
  const requested = Array.isArray(targetKeys) ? targetKeys : [];
  const targets = requested.length ? allTargets.filter((key) => requested.includes(key)) : allTargets;
  const results = {};
  for (let i = 0; i < targets.length; i += 1) {
    const key = targets[i];
    const ok = await ensureFontLoaded(key, force);
    results[key] = ok;
    onProgress?.({
      key,
      index: i + 1,
      total: targets.length,
      ok
    });
  }
  return results;
};
const ensurePreferredFontLoaded = async (fontKey) => {
  if (fontKey === "default") return true;
  const loaded = await ensureFontLoaded(fontKey, false);
  if (loaded) return true;
  return ensureFontLoaded(fontKey, true);
};
const initFontSettings = () => {
  watch(
    state,
    () => {
      const normalized = normalizeSettings$1(state);
      const changed = Object.keys(normalized).some((key) => normalized[key] !== state[key]);
      if (changed) {
        Object.assign(state, normalized);
      }
      try {
        localStorage.setItem(STORAGE_KEY$1, JSON.stringify(normalized));
      } catch {
      }
      if (typeof document !== "undefined") {
        applyFont(normalized.font);
      }
      if (normalized.font !== "default") {
        ensurePreferredFontLoaded(normalized.font).catch(() => {
        });
      }
    },
    { deep: true, immediate: true }
  );
  if (state.font !== "default") {
    ensurePreferredFontLoaded(state.font).catch(() => {
    });
  }
};
const useFontSettings = () => state;
const applyFontSettingsSnapshot = async (raw) => {
  const normalized = normalizeSettings$1(raw);
  if (normalized.font !== "default") {
    const cached = await ensureFontLoaded(normalized.font, false, true);
    if (!cached) {
      normalized.font = "default";
    }
  }
  Object.assign(state, normalized);
};
const replaceCustomCourses = async (studentId, bySemester) => {
  const sid = toSafeText$2(studentId);
  if (!sid) return { deleted: 0, added: 0, semesters: 0 };
  const nextMap = mergeCustomCourseSemesters(bySemester);
  const remoteSemesters = Object.keys(nextMap);
  const localSemesters = await fetchSemestersForSync(sid);
  const semesters = normalizeSemesterList([.../* @__PURE__ */ new Set([...remoteSemesters, ...localSemesters])]).map(
    (item) => toSafeText$2(typeof item === "string" ? item : item?.value)
  );
  let deleted = 0;
  let added = 0;
  for (const semester of semesters) {
    let existing = [];
    try {
      const listRes = await axiosInstance.post(`${API_BASE}/v2/schedule/custom/list`, {
        student_id: sid,
        semester
      });
      const listData = asRecord(listRes?.data);
      if (listData.success) {
        existing = Array.isArray(listData.data) ? listData.data : [];
      }
    } catch {
      existing = [];
    }
    for (const item of existing) {
      const raw = item && typeof item === "object" ? item : {};
      const sourceId = toSafeText$2(raw?.source_id || raw?.id);
      if (!sourceId) continue;
      try {
        const delRes = await axiosInstance.post(`${API_BASE}/v2/schedule/custom/delete`, {
          student_id: sid,
          semester,
          course_id: sourceId,
          mode: "all"
        });
        if (asRecord(delRes?.data).success) {
          deleted += 1;
        }
      } catch {
      }
    }
    const nextCourses = Array.isArray(nextMap[semester]) ? nextMap[semester] : [];
    for (const item of nextCourses) {
      const raw = item && typeof item === "object" ? item : {};
      try {
        const addRes = await axiosInstance.post(`${API_BASE}/v2/schedule/custom/add`, {
          student_id: sid,
          semester,
          name: raw.name,
          teacher: raw.teacher || "",
          room: raw.room || "",
          weekday: raw.weekday,
          period: raw.period,
          djs: raw.djs,
          weeks: raw.weeks
        });
        if (asRecord(addRes?.data).success) {
          added += 1;
        }
      } catch {
      }
    }
  }
  return {
    deleted,
    added,
    semesters: semesters.length
  };
};
const applySettingsFromCloud = async (settings) => {
  const payload = settings && typeof settings === "object" ? settings : {};
  const appSettingsRaw = payload?.app;
  const uiSettingsRaw = payload?.ui;
  const fontSettingsRaw = payload?.font;
  if (appSettingsRaw && typeof appSettingsRaw === "object") {
    applyAppSettingsSnapshot(appSettingsRaw);
  }
  if (uiSettingsRaw && typeof uiSettingsRaw === "object") {
    applyUiSettingsSnapshot(uiSettingsRaw);
  }
  if (fontSettingsRaw && typeof fontSettingsRaw === "object") {
    await applyFontSettingsSnapshot(fontSettingsRaw);
  }
  return {
    app: !!appSettingsRaw,
    ui: !!uiSettingsRaw,
    font: !!fontSettingsRaw
  };
};
const applyAcademicFromCloud = (studentId, academic) => {
  const sid = toSafeText$2(studentId);
  const empty = {
    gradesCached: false,
    rankingCached: false,
    personalInfoCached: false,
    scheduleMetaApplied: false,
    scheduleCacheWrites: 0,
    scheduleSemesters: []
  };
  if (!sid || !academic || typeof academic !== "object") {
    return empty;
  }
  const data = academic;
  let gradesCached = false;
  let rankingCached = false;
  let personalInfoCached = false;
  let scheduleMetaApplied = false;
  let scheduleCacheWrites = 0;
  const scheduleSemesters = [];
  const mergedGrades = [];
  const gradeSeen = /* @__PURE__ */ new Set();
  const addGradeItems = (list = [], fallbackSemester = "") => {
    toArrayOfObjects(list).forEach((item) => {
      const sem = deriveGradeSemester(item, fallbackSemester);
      const fp = makeGradeFingerprint(item, sem);
      if (gradeSeen.has(fp)) return;
      gradeSeen.add(fp);
      mergedGrades.push(item);
    });
  };
  const gradesBySemester = data?.grades_by_semester;
  if (gradesBySemester && typeof gradesBySemester === "object" && !Array.isArray(gradesBySemester)) {
    Object.entries(gradesBySemester).forEach(([semester, list]) => {
      const sem = normalizeSemesterFromText(semester);
      const gradeList = toArrayOfObjects(list);
      if (!sem || !gradeList.length) return;
      addGradeItems(gradeList, sem);
      gradesCached = true;
    });
  }
  const grades = Array.isArray(data?.grades) ? data.grades : Array.isArray(data?.grades_all) ? data.grades_all : [];
  addGradeItems(grades);
  const hasCloudGradesPayload = Array.isArray(data?.grades) || Array.isArray(data?.grades_all) || gradesBySemester && typeof gradesBySemester === "object" && !Array.isArray(gradesBySemester) && Object.keys(gradesBySemester).length > 0;
  if (hasCloudGradesPayload) {
    replaceAuthoritativeGradeCaches(sid, mergedGrades);
    gradesCached = true;
  }
  const rankingBySemester = data?.ranking_by_semester;
  if (rankingBySemester && typeof rankingBySemester === "object" && !Array.isArray(rankingBySemester)) {
    Object.entries(rankingBySemester).forEach(([semester, payload]) => {
      const sem = normalizeSemesterFromText(semester) || (toSafeText$2(semester) || "all");
      const rankingObj = extractRankingObject(payload);
      if (!rankingObj) return;
      setCachedData(`ranking:${sid}:${sem}`, { success: true, data: rankingObj });
      if (sem === "all") {
        setCachedData(`ranking:${sid}`, { success: true, data: rankingObj });
      }
      rankingCached = true;
    });
  }
  const currentRanking = extractRankingObject(data?.ranking) || extractRankingObject(data?.ranking_all);
  if (currentRanking) {
    setCachedData(`ranking:${sid}`, { success: true, data: currentRanking });
    rankingCached = true;
  }
  const personalInfo = normalizePersonalInfoPayload(data?.personal_info || data?.profile, sid);
  if (personalInfo) {
    const payload = { success: true, data: personalInfo };
    setCachedData(`studentinfo:${sid}`, payload);
    setCachedData(`student_info:${sid}`, payload);
    personalInfoCached = true;
  }
  const scheduleMeta = data?.schedule_meta;
  if (scheduleMeta && typeof scheduleMeta === "object") {
    try {
      localStorage.setItem("hbu_schedule_meta", JSON.stringify(scheduleMeta));
      scheduleMetaApplied = true;
    } catch {
    }
  }
  const bySemester = data?.schedule?.by_semester;
  if (bySemester && typeof bySemester === "object" && !Array.isArray(bySemester)) {
    for (const [semester, rawPayload] of Object.entries(bySemester)) {
      const sem = toSafeText$2(semester);
      if (!sem) continue;
      const normalized = normalizeSchedulePayload(rawPayload, sem);
      if (!normalized) continue;
      setCachedData(`schedule:${sid}:${sem}`, normalized);
      scheduleCacheWrites += 1;
      scheduleSemesters.push(sem);
    }
  }
  return {
    gradesCached,
    rankingCached,
    personalInfoCached,
    scheduleMetaApplied,
    scheduleCacheWrites,
    scheduleSemesters: normalizeSemesterList(scheduleSemesters).map(
      (item) => toSafeText$2(typeof item === "string" ? item : item?.value)
    )
  };
};
const readRemoteCloudSync = () => {
  const snapshot = safeParseJson$1(localStorage.getItem(REMOTE_CONFIG_SNAPSHOT_KEY$1), {});
  const cfg = snapshot?.cloud_sync && typeof snapshot.cloud_sync === "object" ? snapshot.cloud_sync : {};
  const cooldownSec = clampNumber(
    cfg?.cooldown_seconds || cfg?.cooldownSeconds,
    10,
    3600,
    DEFAULT_COOLDOWN_SEC
  );
  const uploadCooldownSec = clampNumber(
    cfg?.upload_cooldown_seconds || cfg?.uploadCooldownSeconds || cooldownSec,
    120,
    3600,
    DEFAULT_UPLOAD_COOLDOWN_SEC
  );
  const downloadCooldownSec = clampNumber(
    cfg?.download_cooldown_seconds || cfg?.downloadCooldownSeconds || cooldownSec,
    10,
    3600,
    DEFAULT_DOWNLOAD_COOLDOWN_SEC
  );
  return {
    enabled: cfg?.enabled !== false,
    mode: toSafeText$2(cfg?.mode || snapshot?.cloud_sync_mode || "proxy") || "proxy",
    proxyEndpoint: normalizeProxyEndpoint(
      cfg?.proxy_endpoint || cfg?.proxyEndpoint || cfg?.endpoint || snapshot?.cloud_sync_proxy_endpoint || snapshot?.cloud_sync_endpoint
    ),
    secretRef: toSafeText$2(
      cfg?.secret_ref || cfg?.secretRef || snapshot?.cloud_sync_secret_ref || DEFAULT_SECRET_REF
    ),
    timeoutMs: clampNumber(cfg?.timeout_ms || cfg?.timeoutMs, 3e3, 45e3, DEFAULT_TIMEOUT_MS$1),
    cooldownSec,
    uploadCooldownSec,
    downloadCooldownSec
  };
};
const getCloudSyncRuntimeConfig = () => {
  const backend = useAppSettings()?.backend || {};
  const moduleParams = backend?.moduleParams || {};
  const remote = readRemoteCloudSync();
  const useRemoteConfig = backend?.useRemoteConfig !== false;
  const localEndpoint = normalizeProxyEndpoint(backend?.cloudSyncEndpoint);
  const localSecretRef = toSafeText$2(backend?.cloudSyncSecretRef);
  const defaultEndpoint = normalizeProxyEndpoint(DEFAULT_CLOUD_SYNC_ENDPOINT);
  const proxyEndpoint = useRemoteConfig ? localEndpoint || remote.proxyEndpoint || defaultEndpoint : localEndpoint || defaultEndpoint;
  const secretRef = useRemoteConfig ? localSecretRef || remote.secretRef || DEFAULT_SECRET_REF : localSecretRef || DEFAULT_SECRET_REF;
  const cooldownSec = clampNumber(
    moduleParams?.cloudSyncCooldownSec || remote.cooldownSec,
    10,
    3600,
    DEFAULT_COOLDOWN_SEC
  );
  const uploadCooldownSec = clampNumber(
    moduleParams?.cloudSyncUploadCooldownSec || remote.uploadCooldownSec || cooldownSec,
    120,
    3600,
    DEFAULT_UPLOAD_COOLDOWN_SEC
  );
  const downloadCooldownSec = clampNumber(
    moduleParams?.cloudSyncDownloadCooldownSec || remote.downloadCooldownSec || cooldownSec,
    10,
    3600,
    DEFAULT_DOWNLOAD_COOLDOWN_SEC
  );
  const timeoutMs = clampNumber(
    moduleParams?.requestTimeoutMs || remote.timeoutMs,
    3e3,
    45e3,
    DEFAULT_TIMEOUT_MS$1
  );
  const enabled = Boolean(proxyEndpoint) && (useRemoteConfig ? remote.enabled || !!localEndpoint : true);
  return {
    enabled,
    mode: "proxy",
    endpoint: proxyEndpoint,
    proxyEndpoint,
    secretRef,
    cooldownSec,
    uploadCooldownSec,
    downloadCooldownSec,
    timeoutMs,
    useRemoteConfig
  };
};
const buildAutoUploadSignature = async (studentId, latestGrades = []) => {
  const sid = toSafeText$2(studentId);
  const clientSnapshot = await buildClientSnapshot();
  const signaturePayload = {
    schema: SYNC_SCHEMA_VERSION,
    sid,
    client: clientSnapshot,
    settings: buildSettingsSnapshot(),
    notify: buildNotifySnapshot(sid),
    academic: buildAcademicSnapshot(sid, latestGrades)
  };
  const stable = stableStringify(signaturePayload);
  return {
    version: toSafeText$2(clientSnapshot?.version),
    signature: `${stable.length}:${hashText(stable)}`,
    payload: signaturePayload
  };
};
const resolveAutoUploadReason = (meta = {}, signature = {}, preferredReason = "auto-login") => {
  const currentVersion = toSafeText$2(signature?.version);
  const currentSignature = toSafeText$2(signature?.signature);
  const lastUploadVersion = toSafeText$2(meta?.lastUploadVersion);
  const lastUploadSignature = toSafeText$2(meta?.lastUploadSignature);
  if (currentVersion && lastUploadVersion && currentVersion !== lastUploadVersion) {
    return { reason: "auto-version-change", recentReason: "version-change" };
  }
  if (currentSignature && lastUploadSignature && currentSignature !== lastUploadSignature) {
    return { reason: "auto-signature-change", recentReason: "signature-change" };
  }
  return {
    reason: toSafeText$2(preferredReason) || "auto-login",
    recentReason: toSafeText$2(preferredReason) || "auto-login"
  };
};
const buildSyncPayload = async (studentId, options = {}) => {
  const sid = toSafeText$2(studentId);
  const includeCustomCourses = options?.includeCustomCourses !== false;
  const includeAcademic = options?.includeAcademic !== false;
  const includeSettings = options?.includeSettings !== false;
  const clientSnapshot = await buildClientSnapshot();
  const notifySnapshot = buildNotifySnapshot(sid);
  const settingsSnapshot = buildSettingsSnapshot();
  const bySemester = includeCustomCourses ? await fetchAllCustomCourses(sid) : {};
  const hasCustomCourseData = includeCustomCourses && hasNonEmptyCourseMap(bySemester);
  const courses = hasCustomCourseData ? pruneValue({ by_semester: bySemester }) || void 0 : void 0;
  const academic = includeAcademic ? pruneValue(buildAcademicSnapshot(sid, options?.latestGrades)) || {} : void 0;
  const deviceId = ensureDeviceId$2();
  const payload = {
    v: SYNC_SCHEMA_VERSION,
    sid,
    ts: Date.now(),
    did: deviceId
  };
  if (includeSettings) payload.settings = settingsSnapshot;
  if (courses) payload.courses = courses;
  payload.client = clientSnapshot;
  payload.notify = notifySnapshot;
  if (includeAcademic) payload.academic = academic || {};
  return {
    payload,
    hasCustomCourseData
  };
};
const primeAcademicCaches = async (studentId, seedGrades = [], options = {}) => {
  const sid = toSafeText$2(studentId);
  let grades = Array.isArray(seedGrades) ? seedGrades : [];
  if (!sid) return grades;
  const skipSemesterRankingWarmup = options?.skipSemesterRankingWarmup !== false;
  const semesters = skipSemesterRankingWarmup ? [] : await fetchSemestersForSync(sid);
  let authoritativeGrades = null;
  try {
    if (!grades.length) {
      const gradeRes = await axiosInstance.post(`${API_BASE}/v2/quick_fetch`, { student_id: sid });
      const gradeData = asRecord(gradeRes?.data);
      if (gradeData.success && Array.isArray(gradeData.data)) {
        grades = gradeData.data;
        authoritativeGrades = grades;
      }
    } else {
      authoritativeGrades = grades;
    }
  } catch {
  }
  try {
    const studentInfoRes = await axiosInstance.post(`${API_BASE}/v2/student_info`, { student_id: sid });
    if (asRecord(studentInfoRes?.data).success) {
      setCachedData(`studentinfo:${sid}`, studentInfoRes.data);
      setCachedData(`student_info:${sid}`, studentInfoRes.data);
    }
  } catch {
  }
  try {
    const scheduleRes = await axiosInstance.post(`${API_BASE}/v2/schedule/query`, { student_id: sid });
    const scheduleData = asRecord(scheduleRes?.data);
    if (scheduleData.success) {
      setCachedData(`schedule:${sid}`, scheduleRes.data);
      const sem = toSafeText$2(asRecord(scheduleData.meta).semester);
      if (sem) {
        setCachedData(`schedule:${sid}:${sem}`, scheduleRes.data);
      }
    }
  } catch {
  }
  try {
    const examRes = await axiosInstance.post(`${API_BASE}/v2/exams`, { student_id: sid, semester: "" });
    if (asRecord(examRes?.data).success) {
      setCachedData(`exams:${sid}:current`, examRes.data);
    }
  } catch {
  }
  try {
    const allRankingRes = await axiosInstance.post(`${API_BASE}/v2/ranking`, { student_id: sid, semester: "" });
    if (asRecord(allRankingRes?.data).success) {
      setCachedData(`ranking:${sid}`, allRankingRes.data);
      setCachedData(`ranking:${sid}:all`, allRankingRes.data);
    }
    if (skipSemesterRankingWarmup) {
      pushDebugLog("CloudSync", `跳过分学期排名预热 student=${sid} semesters=${semesters.length}`, "debug");
    }
  } catch {
  }
  const gradesSnapshot = authoritativeGrades != null ? replaceAuthoritativeGradeCaches(sid, authoritativeGrades) : buildGradeSnapshot(sid);
  pushDebugLog(
    "CloudSync",
    `学业缓存预热完成 student=${sid} semesters=${semesters.length} grades=${gradesSnapshot.all.length} authoritative=${authoritativeGrades != null ? 1 : 0}`,
    "debug"
  );
  return authoritativeGrades != null ? authoritativeGrades : gradesSnapshot.all || grades;
};
const CLOUD_SYNC_UPDATED_EVENT = "hbu-cloud-sync-updated";
const dispatchCloudSyncEvent = (detail = {}) => {
  if (typeof window === "undefined" || typeof window.dispatchEvent !== "function") return;
  window.dispatchEvent(new CustomEvent(CLOUD_SYNC_UPDATED_EVENT, { detail }));
};
const commitCloudSyncResult = (studentId, action, detail = {}) => {
  const sid = toSafeText$2(studentId);
  if (!sid) return null;
  const payload = detail && typeof detail === "object" ? detail : {};
  const now = Date.now();
  const result = {
    action: toSafeText$2(action),
    reason: toSafeText$2(payload.reason),
    success: !!payload.success,
    cooldown: !!payload.cooldown,
    error: toSafeText$2(payload.error),
    source: toSafeText$2(payload.source || "runtime"),
    updatedAt: now
  };
  const patch = {};
  if (result.action === "upload") {
    patch.lastUploadAt = now;
    patch.lastUploadOk = result.success;
    patch.lastUploadReason = result.reason;
    patch.lastUploadError = result.success ? "" : result.error;
    if ("includeCustomCourses" in payload) {
      patch.lastUploadIncludeCustomCourses = payload.includeCustomCourses === true;
    }
  } else if (result.action === "download") {
    patch.lastDownloadAt = now;
    patch.lastDownloadOk = result.success;
    patch.lastDownloadReason = result.reason;
    patch.lastDownloadError = result.success ? "" : result.error;
    if ("applyCustomCourses" in payload) {
      patch.lastDownloadApplyCustomCourses = payload.applyCustomCourses === true;
    }
  }
  patch.lastAction = result.action;
  patch.lastActionOk = result.success;
  patch.lastActionError = result.success ? "" : result.error;
  patch.lastActionReason = result.reason;
  patch.lastActionSource = result.source;
  patch.lastCooldown = result.cooldown;
  const saved = writeCloudSyncStatus(sid, patch);
  dispatchCloudSyncEvent({
    ...saved || {},
    ...result,
    studentId: sid,
    ...payload && typeof payload === "object" ? payload : {}
  });
  return saved;
};
const getCloudSyncLocalStatus = (studentId) => {
  const sid = toSafeText$2(studentId);
  if (!sid) return null;
  const parsed = readCloudSyncStatusInternal(sid);
  if (!parsed || typeof parsed !== "object") return null;
  return {
    ...parsed,
    studentId: sid
  };
};
const resetCloudSyncCooldownForSession = (studentId) => {
  const sid = toSafeText$2(studentId);
  if (!sid || !isValidStudentId$2(sid)) return;
  clearLastSuccessTs(sid, "upload");
  clearLastSuccessTs(sid, "download");
  clearLastSuccessTs(sid, "manual");
  pushDebugLog("CloudSync", `登录后重置冷却窗口 sid=${sid}`, "debug");
};
const getCloudSyncCooldownState = (studentId, action = "upload") => {
  const sid = toSafeText$2(studentId);
  if (!sid) {
    return { blocked: true, remainingMs: 0, cooldownMs: 0, reason: "missing-student" };
  }
  if (!isValidStudentId$2(sid)) {
    return { blocked: true, remainingMs: 0, cooldownMs: 0, reason: "invalid-student" };
  }
  const cfg = getCloudSyncRuntimeConfig();
  const normalizedAction = toSafeText$2(action).toLowerCase();
  const actionCooldownSec = normalizedAction === "download" ? Number(cfg.downloadCooldownSec || cfg.cooldownSec || 0) : Number(cfg.uploadCooldownSec || cfg.cooldownSec || 0);
  const cooldownMs = Math.max(0, actionCooldownSec * 1e3);
  const lastTs = getLastSuccessTs(sid, normalizedAction);
  const remainingMs = Math.max(0, cooldownMs - (Date.now() - lastTs));
  return {
    blocked: remainingMs > 0,
    remainingMs,
    cooldownMs
  };
};
const extractCloudData = (response) => {
  if (!response || typeof response !== "object") return null;
  const raw = response;
  if (raw?.data && typeof raw.data === "object") return raw.data;
  return raw;
};
const runCloudSyncUpload = async (input = {}) => {
  const {
    studentId,
    reason = "manual",
    force = false,
    latestGrades = [],
    includeCustomCourses = true,
    includeAcademic = true,
    includeSettings = true,
    skipCooldownRecord = false
  } = input || {};
  const sid = toSafeText$2(studentId);
  const safeReason = toSafeText$2(reason) || "manual";
  if (!sid) {
    const output = { success: false, error: "学号为空，无法上传云同步" };
    commitCloudSyncResult(studentId, "upload", { ...output, reason: safeReason });
    return output;
  }
  if (!isValidStudentId$2(sid)) {
    const output = { success: false, error: "云同步仅支持 10 位学号账号" };
    commitCloudSyncResult(sid, "upload", { ...output, reason: safeReason });
    return output;
  }
  const cfg = getCloudSyncRuntimeConfig();
  if (!cfg.enabled) {
    const output = { success: false, error: "云同步未启用或未配置服务地址" };
    commitCloudSyncResult(sid, "upload", { ...output, reason: safeReason });
    return output;
  }
  if (!force) {
    const cooldown = getCloudSyncCooldownState(sid, "upload");
    if (cooldown.blocked) {
      const output = {
        success: false,
        cooldown: true,
        remainingMs: cooldown.remainingMs,
        error: "同步冷却中"
      };
      commitCloudSyncResult(sid, "upload", { ...output, reason: safeReason });
      return output;
    }
  }
  pushDebugLog("CloudSync", `开始上传 student=${sid} reason=${safeReason}`, "info");
  try {
    pushDebugLog(
      "CloudSync",
      `上传内容 settings=${includeSettings ? 1 : 0} academic=${includeAcademic ? 1 : 0} custom=${includeCustomCourses ? 1 : 0}`,
      "debug"
    );
    const payloadResult = await buildSyncPayload(sid, {
      latestGrades,
      includeCustomCourses,
      includeAcademic,
      includeSettings
    });
    const payload = payloadResult?.payload || {};
    const customCoursesMode = includeCustomCourses && payloadResult?.hasCustomCourseData ? "replace" : "preserve";
    pushDebugLog(
      "CloudSync",
      `上传策略 custom_mode=${customCoursesMode} has_courses=${payloadResult?.hasCustomCourseData ? 1 : 0}`,
      "debug"
    );
    const body = {
      student_id: sid,
      device_id: ensureDeviceId$2(),
      reason: safeReason,
      payload,
      client_version: toSafeText$2(asRecord(payload?.client).version),
      client_time: Date.now(),
      secret_ref: cfg.secretRef,
      sections: {
        settings: includeSettings === true,
        academic: includeAcademic === true,
        custom_courses: includeCustomCourses === true
      },
      custom_courses_mode: customCoursesMode
    };
    const response = await requestCloudSync("/upload", {
      method: "POST",
      body,
      config: cfg
    });
    const uploadedAt = Date.now();
    if (!skipCooldownRecord) {
      setLastSuccessTs(sid, "upload", uploadedAt);
    }
    pushDebugLog("CloudSync", `上传成功 student=${sid}`, "info");
    const output = {
      success: true,
      response,
      uploadedAt
    };
    commitCloudSyncResult(sid, "upload", {
      ...output,
      reason: safeReason,
      includeCustomCourses,
      customCoursesMode
    });
    return output;
  } catch (error) {
    const errorText = String(error?.message || error || "云上传失败");
    commitCloudSyncResult(sid, "upload", {
      success: false,
      reason: safeReason,
      error: errorText,
      includeCustomCourses
    });
    pushDebugLog("CloudSync", `上传失败 student=${sid}`, "warn", error);
    throw error;
  }
};
const runCloudSyncDownload = async (input = {}) => {
  const {
    studentId,
    reason = "manual",
    force = false,
    applySettings = true,
    applyCustomCourses = true,
    applyAcademic = true,
    skipCooldownRecord = false
  } = input || {};
  const sid = toSafeText$2(studentId);
  const safeReason = toSafeText$2(reason) || "manual";
  if (!sid) {
    const output = { success: false, error: "学号为空，无法下载云同步" };
    commitCloudSyncResult(studentId, "download", {
      ...output,
      reason: safeReason,
      applyCustomCourses
    });
    return output;
  }
  if (!isValidStudentId$2(sid)) {
    const output = { success: false, error: "云同步仅支持 10 位学号账号" };
    commitCloudSyncResult(sid, "download", {
      ...output,
      reason: safeReason,
      applyCustomCourses
    });
    return output;
  }
  const cfg = getCloudSyncRuntimeConfig();
  if (!cfg.enabled) {
    const output = { success: false, error: "云同步未启用或未配置服务地址" };
    commitCloudSyncResult(sid, "download", {
      ...output,
      reason: safeReason,
      applyCustomCourses
    });
    return output;
  }
  if (!force) {
    const cooldown = getCloudSyncCooldownState(sid, "download");
    if (cooldown.blocked) {
      const output = {
        success: false,
        cooldown: true,
        remainingMs: cooldown.remainingMs,
        error: "同步冷却中"
      };
      commitCloudSyncResult(sid, "download", {
        ...output,
        reason: safeReason,
        applyCustomCourses
      });
      return output;
    }
  }
  pushDebugLog("CloudSync", `开始下载 student=${sid} reason=${safeReason}`, "info");
  try {
    const query = new URLSearchParams({
      student_id: sid,
      reason: safeReason,
      device_id: ensureDeviceId$2(),
      secret_ref: cfg.secretRef
    }).toString();
    const response = await requestCloudSync(`/download?${query}`, {
      method: "GET",
      config: cfg
    });
    const data = extractCloudData(response);
    if (!data) {
      const now = Date.now();
      if (!skipCooldownRecord) {
        setLastSuccessTs(sid, "download", now);
      }
      const bootstrapKey2 = makeStudentKey(CLOUD_SYNC_BOOTSTRAP_PREFIX, sid);
      if (bootstrapKey2) {
        localStorage.setItem(bootstrapKey2, String(now));
      }
      pushDebugLog("CloudSync", `下载成功但云端为空 student=${sid}`, "info");
      const output2 = {
        success: true,
        empty: true,
        response
      };
      commitCloudSyncResult(sid, "download", {
        ...output2,
        reason: safeReason,
        applyCustomCourses
      });
      return output2;
    }
    let settingResult = { app: false, ui: false, font: false };
    let customResult = { deleted: 0, added: 0, semesters: 0 };
    let academicResult = {
      gradesCached: false,
      rankingCached: false,
      personalInfoCached: false,
      scheduleMetaApplied: false,
      scheduleCacheWrites: 0,
      scheduleSemesters: []
    };
    if (applySettings) {
      settingResult = await applySettingsFromCloud(data?.settings);
    }
    if (applyCustomCourses) {
      const hasCoursesSection = Object.prototype.hasOwnProperty.call(data || {}, "courses");
      if (!hasCoursesSection) {
        pushDebugLog("CloudSync", `下载跳过自定义课表应用 student=${sid} reason=missing-courses-section`, "info");
      } else {
        const remoteCourseMap = mergeCustomCourseSemesters(
          data?.courses?.by_semester
        );
        if (Object.keys(remoteCourseMap).length === 0) {
          pushDebugLog("CloudSync", `下载跳过自定义课表应用 student=${sid} reason=empty-courses-map`, "info");
        } else {
          customResult = await replaceCustomCourses(sid, remoteCourseMap);
        }
      }
    }
    if (applyAcademic) {
      academicResult = applyAcademicFromCloud(sid, data?.academic);
    }
    const successAt = Date.now();
    if (!skipCooldownRecord) {
      setLastSuccessTs(sid, "download", successAt);
    }
    const bootstrapKey = makeStudentKey(CLOUD_SYNC_BOOTSTRAP_PREFIX, sid);
    if (bootstrapKey) {
      localStorage.setItem(bootstrapKey, String(successAt));
    }
    pushDebugLog(
      "CloudSync",
      `下载成功 student=${sid} add=${customResult.added} del=${customResult.deleted} schedule=${academicResult.scheduleCacheWrites}`,
      "info"
    );
    const output = {
      success: true,
      response,
      settingsApplied: settingResult,
      customCoursesApplied: customResult,
      academicApplied: academicResult
    };
    commitCloudSyncResult(sid, "download", {
      ...output,
      reason: safeReason,
      applyCustomCourses
    });
    return output;
  } catch (error) {
    const errorText = String(error?.message || error || "云下载失败");
    commitCloudSyncResult(sid, "download", {
      success: false,
      reason: safeReason,
      error: errorText,
      applyCustomCourses
    });
    pushDebugLog("CloudSync", `下载失败 student=${sid}`, "warn", error);
    throw error;
  }
};
const hasBootstrapDone = (studentId) => {
  const key = makeStudentKey(CLOUD_SYNC_BOOTSTRAP_PREFIX, studentId);
  if (!key) return true;
  return !!toSafeText$2(localStorage.getItem(key));
};
const autoCloudSyncInFlight = {
  studentId: "",
  promise: null
};
const runAutoCloudSyncAfterLogin = async (input = {}) => {
  const { studentId, latestGrades = [], reason = "auto-login", skipDownload = false } = input || {};
  try {
    const { shouldApplyAppStoreRestrictions: shouldApplyAppStoreRestrictions2 } = await __vitePreload(async () => {
      const { shouldApplyAppStoreRestrictions: shouldApplyAppStoreRestrictions3 } = await import("./more-modules-CfNfahoc.js").then((n) => n.z);
      return { shouldApplyAppStoreRestrictions: shouldApplyAppStoreRestrictions3 };
    }, true ? __vite__mapDeps([0,1]) : void 0, import.meta.url);
    const { isTestAccountSession: isTestAccountSession2 } = await __vitePreload(async () => {
      const { isTestAccountSession: isTestAccountSession3 } = await import("./runtime-bridge-BU9jeOXP.js").then((n) => n.a1);
      return { isTestAccountSession: isTestAccountSession3 };
    }, true ? __vite__mapDeps([1,0]) : void 0, import.meta.url);
    if (shouldApplyAppStoreRestrictions2() || isTestAccountSession2()) {
      pushDebugLog("CloudSync", "跳过自动云同步：合规收紧会话或演示会话", "info");
      return { success: false, reason: "app-store-or-demo" };
    }
  } catch {
  }
  const sid = toSafeText$2(studentId);
  if (!sid) return { success: false, reason: "missing-student" };
  if (!isValidStudentId$2(sid)) {
    pushDebugLog("CloudSync", `跳过自动云同步：非 10 位学号 sid=${sid}`, "warn");
    return { success: false, reason: "invalid-student" };
  }
  if (autoCloudSyncInFlight.promise && autoCloudSyncInFlight.studentId === sid) {
    pushDebugLog("CloudSync", `自动同步已在进行中，复用当前任务 sid=${sid}`, "debug");
    return autoCloudSyncInFlight.promise;
  }
  const task = (async () => {
    const summary = {
      download: null,
      upload: null
    };
    if (!skipDownload) {
      try {
        summary.download = await runCloudSyncDownload({
          studentId: sid,
          reason: hasBootstrapDone(sid) ? "auto-login-settings" : "auto-new-device-settings",
          force: true,
          applySettings: true,
          applyCustomCourses: false,
          applyAcademic: true,
          skipCooldownRecord: true
        });
      } catch (error) {
        summary.download = { success: false, error: String(error?.message || error) };
        pushDebugLog("CloudSync", `自动下载失败 student=${sid}`, "warn", error);
      }
    }
    try {
      const syncedGrades = await primeAcademicCaches(sid, latestGrades, {
        skipSemesterRankingWarmup: true
      });
      const uploadSignature = await buildAutoUploadSignature(sid, syncedGrades);
      const uploadReason = resolveAutoUploadReason(readAutoUploadMeta(sid), uploadSignature, reason);
      writeAutoUploadMeta(sid, {
        lastAutoResyncReason: uploadReason.recentReason,
        pendingUploadVersion: uploadSignature.version,
        pendingUploadSignature: uploadSignature.signature
      });
      summary.upload = await runCloudSyncUpload({
        studentId: sid,
        reason: uploadReason.reason,
        force: true,
        latestGrades: syncedGrades,
        includeCustomCourses: false,
        includeAcademic: true,
        includeSettings: true,
        skipCooldownRecord: true
      });
      if (summary.upload?.success) {
        writeAutoUploadMeta(sid, {
          lastUploadVersion: uploadSignature.version,
          lastUploadSignature: uploadSignature.signature,
          lastAutoResyncReason: uploadReason.recentReason,
          lastAutoUploadAt: summary.upload.uploadedAt || Date.now()
        });
      }
    } catch (error) {
      summary.upload = { success: false, error: String(error?.message || error) };
      pushDebugLog("CloudSync", `自动上传失败 student=${sid}`, "warn", error);
    }
    return {
      success: !!summary.upload?.success || !!summary.download?.success,
      ...summary
    };
  })();
  autoCloudSyncInFlight.studentId = sid;
  autoCloudSyncInFlight.promise = task;
  try {
    return await task;
  } finally {
    if (autoCloudSyncInFlight.promise === task) {
      autoCloudSyncInFlight.promise = null;
      autoCloudSyncInFlight.studentId = "";
    }
  }
};
let notifyAutoUploadListenerInstalled = false;
const installNotifyAutoUploadListener = () => {
  if (notifyAutoUploadListenerInstalled || typeof window === "undefined") return;
  window.addEventListener(NOTIFY_SNAPSHOT_EVENT, (event) => {
    const detail = event?.detail;
    const sid = toSafeText$2(
      detail?.studentId || detail?.student_id || localStorage.getItem("hbu_username") || localStorage.getItem("hbu_student_id")
    );
    if (!sid || !isValidStudentId$2(sid)) return;
    runAutoCloudSyncAfterLogin({
      studentId: sid,
      reason: "auto-signature-change",
      skipDownload: true
    }).catch((error) => {
      pushDebugLog("CloudSync", `通知快照触发自动上传失败 student=${sid}`, "warn", error);
    });
  });
  notifyAutoUploadListenerInstalled = true;
};
installNotifyAutoUploadListener();
const CLOUD_SYNC_DEVICE_ID_KEY$1 = "hbu_cloud_sync_device_id";
const USAGE_UPLOAD_LAST_SUCCESS_PREFIX = "hbu_usage_upload_last_success:";
const DEFAULT_TIMEOUT_MS = 12e3;
const DEFAULT_UPLOAD_COOLDOWN_MS = 15 * 60 * 1e3;
const CHALLENGE_SKEW_MS = 3e3;
const CHALLENGE_FALLBACK_TTL_MS = 60 * 1e3;
const BATCH_LIMIT = 200;
const STUDENT_ID_RE$1 = /^\d{10}$/;
const challengeState = {
  token: "",
  expiresAt: 0,
  endpoint: ""
};
let uploadTimer = null;
let uploadInFlight = null;
const toSafeText$1 = (value) => String(value || "").trim();
const isValidStudentId$1 = (value) => STUDENT_ID_RE$1.test(toSafeText$1(value));
const safeParseJson = (raw, fallback = null) => {
  if (!raw) return fallback;
  try {
    return JSON.parse(raw);
  } catch {
    return fallback;
  }
};
const ensureDeviceId$1 = () => {
  let id = toSafeText$1(localStorage.getItem(CLOUD_SYNC_DEVICE_ID_KEY$1));
  if (id) return id;
  try {
    id = (crypto?.randomUUID?.() || "").trim();
  } catch {
    id = "";
  }
  if (!id) {
    id = `device-${Date.now()}-${Math.random().toString(36).slice(2, 10)}`;
  }
  localStorage.setItem(CLOUD_SYNC_DEVICE_ID_KEY$1, id);
  return id;
};
const normalizeUsageStatsEndpoint = (value) => {
  const text = toSafeText$1(value);
  if (!text) return "";
  const withProtocol = /^https?:\/\//i.test(text) ? text : `https://${text}`;
  const normalized = withProtocol.replace(/\/+$/, "");
  if (/\/api\/usage-stats$/i.test(normalized)) return normalized;
  if (/\/api\/cloud-sync$/i.test(normalized)) {
    return normalized.replace(/\/api\/cloud-sync$/i, "/api/usage-stats");
  }
  return `${normalized}/api/usage-stats`;
};
const getUsageStatsRuntimeConfig = () => {
  const cloudCfg = getCloudSyncRuntimeConfig();
  const endpoint = normalizeUsageStatsEndpoint(cloudCfg.proxyEndpoint || cloudCfg.endpoint);
  return {
    enabled: Boolean(endpoint) && cloudCfg.enabled !== false,
    endpoint,
    secretRef: toSafeText$1(cloudCfg.secretRef) || "kv1-main",
    timeoutMs: Math.max(3e3, Number(cloudCfg.timeoutMs || DEFAULT_TIMEOUT_MS))
  };
};
const getLastUploadTs = (studentId) => {
  const sid = toSafeText$1(studentId);
  if (!sid) return 0;
  return Number(localStorage.getItem(`${USAGE_UPLOAD_LAST_SUCCESS_PREFIX}${sid}`) || 0) || 0;
};
const setLastUploadTs = (studentId, ts = Date.now()) => {
  const sid = toSafeText$1(studentId);
  if (!sid) return;
  localStorage.setItem(`${USAGE_UPLOAD_LAST_SUCCESS_PREFIX}${sid}`, String(ts));
};
const canReuseChallenge = (config) => {
  const endpoint = toSafeText$1(config?.endpoint);
  if (!endpoint) return false;
  if (challengeState.endpoint !== endpoint) return false;
  if (!challengeState.token) return false;
  return challengeState.expiresAt > Date.now() + CHALLENGE_SKEW_MS;
};
const loadUsageStatsChallenge = async (config, force = false) => {
  if (!force && canReuseChallenge(config)) {
    return challengeState.token;
  }
  const secretRef = toSafeText$1(config?.secretRef) || "kv1-main";
  const query = new URLSearchParams({ secret_ref: secretRef }).toString();
  const url = `${config.endpoint.replace(/\/+$/, "")}/ping?${query}`;
  const controller = typeof AbortController !== "undefined" ? new AbortController() : null;
  const timer = window.setTimeout(() => controller?.abort?.(), config.timeoutMs);
  try {
    const response = await fetch(url, {
      method: "GET",
      headers: { Accept: "application/json" },
      signal: controller?.signal
    });
    const parsed = safeParseJson(await response.text(), null);
    if (!response.ok) {
      throw new Error(toSafeText$1(parsed?.error) || `usage-stats ping 失败 (${response.status})`);
    }
    const token = toSafeText$1(parsed?.challenge);
    if (!token) throw new Error("usage-stats 鉴权挑战获取失败");
    const ttlSec = Number(parsed?.challenge_expires_in || 0);
    const ttlMs = Number.isFinite(ttlSec) && ttlSec > 0 ? Math.max(1e4, Math.round(ttlSec * 1e3)) : CHALLENGE_FALLBACK_TTL_MS;
    challengeState.token = token;
    challengeState.endpoint = config.endpoint;
    challengeState.expiresAt = Date.now() + ttlMs;
    return token;
  } finally {
    window.clearTimeout(timer);
  }
};
const requestUsageStats = async (path, { method = "GET", body, config, skipChallenge = false } = {}) => {
  const endpoint = toSafeText$1(config?.endpoint);
  if (!endpoint) throw new Error("usage-stats 服务地址未配置");
  const url = `${endpoint.replace(/\/+$/, "")}${path}`;
  const challengeToken = skipChallenge ? "" : await loadUsageStatsChallenge(config);
  const controller = typeof AbortController !== "undefined" ? new AbortController() : null;
  const timer = window.setTimeout(() => controller?.abort?.(), config.timeoutMs);
  try {
    const headers = { Accept: "application/json" };
    if (body !== void 0) headers["Content-Type"] = "application/json";
    if (challengeToken) headers["x-usage-stats-challenge"] = challengeToken;
    const response = await fetch(url, {
      method,
      headers,
      body: body === void 0 ? void 0 : JSON.stringify(body),
      signal: controller?.signal
    });
    const parsed = safeParseJson(await response.text(), null);
    if (!response.ok) {
      throw new Error(toSafeText$1(parsed?.error) || `usage-stats 请求失败 (${response.status})`);
    }
    return parsed;
  } finally {
    window.clearTimeout(timer);
  }
};
const collectPendingBatch = async (studentId, deviceId) => {
  if (isTauriRuntime()) {
    return await invokeNative("usage_stats_list_pending_upload", {
      studentId,
      deviceId,
      limit: BATCH_LIMIT
    });
  }
  const web = getWebUsagePendingQueues();
  return {
    events: (web.events || []).slice(0, BATCH_LIMIT),
    sessions: (web.sessions || []).slice(0, BATCH_LIMIT),
    device_profile: web.deviceProfile || null
  };
};
const markBatchUploaded = async (studentId, eventIds, sessionIds) => {
  const uploadedAt = Date.now();
  if (isTauriRuntime()) {
    await invokeNative("usage_stats_mark_uploaded", {
      eventIds,
      sessionIds,
      uploadedAt
    });
    return;
  }
  clearWebUsagePendingQueues({ eventIds, sessionIds });
};
const runUsageStatsUpload = async ({
  studentId,
  reason = "manual",
  force = false
} = {}) => {
  if (shouldApplyAppStoreRestrictions() || isTestAccountSession()) {
    return { success: false, error: "usage 上传已在当前合规收紧会话/演示会话禁用" };
  }
  const sid = toSafeText$1(studentId);
  if (!isValidStudentId$1(sid)) {
    return { success: false, error: "学号无效，跳过 usage 上传" };
  }
  const cfg = getUsageStatsRuntimeConfig();
  if (!cfg.enabled) {
    return { success: false, error: "usage-stats 未启用" };
  }
  if (!force) {
    const lastTs = getLastUploadTs(sid);
    const remain = DEFAULT_UPLOAD_COOLDOWN_MS - (Date.now() - lastTs);
    if (lastTs > 0 && remain > 0) {
      return { success: false, cooldown: true, remainingMs: remain, error: "usage 上传冷却中" };
    }
  }
  const deviceId = ensureDeviceId$1();
  const batch = await collectPendingBatch(sid, deviceId);
  const events = Array.isArray(batch?.events) ? batch.events : [];
  const sessions = Array.isArray(batch?.sessions) ? batch.sessions : [];
  if (!events.length && !sessions.length && !batch?.device_profile) {
    return { success: true, skipped: true, accepted: 0 };
  }
  pushDebugLog("UsageStats", `开始上传 student=${sid} reason=${reason} events=${events.length}`, "info");
  try {
    const response = await requestUsageStats("/upload", {
      method: "POST",
      config: cfg,
      body: {
        student_id: sid,
        device_id: deviceId,
        secret_ref: cfg.secretRef,
        client_time: Date.now(),
        events,
        sessions,
        device_profile: batch?.device_profile || null
      }
    });
    const acceptedEventIds = events.map((item) => toSafeText$1(item?.event_id)).filter(Boolean);
    const acceptedSessionIds = sessions.map((item) => toSafeText$1(item?.session_id)).filter(Boolean);
    await markBatchUploaded(sid, acceptedEventIds, acceptedSessionIds);
    setLastUploadTs(sid);
    pushDebugLog("UsageStats", `上传成功 student=${sid} accepted=${response?.accepted ?? acceptedEventIds.length}`, "info");
    return { success: true, response };
  } catch (error) {
    pushDebugLog("UsageStats", `上传失败 student=${sid}`, "warn", error);
    return { success: false, error: String(error?.message || error || "usage 上传失败") };
  }
};
const fetchRemotePersonalUsageSummary = async (studentId) => {
  const sid = toSafeText$1(studentId);
  if (!isValidStudentId$1(sid)) return null;
  const cfg = getUsageStatsRuntimeConfig();
  if (!cfg.enabled) return null;
  try {
    const query = new URLSearchParams({ student_id: sid, secret_ref: cfg.secretRef }).toString();
    return await requestUsageStats(`/personal?${query}`, { method: "GET", config: cfg });
  } catch {
    return null;
  }
};
const scheduleUsageUpload = ({ studentId, reason = "scheduled", force = false } = {}) => {
  const sid = toSafeText$1(studentId);
  if (!isValidStudentId$1(sid)) return;
  if (uploadInFlight) return;
  uploadInFlight = runUsageStatsUpload({ studentId: sid, reason, force }).catch(() => ({ success: false })).finally(() => {
    uploadInFlight = null;
  });
};
const startUsageUploadScheduler = (getStudentId) => {
  if (uploadTimer) return;
  const tick = () => {
    const sid = toSafeText$1(typeof getStudentId === "function" ? getStudentId() : getStudentId);
    if (sid) scheduleUsageUpload({ studentId: sid, reason: "interval" });
  };
  uploadTimer = window.setInterval(tick, DEFAULT_UPLOAD_COOLDOWN_MS);
};
const stopUsageUploadScheduler = () => {
  if (!uploadTimer) return;
  window.clearInterval(uploadTimer);
  uploadTimer = null;
};
const CLOUD_SYNC_DEVICE_ID_KEY = "hbu_cloud_sync_device_id";
const WEB_QUEUE_KEY = "hbu_usage_events_queue_v1";
const WEB_SESSIONS_KEY = "hbu_usage_sessions_queue_v1";
const DAILY_EVENT_LIMIT = 5e3;
const STUDENT_ID_RE = /^\d{10}$/;
const toSafeText = (value) => String(value || "").trim();
const isValidStudentId = (value) => STUDENT_ID_RE.test(toSafeText(value));
const randomHex = (length) => {
  if (typeof globalThis.crypto !== "undefined" && typeof crypto.getRandomValues === "function") {
    const bytes = new Uint8Array(Math.ceil(length / 2));
    crypto.getRandomValues(bytes);
    return Array.from(bytes, (byte) => byte.toString(16).padStart(2, "0")).join("").slice(0, length);
  }
  return Math.random().toString(36).slice(2, 2 + length);
};
let clientMetaCache = null;
let clientMetaCacheAt = 0;
let activeView = { id: "", startedAt: 0 };
let activeModule = { id: "", loadMode: "", launchMode: "", startedAt: 0 };
let activeSession = { sessionId: "", startedAt: 0 };
let initialized = false;
let currentStudentId = "anonymous";
let dailyEventCount = 0;
let dailyEventDate = "";
const ensureDeviceId = () => {
  let id = toSafeText(localStorage.getItem(CLOUD_SYNC_DEVICE_ID_KEY));
  if (id) return id;
  try {
    id = (crypto?.randomUUID?.() || "").trim();
  } catch {
    id = "";
  }
  if (!id) {
    id = `device-${Date.now()}-${randomHex(8)}`;
  }
  localStorage.setItem(CLOUD_SYNC_DEVICE_ID_KEY, id);
  return id;
};
const createEventId = () => {
  try {
    const id = crypto?.randomUUID?.();
    if (id) return id;
  } catch {
  }
  return `evt-${Date.now()}-${randomHex(8)}`;
};
const createSessionId = () => {
  try {
    const id = crypto?.randomUUID?.();
    if (id) return id;
  } catch {
  }
  return `sess-${Date.now()}-${randomHex(8)}`;
};
const todayKey = () => {
  const now = /* @__PURE__ */ new Date();
  const y = now.getFullYear();
  const m = String(now.getMonth() + 1).padStart(2, "0");
  const d = String(now.getDate()).padStart(2, "0");
  return `${y}-${m}-${d}`;
};
const bumpDailyCounter = () => {
  const today = todayKey();
  if (dailyEventDate !== today) {
    dailyEventDate = today;
    dailyEventCount = 0;
  }
  dailyEventCount += 1;
  return dailyEventCount <= DAILY_EVENT_LIMIT;
};
const readWebQueue = (key) => {
  try {
    const raw = localStorage.getItem(key);
    const parsed = raw ? JSON.parse(raw) : [];
    return Array.isArray(parsed) ? parsed : [];
  } catch {
    return [];
  }
};
const writeWebQueue = (key, items) => {
  try {
    localStorage.setItem(key, JSON.stringify(items.slice(-2e3)));
  } catch {
  }
};
const getClientMeta = async () => {
  const now = Date.now();
  if (clientMetaCache && now - clientMetaCacheAt < 6e4) {
    return clientMetaCache;
  }
  let version = "";
  try {
    version = toSafeText(await getCurrentVersion());
  } catch {
    version = "";
  }
  const runtime = detectRuntime();
  const platform = typeof navigator !== "undefined" ? toSafeText(navigator.platform || navigator.userAgent || "") : "";
  clientMetaCache = {
    app_version: version,
    runtime,
    platform,
    os_version: typeof navigator !== "undefined" ? toSafeText(navigator.userAgent || "") : "",
    arch: "",
    locale: typeof navigator !== "undefined" ? toSafeText(navigator.language || "") : ""
  };
  clientMetaCacheAt = now;
  return clientMetaCache;
};
const resolveStudentId = () => {
  const sid = toSafeText(currentStudentId);
  if (isValidStudentId(sid)) return sid;
  const stored = toSafeText(localStorage.getItem("hbu_username"));
  if (isValidStudentId(stored)) return stored;
  return "anonymous";
};
const persistEvent = async (event) => {
  if (!bumpDailyCounter()) return false;
  if (isTauriRuntime()) {
    await invokeNative("usage_stats_record_event", { event });
    return true;
  }
  const queue2 = readWebQueue(WEB_QUEUE_KEY);
  queue2.push(event);
  writeWebQueue(WEB_QUEUE_KEY, queue2);
  return true;
};
const persistSession = async (session) => {
  if (isTauriRuntime()) {
    await invokeNative("usage_stats_end_session", { session });
    return true;
  }
  const queue2 = readWebQueue(WEB_SESSIONS_KEY);
  queue2.push(session);
  writeWebQueue(WEB_SESSIONS_KEY, queue2);
  return true;
};
const upsertDeviceProfile = async () => {
  const meta = await getClientMeta();
  const profile = {
    device_id: ensureDeviceId(),
    student_id: resolveStudentId(),
    app_version: meta.app_version,
    runtime: meta.runtime,
    platform: meta.platform,
    os_version: meta.os_version,
    arch: meta.arch,
    locale: meta.locale,
    updated_at: Date.now()
  };
  if (isTauriRuntime()) {
    await invokeNative("usage_stats_upsert_device_profile", { profile });
    return profile;
  }
  try {
    localStorage.setItem("hbu_usage_device_profile_v1", JSON.stringify(profile));
  } catch {
  }
  return profile;
};
const buildEvent = async ({
  eventType,
  targetKind,
  targetId,
  loadMode = "native",
  launchMode = "",
  durationMs = 0,
  extra = {}
}) => {
  const meta = await getClientMeta();
  return {
    event_id: createEventId(),
    student_id: resolveStudentId(),
    device_id: ensureDeviceId(),
    event_type: eventType,
    target_kind: targetKind,
    target_id: toSafeText(targetId) || "unknown",
    load_mode: toSafeText(loadMode) || "native",
    launch_mode: toSafeText(launchMode),
    duration_ms: Math.max(0, Number(durationMs) || 0),
    app_version: meta.app_version,
    runtime: meta.runtime,
    platform: meta.platform,
    extra_json: JSON.stringify(extra || {}),
    occurred_at: Date.now()
  };
};
const maybeScheduleUpload = (reason = "event") => {
  const sid = resolveStudentId();
  if (!isValidStudentId(sid)) return;
  scheduleUsageUpload({ studentId: sid, reason });
};
const setUsageTrackingStudentId = (studentId) => {
  currentStudentId = toSafeText(studentId) || "anonymous";
};
const trackViewNavigation = async (fromView, toView) => {
  if (!initialized) return;
  const from = toSafeText(fromView);
  const to = toSafeText(toView);
  const now = Date.now();
  if (from && from !== to && activeView.id === from && activeView.startedAt > 0) {
    const durationMs = now - activeView.startedAt;
    const closeEvent = await buildEvent({
      eventType: "view_close",
      targetKind: "view",
      targetId: from,
      loadMode: "native",
      durationMs,
      extra: { next_view: to }
    });
    await persistEvent(closeEvent);
  }
  if (from === "more_module_host" && to !== "more_module_host" && activeModule.startedAt > 0) {
    const durationMs = now - activeModule.startedAt;
    const moduleClose = await buildEvent({
      eventType: "module_close",
      targetKind: "module",
      targetId: activeModule.id,
      loadMode: activeModule.loadMode,
      launchMode: activeModule.launchMode,
      durationMs,
      extra: { next_view: to }
    });
    await persistEvent(moduleClose);
    activeModule = { id: "", loadMode: "", launchMode: "", startedAt: 0 };
  }
  if (to && to !== from) {
    const openEvent = await buildEvent({
      eventType: "view_open",
      targetKind: "view",
      targetId: to,
      loadMode: "native",
      extra: { previous_view: from }
    });
    await persistEvent(openEvent);
    activeView = { id: to, startedAt: now };
    maybeScheduleUpload("view-nav");
  }
};
const trackModuleOpen = async ({
  moduleId,
  loadMode = "",
  launchMode = "",
  moduleVersion = "",
  channel = ""
} = {}) => {
  if (!initialized) return;
  const id = toSafeText(moduleId);
  if (!id) return;
  const now = Date.now();
  const event = await buildEvent({
    eventType: "module_open",
    targetKind: "module",
    targetId: id,
    loadMode: loadMode || "remote-site",
    launchMode,
    extra: { module_version: moduleVersion, channel }
  });
  await persistEvent(event);
  activeModule = {
    id,
    loadMode: loadMode || "remote-site",
    launchMode: toSafeText(launchMode),
    startedAt: now
  };
  maybeScheduleUpload("module-open");
};
const trackAppForeground = async () => {
  if (!initialized) return;
  const now = Date.now();
  activeSession = { sessionId: createSessionId(), startedAt: now };
  const event = await buildEvent({
    eventType: "app_foreground",
    targetKind: "app",
    targetId: "mini-hbut",
    loadMode: "native"
  });
  await persistEvent(event);
  await upsertDeviceProfile();
};
const trackAppBackground = async () => {
  if (!initialized) return;
  const now = Date.now();
  if (activeSession.startedAt > 0 && activeSession.sessionId) {
    const meta = await getClientMeta();
    const durationMs = now - activeSession.startedAt;
    await persistSession({
      session_id: activeSession.sessionId,
      student_id: resolveStudentId(),
      device_id: ensureDeviceId(),
      started_at: activeSession.startedAt,
      ended_at: now,
      duration_ms: durationMs,
      app_version: meta.app_version,
      runtime: meta.runtime,
      platform: meta.platform
    });
    const event = await buildEvent({
      eventType: "app_background",
      targetKind: "app",
      targetId: "mini-hbut",
      loadMode: "native",
      durationMs
    });
    await persistEvent(event);
    activeSession = { sessionId: "", startedAt: 0 };
    maybeScheduleUpload("app-background");
  }
};
const initUsageTracker = ({ studentId = "" } = {}) => {
  if (studentId) setUsageTrackingStudentId(studentId);
  if (initialized) return;
  initialized = true;
  const onVisibility = () => {
    if (document.hidden) {
      void trackAppBackground();
      return;
    }
    void trackAppForeground();
  };
  document.addEventListener("visibilitychange", onVisibility);
  if (!document.hidden) {
    void trackAppForeground();
  }
};
const getWebUsagePendingQueues = () => ({
  events: readWebQueue(WEB_QUEUE_KEY),
  sessions: readWebQueue(WEB_SESSIONS_KEY),
  deviceProfile: (() => {
    try {
      const raw = localStorage.getItem("hbu_usage_device_profile_v1");
      return raw ? JSON.parse(raw) : null;
    } catch {
      return null;
    }
  })()
});
const clearWebUsagePendingQueues = ({ eventIds = [], sessionIds = [] } = {}) => {
  if (eventIds.length) {
    const idSet = new Set(eventIds);
    writeWebQueue(WEB_QUEUE_KEY, readWebQueue(WEB_QUEUE_KEY).filter((item) => !idSet.has(item?.event_id)));
  }
  if (sessionIds.length) {
    const idSet = new Set(sessionIds);
    writeWebQueue(WEB_SESSIONS_KEY, readWebQueue(WEB_SESSIONS_KEY).filter((item) => !idSet.has(item?.session_id)));
  }
};
const fetchPersonalUsageSummary = async (studentId) => {
  const sid = toSafeText(studentId);
  if (!sid || !isTauriRuntime()) return null;
  try {
    return await invokeNative("usage_stats_get_personal_summary", { studentId: sid });
  } catch {
    return null;
  }
};
const createNavigationCoordinator = (runtime) => {
  const { state: state2 } = runtime;
  const isIOSLike$1 = isIOSLike();
  const isDesktopLike$1 = isDesktopLike();
  const hasTauri2 = isTauriRuntime();
  const prefetchViewComponent = (view) => {
    const name = normalizeViewName(view);
    if (!Object.prototype.hasOwnProperty.call(VIEW_PREFETCHERS, name)) return;
    const loader = VIEW_PREFETCHERS[name];
    if (typeof loader === "function") {
      void loader();
    }
  };
  const buildModuleHostSession = (payload = {}) => {
    const raw = payload && typeof payload === "object" ? payload : {};
    const resolved = resolveModuleHostPreviewSource(raw);
    const rawPreviewUrl = String(raw.preview_url || raw.previewUrl || "").trim();
    const rawPreviewMode = String(raw.preview_mode || raw.previewMode || "").trim();
    const sanitizedPreviewUrl = !canUseLocalModuleBridgePreview() && isLocalModuleBridgePreviewUrl(resolved.resolvedPreviewUrl) ? "" : String(resolved.resolvedPreviewUrl || "").trim();
    const sanitizedLocalPreviewUrl = !canUseLocalModuleBridgePreview() && isLocalModuleBridgePreviewUrl(String(raw.local_preview_url || raw.localPreviewUrl || resolved.localPreviewUrl || "").trim()) ? "" : String(raw.local_preview_url || raw.localPreviewUrl || resolved.localPreviewUrl || "").trim();
    const bridgeBlocked = !canUseLocalModuleBridgePreview() && (isLocalModuleBridgePreviewUrl(rawPreviewUrl) || rawPreviewMode === "tauri-local");
    const normalizedPreviewMode = String(resolved.sourceKind || "").trim() || (bridgeBlocked ? "" : rawPreviewMode);
    const normalizedInvalidReason = String(
      raw.invalid_reason || raw.invalidReason || (!sanitizedPreviewUrl && bridgeBlocked ? "tauri-bridge-blocked" : "")
    ).trim();
    return {
      module_id: String(raw.module_id || raw.moduleId || "").trim(),
      module_name: String(raw.module_name || raw.moduleName || "").trim(),
      preview_url: sanitizedPreviewUrl,
      version: String(raw.version || "").trim(),
      min_compatible_version: String(raw.min_compatible_version || raw.minCompatibleVersion || "").trim(),
      channel: String(raw.channel || "main").trim() || "main",
      local_ready: !!sanitizedPreviewUrl && raw.local_ready !== false,
      source: String(raw.source || "").trim(),
      preview_mode: normalizedPreviewMode,
      invalid_reason: normalizedInvalidReason,
      open_url: String(raw.open_url || raw.openUrl || resolved.openUrl || "").trim(),
      package_url: String(raw.package_url || raw.packageUrl || resolved.packageUrl || "").trim(),
      package_urls: Array.isArray(raw.package_urls) ? raw.package_urls : Array.isArray(raw.packageUrls) ? raw.packageUrls : resolved.packageUrls,
      entry_path: String(raw.entry_path || raw.entryPath || resolved.entryPath || "").trim(),
      resolved_entry_path: String(
        raw.resolved_entry_path || raw.resolvedEntryPath || resolved.resolvedEntryPath || ""
      ).trim(),
      local_preview_url: sanitizedLocalPreviewUrl,
      site_root_path: String(raw.site_root_path || raw.siteRootPath || resolved.siteRootPath || "").trim(),
      bundle_zip_path: String(
        raw.bundle_zip_path || raw.bundleZipPath || resolved.bundleZipPath || ""
      ).trim(),
      cache_dir: String(raw.cache_dir || "").trim(),
      bundle_path: String(raw.bundle_path || "").trim(),
      manifest_url: String(raw.manifest_url || raw.manifestUrl || resolved.manifestUrl || "").trim(),
      manifest_checked_at: String(raw.manifest_checked_at || raw.manifestCheckedAt || "").trim()
    };
  };
  const writeModuleHostSessionStorage = (payload) => {
    try {
      localStorage.setItem(MODULE_HOST_SESSION_KEY, JSON.stringify(payload));
    } catch {
    }
  };
  const readModuleHostSession = () => {
    try {
      const raw = localStorage.getItem(MODULE_HOST_SESSION_KEY);
      if (!raw) return buildModuleHostSession();
      return buildModuleHostSession(JSON.parse(raw));
    } catch {
      return buildModuleHostSession();
    }
  };
  const persistModuleHostSession = (payload) => {
    const normalized = buildModuleHostSession(payload);
    writeModuleHostSessionStorage(normalized);
    return normalized;
  };
  const repairModuleHostSession = async (payload) => {
    try {
      const repaired = await normalizeModuleHostSessionPayload(payload || {});
      const normalized = buildModuleHostSession(repaired);
      writeModuleHostSessionStorage(normalized);
      return normalized;
    } catch {
      const fallback = buildModuleHostSession(payload);
      writeModuleHostSessionStorage(fallback);
      return fallback;
    }
  };
  const getAppShellScrollTop = () => {
    const shell = state2.appShellRef.value;
    if (shell && typeof shell.scrollTop === "number" && Number.isFinite(shell.scrollTop)) {
      return Math.max(0, shell.scrollTop);
    }
    const windowTop = Number(
      window.scrollY || document.documentElement.scrollTop || document.body.scrollTop || 0
    );
    return Number.isFinite(windowTop) ? Math.max(0, windowTop) : 0;
  };
  const forceScrollTop = () => {
    try {
      window.scrollTo(0, 0);
      document.documentElement.scrollTop = 0;
      document.body.scrollTop = 0;
      if (state2.appShellRef.value) {
        state2.appShellRef.value.scrollTop = 0;
      }
    } catch {
    }
  };
  const rememberHomeScrollPosition = () => {
    if (state2.currentView.value !== "home") return;
    const top = getAppShellScrollTop();
    state2.homeScrollSnapshot.value = top;
    try {
      sessionStorage.setItem(HOME_SCROLL_STORAGE_KEY, String(top));
    } catch {
    }
  };
  const readStoredHomeScrollTop = () => {
    const mem = Math.max(0, Number(state2.homeScrollSnapshot.value || 0));
    if (mem > 0) return mem;
    try {
      const stored = Number(sessionStorage.getItem(HOME_SCROLL_STORAGE_KEY) || 0);
      return Number.isFinite(stored) ? Math.max(0, stored) : 0;
    } catch {
      return 0;
    }
  };
  const restoreHomeScrollPosition = () => {
    const targetTop = readStoredHomeScrollTop();
    if (targetTop <= 0) return;
    state2.homeScrollRestoring.value = true;
    let tries = 0;
    const maxTries = 24;
    let finished = false;
    const finishRestoring = () => {
      if (finished) return;
      finished = true;
      state2.homeScrollRestoring.value = false;
      cleanup();
    };
    const onUserScrollInput = () => {
      if (finished) return;
      finishRestoring();
    };
    const cleanup = () => {
      window.removeEventListener("wheel", onUserScrollInput, { capture: true });
      window.removeEventListener("touchstart", onUserScrollInput, { capture: true });
    };
    window.addEventListener("wheel", onUserScrollInput, { passive: true, capture: true });
    window.addEventListener("touchstart", onUserScrollInput, { passive: true, capture: true });
    const applyScroll = () => {
      if (finished) return;
      if (state2.currentView.value !== "home") {
        finishRestoring();
        return;
      }
      try {
        if (state2.appShellRef.value) {
          state2.appShellRef.value.scrollTop = targetTop;
        }
        window.scrollTo(0, targetTop);
        document.documentElement.scrollTop = targetTop;
        document.body.scrollTop = targetTop;
      } catch {
      }
      const current = getAppShellScrollTop();
      if (Math.abs(current - targetTop) <= 4) {
        finishRestoring();
        return;
      }
      tries += 1;
      if (tries < maxTries) {
        requestAnimationFrame(applyScroll);
      }
    };
    nextTick(() => {
      applyScroll();
      requestAnimationFrame(applyScroll);
      [50, 120, 200, 320, 480, 700].forEach((ms) => {
        window.setTimeout(applyScroll, ms);
      });
      window.setTimeout(finishRestoring, 900);
    });
  };
  const resolveHash = (sid, view) => {
    if (!sid) return "#/";
    if (!view || view === "home") return `#/${sid}`;
    return `#/${sid}/${view}`;
  };
  const replaceHistorySnapshot = (view = state2.currentView.value) => {
    const sid = state2.studentId.value || "";
    const snap = {
      __hbu: true,
      sid,
      view,
      tab: state2.activeTab.value,
      module: view === "more_module_host" ? String(state2.moduleHostSession.value?.module_id || "").trim() : state2.currentModule.value
    };
    window.history.replaceState(snap, "", resolveHash(sid, view));
  };
  const pushHistorySnapshot = (view = state2.currentView.value) => {
    const sid = state2.studentId.value || "";
    const snap = {
      __hbu: true,
      sid,
      view,
      tab: state2.activeTab.value,
      module: view === "more_module_host" ? String(state2.moduleHostSession.value?.module_id || "").trim() : state2.currentModule.value
    };
    window.history.pushState(snap, "", resolveHash(sid, view));
  };
  const applyViewState = (view) => {
    state2.currentView.value = view;
    if (MAIN_TABS.includes(view)) {
      state2.activeTab.value = view;
      state2.currentModule.value = "";
      return;
    }
    if (ME_SUB_VIEWS.includes(view)) {
      state2.activeTab.value = "me";
    }
    state2.currentModule.value = view === "more_module_host" ? String(state2.moduleHostSession.value?.module_id || "more_module_host").trim() : view;
  };
  const readWindowRouteSnapshot = () => {
    if (typeof window === "undefined") return null;
    const historyState = window.history?.state;
    if (historyState && historyState.__hbu) {
      return {
        sid: String(historyState.sid || "").trim(),
        view: normalizeViewName(historyState.view || historyState.module || historyState.tab),
        tab: String(historyState.tab || "").trim(),
        module: String(historyState.module || "").trim()
      };
    }
    const hash = window.location.hash || "#/";
    const match = hash.match(/^#\/(\d{10})(?:\/(\w+))?$/);
    if (!match) return null;
    return {
      sid: match[1],
      view: normalizeViewName(match[2] || "home"),
      tab: "",
      module: ""
    };
  };
  const resolveAccessFallbackView = (view = "home") => {
    const normalized = normalizeViewName(view);
    if (normalized === "home") return "home";
    if (isProtectedView(normalized)) return "home";
    return normalized;
  };
  const queueProtectedViewPrompt = (view, { push = true } = {}) => {
    state2.pendingProtectedView.value = { view: normalizeViewName(view), push: push !== false };
    state2.dailyAccessInput.value = "";
    state2.dailyAccessError.value = "";
    state2.showDailyAccessDialog.value = true;
  };
  const ensureProtectedViewAccess = (view, { push = true, redirectToFallback = false, fallbackView = state2.currentView.value } = {}) => {
    const normalized = normalizeViewName(view);
    if (!isProtectedView(normalized) || hasDailyAccessGrant()) return true;
    if (redirectToFallback) {
      const fallback = resolveAccessFallbackView(fallbackView);
      state2.mutable.pendingScrollToTopOnViewChange = false;
      applyViewState(fallback);
      replaceHistorySnapshot(fallback);
    }
    queueProtectedViewPrompt(normalized, { push });
    return false;
  };
  const ensureLoginRequiredViewAccess = (view) => {
    const normalized = normalizeViewName(view);
    if (!isLoginRequiredView(normalized) || state2.isLoggedIn.value) return true;
    runtime.auth.handleRequireLogin();
    return false;
  };
  const goToViewInternal = (view, { push = true, restoreScroll = false, scrollToTop, direction } = {}) => {
    const normalized = normalizeViewName(view);
    const fromView = state2.currentView.value;
    state2.navDirection.value = direction || (push ? "forward" : "none");
    if (fromView === "home" && normalized !== "home") {
      rememberHomeScrollPosition();
      state2.homeScrollRestoring.value = false;
    }
    const returningHome = normalized === "home" && fromView !== "home";
    const shouldRestoreHomeScroll = normalized === "home" && scrollToTop !== true && (restoreScroll || returningHome);
    state2.mutable.pendingScrollToTopOnViewChange = !shouldRestoreHomeScroll;
    applyViewState(normalized);
    if (push) {
      pushHistorySnapshot(normalized);
    } else {
      replaceHistorySnapshot(normalized);
    }
    if (shouldRestoreHomeScroll) {
      runtime.lifecycle.recoverViewportAfterTransition({ scrollToTop: false, blurActive: true });
      restoreHomeScrollPosition();
    } else {
      runtime.lifecycle.recoverViewportAfterTransition({ scrollToTop: true, blurActive: true });
    }
    void trackViewNavigation(fromView, normalized);
  };
  const goToView = (view, { push = true, restoreScroll = false, direction } = {}) => {
    const normalized = normalizeViewName(view);
    if (!isViewAllowed(normalized)) {
      showToast("当前版本不可用该功能");
      if (normalized !== "home" && isViewAllowed("home")) {
        goToViewInternal("home", { push: false, restoreScroll: true });
      }
      return false;
    }
    const access = canOpenModule(
      { id: normalized, requiresLogin: false },
      { isLoggedIn: state2.isLoggedIn.value }
    );
    if (!access.ok && access.reason && !access.needLogin) {
      showToast(access.reason);
      if (normalized !== "home" && isViewAllowed("home")) {
        goToViewInternal("home", { push: false, restoreScroll: true });
      }
      return false;
    }
    if (!ensureLoginRequiredViewAccess(normalized)) return false;
    if (!ensureProtectedViewAccess(normalized, { push, fallbackView: state2.currentView.value })) {
      return false;
    }
    goToViewInternal(normalized, { push, restoreScroll, direction });
    return true;
  };
  const resolveParentView = (view) => {
    const normalized = normalizeViewName(view);
    if (normalized === "home") return "";
    if (HIERARCHICAL_PARENT_VIEW_MAP[normalized]) {
      return HIERARCHICAL_PARENT_VIEW_MAP[normalized];
    }
    return "home";
  };
  const goToParentView = () => {
    const parentView = resolveParentView(state2.currentView.value);
    if (!parentView) return false;
    goToViewInternal(parentView, {
      push: false,
      restoreScroll: parentView === "home",
      direction: "back"
    });
    return true;
  };
  const parseHashRoute = () => {
    const snapshot = readWindowRouteSnapshot();
    if (!snapshot?.sid) return null;
    return { sid: snapshot.sid, view: normalizeViewName(snapshot.view) };
  };
  const syncFromHash = async ({ scrollToTop = false } = {}) => {
    const route = parseHashRoute();
    if (!route) {
      if (state2.currentView.value !== "home") {
        state2.mutable.pendingScrollToTopOnViewChange = scrollToTop;
        applyViewState("home");
      }
      return;
    }
    state2.studentId.value = route.sid;
    saveRememberedUsername(route.sid);
    const safeView = resolvePolicySafeView(route.view, "home");
    if (!ensureProtectedViewAccess(safeView, {
      push: false,
      redirectToFallback: true,
      fallbackView: "home"
    })) {
      return;
    }
    state2.mutable.pendingScrollToTopOnViewChange = scrollToTop;
    applyViewState(safeView);
    if (safeView === "grades" && state2.gradeData.value.length === 0) {
      void runtime.grade.loadGradesForCurrentView();
    }
  };
  const normalizeNavigateTarget = (target) => {
    if (typeof target === "string") {
      return { view: normalizeViewName(target), payload: null };
    }
    if (target && typeof target === "object") {
      const t = target;
      return {
        view: normalizeViewName(t.view || t.route || t.moduleId || t.module_id),
        payload: t.payload && typeof t.payload === "object" ? t.payload : null
      };
    }
    return { view: "home", payload: null };
  };
  const handleNavigate = async (target) => {
    const normalized = normalizeNavigateTarget(target);
    if (normalized.view === "more_module_host") {
      const session = persistModuleHostSession(normalized.payload || {});
      const repairedSession = await repairModuleHostSession(session);
      state2.moduleHostSession.value = repairedSession;
      if (!repairedSession.preview_url) {
        goToView("more");
        return;
      }
    }
    const navigated = goToView(normalized.view);
    if (!navigated) return;
    if (normalized.view === "grades") {
      void runtime.grade.loadGradesForCurrentView();
    }
  };
  const handleBackToDashboard = () => {
    goToView("home", { restoreScroll: true, direction: "back" });
  };
  const handleBackToMe = () => {
    if (state2.currentView.value === "school_website") {
      void __vitePreload(() => import("./school_website_embed-CYHxPvBP.js"), true ? __vite__mapDeps([66,1,0]) : void 0, import.meta.url).then((mod) => {
        void mod.forceCloseSchoolWebsiteEmbed?.();
      }).catch(() => {
      });
    }
    goToView("me");
  };
  const handleBackToMoreCenter = () => {
    goToView("more");
  };
  const handleTabChange = (tab) => {
    goToView(tab);
  };
  const handleOpenOfficial = () => goToView("official");
  const handleOpenFeedback = () => goToView("feedback");
  const handleOpenConfig = () => goToView("config");
  const handleOpenSettings = () => goToView("settings");
  const handlePopState = async () => {
    if (!isDesktopLike$1) {
      const fromView = state2.currentView.value;
      const handled = goToParentView();
      if (!handled) {
        goToViewInternal("home", { push: false, restoreScroll: true, direction: "back" });
        return;
      }
      if (state2.currentView.value === "home" || fromView !== "home") {
        if (state2.currentView.value !== "home") {
          runtime.lifecycle.recoverViewportAfterTransition({ scrollToTop: true, blurActive: true });
        } else {
          runtime.lifecycle.recoverViewportAfterTransition({ scrollToTop: false, blurActive: true });
          restoreHomeScrollPosition();
        }
      }
      return;
    }
    const prev = state2.currentView.value;
    state2.navDirection.value = "back";
    await syncFromHash({ scrollToTop: false });
    if (state2.currentView.value === "home" && prev !== "home") {
      runtime.lifecycle.recoverViewportAfterTransition({ scrollToTop: false, blurActive: true });
      restoreHomeScrollPosition();
    } else {
      runtime.lifecycle.recoverViewportAfterTransition({ scrollToTop: true, blurActive: true });
    }
  };
  const installCloseInterceptor = async () => {
    if (!hasTauri2) return;
    try {
      const appWindow = await getCurrentNativeWindow();
      if (!appWindow) return;
      state2.mutable.unlistenCloseRequested = await appWindow.onCloseRequested(async (event) => {
        if (state2.mutable.isClosingByUser) return;
        if (state2.currentView.value !== "home") {
          event.preventDefault();
          if (window.history.length > 1 && (window.location.hash || "#/") !== "#/") {
            state2.navDirection.value = "back";
            window.history.back();
          } else {
            goToView("home", { restoreScroll: true, direction: "back" });
          }
          return;
        }
        event.preventDefault();
        state2.showExitDialog.value = true;
        replaceHistorySnapshot("home");
      });
    } catch (e) {
      console.warn("[Navigation] 安装关闭拦截失败:", e);
    }
  };
  const cancelExitDialog = () => {
    state2.showExitDialog.value = false;
  };
  const confirmExitDialog = async () => {
    if (state2.exitingApp.value) return;
    state2.exitingApp.value = true;
    state2.mutable.isClosingByUser = true;
    try {
      await exitNativeApp();
    } catch (e) {
      console.warn("[Navigation] 退出应用失败:", e);
      try {
        const appWindow = await getCurrentNativeWindow();
        if (appWindow) {
          await appWindow.destroy();
        }
      } catch (fallbackErr) {
        console.warn("[Navigation] destroy 回退失败:", fallbackErr);
      }
    } finally {
      state2.showExitDialog.value = false;
      state2.exitingApp.value = false;
      state2.mutable.isClosingByUser = false;
    }
  };
  const closeDailyAccessDialog = () => {
    state2.showDailyAccessDialog.value = false;
    state2.dailyAccessInput.value = "";
    state2.dailyAccessError.value = "";
    state2.pendingProtectedView.value = null;
  };
  const handleDailyAccessInput = (event) => {
    const el = event?.target;
    state2.dailyAccessInput.value = sanitizeDailyAccessInput(el?.value || "");
    if (state2.dailyAccessError.value) {
      state2.dailyAccessError.value = "";
    }
  };
  const submitDailyAccessKey = () => {
    const normalized = sanitizeDailyAccessInput(state2.dailyAccessInput.value);
    state2.dailyAccessInput.value = normalized;
    if (!verifyDailyAccessKey(normalized)) {
      state2.dailyAccessError.value = "今日秘钥不正确，请重新输入。";
      return;
    }
    const targetView = normalizeViewName(state2.pendingProtectedView.value?.view || "home");
    const push = state2.pendingProtectedView.value?.push !== false;
    markDailyAccessGranted();
    state2.showDailyAccessDialog.value = false;
    state2.dailyAccessError.value = "";
    state2.pendingProtectedView.value = null;
    goToViewInternal(targetView, { push });
  };
  const collectCurrentViewSnapshot = () => ({
    sid: String(state2.studentId.value || "").trim(),
    view: normalizeViewName(state2.currentView.value),
    tab: String(state2.activeTab.value || "").trim(),
    module: state2.currentView.value === "more_module_host" ? String(state2.moduleHostSession.value?.module_id || "").trim() : String(state2.currentModule.value || "").trim()
  });
  const restoreViewFromSnapshot = async (snapshot, { softRemount = false, allowHardReload = false, idleMs = 0 } = {}) => {
    const resolved = snapshot || collectCurrentViewSnapshot();
    let targetViewRaw = resolvePolicySafeSnapshotView(resolved, state2.currentView.value, "home");
    if (targetViewRaw === "more_module_host") {
      state2.moduleHostSession.value = await repairModuleHostSession(readModuleHostSession());
    }
    let targetView = targetViewRaw === "more_module_host" && !state2.moduleHostSession.value.preview_url ? resolvePolicySafeView("more", "home") : targetViewRaw;
    targetView = resolvePolicySafeView(targetView, "home");
    if (resolved?.sid) {
      state2.studentId.value = String(resolved.sid || "").trim();
      try {
        saveRememberedUsername(state2.studentId.value);
      } catch {
      }
    }
    if (!ensureProtectedViewAccess(targetView, {
      push: false,
      redirectToFallback: true,
      fallbackView: "home"
    })) {
      return;
    }
    state2.mutable.pendingScrollToTopOnViewChange = false;
    applyViewState(targetView);
    replaceHistorySnapshot(targetView);
    let didSoftRemount = false;
    if (softRemount) {
      const now = Date.now();
      if (now - state2.mutable.lastSoftRemountAt >= 30 * 1e3) {
        state2.mutable.lastSoftRemountAt = now;
        state2.viewRenderNonce.value += 1;
        didSoftRemount = true;
      }
    }
    await nextTick();
    runtime.lifecycle.recoverViewportAfterTransition({ scrollToTop: false, blurActive: false });
    if (isIOSLike$1 && (didSoftRemount || !runtime.lifecycle.isCurrentViewDomHealthy(targetView))) {
      requestAnimationFrame(() => {
        runtime.lifecycle.nudgeWebViewPaint(targetView, {
          verify: true,
          allowReload: didSoftRemount && allowHardReload,
          idleMs
        });
      });
    }
  };
  const handleViewChanged = (view, prev) => {
    prefetchViewComponent(view);
    if (prev === "school_website" && view !== "school_website") {
      void __vitePreload(() => import("./school_website_embed-CYHxPvBP.js"), true ? __vite__mapDeps([66,1,0]) : void 0, import.meta.url).then((mod) => {
        void mod.forceCloseSchoolWebsiteEmbed?.();
      }).catch(() => {
      });
    }
  };
  return {
    goToView,
    goToViewInternal,
    applyViewState,
    replaceHistorySnapshot,
    pushHistorySnapshot,
    goToParentView,
    handleNavigate,
    handleBackToDashboard,
    handleBackToMe,
    handleBackToMoreCenter,
    handleTabChange,
    handleOpenOfficial,
    handleOpenFeedback,
    handleOpenConfig,
    handleOpenSettings,
    handlePopState,
    installCloseInterceptor,
    cancelExitDialog,
    confirmExitDialog,
    closeDailyAccessDialog,
    handleDailyAccessInput,
    submitDailyAccessKey,
    syncFromHash,
    restoreViewFromSnapshot,
    forceScrollTop,
    ensureLoginRequiredViewAccess,
    ensureProtectedViewAccess,
    // bootstrap / boot 辅助
    readWindowRouteSnapshot,
    prefetchViewComponent,
    persistModuleHostSession,
    repairModuleHostSession,
    readModuleHostSession,
    collectCurrentViewSnapshot,
    handleViewChanged,
    restoreHomeScrollPosition,
    resetBootMetrics: (context) => resetBootMetrics(context),
    hasBootMetric,
    markBootMetric: (name, detail) => {
      markBootMetric(name, detail);
    },
    readStartupSnapshot: () => {
      const snapshot = readWindowRouteSnapshot();
      const startupPageSetting = useUiSettings().startupPage || "home";
      const startupView = resolvePolicySafeView(snapshot?.view || startupPageSetting, "home");
      const initialView = isProtectedView(startupView) && !hasDailyAccessGrant() ? "home" : startupView;
      const initialTab = String(
        snapshot?.tab || (MAIN_TABS.includes(initialView) ? initialView : ME_SUB_VIEWS.includes(initialView) ? "me" : "home")
      ).trim() || "home";
      const initialModule = String(
        snapshot?.module || (MAIN_TABS.includes(initialView) ? "" : initialView === "home" ? "" : initialView)
      ).trim();
      const bootStudentIdHint = String(snapshot?.sid || "").trim();
      const bootScheduleSnapshot = initialView === "schedule" && bootStudentIdHint ? readScheduleRenderSnapshot(bootStudentIdHint) : null;
      return {
        snapshot,
        startupPageSetting,
        initialView,
        initialTab,
        initialModule,
        bootStudentIdHint,
        bootScheduleSnapshot,
        skipSplashForFastScheduleBoot: !!bootScheduleSnapshot
      };
    },
    clearScheduleSnapshot: (sid) => clearScheduleRenderSnapshot(sid),
    scheduleKeys: { SCHEDULE_POPUP_PENDING_KEY, SCHEDULE_SWITCH_PENDING_KEY }
  };
};
const DEVICE_KEY_STORAGE = "hbu_device_crypto_key_v2";
const LEGACY_SECRET_KEY = "hbu_grade_secret_key_2026";
async function digestKeyMaterial(material) {
  const data = new TextEncoder().encode(material);
  const hashBuffer = await crypto.subtle.digest("SHA-256", data);
  return crypto.subtle.importKey("raw", hashBuffer, { name: "AES-CBC" }, false, [
    "encrypt",
    "decrypt"
  ]);
}
function ensureDeviceSecretMaterial() {
  let key = localStorage.getItem(DEVICE_KEY_STORAGE);
  if (!key) {
    const bytes = crypto.getRandomValues(new Uint8Array(32));
    key = btoa(String.fromCharCode(...bytes));
    localStorage.setItem(DEVICE_KEY_STORAGE, key);
  }
  return key;
}
const getAesKey = () => digestKeyMaterial(ensureDeviceSecretMaterial());
const getLegacyAesKey = () => digestKeyMaterial(LEGACY_SECRET_KEY);
async function encryptData(data) {
  const key = await getAesKey();
  const iv = crypto.getRandomValues(new Uint8Array(16));
  const jsonData = new TextEncoder().encode(JSON.stringify(data));
  const encrypted = await crypto.subtle.encrypt({ name: "AES-CBC", iv }, key, jsonData);
  const combined = new Uint8Array(iv.length + encrypted.byteLength);
  combined.set(iv);
  combined.set(new Uint8Array(encrypted), iv.length);
  return btoa(String.fromCharCode(...combined));
}
async function decryptWithKey(encryptedStr, key) {
  const combined = Uint8Array.from(atob(encryptedStr), (character) => character.charCodeAt(0));
  if (combined.length <= 16) throw new Error("encrypted payload is too short");
  const iv = combined.slice(0, 16);
  const ciphertext = combined.slice(16);
  const decrypted = await crypto.subtle.decrypt({ name: "AES-CBC", iv }, key, ciphertext);
  const parsed = JSON.parse(new TextDecoder().decode(decrypted));
  if (!parsed || typeof parsed !== "object" || Array.isArray(parsed)) {
    throw new Error("decrypted payload must be an object");
  }
  return parsed;
}
async function decryptData(encryptedStr) {
  const key = await getAesKey();
  try {
    return await decryptWithKey(encryptedStr, key);
  } catch {
    return decryptWithKey(encryptedStr, await getLegacyAesKey());
  }
}
const HBUT_CREDENTIAL_PREFIX = "hbut:";
const CHAOXING_CREDENTIAL_PREFIX = "cx:";
const CAMPUS_CREDENTIAL_PREFIX = "campus:";
const webStorageKey = (accountKey) => `cred:${accountKey}`;
const portalPasswordMemory = /* @__PURE__ */ new Map();
const buildHbutAccountKey = (username) => `${HBUT_CREDENTIAL_PREFIX}${String(username ?? "").trim()}`;
const buildChaoxingAccountKey = (account) => `${CHAOXING_CREDENTIAL_PREFIX}${String(account ?? "").trim()}`;
const buildCampusAccountKey = (username) => `${CAMPUS_CREDENTIAL_PREFIX}${String(username ?? "").trim()}`;
function rememberPortalPasswordInMemory(studentId, password) {
  const sid = String(studentId ?? "").trim();
  const value = String(password ?? "").trim();
  if (sid && value) portalPasswordMemory.set(sid, value);
}
function peekPortalPasswordInMemory(studentId) {
  return String(portalPasswordMemory.get(String(studentId ?? "").trim()) ?? "").trim();
}
async function loadEncryptedWebBackup(accountKey) {
  const raw = localStorage.getItem(webStorageKey(accountKey));
  if (!raw) return "";
  try {
    const data = await decryptData(raw);
    return String(data.password ?? "").trim();
  } catch {
    return "";
  }
}
async function saveEncryptedWebBackup(accountKey, password) {
  const value = String(password ?? "").trim();
  if (!value) {
    localStorage.removeItem(webStorageKey(accountKey));
    return;
  }
  localStorage.setItem(webStorageKey(accountKey), await encryptData({ password: value }));
}
async function saveRememberedCredential(accountKey, password) {
  const key = String(accountKey ?? "").trim();
  const value = String(password ?? "");
  if (!key) return;
  if (!value) {
    if (isTauriRuntime()) {
      try {
        await invokeNative("delete_remembered_credential", { accountKey: key });
      } catch {
      }
    }
    localStorage.removeItem(webStorageKey(key));
    if (key.startsWith(HBUT_CREDENTIAL_PREFIX)) {
      portalPasswordMemory.delete(key.slice(HBUT_CREDENTIAL_PREFIX.length));
    }
    return;
  }
  if (isAppStoreBuild()) {
    if (isTauriRuntime()) {
      try {
        await invokeNative("save_remembered_credential", { accountKey: key, password: value });
      } catch {
      }
    }
    localStorage.removeItem(webStorageKey(key));
    if (key.startsWith(HBUT_CREDENTIAL_PREFIX)) {
      rememberPortalPasswordInMemory(key.slice(HBUT_CREDENTIAL_PREFIX.length), value);
    }
    return;
  }
  if (isTauriRuntime()) {
    try {
      await invokeNative("save_remembered_credential", { accountKey: key, password: value });
    } catch (error) {
      console.warn("[Credential] 密钥环保存失败，已写入本地加密备份:", error);
    }
  }
  await saveEncryptedWebBackup(key, value);
  if (key.startsWith(HBUT_CREDENTIAL_PREFIX)) {
    rememberPortalPasswordInMemory(key.slice(HBUT_CREDENTIAL_PREFIX.length), value);
  }
}
async function loadRememberedCredential(accountKey) {
  const key = String(accountKey ?? "").trim();
  if (!key) return "";
  if (isTauriRuntime()) {
    try {
      const loaded = await invokeNative("load_remembered_credential", { accountKey: key });
      const normalized = String(loaded ?? "").trim();
      if (normalized) return normalized;
    } catch (error) {
      console.warn("[Credential] 密钥环读取失败，尝试本地加密备份:", error);
    }
  }
  return loadEncryptedWebBackup(key);
}
async function readLegacyStoredPassword(legacyPasswordKey, legacyPlaintext = "") {
  const raw = String(
    legacyPlaintext || (legacyPasswordKey ? localStorage.getItem(legacyPasswordKey) : "") || ""
  ).trim();
  if (!raw) return "";
  try {
    const decrypted = await decryptData(raw);
    const password = String(decrypted.password ?? "").trim();
    if (password) return password;
  } catch {
  }
  try {
    const parsed = JSON.parse(raw);
    if (parsed && typeof parsed === "object" && !Array.isArray(parsed)) {
      const password = String(parsed.password ?? "").trim();
      if (password) return password;
    }
  } catch {
  }
  return raw;
}
async function migrateLegacyCredential({
  legacyPasswordKey,
  legacyPlaintext = "",
  accountKey
}) {
  const key = String(accountKey ?? "").trim();
  if (!key) return;
  const existing = await loadRememberedCredential(key);
  const legacyRaw = legacyPasswordKey ? String(localStorage.getItem(legacyPasswordKey) ?? "").trim() : String(legacyPlaintext ?? "").trim();
  if (existing && !legacyRaw) return;
  const legacy = await readLegacyStoredPassword(legacyPasswordKey, legacyPlaintext);
  if (!legacy) return;
  await saveRememberedCredential(key, legacy);
  const verified = String(await loadRememberedCredential(key) ?? "").trim();
  if (verified !== legacy) {
    console.warn("[Credential] 旧凭据迁移后校验失败，保留 legacy 键以便下次重试:", key);
    return;
  }
  if (legacyPasswordKey) localStorage.removeItem(legacyPasswordKey);
}
async function loadPortalRememberedPassword(username) {
  const sid = String(username ?? "").trim();
  if (!sid) return "";
  const cached = peekPortalPasswordInMemory(sid);
  if (cached) return cached;
  await migrateLegacyCredential({
    legacyPasswordKey: "hbu_credentials",
    accountKey: buildHbutAccountKey(sid)
  });
  const password = await loadRememberedCredential(buildHbutAccountKey(sid));
  if (password) {
    rememberPortalPasswordInMemory(sid, password);
    return password;
  }
  if (isTauriRuntime()) {
    try {
      const sessionPassword = await invokeNative("load_session_password", { studentId: sid });
      const normalized = String(sessionPassword ?? "").trim();
      if (normalized) {
        await saveRememberedCredential(buildHbutAccountKey(sid), normalized);
        return normalized;
      }
    } catch {
    }
  }
  return "";
}
async function preservePortalRememberedPasswordOnLogout() {
  const username = String(localStorage.getItem("hbu_username") ?? "").trim();
  if (!username || localStorage.getItem("hbu_remember") === "false") return;
  localStorage.setItem("hbu_remember", "true");
  const password = peekPortalPasswordInMemory(username) || await loadPortalRememberedPassword(username);
  if (!password) return;
  await syncPortalRememberCredential({ username, studentId: username, password, remember: true });
}
async function loadChaoxingRememberedPassword(account) {
  const normalized = String(account ?? "").trim();
  if (!normalized) return "";
  await migrateLegacyCredential({
    legacyPasswordKey: "hbu_cx_password",
    accountKey: buildChaoxingAccountKey(normalized)
  });
  return loadRememberedCredential(buildChaoxingAccountKey(normalized));
}
async function syncPortalRememberCredential({
  username,
  studentId,
  password,
  remember = true
}) {
  if (!remember) return;
  const value = String(password ?? "").trim();
  if (!value) return;
  const keys = /* @__PURE__ */ new Set();
  const loginName = String(username ?? "").trim();
  const sid = String(studentId ?? "").trim();
  if (loginName) keys.add(buildHbutAccountKey(loginName));
  if (sid) keys.add(buildHbutAccountKey(sid));
  for (const accountKey of keys) await saveRememberedCredential(accountKey, value);
  if (sid) rememberPortalPasswordInMemory(sid, value);
  if (loginName) rememberPortalPasswordInMemory(loginName, value);
  localStorage.removeItem("hbu_credentials");
}
async function ensureRememberedPasswordCached(username) {
  const sid = String(username ?? "").trim();
  if (!sid || localStorage.getItem("hbu_remember") === "false") return;
  const existing = await loadPortalRememberedPassword(sid);
  if (existing) {
    await syncPortalRememberCredential({ username: sid, studentId: sid, password: existing });
    return;
  }
  if (!isTauriRuntime()) return;
  try {
    const loaded = await invokeNative("load_session_password", { studentId: sid });
    const sessionPassword = String(loaded ?? "").trim();
    if (sessionPassword) {
      await syncPortalRememberCredential({ username: sid, studentId: sid, password: sessionPassword });
    }
  } catch {
  }
}
const CHAOXING_REMEMBER_KEY = "hbu_cx_remember";
const CHAOXING_ACCOUNT_KEY = "hbu_cx_account";
async function loadPortalStoredPassword() {
  const username = String(localStorage.getItem("hbu_username") || "").trim();
  if (!username) return null;
  const remember = localStorage.getItem("hbu_remember");
  if (remember !== "false") {
    const password = await loadPortalRememberedPassword(username);
    if (password) return { username, password, backendRestorable: false };
  }
  if (isTauriRuntime()) {
    try {
      const restorable = await invokeNative("has_restorable_credentials", {
        studentId: username
      });
      if (restorable) {
        return { username, password: "", backendRestorable: true };
      }
    } catch {
    }
  }
  return null;
}
async function loadChaoxingStoredPassword() {
  const remember = localStorage.getItem(CHAOXING_REMEMBER_KEY);
  const account = String(localStorage.getItem(CHAOXING_ACCOUNT_KEY) || "").trim();
  if (remember === "false" || !account) {
    return null;
  }
  const password = await loadChaoxingRememberedPassword(account);
  if (!password) return null;
  return { account, password };
}
const createSessionCoordinator = (runtime) => {
  const { state: state2 } = runtime;
  const hasTauri2 = isTauriRuntime();
  const invoke2 = (command, args) => invokeNative(command, args);
  const bridgePost2 = async (path, payload = {}) => {
    const bridgeBase = hasTauri2 ? "http://127.0.0.1:4399" : "/bridge";
    const res = await fetch(`${bridgeBase}${path}`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(payload || {})
    });
    return res.json();
  };
  const restoreSessionViaBridge = async (cookies) => {
    const res = await bridgePost2("/restore_session", { cookies });
    if (res?.success && res?.data?.student_id) {
      return res.data;
    }
    throw new Error(res?.error?.message || "恢复会话失败");
  };
  const importCookiesViaBridge = async (snapshot) => {
    const res = await bridgePost2("/import_cookies", snapshot || {});
    if (res?.success && res?.data?.user?.student_id) {
      return res.data.user;
    }
    throw new Error(res?.error?.message || "导入 cookies 失败");
  };
  const persistSessionCookies = async () => {
    if (isTestAccountSession()) return;
    if (!hasTauri2) return;
    try {
      const cookies = await invoke2("get_cookies");
      if (cookies) {
        localStorage.setItem(SESSION_COOKIE_KEY, String(cookies));
        localStorage.setItem(SESSION_COOKIE_TIME_KEY, Date.now().toString());
      }
    } catch (e) {
      console.warn("[Session] 保存 cookies 失败:", e);
    }
  };
  const markLoginSessionToken = () => {
    try {
      localStorage.setItem(LOGIN_SESSION_TOKEN_KEY$1, `${Date.now()}`);
    } catch {
    }
  };
  const isTemporaryLoginSession = () => {
    const method = String(localStorage.getItem(LOGIN_METHOD_KEY) || "").trim();
    const marked = localStorage.getItem(LOGIN_TEMP_FLAG_KEY) === "1";
    return marked || method.endsWith("_temp");
  };
  const isManualLogout = () => localStorage.getItem("hbu_manual_logout") === "true";
  const restoreTestAccountSession = () => {
    if (!isTestAccountSession()) return false;
    const sid = TEST_ACCOUNT.studentId;
    state2.studentId.value = sid;
    state2.gradeData.value = getTestAccountGrades();
    state2.gradeTeacherCache.value = null;
    state2.gradeTeacherCacheSid.value = sid;
    saveRememberedUsername(sid);
    localStorage.setItem(LOGIN_METHOD_KEY, "test_account");
    localStorage.setItem(LOGIN_TEMP_FLAG_KEY, "0");
    localStorage.removeItem("hbu_manual_logout");
    localStorage.removeItem(LOGOUT_REASON_KEY);
    seedTestAccountCaches(setCachedData, sid);
    state2.onlineSessionState.value = "online";
    clearJwxtMaintenance();
    stopJwxtRecoveryPolling();
    return true;
  };
  const restoreCachedIdentityFromLocal = async () => {
    if (isManualLogout()) return false;
    const cachedSid = String(localStorage.getItem("hbu_username") || "").trim();
    if (!cachedSid) return false;
    if (!/^\d{10}$/.test(cachedSid)) {
      clearRememberedUsername();
      return false;
    }
    if (isTestAccountSession() && cachedSid === TEST_ACCOUNT.studentId) {
      return restoreTestAccountSession();
    }
    state2.studentId.value = cachedSid;
    state2.onlineSessionState.value = "cached_offline";
    if (hasTauri2) {
      invoke2("set_offline_user_context", {
        student_id: cachedSid,
        studentId: cachedSid
      }).catch((e) => {
        console.warn("[Session] 设置离线上下文失败:", e);
      });
    }
    return true;
  };
  const tryRestoreSession = async () => {
    if (isTestAccountSession()) return restoreTestAccountSession();
    const cookies = localStorage.getItem(SESSION_COOKIE_KEY);
    if (!cookies && !hasTauri2) {
      try {
        const snapshotRaw = localStorage.getItem(COOKIE_SNAPSHOT_KEY);
        if (!snapshotRaw) return false;
        const snapshot = JSON.parse(snapshotRaw);
        const info = await importCookiesViaBridge(snapshot);
        if (info?.student_id) {
          state2.studentId.value = info.student_id;
          saveRememberedUsername(info.student_id);
          return true;
        }
      } catch (e) {
        console.warn("[Session] 导入 cookies 失败:", e);
      }
      return false;
    }
    if (!cookies) {
      if (isTemporaryLoginSession()) {
        void runtime.auth.handleLogout({
          manual: false,
          reason: TEMP_SESSION_EXPIRED_REASON,
          notice: "扫码临时登录会话已失效，请重新登录。"
        });
      }
      return false;
    }
    try {
      const userInfo = hasTauri2 ? await invoke2("restore_session", { cookies }) : await restoreSessionViaBridge(cookies);
      if (userInfo?.student_id) {
        state2.studentId.value = userInfo.student_id;
        saveRememberedUsername(userInfo.student_id);
        return true;
      }
    } catch (e) {
      if (isTemporaryLoginSession()) {
        console.warn("[Session] 临时登录恢复失败，自动退出:", e);
        void runtime.auth.handleLogout({
          manual: false,
          reason: TEMP_SESSION_EXPIRED_REASON,
          notice: "扫码临时登录会话已失效，请重新登录。"
        });
        return false;
      }
      state2.jwxtSessionLastError.value = formatSessionError(e);
      console.warn("[Session] 恢复会话失败，保留本地缓存以便离线展示:", e);
      if (isManualLogout()) {
        localStorage.removeItem(SESSION_COOKIE_KEY);
        localStorage.removeItem(SESSION_COOKIE_TIME_KEY);
      }
    }
    return false;
  };
  const tryRestoreLatestSession = async () => {
    if (isTestAccountSession()) return restoreTestAccountSession();
    if (!hasTauri2) return false;
    if (isManualLogout()) {
      return false;
    }
    try {
      const userInfo = await invoke2("restore_latest_session");
      if (userInfo?.student_id) {
        state2.studentId.value = userInfo.student_id;
        saveRememberedUsername(userInfo.student_id);
        await persistSessionCookies();
        return true;
      }
    } catch (e) {
      console.warn("[Session] 自动恢复历史会话失败:", e);
    }
    return false;
  };
  const getStoredPassword = () => loadPortalStoredPassword();
  const getStoredChaoxingPassword = () => loadChaoxingStoredPassword();
  const isLikelyStudentId = (value) => /^\d{10}$/.test(String(value || "").trim());
  const resolveAutoLoginStudentId = async (payload) => {
    const payloadSid = String(payload?.student_id || payload?.studentId || "").trim();
    if (isLikelyStudentId(payloadSid)) return payloadSid;
    const cachedSid = String(localStorage.getItem("hbu_username") || "").trim();
    if (isLikelyStudentId(cachedSid)) return cachedSid;
    try {
      const info = await invoke2("fetch_student_info");
      const sid = String(
        info?.student_id || info?.studentId || info?.data?.student_id || info?.data?.xh || ""
      ).trim();
      if (isLikelyStudentId(sid)) return sid;
    } catch (e) {
      console.warn("[Session] 自动重登学号解析失败:", e);
    }
    return "";
  };
  const attemptAutoRelogin = async () => {
    if (isTestAccountSession()) return restoreTestAccountSession();
    if (!hasTauri2) return false;
    if (isManualLogout()) {
      return false;
    }
    const method = String(localStorage.getItem(LOGIN_METHOD_KEY) || "").trim();
    if (method.startsWith("chaoxing_")) {
      const chaoxingCreds = await getStoredChaoxingPassword();
      if (!chaoxingCreds) return false;
      try {
        const payload = await runExclusiveLogin(
          () => invoke2("chaoxing_password_login", {
            account: chaoxingCreds.account,
            password: chaoxingCreds.password
          })
        );
        const sid = await resolveAutoLoginStudentId(payload);
        if (sid) {
          state2.studentId.value = sid;
          saveRememberedUsername(sid);
        } else {
          throw new Error("学习通自动登录未解析到 10 位学号");
        }
        await persistSessionCookies();
        return true;
      } catch (e) {
        state2.jwxtSessionLastError.value = formatSessionError(e);
        console.warn("[Session] 学习通自动登录失败:", e);
        return false;
      }
    }
    const creds = await getStoredPassword();
    if (!creds) {
      state2.jwxtSessionLastError.value = "本地未找到融合门户记住密码";
      return false;
    }
    if (creds.backendRestorable) {
      try {
        const userInfo = await runExclusiveLogin(
          () => invoke2("auto_relogin_from_stored", {
            studentId: creds.username
          })
        );
        await persistSessionCookies();
        const sid = String(
          userInfo?.student_id || userInfo?.studentId || creds.username || ""
        ).trim();
        if (sid) {
          state2.studentId.value = sid;
          saveRememberedUsername(sid);
        }
        return true;
      } catch (e) {
        state2.jwxtSessionLastError.value = formatSessionError(e);
        console.warn("[Session] 后端存储凭据自动登录失败:", e);
        return false;
      }
    }
    const doLogin = () => (
      // #659：invoke('login') 走全局单飞门 —— 与手动登录互斥复用，
      // 绝不在已有登录请求在飞时再次触发
      runExclusiveLogin(async () => {
        const userInfo = await invoke2("login", {
          username: creds.username,
          password: creds.password,
          captcha: "",
          lt: "",
          execution: ""
        });
        await persistSessionCookies();
        const sid = String(userInfo?.student_id || creds.username || "").trim();
        if (sid) {
          state2.studentId.value = sid;
          saveRememberedUsername(sid);
        }
        return userInfo;
      })
    );
    try {
      await doLogin();
      return true;
    } catch (e) {
      const msg = String(e?.message || e || "");
      const cooldownMatch = msg.match(/登录频率过高，请(\d+)秒后再试/);
      if (cooldownMatch) {
        const waitSec = parseInt(cooldownMatch[1], 10);
        if (waitSec > 0 && waitSec <= 120) {
          console.info(`[Session] 登录冷却中，${waitSec}秒后重试...`);
          state2.jwxtSessionLastError.value = `登录冷却中，${waitSec} 秒后重试`;
          markJwxtMaintenance(`登录冷却，${waitSec} 秒后自动重试…`, {
            phase: "recovering",
            detail: state2.jwxtSessionLastError.value
          });
          await new Promise((r) => window.setTimeout(r, (waitSec + 2) * 1e3));
          try {
            await doLogin();
            return true;
          } catch (e2) {
            state2.jwxtSessionLastError.value = formatSessionError(e2);
            console.warn("[Session] 冷却后重试仍失败:", e2);
            return false;
          }
        }
      }
      state2.jwxtSessionLastError.value = formatSessionError(e);
      console.warn("[Session] 自动登录失败:", e);
      return false;
    }
  };
  const classifySessionError = (err) => {
    const raw = String(err?.message || err || "").toLowerCase();
    if (/error sending request|timed? ?out|timeout|connection|connect |network|econnrefused|econnreset|dns |resolve|socket|eof|broken pipe/i.test(raw)) {
      return "network";
    }
    if (/login|401|403|unauthorized|session|expired|cookie|not logged/i.test(raw)) {
      return "auth";
    }
    return "unknown";
  };
  const sessionFailureHint = (fallback) => {
    if (classifySessionError(state2.jwxtSessionLastError.value) === "network") {
      return "网络异常，无法连接教务系统，稍后自动重试。";
    }
    return fallback;
  };
  const refreshSessionVerified = async (options = {}) => {
    if (isTestAccountSession()) return true;
    const cookies = localStorage.getItem(SESSION_COOKIE_KEY);
    if (!cookies) return false;
    if (!hasTauri2) return false;
    try {
      await invoke2("refresh_session");
      await persistSessionCookies();
      return true;
    } catch (e) {
      state2.jwxtSessionLastError.value = formatSessionError(e);
      return false;
    }
  };
  const refreshSessionSilently = async (options = {}) => {
    if (isTestAccountSession()) return;
    const cookies = localStorage.getItem(SESSION_COOKIE_KEY);
    if (!cookies) return;
    if (!hasTauri2) return;
    try {
      await invoke2("refresh_session");
      await persistSessionCookies();
    } catch (e) {
      if (isTemporaryLoginSession()) {
        console.warn("[Session] 临时登录会话已失效，自动退出:", e);
        void runtime.auth.handleLogout({
          manual: false,
          reason: TEMP_SESSION_EXPIRED_REASON,
          notice: "扫码临时登录会话已失效，请重新登录。"
        });
        return;
      }
      state2.jwxtSessionLastError.value = formatSessionError(e);
      const kind = classifySessionError(e);
      if (kind === "network") {
        console.warn("[Session] 会话刷新网络异常，稍后自动重试:", e);
        return;
      }
      if (options.quiet) {
        console.warn("[Session] 登录后会话探测未通过，静默后台恢复:", e);
        const relogged2 = await attemptAutoRelogin();
        if (relogged2) {
          clearJwxtMaintenance();
          startSessionKeepAlive();
          startElectricityKeepAlive();
          await persistSessionCookies();
          notifySessionOnline("auto-relogin");
        }
        return;
      }
      console.warn("[Session] 会话刷新失败，尝试自动登录:", e);
      markJwxtMaintenance("会话失效，正在后台自动登录…", {
        phase: "recovering",
        detail: state2.jwxtSessionLastError.value
      });
      const relogged = await attemptAutoRelogin();
      if (!relogged) {
        stopSessionKeepAlive();
        markJwxtMaintenance(sessionFailureHint("后台自动登录未成功，将定时重试。当前为缓存数据。"), {
          phase: "failed",
          detail: state2.jwxtSessionLastError.value
        });
        startJwxtRecoveryPolling();
      } else {
        clearJwxtMaintenance();
        if (state2.studentId.value) {
          resetCloudSyncCooldownForSession(state2.studentId.value);
          runAutoCloudSyncAfterLogin({
            studentId: state2.studentId.value,
            latestGrades: []
          }).catch((e2) => {
            console.warn("[CloudSync] 后台重登录后自动同步失败:", e2);
          });
        }
      }
    }
  };
  const startSessionKeepAlive = () => {
    stopSessionKeepAlive();
    state2.mutable.sessionKeepAliveTimer = window.setInterval(refreshSessionSilently, SESSION_REFRESH_INTERVAL);
  };
  const stopSessionKeepAlive = () => {
    if (state2.mutable.sessionKeepAliveTimer) {
      window.clearInterval(state2.mutable.sessionKeepAliveTimer);
      state2.mutable.sessionKeepAliveTimer = null;
    }
  };
  const startElectricityKeepAlive = () => {
    stopElectricityKeepAlive();
    state2.mutable.electricityKeepAliveTimer = window.setInterval(async () => {
      try {
        await invoke2("refresh_electricity_token");
      } catch (e) {
        console.warn("[Electricity] Token refresh failed:", e);
      }
    }, ELECTRICITY_REFRESH_INTERVAL);
  };
  const stopElectricityKeepAlive = () => {
    if (state2.mutable.electricityKeepAliveTimer) {
      window.clearInterval(state2.mutable.electricityKeepAliveTimer);
      state2.mutable.electricityKeepAliveTimer = null;
    }
  };
  const stopJwxtRecoveryPolling = () => {
    if (state2.mutable.jwxtRecoveryTimer) {
      window.clearInterval(state2.mutable.jwxtRecoveryTimer);
      state2.mutable.jwxtRecoveryTimer = null;
    }
    state2.mutable.jwxtRecoveryInFlight = false;
  };
  const attemptOnlineRecovery = async (options = {}) => {
    if (isTestAccountSession()) return restoreTestAccountSession();
    if (!hasTauri2 || isManualLogout()) return false;
    if (isLoginInFlight()) return false;
    if (state2.mutable.jwxtRecoveryInFlight) return false;
    state2.mutable.jwxtRecoveryInFlight = true;
    state2.onlineSessionState.value = "recovering";
    if (!options.silent) {
      state2.jwxtRecoveryPhase.value = "recovering";
    }
    try {
      let restored = await tryRestoreSession();
      if (!restored) {
        restored = await tryRestoreLatestSession();
      }
      let relogged = false;
      if (!restored && !isTemporaryLoginSession()) {
        relogged = await attemptAutoRelogin();
      }
      const success = restored || relogged;
      if (success) {
        clearJwxtMaintenance();
        startSessionKeepAlive();
        startElectricityKeepAlive();
        if (state2.studentId.value) {
          startNotificationMonitor({ studentId: state2.studentId.value }).catch((e) => {
            console.warn("[Notify] 恢复后启动通知监控失败:", e);
          });
        }
        await persistSessionCookies();
        stopJwxtRecoveryPolling();
        notifySessionOnline(relogged ? "auto-relogin" : "session-restore");
        if (relogged && state2.studentId.value) {
          resetCloudSyncCooldownForSession(state2.studentId.value);
          runAutoCloudSyncAfterLogin({
            studentId: state2.studentId.value,
            latestGrades: []
          }).catch((e) => {
            console.warn("[CloudSync] 恢复后自动同步失败:", e);
          });
        }
      } else if (!options.silent) {
        markJwxtMaintenance("会话暂不可用，当前展示缓存数据。", {
          phase: "failed",
          detail: state2.jwxtSessionLastError.value || "恢复未成功"
        });
      }
      return success;
    } catch (e) {
      state2.jwxtSessionLastError.value = formatSessionError(e);
      if (!options.silent) {
        markJwxtMaintenance("恢复会话失败", {
          phase: "failed",
          detail: state2.jwxtSessionLastError.value
        });
      }
      return false;
    } finally {
      state2.mutable.jwxtRecoveryInFlight = false;
    }
  };
  const startJwxtRecoveryPolling = () => {
    if (isTestAccountSession()) return;
    if (isManualLogout() || !state2.studentId.value) return;
    stopJwxtRecoveryPolling();
    state2.mutable.jwxtRecoveryTimer = window.setInterval(() => {
      attemptOnlineRecovery({ silent: true }).then((ok) => {
        if (ok) return;
        markJwxtMaintenance(sessionFailureHint("后台自动登录仍未成功，将继续重试。"), {
          phase: "failed",
          detail: state2.jwxtSessionLastError.value || "恢复失败"
        });
      }).catch((e) => {
        console.warn("[Session] 教务恢复轮询失败:", e);
        state2.jwxtSessionLastError.value = formatSessionError(e);
        markJwxtMaintenance("后台恢复异常", {
          phase: "failed",
          detail: state2.jwxtSessionLastError.value
        });
      });
    }, JWXT_RECOVERY_INTERVAL);
  };
  const ensureConfigAccess = () => {
    if (state2.currentView.value === "config" && !state2.isConfigAdmin.value) {
      runtime.navigation.applyViewState("me");
      runtime.navigation.replaceHistorySnapshot("me");
    }
  };
  const formatCheckTime = (ts = Date.now()) => {
    try {
      return new Date(ts).toLocaleString();
    } catch {
      return "";
    }
  };
  const normalizeRecoveryPhase = (phase2, fallback = "maintenance") => {
    const p = String(phase2 || "").trim();
    if (["idle", "recovering", "failed", "need_login", "maintenance"].includes(p)) return p;
    return fallback;
  };
  const markJwxtMaintenance = (hint = "", options = {}) => {
    if (!state2.studentId.value && !options.force) return;
    const detail = String(options.detail || state2.jwxtSessionLastError.value || "").trim();
    const phase2 = normalizeRecoveryPhase(options.phase, detail ? "failed" : "maintenance");
    if (phase2 === "recovering") {
      state2.onlineSessionState.value = "recovering";
    } else if (phase2 === "need_login") {
      state2.onlineSessionState.value = "needs_login";
    } else if (phase2 === "failed") {
      state2.onlineSessionState.value = state2.studentId.value ? "cached_offline" : "unknown";
    }
    state2.jwxtMaintenanceMode.value = true;
    state2.jwxtLastCheckTime.value = formatCheckTime();
    state2.jwxtMaintenanceHint.value = hint || "教务系统正在维护或暂时不可用，当前为缓存数据。";
    state2.jwxtMaintenanceDetail.value = detail;
    state2.jwxtRecoveryPhase.value = phase2;
    if (detail) state2.jwxtSessionLastError.value = detail;
    try {
      localStorage.setItem(JWXT_MAINTENANCE_KEY, "1");
      localStorage.setItem(JWXT_MAINTENANCE_TIME_KEY, String(Date.now()));
      if (state2.jwxtMaintenanceHint.value) {
        localStorage.setItem(JWXT_MAINTENANCE_HINT_KEY, state2.jwxtMaintenanceHint.value);
      }
      if (detail) {
        localStorage.setItem(JWXT_MAINTENANCE_DETAIL_KEY, detail.slice(0, 800));
      }
      localStorage.setItem(JWXT_MAINTENANCE_PHASE_KEY, phase2);
    } catch {
    }
  };
  const clearJwxtMaintenance = () => {
    if (state2.studentId.value && state2.onlineSessionState.value !== "unknown") {
      state2.onlineSessionState.value = "online";
    } else if (!state2.studentId.value) {
      state2.onlineSessionState.value = "unknown";
    }
    state2.jwxtMaintenanceMode.value = false;
    state2.jwxtMaintenanceHint.value = "";
    state2.jwxtLastCheckTime.value = "";
    state2.jwxtMaintenanceDetail.value = "";
    state2.jwxtSessionLastError.value = "";
    state2.jwxtRecoveryPhase.value = "idle";
    try {
      localStorage.removeItem(JWXT_MAINTENANCE_KEY);
      localStorage.removeItem(JWXT_MAINTENANCE_TIME_KEY);
      localStorage.removeItem(JWXT_MAINTENANCE_HINT_KEY);
      localStorage.removeItem(JWXT_MAINTENANCE_DETAIL_KEY);
      localStorage.removeItem(JWXT_MAINTENANCE_PHASE_KEY);
    } catch {
    }
  };
  const notifySessionOnline = (source = "recovery") => {
    if (!state2.studentId.value) return;
    state2.onlineSessionState.value = "online";
    clearJwxtMaintenance();
    window.dispatchEvent(new CustomEvent("hbu-session-online", {
      detail: {
        studentId: state2.studentId.value,
        source
      }
    }));
  };
  const syncJwxtMaintenanceFromStorage = () => {
    const active = localStorage.getItem(JWXT_MAINTENANCE_KEY) === "1";
    if (!active) {
      state2.jwxtMaintenanceMode.value = false;
      state2.jwxtMaintenanceHint.value = "";
      state2.jwxtLastCheckTime.value = "";
      state2.jwxtMaintenanceDetail.value = "";
      state2.jwxtRecoveryPhase.value = "idle";
      return;
    }
    state2.jwxtMaintenanceMode.value = true;
    const hint = String(localStorage.getItem(JWXT_MAINTENANCE_HINT_KEY) || "").trim();
    state2.jwxtMaintenanceHint.value = hint || "教务系统正在维护或暂时不可用，当前为缓存数据。";
    state2.jwxtMaintenanceDetail.value = String(localStorage.getItem(JWXT_MAINTENANCE_DETAIL_KEY) || "").trim();
    state2.jwxtRecoveryPhase.value = normalizeRecoveryPhase(
      localStorage.getItem(JWXT_MAINTENANCE_PHASE_KEY),
      "maintenance"
    );
    const ts = Number(localStorage.getItem(JWXT_MAINTENANCE_TIME_KEY) || Date.now());
    state2.jwxtLastCheckTime.value = formatCheckTime(ts);
  };
  const handleJwxtMaintenanceEvent = (event) => {
    const detail = event?.detail || {};
    if (detail.active) {
      const hint = String(detail.hint || "").trim();
      markJwxtMaintenance(hint, {
        detail: detail.detail || detail.error || "",
        phase: detail.phase || "maintenance"
      });
      if (state2.studentId.value && !isManualLogout()) {
        startJwxtRecoveryPolling();
      }
      return;
    }
    clearJwxtMaintenance();
    stopJwxtRecoveryPolling();
  };
  const formatSessionError = (err) => {
    const raw = String(err?.message || err || "").trim();
    if (!raw) return "";
    const firstLine = raw.split(/[\r\n]/)[0] || raw;
    return firstLine.length > 280 ? `${firstLine.slice(0, 280)}…` : firstLine;
  };
  const handleRetrySessionRecovery = async () => {
    if (isManualLogout() || !state2.studentId.value) {
      runtime.auth.handleRequireLogin();
      return;
    }
    state2.jwxtRecoveryPhase.value = "recovering";
    markJwxtMaintenance("正在后台恢复会话…", {
      phase: "recovering",
      detail: state2.jwxtSessionLastError.value || ""
    });
    try {
      const ok = await attemptOnlineRecovery({ silent: false });
      if (!ok) {
        const hasPortal = !!await getStoredPassword().catch(() => null);
        const hasCx = !!await getStoredChaoxingPassword().catch(() => null);
        if (!hasPortal && !hasCx) {
          markJwxtMaintenance("会话已过期，本地未保存密码，请手动登录融合门户。", {
            phase: "need_login",
            detail: state2.jwxtSessionLastError.value || "无可用记住密码"
          });
        } else {
          markJwxtMaintenance(sessionFailureHint("自动登录未成功，将继续在后台重试。当前展示缓存数据。"), {
            phase: "failed",
            detail: state2.jwxtSessionLastError.value || "恢复失败"
          });
          startJwxtRecoveryPolling();
        }
      }
    } catch (e) {
      state2.jwxtSessionLastError.value = formatSessionError(e);
      markJwxtMaintenance("恢复会话时出错，请稍后重试或手动登录。", {
        phase: "failed",
        detail: state2.jwxtSessionLastError.value
      });
    }
  };
  return {
    restoreCachedIdentityFromLocal,
    tryRestoreSession,
    tryRestoreLatestSession,
    attemptAutoRelogin,
    attemptOnlineRecovery,
    refreshSessionSilently,
    refreshSessionVerified,
    persistSessionCookies,
    startSessionKeepAlive,
    stopSessionKeepAlive,
    startElectricityKeepAlive,
    stopElectricityKeepAlive,
    markLoginSessionToken,
    isTemporaryLoginSession,
    isManualLogout,
    restoreTestAccountSession,
    ensureConfigAccess,
    startJwxtRecoveryPolling,
    stopJwxtRecoveryPolling,
    markJwxtMaintenance,
    clearJwxtMaintenance,
    syncJwxtMaintenanceFromStorage,
    handleJwxtMaintenanceEvent,
    handleRetrySessionRecovery,
    notifySessionOnline,
    formatSessionError
  };
};
const createAuthCoordinator = (runtime) => {
  const { state: state2 } = runtime;
  const hasTauri2 = isTauriRuntime();
  const clearTestAccountRuntimeCaches = () => {
    clearCacheByPrefix(`grades:${TEST_ACCOUNT.studentId}`);
    clearCacheByPrefix(`schedule:${TEST_ACCOUNT.studentId}`);
    clearCacheByPrefix(`classroom:${TEST_ACCOUNT.studentId}`);
    clearCacheByPrefix(`studentinfo:${TEST_ACCOUNT.studentId}`);
    clearCacheByPrefix(`student_info:${TEST_ACCOUNT.studentId}`);
    clearCacheByPrefix(`exams:${TEST_ACCOUNT.studentId}`);
    clearCacheByPrefix(`ranking:${TEST_ACCOUNT.studentId}`);
    clearCacheByPrefix(`calendar:${TEST_ACCOUNT.studentId}`);
    clearCacheByPrefix(`academic:${TEST_ACCOUNT.studentId}`);
    clearCacheByPrefix(`training:options:${TEST_ACCOUNT.studentId}`);
    clearCacheByPrefix(`training:jys:${TEST_ACCOUNT.studentId}`);
    clearCacheByPrefix(`electricity:${TEST_ACCOUNT.studentId}`);
  };
  const handleLoginSuccess = (data) => {
    state2.gradeData.value = data;
    state2.studentId.value = localStorage.getItem("hbu_username") || "";
    state2.gradeTeacherCache.value = null;
    state2.gradeTeacherCacheSid.value = state2.studentId.value;
    if (state2.studentId.value) {
      try {
        window.dispatchEvent(new CustomEvent("hbu-identity-login-resumed", {
          detail: { studentId: state2.studentId.value }
        }));
      } catch {
      }
    }
    runtime.navigation.applyViewState("home");
    runtime.navigation.replaceHistorySnapshot("home");
    if (state2.studentId.value) {
      runtime.session.markLoginSessionToken();
      if (isTestAccountSession()) {
        seedTestAccountCaches(setCachedData, state2.studentId.value);
        state2.gradeData.value = getTestAccountGrades();
      }
      void fetchWithCache(`training:options:${state2.studentId.value}`, async () => {
        const res = await axiosInstance.post(`${API_BASE$2}/v2/training_plan/options`, {
          student_id: state2.studentId.value
        });
        if (res.data?.success && !isTestAccountSession()) {
          localStorage.setItem("hbu_training_options", JSON.stringify({
            options: res.data.options || {},
            defaults: res.data.defaults || {}
          }));
        }
        return res.data;
      });
    }
    localStorage.removeItem("hbu_manual_logout");
    localStorage.removeItem(LOGOUT_REASON_KEY);
    if (!isTestAccountSession()) {
      void runtime.session.persistSessionCookies();
      runtime.session.startSessionKeepAlive();
      runtime.session.startElectricityKeepAlive();
      if (state2.studentId.value) {
        void ensureRememberedPasswordCached(state2.studentId.value).catch((e) => {
          console.warn("[Session] 登录后缓存记住密码失败:", e);
        });
        startNotificationMonitor({ studentId: state2.studentId.value }).catch((e) => {
          console.warn("[Notify] 启动通知监控失败:", e);
        });
        void reconcileLocalReminders({
          studentId: state2.studentId.value,
          reason: "login"
        }).catch((e) => {
          console.warn("[Reminder] 登录后预调度 reconcile 失败:", e);
        });
        resetCloudSyncCooldownForSession(state2.studentId.value);
        runAutoCloudSyncAfterLogin({
          studentId: state2.studentId.value,
          latestGrades: Array.isArray(data) ? data : []
        }).catch((e) => {
          console.warn("[CloudSync] 登录后自动同步失败:", e);
        });
        setUsageTrackingStudentId(state2.studentId.value);
        initUsageTracker({ studentId: state2.studentId.value });
        scheduleUsageUpload({ studentId: state2.studentId.value, reason: "login", force: true });
      }
    }
    runtime.session.clearJwxtMaintenance();
    runtime.session.stopJwxtRecoveryPolling();
    if (!isTestAccountSession() && hasTauri2) {
      window.setTimeout(() => {
        void runtime.session.refreshSessionSilently({ quiet: true });
      }, 2500);
    }
    runtime.lifecycle.recoverViewportAfterTransition();
  };
  const handleLogout = async (options = {}) => {
    const payload = options && typeof options === "object" ? options : {};
    const manual = payload.manual !== false;
    const reason = String(payload.reason || "").trim();
    const notice = String(payload.notice || "").trim();
    const logoutSid = String(state2.studentId.value || localStorage.getItem("hbu_username") || "").trim();
    const wasTestAccountSession = isTestAccountSession();
    if (!wasTestAccountSession) {
      try {
        await preservePortalRememberedPasswordOnLogout();
      } catch (e) {
        console.warn("[Session] 退出前同步记住密码失败:", e);
      }
    }
    if (manual && logoutSid) {
      clearUserScopedCaches(logoutSid);
      clearScheduleRenderSnapshot(logoutSid);
      window.dispatchEvent(new CustomEvent("hbu-session-logout", {
        detail: { studentId: logoutSid, manual: true }
      }));
      void clearRemindersForLogout(logoutSid).catch((e) => {
        console.warn("[Reminder] 登出清理预调度提醒失败:", e);
      });
    }
    runtime.navigation.applyViewState("home");
    state2.gradeData.value = [];
    state2.gradeTeacherCache.value = null;
    state2.gradeTeacherCacheSid.value = "";
    state2.studentId.value = "";
    state2.userUuid.value = "";
    runtime.navigation.replaceHistorySnapshot("home");
    runtime.session.stopSessionKeepAlive();
    runtime.session.stopElectricityKeepAlive();
    void stopNotificationMonitor();
    runtime.session.stopJwxtRecoveryPolling();
    runtime.session.clearJwxtMaintenance();
    localStorage.removeItem(SESSION_COOKIE_KEY);
    localStorage.removeItem(SESSION_COOKIE_TIME_KEY);
    localStorage.removeItem(LOGIN_SESSION_TOKEN_KEY$1);
    localStorage.removeItem(LOGIN_METHOD_KEY);
    localStorage.removeItem(LOGIN_TEMP_FLAG_KEY);
    localStorage.removeItem(SCHEDULE_POPUP_PENDING_KEY);
    localStorage.removeItem(SCHEDULE_SWITCH_PENDING_KEY);
    clearDailyAccessGrant();
    if (reason) {
      localStorage.setItem(LOGOUT_REASON_KEY, reason);
    } else {
      localStorage.removeItem(LOGOUT_REASON_KEY);
    }
    if (manual) {
      localStorage.setItem("hbu_manual_logout", "true");
    } else {
      localStorage.removeItem("hbu_manual_logout");
    }
    if (wasTestAccountSession) {
      clearTestAccountRuntimeCaches();
      clearTestAccountSession();
    } else {
      invokeNative("logout").catch(() => {
      });
    }
    if (notice) {
      window.setTimeout(() => {
        window.alert(notice);
      }, 80);
    }
    if (!wasTestAccountSession) {
      clearWidgetForLogout().catch(() => {
      });
    }
    runtime.notification.stopWidgetCrossDayTimer();
  };
  const handleSwitchLoginMode = (mode) => {
    state2.loginMode.value = mode;
    localStorage.setItem(LOGIN_METHOD_VIEW_KEY, mode);
  };
  const handleRequireLogin = () => {
    state2.showLoginPrompt.value = true;
    setTimeout(() => {
      state2.showLoginPrompt.value = false;
    }, 2200);
  };
  return {
    handleLoginSuccess,
    handleLogout,
    handleSwitchLoginMode,
    handleRequireLogin
  };
};
const STORAGE_KEY = "campus_network_settings_v1";
const CAMPUS_CARRIER_OPTIONS = [
  { id: "campus", label: "校园网", hint: "默认运营商" },
  { id: "cmcc", label: "移动", hint: "YD / @cmcc" },
  { id: "cucc", label: "联通", hint: "LT / @cucc" },
  { id: "ctcc", label: "电信", hint: "DX / @ctcc" }
];
const DEFAULT_CAMPUS_NETWORK_SETTINGS = {
  carrier: "campus",
  auto_login: false,
  remember_password: true,
  gateway_override: "",
  show_advanced: false,
  last_status: "",
  last_message: "",
  last_probe_at: 0,
  last_login_at: 0,
  last_login_success: false
};
const HBUT_CAMPUS_GATEWAYS = [
  "http://172.16.54.18",
  "http://202.114.177.246",
  "http://202.114.177.113",
  "http://202.114.177.114",
  "http://202.114.177.115"
];
const normalizeSettings = (raw) => ({
  ...DEFAULT_CAMPUS_NETWORK_SETTINGS,
  ...raw || {},
  carrier: ["campus", "cmcc", "cucc", "ctcc"].includes(raw?.carrier) ? raw.carrier : "campus"
});
const readCampusNetworkSettings = () => {
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    if (!raw) return { ...DEFAULT_CAMPUS_NETWORK_SETTINGS };
    return normalizeSettings(JSON.parse(raw));
  } catch {
    return { ...DEFAULT_CAMPUS_NETWORK_SETTINGS };
  }
};
const writeCampusNetworkSettings = (patch) => {
  const next = normalizeSettings({ ...readCampusNetworkSettings(), ...patch });
  localStorage.setItem(STORAGE_KEY, JSON.stringify(next));
  return next;
};
const campusStatusLabel = (status) => {
  switch (status) {
    case "authenticated":
      return "已连接";
    case "needs_auth":
      return "需要认证";
    case "checking":
      return "检测中";
    case "error":
      return "失败";
    default:
      return "未知";
  }
};
const FAIL_DEBOUNCE_MS = 10 * 60 * 1e3;
const SUCCESS_SKIP_MS = 30 * 60 * 1e3;
let autoLoginInFlight = false;
let lastAutoAttemptAt = 0;
let lastAutoSuccessAt = 0;
let lastAutoFailAt = 0;
const mapProbeStatus = (status) => {
  const normalized = String(status || "").trim().toLowerCase();
  if (normalized === "authenticated") return "authenticated";
  if (normalized === "needs_auth") return "needs_auth";
  if (normalized === "checking") return "checking";
  if (normalized === "error") return "error";
  return "unknown";
};
const probeCampusNetwork = async (gatewayOverride = "") => {
  if (!isTauriRuntime()) {
    return {
      status: "unknown",
      message: "当前运行环境不支持校园网探测"
    };
  }
  try {
    const result = await invokeNative("campus_network_probe", {
      gatewayOverride: gatewayOverride || null
    });
    const status = mapProbeStatus(result?.status);
    writeCampusNetworkSettings({
      last_status: status,
      last_message: String(result?.message || ""),
      last_probe_at: Date.now()
    });
    return { ...result, status };
  } catch (error) {
    const message = error instanceof Error ? error.message : String(error);
    writeCampusNetworkSettings({
      last_status: "error",
      last_message: message,
      last_probe_at: Date.now()
    });
    return { status: "error", message };
  }
};
const loginCampusNetwork = async (options) => {
  if (!isTauriRuntime()) {
    return { success: false, message: "当前运行环境不支持校园网认证" };
  }
  const studentId = String(options.studentId || "").trim();
  const password = String(options.password || "");
  if (!studentId || !password) {
    return { success: false, message: "请填写学号和密码" };
  }
  try {
    const result = await invokeNative("campus_network_login", {
      studentId,
      password,
      carrier: options.carrier,
      gatewayOverride: options.gatewayOverride || null,
      queryString: options.queryString || null
    });
    writeCampusNetworkSettings({
      last_login_at: Date.now(),
      last_login_success: Boolean(result?.success),
      last_status: result?.success ? "authenticated" : "error",
      last_message: String(result?.message || "")
    });
    if (options.rememberPassword !== false) {
      const settings = readCampusNetworkSettings();
      if (settings.remember_password) {
        await saveRememberedCredential(buildCampusAccountKey(studentId), password);
      }
    }
    return result;
  } catch (error) {
    const message = error instanceof Error ? error.message : String(error);
    writeCampusNetworkSettings({
      last_login_at: Date.now(),
      last_login_success: false,
      last_status: "error",
      last_message: message
    });
    return { success: false, message };
  }
};
const shouldSkipAutoLogin = () => {
  const now = Date.now();
  if (autoLoginInFlight) return true;
  if (now - lastAutoAttemptAt < 15e3) return true;
  if (lastAutoSuccessAt && now - lastAutoSuccessAt < SUCCESS_SKIP_MS) return true;
  if (lastAutoFailAt && now - lastAutoFailAt < FAIL_DEBOUNCE_MS) return true;
  return false;
};
const runCampusNetworkAutoLogin = async (options = {}) => {
  if (!getFeaturePolicy().campusNetwork) {
    return null;
  }
  const settings = readCampusNetworkSettings();
  if (!settings.auto_login) return null;
  if (!isTauriRuntime()) return null;
  if (shouldSkipAutoLogin()) return null;
  const studentId = String(options.studentId || localStorage.getItem("hbu_username") || "").trim();
  if (!studentId) return null;
  const password = await loadRememberedCredential(buildCampusAccountKey(studentId));
  if (!password) return null;
  autoLoginInFlight = true;
  lastAutoAttemptAt = Date.now();
  try {
    const probe = await probeCampusNetwork(settings.gateway_override);
    if (!probe || probe.status === "authenticated") {
      if (probe?.status === "authenticated") {
        lastAutoSuccessAt = Date.now();
      }
      return null;
    }
    if (probe.status !== "needs_auth" && probe.status !== "unknown") {
      return null;
    }
    const result = await loginCampusNetwork({
      studentId,
      password,
      carrier: settings.carrier,
      gatewayOverride: settings.gateway_override,
      queryString: probe.query_string || void 0,
      rememberPassword: true
    });
    if (result.success) {
      lastAutoSuccessAt = Date.now();
    } else {
      lastAutoFailAt = Date.now();
    }
    return result;
  } finally {
    autoLoginInFlight = false;
  }
};
const VIEW_HEALTH_SELECTOR_MAP = Object.freeze({
  home: ".dashboard-root",
  schedule: ".schedule-view",
  classroom: ".classroom-view",
  chaoxing_hub: ".cx-hub",
  more_module_host: ".more-module-host-view",
  school_website: ".school-website-view",
  more: ".more-view",
  me: ".me-view"
});
const createLifecycleCoordinator = (runtime) => {
  const { state: state2, stores } = runtime;
  const isIOSLike$1 = isIOSLike();
  const isAndroidLike$1 = isAndroidLike();
  const isDesktopLike$1 = isDesktopLike();
  const hasTauri2 = isTauriRuntime();
  const isCapacitor = isCapacitorRuntime();
  const forceScrollTop = () => {
    try {
      window.scrollTo(0, 0);
      document.documentElement.scrollTop = 0;
      document.body.scrollTop = 0;
      if (state2.appShellRef.value) {
        state2.appShellRef.value.scrollTop = 0;
      }
    } catch {
    }
  };
  const updateViewportUnit = () => {
    if (typeof window === "undefined") return;
    const viewportHeight = document.documentElement.clientHeight || window.innerHeight || window.visualViewport?.height;
    if (!viewportHeight) return;
    const nextVh = viewportHeight * 0.01;
    const prevVh = Number.parseFloat(document.documentElement.style.getPropertyValue("--app-vh"));
    if (Number.isFinite(prevVh) && Math.abs(prevVh - nextVh) < 0.1) return;
    document.documentElement.style.setProperty("--app-vh", `${nextVh}px`);
  };
  const readCssSafeAreaBottom = () => {
    if (typeof document === "undefined" || !document.body) return 0;
    const probe = document.createElement("div");
    probe.style.cssText = [
      "position:fixed",
      "visibility:hidden",
      "pointer-events:none",
      "padding-bottom:env(safe-area-inset-bottom, 0px)"
    ].join(";");
    document.body.appendChild(probe);
    const safeBottom = Number.parseFloat(window.getComputedStyle(probe).paddingBottom) || 0;
    probe.remove();
    return safeBottom;
  };
  const updateNativeSafeAreaFallback = () => {
    if (typeof window === "undefined" || typeof document === "undefined") return;
    const root = document.documentElement;
    const shouldUseNativeFallback = isIOSLike$1 && (hasTauri2 || isCapacitor);
    if (!shouldUseNativeFallback || readCssSafeAreaBottom() > 0) {
      root.style.setProperty("--app-safe-bottom-fallback", "0px");
      return;
    }
    const screenWidth = Math.min(window.screen?.width || 0, window.screen?.height || 0);
    const screenHeight = Math.max(window.screen?.width || 0, window.screen?.height || 0);
    const portraitFallback = screenWidth >= 744 && screenHeight >= 1024 ? 20 : 34;
    const fallback = screenHeight >= 812 ? portraitFallback : 0;
    root.style.setProperty("--app-safe-bottom-fallback", `${fallback}px`);
  };
  const recoverViewportAfterTransition = ({ scrollToTop = true, blurActive = true } = {}) => {
    const activeEl = document.activeElement;
    if (blurActive && activeEl && typeof activeEl.blur === "function") {
      activeEl.blur();
    }
    updateViewportUnit();
    nextTick(() => {
      if (scrollToTop) {
        forceScrollTop();
      }
      requestAnimationFrame(() => {
        if (scrollToTop) {
          forceScrollTop();
        }
        updateViewportUnit();
      });
    });
  };
  const isCurrentViewDomHealthy = (view = state2.currentView.value) => {
    try {
      const root = state2.appShellRef.value || document.querySelector(".app-shell");
      if (!root) return false;
      const transitionRoot = root.querySelector(".view-transition-root");
      if (!transitionRoot) return false;
      const leaving = transitionRoot.querySelector(
        ".v-leave-active, .v-enter-active, .module-fade-leave-active, .module-fade-enter-active, .module-fade-fwd-leave-active, .module-fade-fwd-enter-active, .module-fade-back-leave-active, .module-fade-back-enter-active"
      );
      if (leaving) return true;
      const expectedSelector = VIEW_HEALTH_SELECTOR_MAP[normalizeViewName(view)];
      if (expectedSelector) {
        const el = transitionRoot.querySelector(expectedSelector);
        if (!el) return false;
        try {
          const rect = el.getBoundingClientRect();
          if (rect.width <= 0 && rect.height <= 0) return false;
        } catch {
        }
        return true;
      }
      return transitionRoot.childElementCount > 0;
    } catch {
      return true;
    }
  };
  const maybeHardReloadAfterResume = (targetView, { idleMs = 0 } = {}) => {
    if (!isIOSLike$1) return false;
    if (state2.mutable.iosHardReloadCount >= IOS_HARD_RELOAD_MAX_PER_SESSION) return false;
    if (idleMs < IOS_RESUME_HARD_RELOAD_MS) return false;
    if (isCurrentViewDomHealthy(targetView)) return false;
    const now = Date.now();
    if (now - state2.mutable.iosReloadFallbackAt < IOS_RELOAD_MIN_INTERVAL_MS) return false;
    state2.mutable.iosReloadFallbackAt = now;
    state2.mutable.iosHardReloadCount += 1;
    try {
      console.warn("[Lifecycle#451] hard reload fallback", {
        view: targetView,
        idleMs,
        count: state2.mutable.iosHardReloadCount
      });
    } catch {
    }
    try {
      window.location.reload();
    } catch {
    }
    return true;
  };
  const nudgeWebViewPaint = (targetView = state2.currentView.value, { verify = false, allowReload = false, idleMs = 0 } = {}) => {
    const root = document.getElementById("app");
    if (!root) return;
    try {
      root.style.opacity = "0.999";
      root.style.transform = "translateZ(0)";
      requestAnimationFrame(() => {
        try {
          root.style.opacity = "1";
          root.style.transform = "";
        } catch {
        }
      });
    } catch {
    }
    if (!verify) return;
    setTimeout(() => {
      if (isCurrentViewDomHealthy(targetView)) return;
      setTimeout(() => {
        if (isCurrentViewDomHealthy(targetView)) return;
        if (!allowReload) return;
        maybeHardReloadAfterResume(targetView, { idleMs });
      }, 400);
    }, 800);
  };
  const scheduleViewportUpdate = () => {
    if (!isIOSLike$1 && !isAndroidLike$1) {
      const hasVh = !!document.documentElement.style.getPropertyValue("--app-vh");
      updateNativeSafeAreaFallback();
      if (!hasVh) updateViewportUnit();
      return;
    }
    updateNativeSafeAreaFallback();
    updateViewportUnit();
  };
  const markDesktopWindowResizing = () => {
    if (!isDesktopLike$1 || typeof document === "undefined") return;
    const root = document.documentElement;
    root.classList.add("window-resizing");
    if (state2.mutable.desktopResizePerfTimer) {
      window.clearTimeout(state2.mutable.desktopResizePerfTimer);
    }
    state2.mutable.desktopResizePerfTimer = window.setTimeout(() => {
      root.classList.remove("window-resizing");
      state2.mutable.desktopResizePerfTimer = null;
    }, 180);
  };
  const handleViewportResize = () => {
    markDesktopWindowResizing();
    if (state2.mutable.viewportResizeRaf) return;
    state2.mutable.viewportResizeRaf = window.requestAnimationFrame(() => {
      state2.mutable.viewportResizeRaf = 0;
      scheduleViewportUpdate();
    });
  };
  const recoverEmbeddedWebAfterResume = async (targetView, idleMs = 0) => {
    try {
      const {
        recoverSchoolWebsiteBridgeOnResume,
        invokeEnsureHttpBridge
      } = await __vitePreload(async () => {
        const {
          recoverSchoolWebsiteBridgeOnResume: recoverSchoolWebsiteBridgeOnResume2,
          invokeEnsureHttpBridge: invokeEnsureHttpBridge2
        } = await import("./school_website_embed-CYHxPvBP.js");
        return {
          recoverSchoolWebsiteBridgeOnResume: recoverSchoolWebsiteBridgeOnResume2,
          invokeEnsureHttpBridge: invokeEnsureHttpBridge2
        };
      }, true ? __vite__mapDeps([66,1,0]) : void 0, import.meta.url);
      let ensureResult = null;
      try {
        ensureResult = await invokeEnsureHttpBridge();
      } catch {
        ensureResult = null;
      }
      const bridgeOk = await recoverSchoolWebsiteBridgeOnResume();
      if (idleMs >= 8e3 || !bridgeOk || targetView === "school_website" || targetView === "more_module_host") {
        window.dispatchEvent(new CustomEvent("hbu-embed-resume", {
          detail: {
            view: targetView,
            bridgeOk,
            idleMs,
            source: "app-resume",
            ensureStatus: ensureResult?.status || null,
            bridgeEnabled: ensureResult?.enabled !== false,
            // 明确降级信号：bridge 仍不可用时宿主应展示可操作 fallback（重试/外开）
            forceFallback: !bridgeOk && idleMs >= 8e3
          }
        }));
      }
    } catch {
    }
  };
  const handleAppResume = (source = "visibilitychange") => {
    if (!state2.mutable.appBootstrapped || document.hidden) return;
    const now = Date.now();
    if (now - state2.mutable.lastResumeHandledAt < 320) return;
    state2.mutable.lastResumeHandledAt = now;
    const idle = stores.lifecycle.consumeHiddenDuration(now);
    const snapshot = state2.mutable.resumePendingSnapshot || runtime.navigation.readWindowRouteSnapshot() || runtime.navigation.collectCurrentViewSnapshot();
    state2.mutable.resumePendingSnapshot = null;
    scheduleViewportUpdate();
    const targetView = normalizeViewName(snapshot?.view || snapshot?.module || state2.currentView.value);
    const softRemount = isIOSLike$1 && idle >= IOS_RESUME_SOFT_REMOUNT_MS && !isCurrentViewDomHealthy(targetView);
    const allowHardReload = isIOSLike$1 && idle >= IOS_RESUME_HARD_RELOAD_MS;
    if (isIOSLike$1 && !softRemount) {
      nudgeWebViewPaint(targetView, { verify: false, allowReload: false, idleMs: idle });
    }
    void runtime.navigation.restoreViewFromSnapshot(snapshot, {
      softRemount,
      allowHardReload,
      idleMs: idle,
      source
    });
    void recoverEmbeddedWebAfterResume(targetView, idle);
    if (state2.studentId.value) {
      runtime.notification.scheduleWidgetCrossDayTimer();
      void __vitePreload(() => Promise.resolve().then(() => local_reminder_scheduler), true ? void 0 : void 0, import.meta.url).then(
        (mod) => mod.reconcileLocalReminders({
          studentId: state2.studentId.value,
          reason: "resume"
        }).catch(() => {
        })
      ).catch(() => {
      });
      void __vitePreload(() => Promise.resolve().then(() => background_notification), true ? void 0 : void 0, import.meta.url).then(
        (mod) => mod.consumeBackgroundEventsOnce({
          studentId: state2.studentId.value,
          reason: source
        }).catch((error) => {
          console.warn("[BackgroundInbox] consume failed:", error);
        })
      ).catch(() => {
      });
    }
    void runCampusNetworkAutoLogin({
      studentId: state2.studentId.value
    }).catch((error) => {
      console.warn("[CampusNetwork] auto login failed:", error);
    });
  };
  const handleVisibilityChange = () => {
    if (document.hidden) {
      stores.lifecycle.markHidden(Date.now());
      state2.mutable.resumePendingSnapshot = runtime.navigation.readWindowRouteSnapshot() || runtime.navigation.collectCurrentViewSnapshot();
      return;
    }
    handleAppResume("visibilitychange");
  };
  const handlePageShow = () => {
    handleAppResume("pageshow");
  };
  const handleWindowFocus = () => {
    handleAppResume("focus");
  };
  const registerResumeListeners = () => {
    document.addEventListener("visibilitychange", handleVisibilityChange);
    window.addEventListener("pageshow", handlePageShow);
    window.addEventListener("focus", handleWindowFocus);
    window.addEventListener("resize", handleViewportResize);
    window.addEventListener("orientationchange", handleViewportResize);
    return () => {
      document.removeEventListener("visibilitychange", handleVisibilityChange);
      window.removeEventListener("pageshow", handlePageShow);
      window.removeEventListener("focus", handleWindowFocus);
      window.removeEventListener("resize", handleViewportResize);
      window.removeEventListener("orientationchange", handleViewportResize);
    };
  };
  const installCapacitorStateListener = () => {
    if (!isCapacitor) return;
    void __vitePreload(() => import("./runtime-bridge-BU9jeOXP.js").then((n) => n.a7), true ? __vite__mapDeps([1,0]) : void 0, import.meta.url).then((mod) => {
      mod.App.addListener("appStateChange", ({ isActive }) => {
        if (!isActive) {
          stores.lifecycle.markHidden(Date.now());
          state2.mutable.resumePendingSnapshot = runtime.navigation.readWindowRouteSnapshot() || runtime.navigation.collectCurrentViewSnapshot();
          return;
        }
        handleAppResume("capacitor-appStateChange");
      }).then((handle) => {
        state2.mutable.capacitorAppStateListener = handle;
      }).catch(() => {
      });
    }).catch(() => {
    });
  };
  const dispose = () => {
    removeResumeListeners();
    if (state2.mutable.viewportResizeRaf) {
      window.cancelAnimationFrame(state2.mutable.viewportResizeRaf);
      state2.mutable.viewportResizeRaf = 0;
    }
    if (state2.mutable.desktopResizePerfTimer) {
      window.clearTimeout(state2.mutable.desktopResizePerfTimer);
      state2.mutable.desktopResizePerfTimer = null;
    }
    if (typeof document !== "undefined") {
      document.documentElement.classList.remove("window-resizing");
    }
    if (state2.mutable.capacitorAppStateListener) {
      state2.mutable.capacitorAppStateListener.remove().catch(() => {
      });
      state2.mutable.capacitorAppStateListener = null;
    }
    if (typeof state2.mutable.removeHomeLayoutDiagnosticsErrorCapture === "function") {
      state2.mutable.removeHomeLayoutDiagnosticsErrorCapture();
      state2.mutable.removeHomeLayoutDiagnosticsErrorCapture = null;
    }
  };
  let removeResumeListeners = () => {
  };
  return {
    handleAppResume,
    handleVisibilityChange,
    handlePageShow,
    handleWindowFocus,
    handleViewportResize,
    scheduleViewportUpdate,
    recoverViewportAfterTransition,
    isCurrentViewDomHealthy,
    nudgeWebViewPaint,
    installResumeListeners: () => {
      removeResumeListeners = registerResumeListeners();
    },
    installCapacitorStateListener,
    dispose
  };
};
const createGradeCoordinator = (runtime) => {
  const { state: state2 } = runtime;
  const hasTauri2 = isTauriRuntime();
  const invoke2 = (command, args) => invokeNative(command, args);
  const fetchGradesRemote = async (sid, { teacherCurrentOnly = false } = {}) => {
    const res = await axiosInstance.post(`${API_BASE$2}/v2/quick_fetch`, {
      student_id: sid,
      teacher_current_only: teacherCurrentOnly
    });
    return res.data;
  };
  const normalizeGradeTeacherKey = (value) => String(value ?? "").trim();
  const gradeTeacherKeys = (grade = {}) => {
    const keys = [
      normalizeGradeTeacherKey(grade.kcbh),
      normalizeGradeTeacherKey(grade.course_code),
      normalizeGradeTeacherKey(grade.courseCode),
      normalizeGradeTeacherKey(grade.grade_id),
      normalizeGradeTeacherKey(grade.gradeId)
    ].filter(Boolean);
    return [...new Set(keys)];
  };
  const mergeGradeTeacherCache = (grades = [], cachePayload = state2.gradeTeacherCache.value) => {
    if (!Array.isArray(grades) || !cachePayload || typeof cachePayload !== "object") {
      return Array.isArray(grades) ? grades : [];
    }
    const byKcbh = cachePayload.by_kcbh && typeof cachePayload.by_kcbh === "object" ? cachePayload.by_kcbh : {};
    if (!Object.keys(byKcbh).length) return grades;
    let changed = false;
    const merged = grades.map((grade) => {
      if (!grade || typeof grade !== "object") return grade;
      const record = grade;
      const currentTeacher = String(record.course_teacher ?? record.courseTeacher ?? "").trim();
      if (currentTeacher) return grade;
      for (const key of gradeTeacherKeys(record)) {
        const teacher = String(byKcbh[key] ?? "").trim();
        if (teacher) {
          changed = true;
          return { ...record, course_teacher: teacher };
        }
      }
      return grade;
    });
    return changed ? merged : grades;
  };
  const refreshGradeTeacherCache = async ({ currentOnly = false } = {}) => {
    const sid = String(state2.studentId.value || "").trim();
    if (!sid || !hasTauri2) return null;
    try {
      const payload = currentOnly ? await invoke2("sync_grade_teachers_current_semester") : await invoke2("get_grade_teacher_cache");
      if (payload?.success !== false) {
        state2.gradeTeacherCache.value = payload;
        state2.gradeTeacherCacheSid.value = sid;
        if (Array.isArray(state2.gradeData.value) && state2.gradeData.value.length > 0) {
          state2.gradeData.value = mergeGradeTeacherCache(state2.gradeData.value, payload);
        }
      }
      return payload;
    } catch (error) {
      console.warn("[Grades] 任课教师缓存刷新失败:", error);
      return null;
    }
  };
  const scheduleGradeTeacherCacheRefresh = () => {
    if (state2.mutable.gradeTeacherRefreshTimer) {
      clearTimeout(state2.mutable.gradeTeacherRefreshTimer);
      state2.mutable.gradeTeacherRefreshTimer = null;
    }
    const delays = [1800, 4200, 8e3];
    let index = 0;
    const run = () => {
      state2.mutable.gradeTeacherRefreshTimer = null;
      if (!state2.studentId.value || state2.currentView.value !== "grades") return;
      void refreshGradeTeacherCache({ currentOnly: false }).finally(() => {
        index += 1;
        if (index < delays.length && state2.currentView.value === "grades") {
          state2.mutable.gradeTeacherRefreshTimer = window.setTimeout(run, delays[index]);
        }
      });
    };
    state2.mutable.gradeTeacherRefreshTimer = window.setTimeout(run, delays[index]);
  };
  const clearGradeRealtimeRetry = () => {
    if (state2.mutable.gradeRealtimeRetryTimer) {
      clearTimeout(state2.mutable.gradeRealtimeRetryTimer);
      state2.mutable.gradeRealtimeRetryTimer = null;
    }
  };
  const scheduleGradeRealtimeRetry = () => {
    clearGradeRealtimeRetry();
    state2.mutable.gradeRealtimeRetryTimer = window.setTimeout(() => {
      state2.mutable.gradeRealtimeRetryTimer = null;
      if (state2.currentView.value === "grades" && state2.gradesOffline.value && state2.studentId.value) {
        void fetchGradesFromAPI(state2.studentId.value, {
          force: true,
          teacherCurrentOnly: true,
          silent: true
        });
      }
    }, GRADE_CACHE_REFRESH_RETRY_MS);
  };
  const resolveGradeSyncTime = (data) => {
    const explicit = String(data?.sync_time || data?.updated_at || data?.timestamp || "").trim();
    if (explicit) return explicit;
    if (data?.offline) return state2.gradesSyncTime.value || "";
    return (/* @__PURE__ */ new Date()).toISOString();
  };
  const applyStaleGradesSnapshot = (sid) => {
    const stale = getStaleCachedData(`grades:${sid}`);
    const data = stale?.data;
    if (!data?.success || !Array.isArray(data.data) || data.data.length === 0) {
      return false;
    }
    return applyGradesPayload(data);
  };
  const applyGradesPayload = (data) => {
    if (data?.success && data?.data) {
      if (state2.gradeTeacherCacheSid.value !== String(state2.studentId.value || "").trim()) {
        state2.gradeTeacherCache.value = null;
      }
      state2.gradeData.value = mergeGradeTeacherCache(data.data);
      state2.gradesOffline.value = !!data.offline;
      state2.gradesSyncTime.value = resolveGradeSyncTime(data);
      if (!data.offline) {
        clearGradeRealtimeRetry();
        void refreshGradeTeacherCache({ currentOnly: false });
        if (data.teacher_enrichment_pending) {
          scheduleGradeTeacherCacheRefresh();
        }
      } else if (state2.currentView.value === "grades") {
        scheduleGradeRealtimeRetry();
      }
      return true;
    }
    return false;
  };
  const fetchGradesFromAPI = async (sid, { force = false, teacherCurrentOnly = false, silent = false } = {}) => {
    if (!sid) return false;
    state2.lastGradeRefreshUsedOffline.value = false;
    clearGradeRealtimeRetry();
    const showedStaleSnapshot = !force ? applyStaleGradesSnapshot(sid) : false;
    if (!silent || showedStaleSnapshot) {
      state2.isLoading.value = true;
    }
    try {
      const { data } = await fetchWithCache(
        `grades:${sid}`,
        () => fetchGradesRemote(sid, { teacherCurrentOnly }),
        void 0,
        { forceRemote: true, priority: "foreground" }
      );
      state2.lastGradeRefreshUsedOffline.value = !!data?.offline;
      if (data?.success && !data.offline) {
        clearCacheByPrefix(`grades:${sid}`);
        setCachedData(`grades:${sid}`, data);
        if (Array.isArray(data.data)) {
          const bySem = {};
          data.data.forEach((item) => {
            const sem = String(item?.term || item?.xnxq || item?.semester || "").trim();
            if (!sem) return;
            if (!Array.isArray(bySem[sem])) bySem[sem] = [];
            bySem[sem].push(item);
          });
          Object.entries(bySem).forEach(([sem, list]) => {
            if (!list.length) return;
            setCachedData(`grades:${sid}:${sem}`, { success: true, data: list });
          });
        }
      }
      return applyGradesPayload(data);
    } catch (e) {
      console.error("获取成绩失败:", e);
    } finally {
      if (!silent || showedStaleSnapshot) {
        state2.isLoading.value = false;
      }
    }
    return false;
  };
  const loadGradesForCurrentView = async (options = {}) => {
    const token = ++state2.mutable.gradeNavigationToken;
    const sid = String(state2.studentId.value || "").trim();
    if (!sid || state2.currentView.value !== "grades") return false;
    const ok = await fetchGradesFromAPI(sid, options);
    if (token !== state2.mutable.gradeNavigationToken || state2.currentView.value !== "grades") {
      return ok;
    }
    if (!ok) {
      showToast("成绩加载失败，请稍后重试", "error");
    }
    return ok;
  };
  const handleRefreshGrades = async () => {
    const ok = await fetchGradesFromAPI(state2.studentId.value, {
      force: true,
      teacherCurrentOnly: true
    });
    if (ok && !state2.lastGradeRefreshUsedOffline.value) {
      void refreshGradeTeacherCache({ currentOnly: true });
    }
    if (ok && state2.lastGradeRefreshUsedOffline.value) {
      showToast("教务系统暂不可用，已显示缓存", "warning");
    } else if (ok) {
      showToast("成绩已刷新", "success");
    } else {
      showToast("成绩刷新失败", "error");
    }
  };
  return {
    loadGradesForCurrentView,
    fetchGradesFromAPI,
    handleRefreshGrades,
    refreshGradeTeacherCache,
    scheduleGradeRealtimeRetry,
    clearGradeRealtimeRetry
  };
};
const MATERIAL_FAMILY = "Material Symbols Outlined";
const MATERIAL_WOFF2 = "fonts/material-symbols-outlined.subset.woff2";
const MATERIAL_CSS = "fonts/material-symbols-outlined.css";
let materialLoadPromise = null;
const resolvePublicAssetUrl = (relativePath) => {
  const raw = String(relativePath || "").replace(/^\//, "");
  const base = String("./");
  const baseNorm = base.endsWith("/") ? base : `${base}/`;
  try {
    return new URL(`${baseNorm}${raw}`, typeof location !== "undefined" ? location.href : "http://localhost/").href;
  } catch {
    return `./${raw}`;
  }
};
const injectStylesheet = (href, id) => {
  if (typeof document === "undefined") return;
  if (document.getElementById(id)) return;
  const link = document.createElement("link");
  link.id = id;
  link.rel = "stylesheet";
  link.href = href;
  document.head.appendChild(link);
};
const ensureMaterialSymbolsFont = () => {
  if (typeof document === "undefined" || typeof FontFace === "undefined") {
    return Promise.resolve(false);
  }
  if (materialLoadPromise) return materialLoadPromise;
  materialLoadPromise = (async () => {
    const cssUrl = resolvePublicAssetUrl(MATERIAL_CSS);
    const fontUrl = resolvePublicAssetUrl(MATERIAL_WOFF2);
    injectStylesheet(cssUrl, "mini-hbut-material-symbols-css");
    try {
      for (const face of [...document.fonts]) {
        if (face.family.replace(/["']/g, "") === MATERIAL_FAMILY && face.status === "error") {
          document.fonts.delete(face);
        }
      }
    } catch {
    }
    try {
      const already = [...document.fonts].some(
        (f) => f.family.replace(/["']/g, "") === MATERIAL_FAMILY && f.status === "loaded"
      );
      if (already) {
        await document.fonts.load(`24px "${MATERIAL_FAMILY}"`).catch(() => {
        });
        return true;
      }
    } catch {
    }
    try {
      const face = new FontFace(MATERIAL_FAMILY, `url(${fontUrl}) format("woff2")`, {
        style: "normal",
        weight: "400",
        display: "block"
      });
      await face.load();
      document.fonts.add(face);
      await document.fonts.load(`24px "${MATERIAL_FAMILY}"`).catch(() => {
      });
      try {
        document.querySelectorAll(".material-symbols-outlined").forEach((el) => {
          const node = el;
          void node.offsetWidth;
        });
      } catch {
      }
      return true;
    } catch (error) {
      console.warn("[icon-fonts] Material Symbols FontFace load failed:", error);
      return false;
    }
  })();
  return materialLoadPromise;
};
const loadLocalIconFonts = () => {
  return Promise.all([
    __vitePreload(() => Promise.resolve({}), true ? __vite__mapDeps([116]) : void 0, import.meta.url),
    __vitePreload(() => Promise.resolve({}), true ? __vite__mapDeps([117]) : void 0, import.meta.url),
    ensureMaterialSymbolsFont()
  ]).then(() => void 0).catch((e) => {
    console.warn("[Bootstrap] local icon fonts load failed:", e);
  });
};
const LOGIN_SESSION_TOKEN_KEY = "hbu_login_session_token";
const isWebsiteDemoBuild = () => String("1") === "1";
const injectWebsiteDemoLayoutStyles = () => {
  if (typeof document === "undefined") return;
  if (document.getElementById("website-demo-layout-style")) return;
  const style = document.createElement("style");
  style.id = "website-demo-layout-style";
  style.textContent = `
    html[data-website-demo='1'] .demo-mode-banner {
      margin: 4px 8px 0;
      padding: 6px 8px;
      font-size: 11px;
      line-height: 1.3;
      gap: 6px;
    }
    html[data-website-demo='1'] .demo-mode-banner__en {
      display: none;
    }
    html[data-website-demo='1'] .bottom-tab-bar {
      max-width: min(100%, 420px);
    }
  `;
  document.head.appendChild(style);
};
function bootstrapWebsiteDemoIfNeeded() {
  if (!isWebsiteDemoBuild()) return false;
  try {
    markTestAccountSession();
    try {
      localStorage.setItem(LOGIN_SESSION_TOKEN_KEY, `website-demo-${Date.now()}`);
      localStorage.setItem("hbu_login_temporary", "0");
      localStorage.removeItem("hbu_manual_logout");
      localStorage.removeItem("hbu_logout_reason");
      localStorage.setItem("hbu_demo_banner_dismissed", "1");
    } catch {
    }
    seedTestAccountCaches(setCachedData, TEST_ACCOUNT.studentId);
    console.info("[website-demo] seeded test account session + fixtures");
  } catch (error) {
    console.warn("[website-demo] bootstrap failed:", error);
  }
  try {
    if (typeof document !== "undefined") {
      document.title = "Mini-HBUT 演示（离线预设数据）";
      document.documentElement.dataset.websiteDemo = "1";
      injectWebsiteDemoLayoutStyles();
    }
  } catch {
  }
  void ensureMaterialSymbolsFont();
  return true;
}
const createUpdateCoordinator = (runtime) => {
  const { state: state2 } = runtime;
  const handleCheckUpdate = () => {
    state2.showUpdateDialog.value = true;
  };
  const autoCheckUpdate = async () => {
    if (isWebsiteDemoBuild() || isTestAccountSession()) return;
    try {
      const currentVersion = await getCurrentVersion();
      if (!allowsInAppGithubUpdater()) {
        const result2 = await checkAppleStoreUpdate(currentVersion);
        const skipped = getSkippedAppleStoreVersion();
        if (result2?.hasUpdate && result2.storeVersion && result2.storeVersion !== skipped) {
          state2.showUpdateDialog.value = true;
        }
        return;
      }
      const { getUpdateChannel: getUpdateChannel2, getSkippedVersion: getSkippedVersion2 } = await __vitePreload(async () => {
        const { getUpdateChannel: getUpdateChannel3, getSkippedVersion: getSkippedVersion3 } = await Promise.resolve().then(() => updater$1);
        return { getUpdateChannel: getUpdateChannel3, getSkippedVersion: getSkippedVersion3 };
      }, true ? void 0 : void 0, import.meta.url);
      const channel = getUpdateChannel2();
      const result = await checkForUpdates(currentVersion, { channel });
      if (result?.hasUpdate && result.latestVersion !== getSkippedVersion2(channel)) {
        state2.showUpdateDialog.value = true;
      }
    } catch (error) {
      console.warn("[Update] 自动检查更新失败:", error);
    }
  };
  const handleForceUpdate = async () => {
    if (!allowsInAppGithubUpdater()) {
      const opened = await openAppStoreUpdatePage({
        trackViewUrl: state2.forceUpdateInfo.value?.store_url,
        trackId: state2.forceUpdateInfo.value?.apple_app_id
      });
      if (!opened && state2.forceUpdateResolvedUrl.value) {
        await openExternal(state2.forceUpdateResolvedUrl.value);
      }
      return;
    }
    if (state2.forceUpdateResolvedUrl.value) {
      await openExternal(state2.forceUpdateResolvedUrl.value);
      return;
    }
    state2.showUpdateDialog.value = true;
  };
  return { autoCheckUpdate, handleCheckUpdate, handleForceUpdate };
};
const DEFAULT_MODULE_CDN_BASE = "https://hbut.6661111.xyz/modules";
const DEFAULT_MODULE_CENTER = Object.freeze({
  channel: "main",
  modules: Object.freeze([
    Object.freeze({
      id: "hecheng_hugongda",
      name: "合成湖工大",
      icon: "🎮",
      description: "下载最新游戏包并打开",
      key_required: false,
      kind: "remote",
      order: 1
    }),
    Object.freeze({
      id: "jump_out_hbut",
      name: "跳出湖工大",
      icon: "🦘",
      description: "跳一跳风格校园跳跃小游戏",
      key_required: false,
      kind: "remote",
      order: 2
    }),
    Object.freeze({
      id: "hbut_2048",
      name: "2048 湖工大版",
      icon: "🔢",
      description: "经典 2048 数字合并游戏",
      key_required: false,
      kind: "remote",
      order: 3
    }),
    Object.freeze({
      id: "clumsy_bird_hbut",
      name: "笨鸟先飞",
      icon: "🐦",
      description: "点击屏幕控制小鸟飞行",
      key_required: false,
      kind: "remote",
      order: 4
    }),
    Object.freeze({
      id: "hbut_monopoly",
      name: "湖工大富翁",
      icon: "🎲",
      description: "投骰走遍校园，经营金币与绩点",
      key_required: false,
      kind: "remote",
      order: 5
    }),
    Object.freeze({
      id: "hbut_miner",
      name: "湖工矿工",
      icon: "⛏️",
      description: "摆动吊钩抓取湖工宝物",
      key_required: false,
      kind: "remote",
      order: 6
    }),
    Object.freeze({
      id: "hbut_memory_match",
      name: "湖工记忆牌",
      icon: "🧠",
      description: "翻开校园记忆，配对湖工地点",
      key_required: false,
      kind: "remote",
      order: 7
    }),
    Object.freeze({
      id: "hbut_gomoku",
      name: "湖工五子棋",
      icon: "◉",
      description: "本地双人十五路五子棋",
      key_required: false,
      kind: "remote",
      order: 8
    }),
    Object.freeze({
      id: "hbut_stack",
      name: "湖工叠塔",
      icon: "🏗️",
      description: "点击落下叠高楼，切除悬挑冲高分",
      key_required: false,
      kind: "remote",
      order: 9
    }),
    Object.freeze({
      id: "hbut_parking",
      name: "湖工挪车",
      icon: "🚌",
      description: "滑块挪车，把校车移出停车场",
      key_required: false,
      kind: "remote",
      order: 10
    }),
    Object.freeze({
      id: "hbut_match3",
      name: "湖工消消乐",
      icon: "🍬",
      description: "校园三消：交换连锁冲高分",
      key_required: false,
      kind: "remote",
      order: 11
    })
  ])
});
const safeText = (value) => String(value ?? "").trim();
const firstNonEmpty$1 = (...values) => {
  for (const value of values) {
    const text = safeText(value);
    if (text) return text;
  }
  return "";
};
const toBool = (value) => value === true || value === 1 || value === "1";
const safeNumber = (value, fallback = 0) => {
  const num = Number(value);
  return Number.isFinite(num) ? num : fallback;
};
const normalizeModuleCenterChannel$1 = (value, fallback = "main") => {
  const normalized = safeText(value).toLowerCase();
  if (normalized === "latest") return "latest";
  if (normalized === "dev") return "dev";
  if (normalized === "main") return "main";
  return fallback === "dev" || fallback === "latest" ? fallback : "main";
};
const buildModuleManifestUrl = ({
  rawUrl = "",
  channel = DEFAULT_MODULE_CENTER.channel,
  moduleId = "",
  moduleCdnBase = DEFAULT_MODULE_CDN_BASE
} = {}) => {
  const explicit = safeText(rawUrl);
  const normalizedChannel = normalizeModuleCenterChannel$1(channel);
  const id = safeText(moduleId);
  const base = safeText(moduleCdnBase).replace(/\/+$/, "") || DEFAULT_MODULE_CDN_BASE;
  if (explicit) {
    try {
      return new URL(explicit, `${base}/${normalizedChannel}/`).toString();
    } catch {
      return explicit;
    }
  }
  return id ? `${base}/${normalizedChannel}/${id}/manifest.json` : "";
};
const normalizeModuleCenterEntry$1 = (value, index = 0, channel = DEFAULT_MODULE_CENTER.channel, options = {}) => {
  const raw = value && typeof value === "object" ? value : {};
  const id = firstNonEmpty$1(raw.id, raw.module_id);
  if (!id) return null;
  const kindText = safeText(raw.kind || raw.type).toLowerCase();
  const kind = kindText === "internal" || id === "shuake" ? "internal" : "remote";
  const view = firstNonEmpty$1(raw.view, raw.route, id === "shuake" ? "more_shuake" : "");
  const order = safeNumber(raw.order, index + 1);
  return {
    id,
    name: firstNonEmpty$1(raw.name, raw.module_name, raw.title, id),
    icon: firstNonEmpty$1(raw.icon, id === "shuake" ? "🔐" : kind === "remote" ? "📦" : "🧩"),
    description: firstNonEmpty$1(raw.description, raw.desc),
    key_required: id === "shuake" || toBool(raw.key_required || raw.keyRequired),
    kind,
    view,
    order,
    min_compatible_version: firstNonEmpty$1(raw.min_compatible_version, raw.minCompatibleVersion),
    manifest_url: kind === "remote" ? buildModuleManifestUrl({
      rawUrl: firstNonEmpty$1(raw.manifest_url, raw.manifestUrl),
      channel,
      moduleId: id,
      moduleCdnBase: options.moduleCdnBase
    }) : ""
  };
};
const mergeRemoteCatalogFields = (item, catalogItem, channel, moduleCdnBase) => {
  if (!item || item.kind !== "remote" || !catalogItem) return item;
  return {
    ...catalogItem,
    ...item,
    kind: "remote",
    manifest_url: firstNonEmpty$1(
      catalogItem.manifest_url,
      catalogItem.manifestUrl,
      item.manifest_url,
      buildModuleManifestUrl({ channel, moduleId: item.id, moduleCdnBase })
    )
  };
};
const buildModuleCenterCards = ({
  channel = DEFAULT_MODULE_CENTER.channel,
  configuredModules = [],
  catalogModules = [],
  moduleCdnBase = DEFAULT_MODULE_CDN_BASE
} = {}) => {
  const normalizedChannel = normalizeModuleCenterChannel$1(channel);
  const normalize = (item, index) => normalizeModuleCenterEntry$1(item, index, normalizedChannel, { moduleCdnBase });
  const builtInModules = DEFAULT_MODULE_CENTER.modules.map(normalize).filter(Boolean);
  const builtInInternalModules = builtInModules.filter((item) => item.kind === "internal");
  const normalizedCatalogModules = (Array.isArray(catalogModules) ? catalogModules : []).map(normalize).filter(Boolean);
  const catalogMap = new Map(normalizedCatalogModules.map((item) => [item.id, item]));
  const normalizedConfiguredModules = (Array.isArray(configuredModules) ? configuredModules : []).map(normalize).filter(Boolean);
  const configuredMap = new Map(normalizedConfiguredModules.map((item) => [item.id, item]));
  const configuredInternalModules = normalizedConfiguredModules.filter((item) => item.kind === "internal");
  const mergedById = /* @__PURE__ */ new Map();
  for (const item of builtInInternalModules) {
    mergedById.set(item.id, item);
  }
  for (const item of configuredInternalModules) {
    mergedById.set(item.id, item);
  }
  for (const item of builtInModules.filter((entry) => entry.kind === "remote")) {
    const configuredItem = configuredMap.get(item.id);
    const catalogItem = catalogMap.get(item.id);
    const baseItem = configuredItem && configuredItem.kind === "remote" ? { ...item, ...configuredItem } : item;
    mergedById.set(
      item.id,
      mergeRemoteCatalogFields(baseItem, catalogItem, normalizedChannel, moduleCdnBase)
    );
  }
  for (const item of normalizedCatalogModules) {
    if (mergedById.has(item.id)) continue;
    mergedById.set(item.id, item);
  }
  return [...mergedById.values()].sort((a, b) => {
    const orderDelta = safeNumber(a.order, 999) - safeNumber(b.order, 999);
    if (orderDelta !== 0) return orderDelta;
    if (a.kind === "internal" && b.kind !== "internal") return -1;
    if (a.kind !== "internal" && b.kind === "internal") return 1;
    return safeText(a.id).localeCompare(safeText(b.id));
  });
};
const REMOTE_CONFIG_URLS = [
  "https://raw.gitcode.com/superdaobo/mini-hbut-config/raw/main/remote_config.json",
  "https://gh-proxy.com/https://raw.gitcode.com/superdaobo/mini-hbut-config/raw/main/remote_config.json"
];
const PACKAGE_CONFIG_URL = "/remote_config.json";
[...REMOTE_CONFIG_URLS, PACKAGE_CONFIG_URL];
const REMOTE_CONFIG_SNAPSHOT_KEY = "hbu_remote_config_snapshot";
const OCR_REMOTE_ENDPOINTS_KEY = "hbu_ocr_remote_endpoints";
const OCR_LOCAL_FALLBACK_ENDPOINTS_KEY = "hbu_ocr_local_fallback_endpoints";
const OCR_PRIMARY_ENDPOINT_KEY = "hbu_ocr_endpoint";
const DEFAULT_OCR_ENDPOINT = "https://mini-hbut-testocr1.hf.space/api/ocr/recognize";
const DEFAULT_OCR_ENDPOINTS = [DEFAULT_OCR_ENDPOINT];
const DEFAULT_LOCAL_OCR_FALLBACK_ENDPOINTS = [
  "http://1.94.167.18:5080/api/ocr/recognize",
  "https://mini-hbut-testocr1.hf.space/api/ocr/recognize"
];
const DEFAULT_WEBDAV_ENDPOINT = "https://mini-hbut-chaoxing-webdav.hf.space";
const DEFAULT_FORUM_ENDPOINT = "https://mini-hbut-testocr1.hf.space/api/forum";
const LOCAL_FORUM_API_BASE_KEY = "hbu_forum_api_base";
const DEFAULT_CLOUD_SYNC_CREDENTIAL_REF_ID = "kv1-main";
const MODULE_CDN_BASE = "https://hbut.6661111.xyz/modules";
const DEFAULT_CONFIG = {
  announcements: {
    pinned: [],
    ticker: [],
    list: [],
    confirm: []
  },
  force_update: {
    min_version: "",
    message: "",
    download_url: ""
  },
  ocr: {
    endpoint: DEFAULT_OCR_ENDPOINT,
    endpoints: [...DEFAULT_OCR_ENDPOINTS],
    local_fallback_endpoints: [...DEFAULT_LOCAL_OCR_FALLBACK_ENDPOINTS],
    enabled: true
  },
  temp_file_server: {
    schedule_upload_endpoint: "",
    enabled: true
  },
  resource_share: {
    enabled: true,
    endpoint: DEFAULT_WEBDAV_ENDPOINT,
    username: "mini-hbut",
    password: "mini-hbut",
    office_preview_proxy: "https://view.officeapps.live.com/op/view.aspx?src=",
    temp_upload_endpoint: ""
  },
  forum: {
    enabled: true,
    api_base: DEFAULT_FORUM_ENDPOINT
  },
  cloud_sync: {
    enabled: true,
    mode: "proxy",
    proxy_endpoint: DEFAULT_CLOUD_SYNC_ENDPOINT,
    secret_ref: DEFAULT_CLOUD_SYNC_CREDENTIAL_REF_ID,
    timeout_ms: 12e3,
    cooldown_seconds: 180
  },
  module_center: {
    channel: DEFAULT_MODULE_CENTER.channel,
    modules: [...DEFAULT_MODULE_CENTER.modules]
  },
  // #360 学习通资料库：远程只需 invite_code；课程名/教师/ID 由邀请码在线解析
  chaoxing_class: {
    enabled: true,
    invite_code: "18853572"
  },
  ai_models: [],
  config_admin_ids: []
};
const DEFAULT_CHAOXING_INVITE_CODE = "18853572";
const CHAOXING_INVITE_CACHE_KEY = "hbu_chaoxing_invite_code_cache_v1";
const REMOTE_CONFIG_KEYS = [
  "announcements",
  "announcement",
  "notices",
  "force_update",
  "ocr",
  "temp_file_server",
  "resource_share",
  "forum",
  "cloud_sync",
  "module_center",
  "more_modules",
  "chaoxing_class",
  "ai_models",
  "config_admin_ids"
];
const REMOTE_CONFIG_MEMORY_TTL_MS = 45 * 1e3;
const REMOTE_CONFIG_FETCH_TIMEOUT_MS = 3e3;
const toArray = (value) => Array.isArray(value) ? value : [];
const toText = (value) => value == null ? "" : String(value);
const firstNonEmpty = (...values) => {
  for (const value of values) {
    const text = toText(value).trim();
    if (text) return text;
  }
  return "";
};
const normalizeCloudSyncProxyEndpoint = (value) => {
  const text = toText(value).trim();
  if (!text) return "";
  const withProtocol = /^https?:\/\//i.test(text) ? text : `https://${text}`;
  const normalized = withProtocol.replace(/\/+$/, "");
  if (/\/api\/cloud-sync$/i.test(normalized)) {
    return normalized;
  }
  return `${normalized}/api/cloud-sync`;
};
const normalizeOcrEndpoint = (value) => {
  const text = toText(value).trim();
  if (!text) return "";
  const withProtocol = /^https?:\/\//i.test(text) ? text : `http://${text}`;
  return withProtocol.includes("/api/ocr/recognize") ? withProtocol : `${withProtocol.replace(/\/+$/, "")}/api/ocr/recognize`;
};
const normalizeForumEndpoint = (value) => {
  const text = toText(value).trim();
  if (!text) return DEFAULT_FORUM_ENDPOINT;
  const withProtocol = /^https?:\/\//i.test(text) ? text : `https://${text}`;
  const normalized = withProtocol.replace(/\/+$/, "");
  if (/\/api\/forum$/i.test(normalized)) {
    return normalized;
  }
  return `${normalized}/api/forum`;
};
const isLocalForumEndpointOverride = (value) => {
  const endpoint = normalizeForumEndpoint(value);
  if (!endpoint) return "";
  try {
    const url = new URL(endpoint);
    const host = url.hostname.toLowerCase();
    const isLoopback = host === "localhost" || host === "127.0.0.1" || host === "[::1]";
    return isLoopback ? endpoint : "";
  } catch {
    return "";
  }
};
const getLocalForumEndpointOverride = () => {
  let fromQuery = "";
  try {
    const search = globalThis.window?.location?.search || "";
    const params = new URLSearchParams(search);
    fromQuery = isLocalForumEndpointOverride(
      firstNonEmpty(params.get("forumApiBase"), params.get("forum_api_base"))
    );
  } catch {
    fromQuery = "";
  }
  let fromStorage = "";
  try {
    fromStorage = isLocalForumEndpointOverride(
      firstNonEmpty(globalThis.localStorage?.getItem(LOCAL_FORUM_API_BASE_KEY))
    );
  } catch {
    fromStorage = "";
  }
  return firstNonEmpty(fromQuery, fromStorage);
};
const normalizeEndpointList = (value) => {
  const array = toArray(value);
  const seen = /* @__PURE__ */ new Set();
  const result = [];
  for (const item of array) {
    const endpoint = normalizeOcrEndpoint(item);
    if (!endpoint || seen.has(endpoint)) continue;
    seen.add(endpoint);
    result.push(endpoint);
  }
  return result;
};
const getBackendSettings = () => {
  try {
    const settings = useAppSettings();
    return settings?.backend || {};
  } catch {
    return {};
  }
};
const isRemoteConfigEnabled = () => getBackendSettings()?.useRemoteConfig !== false;
const safeJsonParse = (raw, fallback) => {
  if (!raw) return fallback;
  try {
    return JSON.parse(raw);
  } catch {
    return fallback;
  }
};
const withCacheBust = (url) => {
  const text = toText(url).trim();
  if (!text) return "";
  const joiner = text.includes("?") ? "&" : "?";
  return `${text}${joiner}_t=${Date.now()}`;
};
const withTimeout = async (promise, timeoutMs, timeoutMessage) => {
  let timer = null;
  try {
    return await Promise.race([
      promise,
      new Promise((_, reject) => {
        timer = setTimeout(() => {
          reject(new Error(timeoutMessage || "请求超时"));
        }, timeoutMs);
      })
    ]);
  } finally {
    if (timer) clearTimeout(timer);
  }
};
const resolveAnnouncements = (cfg) => {
  const root = cfg && typeof cfg === "object" ? cfg : {};
  const obj = root.announcements && typeof root.announcements === "object" ? root.announcements : root.announcement && typeof root.announcement === "object" ? root.announcement : root.notices && typeof root.notices === "object" ? root.notices : {};
  return {
    pinned: toArray(obj.pinned || obj.pin || root.pinned),
    ticker: toArray(obj.ticker || obj.scroll || obj.marquee || root.ticker),
    list: toArray(obj.list || obj.items || root.notice_list || root.list),
    confirm: toArray(obj.confirm || obj.required || root.confirm)
  };
};
const normalizeModuleCenterChannel = (value) => {
  const normalized = toText(value).trim().toLowerCase();
  return normalized === "dev" ? "dev" : "main";
};
const resolveModuleManifestUrl = (rawUrl, channel, moduleId) => {
  const explicit = toText(rawUrl).trim();
  if (explicit) {
    try {
      return new URL(explicit, `${MODULE_CDN_BASE}/${channel}/`).toString();
    } catch {
      return explicit;
    }
  }
  return `${MODULE_CDN_BASE}/${channel}/${moduleId}/manifest.json`;
};
const normalizeModuleCenterEntry = (value, index = 0, channel = "main") => {
  const raw = value && typeof value === "object" ? value : {};
  const id = firstNonEmpty(raw.id, raw.module_id);
  if (!id) return null;
  const view = firstNonEmpty(raw.view, raw.route, id === "shuake" ? "more_shuake" : "");
  const kindText = toText(raw.kind || raw.type).trim().toLowerCase();
  const kind = kindText === "internal" || id === "shuake" ? "internal" : "remote";
  const order = Number(raw.order);
  return {
    id,
    name: firstNonEmpty(raw.name, raw.module_name, raw.title, id),
    icon: firstNonEmpty(raw.icon, id === "shuake" ? "🔐" : kind === "remote" ? "📦" : "🧩"),
    description: firstNonEmpty(raw.description, raw.desc),
    key_required: id === "shuake" || raw.key_required === true || raw.keyRequired === true,
    kind,
    view,
    order: Number.isFinite(order) ? order : index + 1,
    manifest_url: kind === "remote" ? resolveModuleManifestUrl(firstNonEmpty(raw.manifest_url, raw.manifestUrl), channel, id) : ""
  };
};
const resolveModuleCenter = (cfg) => {
  const root = cfg && typeof cfg === "object" ? cfg : {};
  const moduleCenter = root.module_center && typeof root.module_center === "object" ? root.module_center : {};
  const moreModules = root.more_modules && typeof root.more_modules === "object" ? root.more_modules : {};
  const channel = normalizeModuleCenterChannel(
    firstNonEmpty(
      moduleCenter.channel,
      moreModules.channel,
      root.module_channel,
      root.more_modules_channel,
      DEFAULT_MODULE_CENTER.channel
    )
  );
  const rawModules = toArray(
    moduleCenter.modules ?? moreModules.modules ?? (Array.isArray(root.more_modules) ? root.more_modules : [])
  );
  let modules = rawModules.map((item, index) => normalizeModuleCenterEntry(item, index, channel)).filter((item) => item !== null);
  if (modules.length === 0) {
    modules = DEFAULT_MODULE_CENTER.modules.map((item, index) => normalizeModuleCenterEntry(item, index, channel)).filter((item) => item !== null);
  }
  const deduped = [];
  const seen = /* @__PURE__ */ new Set();
  for (const item of modules) {
    if (!item || seen.has(item.id)) continue;
    seen.add(item.id);
    deduped.push(item);
  }
  return {
    channel,
    modules: deduped
  };
};
const persistOcrConfig = (ocr) => {
  const endpoints = normalizeEndpointList(ocr?.endpoints);
  const localFallbackEndpoints = normalizeEndpointList(ocr?.local_fallback_endpoints);
  const primary = endpoints[0] || "";
  try {
    localStorage.setItem(OCR_REMOTE_ENDPOINTS_KEY, JSON.stringify(endpoints));
    localStorage.setItem(OCR_LOCAL_FALLBACK_ENDPOINTS_KEY, JSON.stringify(localFallbackEndpoints));
    if (primary) {
      localStorage.setItem(OCR_PRIMARY_ENDPOINT_KEY, primary);
    } else {
      localStorage.removeItem(OCR_PRIMARY_ENDPOINT_KEY);
    }
  } catch {
  }
  return { endpoints, localFallbackEndpoints, primary };
};
const getStoredOcrConfig = () => {
  const endpoints = normalizeEndpointList(
    safeJsonParse(localStorage.getItem(OCR_REMOTE_ENDPOINTS_KEY) || "[]", [])
  );
  const localFallbackEndpoints = normalizeEndpointList(
    safeJsonParse(localStorage.getItem(OCR_LOCAL_FALLBACK_ENDPOINTS_KEY) || "[]", [])
  );
  const primary = normalizeOcrEndpoint(localStorage.getItem(OCR_PRIMARY_ENDPOINT_KEY) || "");
  return {
    endpoints: endpoints.length > 0 ? endpoints : [...DEFAULT_OCR_ENDPOINTS],
    local_fallback_endpoints: localFallbackEndpoints.length > 0 ? localFallbackEndpoints : [...DEFAULT_LOCAL_OCR_FALLBACK_ENDPOINTS],
    endpoint: primary || endpoints[0] || DEFAULT_OCR_ENDPOINT
  };
};
const REMOTE_CONFIG_UPDATED_EVENT = "hbu-remote-config-updated";
let chaoxingInviteMemory = "";
let remoteConfigMemory = null;
let remoteConfigMemoryAt = 0;
let remoteConfigInFlight = null;
let remoteConfigBackgroundInFlight = null;
const buildLocalOnlyConfig = () => {
  const backend = getBackendSettings();
  const normalized = normalizeRemoteConfig(DEFAULT_CONFIG);
  const moduleParams = backend?.moduleParams || {};
  const localOcrEndpoint = normalizeOcrEndpoint(backend?.ocrEndpoint) || DEFAULT_OCR_ENDPOINT;
  const ocrEndpoints = [localOcrEndpoint];
  const primaryEndpoint = localOcrEndpoint;
  normalized.ocr.endpoint = primaryEndpoint;
  normalized.ocr.endpoints = ocrEndpoints;
  normalized.ocr.local_fallback_endpoints = [...DEFAULT_LOCAL_OCR_FALLBACK_ENDPOINTS];
  normalized.ocr.enabled = true;
  const tempUploadEndpoint = firstNonEmpty(backend?.tempUploadEndpoint);
  normalized.temp_file_server.schedule_upload_endpoint = tempUploadEndpoint;
  normalized.temp_file_server.enabled = true;
  normalized.resource_share.temp_upload_endpoint = tempUploadEndpoint;
  normalized.cloud_sync = {
    enabled: true,
    mode: "proxy",
    proxy_endpoint: normalizeCloudSyncProxyEndpoint(
      firstNonEmpty(backend?.cloudSyncEndpoint, DEFAULT_CLOUD_SYNC_ENDPOINT)
    ),
    secret_ref: firstNonEmpty(backend?.cloudSyncSecretRef, DEFAULT_CLOUD_SYNC_CREDENTIAL_REF_ID),
    timeout_ms: 12e3,
    cooldown_seconds: Number(moduleParams.cloudSyncCooldownSec || 180),
    upload_cooldown_seconds: Number(moduleParams.cloudSyncUploadCooldownSec || 120),
    download_cooldown_seconds: Number(moduleParams.cloudSyncDownloadCooldownSec || 10)
  };
  return normalized;
};
function normalizeRemoteConfig(raw) {
  const cfg = raw && typeof raw === "object" ? raw : {};
  const ocrBlock = cfg.ocr && typeof cfg.ocr === "object" ? cfg.ocr : {};
  const announcements = resolveAnnouncements(cfg);
  const endpointCandidates = [
    ...toArray(ocrBlock.endpoints),
    ocrBlock.endpoint,
    ocrBlock.url,
    cfg.ocr_endpoint,
    cfg.ocrUrl
  ];
  const normalizedEndpoints = normalizeEndpointList(endpointCandidates);
  const ocrEndpoints = normalizedEndpoints.length > 0 ? normalizedEndpoints : [...DEFAULT_OCR_ENDPOINTS];
  const localFallbackCandidates = [
    ...toArray(ocrBlock.local_fallback_endpoints),
    ...toArray(ocrBlock.localFallbackEndpoints),
    ...toArray(cfg.ocr_local_fallback_endpoints)
  ];
  const normalizedLocalFallback = normalizeEndpointList(localFallbackCandidates);
  const localFallbackEndpoints = normalizedLocalFallback.length > 0 ? normalizedLocalFallback : [...DEFAULT_LOCAL_OCR_FALLBACK_ENDPOINTS];
  const forumEndpointOverride = getLocalForumEndpointOverride();
  const forceUpdate = cfg.force_update && typeof cfg.force_update === "object" ? cfg.force_update : {};
  const tempFileServer = cfg.temp_file_server && typeof cfg.temp_file_server === "object" ? cfg.temp_file_server : {};
  const resourceShare = cfg.resource_share && typeof cfg.resource_share === "object" ? cfg.resource_share : {};
  const webdav = cfg.webdav && typeof cfg.webdav === "object" ? cfg.webdav : {};
  const forum = cfg.forum && typeof cfg.forum === "object" ? cfg.forum : {};
  const cloudSync = cfg.cloud_sync && typeof cfg.cloud_sync === "object" ? cfg.cloud_sync : {};
  const sync = cfg.sync && typeof cfg.sync === "object" ? cfg.sync : {};
  return {
    announcements,
    force_update: {
      min_version: firstNonEmpty(forceUpdate.min_version, forceUpdate.minVersion),
      message: firstNonEmpty(forceUpdate.message),
      download_url: firstNonEmpty(forceUpdate.download_url, forceUpdate.downloadUrl)
    },
    ocr: {
      endpoint: ocrEndpoints[0] || DEFAULT_OCR_ENDPOINT,
      endpoints: ocrEndpoints,
      local_fallback_endpoints: localFallbackEndpoints,
      enabled: ocrBlock.enabled !== false
    },
    temp_file_server: {
      schedule_upload_endpoint: firstNonEmpty(
        tempFileServer.schedule_upload_endpoint,
        tempFileServer.upload_endpoint,
        tempFileServer.endpoint,
        cfg.temp_upload_endpoint
      ),
      enabled: tempFileServer.enabled !== false
    },
    resource_share: {
      enabled: resourceShare.enabled !== false,
      endpoint: firstNonEmpty(
        resourceShare.endpoint,
        webdav.endpoint,
        DEFAULT_WEBDAV_ENDPOINT
      ),
      username: firstNonEmpty(resourceShare.username, webdav.username, "mini-hbut"),
      password: firstNonEmpty(resourceShare.password, webdav.password, "mini-hbut"),
      office_preview_proxy: firstNonEmpty(
        resourceShare.office_preview_proxy,
        "https://view.officeapps.live.com/op/view.aspx?src="
      ),
      temp_upload_endpoint: firstNonEmpty(
        resourceShare.temp_upload_endpoint,
        tempFileServer.schedule_upload_endpoint,
        tempFileServer.upload_endpoint
      )
    },
    forum: {
      enabled: forum.enabled !== false,
      api_base: normalizeForumEndpoint(
        firstNonEmpty(
          forumEndpointOverride,
          forum.api_base,
          forum.apiBase,
          forum.endpoint,
          cfg.forum_api_base,
          DEFAULT_FORUM_ENDPOINT
        )
      )
    },
    cloud_sync: {
      enabled: cloudSync.enabled !== false,
      mode: firstNonEmpty(
        cloudSync.mode,
        cfg.cloud_sync_mode,
        sync.mode,
        "proxy"
      ),
      proxy_endpoint: normalizeCloudSyncProxyEndpoint(
        firstNonEmpty(
          cloudSync.proxy_endpoint,
          cloudSync.proxyEndpoint,
          cloudSync.endpoint,
          cfg.cloud_sync_proxy_endpoint,
          cfg.cloud_sync_endpoint,
          sync.proxy_endpoint,
          DEFAULT_CLOUD_SYNC_ENDPOINT
        )
      ),
      secret_ref: firstNonEmpty(
        cloudSync.secret_ref,
        cloudSync.secretRef,
        cfg.cloud_sync_secret_ref,
        sync.secret_ref,
        DEFAULT_CLOUD_SYNC_CREDENTIAL_REF_ID
      ),
      timeout_ms: Number(
        cloudSync.timeout_ms || cloudSync.timeoutMs || sync.timeout_ms || 12e3
      ),
      cooldown_seconds: Number(
        cloudSync.cooldown_seconds || cloudSync.cooldownSeconds || sync.cooldown_seconds || 180
      ),
      upload_cooldown_seconds: Number(
        cloudSync.upload_cooldown_seconds || cloudSync.uploadCooldownSeconds || sync.upload_cooldown_seconds || cloudSync.cooldown_seconds || cloudSync.cooldownSeconds || sync.cooldown_seconds || 120
      ),
      download_cooldown_seconds: Number(
        cloudSync.download_cooldown_seconds || cloudSync.downloadCooldownSeconds || sync.download_cooldown_seconds || cloudSync.cooldown_seconds || cloudSync.cooldownSeconds || sync.cooldown_seconds || 10
      )
    },
    module_center: resolveModuleCenter(cfg),
    chaoxing_class: normalizeChaoxingClassConfig(cfg.chaoxing_class || cfg.chaoxingClass, {
      // 远程 payload 里有 invite 时写入本地缓存（断网可复用）
      persistInvite: !!(cfg.chaoxing_class || cfg.chaoxingClass)
    }),
    ai_models: toArray(cfg.ai_models),
    config_admin_ids: toArray(cfg.config_admin_ids).map((item) => String(item))
  };
}
function applyAppStoreRemoteConfigClamp(config) {
  if (!shouldApplyAppStoreRestrictions() || !config || typeof config !== "object") {
    return config;
  }
  const next = { ...config };
  next.module_center = {
    channel: firstNonEmpty(config.module_center?.channel, "main"),
    modules: []
  };
  next.resource_share = {
    ...config.resource_share || {},
    enabled: false
  };
  next.forum = {
    ...config.forum || {},
    enabled: false
  };
  next.chaoxing_class = {
    ...config.chaoxing_class || {},
    enabled: false
  };
  next.ai_models = [];
  next.config_admin_ids = [];
  const ocr = { ...config.ocr || {} };
  const httpsOnly = (list) => toArray(list).map((u) => String(u || "").trim()).filter((u) => /^https:\/\//i.test(u));
  ocr.local_fallback_endpoints = httpsOnly(ocr.local_fallback_endpoints);
  ocr.endpoints = httpsOnly(ocr.endpoints).length ? httpsOnly(ocr.endpoints) : httpsOnly([ocr.endpoint]);
  if (Array.isArray(ocr.endpoints) && ocr.endpoints[0]) ocr.endpoint = ocr.endpoints[0];
  next.ocr = ocr;
  next.cloud_sync = {
    ...config.cloud_sync || {},
    enabled: false
  };
  return next;
}
const readCachedChaoxingInvite = () => {
  if (chaoxingInviteMemory) return chaoxingInviteMemory;
  try {
    const fromStore = toText(localStorage.getItem(CHAOXING_INVITE_CACHE_KEY)).trim();
    if (fromStore) chaoxingInviteMemory = fromStore;
    return fromStore;
  } catch {
    return chaoxingInviteMemory || "";
  }
};
function persistChaoxingInviteCode(code) {
  const invite = toText(code).trim();
  if (!invite) return "";
  chaoxingInviteMemory = invite;
  try {
    localStorage.setItem(CHAOXING_INVITE_CACHE_KEY, invite);
  } catch {
  }
  return invite;
}
function resolveChaoxingInviteCode(rawBlock) {
  const src = rawBlock && typeof rawBlock === "object" ? rawBlock : {};
  return firstNonEmpty(
    src.invite_code,
    src.inviteCode,
    src.invite,
    readCachedChaoxingInvite(),
    DEFAULT_CHAOXING_INVITE_CODE
  ) || DEFAULT_CHAOXING_INVITE_CODE;
}
function normalizeChaoxingClassConfig(raw, options = {}) {
  const src = raw && typeof raw === "object" ? raw : {};
  const invite = resolveChaoxingInviteCode(src);
  if (options.persistInvite && invite) {
    const explicit = firstNonEmpty(src.invite_code, src.inviteCode, src.invite);
    if (explicit) persistChaoxingInviteCode(explicit);
  }
  return {
    enabled: src.enabled !== false,
    invite_code: invite,
    // 可选元数据：有则用，无则空（禁止回落到已废弃的旧班信息）
    course_id: firstNonEmpty(src.course_id, src.courseId),
    clazz_id: firstNonEmpty(src.clazz_id, src.clazzId, src.class_id),
    course_name: firstNonEmpty(src.course_name, src.courseName),
    teacher_name: firstNonEmpty(src.teacher_name, src.teacherName),
    cpi: firstNonEmpty(src.cpi),
    cover_url: firstNonEmpty(src.cover_url, src.coverUrl)
  };
}
function getChaoxingClassConfig(config) {
  if (config && typeof config === "object") {
    const block = config.chaoxing_class || config.chaoxingClass;
    if (block && typeof block === "object") {
      return normalizeChaoxingClassConfig(block);
    }
    return normalizeRemoteConfig(config).chaoxing_class;
  }
  const mem = readMemoryConfig();
  if (mem?.chaoxing_class) {
    const cached = readCachedChaoxingInvite();
    if (cached && cached !== mem.chaoxing_class.invite_code) {
      if (!mem.chaoxing_class.invite_code) {
        return { ...mem.chaoxing_class, invite_code: cached };
      }
    }
    return mem.chaoxing_class;
  }
  try {
    const raw = localStorage.getItem(REMOTE_CONFIG_SNAPSHOT_KEY);
    if (raw) {
      return normalizeRemoteConfig(JSON.parse(raw)).chaoxing_class;
    }
  } catch {
  }
  return normalizeChaoxingClassConfig({ invite_code: resolveChaoxingInviteCode({}) });
}
const remoteConfigFingerprint = (config) => {
  try {
    const normalized = normalizeRemoteConfig(config || {});
    return JSON.stringify(normalized);
  } catch {
    return "";
  }
};
const setMemoryConfig = (config) => {
  remoteConfigMemory = config && typeof config === "object" ? normalizeRemoteConfig(config) : null;
  remoteConfigMemoryAt = remoteConfigMemory ? Date.now() : 0;
};
const saveSnapshot = (config, { emitEvent = false, previousFingerprint = null } = {}) => {
  const normalized = config && typeof config === "object" ? normalizeRemoteConfig(config) : null;
  setMemoryConfig(normalized);
  if (!normalized) return { saved: false, changed: false };
  const nextFp = remoteConfigFingerprint(normalized);
  const prevFp = previousFingerprint != null ? previousFingerprint : (() => {
    try {
      const raw = localStorage.getItem(REMOTE_CONFIG_SNAPSHOT_KEY);
      return raw ? remoteConfigFingerprint(JSON.parse(raw)) : "";
    } catch {
      return "";
    }
  })();
  const changed = nextFp !== prevFp;
  if (changed || !prevFp) {
    try {
      localStorage.setItem(REMOTE_CONFIG_SNAPSHOT_KEY, JSON.stringify(normalized));
    } catch {
    }
  }
  if (emitEvent && changed) {
    try {
      if (typeof window !== "undefined" && window.dispatchEvent) {
        window.dispatchEvent(
          new CustomEvent(REMOTE_CONFIG_UPDATED_EVENT, {
            detail: { source: "remote", fingerprint: nextFp }
          })
        );
      }
    } catch {
    }
  }
  return { saved: true, changed, fingerprint: nextFp };
};
const readMemoryConfig = () => {
  if (!remoteConfigMemory) return null;
  if (Date.now() - remoteConfigMemoryAt > REMOTE_CONFIG_MEMORY_TTL_MS) {
    remoteConfigMemory = null;
    remoteConfigMemoryAt = 0;
    return null;
  }
  return remoteConfigMemory;
};
const loadSnapshot = () => {
  try {
    const raw = localStorage.getItem(REMOTE_CONFIG_SNAPSHOT_KEY);
    if (!raw) return null;
    return JSON.parse(raw);
  } catch {
    return null;
  }
};
const fetchByInvoke = async (url) => {
  try {
    const payload = await withTimeout(
      invokeNative("fetch_remote_config", { url }),
      REMOTE_CONFIG_FETCH_TIMEOUT_MS,
      "远程配置原生请求超时"
    );
    if (payload && typeof payload === "object") return payload;
  } catch {
  }
  return null;
};
const parseRemoteJson = (raw) => {
  if (raw && typeof raw === "object") return raw;
  if (typeof raw === "string") {
    try {
      const parsed = JSON.parse(raw);
      return parsed && typeof parsed === "object" ? parsed : null;
    } catch {
      return null;
    }
  }
  return null;
};
const isLikelyRemoteConfigPayload = (payload) => {
  if (!payload || typeof payload !== "object" || Array.isArray(payload)) return false;
  return REMOTE_CONFIG_KEYS.some((key) => Object.prototype.hasOwnProperty.call(payload, key));
};
const fetchByCapacitor = async (url) => {
  if (detectRuntime() !== "capacitor") return null;
  try {
    const core = await __vitePreload(() => import("./runtime-bridge-BU9jeOXP.js").then((n) => n.a2), true ? __vite__mapDeps([1,0]) : void 0, import.meta.url);
    const capHttp = core?.CapacitorHttp || window?.Capacitor?.Plugins?.CapacitorHttp;
    if (!capHttp?.request) return null;
    const result = await capHttp.request({
      method: "GET",
      url,
      headers: { Accept: "application/json" },
      connectTimeout: 8e3,
      readTimeout: 8e3
    });
    return parseRemoteJson(result?.data);
  } catch {
    return null;
  }
};
const fetchByWeb = async (url) => {
  const controller = typeof AbortController === "function" ? new AbortController() : null;
  let abortTimer = null;
  try {
    if (controller) {
      abortTimer = setTimeout(() => controller.abort(), REMOTE_CONFIG_FETCH_TIMEOUT_MS);
    }
    const res = await withTimeout(
      fetch(url, {
        cache: "no-store",
        headers: { Accept: "application/json" },
        signal: controller?.signal
      }),
      REMOTE_CONFIG_FETCH_TIMEOUT_MS,
      "远程配置网页请求超时"
    );
    if (!res.ok) {
      throw new Error(`remote config http ${res.status}`);
    }
    return res.json();
  } finally {
    if (abortTimer) clearTimeout(abortTimer);
  }
};
const fetchFromUrlList = async (baseUrls) => {
  let lastError = "";
  for (const baseUrl of baseUrls || []) {
    const url = withCacheBust(baseUrl);
    if (!url) continue;
    const isLocalUrl = String(baseUrl).startsWith("/");
    if (!isLocalUrl) {
      const byInvoke = await fetchByInvoke(url);
      if (byInvoke && isLikelyRemoteConfigPayload(byInvoke)) {
        return { payload: byInvoke, sourceUrl: baseUrl };
      }
      const byCapacitor = await fetchByCapacitor(url);
      if (byCapacitor && isLikelyRemoteConfigPayload(byCapacitor)) {
        return { payload: byCapacitor, sourceUrl: baseUrl };
      }
    }
    try {
      const byWeb = await fetchByWeb(url);
      if (byWeb && isLikelyRemoteConfigPayload(byWeb)) {
        return { payload: byWeb, sourceUrl: baseUrl };
      }
    } catch (e) {
      lastError = e?.message || String(e);
    }
  }
  throw new Error(lastError || "remote config unavailable");
};
const fetchFromRemoteUrls = async () => fetchFromUrlList(REMOTE_CONFIG_URLS);
const fetchFromPackageUrl = async () => fetchFromUrlList([PACKAGE_CONFIG_URL]);
async function refreshRemoteConfigFromNetwork(options = {}) {
  const emitEvent = options?.emitEvent !== false;
  const snapshot = loadSnapshot();
  const prevFp = snapshot ? remoteConfigFingerprint(snapshot) : "";
  try {
    const { payload } = await fetchFromRemoteUrls();
    const normalized = normalizeRemoteConfig(payload);
    const { changed } = saveSnapshot(normalized, {
      emitEvent,
      previousFingerprint: prevFp
    });
    return {
      config: applyAppStoreRemoteConfigClamp(normalized),
      changed,
      source: "remote"
    };
  } catch {
    if (snapshot) {
      const normalized = normalizeRemoteConfig(snapshot);
      setMemoryConfig(normalized);
      return {
        config: applyAppStoreRemoteConfigClamp(normalized),
        changed: false,
        source: "snapshot"
      };
    }
    return { config: void 0, changed: false, source: "none" };
  }
}
const scheduleBackgroundRemoteRefresh = () => {
  if (remoteConfigBackgroundInFlight) return remoteConfigBackgroundInFlight;
  if (isTestAccountSession()) return null;
  if (!isRemoteConfigEnabled()) return null;
  remoteConfigBackgroundInFlight = refreshRemoteConfigFromNetwork({ emitEvent: true }).catch(() => null).finally(() => {
    remoteConfigBackgroundInFlight = null;
  });
  return remoteConfigBackgroundInFlight;
};
const applyOcrRuntimeConfig = async (configLike) => {
  const cfg = configLike && typeof configLike === "object" ? configLike : {};
  const ocrBlock = cfg.ocr && typeof cfg.ocr === "object" ? cfg.ocr : {};
  const enabled = ocrBlock.enabled !== false;
  const ocrPayload = enabled ? ocrBlock : null;
  const persisted = persistOcrConfig({
    endpoints: enabled ? ocrPayload?.endpoints || (ocrPayload?.endpoint ? [ocrPayload.endpoint] : []) : [],
    local_fallback_endpoints: enabled ? ocrPayload?.local_fallback_endpoints || [] : [...DEFAULT_LOCAL_OCR_FALLBACK_ENDPOINTS]
  });
  const runtimePayload = {
    endpoints: enabled ? persisted.endpoints : [],
    localFallbackEndpoints: persisted.localFallbackEndpoints
  };
  try {
    await invokeNative("set_ocr_runtime_config", {
      endpoints: runtimePayload.endpoints,
      local_fallback_endpoints: runtimePayload.localFallbackEndpoints,
      localFallbackEndpoints: runtimePayload.localFallbackEndpoints
    });
  } catch {
    try {
      await invokeNative("set_ocr_endpoint", { endpoint: enabled ? persisted.primary : "" });
    } catch {
    }
  }
  return runtimePayload;
};
async function fetchRemoteConfig(options = {}) {
  const forceRefresh = options?.force === true;
  if (isTestAccountSession()) {
    return applyAppStoreRemoteConfigClamp(normalizeRemoteConfig(DEFAULT_CONFIG));
  }
  if (!isRemoteConfigEnabled()) {
    return applyAppStoreRemoteConfigClamp(buildLocalOnlyConfig());
  }
  if (!forceRefresh) {
    const memory = readMemoryConfig();
    if (memory) {
      scheduleBackgroundRemoteRefresh();
      return applyAppStoreRemoteConfigClamp(memory);
    }
    if (remoteConfigInFlight) {
      return remoteConfigInFlight;
    }
    const snapshot = loadSnapshot();
    if (snapshot) {
      const normalized = normalizeRemoteConfig(snapshot);
      setMemoryConfig(normalized);
      scheduleBackgroundRemoteRefresh();
      return applyAppStoreRemoteConfigClamp(normalized);
    }
  }
  const task = (async () => {
    try {
      const { payload } = await fetchFromRemoteUrls();
      const normalized = normalizeRemoteConfig(payload);
      const prev = loadSnapshot();
      saveSnapshot(normalized, {
        emitEvent: false,
        previousFingerprint: prev ? remoteConfigFingerprint(prev) : ""
      });
      return applyAppStoreRemoteConfigClamp(normalized);
    } catch {
    }
    const snapshot = loadSnapshot();
    if (snapshot) {
      const normalized = normalizeRemoteConfig(snapshot);
      setMemoryConfig(normalized);
      return applyAppStoreRemoteConfigClamp(normalized);
    }
    try {
      const { payload } = await fetchFromPackageUrl();
      const normalized = normalizeRemoteConfig(payload);
      saveSnapshot(normalized, { emitEvent: false, previousFingerprint: "" });
      return applyAppStoreRemoteConfigClamp(normalized);
    } catch {
    }
    const fallback = normalizeRemoteConfig(DEFAULT_CONFIG);
    saveSnapshot(fallback, { emitEvent: false, previousFingerprint: "" });
    return applyAppStoreRemoteConfigClamp(fallback);
  })();
  if (forceRefresh) {
    return task;
  }
  remoteConfigInFlight = task;
  try {
    return await task;
  } finally {
    if (remoteConfigInFlight === task) {
      remoteConfigInFlight = null;
    }
  }
}
const normalizeAnnouncements = (payload) => ({
  pinned: Array.isArray(payload?.pinned) ? payload.pinned : [],
  ticker: Array.isArray(payload?.ticker) ? payload.ticker : [],
  list: Array.isArray(payload?.list) ? payload.list : [],
  confirm: Array.isArray(payload?.confirm) ? payload.confirm : []
});
const hasAnnouncementContent = (payload) => payload.pinned.length + payload.ticker.length + payload.list.length + payload.confirm.length > 0;
const compareVersions = (left, right) => {
  const a = String(left || "").replace(/^v/i, "").split(".").map((part) => Number(part) || 0);
  const b = String(right || "").replace(/^v/i, "").split(".").map((part) => Number(part) || 0);
  const length = Math.max(a.length, b.length);
  for (let index = 0; index < length; index += 1) {
    if ((a[index] || 0) > (b[index] || 0)) return 1;
    if ((a[index] || 0) < (b[index] || 0)) return -1;
  }
  return 0;
};
const createRemoteConfigCoordinator = (runtime) => {
  const { state: state2 } = runtime;
  const restoreAnnouncementSnapshot = () => {
    try {
      const raw = localStorage.getItem(ANNOUNCEMENT_SNAPSHOT_KEY);
      if (!raw) return null;
      const parsed = normalizeAnnouncements(JSON.parse(raw));
      return hasAnnouncementContent(parsed) ? parsed : null;
    } catch {
      return null;
    }
  };
  const persistAnnouncementSnapshot = (payload) => {
    try {
      localStorage.setItem(ANNOUNCEMENT_SNAPSHOT_KEY, JSON.stringify(payload));
    } catch {
    }
  };
  const getConfirmedIds = () => {
    try {
      const value = JSON.parse(localStorage.getItem(ANNOUNCEMENT_CONFIRM_KEY) || "[]");
      return new Set(Array.isArray(value) ? value.map(String) : []);
    } catch {
      return /* @__PURE__ */ new Set();
    }
  };
  const markConfirmed = (id) => {
    const text = String(id || "").trim();
    if (!text) return;
    const ids = getConfirmedIds();
    ids.add(text);
    localStorage.setItem(ANNOUNCEMENT_CONFIRM_KEY, JSON.stringify([...ids]));
  };
  const renderMarkdown = async (content) => {
    const text = String(content || "");
    if (!text) return "";
    try {
      const mod = await __vitePreload(() => import("./markdown-BYynpwuf.js"), true ? __vite__mapDeps([54,55]) : void 0, import.meta.url);
      await mod.initMarkdownRuntime?.();
      return mod.renderMarkdown?.(text) || text;
    } catch {
      return text.replace(/[&<>"']/g, (char) => ({
        "&": "&amp;",
        "<": "&lt;",
        ">": "&gt;",
        '"': "&quot;",
        "'": "&#39;"
      })[char] || char).replace(/\n/g, "<br>");
    }
  };
  const openAnnouncement = (item) => {
    const announcement = item && typeof item === "object" ? item : null;
    if (!announcement) return;
    state2.activeAnnouncement.value = announcement;
    state2.showAnnouncementModal.value = true;
    const token = ++state2.mutable.activeAnnouncementRenderToken;
    void renderMarkdown(announcement.content || announcement.body || "").then((html) => {
      if (token === state2.mutable.activeAnnouncementRenderToken) state2.activeAnnouncementHtml.value = html;
    });
  };
  const closeAnnouncement = () => {
    state2.showAnnouncementModal.value = false;
    state2.activeAnnouncement.value = null;
    state2.activeAnnouncementHtml.value = "";
  };
  const findNextBlockingAnnouncement = () => {
    const confirmed = getConfirmedIds();
    const next = state2.announcementData.value.confirm.find((item) => {
      const id = String(item?.id || item?.title || "").trim();
      return id && !confirmed.has(id);
    });
    state2.blockingAnnouncement.value = next || null;
    state2.showBlockingAnnouncement.value = !!next;
    if (!next) {
      state2.blockingAnnouncementHtml.value = "";
      return;
    }
    const token = ++state2.mutable.blockingAnnouncementRenderToken;
    void renderMarkdown(next.content || next.body || "").then((html) => {
      if (token === state2.mutable.blockingAnnouncementRenderToken) state2.blockingAnnouncementHtml.value = html;
    });
  };
  const confirmBlockingAnnouncement = () => {
    const item = state2.blockingAnnouncement.value;
    markConfirmed(item?.id || item?.title);
    state2.showBlockingAnnouncement.value = false;
    state2.blockingAnnouncement.value = null;
    state2.blockingAnnouncementHtml.value = "";
    findNextBlockingAnnouncement();
  };
  const handleExternalOpen = async (url, event) => {
    const ev = event;
    if (ev) {
      if (ev.__hbuExternalHandled) return;
      ev.__hbuExternalHandled = true;
      ev.preventDefault();
    }
    const target = String(url || "").trim();
    if (!target || !isHttpLink(target)) return;
    await openExternal(target);
  };
  const handleContentClick = async (event) => {
    const ev = event;
    const target = ev?.target;
    const anchor = target?.closest?.("a");
    if (!anchor?.href) return;
    if (ev) {
      if (ev.__hbuExternalHandled) return;
      ev.__hbuExternalHandled = true;
      ev.preventDefault();
    }
    await openExternal(anchor.href);
  };
  const primeOcrEndpointFromCache = async () => {
    if (!isTauriRuntime()) return;
    const cached2 = getStoredOcrConfig();
    try {
      await invokeNative("set_ocr_runtime_config", {
        endpoints: cached2.endpoints || [],
        localFallbackEndpoints: cached2.local_fallback_endpoints || []
      });
    } catch {
      const endpoint = String(localStorage.getItem("hbu_ocr_endpoint") || "").trim();
      await invokeNative("set_ocr_endpoint", { endpoint }).catch(() => {
      });
    }
  };
  const applyRemoteConfig = async () => {
    if (isWebsiteDemoBuild()) return;
    try {
      const config = await fetchRemoteConfig();
      state2.remoteConfig.value = config;
      const announcements = normalizeAnnouncements(config.announcements);
      if (hasAnnouncementContent(announcements)) {
        state2.announcementData.value = announcements;
        persistAnnouncementSnapshot(announcements);
      } else {
        const snapshot = restoreAnnouncementSnapshot();
        if (snapshot) state2.announcementData.value = snapshot;
      }
      await applyOcrRuntimeConfig(config);
      window.dispatchEvent(new CustomEvent("hbu-ocr-config-updated"));
      const uploadEndpoint = String(config?.temp_file_server?.schedule_upload_endpoint || "").trim();
      if (uploadEndpoint) localStorage.setItem("hbu_temp_upload_endpoint", uploadEndpoint);
      else localStorage.removeItem("hbu_temp_upload_endpoint");
      await invokeNative("set_temp_upload_endpoint", { endpoint: uploadEndpoint || null }).catch(() => {
      });
      const minVersion = String(config?.force_update?.min_version || "").trim();
      if (minVersion && compareVersions(await getCurrentVersion(), minVersion) < 0) {
        state2.forceUpdateInfo.value = allowsInAppGithubUpdater() ? {
          min_version: minVersion,
          message: config.force_update?.message || "当前版本过低，请更新后继续使用。",
          download_url: config.force_update?.download_url || ""
        } : {
          min_version: minVersion,
          message: config.force_update?.message || "当前版本过低，请通过 App Store 更新后继续使用。",
          download_url: "",
          store_url: "",
          apple_app_id: ""
        };
        state2.showForceUpdate.value = true;
      }
      findNextBlockingAnnouncement();
    } catch (error) {
      console.warn("[Config] 远程配置加载失败:", error);
    }
  };
  const stopRemoteConfigRefresh = () => {
    if (!state2.mutable.remoteConfigRefreshTimer) return;
    window.clearInterval(state2.mutable.remoteConfigRefreshTimer);
    state2.mutable.remoteConfigRefreshTimer = null;
  };
  const startRemoteConfigRefresh = () => {
    stopRemoteConfigRefresh();
    if (!isRemoteConfigEnabled()) return;
    state2.mutable.remoteConfigRefreshTimer = window.setInterval(() => {
      void applyRemoteConfig();
    }, REMOTE_CONFIG_REFRESH_INTERVAL);
  };
  const handleRemoteConfigModeChanged = () => {
    stopRemoteConfigRefresh();
    void applyRemoteConfig().finally(startRemoteConfigRefresh);
  };
  const handleRemoteConfigUpdated = () => {
    void applyRemoteConfig();
  };
  const cached = restoreAnnouncementSnapshot();
  if (cached) state2.announcementData.value = cached;
  return {
    applyRemoteConfig,
    startRemoteConfigRefresh,
    stopRemoteConfigRefresh,
    handleRemoteConfigModeChanged,
    handleRemoteConfigUpdated,
    primeOcrEndpointFromCache,
    openAnnouncement,
    closeAnnouncement,
    confirmBlockingAnnouncement,
    findNextBlockingAnnouncement,
    handleContentClick,
    handleExternalOpen
  };
};
const msUntilNextDayCrossover = () => {
  const parts = new Intl.DateTimeFormat("en-US", {
    timeZone: "Asia/Shanghai",
    hour: "2-digit",
    minute: "2-digit",
    second: "2-digit",
    hour12: false
  }).formatToParts(/* @__PURE__ */ new Date());
  const get2 = (type) => Number(parts.find((part) => part.type === type)?.value || 0);
  return (86400 - (get2("hour") * 3600 + get2("minute") * 60 + get2("second")) + 60) * 1e3;
};
const createNotificationCoordinator = (runtime) => {
  const { state: state2 } = runtime;
  const stopWidgetCrossDayTimer = () => {
    if (!state2.mutable.widgetCrossDayTimer) return;
    window.clearTimeout(state2.mutable.widgetCrossDayTimer);
    state2.mutable.widgetCrossDayTimer = null;
  };
  const scheduleWidgetCrossDayTimer = () => {
    stopWidgetCrossDayTimer();
    if (!state2.studentId.value) return;
    state2.mutable.widgetCrossDayTimer = window.setTimeout(() => {
      state2.mutable.widgetCrossDayTimer = null;
      if (state2.studentId.value && !isTestAccountSession()) {
        void tryWriteSnapshotFromCache(state2.studentId.value);
        scheduleWidgetCrossDayTimer();
      }
    }, msUntilNextDayCrossover());
  };
  const handleWidgetDeeplinkPayload = (payload) => {
    if (!payload) return;
    const date = String(payload.date || "").trim();
    const source = String(payload.source || "").trim();
    const period = Number(payload.period) || 0;
    if (!/^\d{4}-\d{2}-\d{2}$/.test(date) || source && source !== "widget") return;
    state2.widgetDeeplinkDate.value = date;
    state2.widgetDeeplinkPeriod.value = period;
    if (state2.currentView.value !== "schedule") runtime.navigation.goToView("schedule", { push: true });
  };
  const handleNavigatePayload = (payload) => {
    const view = normalizeViewName(String(payload?.view || "").trim());
    const source = String(payload?.source || "widget").trim();
    if (!["electricity", "exams"].includes(view) || source && source !== "widget") return;
    runtime.navigation.goToView(view, { push: true });
  };
  const runAfterBoot = (callback) => {
    if (state2.mutable.appBootstrapped) return callback();
    const timer = window.setInterval(() => {
      if (!state2.mutable.appBootstrapped) return;
      window.clearInterval(timer);
      callback();
    }, 100);
    window.setTimeout(() => window.clearInterval(timer), 5e3);
  };
  const dispatchMiniHbutDeepLink = (link) => {
    if (link.kind === "widget-schedule") {
      runAfterBoot(
        () => handleWidgetDeeplinkPayload({
          date: link.date,
          period: link.period,
          source: link.source
        })
      );
      return;
    }
    if (link.kind === "navigate") {
      runAfterBoot(() => handleNavigatePayload({ view: link.view, source: link.source }));
      return;
    }
    runtime.identity.submitIntent({
      requestId: link.requestId,
      handoff: link.handoff,
      arrivedAt: Date.now()
    });
  };
  const installWidgetDeeplinkListeners = () => {
    window.addEventListener("widgetDeeplink", ((event) => {
      try {
        const detail = typeof event.detail === "string" ? JSON.parse(event.detail) : event.detail;
        handleWidgetDeeplinkPayload(detail || {});
      } catch {
      }
    }));
    window.addEventListener("widgetNavigate", ((event) => {
      try {
        const detail = typeof event.detail === "string" ? JSON.parse(event.detail) : event.detail;
        handleNavigatePayload(detail || {});
      } catch {
      }
    }));
    void installMiniHbutDeepLinkListeners(dispatchMiniHbutDeepLink);
  };
  const installNotificationActionListener = async () => {
    try {
      const remove = await platformBridge.addNotificationActionListener((payload) => {
        const { view } = resolveNotificationActionTarget(payload);
        runAfterBoot(() => runtime.navigation.goToView(view, { push: true }));
      });
      state2.mutable.removeNotificationActionListener = typeof remove === "function" ? remove : null;
    } catch {
      state2.mutable.removeNotificationActionListener = null;
    }
  };
  return {
    installNotificationActionListener,
    installWidgetDeeplinkListeners,
    scheduleWidgetCrossDayTimer,
    stopWidgetCrossDayTimer,
    handleWidgetDeeplinkPayload
  };
};
const IDENTITY_QUEUE_MAX = 3;
const IDENTITY_QUEUE_FULL_MESSAGE = "已有多个待处理授权，请先完成当前请求";
const DUPLICATE_MESSAGE = "该授权请求已存在，请先完成当前请求";
let phase = "idle";
let activeIntent = null;
const queue = [];
let lastCompleted = null;
const listeners = /* @__PURE__ */ new Set();
const notify = () => {
  for (const listener of [...listeners]) {
    try {
      listener();
    } catch {
    }
  }
};
const toPending = (intent) => ({
  requestId: intent.requestId,
  handoff: intent.handoff,
  arrivedAt: intent.arrivedAt,
  phase: "received"
});
const promoteNext = () => {
  activeIntent = queue.shift() ?? null;
  phase = activeIntent ? "received" : "idle";
};
const getIdentityIntentSnapshot = () => ({
  phase,
  active: activeIntent ? { ...activeIntent } : null,
  queue: queue.map((item) => ({ ...item })),
  lastCompleted: lastCompleted ? { ...lastCompleted } : null
});
const subscribeIdentityIntentStore = (listener) => {
  listeners.add(listener);
  return () => {
    listeners.delete(listener);
  };
};
const enqueueIdentityIntent = (intent) => {
  const exists = activeIntent !== null && activeIntent.requestId === intent.requestId || queue.some((item) => item.requestId === intent.requestId);
  if (exists) {
    return { accepted: false, reason: "duplicate", message: DUPLICATE_MESSAGE };
  }
  if (queue.length >= IDENTITY_QUEUE_MAX) {
    return { accepted: false, reason: "queue-full", message: IDENTITY_QUEUE_FULL_MESSAGE };
  }
  queue.push(toPending(intent));
  if (activeIntent === null) promoteNext();
  notify();
  return { accepted: true };
};
const setIdentityIntentPhase = (requestId, nextPhase, options = {}) => {
  if (activeIntent === null || activeIntent.requestId !== requestId) return;
  activeIntent.phase = nextPhase;
  if (options.detail !== void 0) activeIntent.detail = options.detail;
  if (nextPhase === "error") activeIntent.error = options.error || "授权请求处理失败";
  notify();
};
const completeIdentityIntent = (requestId, status, error) => {
  if (activeIntent === null || activeIntent.requestId !== requestId) return;
  activeIntent.phase = status;
  if (status === "error") activeIntent.error = error || "授权请求处理失败";
  lastCompleted = { ...activeIntent };
  activeIntent = queue.shift() ?? null;
  phase = activeIntent ? "received" : "idle";
  notify();
};
const dismissIdentityIntent = (requestId) => {
  let changed = false;
  if (activeIntent !== null && activeIntent.requestId === requestId) {
    activeIntent = queue.shift() ?? null;
    phase = activeIntent ? "received" : "idle";
    changed = true;
  } else {
    const index = queue.findIndex((item) => item.requestId === requestId);
    if (index >= 0) {
      queue.splice(index, 1);
      changed = true;
    }
  }
  if (changed) notify();
};
const resetIdentityIntentStore = () => {
  queue.length = 0;
  activeIntent = null;
  lastCompleted = null;
  phase = "idle";
  notify();
};
class IdentityServiceError extends Error {
  constructor(code, message, internalDetail) {
    super(message);
    __publicField(this, "code");
    /** 内部脱敏细节（仅日志，不展示） */
    __publicField(this, "internalDetail");
    this.name = "IdentityServiceError";
    this.code = code;
    this.internalDetail = internalDetail;
  }
}
const IDENTITY_DEVICE_ID_KEY = "hbu_identity_device_id";
const IDENTITY_CORE_BASE_URL_DEFAULT = "https://id.xn--vhq74jc2fzpchter27a.com";
const reportIdentityDiag = (event, details) => {
  try {
    void fetch("http://127.0.0.1:4399/debug/logs/push", {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({ scope: "identity-diag", level: "info", message: event, details }),
      signal: AbortSignal.timeout(1500)
    }).catch(() => void 0);
  } catch {
  }
};
const IDENTITY_CORE_BASE_URL_KEY = "hbu_identity_core_base_url";
const IDENTITY_BFF_BASE_URL_DEFAULT = "https://auth.xn--vhq74jc2fzpchter27a.com";
const IDENTITY_BFF_BASE_URL_KEY = "hbu_identity_bff_base_url";
const IDENTITY_REQUEST_TIMEOUT_MS = 8e3;
const IDENTITY_TERMINAL_ACTION_TIMEOUT_MS = 3e3;
const isTestAccountBlocked = () => {
  try {
    return typeof localStorage !== "undefined" && localStorage.getItem("hbu_test_account_session") === "1";
  } catch {
    return false;
  }
};
const resolveLocalIdentityOverride = (storageKey, fallback) => {
  return fallback;
};
const getIdentityBffBaseUrl = () => resolveLocalIdentityOverride(IDENTITY_BFF_BASE_URL_KEY, IDENTITY_BFF_BASE_URL_DEFAULT);
const getIdentityCoreBaseUrl = () => resolveLocalIdentityOverride(IDENTITY_CORE_BASE_URL_KEY, IDENTITY_CORE_BASE_URL_DEFAULT);
const safeHeaders = (headers) => headers;
const requestJson = async (url, options, origin) => {
  if (isTauriRuntime()) {
    try {
      const parsed = new URL(url);
      const output = await invokeNative("identity_core_fetch", {
        input: {
          method: options.method,
          origin: origin ?? "core",
          path: parsed.pathname + parsed.search,
          headers: safeHeaders(options.headers),
          body: options.body
        }
      });
      let data = null;
      if (output.body) {
        try {
          data = JSON.parse(output.body);
        } catch {
          data = null;
        }
      }
      return { status: output.status, data };
    } catch (err) {
      reportIdentityDiag("network_error", {
        url: String(url).slice(0, 120),
        error: String(err?.message || "identity_core_fetch failed").slice(0, 200)
      });
      throw createServiceError(
        "network_unavailable",
        "网络不可用，无法连接身份服务，请稍后重试",
        String(err?.message || "identity_core_fetch failed")
      );
    }
  }
  const controller = new AbortController();
  const timer = window.setTimeout(() => controller.abort(), options.timeoutMs);
  try {
    const response = await fetch(url, {
      method: options.method,
      headers: safeHeaders(options.headers),
      body: options.body === void 0 ? void 0 : JSON.stringify(options.body),
      signal: controller.signal
    });
    let data = null;
    const text = await response.text();
    if (text) {
      try {
        data = JSON.parse(text);
      } catch {
        data = null;
      }
    }
    return { status: response.status, data };
  } catch (err) {
    reportIdentityDiag("network_error", {
      url: String(url).slice(0, 120),
      error: String(err?.message || "fetch failed").slice(0, 200),
      cause: String(err?.cause || "").slice(0, 200)
    });
    throw createServiceError(
      "network_unavailable",
      "网络不可用，无法连接身份服务，请稍后重试",
      String(err?.message || "fetch failed")
    );
  } finally {
    window.clearTimeout(timer);
  }
};
const parseErrorPayload = (data, fallback) => {
  const err = data?.error;
  if (err && typeof err === "object") {
    const code = String(err?.code || "");
    const message = String(err?.message || "").trim();
    return { code: code || `HTTP_${fallback.status}`, message: message || fallback.message };
  }
  if (typeof err === "string") {
    return { code: err, message: fallback.message };
  }
  return { code: `HTTP_${fallback.status}`, message: fallback.message };
};
const mapServerCode = (code) => {
  switch (code.toUpperCase()) {
    case "AUTH_REQUEST_EXPIRED":
    case "EXPIRED":
      return "request_expired";
    case "AUTH_REQUEST_NOT_FOUND":
    case "NOT_FOUND":
      return "request_not_found";
    case "CLIENT_NOT_FOUND":
    case "CLIENT_UNAVAILABLE":
      return "client_unavailable";
    case "INVALID_HANDOFF":
      return "invalid_handoff";
    case "DEVICE_NOT_ACTIVE":
    case "DEVICE_AUTH_FAILED":
      return "device_revoked";
    case "SIGNATURE_INVALID":
    case "STALE_ISSUED_AT":
      return "signature_rejected";
    case "TEST_ACCOUNT_REJECTED":
      return "test_account_blocked";
    case "CHALLENGE_INVALID":
      return "device_not_bound";
    case "LINK_REQUIRED":
    case "AUTH_REQUEST_ALREADY_APPROVED":
    case "AUTH_REQUEST_INVALID_TRANSITION":
      return "request_not_found";
    default:
      return "unknown";
  }
};
const mapStatusFallback = (status) => {
  if (status === 401) return "invalid_handoff";
  if (status === 403) return "device_revoked";
  if (status === 404) return "request_not_found";
  if (status === 410) return "request_expired";
  if (status === 422) return "client_unavailable";
  if (status === 409) return "request_not_found";
  return "unknown";
};
const DEFAULT_MESSAGES = {
  request_expired: "应用请求已过期，请从网页重新发起",
  request_not_found: "请求不存在或已完成",
  client_unavailable: "应用已被暂停，无法继续授权",
  invalid_handoff: "接力凭据无效，请从网页重新发起授权",
  network_unavailable: "网络不可用，无法连接身份服务，请稍后重试",
  device_not_bound: "当前设备尚未绑定到身份服务",
  device_revoked: "当前设备已被撤销，无法继续授权",
  session_revalidation_required: "学校登录需要重新验证，请重新登录后再试",
  secure_storage_unavailable: "本机安全存储不可用，无法完成授权",
  signature_rejected: "服务器无法验证签名，请重试或重新发起授权",
  signing_material_missing: "授权签名材料不完整，请更新 App 后重试",
  test_account_blocked: "测试账号不能用于正式身份服务",
  unknown: "授权处理失败，请稍后重试"
};
const createServiceError = (code, message, internalDetail) => new IdentityServiceError(code, message || DEFAULT_MESSAGES[code], internalDetail);
const throwMappedError = (status, data, context) => {
  const payload = parseErrorPayload(data, {
    status,
    message: DEFAULT_MESSAGES[mapStatusFallback(status)]
  });
  const code = mapServerCode(payload.code);
  const message = payload.message || DEFAULT_MESSAGES[code];
  throw createServiceError(code, message, `${context} -> HTTP ${status} ${payload.code}`);
};
const buildHandoffHeaders = (handoff) => ({
  "Content-Type": "application/json",
  "x-identity-handoff": handoff
});
const buildAuthorizationHandoffHeaders = (handoff) => ({
  "Content-Type": "application/json",
  Authorization: `Handoff ${handoff}`
});
const fetchRequestDetail = async (input) => {
  const { baseUrl, requestId, handoff } = input;
  reportIdentityDiag("detail_start", { baseUrl, requestId });
  try {
    const { status, data } = await requestJson(
      `${baseUrl}/api/auth/requests/${requestId}`,
      {
        method: "GET",
        headers: buildHandoffHeaders(handoff),
        timeoutMs: IDENTITY_REQUEST_TIMEOUT_MS
      },
      "bff"
    );
    reportIdentityDiag("detail_status", { status });
    if (status !== 200) {
      throwMappedError(status, data, "fetchRequestDetail");
    }
    const detail = data;
    if (!detail || typeof detail !== "object" || !detail.request_id || !detail.client) {
      throw createServiceError("unknown", "身份服务返回了无效数据", "fetchRequestDetail malformed payload");
    }
    reportIdentityDiag("detail_ok", { requestId: detail.request_id, client: detail.client?.name });
    return {
      request_id: String(detail.request_id),
      expires_at: String(detail.expires_at || ""),
      client: {
        name: String(detail.client?.name || ""),
        homepage_host: String(detail.client?.homepage_host || ""),
        developer_display_name: String(detail.client?.developer_display_name || ""),
        review_status: String(detail.client?.review_status || ""),
        is_test: detail.client?.is_test === true
      },
      scopes: Array.isArray(detail.scopes) ? detail.scopes.filter((s) => s && typeof s === "object" && typeof s.id === "string").map((s) => ({
        id: String(s.id),
        label: String(s.label || ""),
        risk: s.risk === "sensitive" ? "sensitive" : "basic"
      })) : [],
      // 宽容解析：未来 Core 下发签名材料（issue #623 示例 JSON 含 challenge）
      challenge: typeof detail.challenge === "string" ? detail.challenge : void 0,
      client_id: typeof detail.client_id === "string" ? detail.client_id : void 0
    };
  } catch (err) {
    reportIdentityDiag("detail_failed", {
      requestId,
      error: String(err?.message || "").slice(0, 200)
    });
    throw err;
  }
};
const fetchEnrollmentChallenge = async (input) => {
  if (isTestAccountBlocked()) {
    throw createServiceError("test_account_blocked", DEFAULT_MESSAGES.test_account_blocked);
  }
  const { baseUrl, handoff } = input;
  const { status, data } = await requestJson(`${baseUrl}/api/v1/app/devices/enrollment-challenges`, {
    method: "POST",
    headers: buildAuthorizationHandoffHeaders(handoff),
    body: { purpose: "device_enrollment" },
    timeoutMs: IDENTITY_REQUEST_TIMEOUT_MS
  });
  if (status !== 200) {
    throwMappedError(status, data, "fetchEnrollmentChallenge");
  }
  const challenge = data?.challenge;
  if (typeof challenge !== "string" || !challenge) {
    throw createServiceError("device_not_bound", "设备绑定失败，请重试", "enrollment challenge malformed");
  }
  return {
    challenge,
    expires_at: String(data?.expires_at || "")
  };
};
const submitApprove = async (input) => {
  if (isTestAccountBlocked()) {
    throw createServiceError("test_account_blocked", DEFAULT_MESSAGES.test_account_blocked);
  }
  const { baseUrl, requestId, handoff, approval } = input;
  const body = {
    device_id: approval.device_id,
    issued_at: approval.issued_at,
    nonce: approval.nonce,
    signature: approval.signature,
    canonical_version: approval.canonical_version
  };
  const { status, data } = await requestJson(
    `${baseUrl}/api/v1/app/auth-requests/${requestId}/approve`,
    {
      method: "POST",
      headers: buildAuthorizationHandoffHeaders(handoff),
      body,
      timeoutMs: IDENTITY_REQUEST_TIMEOUT_MS
    }
  );
  if (status !== 200) {
    throwMappedError(status, data, "submitApprove");
  }
  const result = data;
  return {
    request_id: String(result?.request_id || requestId),
    status: String(result?.status || ""),
    approved_at: result?.approved_at ?? null,
    already_approved: !!result?.already_approved
  };
};
const fetchAuthHistory = async () => {
  const deviceId = typeof localStorage !== "undefined" ? (localStorage.getItem(IDENTITY_DEVICE_ID_KEY) || "").trim() : "";
  if (!deviceId) {
    throw createServiceError("device_not_bound", "本机尚未注册为身份签名设备，请先在设置中完成设备注册");
  }
  if (!isTauriRuntime()) {
    throw createServiceError("device_not_bound", "授权记录仅支持在桌面端查看");
  }
  try {
    const output = await identityFetchAuthHistory({
      base_url: getIdentityCoreBaseUrl(),
      device_id: deviceId
    });
    if (output.status !== 200) {
      let data = null;
      if (output.body) {
        try {
          data = JSON.parse(output.body);
        } catch {
          data = null;
        }
      }
      throwMappedError(output.status, data, "fetchAuthHistory");
    }
    const parsed = JSON.parse(output.body || "{}");
    return Array.isArray(parsed?.items) ? parsed.items : [];
  } catch (err) {
    if (err instanceof IdentityServiceError) throw err;
    reportIdentityDiag("auth_history_error", {
      error: String(err?.message || "fetchAuthHistory failed").slice(0, 200)
    });
    throw createServiceError(
      "network_unavailable",
      "无法加载授权记录，请稍后重试",
      String(err?.message || "fetchAuthHistory failed")
    );
  }
};
const submitTerminalAction = async (input) => {
  const { baseUrl, requestId, handoff, action } = input;
  try {
    const { status } = await requestJson(
      `${baseUrl}/api/v1/app/auth-requests/${requestId}/${action}`,
      {
        method: "POST",
        headers: buildAuthorizationHandoffHeaders(handoff),
        body: {},
        timeoutMs: IDENTITY_TERMINAL_ACTION_TIMEOUT_MS
      }
    );
    return status === 200 || status === 204;
  } catch {
    return false;
  }
};
const BOOT_WAIT_INTERVAL_MS = 100;
const BOOT_WAIT_MAX_TICKS = 50;
const IDENTITY_LOGIN_RESUMED_EVENT = "hbu-identity-login-resumed";
const SESSION_LOGOUT_EVENT = "hbu-session-logout";
const createIdentityCoordinator = (runtime) => {
  const { state: state2 } = runtime;
  let pendingBuffer = [];
  let bootWaitTimer = null;
  let flowRequestId = null;
  let actionInFlight = false;
  let pendingTerminal = null;
  let expiryTimer = null;
  let removeResumeListener = null;
  let removeLogoutListener = null;
  const clearExpiryTimer = () => {
    if (expiryTimer !== null) {
      clearTimeout(expiryTimer);
      expiryTimer = null;
    }
  };
  const isCurrentActive = (requestId) => getIdentityIntentSnapshot().active?.requestId === requestId;
  const toUserSafeError = (err) => {
    if (err instanceof IdentityServiceError) {
      return {
        code: err.code,
        message: err.message,
        retryable: err.code === "network_unavailable"
      };
    }
    const message = String(err?.message || err || "授权处理失败").trim();
    return { code: "unknown", message: message || "授权处理失败", retryable: false };
  };
  const scheduleExpiryTimer = (expiresAtIso, requestId) => {
    clearExpiryTimer();
    const expires = new Date(expiresAtIso).getTime();
    if (!Number.isFinite(expires)) return;
    const delay = Math.max(0, expires - Date.now()) + 500;
    expiryTimer = setTimeout(() => {
      expiryTimer = null;
      if (!isCurrentActive(requestId)) return;
      const active = getIdentityIntentSnapshot().active;
      if (!active || active.phase === "done" || active.phase === "error") return;
      if (actionInFlight) return;
      if (pendingTerminal) return;
      clearExpiryTimer();
      finishIdentityRequest(requestId, "expired", {
        message: "应用请求已过期，请从网页重新发起授权",
        errorCode: "request_expired"
      });
      pendingTerminal = { requestId, status: "error", error: "应用请求已过期" };
    }, delay);
  };
  const proceedLoggedIn = async (active, detail) => {
    let sensitiveVerified = false;
    if (hasSensitiveScope(detail.scopes)) {
      setIdentityApprovalPhase("validating_session", { requestId: active.requestId, detail });
      setIdentitySessionValidation("validating");
      let verified = false;
      try {
        verified = await runtime.session.refreshSessionVerified({ quiet: true });
      } catch {
        verified = false;
      }
      if (!isCurrentActive(active.requestId)) return;
      if (!verified) {
        let recovered = false;
        try {
          recovered = await runtime.session.attemptOnlineRecovery({ silent: true });
        } catch {
          recovered = false;
        }
        if (!isCurrentActive(active.requestId)) return;
        if (!recovered) {
          finishIdentityRequest(active.requestId, "error", {
            message: "学校登录需要重新验证，请重新登录后再试",
            errorCode: "session_revalidation_required"
          });
          pendingTerminal = { requestId: active.requestId, status: "error", error: "学校登录需要重新验证" };
          return;
        }
      }
      markIdentityVerifiedNow();
      sensitiveVerified = true;
    }
    setIdentityApprovalPhase("ready", { requestId: active.requestId, detail });
    setIdentityIntentPhase(active.requestId, "ready", { detail });
    if (sensitiveVerified) setIdentitySessionValidation("ok");
  };
  const runActiveFlow = async (active) => {
    if (active.phase !== "received") return;
    if (flowRequestId === active.requestId) return;
    flowRequestId = active.requestId;
    try {
      setIdentityLastResult(null);
      clearExpiryTimer();
      setIdentityApprovalPhase("loading_request", { requestId: active.requestId });
      setIdentityIntentPhase(active.requestId, "loading");
      const detail = await fetchRequestDetail({
        baseUrl: getIdentityBffBaseUrl(),
        requestId: active.requestId,
        handoff: active.handoff
      });
      if (!isCurrentActive(active.requestId)) return;
      scheduleExpiryTimer(detail.expires_at, active.requestId);
      const loggedIn = !!state2.studentId.value && (state2.isLoggedIn.value || !!state2.studentId.value);
      if (!loggedIn) {
        setIdentityApprovalPhase("needs_login", { requestId: active.requestId, detail, needsLogin: true });
        setIdentityIntentPhase(active.requestId, "awaiting_local_login", { detail });
        return;
      }
      await proceedLoggedIn(active, detail);
    } catch (err) {
      if (!isCurrentActive(active.requestId)) return;
      const mapped = toUserSafeError(err);
      finishIdentityRequest(active.requestId, "error", {
        message: mapped.message,
        errorCode: mapped.code,
        retryable: mapped.retryable
      });
      pendingTerminal = { requestId: active.requestId, status: "error", error: mapped.message };
    } finally {
      if (flowRequestId === active.requestId) flowRequestId = null;
    }
  };
  const handleIntentStoreChange = () => {
    const snap = getIdentityIntentSnapshot();
    setIdentityQueuedCount(snap.queue.length);
    if (snap.phase === "received" && snap.active) {
      void runActiveFlow(snap.active);
    }
  };
  const ensureDeviceBound = async (handoff) => {
    let status = null;
    try {
      status = await invokeNative("identity_device_status");
    } catch {
      status = null;
    }
    if (status === null || status.available === false) {
      throw new IdentityServiceError(
        "secure_storage_unavailable",
        "本机安全存储不可用，无法完成授权",
        status?.error || "identity_device_status unavailable"
      );
    }
    if (!status.has_key) {
      const { challenge } = await fetchEnrollmentChallenge({
        baseUrl: getIdentityCoreBaseUrl(),
        handoff
      });
      const enroll = await invokeNative("identity_enroll_device", {
        baseUrl: getIdentityCoreBaseUrl(),
        challenge,
        deviceName: getIdentityDeviceDisplayName(),
        handoff
      });
      setIdentityDeviceMeta({ deviceId: enroll.device_id, userId: enroll.user_id });
      return enroll.device_id;
    }
    const storedDeviceId = identityUiState.deviceId;
    if (!storedDeviceId) {
      throw new IdentityServiceError(
        "device_not_bound",
        "当前设备尚未完成绑定，请先完成绑定后再授权",
        "device key exists but device_id missing locally"
      );
    }
    return storedDeviceId;
  };
  const approveActive = async () => {
    const snap = getIdentityIntentSnapshot();
    const active = snap.active;
    if (!active || active.phase !== "ready") return;
    if (actionInFlight) return;
    if (pendingTerminal) return;
    actionInFlight = true;
    const requestId = active.requestId;
    const detail = active.detail;
    try {
      if (!detail) {
        throw new IdentityServiceError("unknown", "请求数据缺失，请重新发起授权", "approve without detail");
      }
      if (!detail.challenge || !detail.client_id) {
        throw createServiceError(
          "signing_material_missing",
          void 0,
          `core detail missing challenge/client_id (request=${requestId})`
        );
      }
      setIdentityApprovalPhase("approving", { requestId });
      setIdentityIntentPhase(requestId, "approving");
      const deviceId = await ensureDeviceBound(active.handoff);
      if (!isCurrentActive(requestId)) return;
      const signed = await invokeNative("identity_sign_auth_request", {
        input: {
          request_id: requestId,
          challenge: detail.challenge,
          client_id: detail.client_id,
          scopes: detail.scopes.map((scope2) => scope2.id),
          device_id: deviceId
        }
      });
      if (!isCurrentActive(requestId)) return;
      await submitApprove({
        baseUrl: getIdentityCoreBaseUrl(),
        requestId,
        handoff: active.handoff,
        approval: signed
      });
      if (!isCurrentActive(requestId)) return;
      clearExpiryTimer();
      finishIdentityRequest(requestId, "approved", {
        message: "已允许登录，网页将自动完成登录，你可以返回浏览器"
      });
      pendingTerminal = { requestId, status: "done" };
    } catch (err) {
      if (!isCurrentActive(requestId)) return;
      const mapped = toUserSafeError(err);
      clearExpiryTimer();
      finishIdentityRequest(requestId, "error", {
        message: mapped.message,
        errorCode: mapped.code,
        retryable: mapped.retryable
      });
      pendingTerminal = { requestId, status: "error", error: mapped.message };
    } finally {
      actionInFlight = false;
    }
  };
  const runTerminalAction = async (action) => {
    const snap = getIdentityIntentSnapshot();
    const active = snap.active;
    if (!active) return;
    const allowed = ["received", "loading", "ready", "awaiting_local_login"];
    if (!allowed.includes(active.phase)) return;
    if (actionInFlight) return;
    if (pendingTerminal) return;
    actionInFlight = true;
    const requestId = active.requestId;
    const handoff = active.handoff;
    try {
      setIdentityApprovalPhase("denying", { requestId });
      const delivered = await submitTerminalAction({
        baseUrl: getIdentityCoreBaseUrl(),
        requestId,
        handoff,
        action
      });
      if (!isCurrentActive(requestId)) return;
      if (action === "deny") {
        finishIdentityRequest(requestId, "denied", {
          message: delivered ? "已拒绝授权" : "已拒绝授权（服务器端请求将自动过期）"
        });
      } else {
        finishIdentityRequest(requestId, "cancelled", {
          message: delivered ? "已取消授权" : "已取消授权（服务器端请求将自动过期）"
        });
      }
      pendingTerminal = { requestId, status: "done" };
    } finally {
      actionInFlight = false;
    }
  };
  const confirmResult = () => {
    clearIdentityLastResult();
    const pending = pendingTerminal;
    pendingTerminal = null;
    if (pending) {
      completeIdentityIntent(pending.requestId, pending.status, pending.error);
    }
  };
  const resumeAfterLogin = async () => {
    const snap = getIdentityIntentSnapshot();
    const active = snap.active;
    if (!active || active.phase !== "awaiting_local_login") return;
    const detail = active.detail;
    if (!detail) return;
    setIdentitySuppressedForLogin(false);
    setIdentityApprovalPhase("validating_session", { requestId: active.requestId, detail });
    setIdentityIntentPhase(active.requestId, "loading");
    await proceedLoggedIn(active, detail);
  };
  const goLogin = () => {
    const snap = getIdentityIntentSnapshot();
    const active = snap.active;
    if (!active || active.phase !== "awaiting_local_login") return;
    setIdentitySuppressedForLogin(true);
    runtime.navigation.goToView("me", { push: true });
  };
  const stopBootWait = () => {
    if (bootWaitTimer !== null) {
      clearTimeout(bootWaitTimer);
      bootWaitTimer = null;
    }
  };
  const flushPendingIntents = () => {
    stopBootWait();
    if (pendingBuffer.length === 0) return;
    const batch = pendingBuffer;
    pendingBuffer = [];
    for (const intent of batch) {
      const result = enqueueIdentityIntent(intent);
      if (!result.accepted) showToast(result.message, "warning");
    }
  };
  const scheduleBootWait = () => {
    if (bootWaitTimer !== null || state2.mutable.appBootstrapped) return;
    let ticks = 0;
    const tick = () => {
      if (state2.mutable.appBootstrapped) {
        flushPendingIntents();
        return;
      }
      ticks += 1;
      if (ticks >= BOOT_WAIT_MAX_TICKS) {
        bootWaitTimer = null;
        return;
      }
      bootWaitTimer = setTimeout(tick, BOOT_WAIT_INTERVAL_MS);
    };
    bootWaitTimer = setTimeout(tick, BOOT_WAIT_INTERVAL_MS);
  };
  const submitIntent = (intent) => {
    if (!intent || typeof intent.requestId !== "string" || intent.requestId === "") return;
    if (typeof intent.handoff !== "string" || intent.handoff === "") return;
    const normalized = {
      requestId: intent.requestId,
      handoff: intent.handoff,
      arrivedAt: typeof intent.arrivedAt === "number" ? intent.arrivedAt : Date.now()
    };
    if (!state2.mutable.appBootstrapped) {
      pendingBuffer.push(normalized);
      scheduleBootWait();
      return;
    }
    const result = enqueueIdentityIntent(normalized);
    if (!result.accepted) showToast(result.message, "warning");
  };
  const handleLoginResumed = () => {
    void resumeAfterLogin();
  };
  const handleSessionLogout = () => {
    const snap = getIdentityIntentSnapshot();
    const active = snap.active;
    if (!active) return;
    if (active.phase === "ready" || active.phase === "loading") {
      setIdentityApprovalPhase("needs_login", { requestId: active.requestId, needsLogin: true });
      setIdentityIntentPhase(active.requestId, "awaiting_local_login");
    }
  };
  const installGlobalListeners = () => {
    if (removeResumeListener !== null) return;
    const onResume = () => handleLoginResumed();
    const onLogout = () => handleSessionLogout();
    window.addEventListener(IDENTITY_LOGIN_RESUMED_EVENT, onResume);
    window.addEventListener(SESSION_LOGOUT_EVENT, onLogout);
    removeResumeListener = () => window.removeEventListener(IDENTITY_LOGIN_RESUMED_EVENT, onResume);
    removeLogoutListener = () => window.removeEventListener(SESSION_LOGOUT_EVENT, onLogout);
  };
  const uninstallGlobalListeners = () => {
    removeResumeListener?.();
    removeLogoutListener?.();
    removeResumeListener = null;
    removeLogoutListener = null;
  };
  const unsubscribeStore = subscribeIdentityIntentStore(handleIntentStoreChange);
  if (typeof window !== "undefined") {
    installGlobalListeners();
  }
  const dispose = () => {
    stopBootWait();
    clearExpiryTimer();
    pendingBuffer = [];
    pendingTerminal = null;
    unsubscribeStore();
    uninstallGlobalListeners();
  };
  return {
    // #621 合同（原样保留）
    submitIntent,
    flushPendingIntents,
    completeIntent: (requestId, status, error) => completeIdentityIntent(requestId, status, error),
    dismissIntent: (requestId) => dismissIdentityIntent(requestId),
    setPhase: (requestId, nextPhase, options) => setIdentityIntentPhase(requestId, nextPhase, options),
    reset: () => {
      dispose();
      resetIdentityIntentStore();
      resetIdentityUiState();
    },
    getSnapshot: () => getIdentityIntentSnapshot(),
    subscribe: (listener) => subscribeIdentityIntentStore(listener),
    dispose,
    // #623 扩展（审批流程 + 登录恢复）
    approveActive,
    denyActive: () => runTerminalAction("deny"),
    cancelActive: () => runTerminalAction("cancel"),
    resumeAfterLogin,
    goLogin,
    confirmResult
  };
};
const ERROR_STORE_KEY = "__MINI_HBUT_LAYOUT_DEBUG_ERRORS__";
const MAX_DOM_NODES = 90;
const MAX_DOM_DEPTH = 5;
let errorCaptureInstalled = false;
let cleanupErrorCapture = null;
const round = (value) => {
  const numberValue = Number(value);
  if (!Number.isFinite(numberValue)) return value;
  return Math.round(numberValue * 100) / 100;
};
const safeJson = (value) => {
  try {
    return JSON.stringify(value, null, 2);
  } catch (error) {
    return `[unserializable: ${String(error)}]`;
  }
};
const normalizeError = (error) => {
  if (error instanceof Error) {
    return {
      message: error.message,
      stack: error.stack
    };
  }
  if (error && typeof error === "object") {
    return {
      message: String(error.message || error),
      stack: String(error.stack || "")
    };
  }
  return {
    message: String(error || ""),
    stack: ""
  };
};
const pushErrorRecord = (record) => {
  if (typeof window === "undefined") return;
  const list = Array.isArray(window[ERROR_STORE_KEY]) ? window[ERROR_STORE_KEY] || [] : [];
  list.push(record);
  window[ERROR_STORE_KEY] = list.slice(-60);
};
const installHomeLayoutDiagnosticsErrorCapture = () => {
  if (typeof window === "undefined") return () => {
  };
  if (errorCaptureInstalled && cleanupErrorCapture) return cleanupErrorCapture;
  const handleError = (event) => {
    const normalized = normalizeError(event.error || event.message);
    pushErrorRecord({
      ts: Date.now(),
      type: "error",
      message: normalized.message || String(event.message || ""),
      source: event.filename,
      lineno: event.lineno,
      colno: event.colno,
      stack: normalized.stack
    });
  };
  const handleUnhandledRejection2 = (event) => {
    const normalized = normalizeError(event.reason);
    pushErrorRecord({
      ts: Date.now(),
      type: "unhandledrejection",
      message: normalized.message,
      stack: normalized.stack
    });
  };
  window.addEventListener("error", handleError);
  window.addEventListener("unhandledrejection", handleUnhandledRejection2);
  errorCaptureInstalled = true;
  cleanupErrorCapture = () => {
    window.removeEventListener("error", handleError);
    window.removeEventListener("unhandledrejection", handleUnhandledRejection2);
    errorCaptureInstalled = false;
    cleanupErrorCapture = null;
  };
  return cleanupErrorCapture;
};
const readRect = (element) => {
  if (!element || typeof element.getBoundingClientRect !== "function") return null;
  const rect = element.getBoundingClientRect();
  return {
    x: round(rect.x),
    y: round(rect.y),
    top: round(rect.top),
    right: round(rect.right),
    bottom: round(rect.bottom),
    left: round(rect.left),
    width: round(rect.width),
    height: round(rect.height)
  };
};
const readComputedStyle = (element) => {
  if (!element || typeof window === "undefined") return null;
  const style = window.getComputedStyle(element);
  const pick = [
    "position",
    "display",
    "visibility",
    "top",
    "right",
    "bottom",
    "left",
    "inset",
    "width",
    "height",
    "min-height",
    "max-height",
    "padding-top",
    "padding-right",
    "padding-bottom",
    "padding-left",
    "margin-top",
    "margin-right",
    "margin-bottom",
    "margin-left",
    "overflow",
    "overflow-x",
    "overflow-y",
    "transform",
    "z-index",
    "box-sizing",
    "border-radius",
    "background",
    "contain"
  ];
  return Object.fromEntries(pick.map((name) => [name, style.getPropertyValue(name)]));
};
const readElementSnapshot = (selector) => {
  const element = typeof document === "undefined" ? null : document.querySelector(selector);
  return {
    selector,
    found: !!element,
    tag: element?.tagName?.toLowerCase() || "",
    id: element?.id || "",
    className: element instanceof HTMLElement ? element.className : "",
    childElementCount: element?.childElementCount || 0,
    rect: readRect(element),
    style: readComputedStyle(element)
  };
};
const readSafeAreaProbe = () => {
  if (typeof document === "undefined" || typeof window === "undefined" || !document.body) {
    return null;
  }
  const probe = document.createElement("div");
  probe.setAttribute("data-home-layout-diagnostics-safe-area-probe", "true");
  probe.style.cssText = [
    "position: fixed",
    "left: 0",
    "bottom: 0",
    "width: 0",
    "height: 0",
    "padding-top: env(safe-area-inset-top, 0px)",
    "padding-right: env(safe-area-inset-right, 0px)",
    "padding-bottom: env(safe-area-inset-bottom, 0px)",
    "padding-left: env(safe-area-inset-left, 0px)",
    "visibility: hidden",
    "pointer-events: none",
    "z-index: -1"
  ].join(";");
  document.body.appendChild(probe);
  const style = window.getComputedStyle(probe);
  const result = {
    top: style.paddingTop,
    right: style.paddingRight,
    bottom: style.paddingBottom,
    left: style.paddingLeft
  };
  probe.remove();
  return result;
};
const readCssVariables = () => {
  if (typeof document === "undefined" || typeof window === "undefined") return {};
  const root = document.documentElement;
  const style = window.getComputedStyle(root);
  const variables = [
    "--app-vh",
    "--app-safe-bottom",
    "--app-safe-bottom-fallback",
    "--safe-top-fallback",
    "--ui-nav-style",
    "--ux-bottom-safe"
  ];
  return Object.fromEntries(
    variables.map((name) => [
      name,
      {
        inline: root.style.getPropertyValue(name).trim(),
        computed: style.getPropertyValue(name).trim()
      }
    ])
  );
};
const readViewport = () => {
  if (typeof window === "undefined") return null;
  const vv = window.visualViewport;
  return {
    innerWidth: round(window.innerWidth),
    innerHeight: round(window.innerHeight),
    outerWidth: round(window.outerWidth),
    outerHeight: round(window.outerHeight),
    devicePixelRatio: round(window.devicePixelRatio),
    scrollX: round(window.scrollX),
    scrollY: round(window.scrollY),
    visualViewport: vv ? {
      width: round(vv.width),
      height: round(vv.height),
      offsetLeft: round(vv.offsetLeft),
      offsetTop: round(vv.offsetTop),
      pageLeft: round(vv.pageLeft),
      pageTop: round(vv.pageTop),
      scale: round(vv.scale)
    } : null,
    screen: typeof screen === "undefined" ? null : {
      width: round(screen.width),
      height: round(screen.height),
      availWidth: round(screen.availWidth),
      availHeight: round(screen.availHeight),
      orientation: screen.orientation?.type || ""
    }
  };
};
const describeElementLine = (element, depth) => {
  const rect = readRect(element);
  const style = typeof window === "undefined" ? null : window.getComputedStyle(element);
  const id = element.id ? `#${element.id}` : "";
  const className = element instanceof HTMLElement && element.className ? `.${String(element.className).trim().split(/\s+/).slice(0, 5).join(".")}` : "";
  const layout = rect ? ` x=${rect.x} y=${rect.y} w=${rect.width} h=${rect.height} b=${rect.bottom}` : "";
  const css = style ? ` display=${style.display} position=${style.position} overflow=${style.overflow}/${style.overflowY}` : "";
  return `${"  ".repeat(depth)}${element.tagName.toLowerCase()}${id}${className}${layout}${css} children=${element.childElementCount}`;
};
const readDomTree = () => {
  if (typeof document === "undefined") return [];
  const root = document.querySelector("#app") || document.body;
  if (!root) return [];
  const lines = [];
  const walk = (element, depth) => {
    if (lines.length >= MAX_DOM_NODES || depth > MAX_DOM_DEPTH) return;
    lines.push(describeElementLine(element, depth));
    Array.from(element.children).forEach((child) => walk(child, depth + 1));
  };
  walk(root, 0);
  if (lines.length >= MAX_DOM_NODES) {
    lines.push(`... truncated at ${MAX_DOM_NODES} nodes`);
  }
  return lines;
};
const readDocumentMetrics = () => {
  if (typeof document === "undefined") return null;
  const html = document.documentElement;
  const body = document.body;
  return {
    html: {
      clientWidth: html.clientWidth,
      clientHeight: html.clientHeight,
      scrollWidth: html.scrollWidth,
      scrollHeight: html.scrollHeight,
      className: html.className,
      dataset: { ...html.dataset }
    },
    body: body ? {
      clientWidth: body.clientWidth,
      clientHeight: body.clientHeight,
      scrollWidth: body.scrollWidth,
      scrollHeight: body.scrollHeight,
      className: body.className,
      style: body.getAttribute("style") || ""
    } : null
  };
};
const readRuntimeInfo = (context) => {
  if (typeof window === "undefined") return { context };
  const nav = window.navigator;
  return {
    context,
    location: {
      href: window.location.href,
      hash: window.location.hash,
      pathname: window.location.pathname,
      search: window.location.search
    },
    navigator: {
      userAgent: nav.userAgent,
      platform: nav.platform,
      maxTouchPoints: nav.maxTouchPoints,
      standalone: nav.standalone === true,
      language: nav.language
    },
    runtimeFlags: {
      hasTauriGlobal: !!window.__TAURI__,
      hasCapacitorGlobal: !!window.Capacitor,
      userAgentIncludesIOS: /iPad|iPhone|iPod/i.test(nav.userAgent || ""),
      iPadDesktopMode: nav.platform === "MacIntel" && (nav.maxTouchPoints || 0) > 1
    }
  };
};
const readRelationship = () => {
  if (typeof window === "undefined" || typeof document === "undefined") return null;
  const tab = document.querySelector(".bottom-tab-bar");
  const shell = document.querySelector(".app-shell");
  const tabRect = readRect(tab);
  const shellRect = readRect(shell);
  const visualHeight = window.visualViewport?.height || window.innerHeight;
  const bottomGap = tabRect ? round(window.innerHeight - Number(tabRect.bottom)) : null;
  const visualBottomGap = tabRect ? round(visualHeight - Number(tabRect.bottom)) : null;
  return {
    tabToLayoutViewportBottom: bottomGap,
    tabToVisualViewportBottom: visualBottomGap,
    tabToShellBottom: tabRect && shellRect ? round(Number(shellRect.bottom) - Number(tabRect.bottom)) : null,
    shellBottomToLayoutViewportBottom: shellRect ? round(window.innerHeight - Number(shellRect.bottom)) : null
  };
};
const readRecentDebugLogs = () => getDebugLogs(80).map((item) => ({
  time: formatDebugTime(item.ts),
  level: item.level,
  scope: item.scope,
  message: item.message,
  details: item.details
}));
const collectHomeLayoutDiagnostics = (context = {}) => {
  const report = {
    title: "Mini-HBUT 首页布局诊断",
    generatedAt: (/* @__PURE__ */ new Date()).toISOString(),
    runtime: readRuntimeInfo(context),
    viewport: readViewport(),
    document: readDocumentMetrics(),
    cssVariables: readCssVariables(),
    safeAreaProbe: readSafeAreaProbe(),
    trackedElements: [
      "#app",
      "html",
      "body",
      ".app-shell",
      ".view-transition-root",
      ".dashboard-root",
      ".bottom-tab-bar"
    ].map(readElementSnapshot),
    bottomTabRelationship: readRelationship(),
    capturedErrors: typeof window === "undefined" ? [] : (window[ERROR_STORE_KEY] || []).slice(-60),
    recentDebugLogs: readRecentDebugLogs(),
    domTree: readDomTree()
  };
  return [
    "Mini-HBUT 首页布局调试报告",
    "请完整复制这段文本返回给开发者。",
    "",
    safeJson(report),
    "",
    "DOM 树摘要:",
    ...report.domTree
  ].join("\n");
};
const useAppRuntime = () => {
  const stores = {
    auth: useAuthStore(),
    navigation: useNavigationStore(),
    lifecycle: useLifecycleStore(),
    grade: useGradeStore(),
    update: useUpdateStore()
  };
  const runtime = {};
  runtime.stores = stores;
  runtime.state = createAppState(stores);
  runtime.navigation = createNavigationCoordinator(runtime);
  runtime.session = createSessionCoordinator(runtime);
  runtime.auth = createAuthCoordinator(runtime);
  runtime.lifecycle = createLifecycleCoordinator(runtime);
  runtime.grade = createGradeCoordinator(runtime);
  runtime.update = createUpdateCoordinator(runtime);
  runtime.remoteConfig = createRemoteConfigCoordinator(runtime);
  runtime.notification = createNotificationCoordinator(runtime);
  runtime.identity = createIdentityCoordinator(runtime);
  const { state: state2 } = runtime;
  const startup = runtime.navigation.readStartupSnapshot();
  state2.currentView.value = startup.initialView;
  state2.activeTab.value = startup.initialTab;
  state2.currentModule.value = startup.initialModule;
  if (startup.bootStudentIdHint && !state2.studentId.value) state2.studentId.value = startup.bootStudentIdHint;
  if (startup.skipSplashForFastScheduleBoot) state2.showSplash.value = false;
  state2.moduleHostSession.value = runtime.navigation.readModuleHostSession();
  const handleSplashDismissed = () => {
    state2.showSplash.value = false;
    runtime.navigation.markBootMetric("splash_dismissed", {
      current_view: state2.currentView.value,
      fast_schedule_boot: startup.skipSplashForFastScheduleBoot
    });
  };
  const dismissSplash = (reason = "") => {
    try {
      ;
      state2.splashRef.value?.dismiss?.();
    } catch (error) {
      console.warn("[Boot] splash dismiss failed:", error);
    }
    handleSplashDismissed();
    if (reason) console.info("[Boot] dismissSplash:", reason);
  };
  const openWorkspaceLayoutEditor = (tab = "home") => {
    state2.workspaceLayoutEditorTab.value = tab === "notifications" ? "notifications" : "home";
    state2.showWorkspaceLayoutEditor.value = true;
  };
  const closeWorkspaceLayoutEditor = () => {
    state2.showWorkspaceLayoutEditor.value = false;
  };
  const refreshHomeLayoutDebugReport = () => {
    state2.homeLayoutDebugReport.value = collectHomeLayoutDiagnostics();
  };
  const copyHomeLayoutDebugReport = async () => {
    refreshHomeLayoutDebugReport();
    try {
      await navigator.clipboard.writeText(state2.homeLayoutDebugReport.value);
      showToast("调试信息已复制", "success");
    } catch {
      showToast("复制失败，请手动复制", "error");
    }
  };
  const toggleHomeLayoutDebugReport = () => {
    state2.homeLayoutDebugExpanded.value = !state2.homeLayoutDebugExpanded.value;
    if (state2.homeLayoutDebugExpanded.value) refreshHomeLayoutDebugReport();
  };
  const hideHomeLayoutDebug = () => {
    state2.homeLayoutDebugHidden.value = true;
    localStorage.setItem(HOME_LAYOUT_DEBUG_HIDDEN_KEY, "1");
  };
  runtime.handlers = {
    handleNavigate: runtime.navigation.handleNavigate,
    handleLogout: runtime.auth.handleLogout,
    handleRequireLogin: runtime.auth.handleRequireLogin,
    handleRetrySessionRecovery: runtime.session.handleRetrySessionRecovery,
    handleLoginSuccess: runtime.auth.handleLoginSuccess,
    handleSwitchLoginMode: runtime.auth.handleSwitchLoginMode,
    handleCheckUpdate: runtime.update.handleCheckUpdate,
    handleOpenOfficial: runtime.navigation.handleOpenOfficial,
    handleOpenFeedback: runtime.navigation.handleOpenFeedback,
    handleOpenConfig: runtime.navigation.handleOpenConfig,
    handleOpenSettings: runtime.navigation.handleOpenSettings,
    handleBackToDashboard: runtime.navigation.handleBackToDashboard,
    handleBackToMe: runtime.navigation.handleBackToMe,
    handleBackToMoreCenter: runtime.navigation.handleBackToMoreCenter,
    handleRefreshGrades: runtime.grade.handleRefreshGrades,
    handleTabChange: runtime.navigation.handleTabChange,
    handleSplashDismissed,
    handleForceUpdate: runtime.update.handleForceUpdate,
    openAnnouncement: runtime.remoteConfig.openAnnouncement,
    openWorkspaceLayoutEditor,
    closeWorkspaceLayoutEditor,
    closeAnnouncement: runtime.remoteConfig.closeAnnouncement,
    confirmBlockingAnnouncement: runtime.remoteConfig.confirmBlockingAnnouncement,
    handleContentClick: runtime.remoteConfig.handleContentClick,
    handleExternalOpen: runtime.remoteConfig.handleExternalOpen,
    hideHomeLayoutDebug,
    copyHomeLayoutDebugReport,
    toggleHomeLayoutDebugReport,
    closeDailyAccessDialog: runtime.navigation.closeDailyAccessDialog,
    handleDailyAccessInput: runtime.navigation.handleDailyAccessInput,
    submitDailyAccessKey: runtime.navigation.submitDailyAccessKey,
    cancelExitDialog: runtime.navigation.cancelExitDialog,
    confirmExitDialog: runtime.navigation.confirmExitDialog
  };
  const handleGlobalLinkClick = (event) => {
    void runtime.remoteConfig.handleContentClick(event);
  };
  const handlePopState = () => void runtime.navigation.handlePopState();
  const handleMaintenanceEvent = (event) => runtime.session.handleJwxtMaintenanceEvent(event);
  const handleRemoteModeEvent = () => runtime.remoteConfig.handleRemoteConfigModeChanged();
  const handleRemoteUpdatedEvent = () => runtime.remoteConfig.handleRemoteConfigUpdated();
  watch(state2.currentView, (view, previous) => {
    runtime.navigation.handleViewChanged(view, previous);
    nextTick(() => runtime.lifecycle.scheduleViewportUpdate());
  });
  onMounted(async () => {
    console.time("[Boot] total");
    const splashFailsafe = window.setTimeout(() => {
      if (state2.showSplash.value) dismissSplash("failsafe-2.5s");
      state2.mutable.appBootstrapped = true;
      runtime.identity.flushPendingIntents();
    }, 2500);
    document.addEventListener("click", handleGlobalLinkClick, true);
    window.addEventListener("popstate", handlePopState);
    window.addEventListener(JWXT_MAINTENANCE_EVENT, handleMaintenanceEvent);
    window.addEventListener(REMOTE_CONFIG_MODE_EVENT, handleRemoteModeEvent);
    window.addEventListener(REMOTE_CONFIG_UPDATED_EVENT, handleRemoteUpdatedEvent);
    state2.mutable.removeHomeLayoutDiagnosticsErrorCapture = installHomeLayoutDiagnosticsErrorCapture();
    runtime.lifecycle.installResumeListeners();
    runtime.lifecycle.installCapacitorStateListener();
    runtime.lifecycle.scheduleViewportUpdate();
    runtime.notification.installWidgetDeeplinkListeners();
    void runtime.notification.installNotificationActionListener();
    runtime.session.clearJwxtMaintenance();
    let cachedIdentity = false;
    try {
      cachedIdentity = await runtime.session.restoreCachedIdentityFromLocal();
      await runtime.navigation.syncFromHash({ scrollToTop: false });
    } catch (error) {
      console.warn("[Boot] local bootstrap failed:", error);
    }
    dismissSplash(cachedIdentity ? "cached-identity" : "enter-ui-first");
    state2.mutable.appBootstrapped = true;
    runtime.identity.flushPendingIntents();
    runtime.navigation.replaceHistorySnapshot(state2.currentView.value);
    runtime.session.ensureConfigAccess();
    window.clearTimeout(splashFailsafe);
    const restoreTask = (async () => {
      if (isTestAccountSession()) {
        return { restored: runtime.session.restoreTestAccountSession(), relogged: false };
      }
      let restored = await runtime.session.tryRestoreSession();
      if (!restored) restored = await runtime.session.tryRestoreLatestSession();
      let relogged = false;
      if (!restored && !runtime.session.isTemporaryLoginSession()) {
        relogged = await runtime.session.attemptAutoRelogin();
      }
      return { restored, relogged };
    })();
    void restoreTask.then(async (result) => {
      const { restored, relogged } = result || { restored: false, relogged: false };
      if (!restored && !relogged) {
        if (!isTestAccountSession() && (cachedIdentity || state2.studentId.value)) {
          let hasCreds = false;
          try {
            const portalCreds = await loadPortalStoredPassword();
            const cxCreds = await loadChaoxingStoredPassword();
            hasCreds = !!(portalCreds || cxCreds);
          } catch (e) {
            console.warn("[Session] 检查本地凭据失败:", e);
            hasCreds = false;
          }
          if (hasCreds) {
            runtime.session.markJwxtMaintenance("会话需恢复，正在后台自动登录…", {
              phase: "recovering",
              detail: state2.jwxtSessionLastError.value || "cookie 已失效，使用记住密码重登"
            });
            runtime.session.startJwxtRecoveryPolling();
            runtime.session.attemptOnlineRecovery({ silent: true }).then((ok) => {
              if (ok) return;
              runtime.session.markJwxtMaintenance("自动登录未成功，将继续后台重试。当前展示缓存数据。", {
                phase: "failed",
                detail: state2.jwxtSessionLastError.value || "恢复失败"
              });
            }).catch((e) => {
              state2.jwxtSessionLastError.value = runtime.session.formatSessionError(e);
              runtime.session.markJwxtMaintenance("后台恢复异常，将定时重试。", {
                phase: "failed",
                detail: state2.jwxtSessionLastError.value
              });
            });
          } else {
            console.warn("[Session] 会话失效且无记住密码，首页提示手动登录（#355，不再强制 logout）");
            runtime.session.markJwxtMaintenance(
              "登录状态已失效，本地未找到可用密码。请手动登录融合门户以同步最新数据；此前缓存仍可查看。",
              {
                phase: "need_login",
                detail: state2.jwxtSessionLastError.value || "无记住密码，无法后台自动登录"
              }
            );
          }
        }
        return;
      }
      runtime.session.clearJwxtMaintenance();
      runtime.session.stopJwxtRecoveryPolling();
      if (!isTestAccountSession()) {
        runtime.session.startSessionKeepAlive();
        runtime.session.startElectricityKeepAlive();
        if (state2.studentId.value) {
          runtime.session.markLoginSessionToken();
          void ensureRememberedPasswordCached(state2.studentId.value).catch((e) => {
            console.warn("[Session] 启动后缓存记住密码失败:", e);
          });
          void startNotificationMonitor({ studentId: state2.studentId.value });
        }
        runtime.session.notifySessionOnline(relogged ? "boot-auto-relogin" : "boot-session-restore");
      }
    }).catch((error) => console.warn("[Boot] session restore failed:", error));
    void runtime.remoteConfig.applyRemoteConfig().finally(runtime.remoteConfig.startRemoteConfigRefresh);
    void runtime.remoteConfig.primeOcrEndpointFromCache();
    void runtime.navigation.installCloseInterceptor();
    if (state2.studentId.value && !isTestAccountSession()) {
      void tryWriteSnapshotFromCache(state2.studentId.value);
      runtime.notification.scheduleWidgetCrossDayTimer();
    }
    window.setTimeout(() => void runtime.update.autoCheckUpdate(), 1500);
    initUsageTracker({ studentId: state2.studentId.value });
    startUsageUploadScheduler(() => state2.studentId.value);
    void runCampusNetworkAutoLogin({ studentId: state2.studentId.value });
    markBootMetric("app_runtime_ready", { current_view: state2.currentView.value });
    console.timeEnd("[Boot] total");
  });
  onBeforeUnmount(() => {
    stopUsageUploadScheduler();
    document.removeEventListener("click", handleGlobalLinkClick, true);
    window.removeEventListener("popstate", handlePopState);
    window.removeEventListener(JWXT_MAINTENANCE_EVENT, handleMaintenanceEvent);
    window.removeEventListener(REMOTE_CONFIG_MODE_EVENT, handleRemoteModeEvent);
    window.removeEventListener(REMOTE_CONFIG_UPDATED_EVENT, handleRemoteUpdatedEvent);
    runtime.lifecycle.dispose();
    runtime.session.stopSessionKeepAlive();
    runtime.session.stopElectricityKeepAlive();
    runtime.session.stopJwxtRecoveryPolling();
    runtime.remoteConfig.stopRemoteConfigRefresh();
    runtime.notification.stopWidgetCrossDayTimer();
    runtime.identity.dispose();
    if (typeof state2.mutable.removeNotificationActionListener === "function") {
      state2.mutable.removeNotificationActionListener();
      state2.mutable.removeNotificationActionListener = null;
    }
    void stopNotificationMonitor();
    runtime.grade.clearGradeRealtimeRetry();
  });
  return {
    runtime,
    state: state2,
    handlers: runtime.handlers,
    isIOSLike: isIOSLike(),
    isTestAccountSession
  };
};
const _hoisted_1$1 = {
  key: 45,
  class: "coming-soon-page"
};
const _hoisted_2$1 = { class: "coming-soon-content" };
const _hoisted_3$1 = {
  key: 1,
  class: "bottom-tab-bar glass-card"
};
const _hoisted_4$1 = {
  key: 2,
  class: "home-layout-debug-panel",
  "aria-label": "当前首页布局调试"
};
const _hoisted_5$1 = { class: "home-layout-debug-head" };
const _hoisted_6$1 = { class: "home-layout-debug-actions" };
const _hoisted_7$1 = ["value"];
const _hoisted_8 = {
  key: 0,
  class: "login-mask"
};
const _hoisted_9 = {
  key: 0,
  class: "daily-access-overlay"
};
const _hoisted_10 = { class: "daily-access-card modal-pop-card" };
const _hoisted_11 = { class: "daily-access-desc" };
const _hoisted_12 = ["value"];
const _hoisted_13 = {
  key: 0,
  class: "daily-access-error"
};
const _hoisted_14 = { class: "daily-access-actions" };
const _hoisted_15 = {
  key: 0,
  class: "exit-dialog-overlay"
};
const _hoisted_16 = { class: "exit-dialog-card modal-pop-card" };
const _hoisted_17 = { class: "exit-dialog-actions" };
const _hoisted_18 = ["disabled"];
const _hoisted_19 = ["disabled"];
const _hoisted_20 = {
  key: 0,
  class: "force-update-overlay"
};
const _hoisted_21 = { class: "force-update-card modal-pop-card" };
const _hoisted_22 = { class: "force-update-message" };
const _hoisted_23 = { class: "force-update-meta" };
const _hoisted_24 = {
  key: 0,
  class: "force-update-download"
};
const _hoisted_25 = { class: "force-update-actions" };
const _hoisted_26 = { class: "notice-modal modal-pop-card" };
const _hoisted_27 = { class: "notice-modal-header" };
const _hoisted_28 = {
  key: 0,
  class: "notice-modal-meta"
};
const _hoisted_29 = {
  key: 1,
  class: "notice-modal-image"
};
const _hoisted_30 = ["src", "alt"];
const _hoisted_31 = ["innerHTML"];
const _hoisted_32 = ["href"];
const _hoisted_33 = {
  key: 0,
  class: "notice-confirm-overlay"
};
const _hoisted_34 = { class: "notice-confirm-card modal-pop-card" };
const _hoisted_35 = { class: "notice-confirm-title" };
const _hoisted_36 = ["innerHTML"];
const _hoisted_37 = { class: "notice-confirm-actions" };
const _sfc_main$1 = /* @__PURE__ */ defineComponent({
  __name: "App",
  setup(__props) {
    const { state: state2, handlers, runtime, isIOSLike: isIOSLike2, isTestAccountSession: isTestAccountSession2 } = useAppRuntime();
    const {
      currentView,
      activeTab,
      currentModule,
      studentId,
      userUuid,
      isLoggedIn,
      gradeData,
      gradesOffline,
      gradesSyncTime,
      isLoading,
      showUpdateDialog,
      showForceUpdate,
      forceUpdateInfo,
      forceUpdateResolvedUrl,
      forceUpdateDisplayUrl,
      showSplash,
      splashStatus,
      splashStatusText,
      viewRenderNonce,
      widgetDeeplinkDate,
      widgetDeeplinkPeriod,
      moduleHostSession,
      loginMode,
      showLoginPrompt,
      showExitDialog,
      exitingApp,
      showDailyAccessDialog,
      dailyAccessInput,
      dailyAccessError,
      jwxtMaintenanceMode,
      jwxtMaintenanceHint,
      jwxtMaintenanceDetail,
      jwxtRecoveryPhase,
      jwxtLastCheckTime,
      announcementData,
      activeAnnouncement,
      showAnnouncementModal,
      blockingAnnouncement,
      showBlockingAnnouncement,
      activeAnnouncementHtml,
      blockingAnnouncementHtml,
      showWorkspaceLayoutEditor,
      workspaceLayoutEditorTab,
      homeLayoutDebugExpanded,
      homeLayoutDebugReport,
      viewTransitionEnterActive,
      viewTransitionLeaveActive,
      viewTransitionEnterFrom,
      viewTransitionLeaveTo,
      protectedViewPromptTitle,
      configAdminIds,
      isConfigAdmin,
      aiModelOptions,
      showTabBar,
      showHomeLayoutDebug,
      homeScrollRestoring
    } = state2;
    const {
      handleNavigate,
      handleLogout,
      handleRequireLogin,
      handleRetrySessionRecovery,
      handleLoginSuccess,
      handleSwitchLoginMode,
      handleCheckUpdate,
      handleOpenOfficial,
      handleOpenFeedback,
      handleOpenConfig,
      handleOpenSettings,
      handleBackToDashboard,
      handleBackToMe,
      handleBackToMoreCenter,
      handleRefreshGrades,
      handleTabChange,
      handleSplashDismissed,
      handleForceUpdate,
      openAnnouncement,
      openWorkspaceLayoutEditor,
      closeWorkspaceLayoutEditor,
      closeAnnouncement,
      confirmBlockingAnnouncement,
      handleContentClick,
      handleExternalOpen,
      hideHomeLayoutDebug,
      copyHomeLayoutDebugReport,
      toggleHomeLayoutDebugReport,
      closeDailyAccessDialog,
      handleDailyAccessInput,
      submitDailyAccessKey,
      cancelExitDialog,
      confirmExitDialog
    } = handlers;
    const {
      home: Dashboard,
      grades: GradeView,
      electricity: ElectricityView,
      classroom: ClassroomView,
      schedule: ScheduleView,
      qxzkb: GlobalScheduleView,
      course_selection: CourseSelectionView,
      studentinfo: StudentInfoView,
      exams: ExamView,
      ranking: RankingView,
      calendar: CalendarView,
      school_inbox: SchoolInboxView,
      academic: AcademicProgressView,
      training: TrainingPlanView,
      forum: ForumView,
      me: MeView,
      official: OfficialView,
      feedback: FeedbackView,
      notifications: NotificationView,
      config: ConfigEditor,
      settings: SettingsView,
      privacy_data: PrivacyDataView,
      export_center: ExportCenterView,
      service_stats: ServiceStatsView,
      school_website: SchoolWebsiteView,
      quick_links: QuickLinksView,
      campus_network: CampusNetworkView,
      more: MoreView,
      more_module_host: MoreModuleHostView,
      more_chaoxing_checkin: MoreChaoxingCheckinView,
      transactions: TransactionHistory,
      campus_code: CampusCodeView,
      ai: AiChatView,
      campus_map: CampusMapView,
      library: LibraryView,
      resource_share: ResourceShareView,
      chaoxing_class: ChaoxingClassView,
      chaoxing_hub: ChaoxingHubView,
      chaoxing_inbox: ChaoxingInboxView,
      teaching_eval: TeachingEvalView,
      broadband: BroadbandView,
      sports_venue: SportsVenueView,
      towergo: TowerGoView,
      smart_orientation: SmartOrientationView,
      identity_auth_history: IdentityAuthHistoryView
    } = VIEW_COMPONENTS;
    return (_ctx, _cache) => {
      return openBlock(), createBlock(_sfc_main$6, {
        view: unref(currentView),
        "student-id": unref(studentId),
        "logged-in": unref(isLoggedIn)
      }, {
        default: withCtx(() => [
          unref(showSplash) ? (openBlock(), createBlock(SplashScreen, {
            key: 0,
            status: unref(splashStatus),
            "status-text": unref(splashStatusText),
            onDismiss: unref(handleSplashDismissed)
          }, null, 8, ["status", "status-text", "onDismiss"])) : createCommentVNode("", true),
          createBaseVNode("main", {
            class: normalizeClass(["app-shell", {
              "no-scroll": unref(currentView) === "ai",
              "ai-full": unref(currentView) === "ai",
              "schedule-full": unref(currentView) === "schedule",
              "module-host-full": unref(currentView) === "more_module_host",
              "ios-safe": unref(isIOSLike2)
            }])
          }, [
            unref(isLoggedIn) && unref(isTestAccountSession2)() ? (openBlock(), createBlock(DemoModeBanner, { key: 0 })) : createCommentVNode("", true),
            createVNode(Transition, {
              name: "module-fade",
              mode: "out-in",
              "enter-active-class": unref(viewTransitionEnterActive),
              "leave-active-class": unref(viewTransitionLeaveActive),
              "enter-from-class": unref(viewTransitionEnterFrom),
              "leave-to-class": unref(viewTransitionLeaveTo)
            }, {
              default: withCtx(() => [
                (openBlock(), createElementBlock("div", {
                  key: `${unref(currentView)}:${unref(viewRenderNonce)}`,
                  class: normalizeClass(["view-transition-root", { "home-scroll-restoring": unref(homeScrollRestoring) }])
                }, [
                  unref(currentView) === "home" ? (openBlock(), createBlock(unref(Dashboard), {
                    key: 0,
                    "student-id": unref(studentId),
                    "user-uuid": unref(userUuid),
                    "is-logged-in": unref(isLoggedIn),
                    "jwxt-maintenance": unref(jwxtMaintenanceMode),
                    "jwxt-maintenance-hint": unref(jwxtMaintenanceHint),
                    "jwxt-maintenance-detail": unref(jwxtMaintenanceDetail),
                    "jwxt-recovery-phase": unref(jwxtRecoveryPhase),
                    "jwxt-last-check-time": unref(jwxtLastCheckTime),
                    "ticker-notices": unref(announcementData).ticker,
                    "pinned-notices": unref(announcementData).pinned,
                    "notice-list": unref(announcementData).list,
                    onNavigate: unref(handleNavigate),
                    onLogout: unref(handleLogout),
                    onRequireLogin: unref(handleRequireLogin),
                    onRetrySessionRecovery: unref(handleRetrySessionRecovery),
                    onOpenNotice: unref(openAnnouncement),
                    onOpenSettings: _cache[0] || (_cache[0] = ($event) => unref(openWorkspaceLayoutEditor)("home"))
                  }, null, 8, ["student-id", "user-uuid", "is-logged-in", "jwxt-maintenance", "jwxt-maintenance-hint", "jwxt-maintenance-detail", "jwxt-recovery-phase", "jwxt-last-check-time", "ticker-notices", "pinned-notices", "notice-list", "onNavigate", "onLogout", "onRequireLogin", "onRetrySessionRecovery", "onOpenNotice"])) : unref(currentView) === "schedule" ? (openBlock(), createBlock(unref(ScheduleView), {
                    key: 1,
                    "student-id": unref(studentId),
                    "widget-date": unref(widgetDeeplinkDate),
                    "widget-period": unref(widgetDeeplinkPeriod),
                    onBack: unref(handleBackToDashboard),
                    onLogout: unref(handleLogout),
                    onWidgetDeeplinkConsumed: _cache[1] || (_cache[1] = ($event) => {
                      widgetDeeplinkDate.value = "";
                      widgetDeeplinkPeriod.value = 0;
                    })
                  }, null, 8, ["student-id", "widget-date", "widget-period", "onBack", "onLogout"])) : unref(currentView) === "qxzkb" ? (openBlock(), createBlock(unref(GlobalScheduleView), {
                    key: 2,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard),
                    onLogout: unref(handleLogout)
                  }, null, 8, ["student-id", "onBack", "onLogout"])) : unref(currentView) === "course_selection" ? (openBlock(), createBlock(unref(CourseSelectionView), {
                    key: 3,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard),
                    onLogout: unref(handleLogout)
                  }, null, 8, ["student-id", "onBack", "onLogout"])) : unref(currentView) === "forum" ? (openBlock(), createBlock(unref(ForumView), {
                    key: 4,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard),
                    onRequireLogin: unref(handleRequireLogin)
                  }, null, 8, ["student-id", "onBack", "onRequireLogin"])) : unref(currentView) === "me" ? (openBlock(), createBlock(unref(MeView), {
                    key: 5,
                    "student-id": unref(studentId),
                    "is-logged-in": unref(isLoggedIn),
                    "login-mode": unref(loginMode),
                    "config-admin-ids": unref(configAdminIds),
                    onSuccess: unref(handleLoginSuccess),
                    onSwitchMode: unref(handleSwitchLoginMode),
                    onNavigate: unref(handleNavigate),
                    onLogout: unref(handleLogout),
                    onCheckUpdate: unref(handleCheckUpdate),
                    onOpenOfficial: unref(handleOpenOfficial),
                    onOpenFeedback: unref(handleOpenFeedback),
                    onOpenConfig: unref(handleOpenConfig),
                    onOpenSettings: unref(handleOpenSettings)
                  }, null, 8, ["student-id", "is-logged-in", "login-mode", "config-admin-ids", "onSuccess", "onSwitchMode", "onNavigate", "onLogout", "onCheckUpdate", "onOpenOfficial", "onOpenFeedback", "onOpenConfig", "onOpenSettings"])) : unref(currentView) === "official" ? (openBlock(), createBlock(unref(OfficialView), {
                    key: 6,
                    onBack: unref(handleBackToMe)
                  }, null, 8, ["onBack"])) : unref(currentView) === "feedback" ? (openBlock(), createBlock(unref(FeedbackView), {
                    key: 7,
                    onBack: unref(handleBackToMe)
                  }, null, 8, ["onBack"])) : unref(currentView) === "config" && unref(isConfigAdmin) ? (openBlock(), createBlock(unref(ConfigEditor), {
                    key: 8,
                    onBack: unref(handleBackToMe)
                  }, null, 8, ["onBack"])) : unref(currentView) === "settings" ? (openBlock(), createBlock(unref(SettingsView), {
                    key: 9,
                    identity: unref(runtime).identity,
                    onBack: unref(handleBackToMe),
                    onOpenWorkspaceLayout: unref(openWorkspaceLayoutEditor)
                  }, null, 8, ["identity", "onBack", "onOpenWorkspaceLayout"])) : unref(currentView) === "privacy_data" ? (openBlock(), createBlock(unref(PrivacyDataView), {
                    key: 10,
                    onBack: unref(handleBackToMe),
                    onLogout: unref(handleLogout),
                    onCleared: unref(handleLogout)
                  }, null, 8, ["onBack", "onLogout", "onCleared"])) : unref(currentView) === "export_center" ? (openBlock(), createBlock(unref(ExportCenterView), {
                    key: 11,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToMe),
                    onLogout: unref(handleLogout)
                  }, null, 8, ["student-id", "onBack", "onLogout"])) : unref(currentView) === "service_stats" ? (openBlock(), createBlock(unref(ServiceStatsView), {
                    key: 12,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToMe)
                  }, null, 8, ["student-id", "onBack"])) : unref(currentView) === "school_website" && unref(isLoggedIn) ? (openBlock(), createBlock(unref(SchoolWebsiteView), {
                    key: 13,
                    onBack: unref(handleBackToMe)
                  }, null, 8, ["onBack"])) : unref(currentView) === "quick_links" && unref(isLoggedIn) ? (openBlock(), createBlock(unref(QuickLinksView), {
                    key: 14,
                    onBack: unref(handleBackToMe)
                  }, null, 8, ["onBack"])) : unref(currentView) === "campus_network" && unref(isLoggedIn) ? (openBlock(), createBlock(unref(CampusNetworkView), {
                    key: 15,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToMe)
                  }, null, 8, ["student-id", "onBack"])) : unref(currentView) === "identity_auth_history" ? (openBlock(), createBlock(unref(IdentityAuthHistoryView), {
                    key: 16,
                    onBack: unref(handleBackToMe)
                  }, null, 8, ["onBack"])) : unref(currentView) === "more" ? (openBlock(), createBlock(unref(MoreView), {
                    key: 17,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToMe),
                    onNavigate: unref(handleNavigate)
                  }, null, 8, ["student-id", "onBack", "onNavigate"])) : unref(currentView) === "more_module_host" ? (openBlock(), createBlock(unref(MoreModuleHostView), {
                    key: 18,
                    session: unref(moduleHostSession),
                    onBack: unref(handleBackToMoreCenter)
                  }, null, 8, ["session", "onBack"])) : unref(currentView) === "more_chaoxing_checkin" ? (openBlock(), createBlock(unref(MoreChaoxingCheckinView), {
                    key: 19,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToMoreCenter)
                  }, null, 8, ["student-id", "onBack"])) : unref(currentView) === "grades" ? (openBlock(), createBlock(unref(GradeView), {
                    key: 20,
                    grades: unref(gradeData),
                    "student-id": unref(studentId),
                    offline: unref(gradesOffline),
                    "sync-time": unref(gradesSyncTime),
                    refreshing: unref(isLoading),
                    onBack: unref(handleBackToDashboard),
                    onLogout: unref(handleLogout),
                    onRefresh: unref(handleRefreshGrades)
                  }, null, 8, ["grades", "student-id", "offline", "sync-time", "refreshing", "onBack", "onLogout", "onRefresh"])) : unref(currentView) === "electricity" ? (openBlock(), createBlock(unref(ElectricityView), {
                    key: 21,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard),
                    onLogout: unref(handleLogout)
                  }, null, 8, ["student-id", "onBack", "onLogout"])) : unref(currentView) === "transactions" ? (openBlock(), createBlock(unref(TransactionHistory), {
                    key: 22,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard)
                  }, null, 8, ["student-id", "onBack"])) : unref(currentView) === "campus_code" ? (openBlock(), createBlock(unref(CampusCodeView), {
                    key: 23,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard),
                    onLogout: unref(handleLogout)
                  }, null, 8, ["student-id", "onBack", "onLogout"])) : unref(currentView) === "notifications" ? (openBlock(), createBlock(unref(NotificationView), {
                    key: 24,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard),
                    onOpenWorkspaceLayout: _cache[2] || (_cache[2] = ($event) => unref(openWorkspaceLayoutEditor)("notifications"))
                  }, null, 8, ["student-id", "onBack"])) : unref(currentView) === "classroom" ? (openBlock(), createBlock(unref(ClassroomView), {
                    key: 25,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard),
                    onLogout: unref(handleLogout)
                  }, null, 8, ["student-id", "onBack", "onLogout"])) : unref(currentView) === "studentinfo" ? (openBlock(), createBlock(unref(StudentInfoView), {
                    key: 26,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard),
                    onLogout: unref(handleLogout)
                  }, null, 8, ["student-id", "onBack", "onLogout"])) : unref(currentView) === "exams" ? (openBlock(), createBlock(unref(ExamView), {
                    key: 27,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard),
                    onLogout: unref(handleLogout)
                  }, null, 8, ["student-id", "onBack", "onLogout"])) : unref(currentView) === "ranking" ? (openBlock(), createBlock(unref(RankingView), {
                    key: 28,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard),
                    onLogout: unref(handleLogout)
                  }, null, 8, ["student-id", "onBack", "onLogout"])) : unref(currentView) === "calendar" ? (openBlock(), createBlock(unref(CalendarView), {
                    key: 29,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard),
                    onLogout: unref(handleLogout)
                  }, null, 8, ["student-id", "onBack", "onLogout"])) : unref(currentView) === "school_inbox" ? (openBlock(), createBlock(unref(SchoolInboxView), {
                    key: 30,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard),
                    onLogout: unref(handleLogout)
                  }, null, 8, ["student-id", "onBack", "onLogout"])) : unref(currentView) === "academic" ? (openBlock(), createBlock(unref(AcademicProgressView), {
                    key: 31,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard),
                    onLogout: unref(handleLogout)
                  }, null, 8, ["student-id", "onBack", "onLogout"])) : unref(currentView) === "training" ? (openBlock(), createBlock(unref(TrainingPlanView), {
                    key: 32,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard),
                    onLogout: unref(handleLogout)
                  }, null, 8, ["student-id", "onBack", "onLogout"])) : unref(currentView) === "ai" ? (openBlock(), createBlock(unref(AiChatView), {
                    key: 33,
                    "student-id": unref(studentId),
                    "model-options": unref(aiModelOptions),
                    onBack: unref(handleBackToDashboard)
                  }, null, 8, ["student-id", "model-options", "onBack"])) : unref(currentView) === "campus_map" ? (openBlock(), createBlock(unref(CampusMapView), {
                    key: 34,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard),
                    onLogout: unref(handleLogout)
                  }, null, 8, ["student-id", "onBack", "onLogout"])) : unref(currentView) === "library" ? (openBlock(), createBlock(unref(LibraryView), {
                    key: 35,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard),
                    onLogout: unref(handleLogout)
                  }, null, 8, ["student-id", "onBack", "onLogout"])) : unref(currentView) === "resource_share" ? (openBlock(), createBlock(unref(ResourceShareView), {
                    key: 36,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard)
                  }, null, 8, ["student-id", "onBack"])) : unref(currentView) === "chaoxing_class" ? (openBlock(), createBlock(unref(ChaoxingClassView), {
                    key: 37,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard)
                  }, null, 8, ["student-id", "onBack"])) : unref(currentView) === "chaoxing_hub" ? (openBlock(), createBlock(unref(ChaoxingHubView), {
                    key: 38,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard)
                  }, null, 8, ["student-id", "onBack"])) : unref(currentView) === "chaoxing_inbox" ? (openBlock(), createBlock(unref(ChaoxingInboxView), {
                    key: 39,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard),
                    onLogout: unref(handleLogout)
                  }, null, 8, ["student-id", "onBack", "onLogout"])) : unref(currentView) === "teaching_eval" ? (openBlock(), createBlock(unref(TeachingEvalView), {
                    key: 40,
                    onBack: unref(handleBackToDashboard)
                  }, null, 8, ["onBack"])) : unref(currentView) === "broadband" ? (openBlock(), createBlock(unref(BroadbandView), {
                    key: 41,
                    onBack: unref(handleBackToDashboard)
                  }, null, 8, ["onBack"])) : unref(currentView) === "sports_venue" ? (openBlock(), createBlock(unref(SportsVenueView), {
                    key: 42,
                    onBack: unref(handleBackToDashboard)
                  }, null, 8, ["onBack"])) : unref(currentView) === "towergo" ? (openBlock(), createBlock(unref(TowerGoView), {
                    key: 43,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard)
                  }, null, 8, ["student-id", "onBack"])) : unref(currentView) === "smart_orientation" ? (openBlock(), createBlock(unref(SmartOrientationView), {
                    key: 44,
                    "student-id": unref(studentId),
                    onBack: unref(handleBackToDashboard),
                    onLogout: unref(handleLogout)
                  }, null, 8, ["student-id", "onBack", "onLogout"])) : (openBlock(), createElementBlock("div", _hoisted_1$1, [
                    createBaseVNode("div", _hoisted_2$1, [
                      _cache[25] || (_cache[25] = createBaseVNode("div", { class: "emoji" }, "🚧", -1)),
                      createBaseVNode("h2", null, toDisplayString(unref(currentModule)) + " 模块开发中", 1),
                      _cache[26] || (_cache[26] = createBaseVNode("p", null, "敬请期待...", -1)),
                      createBaseVNode("button", {
                        onClick: _cache[3] || (_cache[3] = //@ts-ignore
                        (...args) => unref(handleBackToDashboard) && unref(handleBackToDashboard)(...args))
                      }, "返回仪表盘")
                    ])
                  ]))
                ], 2))
              ]),
              _: 1
            }, 8, ["enter-active-class", "leave-active-class", "enter-from-class", "leave-to-class"])
          ], 2),
          unref(showTabBar) ? (openBlock(), createElementBlock("nav", _hoisted_3$1, [
            createBaseVNode("button", {
              class: normalizeClass(["tab-item btn-ripple", { active: unref(activeTab) === "home" }]),
              onClick: _cache[4] || (_cache[4] = ($event) => unref(handleTabChange)("home"))
            }, [..._cache[27] || (_cache[27] = [
              createBaseVNode("span", {
                class: "tab-icon",
                "aria-hidden": "true"
              }, [
                createBaseVNode("svg", {
                  class: "tab-icon-svg",
                  viewBox: "0 0 24 24",
                  fill: "none"
                }, [
                  createBaseVNode("path", { d: "M3 10.8L12 3l9 7.8V21a1 1 0 0 1-1 1h-5.3a1 1 0 0 1-1-1v-5h-3.4v5a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1V10.8z" })
                ])
              ], -1),
              createBaseVNode("span", { class: "tab-label" }, "首页", -1)
            ])], 2),
            createBaseVNode("button", {
              class: normalizeClass(["tab-item btn-ripple", { active: unref(activeTab) === "schedule" }]),
              onClick: _cache[5] || (_cache[5] = ($event) => unref(handleTabChange)("schedule"))
            }, [..._cache[28] || (_cache[28] = [
              createBaseVNode("span", {
                class: "tab-icon",
                "aria-hidden": "true"
              }, [
                createBaseVNode("svg", {
                  class: "tab-icon-svg",
                  viewBox: "0 0 24 24",
                  fill: "none"
                }, [
                  createBaseVNode("rect", {
                    x: "3.2",
                    y: "4.5",
                    width: "17.6",
                    height: "16.3",
                    rx: "3.1"
                  }),
                  createBaseVNode("path", { d: "M7 2.8v3.5M17 2.8v3.5M3.2 9.3h17.6" })
                ])
              ], -1),
              createBaseVNode("span", { class: "tab-label" }, "课表", -1)
            ])], 2),
            createBaseVNode("button", {
              class: normalizeClass(["tab-item btn-ripple", { active: unref(activeTab) === "notifications" }]),
              onClick: _cache[6] || (_cache[6] = ($event) => unref(handleTabChange)("notifications"))
            }, [..._cache[29] || (_cache[29] = [
              createBaseVNode("span", {
                class: "tab-icon",
                "aria-hidden": "true"
              }, [
                createBaseVNode("svg", {
                  class: "tab-icon-svg",
                  viewBox: "0 0 24 24",
                  fill: "none"
                }, [
                  createBaseVNode("path", { d: "M12 3.5a5.8 5.8 0 0 0-5.8 5.8v3.2c0 .8-.2 1.6-.6 2.3L4.3 17a1.3 1.3 0 0 0 1.1 2h13.2a1.3 1.3 0 0 0 1.1-2l-1.3-2.2a4.7 4.7 0 0 1-.6-2.3V9.3A5.8 5.8 0 0 0 12 3.5z" }),
                  createBaseVNode("path", { d: "M9.3 19a2.7 2.7 0 0 0 5.4 0" })
                ])
              ], -1),
              createBaseVNode("span", { class: "tab-label" }, "通知", -1)
            ])], 2),
            createBaseVNode("button", {
              class: normalizeClass(["tab-item btn-ripple", { active: unref(activeTab) === "me" }]),
              onClick: _cache[7] || (_cache[7] = ($event) => unref(handleTabChange)("me"))
            }, [..._cache[30] || (_cache[30] = [
              createBaseVNode("span", {
                class: "tab-icon",
                "aria-hidden": "true"
              }, [
                createBaseVNode("svg", {
                  class: "tab-icon-svg",
                  viewBox: "0 0 24 24",
                  fill: "none"
                }, [
                  createBaseVNode("circle", {
                    cx: "12",
                    cy: "8",
                    r: "4.1"
                  }),
                  createBaseVNode("path", { d: "M4 20c1.8-3.6 4.7-5.5 8-5.5s6.2 1.9 8 5.5" })
                ])
              ], -1),
              createBaseVNode("span", { class: "tab-label" }, "我的", -1)
            ])], 2)
          ])) : createCommentVNode("", true),
          unref(showHomeLayoutDebug) ? (openBlock(), createElementBlock("section", _hoisted_4$1, [
            createBaseVNode("div", _hoisted_5$1, [
              _cache[31] || (_cache[31] = createBaseVNode("div", null, [
                createBaseVNode("p", { class: "home-layout-debug-kicker" }, "当前首页布局调试"),
                createBaseVNode("p", { class: "home-layout-debug-copy" }, "复制后发回，用于定位 iOS 安全区和底栏位置。")
              ], -1)),
              createBaseVNode("button", {
                class: "home-layout-debug-close",
                type: "button",
                onClick: _cache[8] || (_cache[8] = //@ts-ignore
                (...args) => unref(hideHomeLayoutDebug) && unref(hideHomeLayoutDebug)(...args))
              }, "隐藏")
            ]),
            createBaseVNode("div", _hoisted_6$1, [
              createBaseVNode("button", {
                class: "home-layout-debug-primary",
                type: "button",
                onClick: _cache[9] || (_cache[9] = //@ts-ignore
                (...args) => unref(copyHomeLayoutDebugReport) && unref(copyHomeLayoutDebugReport)(...args))
              }, "复制调试信息"),
              createBaseVNode("button", {
                class: "home-layout-debug-secondary",
                type: "button",
                onClick: _cache[10] || (_cache[10] = //@ts-ignore
                (...args) => unref(toggleHomeLayoutDebugReport) && unref(toggleHomeLayoutDebugReport)(...args))
              }, toDisplayString(unref(homeLayoutDebugExpanded) ? "收起" : "显示"), 1)
            ]),
            unref(homeLayoutDebugExpanded) ? (openBlock(), createElementBlock("textarea", {
              key: 0,
              class: "home-layout-debug-output",
              value: unref(homeLayoutDebugReport),
              readonly: "",
              rows: "12"
            }, null, 8, _hoisted_7$1)) : createCommentVNode("", true)
          ])) : createCommentVNode("", true),
          createVNode(Transition, { name: "modal-pop" }, {
            default: withCtx(() => [
              unref(showLoginPrompt) ? (openBlock(), createElementBlock("div", _hoisted_8, [..._cache[32] || (_cache[32] = [
                createBaseVNode("div", { class: "login-mask-card modal-pop-card" }, "请先在个人中心登录", -1)
              ])])) : createCommentVNode("", true)
            ]),
            _: 1
          }),
          createVNode(Transition, { name: "modal-pop" }, {
            default: withCtx(() => [
              unref(showDailyAccessDialog) ? (openBlock(), createElementBlock("div", _hoisted_9, [
                createBaseVNode("div", _hoisted_10, [
                  _cache[34] || (_cache[34] = createBaseVNode("h3", null, "输入今日秘钥", -1)),
                  createBaseVNode("p", _hoisted_11, toDisplayString(unref(protectedViewPromptTitle)) + " 已启用访问门禁，请输入根据当天日期生成的秘钥后再进入。 ", 1),
                  _cache[35] || (_cache[35] = createBaseVNode("p", { class: "daily-access-tip" }, "详情请询问管理员。", -1)),
                  createBaseVNode("form", {
                    class: "daily-access-form",
                    onSubmit: _cache[13] || (_cache[13] = withModifiers(
                      //@ts-ignore
                      (...args) => unref(submitDailyAccessKey) && unref(submitDailyAccessKey)(...args),
                      ["prevent"]
                    ))
                  }, [
                    createBaseVNode("input", {
                      class: "daily-access-input",
                      value: unref(dailyAccessInput),
                      type: "text",
                      placeholder: "例如 ABCDE-FGHIJ",
                      autocomplete: "off",
                      autocapitalize: "characters",
                      spellcheck: "false",
                      maxlength: "11",
                      autofocus: "",
                      onInput: _cache[11] || (_cache[11] = //@ts-ignore
                      (...args) => unref(handleDailyAccessInput) && unref(handleDailyAccessInput)(...args))
                    }, null, 40, _hoisted_12),
                    unref(dailyAccessError) ? (openBlock(), createElementBlock("p", _hoisted_13, toDisplayString(unref(dailyAccessError)), 1)) : createCommentVNode("", true),
                    createBaseVNode("div", _hoisted_14, [
                      createBaseVNode("button", {
                        type: "button",
                        class: "btn-secondary",
                        onClick: _cache[12] || (_cache[12] = //@ts-ignore
                        (...args) => unref(closeDailyAccessDialog) && unref(closeDailyAccessDialog)(...args))
                      }, "取消"),
                      _cache[33] || (_cache[33] = createBaseVNode("button", {
                        type: "submit",
                        class: "btn-primary"
                      }, "验证并进入", -1))
                    ])
                  ], 32)
                ])
              ])) : createCommentVNode("", true)
            ]),
            _: 1
          }),
          createVNode(Transition, { name: "modal-pop" }, {
            default: withCtx(() => [
              unref(showExitDialog) ? (openBlock(), createElementBlock("div", _hoisted_15, [
                createBaseVNode("div", _hoisted_16, [
                  _cache[36] || (_cache[36] = createBaseVNode("h3", null, "退出应用", -1)),
                  _cache[37] || (_cache[37] = createBaseVNode("p", null, "是否退出 Mini-HBUT？", -1)),
                  createBaseVNode("div", _hoisted_17, [
                    createBaseVNode("button", {
                      class: "btn-secondary",
                      disabled: unref(exitingApp),
                      onClick: _cache[14] || (_cache[14] = //@ts-ignore
                      (...args) => unref(cancelExitDialog) && unref(cancelExitDialog)(...args))
                    }, "取消", 8, _hoisted_18),
                    createBaseVNode("button", {
                      class: "btn-danger",
                      disabled: unref(exitingApp),
                      onClick: _cache[15] || (_cache[15] = //@ts-ignore
                      (...args) => unref(confirmExitDialog) && unref(confirmExitDialog)(...args))
                    }, toDisplayString(unref(exitingApp) ? "退出中..." : "退出"), 9, _hoisted_19)
                  ])
                ])
              ])) : createCommentVNode("", true)
            ]),
            _: 1
          }),
          unref(showUpdateDialog) ? (openBlock(), createBlock(UpdateDialog, {
            key: 3,
            onClose: _cache[16] || (_cache[16] = ($event) => showUpdateDialog.value = false)
          })) : createCommentVNode("", true),
          createVNode(Transition, { name: "modal-pop" }, {
            default: withCtx(() => [
              unref(showForceUpdate) ? (openBlock(), createElementBlock("div", _hoisted_20, [
                createBaseVNode("div", _hoisted_21, [
                  _cache[39] || (_cache[39] = createBaseVNode("h3", null, "需要更新", -1)),
                  createBaseVNode("p", _hoisted_22, toDisplayString(unref(forceUpdateInfo)?.message || "当前版本过低，请更新后继续使用。"), 1),
                  createBaseVNode("p", _hoisted_23, "最低版本：v" + toDisplayString(unref(forceUpdateInfo)?.min_version), 1),
                  unref(forceUpdateResolvedUrl) ? (openBlock(), createElementBlock("div", _hoisted_24, [
                    _cache[38] || (_cache[38] = createBaseVNode("span", { class: "force-update-label" }, "下载地址", -1)),
                    createBaseVNode("button", {
                      class: "force-update-link",
                      onClick: _cache[17] || (_cache[17] = //@ts-ignore
                      (...args) => unref(handleForceUpdate) && unref(handleForceUpdate)(...args))
                    }, toDisplayString(unref(forceUpdateDisplayUrl)), 1)
                  ])) : createCommentVNode("", true),
                  createBaseVNode("div", _hoisted_25, [
                    createBaseVNode("button", {
                      class: "btn-primary force-update-btn",
                      onClick: _cache[18] || (_cache[18] = //@ts-ignore
                      (...args) => unref(handleForceUpdate) && unref(handleForceUpdate)(...args))
                    }, "立即更新")
                  ])
                ])
              ])) : createCommentVNode("", true)
            ]),
            _: 1
          }),
          createVNode(Transition, { name: "modal-pop" }, {
            default: withCtx(() => [
              unref(showAnnouncementModal) ? (openBlock(), createElementBlock("div", {
                key: 0,
                class: "notice-modal-overlay",
                onClick: _cache[22] || (_cache[22] = withModifiers(
                  //@ts-ignore
                  (...args) => unref(closeAnnouncement) && unref(closeAnnouncement)(...args),
                  ["self"]
                ))
              }, [
                createBaseVNode("div", _hoisted_26, [
                  createBaseVNode("div", _hoisted_27, [
                    createBaseVNode("h3", null, toDisplayString(unref(activeAnnouncement)?.title), 1),
                    createBaseVNode("button", {
                      class: "notice-close",
                      onClick: _cache[19] || (_cache[19] = //@ts-ignore
                      (...args) => unref(closeAnnouncement) && unref(closeAnnouncement)(...args))
                    }, "×")
                  ]),
                  unref(activeAnnouncement)?.updated_at ? (openBlock(), createElementBlock("div", _hoisted_28, " 更新时间：" + toDisplayString(unref(activeAnnouncement).updated_at), 1)) : createCommentVNode("", true),
                  unref(activeAnnouncement)?.image ? (openBlock(), createElementBlock("div", _hoisted_29, [
                    createBaseVNode("img", {
                      src: unref(activeAnnouncement).image,
                      alt: unref(activeAnnouncement).title
                    }, null, 8, _hoisted_30)
                  ])) : createCommentVNode("", true),
                  createBaseVNode("div", {
                    class: "notice-modal-content select-text",
                    onClick: _cache[20] || (_cache[20] = //@ts-ignore
                    (...args) => unref(handleContentClick) && unref(handleContentClick)(...args)),
                    innerHTML: unref(activeAnnouncementHtml)
                  }, null, 8, _hoisted_31),
                  unref(activeAnnouncement)?.link ? (openBlock(), createElementBlock("a", {
                    key: 2,
                    class: "notice-link",
                    href: unref(activeAnnouncement).link,
                    target: "_blank",
                    onClick: _cache[21] || (_cache[21] = withModifiers(($event) => unref(handleExternalOpen)(unref(activeAnnouncement).link, $event), ["prevent"]))
                  }, " 查看原文 ", 8, _hoisted_32)) : createCommentVNode("", true)
                ])
              ])) : createCommentVNode("", true)
            ]),
            _: 1
          }),
          createVNode(Transition, { name: "modal-pop" }, {
            default: withCtx(() => [
              unref(showBlockingAnnouncement) ? (openBlock(), createElementBlock("div", _hoisted_33, [
                createBaseVNode("div", _hoisted_34, [
                  _cache[40] || (_cache[40] = createBaseVNode("h3", null, "重要公告", -1)),
                  createBaseVNode("p", _hoisted_35, toDisplayString(unref(blockingAnnouncement)?.title), 1),
                  createBaseVNode("div", {
                    class: "notice-confirm-content",
                    innerHTML: unref(blockingAnnouncementHtml)
                  }, null, 8, _hoisted_36),
                  createBaseVNode("div", _hoisted_37, [
                    createBaseVNode("button", {
                      class: "btn-secondary",
                      onClick: _cache[23] || (_cache[23] = ($event) => unref(openAnnouncement)(unref(blockingAnnouncement)))
                    }, "查看详情"),
                    createBaseVNode("button", {
                      class: "btn-primary",
                      onClick: _cache[24] || (_cache[24] = //@ts-ignore
                      (...args) => unref(confirmBlockingAnnouncement) && unref(confirmBlockingAnnouncement)(...args))
                    }, "我已知晓")
                  ])
                ])
              ])) : createCommentVNode("", true)
            ]),
            _: 1
          }),
          createVNode(WorkspaceLayoutEditor, {
            visible: unref(showWorkspaceLayoutEditor),
            "initial-tab": unref(workspaceLayoutEditorTab),
            onClose: unref(closeWorkspaceLayoutEditor)
          }, null, 8, ["visible", "initial-tab", "onClose"]),
          createVNode(Toast),
          createVNode(IdentityApprovalOverlay, {
            identity: unref(runtime).identity,
            "student-id": unref(studentId),
            "is-logged-in": unref(isLoggedIn),
            "force-update-visible": unref(showForceUpdate),
            "blocking-announcement-visible": unref(showBlockingAnnouncement)
          }, null, 8, ["identity", "student-id", "is-logged-in", "force-update-visible", "blocking-announcement-visible"])
        ]),
        _: 1
      }, 8, ["view", "student-id", "logged-in"]);
    };
  }
});
const App = /* @__PURE__ */ _export_sfc(_sfc_main$1, [["__scopeId", "data-v-b379bc04"]]);
const _hoisted_1 = ["value", "disabled"];
const _hoisted_2 = ["disabled"];
const _hoisted_3 = {
  class: "ios26-select-sheet",
  role: "dialog",
  "aria-modal": "true"
};
const _hoisted_4 = { class: "ios26-select-title" };
const _hoisted_5 = { class: "ios26-select-list" };
const _hoisted_6 = ["disabled", "onClick"];
const _hoisted_7 = { class: "ios26-select-item-label" };
const _sfc_main = /* @__PURE__ */ Object.assign({ inheritAttrs: false }, {
  __name: "IOSSelect",
  props: {
    modelValue: {
      type: [String, Number, Boolean],
      default: ""
    },
    modelModifiers: {
      type: Object,
      default: () => ({})
    },
    placeholder: {
      type: String,
      default: "请选择"
    },
    title: {
      type: String,
      default: ""
    },
    disabled: {
      type: Boolean,
      default: false
    }
  },
  emits: ["update:modelValue", "change"],
  setup(__props, { emit: __emit }) {
    const props = __props;
    const emit = __emit;
    const nativeSelectRef = ref(null);
    const open = ref(false);
    const optionList = ref([]);
    let lastBodyOverflow = "";
    let optionObserver = null;
    const asComparableValue = (value) => {
      if (value === null || value === void 0) return "";
      return String(value);
    };
    const parseOutgoingValue = (value) => {
      if (props.modelModifiers?.number) {
        const n = Number(value);
        if (Number.isFinite(n)) return n;
      }
      return value;
    };
    const isSameOptionList = (a, b) => {
      if (!Array.isArray(a) || !Array.isArray(b)) return false;
      if (a.length !== b.length) return false;
      for (let i = 0; i < a.length; i += 1) {
        const x = a[i];
        const y = b[i];
        if (!x || !y) return false;
        if (x.value !== y.value) return false;
        if (x.label !== y.label) return false;
        if (x.disabled !== y.disabled) return false;
      }
      return true;
    };
    const refreshOptionsFromNative = () => {
      const el = nativeSelectRef.value;
      if (!el) {
        if (optionList.value.length) optionList.value = [];
        return;
      }
      const nextList = Array.from(el.options || []).map((option, idx) => ({
        key: `${idx}:${option.value}:${option.textContent || ""}`,
        value: option.value,
        label: String(option.textContent || "").trim(),
        disabled: !!option.disabled
      }));
      if (!isSameOptionList(optionList.value, nextList)) {
        optionList.value = nextList;
      }
    };
    const selectedOption = computed(() => {
      const target = asComparableValue(props.modelValue);
      return optionList.value.find((option) => asComparableValue(option.value) === target) || null;
    });
    const selectedLabel = computed(() => selectedOption.value?.label || props.placeholder);
    const syncNativeSelectValue = () => {
      const el = nativeSelectRef.value;
      if (!el) return;
      const target = asComparableValue(props.modelValue);
      if (el.value !== target) {
        el.value = target;
      }
    };
    const emitChangeEvent = (value) => {
      emit("change", { target: { value }, detail: { value } });
    };
    const closePicker = () => {
      open.value = false;
    };
    const openPicker = async () => {
      if (props.disabled) return;
      await nextTick();
      refreshOptionsFromNative();
      if (!optionList.value.length) return;
      open.value = true;
    };
    const selectOption = (option) => {
      if (!option || option.disabled) return;
      const value = parseOutgoingValue(option.value);
      emit("update:modelValue", value);
      emitChangeEvent(value);
      closePicker();
    };
    const handleKeydown = (event) => {
      if (event.key === "Escape") {
        closePicker();
      }
    };
    const disconnectOptionObserver = () => {
      if (optionObserver) {
        optionObserver.disconnect();
        optionObserver = null;
      }
    };
    const connectOptionObserver = () => {
      disconnectOptionObserver();
      const el = nativeSelectRef.value;
      if (!el || typeof MutationObserver === "undefined") return;
      optionObserver = new MutationObserver(() => {
        refreshOptionsFromNative();
        syncNativeSelectValue();
      });
      optionObserver.observe(el, {
        childList: true,
        subtree: true,
        characterData: true,
        attributes: true
      });
    };
    watch(
      () => props.modelValue,
      async () => {
        await nextTick();
        syncNativeSelectValue();
        refreshOptionsFromNative();
      },
      { immediate: true }
    );
    watch(open, (value) => {
      if (typeof document === "undefined") return;
      if (value) {
        lastBodyOverflow = document.body.style.overflow;
        document.body.style.overflow = "hidden";
        window.addEventListener("keydown", handleKeydown);
        return;
      }
      document.body.style.overflow = lastBodyOverflow || "";
      window.removeEventListener("keydown", handleKeydown);
    });
    onMounted(async () => {
      await nextTick();
      refreshOptionsFromNative();
      syncNativeSelectValue();
      connectOptionObserver();
    });
    watch(
      () => nativeSelectRef.value,
      async () => {
        await nextTick();
        refreshOptionsFromNative();
        syncNativeSelectValue();
        connectOptionObserver();
      }
    );
    onBeforeUnmount(() => {
      closePicker();
      disconnectOptionObserver();
      if (typeof document !== "undefined") {
        document.body.style.overflow = lastBodyOverflow || "";
      }
      if (typeof window !== "undefined") {
        window.removeEventListener("keydown", handleKeydown);
      }
    });
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock(Fragment, null, [
        createBaseVNode("div", {
          class: normalizeClass(["ios26-select", { "is-disabled": __props.disabled }])
        }, [
          createBaseVNode("select", {
            ref_key: "nativeSelectRef",
            ref: nativeSelectRef,
            class: "ios26-select-native",
            value: asComparableValue(__props.modelValue),
            disabled: __props.disabled,
            tabindex: "-1",
            "aria-hidden": "true"
          }, [
            renderSlot(_ctx.$slots, "default", {}, void 0, true)
          ], 8, _hoisted_1),
          createBaseVNode("button", {
            type: "button",
            class: "ios26-select-trigger",
            disabled: __props.disabled,
            onClick: openPicker
          }, [
            createBaseVNode("span", {
              class: normalizeClass(["ios26-select-text", { placeholder: !selectedOption.value }])
            }, toDisplayString(selectedLabel.value), 3),
            _cache[0] || (_cache[0] = createBaseVNode("span", {
              class: "ios26-select-arrow",
              "aria-hidden": "true"
            }, [
              createBaseVNode("svg", {
                viewBox: "0 0 20 20",
                fill: "none"
              }, [
                createBaseVNode("path", { d: "M5 7.5L10 12.5L15 7.5" })
              ])
            ], -1))
          ], 8, _hoisted_2)
        ], 2),
        (openBlock(), createBlock(Teleport, { to: "body" }, [
          createVNode(Transition, { name: "ios26-select-fade" }, {
            default: withCtx(() => [
              open.value ? (openBlock(), createElementBlock("div", {
                key: 0,
                class: "ios26-select-overlay",
                onClick: withModifiers(closePicker, ["self"])
              }, [
                createBaseVNode("div", _hoisted_3, [
                  _cache[2] || (_cache[2] = createBaseVNode("div", { class: "ios26-select-handle" }, null, -1)),
                  createBaseVNode("div", _hoisted_4, toDisplayString(__props.title || "选择选项"), 1),
                  createBaseVNode("div", _hoisted_5, [
                    (openBlock(true), createElementBlock(Fragment, null, renderList(optionList.value, (option) => {
                      return openBlock(), createElementBlock("button", {
                        key: option.key,
                        type: "button",
                        class: normalizeClass(["ios26-select-item", {
                          "is-active": asComparableValue(option.value) === asComparableValue(__props.modelValue),
                          "is-disabled": option.disabled
                        }]),
                        disabled: option.disabled,
                        onClick: ($event) => selectOption(option)
                      }, [
                        createBaseVNode("span", _hoisted_7, toDisplayString(option.label || "未命名选项"), 1),
                        _cache[1] || (_cache[1] = createBaseVNode("span", {
                          class: "ios26-select-item-check",
                          "aria-hidden": "true"
                        }, "✓", -1))
                      ], 10, _hoisted_6);
                    }), 128))
                  ]),
                  createBaseVNode("button", {
                    type: "button",
                    class: "ios26-select-cancel",
                    onClick: closePicker
                  }, "取消")
                ])
              ])) : createCommentVNode("", true)
            ]),
            _: 1
          })
        ]))
      ], 64);
    };
  }
});
const IOSSelect = /* @__PURE__ */ _export_sfc(_sfc_main, [["__scopeId", "data-v-c8361503"]]);
const UI_SETTINGS_STORAGE_KEY = "hbu_ui_settings_v2";
const DEFAULT_PRESET_KEY = "campus_blue";
function hexToHsl(hex) {
  if (!hex || typeof hex !== "string") {
    console.warn("[theme-bridge] hexToHsl: 无效颜色值", hex);
    return "0 0% 0%";
  }
  const cleaned = hex.replace(/^#/, "").trim();
  if (!/^[0-9a-fA-F]{3}$|^[0-9a-fA-F]{6}$/.test(cleaned)) {
    console.warn("[theme-bridge] hexToHsl: 无效颜色格式", hex);
    return "0 0% 0%";
  }
  const fullHex = cleaned.length === 3 ? cleaned[0] + cleaned[0] + cleaned[1] + cleaned[1] + cleaned[2] + cleaned[2] : cleaned;
  const r = parseInt(fullHex.slice(0, 2), 16) / 255;
  const g = parseInt(fullHex.slice(2, 4), 16) / 255;
  const b = parseInt(fullHex.slice(4, 6), 16) / 255;
  const max = Math.max(r, g, b);
  const min = Math.min(r, g, b);
  const delta = max - min;
  const l = (max + min) / 2;
  if (delta === 0) {
    return `0 0% ${Math.round(l * 100)}%`;
  }
  const s = l > 0.5 ? delta / (2 - max - min) : delta / (max + min);
  let h;
  if (max === r) {
    h = ((g - b) / delta + (g < b ? 6 : 0)) * 60;
  } else if (max === g) {
    h = ((b - r) / delta + 2) * 60;
  } else {
    h = ((r - g) / delta + 4) * 60;
  }
  return `${Math.round(h)} ${Math.round(s * 100)}% ${Math.round(l * 100)}%`;
}
function transformPresetToAppleDesign(preset) {
  const isDark = preset.category === "dark";
  if (isDark) {
    return {
      "--primary": hexToHsl("#2997ff"),
      // primary-on-dark
      "--primary-foreground": hexToHsl("#ffffff"),
      // on-dark
      "--secondary": hexToHsl("#2a2a2c"),
      // surface-tile-2
      "--secondary-foreground": hexToHsl("#ffffff"),
      "--background": hexToHsl("#272729"),
      // surface-tile-1
      "--foreground": hexToHsl("#ffffff"),
      // on-dark
      "--card": hexToHsl("#2a2a2c"),
      // surface-tile-2
      "--card-foreground": hexToHsl("#ffffff"),
      "--border": hexToHsl("#252527"),
      // surface-tile-3
      "--muted": hexToHsl("#252527"),
      // surface-tile-3
      "--muted-foreground": hexToHsl("#cccccc"),
      // body-muted
      "--accent": hexToHsl("#252527"),
      // surface-tile-3
      "--accent-foreground": hexToHsl("#ffffff"),
      "--destructive": hexToHsl("#ef4444"),
      // 标准 destructive
      "--destructive-foreground": hexToHsl("#ffffff"),
      "--radius": "0.6875rem"
      // 11px = rounded.md
    };
  }
  return {
    "--primary": hexToHsl("#0066cc"),
    // Apple primary blue
    "--primary-foreground": hexToHsl("#ffffff"),
    // on-primary
    "--secondary": hexToHsl("#f5f5f7"),
    // canvas-parchment
    "--secondary-foreground": hexToHsl("#1d1d1f"),
    // ink
    "--background": hexToHsl("#f5f5f7"),
    // canvas-parchment
    "--foreground": hexToHsl("#1d1d1f"),
    // ink
    "--card": hexToHsl("#ffffff"),
    // canvas (white)
    "--card-foreground": hexToHsl("#1d1d1f"),
    // ink
    "--border": hexToHsl("#e0e0e0"),
    // hairline
    "--muted": hexToHsl("#f5f5f7"),
    // canvas-parchment
    "--muted-foreground": hexToHsl("#7a7a7a"),
    // ink-muted-48
    "--accent": hexToHsl("#f5f5f7"),
    // canvas-parchment
    "--accent-foreground": hexToHsl("#1d1d1f"),
    // ink
    "--destructive": hexToHsl("#ef4444"),
    // 标准 destructive
    "--destructive-foreground": hexToHsl("#ffffff"),
    "--radius": "0.6875rem"
    // 11px = rounded.md
  };
}
function applyThemePreset(presetKey) {
  const preset = UI_PRESETS[presetKey];
  if (!preset) {
    console.warn(`[theme-bridge] 未知预设 "${presetKey}"，回退到 "${DEFAULT_PRESET_KEY}"`);
    const fallback = UI_PRESETS[DEFAULT_PRESET_KEY];
    if (!fallback) return;
    applyThemePresetInternal(fallback);
    return;
  }
  applyThemePresetInternal(preset);
}
function applyThemePresetInternal(preset) {
  if (typeof document === "undefined") return;
  const vars = transformPresetToAppleDesign(preset);
  const root = document.documentElement;
  for (const [key, value] of Object.entries(vars)) {
    root.style.setProperty(key, value);
  }
}
function detectOsColorScheme() {
  try {
    if (typeof window !== "undefined" && window.matchMedia) {
      const darkQuery = window.matchMedia("(prefers-color-scheme: dark)");
      if (darkQuery.matches) return "dark";
    }
  } catch {
  }
  return "light";
}
function readPersistedPresetKey() {
  try {
    const raw = localStorage.getItem(UI_SETTINGS_STORAGE_KEY);
    if (!raw) return null;
    const parsed = JSON.parse(raw);
    if (parsed && typeof parsed.preset === "string" && UI_PRESETS[parsed.preset]) {
      return parsed.preset;
    }
    return null;
  } catch {
    return null;
  }
}
function initThemeBridge() {
  if (typeof document === "undefined") return;
  const persistedKey = readPersistedPresetKey();
  let presetKey;
  if (persistedKey) {
    presetKey = persistedKey;
  } else {
    const osScheme = detectOsColorScheme();
    if (osScheme === "dark") {
      const darkPresetKey = Object.keys(UI_PRESETS).find(
        (key) => UI_PRESETS[key].category === "dark"
      );
      presetKey = darkPresetKey || DEFAULT_PRESET_KEY;
    } else {
      presetKey = DEFAULT_PRESET_KEY;
    }
  }
  applyThemePreset(presetKey);
  setupOsSchemeListener();
  initDarkModeClass();
}
function initDarkModeClass() {
  initNightModeClass();
}
function setupOsSchemeListener() {
  try {
    if (typeof window === "undefined" || !window.matchMedia) return;
    const darkQuery = window.matchMedia("(prefers-color-scheme: dark)");
    const handler = (event) => {
      const persisted = readPersistedPresetKey();
      if (persisted) return;
      if (event.matches) {
        const darkPresetKey = Object.keys(UI_PRESETS).find(
          (key) => UI_PRESETS[key].category === "dark"
        );
        applyThemePreset(darkPresetKey || DEFAULT_PRESET_KEY);
      } else {
        applyThemePreset(DEFAULT_PRESET_KEY);
      }
    };
    if (darkQuery.addEventListener) {
      darkQuery.addEventListener("change", handler);
    } else if (darkQuery.addListener) {
      darkQuery.addListener(handler);
    }
  } catch {
  }
}
const LEGACY_TO_NEW_KEY_MAP = [
  ["hbu_bg_enabled", "hbu_notify_bg"],
  ["hbu_bg_enable_grade", "hbu_notify_grade"],
  ["hbu_bg_enable_exam", "hbu_notify_exam"],
  ["hbu_bg_enable_power", "hbu_notify_power"],
  ["hbu_bg_enable_class", "hbu_notify_class"],
  ["hbu_bg_enable_school_inbox", "hbu_notify_school_inbox"],
  ["hbu_bg_class_lead_min", "hbu_notify_class_lead_min"],
  ["hbu_bg_interval_min", "hbu_notify_interval"]
];
const LEGACY_DISCARD_KEYS = [
  "hbu_bg_student_id",
  "hbu_bg_api_base",
  "hbu_bg_login_method",
  "hbu_bg_dorm_selection",
  "hbu_bg_chaoxing_notice_cookie"
];
const MIGRATED_MARKER_KEY = "hbu_legacy_bg_migrated_v1";
const toBooleanString = (value) => {
  const text = String(value ?? "").trim();
  if (text === "1" || text === "true") return "true";
  if (text === "0" || text === "false") return "false";
  return null;
};
const migrateLocalStorageKeys = () => {
  for (const [legacy, next] of LEGACY_TO_NEW_KEY_MAP) {
    const raw = localStorage.getItem(legacy);
    if (raw === null) continue;
    const converted = toBooleanString(raw);
    if (converted !== null && localStorage.getItem(next) === null) {
      localStorage.setItem(next, converted);
    }
    localStorage.removeItem(legacy);
  }
  for (const key of LEGACY_DISCARD_KEYS) {
    localStorage.removeItem(key);
  }
  const staleKeys = [];
  for (let i = 0; i < localStorage.length; i += 1) {
    const key = localStorage.key(i);
    if (key && key.startsWith("hbu_bg_school_inbox_state:")) staleKeys.push(key);
  }
  for (const key of staleKeys) {
    localStorage.removeItem(key);
  }
};
const tryMigrateNativePreferences = async () => {
  try {
    const w = window;
    const preferences = w.Capacitor?.Plugins?.Preferences;
    if (!preferences?.get) return;
    for (const [legacy, next] of LEGACY_TO_NEW_KEY_MAP) {
      if (localStorage.getItem(next) !== null) continue;
      const result = await preferences.get({ key: legacy });
      const converted = toBooleanString(result?.value);
      if (converted !== null) {
        localStorage.setItem(next, converted);
      }
    }
  } catch {
  }
};
const migrateLegacyBackgroundState = async () => {
  try {
    if (typeof localStorage === "undefined") return;
    if (localStorage.getItem(MIGRATED_MARKER_KEY) === "1") return;
    migrateLocalStorageKeys();
    await tryMigrateNativePreferences();
    localStorage.setItem(MIGRATED_MARKER_KEY, "1");
    pushDebugLog("LegacyMigration", "旧 Capacitor 后台状态迁移完成（幂等）", "info");
  } catch (error) {
    pushDebugLog("LegacyMigration", "旧后台状态迁移失败，将在下次启动重试", "warn", error);
  }
};
const MAX_SNIPPET = 500;
let installed = false;
const redactSensitive = (text) => text.replace(
  /((?:access|refresh|id)?[_-]?token|cookie|authorization|password|passwd|secret|session[_-]?id)\s*[=:]\s*[^\s;&"',}]+/gi,
  "$1=[已脱敏]"
);
const toSnippet = (input, max = MAX_SNIPPET) => {
  let text = "";
  if (typeof input === "string") {
    text = input;
  } else if (input instanceof Error) {
    text = String(input.stack || input.message || input);
  } else if (input === null || input === void 0) {
    text = String(input);
  } else {
    try {
      text = JSON.stringify(input);
    } catch {
      text = String(input);
    }
  }
  text = redactSensitive(String(text || "").trim());
  return text.length > max ? `${text.slice(0, max)}…(截断)` : text;
};
const handleWindowError = (event) => {
  try {
    const stack = toSnippet(event.error?.stack || "");
    const parts = [
      `未捕获异常: ${toSnippet(event.message || "(无消息)", 200)}`,
      `位置: ${toSnippet(event.filename || "(未知文件)", 160)}:${event.lineno || 0}:${event.colno || 0}`
    ];
    if (stack) parts.push(`堆栈: ${stack}`);
    pushDebugLog("GlobalError", parts.join(" | "), "error");
  } catch {
  }
};
const handleUnhandledRejection = (event) => {
  try {
    const reason = event.reason;
    const stack = toSnippet(reason?.stack || "");
    const parts = [`未处理的 Promise 拒绝: ${toSnippet(String(event.reason), 200)}`];
    if (stack) parts.push(`堆栈: ${stack}`);
    pushDebugLog("GlobalError", parts.join(" | "), "error");
  } catch {
  }
};
const installGlobalErrorCapture = () => {
  if (installed || typeof window === "undefined") return;
  window.addEventListener("error", handleWindowError);
  window.addEventListener("unhandledrejection", handleUnhandledRejection);
  installed = true;
  pushDebugLog("GlobalError", "全局错误捕获已安装（error / unhandledrejection）", "info");
};
const attachVueErrorCapture = (app) => {
  const previous = app.config.errorHandler;
  app.config.errorHandler = (error, instance, info) => {
    try {
      const componentName = instance?.$options && (instance.$options.name || instance.$options.__name) || "(匿名组件)";
      pushDebugLog(
        "GlobalError",
        `Vue 错误 [${toSnippet(String(info), 80)}] 组件: ${toSnippet(String(componentName), 80)} | ${toSnippet(error)}`,
        "error"
      );
    } catch {
    }
    if (typeof previous === "function") {
      previous(error, instance, info);
      return;
    }
    console.error("[Vue] 组件错误:", error);
  };
};
const isWebsiteDemo = bootstrapWebsiteDemoIfNeeded();
void ensureMaterialSymbolsFont();
const removeNativeSplash = () => {
  try {
    const w = window;
    if (typeof w.__removeNativeSplash === "function") {
      w.__removeNativeSplash("vue-mount");
      return;
    }
  } catch {
  }
  try {
    document.getElementById("native-splash")?.remove();
  } catch {
  }
};
const mountApp = () => {
  const app = createApp(App);
  app.use(createPinia());
  attachVueErrorCapture(app);
  app.component("IOSSelect", IOSSelect);
  app.mount("#app");
  removeNativeSplash();
  window.setTimeout(removeNativeSplash, 0);
  window.setTimeout(removeNativeSplash, 500);
};
const runDeferredInitializers = () => {
  const run = () => {
    void loadLocalIconFonts();
    void __vitePreload(async () => {
      const { initMarkdownRuntime } = await import("./markdown-BYynpwuf.js");
      return { initMarkdownRuntime };
    }, true ? __vite__mapDeps([54,55]) : void 0, import.meta.url).then(({ initMarkdownRuntime }) => initMarkdownRuntime(6e3)).catch((error) => {
      console.warn("[Bootstrap] markdown runtime init failed:", error);
    });
    void migrateLegacyBackgroundState();
    if (isTauriRuntime()) {
      void invokeNative("get_debug_runtime_config").then((config) => {
        if (!config?.enableBridgeTools) return null;
        return __vitePreload(async () => {
          const { initDebugBridgeClient } = await import("./debug-tools-BgYILSb8.js").then((n) => n.d);
          return { initDebugBridgeClient };
        }, true ? __vite__mapDeps([7,1,0,8]) : void 0, import.meta.url).then(({ initDebugBridgeClient }) => initDebugBridgeClient());
      }).catch((error) => {
        console.warn("[Bootstrap] debug bridge init failed:", error);
      });
    }
  };
  if (isWebsiteDemo) {
    run();
    return;
  }
  if (typeof window !== "undefined" && typeof window.requestIdleCallback === "function") {
    window.requestIdleCallback(() => run(), { timeout: 1200 });
    return;
  }
  setTimeout(run, 0);
};
const bootstrap = () => {
  initThemeBridge();
  initDebugLogger();
  installGlobalErrorCapture();
  pushDebugLog("Bootstrap", "开始初始化应用");
  initUiSettings();
  initAppSettings();
  initFontSettings();
  mountApp();
  runDeferredInitializers();
  pushDebugLog("Bootstrap", "应用初始化完成");
};
try {
  bootstrap();
} catch (error) {
  console.error("[Bootstrap] failed:", error);
  try {
    mountApp();
  } catch (e2) {
    console.error("[Bootstrap] mountApp failed:", e2);
    removeNativeSplash();
  }
}
if (typeof window !== "undefined") {
  window.setTimeout(() => {
    removeNativeSplash();
  }, 4e3);
}
export {
  saveRememberedCredential as $,
  getCloudSyncCooldownState as A,
  consumeScheduleSwitchPending as B,
  CLOUD_SYNC_UPDATED_EVENT as C,
  DEFAULT_SWR_OPTIONS as D,
  EXTRA_LONG_TTL as E,
  isAutoScheduleLockReason as F,
  clearScheduleLock as G,
  readScheduleLock as H,
  warmupScheduleForStudent as I,
  getPreferredSemesterFast as J,
  mergeSemesterOptions as K,
  LONG_TTL as L,
  writeExamToWidget as M,
  getStaleCachedData as N,
  markSchoolInboxNotified as O,
  decryptData as P,
  encryptData as Q,
  getIdentityAccessToken as R,
  SHORT_TTL as S,
  fetchRemoteConfig as T,
  loadPortalRememberedPassword as U,
  loadChaoxingRememberedPassword as V,
  applyOcrRuntimeConfig as W,
  getStoredOcrConfig as X,
  syncPortalRememberCredential as Y,
  runExclusiveLogin as Z,
  _export_sfc as _,
  useUiSettings as a,
  buildHbutAccountKey as a0,
  buildChaoxingAccountKey as a1,
  getLastNotifySnapshot as a2,
  NOTIFY_SNAPSHOT_EVENT as a3,
  getNotificationMonitorSettings as a4,
  runNotificationCheck as a5,
  reconcileLocalReminders as a6,
  identityUiState as a7,
  setIdentityDeviceRefreshing as a8,
  setIdentityDeviceError as a9,
  campusStatusLabel as aA,
  loginCampusNetwork as aB,
  writeCampusNetworkSettings as aC,
  normalizeModuleCenterChannel$1 as aD,
  buildModuleCenterCards as aE,
  trackModuleOpen as aF,
  TModal as aG,
  getChaoxingClassConfig as aH,
  fetchAuthHistory as aI,
  IdentityServiceError as aJ,
  setIdentityDeviceStatus as aa,
  setIdentityRevoking as ab,
  getIdentityCoreBaseUrl as ac,
  clearIdentityDeviceMeta as ad,
  FONT_CDN_OPTIONS as ae,
  getCloudSyncLocalStatus as af,
  getCloudSyncRuntimeConfig as ag,
  useFontSettings as ah,
  setFontCdnProvider as ai,
  prefetchCdnFonts as aj,
  loadDeyiHeiFont as ak,
  ensureFontLoaded as al,
  resetAppSettings as am,
  DEFAULT_CLOUD_SYNC_ENDPOINT as an,
  DEFAULT_BACKEND_TARGETS as ao,
  UI_PRESETS as ap,
  resetUiSettings as aq,
  applyPreset as ar,
  fetchPersonalUsageSummary as as,
  fetchRemotePersonalUsageSummary as at,
  readCampusNetworkSettings as au,
  loadRememberedCredential as av,
  buildCampusAccountKey as aw,
  CAMPUS_CARRIER_OPTIONS as ax,
  HBUT_CAMPUS_GATEWAYS as ay,
  probeCampusNetwork as az,
  axiosInstance as b,
  cloneWorkspaceLayout as c,
  decideHomeNavigate as d,
  flushUiSettings as e,
  fetchWithCache as f,
  getCachedData as g,
  buildDefaultWorkspaceLayout as h,
  showToast as i,
  resolveCurrentSemester as j,
  compareSemesterDesc as k,
  useAppSettings as l,
  SCHEDULE_POPUP_PENDING_KEY as m,
  normalizeSemesterList as n,
  openExternal as o,
  readScheduleRenderSnapshot as p,
  writeScheduleRenderSnapshot as q,
  readScheduleLockDetail as r,
  setCachedData as s,
  afterScheduleRefresh as t,
  useAuthStore as u,
  writeScheduleLock as v,
  writeElectricityToWidget as w,
  getCachedScheduleSnapshot as x,
  runCloudSyncDownload as y,
  runCloudSyncUpload as z
};
