import { a as isTauriRuntime, p as pushDebugLog, P as isMobileLike, e as platformBridge, d as invokeNative } from "./runtime-bridge-Dt57BD2i.js";
import { _ as _export_sfc, u as useLocale, aO as getChaoxingClassConfig, $ as fetchRemoteConfig, j as showToast, a0 as loadPortalRememberedPassword, o as openExternal } from "./app-demo-CUOWTnz-.js";
import { _ as _sfc_main$1 } from "./TPageHeader.vue_vue_type_script_setup_true_lang-Dh0eXg1s.js";
import { a as openBlock, c as createElementBlock, p as createVNode, S as createSlots, k as withCtx, b as createBaseVNode, n as normalizeClass, t as toDisplayString, F as Fragment, d as createCommentVNode, g as createTextVNode, f as renderList, m as withKeys, w as withModifiers, j as createBlock, s as Teleport, r as ref, o as onMounted, h as computed } from "./vue-core-Dzs4fLAU.js";
import "./more-modules-DaLSEgdg.js";
import "./debug-tools-XN5ic1q-.js";
import "./capture-D-zd0oUS.js";
const _hoisted_1 = { class: "cx-page" };
const _hoisted_2 = ["disabled", "title"];
const _hoisted_3 = {
  key: 0,
  class: "cx-boot"
};
const _hoisted_4 = { class: "cx-boot-text" };
const _hoisted_5 = { class: "cx-boot-sub" };
const _hoisted_6 = { class: "cx-sso-label" };
const _hoisted_7 = {
  key: 1,
  class: "cx-alert",
  role: "alert"
};
const _hoisted_8 = { class: "cx-alert-text" };
const _hoisted_9 = ["disabled"];
const _hoisted_10 = { class: "cx-nimbus-head" };
const _hoisted_11 = { class: "cx-nimbus-title-row" };
const _hoisted_12 = { class: "cx-nimbus-titles" };
const _hoisted_13 = { class: "cx-nimbus-course" };
const _hoisted_14 = {
  key: 0,
  class: "cx-nimbus-sub"
};
const _hoisted_15 = ["aria-label"];
const _hoisted_16 = ["aria-selected", "onClick"];
const _hoisted_17 = { class: "cx-toolbar" };
const _hoisted_18 = { class: "cx-toolbar-label" };
const _hoisted_19 = { class: "cx-toolbar-meta" };
const _hoisted_20 = ["aria-label"];
const _hoisted_21 = ["disabled", "onClick"];
const _hoisted_22 = {
  key: 0,
  class: "material-symbols-outlined cx-crumb-chev",
  "aria-hidden": "true"
};
const _hoisted_23 = { class: "cx-list" };
const _hoisted_24 = {
  key: 0,
  class: "cx-skeleton-list"
};
const _hoisted_25 = {
  key: 1,
  class: "cx-empty"
};
const _hoisted_26 = { class: "sub" };
const _hoisted_27 = ["onClick", "onKeydown"];
const _hoisted_28 = ["data-kind"];
const _hoisted_29 = ["src", "alt", "onError"];
const _hoisted_30 = {
  key: 1,
  class: "material-symbols-outlined fill"
};
const _hoisted_31 = { class: "cx-row-main" };
const _hoisted_32 = ["title"];
const _hoisted_33 = {
  key: 0,
  class: "cx-row-meta"
};
const _hoisted_34 = {
  key: 0,
  class: "cx-last-tag"
};
const _hoisted_35 = {
  key: 1,
  class: "cx-row-meta"
};
const _hoisted_36 = {
  key: 0,
  class: "material-symbols-outlined cx-chev"
};
const _hoisted_37 = ["disabled", "title", "onClick"];
const _hoisted_38 = { class: "material-symbols-outlined" };
const _hoisted_39 = { class: "cx-secure-note" };
const _hoisted_40 = {
  key: 3,
  class: "cx-welcome"
};
const _hoisted_41 = { class: "cx-welcome-card" };
const _hoisted_42 = { class: "cx-welcome-badge" };
const _hoisted_43 = {
  key: 0,
  class: "cx-welcome-cover"
};
const _hoisted_44 = ["src"];
const _hoisted_45 = {
  key: 1,
  class: "cx-welcome-icon"
};
const _hoisted_46 = {
  key: 2,
  class: "cx-welcome-teacher"
};
const _hoisted_47 = { class: "cx-welcome-desc" };
const _hoisted_48 = { class: "cx-welcome-actions" };
const _hoisted_49 = {
  key: 3,
  class: "cx-welcome-note"
};
const _hoisted_50 = {
  key: 4,
  class: "cx-welcome-note"
};
const _hoisted_51 = ["aria-label"];
const _hoisted_52 = { class: "cx-preview-sheet" };
const _hoisted_53 = { class: "cx-preview-head" };
const _hoisted_54 = { class: "cx-preview-titles" };
const _hoisted_55 = { class: "cx-preview-kicker" };
const _hoisted_56 = { class: "cx-preview-title" };
const _hoisted_57 = {
  key: 0,
  class: "cx-preview-method-hint"
};
const _hoisted_58 = { class: "cx-preview-head-actions" };
const _hoisted_59 = ["aria-label"];
const _hoisted_60 = {
  key: 0,
  class: "cx-preview-state"
};
const _hoisted_61 = {
  key: 1,
  class: "cx-preview-state err"
};
const _hoisted_62 = { class: "cx-preview-state-actions" };
const _hoisted_63 = ["disabled"];
const _hoisted_64 = {
  key: 0,
  class: "cx-preview-warn"
};
const _hoisted_65 = {
  key: 1,
  class: "cx-preview-image-wrap"
};
const _hoisted_66 = ["src", "alt"];
const _hoisted_67 = ["src"];
const _hoisted_68 = ["src", "title"];
const _hoisted_69 = {
  key: 4,
  class: "cx-preview-state"
};
const _hoisted_70 = { class: "sub" };
const _hoisted_71 = {
  key: 0,
  class: "cx-preview-toolbar"
};
const _hoisted_72 = { class: "cx-preview-toolbar-inner" };
const _hoisted_73 = { class: "cx-open-method-wrap" };
const _hoisted_74 = ["disabled"];
const _hoisted_75 = { class: "cx-toolbar-btn-text" };
const _hoisted_76 = { class: "cx-toolbar-btn-label" };
const _hoisted_77 = { class: "cx-toolbar-btn-sub" };
const _hoisted_78 = { class: "material-symbols-outlined cx-chev-sm" };
const _hoisted_79 = { class: "cx-open-method-menu-title" };
const _hoisted_80 = ["onClick"];
const _hoisted_81 = { class: "material-symbols-outlined" };
const _hoisted_82 = { class: "cx-open-method-item-text" };
const _hoisted_83 = { class: "name" };
const _hoisted_84 = { class: "desc" };
const _hoisted_85 = {
  key: 0,
  class: "material-symbols-outlined check"
};
const _hoisted_86 = ["disabled", "title"];
const _hoisted_87 = { class: "cx-toolbar-btn-label only" };
const _hoisted_88 = ["disabled"];
const _hoisted_89 = { class: "material-symbols-outlined" };
const _hoisted_90 = { class: "cx-toolbar-btn-label only" };
const _hoisted_91 = {
  key: 0,
  class: "cx-dialog-root",
  role: "dialog",
  "aria-modal": "true",
  "aria-labelledby": "cx-join-title"
};
const _hoisted_92 = { class: "cx-dialog" };
const _hoisted_93 = ["src"];
const _hoisted_94 = {
  key: 1,
  class: "material-symbols-outlined fill"
};
const _hoisted_95 = { class: "cx-dialog-kicker" };
const _hoisted_96 = { id: "cx-join-title" };
const _hoisted_97 = { class: "cx-dialog-course" };
const _hoisted_98 = {
  key: 0,
  class: "cx-dialog-teacher"
};
const _hoisted_99 = { class: "cx-dialog-desc" };
const _hoisted_100 = { class: "cx-dialog-actions" };
const _hoisted_101 = ["disabled"];
const _hoisted_102 = ["disabled"];
function render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", _hoisted_1, [
    createVNode($setup["TPageHeader"], {
      title: $setup.t("chaoxing.brand"),
      icon: "menu_book",
      onBack: _cache[0] || (_cache[0] = ($event) => $setup.emit("back"))
    }, createSlots({ _: 2 }, [
      $setup.isJoined ? {
        name: "actions",
        fn: withCtx(() => [
          createBaseVNode("button", {
            type: "button",
            class: "cx-icon-btn",
            disabled: $setup.loadingResources,
            title: $setup.t("chaoxing.class.refreshTitle"),
            onClick: $setup.loadResources
          }, [
            createBaseVNode("span", {
              class: normalizeClass(["material-symbols-outlined", { spin: $setup.loadingResources }])
            }, "refresh", 2)
          ], 8, _hoisted_2)
        ]),
        key: "0"
      } : void 0
    ]), 1032, ["title"]),
    $setup.loadingBoot ? (openBlock(), createElementBlock("div", _hoisted_3, [
      _cache[6] || (_cache[6] = createBaseVNode("div", { class: "cx-spinner" }, null, -1)),
      createBaseVNode("p", _hoisted_4, toDisplayString($setup.bootPhase === "sso" ? $setup.t("chaoxing.class.bootSso") : $setup.bootPhase === "preview" ? $setup.t("chaoxing.class.bootPreview") : $setup.t("chaoxing.class.bootFirstSso")), 1),
      createBaseVNode("p", _hoisted_5, toDisplayString($setup.t("chaoxing.class.bootSub")), 1)
    ])) : (openBlock(), createElementBlock(Fragment, { key: 1 }, [
      !$setup.ssoReady || $setup.ssoHint ? (openBlock(), createElementBlock("div", {
        key: 0,
        class: normalizeClass(["cx-sso-chip", { ok: $setup.ssoReady, bad: !$setup.ssoReady }]),
        role: "status"
      }, [
        _cache[7] || (_cache[7] = createBaseVNode("span", { class: "cx-sso-dot" }, null, -1)),
        createBaseVNode("span", _hoisted_6, toDisplayString($setup.ssoReady ? $setup.ssoHint || $setup.t("chaoxing.class.portalConnected") : $setup.ssoHint || $setup.t("chaoxing.class.sessionBad")), 1),
        !$setup.ssoReady ? (openBlock(), createElementBlock("button", {
          key: 0,
          type: "button",
          class: "cx-link-btn",
          onClick: $setup.boot
        }, toDisplayString($setup.t("chaoxing.class.retry")), 1)) : createCommentVNode("", true),
        !$setup.ssoReady ? (openBlock(), createElementBlock("button", {
          key: 1,
          type: "button",
          class: "cx-link-btn",
          onClick: _cache[1] || (_cache[1] = ($event) => $setup.emit("back"))
        }, toDisplayString($setup.t("chaoxing.class.goLogin")), 1)) : createCommentVNode("", true)
      ], 2)) : createCommentVNode("", true),
      $setup.error ? (openBlock(), createElementBlock("div", _hoisted_7, [
        createBaseVNode("p", _hoisted_8, toDisplayString($setup.error), 1),
        $setup.isJoined ? (openBlock(), createElementBlock("button", {
          key: 0,
          type: "button",
          class: "cx-btn secondary sm",
          disabled: $setup.loadingResources,
          onClick: $setup.loadResources
        }, toDisplayString($setup.t("chaoxing.class.retryLoad")), 9, _hoisted_9)) : createCommentVNode("", true)
      ])) : createCommentVNode("", true),
      $setup.isJoined ? (openBlock(), createElementBlock(Fragment, { key: 2 }, [
        createBaseVNode("header", _hoisted_10, [
          createBaseVNode("div", _hoisted_11, [
            createBaseVNode("div", _hoisted_12, [
              createBaseVNode("h2", _hoisted_13, [
                createTextVNode(toDisplayString($setup.courseTitle) + " ", 1),
                _cache[8] || (_cache[8] = createBaseVNode("span", { class: "material-symbols-outlined cx-drop" }, "arrow_drop_down", -1))
              ]),
              $setup.teacherName ? (openBlock(), createElementBlock("p", _hoisted_14, toDisplayString($setup.teacherName) + " · " + toDisplayString($setup.statusMsg || $setup.tFmt("chaoxing.class.itemsUnit", { n: $setup.resourceCount })), 1)) : createCommentVNode("", true)
            ])
          ]),
          createBaseVNode("div", {
            class: "cx-chips",
            role: "tablist",
            "aria-label": $setup.t("chaoxing.class.filterAria")
          }, [
            (openBlock(true), createElementBlock(Fragment, null, renderList($setup.filterChips, (chip) => {
              return openBlock(), createElementBlock("button", {
                key: chip.id,
                type: "button",
                role: "tab",
                class: normalizeClass(["cx-chip", { active: $setup.filterChip === chip.id }]),
                "aria-selected": $setup.filterChip === chip.id,
                onClick: ($event) => $setup.filterChip = chip.id
              }, toDisplayString(chip.label), 11, _hoisted_16);
            }), 128))
          ], 8, _hoisted_15),
          createBaseVNode("div", _hoisted_17, [
            createBaseVNode("span", _hoisted_18, [
              createTextVNode(toDisplayString($setup.currentFolder?.name || $setup.t("chaoxing.class.smartSort")) + " ", 1),
              _cache[9] || (_cache[9] = createBaseVNode("span", { class: "material-symbols-outlined" }, "expand_more", -1))
            ]),
            createBaseVNode("span", _hoisted_19, toDisplayString($setup.tFmt("chaoxing.class.itemsUnit", { n: $setup.filteredResources.length })), 1)
          ]),
          $setup.folderStack.length ? (openBlock(), createElementBlock("nav", {
            key: 0,
            class: "cx-breadcrumb",
            "aria-label": $setup.t("chaoxing.class.breadcrumbAria")
          }, [
            (openBlock(true), createElementBlock(Fragment, null, renderList($setup.breadcrumbLabels, (label, idx) => {
              return openBlock(), createElementBlock("button", {
                key: `${idx}-${label}`,
                type: "button",
                class: normalizeClass(["cx-crumb", { current: idx === $setup.breadcrumbLabels.length - 1 }]),
                disabled: idx === $setup.breadcrumbLabels.length - 1 || $setup.loadingResources,
                onClick: ($event) => $setup.handleBreadcrumb(idx)
              }, [
                createTextVNode(toDisplayString(label) + " ", 1),
                idx < $setup.breadcrumbLabels.length - 1 ? (openBlock(), createElementBlock("span", _hoisted_22, "chevron_right")) : createCommentVNode("", true)
              ], 10, _hoisted_21);
            }), 128))
          ], 8, _hoisted_20)) : createCommentVNode("", true)
        ]),
        createBaseVNode("main", _hoisted_23, [
          $setup.loadingResources ? (openBlock(), createElementBlock("div", _hoisted_24, [
            (openBlock(), createElementBlock(Fragment, null, renderList(5, (n) => {
              return createBaseVNode("div", {
                key: n,
                class: "cx-skeleton-row"
              });
            }), 64))
          ])) : !$setup.filteredResources.length ? (openBlock(), createElementBlock("div", _hoisted_25, [
            _cache[10] || (_cache[10] = createBaseVNode("span", { class: "material-symbols-outlined" }, "folder_off", -1)),
            createBaseVNode("p", null, toDisplayString($setup.t("chaoxing.class.noResources")), 1),
            createBaseVNode("p", _hoisted_26, toDisplayString($setup.filterChip !== "all" ? $setup.t("chaoxing.class.emptyFiltered") : $setup.currentFolder ? $setup.t("chaoxing.class.emptyFolder") : $setup.t("chaoxing.class.emptyWaitUpload")), 1),
            $setup.folderStack.length ? (openBlock(), createElementBlock("button", {
              key: 0,
              type: "button",
              class: "cx-btn secondary",
              onClick: _cache[2] || (_cache[2] = ($event) => $setup.handleBreadcrumb($setup.folderStack.length - 1))
            }, toDisplayString($setup.t("chaoxing.class.goUp")), 1)) : createCommentVNode("", true)
          ])) : (openBlock(true), createElementBlock(Fragment, { key: 2 }, renderList($setup.filteredResources, (item) => {
            return openBlock(), createElementBlock("article", {
              key: item.data_id || item.name + item.folder_kind,
              class: normalizeClass(["cx-row", { folder: item.is_folder }]),
              role: "button",
              tabindex: "0",
              onClick: ($event) => $setup.handleRowClick(item),
              onKeydown: withKeys(withModifiers(($event) => $setup.handleRowClick(item), ["prevent"]), ["enter"])
            }, [
              createBaseVNode("div", {
                class: "cx-thumb",
                "data-kind": $setup.fileKind(item)
              }, [
                $setup.showThumb(item) ? (openBlock(), createElementBlock("img", {
                  key: 0,
                  class: "cx-thumb-img",
                  src: item.thumbnail_url,
                  alt: item.name,
                  loading: "lazy",
                  referrerpolicy: "no-referrer",
                  onError: ($event) => $setup.onThumbError(item)
                }, null, 40, _hoisted_29)) : (openBlock(), createElementBlock("span", _hoisted_30, toDisplayString($setup.kindMeta[$setup.fileKind(item)].icon), 1))
              ], 8, _hoisted_28),
              createBaseVNode("div", _hoisted_31, [
                createBaseVNode("h3", {
                  class: "cx-row-name",
                  title: item.name
                }, toDisplayString(item.name), 9, _hoisted_32),
                $setup.metaLine(item) ? (openBlock(), createElementBlock("p", _hoisted_33, [
                  $setup.fileKind(item) === "image" || $setup.fileKind(item) === "video" ? (openBlock(), createElementBlock("span", _hoisted_34, toDisplayString($setup.kindMeta[$setup.fileKind(item)].label), 1)) : createCommentVNode("", true),
                  createTextVNode(" " + toDisplayString($setup.metaLine(item)), 1)
                ])) : item.is_folder ? (openBlock(), createElementBlock("p", _hoisted_35, toDisplayString($setup.kindMeta[$setup.fileKind(item)].label), 1)) : createCommentVNode("", true)
              ]),
              createBaseVNode("div", {
                class: "cx-row-trail",
                onClick: _cache[3] || (_cache[3] = withModifiers(() => {
                }, ["stop"]))
              }, [
                item.is_folder ? (openBlock(), createElementBlock("span", _hoisted_36, "chevron_right")) : (openBlock(), createElementBlock("button", {
                  key: 1,
                  type: "button",
                  class: "cx-trail-btn",
                  disabled: !!$setup.actingId,
                  title: $setup.t("chaoxing.class.downloadTitle"),
                  onClick: ($event) => $setup.handleDownloadResource(item)
                }, [
                  createBaseVNode("span", _hoisted_38, toDisplayString($setup.actingId === `d-${item.data_id}` ? "progress_activity" : "download"), 1)
                ], 8, _hoisted_37))
              ])
            ], 42, _hoisted_27);
          }), 128)),
          createBaseVNode("p", _hoisted_39, [
            _cache[11] || (_cache[11] = createBaseVNode("span", { class: "material-symbols-outlined" }, "verified_user", -1)),
            createTextVNode(" " + toDisplayString($setup.t("chaoxing.class.secureNote")), 1)
          ])
        ])
      ], 64)) : (openBlock(), createElementBlock("section", _hoisted_40, [
        createBaseVNode("div", _hoisted_41, [
          createBaseVNode("div", _hoisted_42, toDisplayString($setup.t("chaoxing.class.welcomeBadge")), 1),
          $setup.coverUrl ? (openBlock(), createElementBlock("div", _hoisted_43, [
            createBaseVNode("img", {
              src: $setup.coverUrl,
              alt: ""
            }, null, 8, _hoisted_44)
          ])) : (openBlock(), createElementBlock("div", _hoisted_45, [..._cache[12] || (_cache[12] = [
            createBaseVNode("span", { class: "material-symbols-outlined fill" }, "cloud", -1)
          ])])),
          createBaseVNode("h2", null, toDisplayString($setup.courseTitle || $setup.t("chaoxing.class.welcomeTitleFallback")), 1),
          $setup.teacherName ? (openBlock(), createElementBlock("p", _hoisted_46, toDisplayString($setup.tFmt("chaoxing.class.welcomeTeacher", { name: $setup.teacherName })), 1)) : createCommentVNode("", true),
          createBaseVNode("p", _hoisted_47, toDisplayString($setup.t("chaoxing.class.welcomeDesc")), 1),
          createBaseVNode("div", _hoisted_48, [
            createBaseVNode("button", {
              type: "button",
              class: "cx-btn primary lg",
              onClick: $setup.reopenJoinDialog
            }, toDisplayString($setup.needsRejoin ? $setup.t("chaoxing.class.rejoin") : $setup.t("chaoxing.class.joinAndView")), 1)
          ]),
          $setup.needsRejoin ? (openBlock(), createElementBlock("p", _hoisted_49, toDisplayString($setup.statusMsg || $setup.t("chaoxing.class.rejoinNote")), 1)) : $setup.joinDeclined ? (openBlock(), createElementBlock("p", _hoisted_50, toDisplayString($setup.t("chaoxing.class.declinedNote")), 1)) : createCommentVNode("", true)
        ])
      ]))
    ], 64)),
    (openBlock(), createBlock(Teleport, { to: "body" }, [
      $setup.showPreviewModal ? (openBlock(), createElementBlock("div", {
        key: 0,
        class: "cx-preview-root",
        role: "dialog",
        "aria-modal": "true",
        "aria-label": $setup.previewModalTitle || $setup.t("chaoxing.class.previewKicker")
      }, [
        createBaseVNode("div", {
          class: "cx-preview-backdrop",
          onClick: $setup.closePreviewModal
        }),
        createBaseVNode("div", _hoisted_52, [
          createBaseVNode("header", _hoisted_53, [
            createBaseVNode("div", _hoisted_54, [
              createBaseVNode("p", _hoisted_55, toDisplayString($setup.previewModalOfficial ? $setup.t("chaoxing.class.previewKickerOfficial") : $setup.t("chaoxing.class.previewKicker")), 1),
              createBaseVNode("h3", _hoisted_56, toDisplayString($setup.previewModalTitle), 1),
              $setup.currentOpenMethod ? (openBlock(), createElementBlock("p", _hoisted_57, toDisplayString($setup.tFmt("chaoxing.class.currentMethod", { name: $setup.currentOpenMethod.label })), 1)) : createCommentVNode("", true)
            ]),
            createBaseVNode("div", _hoisted_58, [
              createBaseVNode("button", {
                type: "button",
                class: "cx-icon-btn",
                "aria-label": $setup.t("chaoxing.class.close"),
                onClick: $setup.closePreviewModal
              }, [..._cache[13] || (_cache[13] = [
                createBaseVNode("span", { class: "material-symbols-outlined" }, "close", -1)
              ])], 8, _hoisted_59)
            ])
          ]),
          createBaseVNode("div", {
            class: "cx-preview-body",
            onClick: _cache[4] || (_cache[4] = ($event) => $setup.showOpenMethodMenu = false)
          }, [
            $setup.previewModalLoading ? (openBlock(), createElementBlock("div", _hoisted_60, [
              _cache[14] || (_cache[14] = createBaseVNode("div", { class: "cx-spinner" }, null, -1)),
              createBaseVNode("p", null, toDisplayString($setup.t("chaoxing.class.loadingPreview")), 1)
            ])) : $setup.previewModalError && !$setup.previewModalUrl ? (openBlock(), createElementBlock("div", _hoisted_61, [
              _cache[15] || (_cache[15] = createBaseVNode("span", { class: "material-symbols-outlined" }, "error", -1)),
              createBaseVNode("p", null, toDisplayString($setup.previewModalError), 1),
              createBaseVNode("div", _hoisted_62, [
                createBaseVNode("button", {
                  type: "button",
                  class: "cx-btn secondary",
                  onClick: withModifiers($setup.toggleOpenMethodMenu, ["stop"])
                }, toDisplayString($setup.t("chaoxing.class.switchMethod")), 1),
                $setup.previewDownloadUrl || $setup.previewOpenMethods.some((m) => m.kind === "download") ? (openBlock(), createElementBlock("button", {
                  key: 0,
                  type: "button",
                  class: "cx-btn primary",
                  disabled: $setup.previewDownloading,
                  onClick: withModifiers($setup.handlePreviewDownload, ["stop"])
                }, toDisplayString($setup.previewDownloading ? $setup.t("chaoxing.class.downloading") : $setup.t("chaoxing.class.openDownload")), 9, _hoisted_63)) : createCommentVNode("", true)
              ])
            ])) : (openBlock(), createElementBlock(Fragment, { key: 2 }, [
              $setup.previewModalError ? (openBlock(), createElementBlock("p", _hoisted_64, toDisplayString($setup.previewModalError), 1)) : createCommentVNode("", true),
              $setup.previewModalMode === "image" && $setup.previewModalUrl ? (openBlock(), createElementBlock("div", _hoisted_65, [
                createBaseVNode("img", {
                  class: "cx-preview-image",
                  src: $setup.previewModalUrl,
                  alt: $setup.previewModalTitle,
                  referrerpolicy: "no-referrer",
                  onError: $setup.onPreviewImageError
                }, null, 40, _hoisted_66)
              ])) : $setup.previewModalMode === "video" && $setup.previewModalUrl ? (openBlock(), createElementBlock("video", {
                key: 2,
                class: "cx-preview-video",
                src: $setup.previewModalUrl,
                controls: "",
                playsinline: ""
              }, null, 8, _hoisted_67)) : $setup.previewModalUrl ? (openBlock(), createElementBlock("iframe", {
                key: 3,
                class: "cx-preview-frame",
                src: $setup.previewModalUrl,
                title: $setup.t("chaoxing.class.previewKicker"),
                allow: "fullscreen; autoplay",
                referrerpolicy: "no-referrer-when-downgrade"
              }, null, 8, _hoisted_68)) : (openBlock(), createElementBlock("div", _hoisted_69, [
                _cache[16] || (_cache[16] = createBaseVNode("span", { class: "material-symbols-outlined" }, "visibility_off", -1)),
                createBaseVNode("p", null, toDisplayString($setup.t("chaoxing.class.noEmbedContent")), 1),
                createBaseVNode("p", _hoisted_70, toDisplayString($setup.t("chaoxing.class.noEmbedContentSub")), 1)
              ]))
            ], 64))
          ]),
          !$setup.previewModalLoading ? (openBlock(), createElementBlock("footer", _hoisted_71, [
            createBaseVNode("div", _hoisted_72, [
              createBaseVNode("div", _hoisted_73, [
                createBaseVNode("button", {
                  type: "button",
                  class: normalizeClass(["cx-toolbar-btn method", { open: $setup.showOpenMethodMenu }]),
                  disabled: !$setup.previewOpenMethods.length,
                  onClick: withModifiers($setup.toggleOpenMethodMenu, ["stop"])
                }, [
                  _cache[17] || (_cache[17] = createBaseVNode("span", { class: "material-symbols-outlined" }, "swap_horiz", -1)),
                  createBaseVNode("span", _hoisted_75, [
                    createBaseVNode("span", _hoisted_76, toDisplayString($setup.t("chaoxing.class.switchMethod")), 1),
                    createBaseVNode("span", _hoisted_77, toDisplayString($setup.currentOpenMethod?.label || $setup.t("chaoxing.class.chooseMethod")), 1)
                  ]),
                  createBaseVNode("span", _hoisted_78, toDisplayString($setup.showOpenMethodMenu ? "expand_less" : "expand_more"), 1)
                ], 10, _hoisted_74),
                $setup.showOpenMethodMenu ? (openBlock(), createElementBlock("div", {
                  key: 0,
                  class: "cx-open-method-menu",
                  role: "menu",
                  onClick: _cache[5] || (_cache[5] = withModifiers(() => {
                  }, ["stop"]))
                }, [
                  createBaseVNode("p", _hoisted_79, toDisplayString($setup.t("chaoxing.class.chooseMethodTitle")), 1),
                  (openBlock(true), createElementBlock(Fragment, null, renderList($setup.previewOpenMethods, (m) => {
                    return openBlock(), createElementBlock("button", {
                      key: m.id,
                      type: "button",
                      class: normalizeClass(["cx-open-method-item", { active: m.id === $setup.previewMethodId }]),
                      role: "menuitem",
                      onClick: ($event) => $setup.applyOpenMethod(m)
                    }, [
                      createBaseVNode("span", _hoisted_81, toDisplayString(m.icon), 1),
                      createBaseVNode("span", _hoisted_82, [
                        createBaseVNode("span", _hoisted_83, toDisplayString(m.label), 1),
                        createBaseVNode("span", _hoisted_84, toDisplayString(m.desc), 1)
                      ]),
                      m.id === $setup.previewMethodId ? (openBlock(), createElementBlock("span", _hoisted_85, "check")) : createCommentVNode("", true)
                    ], 10, _hoisted_80);
                  }), 128))
                ])) : createCommentVNode("", true)
              ]),
              createBaseVNode("button", {
                type: "button",
                class: "cx-toolbar-btn browser",
                disabled: $setup.previewModalLoading,
                title: $setup.t("chaoxing.class.openBrowser"),
                onClick: $setup.handleBrowserOpenCurrent
              }, [
                _cache[18] || (_cache[18] = createBaseVNode("span", { class: "material-symbols-outlined" }, "open_in_browser", -1)),
                createBaseVNode("span", _hoisted_87, toDisplayString($setup.t("chaoxing.class.browserShort")), 1)
              ], 8, _hoisted_86),
              createBaseVNode("button", {
                type: "button",
                class: "cx-toolbar-btn download",
                disabled: $setup.previewDownloading || !$setup.previewDownloadUrl && !$setup.previewItem,
                onClick: $setup.handlePreviewDownload
              }, [
                createBaseVNode("span", _hoisted_89, toDisplayString($setup.previewDownloading ? "progress_activity" : "download"), 1),
                createBaseVNode("span", _hoisted_90, toDisplayString($setup.previewDownloading ? $setup.t("chaoxing.class.downloadingShort") : $setup.t("chaoxing.class.openDownload")), 1)
              ], 8, _hoisted_88)
            ])
          ])) : createCommentVNode("", true)
        ])
      ], 8, _hoisted_51)) : createCommentVNode("", true)
    ])),
    (openBlock(), createBlock(Teleport, { to: "body" }, [
      $setup.showJoinDialog ? (openBlock(), createElementBlock("div", _hoisted_91, [
        createBaseVNode("div", {
          class: "cx-dialog-backdrop",
          onClick: $setup.markDeclined
        }),
        createBaseVNode("div", _hoisted_92, [
          createBaseVNode("div", {
            class: normalizeClass(["cx-dialog-cover", { empty: !$setup.coverUrl }])
          }, [
            $setup.coverUrl ? (openBlock(), createElementBlock("img", {
              key: 0,
              src: $setup.coverUrl,
              alt: ""
            }, null, 8, _hoisted_93)) : (openBlock(), createElementBlock("span", _hoisted_94, "menu_book"))
          ], 2),
          createBaseVNode("p", _hoisted_95, toDisplayString($setup.needsRejoin ? $setup.t("chaoxing.class.joinKickerRejoin") : $setup.t("chaoxing.class.joinKicker")), 1),
          createBaseVNode("h3", _hoisted_96, toDisplayString($setup.t("chaoxing.class.joinQuestion")), 1),
          createBaseVNode("p", _hoisted_97, toDisplayString($setup.courseTitle || $setup.t("chaoxing.class.fallbackShort")), 1),
          $setup.teacherName ? (openBlock(), createElementBlock("p", _hoisted_98, toDisplayString($setup.tFmt("chaoxing.class.joinTeacher", { name: $setup.teacherName })), 1)) : createCommentVNode("", true),
          createBaseVNode("p", _hoisted_99, toDisplayString($setup.t("chaoxing.class.joinDesc")), 1),
          createBaseVNode("div", _hoisted_100, [
            createBaseVNode("button", {
              type: "button",
              class: "cx-btn dialog-secondary",
              disabled: $setup.loadingJoin,
              onClick: $setup.markDeclined
            }, toDisplayString($setup.t("chaoxing.class.joinDecline")), 9, _hoisted_101),
            createBaseVNode("button", {
              type: "button",
              class: "cx-btn dialog-primary",
              disabled: $setup.loadingJoin,
              onClick: $setup.handleJoinConfirm
            }, toDisplayString($setup.loadingJoin ? $setup.t("chaoxing.class.joining") : $setup.t("chaoxing.class.joinBtn")), 9, _hoisted_102)
          ])
        ])
      ])) : createCommentVNode("", true)
    ]))
  ]);
}
const LAST_CLASS_KEY = "hbu_chaoxing_class_last_v1";
const JOIN_DECLINED_KEY = "hbu_chaoxing_class_declined_v1";
const _sfc_main = {
  __name: "ChaoxingClassView",
  props: {
    studentId: { type: String, default: "" }
  },
  emits: ["back"],
  setup(__props, { expose: __expose, emit: __emit }) {
    __expose();
    const { t } = useLocale();
    const tFmt = (key, params = {}) => Object.entries(params).reduce(
      (acc, [k, v]) => acc.replaceAll(`{${k}}`, String(v)),
      t(key)
    );
    const classConfig = ref(getChaoxingClassConfig());
    const inviteCode = computed(() => String(classConfig.value?.invite_code || "").trim());
    const classMeta = computed(() => {
      const c = classConfig.value || {};
      return {
        invite_code: String(c.invite_code || "").trim(),
        course_id: String(c.course_id || "").trim(),
        clazz_id: String(c.clazz_id || "").trim(),
        course_name: String(c.course_name || "").trim(),
        teacher_name: String(c.teacher_name || "").trim(),
        cover_url: "",
        cpi: String(c.cpi || "0").trim() || "0"
      };
    });
    const defaultCpi = () => classMeta.value.cpi || "0";
    const props = __props;
    const emit = __emit;
    const loadingSso = ref(false);
    const loadingBoot = ref(true);
    const loadingJoin = ref(false);
    const loadingResources = ref(false);
    const actingId = ref("");
    const ssoReady = ref(false);
    const ssoHint = ref("");
    const error = ref("");
    const statusMsg = ref("");
    const preview = ref(null);
    const resources = ref([]);
    const activeClass = ref(null);
    const showJoinDialog = ref(false);
    const joinDeclined = ref(false);
    const needsRejoin = ref(false);
    const bootPhase = ref("init");
    const folderStack = ref([]);
    const showPreviewModal = ref(false);
    const previewModalTitle = ref("");
    const previewModalUrl = ref("");
    const previewModalLoading = ref(false);
    const previewModalError = ref("");
    const previewModalOfficial = ref(false);
    const previewDownloadUrl = ref("");
    const previewModalMode = ref("iframe");
    const previewCandidates = ref([]);
    const previewCandidateIdx = ref(0);
    const previewItem = ref(null);
    const previewOfficialUrl = ref("");
    const previewOpenMethods = ref([]);
    const previewMethodId = ref("");
    const showOpenMethodMenu = ref(false);
    const previewDownloading = ref(false);
    const filterChip = ref("all");
    const thumbFailed = ref({});
    let loadSeq = 0;
    let suppressNotJoinedUntil = 0;
    const hasTauri = isTauriRuntime();
    const courseTitle = computed(() => {
      const p = activeClass.value || preview.value;
      return String(p?.course_name || p?.courseName || t("chaoxing.class.fallbackTitle")).trim();
    });
    const teacherName = computed(() => {
      const p = activeClass.value || preview.value;
      return String(p?.teacher_name || p?.teacherName || "").trim();
    });
    const coverUrl = computed(() => {
      const p = activeClass.value || preview.value;
      const raw = String(p?.cover_url || p?.coverUrl || "").trim();
      if (!raw) return "";
      if (raw.startsWith("//")) return `https:${raw}`;
      if (raw.startsWith("http://")) return `https://${raw.slice(7)}`;
      return raw;
    });
    const resourceCount = computed(() => resources.value.length);
    const isJoined = computed(() => !!(activeClass.value?.course_id && activeClass.value?.clazz_id));
    const formatErr = (e) => {
      if (!e) return t("chaoxing.class.unknownError");
      if (typeof e === "string") return e;
      const msg = e?.message || e?.error || String(e);
      return String(msg || t("chaoxing.class.unknownError"));
    };
    const studentPayload = () => {
      const sid = String(props.studentId || "").trim();
      return sid ? { student_id: sid } : { student_id: null };
    };
    const ssoPayload = async () => {
      const base = studentPayload();
      const sid = String(props.studentId || "").trim();
      if (!sid) return { ...base, portal_password: null };
      try {
        const pwd = String(await loadPortalRememberedPassword(sid) || "").trim();
        return { ...base, portal_password: pwd || null };
      } catch {
        return { ...base, portal_password: null };
      }
    };
    const loadLastClass = () => {
      try {
        const raw = localStorage.getItem(LAST_CLASS_KEY);
        if (!raw) return null;
        const parsed = JSON.parse(raw);
        if (parsed?.course_id && parsed?.clazz_id) {
          const savedInvite = String(parsed.invite_code || "").trim();
          if (inviteCode.value && savedInvite !== inviteCode.value) {
            clearLastClassStorageOnly();
            return null;
          }
          activeClass.value = {
            invite_code: savedInvite || inviteCode.value,
            course_id: String(parsed.course_id),
            clazz_id: String(parsed.clazz_id),
            course_name: String(parsed.course_name || ""),
            teacher_name: String(parsed.teacher_name || ""),
            cover_url: String(parsed.cover_url || ""),
            cpi: String(parsed.cpi || defaultCpi())
          };
          return activeClass.value;
        }
      } catch {
      }
      return null;
    };
    const clearLastClassStorageOnly = () => {
      try {
        localStorage.removeItem(LAST_CLASS_KEY);
      } catch {
      }
    };
    const clearLastClass = () => {
      clearLastClassStorageOnly();
      activeClass.value = null;
      resources.value = [];
      folderStack.value = [];
    };
    const saveLastClass = (cls) => {
      try {
        localStorage.setItem(LAST_CLASS_KEY, JSON.stringify(cls));
        localStorage.removeItem(JOIN_DECLINED_KEY);
      } catch {
      }
    };
    const loadDeclined = () => {
      try {
        joinDeclined.value = localStorage.getItem(JOIN_DECLINED_KEY) === inviteCode.value;
      } catch {
        joinDeclined.value = false;
      }
    };
    const markDeclined = () => {
      joinDeclined.value = true;
      showJoinDialog.value = false;
      try {
        localStorage.setItem(JOIN_DECLINED_KEY, inviteCode.value);
      } catch {
      }
    };
    const enterNotJoinedState = async ({ openDialog = false, reason = "", rejoin = false } = {}) => {
      clearLastClass();
      needsRejoin.value = !!rejoin;
      error.value = "";
      statusMsg.value = rejoin ? reason || t("chaoxing.class.rejoinMsg") : "";
      preview.value = { ...classMeta.value };
      bootPhase.value = "ready";
      loadingBoot.value = false;
      if (openDialog && !joinDeclined.value) {
        try {
          void fetchPreview().catch(() => {
          });
          showJoinDialog.value = true;
        } catch {
          showJoinDialog.value = true;
        }
      }
    };
    const isNotJoinedSignal = (resOrMsg) => {
      if (Date.now() < suppressNotJoinedUntil) return false;
      if (resOrMsg && typeof resOrMsg === "object") {
        const m = String(resOrMsg.membership || "").toLowerCase();
        const role = String(resOrMsg.role || "").toLowerCase();
        const ut = String(resOrMsg.ut || "").toLowerCase();
        if (m === "ok" || role === "teacher" || ut === "t") return false;
        if (m === "not_joined" || m === "not-joined") return true;
        if (resOrMsg.enrolled === false || resOrMsg.enrolled === 0 || resOrMsg.enrolled === "false") {
          return true;
        }
      }
      const msg = typeof resOrMsg === "string" ? resOrMsg : formatErr(resOrMsg);
      return /未加入|不在该班|无权限|请先加入|不是该班|未选课|已退课|退班|无权访问/.test(String(msg || ""));
    };
    const ensureSso = async () => {
      loadingSso.value = true;
      if (bootPhase.value === "init" || bootPhase.value === "sso") {
        bootPhase.value = "sso";
      }
      ssoHint.value = t("chaoxing.class.ssoConnecting");
      try {
        if (!hasTauri) {
          throw new Error(t("chaoxing.class.clientOnly"));
        }
        const req = await ssoPayload();
        const res = await invokeNative("chaoxing_class_ensure_sso", { req });
        ssoReady.value = !!(res?.success ?? res?.sso);
        ssoHint.value = ssoReady.value ? res?.partial ? t("chaoxing.class.ssoPartial") : res?.from_cache || res?.cookie_reuse ? t("chaoxing.class.ssoReused") : res?.silent_relogin ? t("chaoxing.class.ssoRelogin") : t("chaoxing.class.ssoOk") : t("chaoxing.class.ssoNotReady");
        return ssoReady.value;
      } catch (e) {
        ssoReady.value = false;
        const msg = formatErr(e);
        ssoHint.value = msg;
        error.value = msg.includes("过期") || msg.includes("登录") || msg.includes("密码") ? `${msg}${t("chaoxing.class.ssoErrorPortalOk")}` : msg;
        return false;
      } finally {
        loadingSso.value = false;
      }
    };
    const fetchPreview = async () => {
      bootPhase.value = "preview";
      const code = inviteCode.value;
      if (!code) throw new Error(t("chaoxing.class.inviteNotConfigured"));
      const ssoReq = await ssoPayload();
      try {
        const res = await invokeNative("chaoxing_class_preview_invite", {
          req: {
            invite_code: code,
            student_id: ssoReq.student_id ?? null,
            portal_password: ssoReq.portal_password ?? null
          }
        });
        preview.value = {
          invite_code: code,
          course_id: String(res.course_id || classMeta.value.course_id || ""),
          clazz_id: String(res.clazz_id || classMeta.value.clazz_id || ""),
          course_name: String(res.course_name || classMeta.value.course_name || t("chaoxing.class.fallbackShort")),
          teacher_name: String(res.teacher_name || classMeta.value.teacher_name || ""),
          cover_url: String(res.cover_url || ""),
          cpi: String(res.cpi || defaultCpi())
        };
        return preview.value;
      } catch (e) {
        const msg = formatErr(e);
        pushDebugLog("ChaoxingInvite", msg, "error", {
          invite_code_len: code.length,
          student_id: ssoReq.student_id || null,
          has_portal_password: !!ssoReq.portal_password
        });
        throw e;
      }
    };
    const sleepMs = (ms) => new Promise((r) => setTimeout(r, ms));
    const refreshAfterJoin = async (cls, statusText = "") => {
      const joinedText = statusText || t("chaoxing.class.joinSuccess");
      joinDeclined.value = false;
      needsRejoin.value = false;
      showJoinDialog.value = false;
      folderStack.value = [];
      resources.value = [];
      filterChip.value = "all";
      error.value = "";
      preview.value = cls;
      activeClass.value = cls;
      saveLastClass(cls);
      bootPhase.value = "ready";
      loadingBoot.value = false;
      statusMsg.value = t("chaoxing.class.syncMaterials");
      suppressNotJoinedUntil = Date.now() + 2e4;
      const gapsMs = [0, 500, 1e3, 1600, 2500, 3500];
      for (let i = 0; i < gapsMs.length; i++) {
        if (gapsMs[i] > 0) {
          statusMsg.value = tFmt("chaoxing.class.refreshingMaterials", { i, n: gapsMs.length - 1 });
          await sleepMs(gapsMs[i]);
        }
        if (!isJoined.value && cls?.course_id && cls?.clazz_id) {
          needsRejoin.value = false;
          activeClass.value = cls;
          preview.value = cls;
          saveLastClass(cls);
          bootPhase.value = "ready";
        }
        try {
          await loadResources();
        } catch {
        }
        if (resources.value.length > 0) {
          break;
        }
      }
      if (!isJoined.value && cls?.course_id && cls?.clazz_id) {
        needsRejoin.value = false;
        activeClass.value = cls;
        preview.value = cls;
        saveLastClass(cls);
        bootPhase.value = "ready";
        try {
          await loadResources();
        } catch {
        }
      }
      if (isJoined.value) {
        needsRejoin.value = false;
        error.value = "";
        statusMsg.value = resources.value.length ? tFmt("chaoxing.class.itemsTotal", { n: resources.value.length }) : joinedText || t("chaoxing.class.alreadyInClassMsg");
      }
    };
    const handleJoinConfirm = async () => {
      loadingJoin.value = true;
      error.value = "";
      statusMsg.value = "";
      try {
        const code = inviteCode.value;
        if (!code) throw new Error(t("chaoxing.class.inviteNotConfigured"));
        const ssoReq = await ssoPayload();
        const res = await invokeNative("chaoxing_class_accept_invite", {
          req: {
            invite_code: code,
            student_id: ssoReq.student_id ?? null,
            portal_password: ssoReq.portal_password ?? null
          }
        });
        const p = res?.preview || preview.value || {};
        const cls = {
          invite_code: code,
          course_id: String(p.course_id || preview.value?.course_id || classMeta.value.course_id || ""),
          clazz_id: String(p.clazz_id || preview.value?.clazz_id || classMeta.value.clazz_id || ""),
          course_name: String(p.course_name || preview.value?.course_name || classMeta.value.course_name || ""),
          teacher_name: String(
            p.teacher_name || preview.value?.teacher_name || classMeta.value.teacher_name || ""
          ),
          cover_url: String(p.cover_url || preview.value?.cover_url || ""),
          cpi: String(p.cpi || preview.value?.cpi || defaultCpi())
        };
        if (!cls.course_id || !cls.clazz_id) {
          throw new Error(t("chaoxing.class.joinNoCourseInfo"));
        }
        await refreshAfterJoin(
          cls,
          res?.already_joined ? t("chaoxing.class.alreadyInClass") : t("chaoxing.class.joinSuccess")
        );
      } catch (e) {
        const msg = formatErr(e);
        if (msg.includes("已") && (msg.includes("加入") || msg.includes("在"))) {
          const fallback = preview.value?.course_id ? {
            ...preview.value,
            invite_code: inviteCode.value,
            cpi: String(preview.value.cpi || defaultCpi())
          } : classMeta.value.course_id ? { ...classMeta.value } : null;
          if (fallback?.course_id && fallback?.clazz_id) {
            await refreshAfterJoin(fallback, t("chaoxing.class.alreadyInClass"));
            return;
          }
        }
        error.value = msg;
      } finally {
        loadingJoin.value = false;
      }
    };
    const reopenJoinDialog = async () => {
      error.value = "";
      joinDeclined.value = false;
      try {
        localStorage.removeItem(JOIN_DECLINED_KEY);
      } catch {
      }
      clearLastClass();
      preview.value = { ...classMeta.value };
      try {
        if (!preview.value?.course_id) await fetchPreview();
        showJoinDialog.value = true;
      } catch (e) {
        preview.value = { ...classMeta.value };
        showJoinDialog.value = true;
        error.value = formatErr(e);
      }
    };
    const currentFolder = computed(
      () => folderStack.value.length ? folderStack.value[folderStack.value.length - 1] : null
    );
    const breadcrumbLabels = computed(() => {
      const base = [t("chaoxing.class.breadcrumbRoot")];
      return base.concat(folderStack.value.map((f) => f.name || t("chaoxing.class.folder")));
    });
    const mapResourceItem = (item) => {
      const name = String(item.name || t("chaoxing.class.unnamed"));
      const file_type = String(item.file_type || item.fileType || "");
      const object_id = String(item.object_id || item.objectId || "");
      let thumbnail_url = String(item.thumbnail_url || item.thumbnailUrl || "");
      if (!thumbnail_url && object_id) {
        const t2 = `${file_type} ${name}`.toLowerCase();
        if (/\b(jpg|jpeg|png|gif|webp|bmp|heic)\b/.test(t2)) {
          thumbnail_url = `https://p.ananas.chaoxing.com/star3/150_150c/${object_id}`;
        }
      }
      return {
        data_id: String(item.data_id || item.dataId || ""),
        name,
        file_type,
        object_id,
        size_label: String(item.size_label || item.sizeLabel || "-"),
        creator: String(item.creator || ""),
        created_at: String(item.created_at || item.createdAt || ""),
        is_folder: !!(item.is_folder ?? item.isFolder),
        folder_kind: String(item.folder_kind || item.folderKind || ""),
        download_url: String(item.download_url || item.downloadUrl || ""),
        preview_cdn_url: String(item.preview_cdn_url || item.previewCdnUrl || ""),
        thumbnail_url,
        is_downloadable: !!(item.is_downloadable ?? item.isDownloadable ?? true)
      };
    };
    const thumbKey = (item) => item.data_id || item.object_id || item.name;
    const onThumbError = (item) => {
      thumbFailed.value = { ...thumbFailed.value, [thumbKey(item)]: true };
    };
    const showThumb = (item) => !!(item.thumbnail_url && !thumbFailed.value[thumbKey(item)] && fileKind(item) === "image");
    const isTransientListError = (msg) => {
      const m = String(msg || "");
      return m.includes("网络失败") || m.includes("连接") || m.includes("超时") || m.includes("error sending") || m.includes("timed out") || m.includes("connection");
    };
    const loadResources = async (opts = {}) => {
      const rejoinOnNotJoined = opts.rejoinOnNotJoined === true;
      const cls = activeClass.value || preview.value;
      if (!cls?.course_id || !cls?.clazz_id) {
        error.value = t("chaoxing.class.notJoinedYet");
        return;
      }
      const seq = ++loadSeq;
      const folderSnap = currentFolder.value ? { ...currentFolder.value } : null;
      loadingResources.value = true;
      error.value = "";
      const invokeOnce = () => invokeNative("chaoxing_class_list_resources", {
        req: {
          course_id: cls.course_id,
          clazz_id: cls.clazz_id,
          cpi: cls.cpi || defaultCpi(),
          parent_data_id: folderSnap?.parent_data_id || null,
          data_name: folderSnap?.data_name || null,
          parent_chain: folderSnap?.parent_chain || null,
          folder_kind: folderSnap?.folder_kind || null,
          ...studentPayload()
        }
      });
      const handleNotJoined = async () => {
        if (folderSnap) return false;
        await enterNotJoinedState({
          openDialog: rejoinOnNotJoined || !joinDeclined.value,
          rejoin: rejoinOnNotJoined,
          reason: rejoinOnNotJoined ? t("chaoxing.class.rejoinMsg") : ""
        });
        return true;
      };
      try {
        let res;
        try {
          res = await invokeOnce();
        } catch (e1) {
          const msg1 = formatErr(e1);
          if (seq !== loadSeq) return;
          if (isNotJoinedSignal(msg1) && await handleNotJoined()) return;
          if (!isTransientListError(msg1)) throw e1;
          await new Promise((r) => setTimeout(r, 200));
          if (seq !== loadSeq) return;
          res = await invokeOnce();
        }
        if (seq !== loadSeq) return;
        if (isNotJoinedSignal(res) && await handleNotJoined()) return;
        if (res?.cpi && activeClass.value) {
          activeClass.value = {
            ...activeClass.value,
            cpi: String(res.cpi),
            // 记住角色，便于下载走教师 ut
            role: String(res.role || activeClass.value.role || ""),
            ut: String(res.ut || activeClass.value.ut || "s")
          };
          saveLastClass(activeClass.value);
        }
        const list = Array.isArray(res?.resources) ? res.resources : [];
        resources.value = list.map(mapResourceItem);
        bootPhase.value = "ready";
        statusMsg.value = resources.value.length ? tFmt("chaoxing.class.itemsTotal", { n: resources.value.length }) : t("chaoxing.class.noResources");
        error.value = "";
      } catch (e) {
        if (seq !== loadSeq) return;
        const msg = formatErr(e);
        if (isNotJoinedSignal(msg) && await handleNotJoined()) return;
        error.value = isTransientListError(msg) ? `${msg}${t("chaoxing.class.transientListHint")}` : msg;
      } finally {
        if (seq === loadSeq) {
          loadingResources.value = false;
        }
      }
    };
    const openUrl = async (url) => {
      const href = String(url || "").trim();
      if (!href) {
        error.value = t("chaoxing.class.emptyLink");
        return;
      }
      if (/coursedata\/downloadData/i.test(href) || /mooc1\.chaoxing\.com\/coursedata\/download/i.test(href)) {
        throw new Error(t("chaoxing.class.downloadNeedsSession"));
      }
      await openExternal(href);
    };
    const resolveAccess = async (item) => {
      const cls = activeClass.value || preview.value;
      if (!cls) throw new Error(t("chaoxing.class.notJoinedYet"));
      return invokeNative("chaoxing_class_resolve_resource", {
        req: {
          course_id: cls.course_id,
          clazz_id: cls.clazz_id,
          data_id: item.data_id,
          object_id: item.object_id || null,
          cpi: cls.cpi || defaultCpi(),
          file_name: item.name || null,
          file_type: item.file_type || null,
          ...studentPayload()
        }
      });
    };
    const isMobileClient = () => {
      if (typeof navigator === "undefined") return false;
      if (isMobileLike()) return true;
      try {
        return !!(window.Capacitor?.isNativePlatform?.() || window.Capacitor?.getPlatform?.());
      } catch {
        return false;
      }
    };
    const downloadWithSession = async (item, { retries = 2 } = {}) => {
      const cls = activeClass.value || preview.value;
      if (!cls) throw new Error(t("chaoxing.class.notJoinedYet"));
      if (!hasTauri) {
        throw new Error(t("chaoxing.class.downloadClientOnly"));
      }
      let lastErr = null;
      for (let i = 0; i <= retries; i++) {
        try {
          const res = await invokeNative("chaoxing_class_download_resource", {
            req: {
              course_id: cls.course_id,
              clazz_id: cls.clazz_id,
              data_id: item.data_id,
              object_id: item.object_id || null,
              cpi: cls.cpi || defaultCpi(),
              file_name: item.name || null,
              ...studentPayload()
            }
          });
          const path = String(res?.path || "").trim();
          const fileUri = String(res?.file_uri || "").trim();
          const name = String(res?.file_name || item.name || t("chaoxing.class.fileFallback")).trim();
          if (!path) throw new Error(t("chaoxing.class.downloadNoPath"));
          const mobile = !!(res?.mobile_share || isMobileClient());
          if (mobile) {
            showToast(t("chaoxing.class.downloadSharePrompt"), "success", 2400);
            const shareTarget = fileUri || path;
            try {
              const ok = await platformBridge.shareLinkOrFile(
                shareTarget,
                tFmt("chaoxing.class.shareOrSave", { name })
              );
              if (!ok) {
                showToast(tFmt("chaoxing.class.savedSeeFiles", { name }), "info", 4e3);
              }
            } catch (shareErr) {
              console.warn("[chaoxing] share failed:", shareErr);
              showToast(tFmt("chaoxing.class.downloadedShareFail", { name }), "warning", 4200);
            }
          } else {
            showToast(tFmt("chaoxing.class.savedFile", { name }), "success", 3600);
          }
          return res;
        } catch (e) {
          lastErr = e;
          const msg = formatErr(e);
          if (i < retries) {
            showToast(
              tFmt("chaoxing.class.downloadRetry", { i: i + 1, n: retries }),
              "warning",
              2e3
            );
            await new Promise((r) => setTimeout(r, 600 * (i + 1)));
            continue;
          }
          throw new Error(msg || t("chaoxing.class.downloadFailed"));
        }
      }
      throw lastErr || new Error(t("chaoxing.class.downloadFailed"));
    };
    const closePreviewModal = () => {
      showPreviewModal.value = false;
      previewModalUrl.value = "";
      previewModalError.value = "";
      previewModalTitle.value = "";
      previewDownloadUrl.value = "";
      previewModalMode.value = "iframe";
      previewCandidates.value = [];
      previewCandidateIdx.value = 0;
      previewItem.value = null;
      previewOfficialUrl.value = "";
      previewOpenMethods.value = [];
      previewMethodId.value = "";
      showOpenMethodMenu.value = false;
      previewDownloading.value = false;
    };
    const isOfficialPreviewPage = (url) => {
      const u = String(url || "").toLowerCase();
      return u.includes("objectshowpreview") || u.includes("mooc2-resource-index") || u.includes("ananas/modules") || u.includes("preview/v2");
    };
    const isLikelyDirectMedia = (url) => {
      const u = String(url || "").toLowerCase();
      if (!u || isOfficialPreviewPage(u)) return false;
      if (u.startsWith("data:image/")) return true;
      return u.includes(".jpg") || u.includes(".jpeg") || u.includes(".png") || u.includes(".gif") || u.includes(".webp") || u.includes("/star3/") || u.includes(".mp4") || u.includes(".webm") || u.includes(".m4v");
    };
    const applyPreviewCandidate = (idx) => {
      const list = previewCandidates.value;
      if (!list.length) return;
      const i = Math.max(0, Math.min(idx, list.length - 1));
      previewCandidateIdx.value = i;
      previewModalUrl.value = list[i];
    };
    const onPreviewImageError = () => {
      const next = previewCandidateIdx.value + 1;
      if (next < previewCandidates.value.length) {
        applyPreviewCandidate(next);
        return;
      }
      previewModalError.value = previewModalError.value || t("chaoxing.class.imageLoadFail");
    };
    const currentOpenMethod = computed(
      () => previewOpenMethods.value.find((m) => m.id === previewMethodId.value) || null
    );
    const buildOpenMethods = ({ item, mode, cands, officialUrl, downloadUrl }) => {
      const kind = fileKind(item);
      const methods = [];
      const mediaUrls = cands.filter((u) => isLikelyDirectMedia(u));
      const official = officialUrl || cands.find((u) => isOfficialPreviewPage(u)) || (mode === "iframe" ? cands[0] : "") || "";
      if (kind === "image" && mediaUrls.length) {
        methods.push({
          id: "embed-image",
          label: t("chaoxing.class.openImage"),
          desc: t("chaoxing.class.openImageDesc"),
          icon: "image",
          kind: "embed",
          mode: "image",
          url: mediaUrls[0]
        });
      }
      if (kind === "video" && mediaUrls.length) {
        methods.push({
          id: "embed-video",
          label: t("chaoxing.class.openVideo"),
          desc: t("chaoxing.class.openVideoDesc"),
          icon: "movie",
          kind: "embed",
          mode: "video",
          url: mediaUrls[0]
        });
      }
      if (official) {
        methods.push({
          id: "embed-official",
          label: t("chaoxing.class.openOfficial"),
          desc: t("chaoxing.class.openOfficialDesc"),
          icon: "preview",
          kind: "embed",
          mode: "iframe",
          url: official
        });
      } else if (cands[0] && kind !== "image" && kind !== "video") {
        methods.push({
          id: "embed-default",
          label: t("chaoxing.class.openEmbed"),
          desc: t("chaoxing.class.openEmbedDesc"),
          icon: "visibility",
          kind: "embed",
          mode: "iframe",
          url: cands[0]
        });
      }
      const browserTarget = official || mediaUrls[0] || cands[0] || "";
      if (browserTarget && !String(browserTarget).startsWith("data:")) {
        methods.push({
          id: "browser-preview",
          label: t("chaoxing.class.openBrowser"),
          desc: t("chaoxing.class.openBrowserDesc"),
          icon: "open_in_browser",
          kind: "browser",
          url: browserTarget
        });
      }
      if (downloadUrl) {
        methods.push({
          id: "download",
          label: t("chaoxing.class.openDownload"),
          desc: t("chaoxing.class.openDownloadDesc"),
          icon: "download",
          kind: "download",
          url: downloadUrl
        });
      }
      return methods;
    };
    const applyOpenMethod = async (method, { externalOnly = false } = {}) => {
      if (!method) return;
      showOpenMethodMenu.value = false;
      previewMethodId.value = method.id;
      previewModalError.value = "";
      if (method.kind === "download") {
        previewDownloading.value = true;
        try {
          const item = previewItem.value || {
            data_id: previewItem.value?.data_id,
            object_id: previewItem.value?.object_id,
            name: previewModalTitle.value
          };
          if (!previewItem.value?.data_id) {
            const url = method.url || previewDownloadUrl.value;
            if (url && !/downloadData/i.test(url)) {
              await openUrl(url);
              return;
            }
            throw new Error(t("chaoxing.class.noAuthDownload"));
          }
          await downloadWithSession(previewItem.value);
        } catch (e) {
          previewModalError.value = formatErr(e);
        } finally {
          previewDownloading.value = false;
        }
        return;
      }
      if (method.kind === "browser" || externalOnly) {
        const url = method.url || previewModalUrl.value;
        if (!url || String(url).startsWith("data:")) {
          previewModalError.value = t("chaoxing.class.browserBlocked");
          return;
        }
        await openUrl(url);
        return;
      }
      if (method.mode) previewModalMode.value = method.mode;
      if (method.url) {
        previewModalUrl.value = method.url;
        if (method.mode === "image") {
          const rest = previewCandidates.value.filter((u) => u !== method.url);
          previewCandidates.value = [method.url, ...rest];
          previewCandidateIdx.value = 0;
        }
      }
    };
    const toggleOpenMethodMenu = () => {
      if (previewModalLoading.value) return;
      showOpenMethodMenu.value = !showOpenMethodMenu.value;
    };
    const handlePreviewDownload = async () => {
      if (!previewItem.value?.data_id) {
        previewModalError.value = t("chaoxing.class.noDownloadItem");
        return;
      }
      previewDownloading.value = true;
      try {
        await downloadWithSession(previewItem.value);
      } catch (e) {
        previewModalError.value = formatErr(e);
      } finally {
        previewDownloading.value = false;
      }
    };
    const handleBrowserOpenCurrent = async () => {
      const m = previewOpenMethods.value.find((x) => x.kind === "browser") || previewOpenMethods.value.find((x) => x.kind === "embed" && x.url && !String(x.url).startsWith("data:"));
      if (m) {
        await applyOpenMethod({ ...m, kind: "browser" });
        return;
      }
      if (previewModalUrl.value && !String(previewModalUrl.value).startsWith("data:")) {
        await openUrl(previewModalUrl.value);
      } else {
        previewModalError.value = t("chaoxing.class.noBrowserLink");
      }
    };
    const handleOpenFolder = async (item) => {
      if (!item?.is_folder) return;
      const kind = item.folder_kind || (item.file_type === "tch-courseware" ? "tch-courseware" : "afolder");
      folderStack.value = [
        ...folderStack.value,
        {
          name: item.name || t("chaoxing.class.folder"),
          parent_data_id: item.data_id || "0",
          folder_kind: kind,
          data_name: item.name || "",
          parent_chain: folderStack.value.map((f) => f.parent_data_id).filter((id) => id && id !== "0").join(",")
        }
      ];
      filterChip.value = "all";
      await loadResources();
    };
    const handleBreadcrumb = async (index) => {
      if (index <= 0) {
        folderStack.value = [];
      } else {
        folderStack.value = folderStack.value.slice(0, index);
      }
      filterChip.value = "all";
      await loadResources();
    };
    const handlePreviewResource = async (item) => {
      if (item.is_folder) {
        await handleOpenFolder(item);
        return;
      }
      error.value = "";
      actingId.value = `p-${item.data_id}`;
      previewItem.value = item;
      previewModalTitle.value = item.name;
      previewModalLoading.value = true;
      previewModalError.value = "";
      previewModalUrl.value = "";
      previewModalOfficial.value = false;
      previewModalMode.value = fileKind(item) === "image" ? "image" : "iframe";
      previewCandidates.value = [];
      previewCandidateIdx.value = 0;
      previewDownloadUrl.value = item.download_url || "";
      previewOfficialUrl.value = "";
      previewOpenMethods.value = [];
      previewMethodId.value = "";
      showOpenMethodMenu.value = false;
      showPreviewModal.value = true;
      try {
        const res = await resolveAccess(item);
        const url = String(res?.preview_url || "").trim();
        const official = !!(res?.official_preview ?? res?.embeddable);
        const mode = String(res?.preview_mode || previewModalMode.value || "iframe").toLowerCase();
        const cands = Array.isArray(res?.preview_candidates) ? res.preview_candidates.map((u) => String(u || "").trim()).filter(Boolean) : [];
        if (url && !cands.includes(url)) cands.unshift(url);
        if ((mode === "image" || fileKind(item) === "image") && item.object_id) {
          for (const u of [
            item.thumbnail_url,
            `https://p.ananas.chaoxing.com/star3/400_400c/${item.object_id}`,
            `https://p.ananas.chaoxing.com/star3/origin/${item.object_id}`
          ]) {
            const s = String(u || "").trim();
            if (s && !cands.includes(s)) cands.push(s);
          }
        }
        const dl = String(res?.download_url || item.download_url || "");
        previewDownloadUrl.value = dl;
        previewModalOfficial.value = official;
        previewModalMode.value = mode === "image" || mode === "video" ? mode : "iframe";
        previewCandidates.value = cands;
        const officialFromCands = cands.find((u) => isOfficialPreviewPage(u)) || "";
        previewOfficialUrl.value = officialFromCands || (isOfficialPreviewPage(url) ? url : "") || "";
        const methods = buildOpenMethods({
          item,
          mode: previewModalMode.value,
          cands,
          officialUrl: previewOfficialUrl.value,
          downloadUrl: dl
        });
        previewOpenMethods.value = methods;
        if (!cands.length && !url && !dl) throw new Error(t("chaoxing.class.noPreviewUrl"));
        const defaultEmbed = methods.find((m) => m.kind === "embed") || methods.find((m) => m.kind === "download");
        if (defaultEmbed?.kind === "embed") {
          await applyOpenMethod(defaultEmbed);
        } else if (cands.length) {
          applyPreviewCandidate(0);
          previewMethodId.value = methods[0]?.id || "";
        } else if (dl) {
          previewMethodId.value = methods.find((m) => m.kind === "download")?.id || "";
          previewModalError.value = t("chaoxing.class.noEmbedPreview");
        }
        if (previewModalUrl.value.includes("star3/origin") && mode !== "image") {
          previewModalError.value = t("chaoxing.class.unsignedPreviewHint");
        }
      } catch (e) {
        previewModalError.value = formatErr(e);
        if (previewDownloadUrl.value || item.download_url) {
          previewOpenMethods.value = buildOpenMethods({
            item,
            mode: "iframe",
            cands: [],
            officialUrl: "",
            downloadUrl: previewDownloadUrl.value || item.download_url
          });
        }
      } finally {
        previewModalLoading.value = false;
        actingId.value = "";
      }
    };
    const handleDownloadResource = async (item) => {
      error.value = "";
      actingId.value = `d-${item.data_id}`;
      try {
        await downloadWithSession(item);
      } catch (e) {
        error.value = formatErr(e);
      } finally {
        actingId.value = "";
      }
    };
    const handleRowClick = async (item) => {
      if (item.is_folder) {
        await handleOpenFolder(item);
      } else {
        await handlePreviewResource(item);
      }
    };
    const fileKind = (item) => {
      if (item.is_folder || item.folder_kind === "tch-courseware" || item.folder_kind === "afolder") {
        return item.folder_kind === "tch-courseware" ? "courseware" : "folder";
      }
      const t2 = `${item.file_type} ${item.name}`.toLowerCase();
      if (/\b(mp4|mov|avi|mkv|webm)\b/.test(t2) || t2.endsWith(".mp4")) return "video";
      if (/\b(jpg|jpeg|png|gif|webp|bmp)\b/.test(t2)) return "image";
      if (/\bpdf\b/.test(t2)) return "pdf";
      if (/\b(ppt|pptx)\b/.test(t2)) return "ppt";
      if (/\b(doc|docx)\b/.test(t2)) return "doc";
      if (/\b(xls|xlsx)\b/.test(t2)) return "xls";
      if (/\b(zip|rar|7z)\b/.test(t2)) return "zip";
      return "file";
    };
    const kindMeta = computed(() => ({
      folder: { icon: "folder", label: t("chaoxing.class.folder"), chip: "folder" },
      courseware: { icon: "folder_special", label: t("chaoxing.class.courseware"), chip: "folder" },
      video: { icon: "movie", label: t("chaoxing.class.video"), chip: "video" },
      image: { icon: "image", label: t("chaoxing.class.image"), chip: "image" },
      pdf: { icon: "picture_as_pdf", label: "PDF", chip: "doc" },
      ppt: { icon: "slideshow", label: t("chaoxing.class.slides"), chip: "doc" },
      doc: { icon: "description", label: t("chaoxing.class.document"), chip: "doc" },
      xls: { icon: "table_chart", label: t("chaoxing.class.spreadsheet"), chip: "doc" },
      zip: { icon: "folder_zip", label: t("chaoxing.class.archive"), chip: "all" },
      file: { icon: "draft", label: t("chaoxing.class.fileFallback"), chip: "all" }
    }));
    const filterChips = computed(() => [
      { id: "all", label: t("chaoxing.class.filterAll") },
      { id: "folder", label: t("chaoxing.class.filterFolder") },
      { id: "image", label: t("chaoxing.class.filterImage") },
      { id: "video", label: t("chaoxing.class.filterVideo") },
      { id: "doc", label: t("chaoxing.class.filterDoc") }
    ]);
    const filteredResources = computed(() => {
      const list = resources.value;
      const chip = filterChip.value;
      if (chip === "all") return list;
      return list.filter((item) => {
        const k = fileKind(item);
        const meta = kindMeta.value[k] || kindMeta.value.file;
        return meta.chip === chip;
      });
    });
    const metaLine = (item) => {
      const parts = [];
      if (item.created_at) parts.push(item.created_at);
      if (item.size_label && item.size_label !== "-") parts.push(item.size_label);
      return parts.join(" · ");
    };
    const boot = async () => {
      loadingBoot.value = true;
      error.value = "";
      needsRejoin.value = false;
      try {
        const remote = await fetchRemoteConfig({ force: false }).catch(() => null);
        classConfig.value = getChaoxingClassConfig(remote || void 0);
      } catch {
        classConfig.value = getChaoxingClassConfig();
      }
      loadDeclined();
      const saved = loadLastClass();
      if (saved) {
        activeClass.value = saved;
        bootPhase.value = "ready";
        loadingBoot.value = false;
        ssoHint.value = t("chaoxing.class.bootSso");
        statusMsg.value = t("chaoxing.class.syncMaterials");
        const ssoPromise = ensureSso();
        try {
          await loadResources({ rejoinOnNotJoined: true });
        } catch (e) {
          const msg = formatErr(e);
          if (isNotJoinedSignal(msg)) {
            await enterNotJoinedState({
              openDialog: true,
              rejoin: true,
              reason: t("chaoxing.class.rejoinMsg")
            });
          } else {
            error.value = msg;
          }
        }
        const ssoOk2 = await ssoPromise;
        if (!ssoOk2 && resources.value.length === 0) {
          bootPhase.value = "error";
          if (!error.value && ssoHint.value) {
            error.value = ssoHint.value;
          }
        }
        return;
      }
      bootPhase.value = "sso";
      ssoHint.value = t("chaoxing.class.bootSso");
      const ssoOk = await ensureSso();
      if (!ssoOk) {
        bootPhase.value = "error";
        loadingBoot.value = false;
        return;
      }
      activeClass.value = null;
      resources.value = [];
      error.value = "";
      needsRejoin.value = false;
      bootPhase.value = "preview";
      try {
        const p = await fetchPreview();
        preview.value = p;
        activeClass.value = { ...p };
        bootPhase.value = "ready";
        loadingBoot.value = false;
        await loadResources({ rejoinOnNotJoined: false });
        if (isJoined.value) {
          saveLastClass(activeClass.value);
          statusMsg.value = resources.value.length ? tFmt("chaoxing.class.itemsTotal", { n: resources.value.length }) : t("chaoxing.class.accessibleClass");
          return;
        }
        preview.value = p;
        if (!joinDeclined.value) {
          showJoinDialog.value = true;
        }
        bootPhase.value = "ready";
      } catch (e) {
        activeClass.value = null;
        preview.value = { ...classMeta.value, invite_code: inviteCode.value };
        bootPhase.value = "ready";
        loadingBoot.value = false;
        if (!joinDeclined.value) {
          showJoinDialog.value = true;
        }
        statusMsg.value = formatErr(e);
      }
    };
    onMounted(() => {
      void boot();
    });
    const __returned__ = { t, tFmt, LAST_CLASS_KEY, JOIN_DECLINED_KEY, classConfig, inviteCode, classMeta, defaultCpi, props, emit, loadingSso, loadingBoot, loadingJoin, loadingResources, actingId, ssoReady, ssoHint, error, statusMsg, preview, resources, activeClass, showJoinDialog, joinDeclined, needsRejoin, bootPhase, folderStack, showPreviewModal, previewModalTitle, previewModalUrl, previewModalLoading, previewModalError, previewModalOfficial, previewDownloadUrl, previewModalMode, previewCandidates, previewCandidateIdx, previewItem, previewOfficialUrl, previewOpenMethods, previewMethodId, showOpenMethodMenu, previewDownloading, filterChip, thumbFailed, get loadSeq() {
      return loadSeq;
    }, set loadSeq(v) {
      loadSeq = v;
    }, get suppressNotJoinedUntil() {
      return suppressNotJoinedUntil;
    }, set suppressNotJoinedUntil(v) {
      suppressNotJoinedUntil = v;
    }, hasTauri, courseTitle, teacherName, coverUrl, resourceCount, isJoined, formatErr, studentPayload, ssoPayload, loadLastClass, clearLastClassStorageOnly, clearLastClass, saveLastClass, loadDeclined, markDeclined, enterNotJoinedState, isNotJoinedSignal, ensureSso, fetchPreview, sleepMs, refreshAfterJoin, handleJoinConfirm, reopenJoinDialog, currentFolder, breadcrumbLabels, mapResourceItem, thumbKey, onThumbError, showThumb, isTransientListError, loadResources, openUrl, resolveAccess, isMobileClient, downloadWithSession, closePreviewModal, isOfficialPreviewPage, isLikelyDirectMedia, applyPreviewCandidate, onPreviewImageError, currentOpenMethod, buildOpenMethods, applyOpenMethod, toggleOpenMethodMenu, handlePreviewDownload, handleBrowserOpenCurrent, handleOpenFolder, handleBreadcrumb, handlePreviewResource, handleDownloadResource, handleRowClick, fileKind, kindMeta, filterChips, filteredResources, metaLine, boot, computed, onMounted, ref, get invokeNative() {
      return invokeNative;
    }, get isTauriRuntime() {
      return isTauriRuntime;
    }, get platformBridge() {
      return platformBridge;
    }, get isMobileLike() {
      return isMobileLike;
    }, get openExternal() {
      return openExternal;
    }, get fetchRemoteConfig() {
      return fetchRemoteConfig;
    }, get getChaoxingClassConfig() {
      return getChaoxingClassConfig;
    }, get loadPortalRememberedPassword() {
      return loadPortalRememberedPassword;
    }, get pushDebugLog() {
      return pushDebugLog;
    }, get showToast() {
      return showToast;
    }, get useLocale() {
      return useLocale;
    }, get TPageHeader() {
      return _sfc_main$1;
    } };
    Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
    return __returned__;
  }
};
const ChaoxingClassView = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", render], ["__scopeId", "data-v-9ab045a6"]]);
export {
  ChaoxingClassView as default
};
