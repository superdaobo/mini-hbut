# 空教室查询模块逻辑 (classroom.rs)

## 1. 模块概述
`classroom.rs` 负责查询学校的空闲教室资源。
该功能对应 Python 后端未完全实现或依赖抓包分析的部分。
目前在 Rust 端主要是结构定位和基础查询逻辑。

## 2. 核心结构
*   **Classroom**: 教室实体，包含名称、楼栋、容量、校区等信息。
*   **Building**: 教学楼实体。

## 3. 查询逻辑 (Todo)
目前该模块的功能尚未完全移植，主要逻辑涉及：
1.  查询可用教学楼列表。
2.  根据周次、星期、节次查询空教室。
3.  API: 猜测为 `/admin/kbcx/kbcx/queryKbcx` 或类似接口。

## 4. 现状
在 `http_client.rs` 中，空教室查询目前是 Stub (返回 "Not implemented")。
本模块文件可能包含之前尝试实现的逻辑或数据结构定义。
